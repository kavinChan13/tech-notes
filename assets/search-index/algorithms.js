/* AUTO-GENERATED by scripts/build_search_index.py — do not edit by hand. */
(window.TN_SEARCH_SHARD=window.TN_SEARCH_SHARD||{})["algorithms"]=[{"u":"algorithms/algorithms_study_path.html","t":"算法与数据结构学习路径","c":"算法与数据结构","s":[{"a":"","h":"","x":"算法与数据结构学习路径 · Study Path Kavin Tech Notes ← 主页 目录 ◐ 算法 · 学习路径 P1 · 分析基础 P2 · 排序与查找 P3 · 线性结构技巧 P4 · 核心数据结构 P5 · 树与区间结构 P6 · 图论 P7 · 算法范式与字符串 Algorithms · Study Path 算法与数据结构学习路径 P1 分析基础 ：先把「怎么衡量快慢」和「怎么证明算法对」这两件事立起来——复杂度记号、摊销分析、主定理，再是递归与分治这个最重要的算法范式 → P2 排序与查找 ：用排序和二分把 P1 的工具全部用一遍，并深入到工业级实现 → P3 线性结构技巧 ：双指针、滑动窗口、单调栈、前缀和——这批技巧的共同点是「靠一个论证丢掉一半候选」→ P4 核心数据结构 ：哈希表与堆的内核剖析，以及它们在生产系统里的选型判据 → P5 树与区间结构 ：从「用层级换对数」到平衡树四条路，再到并查集与三种区间结构各自的 前提 → P6 图论 ：图的表示如何决定复杂度、拓扑排序的两副面孔、松弛与割性质如何统一六个算法 → P7 算法范式与字符串 ：DP 的状态设计方法论、贪心的证明与失效判据、字符串算法共用的「复用已知区间」框架。适合有 C++ 基础、想把算法从「刷题」升级到「工程」的读者。 ~52 h 180 道面试题 代码 C++17/20"},{"a":"ch1","h":"","x":"P1 分析基础（~7 h） 先有度量与证明工具，再谈具体算法 # 内容 时间 1 复杂度与分析 3.5 h 2 递归与分治 3.5 h ✅ 完成标志 能说清 O / Θ / Ω 的区别，以及「快排是 O(n log n)」为什么不算完整回答 能用聚合法和势能法两种方式证明 vector::push_back 的摊销 O(1) 拿到一个递推式能立刻判断落在主定理哪一情形，并说出该情形里优化该往哪使劲 能解释为什么 std::list 的 O(1) 插入实测输给 std::vector 的 O(n) 能说清在 C++ 里为什么不该依赖尾调用消除，以及深递归的两条正确出路 P2 排序与查找（~7 h） 把 P1 的工具用一遍，并下探到工业级实现 # 内容 时间 3 排序全景 4 h 4 二分查找 3 h ✅ 完成标志 能用决策树模型证明比较排序的 Ω(n log n) 下界，并说清这个下界约束不到谁 能逐段讲出 libstdc++ std::sort 的三段结构，以及三者短板为何互补 知道比较器违反严格弱序为什么会导致越界而不只是顺序错，以及怎么在 CI 里挡住 能写下二分的循环不变式，并用它推出四种边界写法而不是靠试 看到「最小化最大值」能立刻想到二分答案，并主动去证明 feasible 的单调性 P3 线性结构技巧（~6.5 h） 共同点：靠一个论证丢掉一半候选 # 内容 时间 5 双指针与滑动窗口 3 h 6 单调栈与前缀和 3.5 h ✅ 完成标志 写完双指针解法后，能立刻指出那个「单调变化且有界」的势能量，从而确认它真是 O(n) 能说清滑动窗口的隐藏前提（数组是否全为正），以及不满足时该换什么工具 「求最长」与「求最短」的收缩条件和统计位置不再靠试，而是先判断题型再落笔 能解释单调栈弹出元素为什么安全（完全支配），以及哨兵技巧消掉了哪两类边界判断 知道 max 没有逆元，因此区间最值不能用前缀和、窗口最值不能加一个减一个 P4 核心数据结构（~6.5 h） 从内核剖析到生产选型 # 内容 时间 7 哈希表内核 3.5 h 8 堆与 Top-K 3 h ✅ 完成标志 能把「哈希表是 O(1)」这句话补完整，并说出三个限定词各自对应的真实故障 能解释标准的哪几条要求把 unordered_map 锁死成拉链法，以及 Swiss Table 放弃了什么 知道 Hash-DoS 的原理，能说出 C++ 里三种修法以及为什么限制参数个数也必须做 能完整证明自底向上建堆是 Θ(n)，并说清它推不出「堆排序也是 O(n)」 拿到「Top-K」「流式中位数」「定时器」三类需求，能各自选对结构并说出不选其他方案的理由 看到无界队列会主动警觉——知道它把背压问题藏到 OOM 才暴露 P5 树与区间结构（~7 h） 每个漂亮的复杂度背后都有一个明确的前提 # 内容 时间 9 树与平衡树 3.5 h 10 并查集与区间结构 3.5 h ✅ 完成标志 选树遍历序时不再靠记名字，而是问「信息是从上往下传还是从下往上收」 能推出红黑树的 2 log(n+1) 上界，并说清「删除最多 3 次旋转」为什么让标准库选了它 能算出为什么 B+ 树把 30 次磁盘 I/O 降到 3~4 次，以及范围扫描为何是它胜过 B 树的关键 知道 Redis 选跳表和 RocksDB 选跳表是 两个不同的理由 能用势能法说清并查集 α(n) 的来源，并解释为什么可撤销就必须放弃路径压缩 拿到区间问题先问两句：「修改与查询是否交错」「这个运算可逆吗 / 可重叠合并吗」 P6 图论（~7 h） 难的不是算法，是「这是个图问题」和「图怎么存」 # 内容 时间 11 图遍历与拓扑排序 3.5 h 12 最短路与最小生成树 3.5 h ✅ 完成标志 能说清为什么有向图环检测需要三种颜色，以及只用 visited 会在哪个反例上误报 看到「某些移动免费、某些花 1」能立刻想到 0-1 BFS，而不是上 Dijkstra 拿到「依赖 / 先后」立刻想到拓扑排序，并知道按层剥离顺带给出关键路径长度 能指出 Dijkstra 正确性证明的 哪一步 用到了非负性，从而解释负权为何失效 知道「所有边权加一个大常数」为什么救不了负权图 能用割性质同时解释 Kruskal 和 Prim，并指出 Prim 与 Dijkstra 代码里那一处关键差别 拿到最短路问题的第一个动作是问「这图是 DAG 吗」 P7 算法范式与字符串（~11 h） 状态设计、正确性证明、以及「复用已比较的信息」 # 内容 时间 13 动态规划 4 h 14 贪心与回溯 3.5 h 15 字符串匹配 3.5 h ~52 h ✅ 完成标志 拿到 DP 题能按五步走，并从 n 的范围反推出状态大概有几维 能解释背包的两组循环顺序各自控制什么（能否重复选 / 组合还是排列） 怀疑状态维度不够时，会去构造「当前状态相同但历史不同」的反例来验证 能说出单调队列优化与斜率优化的那一句判据（候选优劣是否依赖 i） 用贪心前会先写下交换论证的第三步，并用暴力对拍验证 能说清排列去重条件里那个感叹号为什么不能去掉 理解 border 之后不再需要背 KMP，并能用它顺手求出最小周期 知道 ReDoS 和 Hash-DoS、递归爆栈、zip bomb 是同一类问题的四个化身 ← 目录 C++ 路径 性能与调试路径 全站路径"}]},{"u":"algorithms/binary_search_patterns.html","t":"二分查找","c":"算法与数据结构","s":[{"a":"","h":"","x":"二分查找 · 循环不变式证明 / 四种边界模板 / lower_bound 实现 / 二分答案 / 旋转数组 / cache 友好布局 Kavin Tech Notes ← 主页 目录 30 min ◐ 二分查找 本质不是「找数」 与其他章节的关系 为什么大家都写错 mid 溢出 死循环与少一次循环 用不变式一次写对 半开区间 + 不变式 左闭右开 vs 闭区间 左 / 右边界 一块写完 lower_bound 四种边界互相表达 变体对照 STL 与复杂度陷阱 比较次数 ≠ 运行时间 全家桶对照 二分答案 求最优 → 判定可行 最短判定函数 单调性的证明义务 找回单调性 旋转有序数组 山峰数组 浮点二分 工程里的二分 git bisect B+ 树页内二分 cache 友好的布局 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Binary Search · 2026 二分查找 二分是最简单的算法，也是 最多人写不对 的算法。原因是大家把它记成了「在有序数组里找一个数」这个特例，于是每换一种边界要求就得重新猜一遍 < 还是 <= 。这一篇换一个视角：二分的本质是 在单调谓词上找分界点 ；理解这一点后，一个模板就能覆盖全部边界，而且每次都能 证明 它对。 核心难度 ★★★☆☆ 面试出现率 极高 阅读时长 ~30 min 前置 复杂度与分析 循环不变式证明 四种边界统一模板 lower_bound 实现 二分答案 旋转数组 cache 友好布局"},{"a":"s1","h":"本质不是「找数」","x":"01 本质不是「找数」 而是在单调谓词上找那条分界线 教科书把二分定义成「在有序数组中查找目标值」。太窄：那只是 P(i) = (a[i] >= target) 的特例。工作里直接用 std::lower_bound ；面试里常遇到 没有数组 的二分答案。前提只有一句—— 能证明谓词单调 。 一般形式 ：区间 [lo, hi) 上有单调布尔谓词 P （存在分界点，之前全假、之后全真），O(log n) 次求值找到 第一个真 。数组不必存在，答案不必是下标。 有序才能二分：不是「数组里有数」，而是谓词必须前假后真 无序 7 2 9 1 5 P=a[i]≥5 → T F T F T，砍哪半都可能丢答案 有序 1 2 5 7 9 F F T T T → 分界唯一，砍一半是安全的 git bisect、二分答案、山峰数组：都没有「排好序的目标值」，但各自构造出了单调 P。证不出单调 → 不要硬套二分。 无序时谓词乱跳，任何一半都可能藏着答案；排好序（或构造出单调导出量）之后，才存在唯一分界点。 谓词 P 在区间上单调 ⟹ 存在唯一分界点 P = false P = true 答案：第一个 true lo hi 这个视角带来什么 ① 一个模板覆盖全部边界（§4） ② 数组不必存在 → 二分答案（§6） 写二分时唯一要问的问题 「我的 P 是什么，它真的单调吗？」 证不出单调性 → 二分不适用 把二分理解成「找单调谓词的分界点」，边界问题就退化成「谓词怎么写」。证不出单调性，就不要硬套。 写二分时只问两句： 我的 P 是什么？它真的单调吗？ 有序数组找目标只是 P(i)=a[i]≥target。git bisect、二分答案、山峰数组都没有「排好序的目标值」，但各自构造出了单调 P。 二分像 git bisect ：500 个提交里定位引入 bug 的那一次，谓词是「这个提交上 bug 还在吗」——一旦引入就一直为真，约 9 次测试即可。没有有序数组，也没有目标值。若测试时好时坏（flaky），谓词不单调，bisect 不报错，只安静地骗你 。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 复杂度与分析 —— O(log n) 的来源，以及为什么 log n 在工程规模内是小常数 排序全景 —— 二分的前置条件由排序提供；决策树论证也给出了二分的下界 递归与分治 —— 二分是「只有一个子问题」的退化分治 STL 算法库 —— lower_bound / upper_bound / equal_range / binary_search 存储与数据架构 —— B+ 树、跳表、LSM 里二分的真实用途"},{"a":"s2","h":"为什么大家都写错","x":"02 为什么大家都写错 三行代码里有四个能出错的地方 循环条件、mid 取整方向、lo/hi 赋值、区间开闭——任意一对拧了，轻则漏解，重则死循环或越界。下面两个陷阱（溢出、少一次循环）加上「区间没缩小」几乎覆盖全部现场事故。"},{"a":"s2-1","h":"2.1 mid 溢出","x":"2.1 mid 溢出 2006 年 Joshua Bloch 披露：JDK 的 Arrays.binarySearch 带着 (lo + hi) / 2 这个 bug 活了九年，源头是《Programming Pearls》。有符号加法溢出是 UB，迭代器则根本不能相加。 陷阱：整数溢出 mid ❌ mid = (lo + hi) / 2 lo = hi = 2×10⁹ lo + hi = 4×10⁹ > INT_MAX ≈ 2.1×10⁹ 有符号溢出 = UB，常得到负数 接着 a[负数] 越界读 ✅ mid = lo + (hi − lo) / 2 差值先算，结果落在 [lo, hi) 无符号：((unsigned)lo + hi) >> 1 指针 / 迭代器只能用减法版 ptr1 + ptr2 在 C++ 里不合法 mid 向下取整（偏左）：当 hi = lo+1 时 mid = lo。因此 lo = mid 可能不动；必须 lo = mid+1，hi = mid 才安全。 负数下标时 /2 向零取整、>>1 向下取整（−3/2 == −1，−3>>1 == −2），不要混用。 JDK 这个 bug 的根因就是 lo + hi 。STL 写成 lo + (hi - lo) / 2 ，是因为迭代器只能相减。 对指针和迭代器， lo + (hi - lo) / 2 不是防御性编程而是必须： ptr1 + ptr2 在 C++ 里不合法。这也是 STL 实现长这样的原因。"},{"a":"s2-2","h":"2.2 死循环与少一次循环","x":"2.2 死循环与少一次循环 死循环几乎总是「这一轮区间没缩小」。少一次循环则是区间约定和 while 条件拧在一起，退出时还剩一个没看过的元素。 写法 为什么坏 何时爆 while (lo < hi) + lo = mid mid 偏左， hi == lo+1 时 mid==lo，lo 不动 剩 2 个元素 while (lo <= hi) + hi = mid 同理，hi 可能不动 剩 1–2 个 半开初始化 + 闭区间循环 hi = n 却写 lo <= hi → 读 a[n] 目标大于全部 ✅ 正确：[lo, hi) 半开 + mid 向下取整 + lo = mid+1 / hi = mid lo=0 mid=3 hi=8 lo=0 mid=1 hi=4 lo=0 m=0 hi=2 lo=hi 区间长度 8 → 4 → 2 → 1 → 0，每轮至少减半 ⇒ 必然终止 ❌ 错误：lo = mid（不加 1） lo=0 mid=0 hi=2 lo=0 mid=0 hi=2 永远不变 → 挂死 规律：mid 向下取整时，「lo = mid」不安全（可能不动）；「hi = mid」安全（hi > mid 恒成立） 只要保证每轮区间长度严格减少，终止性就是自动的。所有死循环都是这一条被破坏。mid 偏左时 lo=mid 可能不动，必须 +1。 陷阱：少一次循环（区间约定和 while 拧了） ❌ 闭区间 [lo, hi] + while (lo < hi) 目标在 a[3]，循环收到 lo = hi = 3 lo < hi 为假 → 直接退出 a[3] 从没被比较 → 漏解 ✅ 半开 [lo, hi) + while (lo < hi) 退出时区间为空，lo == hi 不变式已经保证：i < lo 全假、i ≥ lo 全真 lo 就是答案，不用再补一次循环 闭区间必须写 while (lo ≤ hi)，且命中时 return；半开则退出即答案。一份代码只用一种约定。 漏解往往不是「少写了一行比较」，而是半开/闭区间和循环条件不配对。 mid 向下取整时： hi = mid 安全（ mid < hi ）， lo = mid 不安全，必须 lo = mid + 1 。若逻辑真要 lo = mid ，改成向上取整 lo + (hi - lo + 1) / 2 。 取整方向和赋值必须配对。"},{"a":"s3","h":"用不变式一次写对","x":"03 用不变式一次写对 不靠试错，靠证明 写二分最稳的办法不是跑几个用例猜 < 还是 <= ，而是先写下循环不变式，让每一次收缩都保持它。半开区间让「空」只有一种表示，终止条件、没找到、长度计算都从同一套符号推出来。"},{"a":"s3-1","h":"3.1 半开区间 + 不变式","x":"3.1 半开区间 + 不变式 下面这一块是全文唯一需要记住的循环。不变式写在注释里：所有边界都从它推出来，不必猜 < 还是 <= 。 // [0, n) 上找第一个使 P 为真的下标；全假则返回 n // 不变式：∀ i < lo, ¬P(i)；∀ i ≥ hi, P(i)（若 i < n）→ 答案 ∈ [lo, hi] int lo = 0 , hi = n; while (lo < hi) { int mid = lo + (hi - lo) / 2 ; // 避免溢出；mid 偏左，lo ≤ mid < hi if ( P (mid)) hi = mid; // 答案 ≤ mid else lo = mid + 1 ; // 答案 > mid } return lo; // lo == hi，区间空 不变量区间：两道封条往中间挪，外面的已经验完 [0, lo) 全假 已确认 ¬P [lo, hi) 待查 答案在这里 [hi, n) 全真 已确认 P lo hi n P(mid) 真 → hi = mid（右封条左移）；假 → lo = mid+1（左封条右移）。两分支都严格缩短。 退出 lo == hi：左侧全假、右侧全真 → lo 就是第一个真。初始 lo=0、hi=n 是空真。 封条外的格子不再回头查。正确性就是三步：初始空真、每轮保持、退出时空区间。 ① 初始 lo=0, hi=n。∀ i<0 假、∀ i≥n 真，都是空真。 ② 保持 mid 满足 lo ≤ mid < hi。 P(mid) 真 → 单调性给出 ∀ i≥mid 皆真 → hi=mid。 P(mid) 假 → ∀ i≤mid 皆假 → lo=mid+1。 ③ 退出 lo==hi，不变式变成「i<lo 全假、i≥lo 全真」→ lo 就是第一个真。 ④ 终止 两分支都严格缩短区间 → 至多 ⌈log₂ n⌉ 轮。"},{"a":"s3-2","h":"3.2 左闭右开 vs 闭区间","x":"3.2 左闭右开 vs 闭区间 左闭右开 [lo, hi) vs 闭区间 [lo, hi] [lo, hi) 半开（推荐） 空区间：lo == hi（一种） 长度：hi − lo，不用 ±1 没找到：hi = n，不是 −1 与 STL [first, last) 一致 [lo, hi] 闭区间 空区间：lo > hi（多一种状态） 长度：hi − lo + 1 循环必须 lo ≤ hi，命中要 return hi = n−1，越界差一格 STL 全部用半开，不是审美：空区间有唯一表示，能消掉一整类 off-by-one。 不变式像装修封条：「外面已经验收」。房子分成已确认全假、待查、已确认全真三块，每次只拆中间一间再把封条往里挪。待查区变空时，两道封条之间就是答案——不必回头复查任何一间。"},{"a":"s4","h":"左 / 右边界","x":"04 左 / 右边界 只写一个 lower_bound，别写三份几乎一样的循环"},{"a":"s4-1","h":"4.1 一块写完 lower_bound","x":"4.1 一块写完 lower_bound 左边界 = 第一个 ≥ x，右边界 = 第一个 > x。两者夹出的 [L, R) 就是全部等于 x 的元素。「最后一个满足 Q」=「第一个满足 ¬Q」减一，不要另写一套 hi = mid - 1 的循环。 要找什么 谓词 P(i) 返回 第一个 ≥ x（lower_bound） a[i] >= x 下标或 n 第一个 > x（upper_bound） a[i] > x 下标或 n 最后一个 < x a[i] >= x 返回值 − 1（可能 −1） 最后一个 ≤ x a[i] > x 返回值 − 1（可能 −1） // 同一套 §3 模板，只换谓词 int L = lower_bound_by (n, [&]( int i){ return a[i] >= x; }); // 左 = 第一个 ≥ x int R = lower_bound_by (n, [&]( int i){ return a[i] > x; }); // 右 = 第一个 > x // 出现次数 = R - L；存在 ⟺ L < n && a[L] == x // 最后一个 < x = L-1；最后一个 ≤ x = R-1（先判 > −1） 「最后一个」在全体 ≥ x 时得到 −1。忘了检查就是 a[-1] 。不是逻辑更难，只是多了一个失败返回值。"},{"a":"s4-2","h":"4.2 四种边界互相表达","x":"4.2 四种边界互相表达 这四个位置不是四套算法： lower_bound / upper_bound 是「第一个」，各减一得到「最后一个」。 数组： 1 3 5 5 5 7 9 0 1 2 3 4 5 6 x = 5 lower_bound = 2 第一个 ≥ 5 upper_bound = 5 第一个 > 5 last_lt = 1 = lower_bound − 1 last_le = 4 = upper_bound − 1 equal_range = [2, 5) = [lower_bound, upper_bound) 出现次数 = 5 − 2 = 3 四个边界两两成对： lower_bound / upper_bound 是「第一个」，各减一得到对应的「最后一个」。 一句话 ： [lower_bound, upper_bound) = 全部等于 x。插入点在 L（前插，保持相对顺序时用 R 后插）。出现次数就是 R−L；L==R 表示不存在。"},{"a":"s4-3","h":"4.3 变体对照","x":"4.3 变体对照 每种变体只记「问什么 → 不变量 → 收缩哪边」。循环形状与 §3 相同，不要为每道题再写一份。 问什么 不变量 收缩哪边 第一个 ≥ x / > x 答案 ∈ [lo, hi]；i < lo 皆不满足 P P(mid) → hi=mid，否则 lo=mid+1 最后一个 < x / ≤ x 等于「第一个 ¬Q」− 1 先套上一行，再减一（检查 −1） 最小化最大值 值域 [lo, hi)；feasible 单调 可行 → hi=mid，否则 lo=mid+1 最大化最小值 可行在「更大」一侧 P 取反，或 mid 上取整配 lo=mid 旋转点（无重复） P(i)= a[i] ≤ a[n−1] 单调 同 §3 模板 峰值 P(i)= a[i] > a[i+1]（已下降） 已下降 → hi=mid，否则 lo=mid+1 第 k 小 count(≤ x) 关于 x 递增 在值域上二分 x 整体有序矩阵 摊平一维 [0, mn) 同 lower_bound 行列有序（非整体） 不该硬套二分 右上角走，O(m+n) 排除整行/列 浮点最优 固定 100 轮，不靠 eps 可行 → hi=mid，否则 lo=mid set 查找 要用成员函数 算法版 advance 是 O(n) 五种场景的谓词 点场景看 P 怎么写、为何单调。 四种边界 二分答案 旋转数组 山峰数组 浮点二分"},{"a":"s5","h":"STL 与复杂度陷阱","x":"05 STL 与复杂度陷阱 保证的是比较次数，不是运行时间"},{"a":"s5-1","h":"5.1 比较次数 ≠ 运行时间","x":"5.1 比较次数 ≠ 运行时间 std::lower_bound 保证的是 O(log n) 次比较 ，不是 O(log n) 运行时间。它维护 len 而不是 last ，用 advance(mid, half) 前进——对 ForwardIterator 也能编译。随机访问上这是真 O(log n)； list / set 的迭代器上 advance 沿中序一步步走，总推进 O(n)， 比线性扫描还慢 （同样 O(n) 移动，外加 log n 次比较和更差的访问模式）。 迭代器 比较 推进 总时间 RandomAccess（vector） O(log n) O(log n) O(log n) Bidirectional（set / list） O(log n) O(n) O(n) 容器提供了同名成员函数就用成员： s.lower_bound(x) 沿红黑树从根往下，才是真 O(log n)。算法版只能通过迭代器接口，拿不到树形结构。同类差异： set::find vs std::find 、 string::find vs std::search 。"},{"a":"s5-2","h":"5.2 全家桶对照","x":"5.2 全家桶对照 接口 返回 用途 lower_bound 第一个 ≥ x 插入点、左边界 upper_bound 第一个 > x 右边界、稳定插入 equal_range 两者一起 等于 x 的区间 binary_search 只给 bool 先它再 lower_bound = 二分两次；直接 lower_bound 一次再检查 partition_point 第一个使 P 为假 谓词已划分时的二分，最接近 §3 partition_point 找第一个使 P 为 假 的位置，要求区间已按 P 划分——就是「谓词单调」的另一种说法。按派生字段或外部条件二分时，用它比硬凑 comparator 干净。 binary_search 只给 bool：先它再 lower_bound 是二分了两次，直接 lower_bound 一次再检查即可。"},{"a":"s6","h":"二分答案","x":"06 二分答案 把「求最优值」变成「判定可行性」"},{"a":"s6-1","h":"6.1 求最优 → 判定可行","x":"6.1 求最优 → 判定可行 直接求最优 x* 往往很难；给定候选判断「行不行」通常只要贪心扫一遍。若 feasible(x) 单调，就在 值域 上套 §3 模板。代价 O(log(值域) × 一次判定)。 二分答案：搜索值域，不是数组下标 值域 [max(a), sum(a)+1) mid = 某段和上限 没有 a[mid] 这个元素 feasible = 假 feasible = 真 第一个可行 = 最小可行上限（最小值最大化 / 最大值最小化的分界） 信号：题面是「最小化最大值 / 最大化最小值」，值域已知，验证比求解容易。 像试衣服：不测 37 个围度，直接问「这一号能不能穿」。M 能穿则 L、XL 一定能——这就是单调。尺码乱标则二分会给错号。 题型 二分对象 判定 单调方向 分割数组最大值最小 每段和上限 贪心分段，数段数 上限越大越可行 船运载重 / 珂珂吃香蕉 载重 / 速度 贪心装货或 Σ⌈pile/speed⌉ 越大越可行 木材切割 每段长度 Σ ⌊len_i / L⌋ 长度越小越可行 第 k 小的距离对 距离 d 双指针数 ≤ d 的对数 距离越大越可行"},{"a":"s6-2","h":"6.2 最短判定函数","x":"6.2 最短判定函数 分割数组：分成 k 段，使各段和的最大值最小。判定「上限 limit 能否 ≤ k 段」——贪心，塞不下就开新段。 // 最小值最大化 / 最大值最小化：判定尽量短 bool feasible ( long long limit, int k) { int segs = 1 ; long long cur = 0 ; for ( int x : a) { if (x > limit) return false ; if (cur + x > limit) { ++segs; cur = x; } else cur += x; } return segs <= k; // 值域 [max, sum]，套 §3 } 三个识别信号都命中就基本确定用二分答案：① 最小化最大值或最大化最小值；② 答案是数值且值域已知或易估；③ 验证一个候选比找到它容易。判定几乎总是「贪心扫一遍 + 计数」，比直接 DP 的 O(n²k) 好一个数量级。"},{"a":"s6-3","h":"6.3 单调性的证明义务","x":"6.3 单调性的证明义务 单调判定：feasible 必须是 FFF…FTTT…T，不能交替 limit 3 4 5 6 7 8 F F F T T T → 可二分 「恰好 k 个使和 = T」 F T F T 不单调 → 二分安静给错答案 证明费：写下 feasible(x) ⟹ feasible(更松的 x′)，理由通常是「原方案在放宽后仍合法」。小数据枚举打印序列，确认不是 FTFT。 带「恰好 / 正好等于」的约束最常破坏单调性，正解多半是 DP。小数据把值域枚举一遍，打印 feasible 序列，确认是 FFF…T 而不是 FTFT。"},{"a":"s7","h":"找回单调性","x":"07 找回单调性 数组不全局有序时，二分还能用吗"},{"a":"s7-1","h":"7.1 旋转有序数组","x":"7.1 旋转有序数组 [4,5,6,7,0,1,2] 不全局有序，但任意划分后至少一半有序。更干净的做法：构造单调谓词，套 §3。 旋转数组： 4 5 6 7 0 1 2 mid 旋转点在 7 → 0 之间 左半 [4..7] 有序 ✓ 右半 [0..2] 也有序（含断点在别处时未必） 判断哪半有序：比较 a[lo] 与 a[mid] a[lo] ≤ a[mid] → 左半有序 a[lo] > a[mid] → 右半有序 然后在有序的那半里判断 target 是否落在其中 有重复元素时会退化 [1,1,1,0,1,1,1] 找 0：a[lo]==a[mid]==1 → 无法判断哪半有序 → 只能 ++lo 收缩 最坏 O(n)，且这个下界不可改善 旋转数组的可二分性来自「任一划分后至少一半有序」。有重复元素时这个判断失效，最坏退化到 O(n)。 不要为每种变体重新设计二分： P(i) = (a[i] <= a[n-1]) 在旋转数组上恰好是前段大值（假）、后段小值（真），模板一字不改。找到旋转点后再在对应有序段做标准二分。 // 找旋转点 = 第一个 a[i] ≤ a[n-1]；P 在旋转数组上单调 int lo = 0 , hi = n - 1 ; while (lo < hi) { int mid = lo + (hi - lo) / 2 ; if (a[mid] <= a[hi]) hi = mid; else lo = mid + 1 ; } // lo = 最小值下标，再在有序段做标准二分 有重复时 [1,1,1,0,1] 无法判断哪半有序，只能 ++lo ，最坏 O(n)——那是信息论下界（0 可在任意位置），不是实现不够好。"},{"a":"s7-2","h":"7.2 山峰数组","x":"7.2 山峰数组 数组本身先增后减，不单调；导出量 a[i] > a[i+1] （已开始下降）单调。循环形状同 §3， hi 取 n−1 以保证 mid+1 合法。细节见 §4.3 表。 峰值局部：不看全局，只比较相邻，导出量单调即可二分 1 3 5 7 6 4 2 峰 P(i)=a[i]>a[i+1]：左半 F（仍在升），右半 T（已在降） mid 与 mid+1：仍升 → 峰在右（lo=mid+1）；已降 → 峰在左含 mid（hi=mid） 同类导出量： a[i]−i 找缺失、 count(≤ x) 找第 k 小。原数组不必单调。 导出量 含义 用来解 a[i] > a[i+1] 是否已开始下降 山峰、局部极值 a[i] ≤ a[n-1] 是否已过旋转点 旋转数组最小值 a[i] − i 是否已出现空洞 缺失的数、第 k 个缺失 count(≤ x) 不超过 x 的个数，关于 x 递增 第 k 小、有序矩阵第 k 小 行列都有序但 不是 「下一行首 > 上一行尾」时，不要对每行二分。从右上角走：比目标大就弃整列，小就弃整行，O(m+n)，信息利用率高于 O(m log n)。 能二分不等于该二分 ——先问每步能排除多少候选。整体有序的矩阵才摊平当一维 lower_bound。"},{"a":"s8","h":"浮点二分","x":"08 浮点二分 为什么固定迭代次数比 eps 更稳 浮点上「相邻两个可表示数」的间距随数量级变大。用绝对 eps 当终止条件，等于把算法和题目精度、数值大小绑死：1e18 上 1e-9 永远达不到，小数上又可能过早停。固定 100 轮把循环次数变成与数值无关的常数；能乘 10^k 做成整数二分就不要走浮点。 浮点二分：不要用 eps 当刹车 ❌ while (hi − lo > 1e-9) lo、hi ≈ 1e18 时，相邻 double 间隔已 > 1e-9 → 条件永不成立 死循环；eps 还耦合题目精度 ✅ for (it = 0; it < 100; ++it) 每轮减半，100 轮 → 2⁻¹⁰⁰ ≈ 1e-30 double 53 位，约 60 轮已到极限 次数与数值大小无关，不会挂死 能整数化就整数化（保留 4 位小数 → 在 ans×10⁴ 上做整数二分）。浮点不要写 while (lo < hi)。 相对误差：fabs(a−b) ≤ eps × max(1, |a|)。输出用 printf(\"%.10f\")。 100 轮是留余量；关键是循环次数变成与数值无关的常数。 坑 症状 修法 hi - lo > eps 且数值巨大 死循环 固定迭代次数 浮点 while (lo < hi) 几乎必然死循环 不要用严格不等判终止 绝对 eps 判相等 大数判不出、小数过早判 相对误差 eps * max(1, |a|) 输出默认 6 位 算对被判错 printf(\"%.10f\") 上下界估错 答案卡在边界、偶发才暴露 宁可放宽"},{"a":"s9","h":"工程里的二分","x":"09 工程里的二分 从 git bisect 到 cache 友好的内存布局 工程里二分的「一次判定」往往比比较两个整数贵几个数量级：一次编译、一次磁盘页、一次 cache miss。所以真正要优化的是 判定次数和每次判定碰到的硬件层次 ，而不是再把比较次数从 30 降到 28。"},{"a":"s9-1","h":"9.1 git bisect","x":"9.1 git bisect 日常里最贵的「一次判定」：编译 + 测试。500 个提交约 9 次。谓词是「这个提交上 bug 还在」——前提是 bug 一旦引入就一直在。 要素 在 git bisect 里 区间 good 到 bad 之间的提交 谓词 这个提交上 bug 存在吗 单调性 引入后一直存在 失效 flaky、中间编不过（该 skip）、修了又引入（FTFT） 单调性坏了不会报错，只安静给错答案。判定必须是纯函数、处处有定义、真单调。"},{"a":"s9-2","h":"9.2 B+ 树页内二分","x":"9.2 B+ 树页内二分 数据库和文件系统的 B+ 树里，二分出现在两个层次上： [ 100 | 300 | 700 ] 根页 页内二分找子指针 [ 20 | 50 | 80 ] [ 150 | 220 ] [ 400 | 550 ] 1..19 20..49 100..149 150..219 叶子按序链起来 → 范围扫描 层次一：树高 = log_B(n)，B ≈ 数百 → 十亿行只需 3~4 层 层次二： 每页内部对几百个 key 做二分 = log₂B ≈ 8~10 次比较 B+ 树的设计动机就是「让树高层面的每一步都对应一次磁盘 I/O，而页内的二分在内存里完成」。 层次 次数 单次代价 优化目标 树高（跨页） log_B(n) 一次磁盘 I/O ≈ 100 µs 把 B 做大，树尽量矮 页内二分 log₂B ≈ 8–10 内存访问 ≈ 100 ns 次要（差一千倍） 两层代价差 1000 倍。节点做成页（B ≈ 数百）， log_B(10⁹) ≈ 3~4 ；二叉搜索树要 ~30 次 I/O。 用页内廉价二分换掉跨页跳转。 展开见 存储与数据架构 。LSM 块内二分、跳表、列存 min/max 剪枝是同一套路。"},{"a":"s9-3","h":"9.3 cache 友好的布局","x":"9.3 cache 友好的布局 大数组上标准二分前几步跳 n/2、n/4……每步一次 cache miss，而且下一步依赖比较结果，预取器完全失效。4 GB 的 int 约 30 步里前 20 步都在等主存。只读热点才值得换布局。 标准二分在有序数组上的访问顺序 ① ② ③ ④ 前几步跳跃距离巨大（n/2, n/4, n/8…）→ 每步一次 cache miss，预取器完全失效 eytzinger 布局：把二叉搜索树按层序（BFS）压平存放 root L1 两个 L2 四个 L3 八个 … 前几层挤在同一条 cache line 里 → 头几步几乎零 miss；下标计算变成 i = 2i 或 2i+1（无分支） 代价：布局要预先构建、不支持随时插入 → 只适合「构建一次、查询很多次」的只读索引 同样 O(log n) 次比较，把数据摆放方式换掉就能显著降低 cache miss——这是渐进分析看不见的一层。只读热点才值得换；多数业务代码里 std::lower_bound 已经够。 手段 思路 前提 分支消除 cmov 代替 50% 误预测的分支 先做这个 eytzinger 层序存放，前几层同一 cache line 只读索引 cache 层 B 树 一行多个 key，SIMD 一次比 8–16 个 实现重 插值查找 均匀时 O(log log n) 不均则退化 O(n) 这些优化只在「数组远超 L3 + 查询极频繁 + 只读」时值得做：数据库只读索引、倒排表、游戏静态资源表。绝大多数业务代码里 std::lower_bound 已经够——先看 profile 里这段是否超过 5%。点查且不需要范围扫描时，哈希表的 O(1) 比任何二分优化都强。测量见 性能优化指南 。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 手写一个二分查找，并说明 mid 为什么不能写 (lo + hi) / 2 。 // 找第一个 >= target 的下标；没有则返回 n int lower_bound ( const int * a, int n, int target) { int lo = 0 , hi = n; // 半开区间 [lo, hi) while (lo < hi) { int mid = lo + (hi - lo) / 2 ; // lo ≤ mid < hi if (a[mid] >= target) hi = mid; else lo = mid + 1 ; } return lo; } 溢出问题 lo + hi 在两者都接近 INT_MAX/2 以上时会 有符号整数溢出 ——这是 UB，实际常得到负数，然后 a[负数] 越界读。 这个 bug 在 JDK 的 Arrays.binarySearch 里活了九年 （2006 年 Joshua Bloch 披露），而它照抄自《Programming Pearls》——那本书里错了二十年。 正确写法 lo + (hi - lo) / 2 （最通用）或 (int)(((unsigned)lo + hi) >> 1) 。 对 指针和迭代器 来说前者是唯一写法—— ptr1 + ptr2 在 C++ 里根本不合法。这也是 STL 实现长这样的原因。 为什么用半开区间 「空区间」有唯一表示（ lo == hi ），区间长度就是 hi - lo ， hi = n 天然表示「没找到」，而且和 STL 的 [first, last) 一致。 它能消掉一整类 off-by-one 错误 ，不是审美偏好。 基础 二分的死循环通常怎么产生？怎么从结构上避免？ 根因只有一个： 某轮循环后区间没有严格缩小 。 最常见的一种 mid 向下取整时，如果区间只剩 2 个元素（ hi == lo+1 ），那么 mid == lo 。此时写 lo = mid 等于没变 → 永远循环。 记住这条配对规则 mid 向下取整时 （ lo + (hi-lo)/2 ）： hi = mid 安全 （ mid < hi 恒成立）， lo = mid 不安全 ，必须写 lo = mid + 1 。 如果逻辑确实需要 lo = mid ，就把 mid 改成 向上取整 ： lo + (hi - lo + 1) / 2 ，此时 mid > lo 恒成立， lo = mid 变安全而 hi = mid 变不安全。 取整方向和赋值方式必须配对。 另一类：区间约定混用 初始 hi = n （半开）但循环条件写 lo <= hi （闭区间的写法）→ 会访问 a[n] 越界。 一份代码里只用一种区间约定。 结构上的根治 写一个 循环不变式 并保证每轮都成立，终止性就是自动的推论： hi = mid 时新长度 mid - lo < hi - lo ； lo = mid+1 时新长度 hi - mid - 1 < hi - lo 。 两个分支都严格缩短 ⟹ 至多 ⌈log₂n⌉ 轮必然退出。 基础 lower_bound 和 upper_bound 的区别是什么？怎么用它们数出 x 出现了几次？ 返回 谓词 lower_bound(x) 第一个 不小于 x（≥ x）的位置 a[i] >= x upper_bound(x) 第一个 大于 x（> x）的位置 a[i] > x 对 [1,3,5,5,5,7,9] 查 x=5： lower_bound =2、 upper_bound =5。 一句话记牢 两者夹出的 [lo, up) 就是所有等于 x 的元素 。所以： · 出现次数 = up - lo （等于 0 就是不存在） · 最后一个 < x 的位置 = lo - 1 · 最后一个 ≤ x 的位置 = up - 1 · equal_range 一次同时返回两者 auto [lo, up] = std:: equal_range (v. begin (), v. end (), x); int count = up - lo; bool exists = (lo != up); 「最后一个」要检查 −1 当所有元素都 ≥ x 时 lower_bound 返回 0，减一得 −1 → 表示不存在。 忘了检查就是 a[-1] 越界。 这是「找最后一个」比「找第一个」更易错的唯一原因——不是逻辑更难，而是多了一个要显式处理的失败返回值。 别二分两次 binary_search 只返回 bool ，拿不到位置。「先 binary_search 判存在再 lower_bound 取位置」是 二分了两次 。直接 lower_bound 一次，然后查 it != last && *it == x 。 基础 二分查找的前提是什么？「数组必须有序」这个说法准确吗？ 不准确，它太窄了。 真正的前提是： 存在一个在区间上单调的布尔谓词 ——即存在某个分界点，之前全假、之后全真。 「有序数组找目标值」只是取 P(i) = (a[i] >= target) 的特例。 只要能构造出单调谓词，二分就适用—— 数组不必存在，答案也不必是下标。 三个「不是有序数组」但能二分的例子： git bisect 提交历史不是排好序的数字，找的也不是某个值，而是「状态从好翻转到坏」的位置。谓词是「这个提交上 bug 存在吗」，单调性来自「bug 一旦引入就一直存在」。 二分答案 「最小的能在 D 天内运完的船载重」——根本没有数组，二分的是 答案的值域 。谓词是 feasible(cap) 。 山峰数组 数组本身不单调（先增后减），但 导出量 a[i] > a[i+1] （「是否已开始下降」）单调。 这也说明了失效条件 谓词不单调时， 二分不会报错，只会安静地给出错误答案 。所以每次用二分都要先确认单调性，而不是假设它成立。 git bisect 遇到 flaky test 就是这么被带偏的。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 写出二分的循环不变式，并用它证明算法的正确性与终止性。 对 [lo, hi) 半开区间模板： 不变式 答案 ∈ [lo, hi]，即 ∀ i < lo: P(i) 为假 ∀ i ≥ hi: P(i) 为真 三步走： ① 初始化 lo=0, hi=n 。「∀ i < 0 假」和「∀ i ≥ n 真」都是 空真 （vacuously true）。✓ ② 保持 由 mid = lo + (hi-lo)/2 得 lo ≤ mid < hi 。 · P(mid) 真 → 由单调性 ∀ i ≥ mid 皆真 → 答案 ≤ mid → 置 hi = mid ，不变式保持。 · P(mid) 假 → 由单调性 ∀ i ≤ mid 皆假 → 答案 > mid → 置 lo = mid+1 ，不变式保持。✓ ③ 终止时给出答案 退出时 lo == hi ，不变式给出「∀ i < lo 假、∀ i ≥ lo 真」⟹ lo 就是 第一个使 P 为真的下标。∎ ④ 必然终止 hi = mid 时新长度 mid − lo < hi − lo （因 mid < hi ）； lo = mid+1 时新长度 hi − mid − 1 < hi − lo 。 两分支都严格缩短 ⟹ 至多 ⌈log₂n⌉ 轮。∎ 这个方法的价值 写二分时 不靠试错试出 < 还是 <= ，而是先写下不变式，让边界从不变式里推出来 。这样每次都能证明它对，而不是「跑几个用例看着没错」。 循环不变式是验证任何循环的通用工具，不只用于二分——它也是 Dijkstra、并查集、Floyd 等算法正确性证明的骨架。 中级 什么是「二分答案」？举一道题说明怎么把「求最优」变成「判定可行」。 直接求最优值 x* 往往很难；但给定一个候选值判断它可行不可行，常常容易得多。如果 feasible(x) 关于 x 单调 ，就在答案的 值域 上二分找分界点。 代价：O(log(值域) × 一次判定的代价) 收益：判定通常只要贪心扫一遍，而求最优可能要 DP 甚至枚举 例 · 分割数组的最大值最小化 ：把数组分成 k 个连续子数组，使各段和的最大值最小。 // 判定：以 limit 为「每段和上限」，能否分成 ≤ k 段？→ 贪心 O(n) bool feasible ( const std::vector< int >& a, long long limit, int k) { int segs = 1 ; long long cur = 0 ; for ( int x : a) { if (x > limit) return false ; if (cur + x > limit) { ++segs; cur = x; } else cur += x; } return segs <= k; } // 值域：[max(a), sum(a)]，套 §3 模板一字不改 → O(n log(sum)) // 对比直接 DP 的 O(n²k) 三个识别信号 ① 题目说「最小化最大值」或「最大化最小值」； ② 答案是数值且值域已知/易估； ③ 验证一个候选答案比找到它容易。三个都命中就基本确定。 同类题 船运载重（D 天运完）、木材切割（得 k 段长 ≥ L）、爱吃香蕉的珂珂（H 小时吃完）、第 k 小的距离对、最小化最大等待时间。 判定函数几乎总是「贪心扫一遍 + 计数」。 还有一类：二分「有多少个 ≤ x」 「有多少个元素 ≤ x」关于 x 天然单调递增 ，所以任何「第 k 小」问题都可以试着二分答案 + 数个数。这在「有序矩阵第 k 小」「第 k 小的距离对」里是标准解法。 中级 在旋转有序数组里查找一个数怎么做？有重复元素时会怎样？ 数组不全局有序，但 任意一次划分后至少有一半是有序的 ——这就是可二分的依据。 解法一 · 判断哪半有序 ：比较 a[lo] 与 a[mid] ，然后在有序的那半里判断 target 是否落在其中。 if (a[lo] <= a[mid]) { // 左半有序 if (a[lo] <= target && target < a[mid]) hi = mid - 1 ; else lo = mid + 1 ; } else { // 右半有序 if (a[mid] < target && target <= a[hi]) lo = mid + 1 ; else hi = mid - 1 ; } 解法二（更推荐）· 先找旋转点 把它变成一个标准的谓词二分： 谓词 P(i) = (a[i] <= a[n-1]) 在旋转数组上恰好单调 （前段是旋转前的大值 → 假，后段是小值 → 真）。 while (lo < hi) { int mid = lo + (hi - lo) / 2 ; if (a[mid] <= a[hi]) hi = mid; else lo = mid + 1 ; } // lo = 最小值下标 找到旋转点后在对应的有序段做标准二分。 好处是模板一字不改 ——这正是「构造单调谓词而不是重新设计二分」的做法。 有重复元素时退化 O(n) [1,1,1,0,1,1,1] 找 0： a[lo] == a[mid] == 1 ， 无法判断哪半有序 ，只能 ++lo 逐个收缩。 而且这个 O(n) 不可改善 考虑 [1,1,...,1,0,1,...,1] ——那个 0 可能在任何位置，且任何比较都无法排除超过常数个候选。 所以 O(n) 是信息论下界，不是实现不够好。 面试时能指出这一点是加分项。 中级 为什么在 std::set 上不该用 std::lower_bound ？ std::partition_point 又是什么？ 因为 std::lower_bound 保证的是 O(log n) 次 比较 ，而不是 O(log n) 的 运行时间 。 // 实现里维护 len 而不是 last，用 advance 推进 auto half = len >> 1 ; It mid = first; std:: advance (mid, half); // ← 关键 迭代器类别 比较 推进 总复杂度 RandomAccess（vector） O(log n) O(log n) O(log n) Bidirectional（set / list） O(log n) O(n) O(n) 在 set 上， advance 要沿红黑树中序 一步步走 。所以 std::lower_bound(s.begin(), s.end(), x) 能编译、结果对，但 O(n)—— 甚至比线性扫描更慢 （同样 O(n) 移动，外加 log n 次比较）。 正确做法 用 成员函数 s.lower_bound(x) ——它直接沿红黑树从根往下走，真正 O(log n)。 通用规则：容器提供了同名成员函数时一律用成员函数 ，因为它能利用内部结构而算法版只能通过迭代器接口。同类差异还有 set::find vs std::find 、 string::find vs std::search 。 partition_point（被低估的一个） C++11 的 std::partition_point(first, last, P) 返回 第一个使谓词 P 为假的位置 ，要求区间已按 P 划分（也就是「谓词单调」的另一种说法）。 它就是本文 §3 的谓词版模板。 凡是「有序但不是按元素值排序」的二分——按某个派生字段、按外部条件——用它比硬凑一个 comparator 干净得多。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 你用二分答案解了一道题，但线上偶尔给出错误结果。可能是什么原因？ 二分答案最危险的性质是： 单调性不成立时它不会报错，只会安静地给出错误答案 。「偶尔错」这个描述指向三类问题。 ① 谓词根本不单调 典型是带「 恰好 」「 正好等于 」的约束。比如「选恰好 k 个元素使和等于 v」—— feasible 序列可能是 F T F T ，二分会随机落到某个可行点。 放宽资源反而可能让「恰好」失效 ，这是单调性被破坏的最常见来源。这类题的正解通常是 DP，不是二分。 ② 判定函数不确定（有随机性/依赖外部状态） 如果 feasible 读了缓存、依赖当前时间、或用了浮点累加导致同一输入两次结果不同——那就是 git bisect 遇到 flaky test 的情形： 一次误判会把整个二分带偏 ，而且表现为「偶发」。 判定函数必须是纯函数。 ③ 值域上下界估错 下界比真实答案大、或上界比真实答案小，二分会返回边界值。这类错误 只在数据接近极端时暴露 ，所以也表现为「偶尔」。 上下界宁可放宽 ——多几轮迭代的代价可以忽略。 怎么定位和根治： 对拍验证单调性 小数据下把值域 全部枚举 一遍，打印 feasible 序列，肉眼确认它是 FFF...FTTT...T 而不是交替的。这是最直接的验证，应该作为写二分答案的标准步骤。 写下形式化的蕴含式 明确写出 feasible(x) ⟹ feasible(x') （x' 是更宽松那侧）并给出一句话理由。 最常用的理由是「原方案在放宽约束后仍然合法」 ——如果你说不出这句话，单调性很可能不成立。 断言 + 暴力对拍进 CI 把「小规模下二分答案 == 暴力枚举」写成一个属性测试挂进 CI。二分答案的正确性依赖一个 数学性质 而不是代码逻辑，所以它需要的是 性质测试 而不是几个用例。 高级 B+ 树里二分出现在哪几个层次？为什么 B+ 树要设计成「又矮又胖」？ 两个层次，而它们的 代价单位完全不同 ——这正是 B+ 树设计的全部动机。 层次 次数 单次代价 优化目标 树高（跨页导航） log_B(n) 一次磁盘/SSD I/O ≈ 100 µs 把 B 做大 → 树高尽量低 页内二分 log₂B ≈ 8–10 一次内存访问 ≈ 100 ns 次要（差 1000 倍） 为什么又矮又胖 把节点做成一个磁盘页大小（4–16 KB），每页能放 几百个 key ，于是 B ≈ 数百： log_B(10⁹) ≈ 3~4 ← 十亿行只需 3~4 次 I/O log₂(10⁹) ≈ 30 ← 二叉搜索树要 30 次 I/O B+ 树的全部智慧就是「用页内的廉价二分（100 ns）换掉昂贵的跨页跳转（100 µs）」。 B+ 而不是 B 的额外理由 B+ 树把 数据只放叶子 、内部节点只放 key。这让内部节点能塞更多 key（B 更大 → 树更矮），而且 叶子按序链成链表 ，范围扫描（ WHERE x BETWEEN a AND b 、 ORDER BY ）变成顺序 I/O 而不是回树遍历。 同一思路的其他化身 跳表 ：用多层链表模拟二分，每层跳过约一半——本质是「链表上的二分」，而且靠随机层高避免了红黑树的旋转（Redis ZSet 用它）。 LSM-Tree 的 SSTable ：块索引 + 块内二分，外加 Bloom 过滤器先挡掉不存在的 key。 Parquet/ORC 列存 ：先用块级 min/max 统计剪枝，再在块内二分。 cache 层的 B 树 ：把同一思路下移一层——一个 cache line 放多个 key，用 SIMD 一次比较 8–16 个，把「内存访问」当成新的「磁盘 I/O」来省。 展开见 存储与数据架构 。 高级 一个 4 GB 的只读有序索引，二分查询是热点。怎么优化？ 标准二分在超出 cache 的大数组上 渐进最优但常数很差 ：前几步跳跃距离是 n/2、n/4、n/8…… 每步一次 cache miss，而且预取器完全失效 （下一步位置依赖当前比较结果，不可预测）。4 GB 数据 ≈ 10⁹ 个 int，log₂n ≈ 30 步，前约 20 步全是主存访问 → 单次查询约 2 µs 里绝大部分在等内存。 ① 分支消除（最容易，先做这个） 用条件移动代替分支： lo += cond ? half+1 : 0 。二分的分支 本质不可预测 （约 50% 误预测率），每次误预测约 15–20 周期。写成 cmov 形式后能省掉这部分。 ② eytzinger 布局（收益最大） 把二叉搜索树 按层序（BFS）压平 存放：root 在下标 1，子节点在 2i / 2i+1。 好处： 前几层挤在同一条 cache line 里 ，头几步几乎零 miss；下标计算是纯乘法，无分支。 代价： 只适合只读索引 ——布局要预先构建，不支持随时插入。 ③ B 树式扁平索引（终极方案） 把 B+ 树的思路下移到 cache 层：一个 cache line（64 B）放 16 个 int32 key，用 SIMD 一次比较 16 个 ，一次 cache miss 换 4 位信息（而标准二分一次 miss 只换 1 位）。 树高从 30 降到约 8。 这是现代内存索引（如 ART、Masstree、FAST）的核心手段。 ④ 插值查找（看分布） 数据近似均匀时按值估算位置，期望 O(log log n)。但 分布不均时退化到 O(n) ，所以生产上要做成「插值探测几步，不收敛就退回二分」的混合形式。 ⑤ 预取 __builtin_prefetch 提前取两个可能的下一步。用一半带宽换延迟——只在带宽有余量时划算。 但先问两个问题 ① 这段二分在 profile 里真的超过 5% 了吗？如果没有，以上全是过早优化。 ② 能不能 换掉二分 ？如果查询是点查且不需要范围扫描，一个哈希表就是 O(1)——比任何二分优化都强。如果查询有 局部性 （相邻查询的 key 接近），加一层缓存或改成从上次位置开始的指数搜索（galloping）可能比优化二分更有效。 算法层面的选择永远排在常数优化之前。 测量方法见 性能优化指南 。 高级 浮点二分为什么建议固定迭代次数而不是用 eps 判终止？有哪些别的坑？ 用 eps 的两个问题 ① 大数值时死循环 ： while (hi - lo > 1e-9) 在 lo, hi ≈ 1e18 时， 相邻 double 的间隔已远大于 1e-9 → 条件永远为真 → 挂死。 ② eps 难定 ：取大了精度不够，取小了白跑（甚至不收敛）。而且它耦合了「算法」和「题目精度要求」两件事。 固定迭代次数 for ( int it = 0 ; it < 100 ; ++it) { double mid = lo + (hi - lo) / 2 ; if ( ok (mid)) hi = mid; else lo = mid; } double 有 53 位有效位 → 相对精度上限约 2⁻⁵³ ≈ 1.1e-16， 约 60 轮就已到极限 （再多轮 lo/hi 会变成相邻浮点数，区间无法再缩）。100 轮是保守余量，开销可忽略。 关键是把循环次数变成与数值大小无关的常数。 其他坑： 用 lo < hi 判终止 浮点上这几乎必然死循环—— mid 会变成等于 lo 或 hi ，区间不再缩小但 lo != hi 。 浮点二分不能用严格不等式判终止。 用绝对 eps 判「相等」 fabs(a-b) < 1e-9 在大数上判不出、在小数上过早判定。要用 相对误差 ： fabs(a-b) <= eps * max(1.0, fabs(a)) 。 输出精度 算对了但 cout 默认只输出 6 位有效数字。要 printf(\"%.10f\") 或 setprecision(10) 。 最好的办法：整数化 很多「浮点二分」的题可以 乘以 10^k 变成整数二分 （要求保留 4 位小数就在 [0, ans_max*10⁴] 上做整数二分）。这样既避开全部浮点陷阱，又精确控制精度。 能整数化就整数化 ——和「金额用『分』而不是浮点『元』」是同一个原则。 ⬅️ 排序全景 📐 复杂度与分析 🔁 递归与分治 🗄️ 存储与数据架构 📚 目录 🏠 主页 二分查找 — 循环不变式证明 · 四种边界统一模板 · lower_bound 实现 · 二分答案 · 旋转数组 · cache 友好布局 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/complexity_and_analysis.html","t":"复杂度与分析","c":"算法与数据结构","s":[{"a":"","h":"","x":"复杂度与分析 · Big-O 严格定义 / 摊销分析三法 / 主定理 / 常数因子与 cache 局部性 / P 与 NP Kavin Tech Notes ← 主页 目录 30 min ◐ 复杂度与分析 为什么需要渐进分析 与其他章节的关系 五个记号的严格定义 定义与直觉 四种常见误用 大 O 光谱 比较排序下界 怎么把代码数成复杂度 三条运算法则 循环不能只看层数 复杂度速查 摊销分析三法 聚合法：vector 扩容 记账法 势能法 1.5× 还是 2× 摊销 ≠ 平均 递归树与主定理 递归树：把递归摊平 主定理三情形 典型算法归位 主定理不适用时 空间复杂度与递归栈 渐进之外：常数因子 内存墙与 cache line list 的 O(1) 输给 vector 的 O(n) 常数 vs 复杂度（交互） 最坏 / 平均 / 期望 / 摊销 四种口径的区别 为什么线上看 p99 P、NP 与工程含义 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Complexity Analysis · 2026 复杂度与分析 渐进复杂度是一门 把实现细节和硬件差异全部抹掉 的语言，它让「哪个算法更好」变成一个可以在纸上回答的问题。但它抹掉的东西不是不存在——同样是 O(n) 的遍历，链表能比数组慢一个数量级。这一篇既要把定义、摊销、主定理讲到能证明的程度，也要讲清楚渐进符号 沉默的地方 藏着什么。 核心难度 ★★★☆☆ 面试出现率 极高 阅读时长 ~30 min 前置 无 Big-O 的严格定义 摊销分析三法 递归树与主定理 常数因子与 cache P 与 NP"},{"a":"s1","h":"为什么需要渐进分析","x":"01 为什么需要渐进分析 从「跑得快」到「可比较」 「这个算法快不快」取决于 CPU、编译器、输入和 cache，没法拿实测数字比较算法本身。渐进分析只问 输入翻倍时工作量怎么变 ：丢掉常数和低阶项，换来跨机器都成立的结论。这个取舍贯穿全章——§2–§6 用它在纸上比较算法，§7 再把被抹掉的常数和 cache 捡回来。同一份 O(n) 遍历，链表能比数组慢一个数量级，那不是渐进分析错了，是它根本没声称自己管这件事。 像搬家按 箱数 报价，不写「哪个师傅快 15%」。O(n) 是按箱线性计费，O(n²) 是每箱都要和其他箱对一遍清单——1000 箱时后者必炸。§7 的常数因子则是报价单没写的那句： 有没有电梯，同样箱数差十倍。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 递归与分治 —— 本章 §5 的主定理正是分治复杂度的求解工具 排序全景 —— 比较排序的 Ω(n log n) 下界要用到本章 §2 的 Ω 记号 二分查找 —— O(log n) 的典型来源，也是主定理情形二的例子 STL 容器深度剖析 —— 各容器的复杂度承诺与迭代器失效 性能优化指南 —— §7 的常数因子问题在这里有完整的测量方法论"},{"a":"s2","h":"五个记号的严格定义","x":"02 五个记号的严格定义 O 是上界，不是「就是」"},{"a":"s2-1","h":"2.1 定义与直觉","x":"2.1 定义与直觉 五个记号描述的是 n→∞ 时两个函数的相对增长速度。前缀「∃ c, n₀」就是「忽略常数、只看足够大的 n」。工程习惯写 f = O(g)，但等号是单向的：3n+7 = O(n²) 成立，反过来说就不成立——O(n²) 是一个 函数集合 。 O(g) 上界（≤） ∃ c>0, n₀, ∀ n≥n₀: f(n) ≤ c·g(n) Ω(g) 下界（≥） ∃ c>0, n₀, ∀ n≥n₀: f(n) ≥ c·g(n) Θ(g) 确界（=） f ∈ O(g) 且 f ∈ Ω(g) o(g) 严格上界（<） ∀ c>0 最终都 f < c·g ω(g) 严格下界（>） ∀ c>0 最终都 f > c·g O 只要 存在一个 c；o 要求 任意小的 c 都最终成立。故 n² ∈ O(n²) 但 n² ∉ o(n²)；n ∈ o(n²)。O(n²) 是集合：可写 3n+7 = O(n²)，不能反过来说。 O 像体检「血压不超过 140」——实际 110 也成立。答「快排是 O(n²)」是真话但没信息。要听的是 平均 Θ(n log n)、最坏 Θ(n²) 。"},{"a":"s2-2","h":"2.2 四种常见误用","x":"2.2 四种常见误用 错 对 「快排是 O(n log n)」 缺口径。平均 Θ(n log n)，最坏 Θ(n²)；随机 pivot 后最坏很难被构造 「O(2n) 比 O(n) 慢」 同一集合。想说「常数大一倍」就说常数因子，不要用 O 「哈希 O(1) 一定快过树的 O(log n)」 n=64 时 log n=6；hash 还要扫 key + 可能 miss，红黑树上层常驻 cache 「最坏 O(n²) 就不能用」 先问最坏输入 能否被攻击者构造 。能 → 安全问题；不能 → 通常可接受 第 4 条最贵。2011 Hash-DoS：构造大量冲突 key，把哈希 O(1) 拖成 O(n)，几十 KB 的 POST 就能打满一个核。修复不是换数据结构，而是给哈希加随机种子（SipHash），让攻击者 算不出 最坏输入。工程含义：最坏情形的价值，取决于谁能决定输入。"},{"a":"s2-3","h":"2.3 大 O 光谱","x":"2.3 大 O 光谱 纵轴每一格 10 倍。n log n 与 n 的缝远小于 n² 与 n log n：n=10⁹ 时 log n≈30，几乎是常数。「把 O(n log n) 抠成 O(n)」收益通常有限，「干掉 O(n²)」几乎总值得。优化精力按这个顺序分配，不要在已经线性对数的热路径上死磕常数，却放着一层嵌套循环不管。 大 O 光谱 · n = 10⁶、约 10⁸ 次操作/秒 O(1) 数组下标 摊销哈希 稳过 O(log n) 二分 / 堆 ~20 次 O(n) 单遍扫描 ~10⁶ 次 O(n log n) 比较排序 ~2×10⁷ O(n²) 双层循环 10¹² 炸 O(2ⁿ) 子集枚举 n≤25 O(n!) 全排列 n≤11 可行 ← log n 在工程规模几乎是小常数 → 组合爆炸 n=10⁶：线性对数还轻松，平方已经不可行。优化顺序是先消灭 n²，再谈把 n log n 抠成 n。"},{"a":"s2-4","h":"2.4 比较排序下界","x":"2.4 比较排序下界 任意只靠「比较两个元素」的排序，最坏至少 Ω(n log n)。决策树有 n! 片叶子（每种排列一条路径），二叉树高 ≥ log₂(n!)，Stirling 给出 log(n!) = Θ(n log n)。归并和堆排的 Θ(n log n) 因此是 最优的 ，不是凑巧。要打破下界必须动用元素值本身：计数 / 基数 / 桶排序的模型已经不是「只比较」。平均情形在「所有排列等概率」下同样是 Ω(n log n)——信息论对期望高度也给同样的界。 比较一次 = 二叉决策的一层 · 必须区分 n! 种输入 a[i] ? a[j] ≤ 分支 > 分支 π₁ π₂ ··· πₖ πₙ！ 叶子 ≥ n! → 高 ≥ log₂(n!) = Θ(n log n) 归并/堆排达标，计数排序靠的是不比较 这是信息论下界，不是某算法的上界。要打破它必须用元素值本身（计数 / 基数 / 桶）。"},{"a":"s3","h":"怎么把代码数成复杂度","x":"03 怎么把代码数成复杂度 三条法则，以及循环层数骗人的地方"},{"a":"s3-1","h":"3.1 三条运算法则","x":"3.1 三条运算法则 法则 形式 说明 加法 O(f)+O(g)=O(max) 排序再遍历 = O(n log n) 乘法 O(f)×O(g)=O(f·g) 外 n 次、内 log n = O(n log n) 常数吸收 O(c·f)=O(f) O(2ⁿ⁺¹)=O(2ⁿ)，但 O(2²ⁿ)≠O(2ⁿ) 常数在指数 外面 可吸收；在 里面 是改增长率。O(2ⁿ⁺¹)=O(2ⁿ)，但 O(2²ⁿ)=O(4ⁿ)≠O(2ⁿ)。O(log n²)=O(log n)，O((log n)²) 不能化简。加法法则也是「只优化瓶颈」的依据：O(n²)+O(n) 的流水线，把线性那段抠到极致也没用。"},{"a":"s3-2","h":"3.2 循环不能只看层数","x":"3.2 循环不能只看层数 数的是 循环体总执行次数 。外层翻倍、内层 n → n log n；双指针 j 只进不退 → O(n)。下面只留一组最刺眼的对照。 「两层循环」三种完全不同的总次数 外层 i *= 2，内层 n 层数 = log n 总次数 n·log n 看着像平方 j 只增不减 Δj 全程 ≤ n 总次数 ≤ 2n 双指针 / 滑窗 / 单调栈 内层每次都从头 n × n 次 work 总次数 n² 字符串反复拷贝也是它 右边才是真平方。中间那种「某个量全程只朝一个方向走」就是 §4 的摊销思想。 ❌ O(n²)：内层每次完整扫 for ( int i = 0 ; i < n; ++i) for ( int j = 0 ; j < n; ++j) work (i, j); // n · n 次 ✅ O(n)：j 全程只前进 int j = 0 ; for ( int i = 0 ; i < n; ++i) while (j < n && ok (i, j)) ++j; // j 最多 +n → ≤ 2n 同类平方还藏在运算符里：循环里 s = s + x 每次拷整串、对 vector insert(begin(), x) 、反复 substr 。数复杂度时把每次调用换成它的代价再加。调和级数也常被漏： for i=1..n for j=i; j<=n; j+=i 的总次数是 n(1+1/2+…+1/n)≈ n ln n，埃氏筛就长这样——两层循环却是 O(n log n)。"},{"a":"s3-3","h":"3.3 复杂度速查","x":"3.3 复杂度速查 复杂度 n=10⁶ 典型来源 O(1) 1 下标、摊销哈希、栈顶 O(log n) 20 二分、平衡树、堆调整 O(n) 10⁶ 单遍、双指针、计数排序 O(n log n) 2×10⁷ 比较排序、分治合并 O(n·α(n)) ≈4×10⁶ 并查集（α 实际 ≤ 4，当常数） O(n²) 10¹² 不可行 朴素双层、插入排序、Floyd 一层 O(n³) 10¹⁸ 不可行 朴素矩阵乘、Floyd 全源 O(2ⁿ) / O(n!) n≤25 / ≤11 状压、子集枚举、全排列 心算：单线程约 10⁸～10⁹ 次/秒。n≤10⁵ 想 O(n log n)，n≤5000 可以 O(n²)，n≤20 是状压。数据范围就是出题人的暗示。"},{"a":"s4","h":"摊销分析三法","x":"04 摊销分析三法 为什么 push_back 是 O(1)，尽管它有时要搬走整个数组 vector::push_back 多数时候写一格（O(1)），满了要搬走全部（O(n)）。摊销分析把「偶尔很贵」摊到整段序列上：任意 m 次操作总代价 ≤ m·T(n)，则单次摊销为 T(n)。这是确定性上界，不依赖概率——和「平均」的区别见 §8。"},{"a":"s4-1","h":"4.1 聚合法：vector 扩容","x":"4.1 聚合法：vector 扩容 容量从 1 翻倍。n 次 push 的搬移发生在 1、2、4、…、2ᵏ（2ᵏ<n），总量 2ᵏ⁺¹−1 < 2n。写入 n 次 + 搬移 < 2n → 总 < 3n，摊销 O(1)。关键就是等比求和： 公比大于 1 时，和由最后一项主导，是末项的常数倍 ，所以「所有历史扩容」不超过「最后一次扩容」的两倍。增长必须是乘性的——每次容量 +1，搬移变成 1+2+…+n = Θ(n²)，摊销 O(n)，vector 就没法当动态数组用了。 n 次 push_back 的实际代价 · 尖峰是扩容搬移 2 4 8 … 下一次尖峰在 16 均摊高度 ≈ 3 搬移 1+2+4+…+2ᵏ < 2n，加 n 次写入 < 3n。每次 +1 扩容则变成三角形 1+2+…+n。 尖峰可以很高，但越来越稀。均摊高度是常数，单次仍可能 O(n)——这是尾延迟来源。 换更大的房子：等塞满再搬到两倍处，一辈子搬家总量不超过最后一次的两倍，摊到每件家具头上只是常数次搬运。每次只加一间房，每添一件家具都要搬家，总代价变成三角形。增长因子的工程选择见 4.4：渐进相同，碎片和搬移次数不同。 哈希表 rehash 是同一张图：装载因子超阈值 → 容量翻倍、全部键重映射，单次 O(n)，序列摊销 O(1)。拉链在 α=1 时平均链长才 1，还可以继续涨；开放寻址的探测随 α→1 发散，所以 Swiss Table 用 0.875 而不是 1。和 vector 的差别：rehash 会让迭代器失效，低延迟要么 reserve(峰值) ，要么像 Redis 那样每次操作搬一个桶，把尖峰切碎。 unordered_map 插入：平时 O(1)，α 超阈值整表 rehash 旧表 8 槽 · 已用 7 空 第 8 次插入 → 停顿 O(n) 新表 16 槽 · α 回到 ~0.5 · 历史 rehash 之和同样 < 2n 低延迟要 reserve(峰值) 或渐进式搬桶 和 vector 同一笔账。区别是 rehash 还会让迭代器失效；Redis 把这次停顿切成每次操作搬一个桶。"},{"a":"s4-2","h":"4.2 记账法","x":"4.2 记账法 每次操作预收一笔固定费用，便宜的把余款存起来，贵的用存款付。账户永不透支，预收款就是合法摊销上界。vector 每次 push 收 3： 用途 费用 说明 写入自己 1 立刻花掉 预付自己将来被搬一次 1 存起来 帮一个老元素搬一次 1 存起来 容量 c→2c 要搬 c 个。后一半 c/2 个是上次扩容之后新来的，各存了 2，合计正好 c。第三笔看起来像「凭空多收」，其实是在给前一半老元素付搬家费。"},{"a":"s4-3","h":"4.3 势能法","x":"4.3 势能法 Φ 度量结构有多「紧」。摊销 = 实际 + ΔΦ。若 Φ(初)=0 且始终 Φ≥0，则 Σ摊销 = Σ实际 + Φ(末) ≥ Σ实际，摊销之和是合法上界。聚合法要写出整段序列的闭式；势能法是 局部 的——对每种操作各验证一个不等式，所以能处理并查集、斐波那契堆、splay 这类操作交错的结构。难点全在找对 Φ。 vector 取 Φ = 2·size − capacity：刚翻倍时 size=cap/2，Φ=0；写满时 Φ=cap。不扩容：实际 1、Φ 增 2，摊销 3。扩容（size=cap=c）：实际 c+1，新 Φ=2，旧 Φ=c，ΔΦ=2−c，摊销仍是 3。 Φ = 2·size − cap 刚翻倍后 Φ=0，写满时 Φ=cap 写满 · size = cap = c Φ = c（蓄满） 下一次 push 要搬 c 个 实际贵，势能高 扩容 cap=2c · size=c+1 Φ = 2 → 几乎抽空 ΔΦ = 2 − c 摊销 (c+1)+(2−c) = 3 贵操作发生时势能释放，正好抵消搬移。并查集 α(n)、splay 的摊销对数，都靠找对 Φ。"},{"a":"s4-4","h":"4.4 1.5× 还是 2×","x":"4.4 1.5× 还是 2× 2×（libstdc++） 1.5×（MSVC / folly） 每元素搬移 ~1 次 ~2 次 浪费上限 50% 33% 复用旧块 历史和永远差 1 终会超过下一块 两者都是摊销 O(1)，差别在常数和碎片。2× 时历史块之和 1+2+…+2^{k−1}=2^k−1， 永远差 1 放不下下一块，分配器很难原地拼回去，堆往高地址涨。1.5× 时历史和终会超过下一块，有机会复用。这个差异只在「单一大 vector 反复增长」时明显；堆上夹杂别的分配时会被稀释。 reserve 把摊销变成真 O(1)；超出预留后仍按因子从 当前值 增长，不会退回按需精确分配。 shrink_to_fit 是非绑定请求。"},{"a":"s4-5","h":"4.5 摊销 ≠ 平均","x":"4.5 摊销 ≠ 平均 摊销 平均 概率假设 不需要 依赖输入分布 保证 任意操作序列 只对「典型输入」 能否被攻击 不能 能（构造最坏输入） 例子 push_back、并查集 快排 Θ(n log n)、均匀哈希 不存在让 n 次 push 超过 O(n) 的「运气差」序列。快排的平均则是对排列求期望——具体输入可以退化成 Θ(n²)。摊销 O(1) 也不等于每次都快：那次 O(n) 扩容落在哪个请求上，哪个就是 p99。低延迟场景启动时 reserve 到峰值。详见 低延迟编程 。"},{"a":"s5","h":"递归树与主定理","x":"05 递归树与主定理 分治算法复杂度的通用解法 分治是 T(n)=a·T(n/b)+f(n) 。归并 2T(n/2)+Θ(n)，二分 T(n/2)+Θ(1)。工具：递归树看谁主导，主定理给闭式。"},{"a":"s5-1","h":"5.1 递归树：把递归摊平","x":"5.1 递归树：把递归摊平 每层代价加总。问三件事：每层多少、树多高（log_b n）、叶子多少（n^{log_b a}）。然后看代价集中在哪：根（每层递减）、均匀（每层相同）、叶子（每层递增）——正好对应主定理三情形。递归树不需要记公式，而且能 看见 是谁在主导，猜错量级时也知道该往哪边改。 n 每层 = n 第 0 层 n/2 n/2 2·n/2 = n 第 1 层 n/4 n/4 n/4 n/4 4·n/4 = n 第 2 层 ⋮ ⋮ 1 1 1 ··· 1 n·1 = n 叶子 log₂ n 总代价 = 每层 n × (log n + 1) = Θ(n log n) 各层均匀 —— 主定理情形二 归并是「每层都一样贵」。换一张图看同一式子的代数展开。 T(n)=2T(n/2)+n 往下代，直到子问题变成 1 T(n) = n + 2T(n/2) = n + 2(n/2 + 2T(n/4)) = n + n + 4T(n/4) = n + n + n + … + n ← log₂ n 个 n 每一层贡献恰好 n 层数 = log₂ n ⇒ Θ(n log n) 展开比记公式更不容易混情形：你能看见「n 被加了多少次」。"},{"a":"s5-2","h":"5.2 主定理三情形","x":"5.2 主定理三情形 对 T(n)=a T(n/b)+f(n)，临界指数 c*=log_b a（叶子数的指数）。 把 f(n)（根的合并代价）和 n^{c*}（叶子总代价）比大小：谁大听谁的，一样大就多乘 log n。 情形一 f = O(n^{c*−ε}) → Θ(n^{c*})；情形二 f = Θ(n^{c*} log^k n) → Θ(n^{c*} log^{k+1} n)；情形三 f = Ω(n^{c*+ε}) 且满足正规性 a·f(n/b)≤c f(n)（某个 c<1）→ Θ(f(n))。正规性保证各层按几何级数递减；多项式的 f 几乎都满足，反例是 n² sin² n 这种震荡——面试极少考，但知道它存在说明你不是死背公式。 情形 条件（直观） 递归树 T(n) 一 叶子主导 f 比 n^{c*} 低一截 越往下越贵 Θ(n^{c*}) 二 势均力敌 f ~ n^{c*} log^k n 每层一样 Θ(n^{c*} log^{k+1} n) 三 根主导 f 比 n^{c*} 高一截，且正规 根最贵、几何递减 Θ(f(n)) 三例对照 · 同一棵树，贵的位置不同 情形一 · 叶子 8T(n/2)+n² c*=3 n² < n³ Θ(n³) 朴素矩阵乘 降 a 才值钱（Strassen） 情形二 · 均匀 2T(n/2)+n c*=1 n = n¹ Θ(n log n) 归并 / 最优堆构建 二分是 T(n/2)+1 情形三 · 根 T(n/2)+n c*=0 n > n⁰ Θ(n) 快速选择（理想划分） 合并已经线性，够了 情形三还要正规性 a·f(n/b) ≤ c f(n)（c<1）。多项式的 f 几乎都满足。"},{"a":"s5-3","h":"5.3 典型算法归位","x":"5.3 典型算法归位 算法 递推 c* 情形 结果 二分 T(n/2)+1 0 二 Θ(log n) 归并 2T(n/2)+n 1 二 Θ(n log n) 快速选择（理想） T(n/2)+n 0 三 Θ(n) 朴素矩阵乘 8T(n/2)+n² 3 一 Θ(n³) Strassen 7T(n/2)+n² ≈2.81 一 Θ(n^{log₂7}) Karatsuba 3T(n/2)+n ≈1.58 一 Θ(n^{log₂3}) 朴素大数乘 4T(n/2)+n 2 一 Θ(n²)（没减 a） Strassen / Karatsuba 都是 减 a ：8→7、4→3。情形一像层层转包——总包自己干的活（f）再努力也改不了大局，成本在最底层无数小工队（叶子）上，唯一有效的是减少每级分包数。情形三反过来：协调工作已经贵过所有叶子，优化 f 才有意义。"},{"a":"s5-4","h":"5.4 主定理不适用时","x":"5.4 主定理不适用时 主定理要求子问题 规模相等 。不等分如 T(n/3)+T(2n/3)+n（快排某些划分）：最短路径 log₃n、最长 log_{3/2}n，每层代价 ≤ n，得 O(n log n)；精确解用 Akra–Bazzi。减法如 T(n−1)+n：展开成三角形 Θ(n²)。子问题个数随 n 变则换元或展开。 兜底是代入法：猜 T(n)≤c n log n，假设对所有更小规模成立，代进递推看能否推出 n 也成立。猜错了归纳会卡，卡在哪通常就提示了正确量级。"},{"a":"s6","h":"空间复杂度与递归栈","x":"06 空间复杂度与递归栈 递归深度是会崩溃的那一维 额外空间不含输入：显式结构和 递归栈 。Linux 默认线程栈 8 MB，深度大约 10⁴～10⁵ 就爆；O(log n) 的递归（n=10⁹ 才 30 层）安全，n=10⁶ 的链表递归必崩。 算法 额外空间 备注 归并（数组 / 链表） O(n) / O(log n) 数组要缓冲；链表改指针 快排（朴素 / 先小后大） O(n) / O(log n) 只递归短边，栈深无条件 log n 堆排 / BFS / DFS O(1) / O(V) / O(V) 深链 DFS 会爆栈 「只递归较短一半、长的改循环」把栈深从 O(n) 压到 O(log n)，与 pivot 质量无关：较短的那半必然 ≤ n/2。这是无条件保证，和随机化 pivot（只降概率）互补。libstdc++ 的 std::sort 还叠了 introsort：递归深度超限就转堆排，把 时间 最坏也拉回 O(n log n)。代码见面试题；栈帧布局见 递归与分治 。 同一问题，用空间买时间（或反过来） 查成员 排序 + 二分 时间 n log n · 额外 O(1) 哈希表 摊销 O(1) · 额外 O(n) 斐波那契 / 路径数 朴素递归 时间指数 · 栈 O(n) DP 表 / 滚动数组 时间 O(n) · 空间 O(n)/O(1) 字符串匹配 每次从头扫 O(n m) · 额外 O(1) KMP / 自动机 O(n+m) · 预处理表 先问「多出来的空间换到的是量级还是常数」。换到量级几乎总值得（指数递归 → DP）；换常数要看 n 和 cache（哈希 vs 排序+二分在 n=64 时后者常更快）。空间也不是免费的：O(n) 额外表可能把工作集挤出 L3，时间反而变差——这又回到 §7。"},{"a":"s7","h":"渐进之外：常数因子","x":"07 渐进之外：常数因子 渐进符号沉默的地方，藏着 10 倍的性能差 前面在用渐进分析。这里讲它 什么时候会骗你 ：RAM 模型假设每次访问同价，今天 L1 ~1 ns、主存 ~100 ns，被算作 O(1) 的一次读内部差 100 倍。"},{"a":"s7-1","h":"7.1 内存墙与 cache line","x":"7.1 内存墙与 cache line CPU 按 64B cache line 整块读。 空间局部性 ：读 a[0]，同 line 的 a[1..15]（4 字节 int）已在 cache，顺序遍历每 16 个元素才 1 次 miss。 硬件预取 检测到顺序模式会提前取下一行，只对可预测访问生效——指针追逐完全无效。 层级 延迟 ~3 GHz 周期 L1 / L2 / L3 ~1 / 4 / 15 ns 4 / 12 / 45 主存 DRAM ~80–100 ns ~300 NVMe SSD ~50–100 µs ~300 000 去图书馆一次借一整格 16 本。数组按序摆，搬一次够读 16 本，管理员还会预取下一格。链表每本写着下一本在哪栋楼——每本跑一趟。渐进分析说两者都是 O(n)。"},{"a":"s7-2","h":"7.2 list 的 O(1) 输给 vector 的 O(n)","x":"7.2 list 的 O(1) 输给 vector 的 O(n) 教科书：中间插入 list O(1)、vector O(n)。几万元素规模下 vector 常快 5–10 倍。list 的 O(1) 前提是 已持有迭代器 ；先找位置就是最慢那种 O(n)（每个节点一次 miss，预取失效）。vector 的 memmove 是连续内存上的 SIMD 批量搬，带宽约 10 GB/s；list 每次插入还要一次 malloc，节点 24–32 字节 vs int 的 4 字节。 std::vector<int> — 连续内存 0..15 16..31 32..47 48..63 1 次 miss 装满 64B，覆盖 16 个 int miss 率 1/16 + 预取几乎全命中 std::list<int> — 散落的节点 val | prev next → val | prev next → val | prev next → val | prev next → miss 率 ≈ 1/1 预取失效：读完当前 才知下一个在哪 节点 24–32B 且地址随机。list 值钱的是迭代器不失效和 splice，不是「插入快」。LRU 才用得上。 同是 O(n) 遍历：vector 每 16 个元素一次 miss，list 每个元素一次。渐进决定量级，常数决定同量级胜负。 vector 中间插入 list 中间插入 找到位置 O(1) 随机访问 O(n) 指针追逐 + n 次 miss 插入本身 O(n) memmove O(1) 改两个指针 搬移速度 SIMD ~10 GB/s — 每元素开销 4 字节（int） 24–32 字节 + 每次 malloc 先用渐进排除 O(n²)，再用测量优化常数。跳过第一步是在给错误算法抛光。 std::list 真正值钱的是：已持有迭代器 且 插删后迭代器/引用仍有效 且 用得上 splice。典型是 LRU：哈希表存 key→list 迭代器，移到表头是真 O(1)。"},{"a":"s7-3","h":"7.3 常数 vs 复杂度（交互）","x":"7.3 常数 vs 复杂度（交互） 常数大的低阶算法，在什么 n 被常数小的高阶反超——这就是 std::sort 小区间切插入排序的依据。插入排序的常数极小（顺序写、零分配、分支可预测），归并要缓冲和两路交错访问；交叉点通常在几十到一两百，所以 introsort 在 ≤16 就停递归，最后再扫一遍插入排序。这个 16 比理论交叉点更小，因为小区间能整块留在 L1。 交叉点计算器 点击场景，看两个算法在哪个 n 处交叉。数字来自典型量级的经验值，重点看结构而非精确值。 插入排序 vs 归并排序 list vs vector 遍历 哈希表 vs 红黑树 Strassen vs 朴素矩阵乘"},{"a":"s8","h":"最坏 / 平均 / 期望 / 摊销","x":"08 最坏 / 平均 / 期望 / 摊销 四种口径，以及为什么线上只看 p99"},{"a":"s8-1","h":"8.1 四种口径的区别","x":"8.1 四种口径的区别 口径 对什么取极值 假设 例子 最坏 所有输入里最差的 无 归并 / 堆排 Θ(n log n) 平均 对输入分布求期望 输入分布 快排在等概率排列下 期望 对算法内部随机数 随机源可靠 随机化快排、跳表、随机哈希 摊销 对操作序列求平均 无 push_back、并查集 快排三种口径 · 同一段代码，输入不同 最好 / 期望 划分接近对半 Θ(n log n) 随机 pivot：对任意固定 输入都有好期望 平均 所有排列等概率 Θ(n log n) 假设在输入上 · 攻击者 可构造最坏排列 最坏 每次切掉 1 个 Θ(n²) 已排序 + 取端点 pivot 摊销管不了这种 平均把运气放在输入上；期望放在算法自己的随机源上。摊销是第三种：对任意操作序列的确定性上界（vector / 并查集）。 随机化不改善最坏（仍 Θ(n²)），但让最坏从「攻击者可控」变成「不可控」——这是安全性质，不是性能性质。跳表用随机层高代替红黑树旋转、哈希用随机种子抵御 Hash-DoS，都是同一思路。归并/堆排没有这条担忧：它们的最坏就是 Θ(n log n)，口径只有一种。"},{"a":"s8-2","h":"8.2 为什么线上看 p99","x":"8.2 为什么线上看 p99 扇出放大尾延迟：一个用户请求并行调 n 个下游并等全部返回，可见延迟 = max(所有调用)。单次 p99=L 时，P(至少一次超过 L)=1−0.99^n；n=100 已经约 63%。算法课看均值，生产看尾部——摊销 O(1) 的 push_back 均值完美，那次 O(n) 扩容落在谁头上谁就是 p99。 扇出 100 · 用户延迟 = max(下游) · 约 63% 请求会吃到 p99 绿 = 全中 p99 以内 红 = 至少一个下游超 L n=10 → 9.6% n=100 → 63% n=1000 → ≈100% 选型：reserve 到峰值；宁要稳定 O(log n) 也不要会抖的摊销 O(1)；贵操作切碎（增量 rehash） 均值漂亮救不了 p99 那次 O(n) 扩容落在谁头上谁就是尾 完整测法见 低延迟编程 。对冲请求（等到 p95 再向副本发一份）能把扇出场景的 p99 拉下来，代价是约 5% 额外负载。 预分配 ：启动时 reserve 到峰值，摊销 O(1) 变成最坏 O(1)。 宁要稳定的 O(log n) ：实时系统常用平衡树而不是哈希——树每次都是 log n，哈希 rehash 会抖。 把贵操作切碎 ：Redis 增量 rehash、分代 GC，一次长停顿变成多次短停顿。 运行期零 malloc ：对象池 / arena / 固定环形缓冲。 malloc 自身的尾延迟就不受控。"},{"a":"s9","h":"P、NP 与工程含义","x":"09 P、NP 与工程含义 当问题本身就是难的，该怎么办 前面问「算法多快」。复杂度类问「问题有没有快算法」。NP-hard 是止损信号：别再找精确多项式解，改近似或启发式。 工程上只要记住包含关系，不必卷证明 P 能高效解 NP · 能高效验 NPC 彼此等价最难 NP-hard 可在 NP 外 （停机问题） NP 不是 non-polynomial，是 Nondeterministic Polynomial。P ⊆ NP；P=NP 未知。 类 直觉 例子 P 能高效解出来 排序、最短路、最大流 NP 能高效验算 SAT、TSP 判定版 NP-完全 NP 里最难、彼此可归约 3-SAT、团、顶点覆盖 NP-hard 至少一样难，不必属于 NP TSP 优化版、停机问题 填好的数独几分钟能验（NP）；空盘广义 n×n 数独有没有快算法未知。NP-完全再加一层：这些问题可以互相「翻译」——攻破 SAT 等于攻破全部 NP，所以它是实用的止损信号：几十年里没人拿下其中任何一个，你手上这个大概也不会例外。证明 NP-hard 的标准姿势是归约：把已知难题 X 的实例多项式变换成你的问题 Y 的实例，于是「Y 能快解 ⇒ X 也能快解」。方向反了只说明 Y 不比 X 难，是另一回事。 遇到 NP-hard 的五条出路 策略 做法 代价 实例 近似 放弃最优，换有保证的比 质量有界损失 顶点覆盖 2-近似、集合覆盖 ln n 启发式 经验规则，跑得快 无质量下界 退火、贪心 + 局部搜索 FPT 抓住小参数 k 只在 k 小时有效 O(f(k)·poly(n))、树宽有界 精确 + 剪枝 分支限界 / ILP 最坏仍指数 Gurobi、SAT 求解器 改问题 放松成 P 里的近亲 不再是原题 整数规划 → LP 松弛取整 先确认业务真要最优解。「1000 任务最优调度」是 NP-hard，「95% 在 SLA 内 + 最坏不超过 2 倍」往往贪心就够。n 常年 ≤30 时 ILP 求解器几毫秒就能给最优，标签吓不死人。结果要对外承诺（计费、合规）→ 用有界近似比；只影响内部效率 → 启发式加监控；有几小时离线窗口 → 直接上求解器，通常比自己写遗传算法强。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 O、Θ、Ω 有什么区别？为什么说「快排是 O(n log n)」不算完整回答？ O 是 上界 （f 增长不快于 g），Ω 是 下界 ，Θ 是两者同时成立即 确界 。三者都带「存在常数 c 和阈值 n₀」的前缀，用来忽略常数因子和小规模行为。 「快排是 O(n log n)」有两个问题： 缺口径 没说是最坏、平均还是期望。快排的 最坏是 Θ(n²) （例如已排序输入 + 取端点为 pivot），平均才是 Θ(n log n)。不说口径等于隐瞒了退化风险。 用 O 而非 Θ O 只给上界，所以「快排是 O(n²)」也是真话——但没信息量。要表达「就是这个量级」应该用 Θ。 完整答法： 平均/期望 Θ(n log n)，最坏 Θ(n²)；随机化 pivot 或 median-of-medians 后最坏可控。 基础 下面两段代码的复杂度分别是多少？为什么？ // A for ( int i = 1 ; i <= n; i *= 2 ) for ( int j = 0 ; j < n; ++j) work (); // B int j = 0 ; for ( int i = 0 ; i < n; ++i) while (j < n && ok (i, j)) ++j; A 是 Θ(n log n) ：外层 i 每次翻倍，只有 ⌊log₂n⌋+1 层，每层内层跑 n 次。 B 是 Θ(n) ：虽然是两层，但 j 在整个过程中 只增不减 ，最多前进 n 次。外层 n 次 + 内层总共 n 次 = 2n。 通用方法 不要数循环层数，数 循环体的总执行次数 。B 用的是「势能/记账」思路：找一个单调变化的量（这里是 j），它的总变化量就是内层的总代价上界。双指针、滑动窗口、单调栈的 O(n) 都靠这个论证。 基础 为什么 std::vector::push_back 是 O(1)，尽管它有时要搬走整个数组？ 因为它是 摊销 O(1) 。容量每次翻倍，所以做 n 次 push_back ，扩容发生在第 1、2、4、…、2^k 次，搬移总量是等比数列： 1 + 2 + 4 + ... + 2^k = 2^(k+1) − 1 < 2n 总代价 < n（写入）+ 2n（搬移）= 3n，除以 n 次操作得摊销 O(1)。 关键 公比 > 1 的等比数列，其和是 最后一项的常数倍 。所以「所有历史扩容之和」不超过「最后一次扩容」的两倍。 反例 如果每次容量 +1（加性增长），搬移总量变成 1+2+…+n = Θ(n²)，摊销就是 O(n)。 增长必须是乘性的。 要注意摊销 O(1) 不代表每次都快——单次仍可能 O(n)，这是尾延迟来源，低延迟场景要 reserve 规避。 基础 n = 10⁵ 和 n = 5000 分别提示你用什么复杂度的算法？ 基准：现代 CPU 单线程每秒约 10⁸～10⁹ 次简单操作 ，常见时限 1 秒。 n 可行复杂度 常见算法 10⁸～10⁹ O(n) 单遍扫描、双指针、计数排序 10⁵～10⁶ O(n log n) 排序、堆、平衡树、线段树 5000 O(n²) 双层循环 DP、Floyd 的一层、朴素最近对 300 O(n³) Floyd 全源最短路、区间 DP 20～25 O(2ⁿ) 状压 DP、子集枚举 ≤ 11 O(n!) 全排列枚举 反向使用 数据范围 就是出题人给的复杂度暗示 。看到 n≤20 基本是状压/回溯；看到 n≤10⁵ 但要求多次查询，大概率要预处理成 O(1)/O(log n) 查询。面试时把这个推理说出来，比直接报答案更能体现思路。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 「摊销复杂度」和「平均复杂度」的本质区别是什么？举例说明。 摊销不依赖任何概率假设，平均依赖输入分布假设。 摊销 对 任意 长度为 m 的操作序列，总代价 ≤ m·T(n)。这是确定性上界——不存在「运气不好」的序列让 n 次 push_back 超过 O(n)。 平均 对输入分布求期望。快排的 Θ(n log n) 假设「所有排列等概率」； 存在具体输入让它退化成 Θ(n²) ，只是在假设的分布下概率很低。 工程后果 平均复杂度 可被攻击 。2011 年的 Hash-DoS 就是构造大量哈希冲突的 key，把哈希表的 O(1) 拖成 O(n)，几十 KB 请求打满一个核。修复不是换数据结构，而是给哈希函数加 随机种子 （SipHash），让攻击者算不出最坏输入。 还有第三种：期望 随机化算法的「期望 O(n log n)」把不确定性放在 算法自己的随机源 上，而不是输入上。所以对任意固定输入都有好的期望性能——这是安全性质。随机化快排、跳表都属于这一类。 中级 用主定理分析 T(n)=2T(n/2)+n 、 T(n)=T(n/2)+1 、 T(n)=7T(n/2)+n² 。 对 T(n)=a·T(n/b)+f(n) ，先算临界指数 c* = log_b a ，再把 f(n) 和 n^c* 比大小。 递推式 c* = log_b a f(n) vs n^c* 情形 结果 2T(n/2)+n log₂2 = 1 n = n¹，相等 二（k=0） Θ(n log n) T(n/2)+1 log₂1 = 0 1 = n⁰，相等 二（k=0） Θ(log n) 7T(n/2)+n² log₂7 ≈ 2.807 n² < n^2.807 一 Θ(n^log₂7) ≈ Θ(n^2.807) 三者分别是归并排序、二分查找、Strassen 矩阵乘法。 直觉记法 n^c* 是 叶子的总代价 ，f(n) 是 根的代价 。谁大听谁的（情形一 / 三），一样大就多乘一个 log n（情形二）。Strassen 落在情形一说明它的收益全部来自「把 8 次子乘降到 7 次」（降 a），而不是优化合并。 情形三别忘正规性条件 还要满足 a·f(n/b) ≤ c·f(n) （某个 c<1），保证各层代价按几何级数递减。多项式形式的 f 基本都满足。 中级 递归实现的快排，为什么最坏栈深是 O(n)？怎么无条件压到 O(log n)？ 最坏 O(n) 的来源 ：pivot 每次都取到极值（如已排序数组取末位），划分变成「1 个元素 + n−1 个元素」，递归链长 n。Linux 默认线程栈 8 MB，n 到 10⁵ 量级就 SIGSEGV。 修复不是「选更好的 pivot」 ——那只降低概率，不给保证。正确做法是两个技巧叠加： 只递归小半边 比较两侧长度， 对较短的那半递归 ，较长的那半通过修改 lo/hi 转成循环（手工尾递归消除）。递归的那半必然 ≤ n/2，所以栈深 ≤ log₂n。 while (lo < hi) { int p = partition (a, lo, hi); if (p - lo < hi - p) { qsort (a, lo, p- 1 ); lo = p+ 1 ; } else { qsort (a, p+ 1 , hi); hi = p- 1 ; } } 为什么这是无条件保证 它 不依赖 pivot 质量 。哪怕每次划分都极度不均，「较短的一半」也一定不超过一半，log 上界照样成立。这是确定性的，和随机化 pivot（概率性的）互补——工程上两个都要。 libstdc++ 的 std::sort 同时用了这个模式和「深度超限转堆排」（introsort），后者把 时间 最坏也拉回 O(n log n)。 中级 vector 的增长因子选 2× 还是 1.5×？各自的理由是什么？ 两者都给出摊销 O(1)（只要是乘性增长），差别在常数与内存行为。libstdc++ 用 2×，MSVC STL 和 folly::fbvector 用 1.5×。 维度 2× 1.5× 每元素摊销搬移次数 约 1 次 约 2 次 内存浪费上限 50% 33% 扩容次数（到 n） log₂n ≈1.7 log₂n 能否复用已释放空间 不能 可以 1.5× 最有意思的理由 用 2× 时历史块之和 1+2+4+…+2^(k−1) = 2^k − 1 ， 永远差 1 个单位 不够放下一块，所以分配器没法把前面释放的空间拼起来复用，堆持续向高地址增长。1.5× 时历史块之和最终会超过下一块所需，有机会原地复用，长期碎片更少。 实践要点 reserve 之后超出预留量，容量仍按增长因子从 当前值 翻倍，不会退回按需精确分配。 shrink_to_fit 是非绑定请求，标准只要求「不增加容量」。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 教科书说链表中间插入是 O(1)、vector 是 O(n)，为什么实测常常 vector 快 5–10 倍？ 因为渐进分析基于 RAM 模型，假设 每次内存访问代价相同 。真实机器上 L1 命中约 1 ns、主存约 100 ns，差两个数量级——「访问一个内存单元」这个被算作 O(1) 的操作内部有 100 倍浮动。 ① O(1) 的前提没成立 list 的 O(1) 插入前提是 你已经持有那个位置的迭代器 。若需先找位置，那 O(n) 的指针追逐不但没省掉，还是最慢的一种 O(n)：每个节点一次 cache miss，且 硬件预取完全失效 （读完当前节点才知道下一个地址在哪）。 ② 两个 O(n) 的常数差 16 倍以上 vector 顺序访问每 64 字节 cache line 覆盖 16 个 int，miss 率 1/16，且预取器几乎全命中。list 每个节点一次 miss，miss 率约 1/1。 ③ memmove 极快 vector 的 O(n) 搬移是连续内存上的 SIMD 批量拷贝，带宽约 10 GB/s；而 list 每次插入还要一次 malloc ，节点开销 24–32 字节 vs int 的 4 字节。 list 真正的适用场景 已持有迭代器 且 需要迭代器/引用在插删后保持有效 且 用得上 splice 常数时间拼接。典型是 LRU 缓存：哈希表存「key → list 迭代器」，移动到表头是真 O(1) 且不失效其他迭代器。 正确的结论 不是「渐进分析没用」，而是 渐进分析决定 n 大时的量级（不可违抗），常数因子决定同量级内的胜负（可优化 10 倍） 。先用渐进排除 O(n²)，再用测量优化常数。跳过第一步去调常数，是在给错误的算法抛光。 高级 什么是势能法？用它证明 vector::push_back 的摊销 O(1)。它比聚合法强在哪？ 势能法定义一个函数 Φ 度量数据结构的「紧张程度」，把摊销代价定义为 实际代价 + 势能变化 ： 摊销代价 = 实际代价 + Φ(after) − Φ(before) 若 Φ(初始)=0 且 Φ(任何时刻) ≥ 0，则 Σ摊销 = Σ实际 + Φ(末) − Φ(初) ≥ Σ实际 → 合法上界 对 vector 取 Φ = 2·size − capacity （刚扩容后 size=capacity/2 时 Φ=0，写满时 Φ=capacity）： 不扩容 实际代价 1；size+1 使 Φ 增加 2 → 摊销 = 1+2 = 3 。 触发扩容 size=capacity=c。实际代价 = c 次搬移 + 1 次写入 = c+1。扩容后 capacity=2c、size=c+1，新 Φ = 2(c+1)−2c = 2；旧 Φ = 2c−c = c。ΔΦ = 2−c。摊销 = (c+1)+(2−c) = 3 。 两种情况都是常数，故摊销 O(1)。 Φ 的作用是把贵操作的代价提前存起来，贵操作发生时释放势能正好抵消。 比聚合法强在哪 聚合法要求你能算出整个序列的总代价闭式解，只适用于操作模式规整的情况。势能法是 局部 论证——只需对每种操作单独验证一个不等式，不必知道操作序列长什么样。所以它能处理操作交错、类型多样的结构：并查集的 α(n)、斐波那契堆的 O(1) 摊销 decrease-key、splay 树的 O(log n) 摊销，都只能用势能法证。代价是 找到合适的 Φ 是全部难点 ，没有机械方法。 高级 为什么线上系统看 p99 而不是均值？这对算法与数据结构选型有什么具体影响？ 因为 请求扇出会放大尾延迟 。一个用户请求并行调用 n 个下游并等全部返回时，用户可见延迟 = max(所有调用延迟)： P(至少一次超过 p99 阈值) = 1 − 0.99^n n=10 → 9.6% n=100 → 63.4% n=1000 → 99.996% 也就是说，扇出 100 次时， 大多数用户请求都会撞上至少一个 p99 慢响应 。均值再漂亮也没用。 对选型的四条具体影响： 预分配 启动时 reserve 到峰值，把摊销 O(1) 变成 最坏 O(1)。摊销 O(1) 在均值上完美，但那次 O(n) 扩容落在哪个请求上，那个请求就是 p99。 宁要稳定 O(log n) 很多实时系统用平衡树而非哈希表：红黑树每次查找都是稳定的 log n，哈希表 rehash 时会突然长停顿。 可预测性有时比平均速度更值钱。 分摊贵操作 把一次长停顿切成很多次短停顿：Redis 的增量式 rehash（每次操作搬几个 bucket）、分代 GC、分批 compaction。 消除不可预测的分配 对象池 / arena / 固定容量环形缓冲区，让运行期零 malloc 。 malloc 本身的尾延迟就不受控。 再往上一层 算法层面压不下去时，靠架构： 对冲请求 （hedged request，等到 p95 还没回就向副本再发一份，取先到的）能把扇出场景的 p99 显著拉低，代价是约 5% 的额外负载。这是「用吞吐换延迟」的典型交易。 高级 你负责的服务里有个需求被判定为 NP-hard，接下来怎么做？ NP-hard 的工程价值是 止损信号 ：停止寻找精确多项式算法。但在换算法之前，先做一件更重要的事。 ① 先质疑需求 很多 NP-hard 是 把业务约束抽象过头 的结果。「给 1000 个任务排最优调度」是 NP-hard，但业务上往往只要「95% 任务在 SLA 内 + 最坏不超过 2 倍」——那就变成一个贪心能解的问题。 动手找算法之前，先确认真的需要最优解。 ② 量一量真实输入规模 NP-hard 说的是最坏情形随 n 增长的行为。如果生产数据里 n 常年 ≤ 30，分支限界或 ILP 求解器几毫秒就返回最优解，那这个「NP-hard」在工程上根本不是问题。 先看数据，再谈复杂度类。 确认必须放弃精确解后，按「保证强度」排序的五条出路： 策略 保证 实例 近似算法 有界近似比 顶点覆盖 2-近似、集合覆盖 ln n-近似 限定参数（FPT） k 小时精确 O(f(k)·poly(n))，树宽有界的图 精确 + 剪枝 最优解，时间不保证 分支限界、ILP（Gurobi/CBC）、SAT 求解器 启发式 无保证 模拟退火、遗传算法、贪心 + 局部搜索 松弛改问题 不再是原问题 整数规划 → LP 松弛后取整 选型的判断标准 问「解差一点的代价是多少」。 结果要对外承诺 （计费、合规、资源配额）→ 用近似算法，因为你需要一个能写进文档的界。 结果只影响内部效率 （缓存预热顺序、任务打包）→ 启发式够用，把精力花在监控实际解质量上。 离线批处理、有几小时窗口 → 直接上 ILP 求解器，通常能拿到最优或近最优，而且比自己写启发式省事得多。 别掉进的坑 自己从零写启发式（尤其是遗传算法这类）往往打不过成熟求解器，还留下无人能维护的代码。 先试通用求解器，打不过再自己写。 ➡️ 递归与分治 🔀 排序全景 🎯 二分查找 ⚡ 性能优化 📚 目录 🏠 主页 复杂度与分析 — Big-O 严格定义 · 摊销分析三法 · 递归树与主定理 · 常数因子与 cache 局部性 · P 与 NP — Kavin Tech Notes · 2026"}]},{"u":"algorithms/dynamic_programming.html","t":"动态规划","c":"算法与数据结构","s":[{"a":"","h":"","x":"动态规划 · 五步方法论 / LIS 的 O(n log n) / 背包全家桶与循环顺序 / 区间与树形与状压与数位 DP / 滚动数组与斜率优化 Kavin Tech Notes ← 主页 目录 30 min ◐ 动态规划 两个前提，一句本质 与其他章节的关系 设计 DP 的五步 五步方法论 状态设计的三个信号 线性 DP LIS 与它的 O(n log n) LCS 与编辑距离 Kadane：最大子数组和 背包全家桶 01 背包与循环顺序 完全背包 多重背包与二进制拆分 分组背包与「恰好装满」 组合数 vs 排列数 区间 DP 树形 DP 状压 DP 数位 DP 优化四件套 滚动数组 单调队列优化 斜率优化 决策单调性与分治 优化对照（交互） 陷阱清单 面试题集（12 道） 11.1 基础 11.2 中级 11.3 高级 Algorithms · Dynamic Programming · 2026 动态规划 DP 难在 「状态该怎么定」 。先给可复用的五步流程，再用图把重叠子问题 DAG、最优子结构、从记忆化到递推、背包滚动、LCS 表、LIS 二分、区间切点、树形子树和状压走一遍；代码只留一维滚动、一张二维背包表、一副区间骨架和一副树形骨架。 核心难度 ★★★★★ 面试出现率 极高 阅读时长 ~30 min 前置 递归 · 单调队列 五步方法论 LIS 的 O(n log n) 背包全家桶 区间 / 树形 / 状压 / 数位 四种优化"},{"a":"s1","h":"两个前提，一句本质","x":"01 两个前提，一句本质 DP 不是技巧，是一种「把指数搜索空间折叠成多项式」的手段 DP 适用的两个前提 ： 最优子结构 ：最优解可以由子问题最优解拼出来——这是能「递推」的前提。 重叠子问题 ：同一个子问题会被反复算到——这是「记下来」有价值的前提。 只有最优子结构、没有重叠 → 分治 。两者都有 → DP 。没有最优子结构 → 搜索 ，或换一个有最优子结构的建模。 重叠子问题：递归树是指数，记下来就变成 DAG 暴力递归 = 树（重复算） f(5) f(4) f(3) f(3) f(2) f(2) f(1) 黄块是同一子问题被算了多次 记忆化 / 递推 = DAG（每个点一次） f(5) f(4) f(3) f(2) f(1) 边 = 依赖；共享节点只填一次 没有重叠，记表没收益（那是分治）。有重叠，指数树被折叠成多项式个点——这就是 DP 的 DAG。 归并排序也把问题拆成子问题，但每次切的区间都不一样， 没有重叠 ，记表等于白记。斐波那契、背包、最短路则相反：递归树里同一节点出现指数次。DP 做的事就是把这棵树认成 DAG，每个点算一次。状态设计回答的是： 图上的一个点要携带哪些信息，出边的决策才不依赖更早的路径？ 最优子结构：最后一步把问题拆开，历史可以被忘掉 …怎么到 第 n−2 级 dp[n−2] 最优已收好 dp[n−1] 最优已收好 dp[n] = dp[n−1]+dp[n−2] 状态设计 = 要记住哪些信息，后面的决策才不依赖更早的历史？ 多记 → 状态数爆炸；少记 → 后面决策缺信息，答案错。 爬楼梯最后一步走 1 或 2；01 背包最后一件选或不选——都是「站在最后一步」拆转移。 到第 n 级只需两项，不需要整条路线。指数路线被折叠成 n 个状态。 暴力是穷举每一条上楼路线。DP 说：第 10 层只取决于第 9 层和第 8 层的最优， 不取决于我是怎么到第 9 层的 。按层往上各记一个数即可。少记一层信息（比如忘了「最后一步走了几级」），后面就拼不出转移；多记一层（比如把整条路径当状态），点数立刻爆炸。 同一张 DAG：记忆化按需填，递推按拓扑序填满 自顶向下 · 记忆化 f(n) → 查表 → 未算则递归 f(n−1)、f(n−2) · 和递归定义一一对应，好写 · 只算真正需要的状态 · 栈深 O(递归深度)，稀疏时省 建议：先写这个，确认对了再改迭代 直接写递推容易在循环顺序上静默算错 自底向上 · 递推 for i = 1..n: dp[i] = dp[i−1] + dp[i−2] · 无栈，cache 友好 · 容易滚动压缩空间 · 会填掉用不到的格子 代价：必须先想清拓扑序 阶段维必须在最外层（§4、§5） 先记忆化（状态+转移不易想错），再递推（消栈深），最后才滚动。循环顺序错不报错、只给错答案。 记忆化递归 迭代 DP 栈深 O(深度) 0 无用状态 不算 全表都填 压缩空间 难 滚动数组 稀疏空间 哈希表存 浪费 记忆化在状态空间稀疏时（很多格子永远走不到）更省；迭代在稠密时更快、也好滚空间。生产里 n 大且深度不可控，优先迭代。DAG 上的 DP 必须按拓扑序填；Floyd / Bellman-Ford 本质也是 DP，阶段维分别是「允许的中间点」和「松弛轮数」。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 递归与分治 —— 「子问题是否重叠」是分治与 DP 的分界线；记忆化在那里首次出现 单调栈与前缀和 —— §9.2 的单调队列优化、前缀和加速转移 图遍历与拓扑排序 —— DAG 上的 DP 必须按拓扑序转移 最短路与最小生成树 —— Floyd 和 Bellman-Ford 本质都是 DP 贪心与回溯 —— 贪心是「不需要记状态的 DP」；没有最优子结构时退回回溯 前向与反向传播 —— 反向传播本质是计算图上的 DP"},{"a":"s2","h":"设计 DP 的五步","x":"02 设计 DP 的五步 把「灵光一闪」变成可执行的流程"},{"a":"s2-1","h":"2.1 五步方法论","x":"2.1 五步方法论 ① 定义状态：dp[...] 表示什么？ 关键问题：「要记住哪些信息，才足以让后面的决策不依赖更早的历史？」多记浪费，少记就错 ② 写转移：dp[当前] 由哪些 dp[更早] 得到？ 技巧：站在「最后一步」问「最后这一步有几种选择」——每种选择对应一项转移 ③ 定初值与边界：最小的子问题是什么？ 高发点：「无解」用什么表示（−∞ / +∞ / −1）？「空」是合法状态吗？ ④ 定循环顺序：保证「用到的 dp 值都已算好」 规律： DP 的「阶段」必须在最外层 ；滚动数组时正序/倒序决定语义（§4.1） ⑤ 优化：滚动数组 / 单调队列 / 斜率优化 / 决策单调性（§9）—— 最后才做，不要提前优化 五步的顺序不能乱：状态定不对，后面全是白费；优化必须放最后，否则会掩盖状态设计的问题。拿到新题先看 n 反推复杂度，再按这五步走；直接写自底向上最容易在循环顺序上静默算错。 第 ② 步：站在「最后一步」问有几种选择 。爬楼梯走 1 或 2；01 背包选或不选；LCS 末字符相等则对角 +1、不等则取上/左；区间 DP 枚举分界点 k。正向想不通就反向（戳气球枚举最后戳的）。这一步把「想方程」变成「枚举最后一步」，比凭直觉可靠。"},{"a":"s2-2","h":"2.2 状态设计的三个信号","x":"2.2 状态设计的三个信号 状态维数几乎都能从题面关键词读出来。下表是高频对应；真正容易错的是「漏了一维」——答案会错且不报错，所以不确定时宁可多加。 题目里出现 状态里可能需要加 DP 族 「前 i 个」「以 i 结尾」 一维下标 i 线性 DP 「容量 / 预算 / 总和不超过 W」 第二维 j = 已用容量 背包 「区间 [l, r] 上的操作」「合并相邻」 两维 (l, r) 区间 DP 「树上的子树」 (节点, 该节点的选择) 树形 DP 「n ≤ 20」「哪些已被使用」 一个 位掩码 状压 DP 「统计 [L, R] 内满足条件的数」 (第几位, 是否贴上界, 附加信息) 数位 DP 「最多操作 k 次」「有 k 次机会」 加一维 k 分层 DP 「持有 / 不持有」「冷却中」 加一维 状态机 状态机 DP（股票买卖） n 的范围反推复杂度 （见 复杂度与分析 ）：n ≤ 20 → 状压或回溯；n ≤ 500 → O(n³) 区间 / Floyd；n ≤ 5000 → O(n²) 线性 / LCS / 编辑距离；n ≤ 10⁵ → 必须 O(n log n) 或 O(n)，说明 §9 的优化或换算法族；值域大但位数少（≤10¹⁸）→ 数位 DP。先反推「状态大概几维」，比盲想快。 维度够不够的判据 ：给定当前状态，后续最优决策是否完全确定、不再依赖更早的历史？还依赖就说明漏维。 股票含冷却期：只用 dp[i] 不够，还要「持有吗 / 是否刚卖出」→ 状态机三维。 打家劫舍 III：只用 dp[u] 不够，父亲的决策依赖 u 有没有被选 → dp[u][0/1] 。 最多交易 k 次：还要记已用次数。 自检方法：构造两个「当前状态相同但历史不同」的输入，看后续最优是否一样。不一样 → 加维。多加只是慢，少加是错；跑通后再想哪一维能滚掉或由其他维推出。"},{"a":"s3","h":"线性 DP","x":"03 线性 DP 一维滚动一张；LIS / LCS 用图表；Kadane 是压到 O(1) 一维 DP 的最短形态是爬楼梯（斐波那契）： dp[i] = dp[i−1] + dp[i−2] 。记忆化是按需填这张 DAG；递推按 i 从小到大填满；滚动后只留两项。下面这份是滚动版——§1 的图已经说明它和记忆化是同一张图的两种填法。 // 爬楼梯：到 i 级的方案数。滚动数组只留两项 int climb ( int n) { if (n <= 2 ) return n; int a = 1 , b = 2 ; for ( int i = 3 ; i <= n; ++i) { int c = a + b; a = b; b = c; } return b; }"},{"a":"s3-1","h":"3.1 LIS 与它的 O(n log n)","x":"3.1 LIS 与它的 O(n log n) tails[k] = 长度 k+1 的上升子序列的最小可能结尾 · 例 [3, 1, 4, 2] 读 3 3 接长 读 1 1 lower_bound 替换 3→1（顶更矮） 读 4 1 4 比所有塔顶高 → 接长，len=2 读 2 1 2 替换 4→2；长度仍是 2 三条必须记住 · tails 递增（否则与「最小结尾」矛盾） · 替换不缩短已有长度，只让将来更好接 · tails 不是答案序列 它只记「各长度的最小结尾」 要输出序列须另记前驱 严格 → lower_bound；非严格 → upper_bound 朴素 O(n²)：dp[i]=max{dp[j]+1 : a[j]<a[i]} 像整理不同高度的塔：新积木要么搭更高的塔，要么把某座塔顶换成更矮的。tails 记的是可能性，不是某一座具体的塔。 为什么 tails 递增、替换安全 ：若 tails[k] ≥ tails[k+1] ，长度 k+2 的序列去掉末项就得到长度 k+1、结尾更小的序列，与「最小结尾」矛盾。把 tails[k] 换成更小的 x，已有长度不变，只是将来更好接。要输出具体序列必须另记前驱——这是本题最常见的误解。严格上升用 lower_bound （相等则替换），非严格用 upper_bound （相等可接长）；最长不下降 / 下降 / 不上升靠取反数组和换 bound 组合，写之前先确认题面。"},{"a":"s3-2","h":"3.2 LCS 与编辑距离","x":"3.2 LCS 与编辑距离 LCS 表 dp[i][j] = s 前 i 与 t 前 j 的公共子序列长度 · s=ABC t=AC ε A C ε A B C 0 0 0 0 1 1 0 1 1 0 1 2 转移（看最后两个字符） 相等：dp[i][j] = dp[i−1][j−1] + 1 对角来的，绿格 A–A、C–C 不等：max(上, 左) —— 删 s 或删 t 编辑距离同形：替换/删/插三项 +1 表开 (m+1)×(n+1)，第 0 行/列是空串哨兵 字符用 s[i−1]，省掉一整类边界特判 答案在右下角。下标偏移 1 与前缀和 S[0]=0、二分半开区间是同一类设计：多一个哨兵，换掉边界判断。 编辑距离同形：相等则抄对角；不等则替换 / 删 s / 插 t 三项取 min 再 +1。第 0 行是「把空串变成 t 的前缀」（全插），第 0 列是全删。要还原具体 LCS / 操作序列就不能滚动，每个格子要记「从哪来」。"},{"a":"s3-3","h":"3.3 Kadane：最大子数组和","x":"3.3 Kadane：最大子数组和 Kadane ： dp[i] = max(a[i], dp[i−1]+a[i]) ，只依赖前一项 → 压成两个变量。 全负数 时初值必须是 a[0] 不能是 0——否则「不允许空子数组」时答案会变成 0。环形 = max(普通 Kadane, 总和 − 最小子数组)，并处理「全负时后者退化成空」。另一种视角 max(prefix[j] − min_{i<j} prefix[i]) ，可推广到「长度 ≤ k」（单调队列维护窗口内最小前缀和，§9）。一个问题有两种等价表述时，往往能各自推到不同变体。"},{"a":"s4","h":"背包全家桶","x":"04 背包全家桶 循环顺序为什么能改变语义"},{"a":"s4-1","h":"4.1 01 背包与循环顺序","x":"4.1 01 背包与循环顺序 // 01 背包完整二维：dp[i][j] = 前 i 件、容量 j 的最大价值 int knapsack ( const std::vector< int >& w, const std::vector< int >& v, int W) { int n = ( int )w. size (); std::vector<std::vector< int >> dp (n + 1 , std::vector< int >(W + 1 , 0 )); for ( int i = 1 ; i <= n; ++i) for ( int j = 0 ; j <= W; ++j) { dp[i][j] = dp[i - 1 ][j]; if (j >= w[i - 1 ]) dp[i][j] = std:: max (dp[i][j], dp[i - 1 ][j - w[i - 1 ]] + v[i - 1 ]); } return dp[n][W]; } 二维表：dp[i][j] 只看上一行 → 可滚成一行。例 w=2, W=5 容量 j 0 1 2 3 4 5 i−1 行 · · j−w · j · i 行 新 滚成一行之后 不选 = 同槽旧值；选 = 左移 w 格的旧值 倒序 j=W→w：先写大下标 左边 j−w 仍是上一件的值 → 01 正序会怎样？j−w 已被本轮写过 → 同一件被用了两次 → 完全背包 要还原方案就别滚动（旧行被覆盖）。「恰好装满」只改初值：dp[0]=0，其余 −∞（别用 INT_MIN，加法会溢出）。 分组背包：组 → 容量倒序 → 物品最内层。物品提到外面 = 每组可选多个。 绿格与蓝格都在上一行。压成一行后，必须先更新大容量，才能保证读到的仍是「不含本件」的旧值。 滚动数组下，j 的遍历方向 直接决定了物品能不能重复选 01 背包： j 倒序 （W → w[i]） dp[j] = max(dp[j], dp[j − w[i]] + v[i]) 倒序时 dp[j − w[i]] 还没被本轮更新过 ⟹ 它代表「 上一轮 （不含第 i 个物品）」的值 ⟹ 每个物品最多选 1 次 完全背包： j 正序 （w[i] → W） dp[j] = max(dp[j], dp[j − w[i]] + v[i]) 正序时 dp[j − w[i]] 已被本轮更新过 ⟹ 它已经可能含第 i 个物品 ⟹ 同一物品可以重复选 外层物品、内层容量 → 组合数 每种物品只在它自己那一轮被考虑 ⟹ (1,2) 和 (2,1) 视为 同一种 方案 「有多少种 组合 凑出金额 W」用这个 外层容量、内层物品 → 排列数 每个容量下都重新考虑所有物品 ⟹ (1,2) 和 (2,1) 算 两种 方案 「有多少种 排列 （顺序不同算不同）」用这个 背包的两组循环顺序控制两件独立的事： j 的方向 决定能否重复选， 物品/容量谁在外层 决定组合还是排列。 倒序 = 01，正序 = 完全 ：不是口诀，是「下标更小的槽是旧值还是新值」。一维数组把上一轮和本轮压在同一块内存：倒序时 dp[j−w] 还没被本轮写过，仍是「不含本件」；正序时它已被本件更新过，于是同一件可以再选。识别信号：每种一件 → 01；任意多次 → 完全（零钱兑换、单词拆分、完全平方数）；每种 k 个 → 多重。"},{"a":"s4-2","h":"4.2 完全背包","x":"4.2 完全背包 循环与 01 只差 j 正序 。图里绿框已经画清： dp[j−w] 已被本轮更新，同一件可以再选。典型伪装：零钱兑换（最少硬币 / 方案数）、完全平方数、单词拆分。读题先找「每种能不能用多次」——它决定循环方向，不决定你用二维还是一维。"},{"a":"s4-3","h":"4.3 多重背包与二进制拆分","x":"4.3 多重背包与二进制拆分 做法 复杂度 思路 朴素 O(W · Σkᵢ) 枚举取 0..k 个 二进制拆分 O(W · Σlog kᵢ) 1,2,4,…,剩余 → 转 01。能且只能凑出 0..k 单调队列 O(W · n) 按 j mod w 分组求窗口 max 二进制拆分与快速幂、稀疏表、倍增是同一思想：把 0..k 编成 log k 个独立 0/1 选择。 1,2,4,…,2^t,r 能且只能凑出 0..k——前半段是二进制表示，加上剩余 r 刚好盖到 k，总和又恰好是 k 所以不会超。k 很大时改用单调队列：按 j mod w[i] 分组，组内变成窗口 max。"},{"a":"s4-4","h":"4.4 分组背包与「恰好装满」","x":"4.4 分组背包与「恰好装满」 三层顺序： 组 → 容量倒序 → 物品最内层 。物品提到外面 = 每组可选多个，语义立刻错——又是循环顺序承载语义。恰好装满只改初值：不超过全 0（空背包合法）；恰好求最大则 dp[0]=0 其余 −∞（不可达不会被当成解）；恰好求最小则其余 +∞；求方案数 dp[0]=1 。−∞ 用 INT_MIN/2 或 -1e9 ，别用 INT_MIN ， dp[j−w]+v 会有符号溢出变成大正数。"},{"a":"s4-5","h":"4.5 组合数 vs 排列数","x":"4.5 组合数 vs 排列数 看 [1,2] 与 [2,1] 算不算两种。 物品在外 = 每种物品只在自己那一轮被考虑 = 组合（零钱兑换 II）； 容量在外 = 每一步都能重选所有物品 = 排列（组合总和 IV 是命名陷阱；爬楼梯每次走 1 或 2、顺序不同算不同，也是排列，就是斐波那契）。上图黄/紫两框即此。记法：物品有固定顺序 → 组合；每步都能重选 → 排列。"},{"a":"s5","h":"区间 DP","x":"05 区间 DP 按区间长度递增，枚举分界点 dp[l][r] = minₖ dp[l][k] + dp[k+1][r] + cost · 长度是阶段，必须最外层 l 区间 [l, r] r [l, k] 已算完（更短） [k+1, r] 已算完（更短） 切 k 枚举 k 从 l 扫到 r−1。依赖的两段长度都 < r−l+1 → 和 Floyd 的 k 一样，阶段在最外层。 戳气球：正向「先戳谁」让左右不独立；反向「最后戳谁」则两边边界固定。卡住时改想最后一步。 环形石子合并把数组复制一倍。四边形不等式可把 O(n³) 收到 O(n²)（§9.4）。 先算短区间，再拼长区间。等价写法是倒序 l、正序 r；按长度显式循环更不容易写反。Floyd 的 k（允许经过哪些中间点）也是阶段，必须最外层——同一条规律。 // 区间 DP 骨架：len 是阶段，必须最外层；k 枚举切点 for ( int len = 2 ; len <= n; ++len) for ( int l = 0 ; l + len - 1 < n; ++l) { int r = l + len - 1 ; for ( int k = l; k < r; ++k) dp[l][r] = std:: min (dp[l][r], dp[l][k] + dp[k + 1 ][r] + cost(l, k, r)); } 典型题 转移特点 石子合并 标准形式；环形版把数组复制一倍 矩阵链乘法 cost 是三个维度的乘积 最长回文子序列 转移 O(1) → 总 O(n²) 戳气球 枚举「最后戳的」而不是第一个，左右才独立 括号 / 表达式求值 额外记录运算符位置"},{"a":"s6","h":"树形 DP","x":"06 树形 DP 状态是「子树 + 该节点的选择」 后序从下往上收：dp[u][0/1] = 不选 / 选 u 时，u 子树最优 u 先 dfs 完所有孩子 v₁ dp[v][0] / dp[v][1] v₂ 已是整棵子树答案 v₃ 再往上合并 不选 u：每个孩子 max(选, 不选) 相加。选 u：每个孩子必须不选。链状树 DFS 深度 = n，n=10⁵ 改迭代。 子树信息只能从下往上收，所以转移写在 DFS 后序。答案是 max(dp[root][0], dp[root][1])。直径/重心也是后序求向下最长链并更新全局；点分治处理路径统计。链状树深度 = n，n=10⁵ 必须改迭代 DFS 或按 BFS 序倒序处理。 // 树形骨架：没有上司的舞会。后序合并孩子 void dfs ( int u) { dp[u][ 0 ] = 0 ; dp[u][ 1 ] = happy[u]; for ( int v : children[u]) { dfs (v); dp[u][ 0 ] += std:: max (dp[v][ 0 ], dp[v][ 1 ]); dp[u][ 1 ] += dp[v][ 0 ]; } } 类型 状态 要点 选 / 不选 dp[u][0/1] 最大独立集、打家劫舍 III 树上背包 dp[u][j] 合并上界用 size[u] 不是 m → 实际 O(n·m)（每对点只在 LCA 合并一次） 换根 down / up 两遍 DFS；难点是「排除自己」（前缀+后缀，max 不可减） 换根 DP ：要对每个点求「以它为根」的答案。朴素对每个点 DFS 是 O(n²)。两遍即可 O(n)：后序算 down[u] （只看子树），前序算 up[v] （父亲传来的贡献）。 up[v] 需要「父亲其他孩子」的信息——前缀+后缀预处理，或总和减 v；后者要小心 max 不可减。树上背包合并时上界用 min(size[u], m) 而不是 m，每对点只在 LCA 处相遇一次，所以是 O(n·m) 不是 O(n·m²)； size[u]+=size[v] 必须在合并之后。"},{"a":"s7","h":"状压 DP","x":"07 状压 DP n ≤ 20 时，把「哪些已选」压成一个整数 n 个元素 ↔ n 位整数。第 k 位 = 元素 k 在不在集合里 元素 3 2 1 0 mask 1 0 1 1 = 11（十进制）已用 {0,1,3} 转移：走到未访问的 j mask | (1<<j)，当前点改成 j TSP：dp[mask][i] = 已访问 mask、停在 i 的最小代价。mask 递增 = 阶段。 n=20 → 2²⁰≈10⁶，×n² 约 10⁸ 勉强；n=25 基本不行。dp[1<<20][20] 用 int 约 80MB，按 popcount 滚动或改记忆化。 枚举子集 for (s=mask; s; s=(s-1)&mask) 总复杂度 O(3ⁿ)（每元素三种角色），不是 O(4ⁿ)。1<<n 在 n≥31 写 1LL<<n。 位就是集合。查第 i 位 mask>>i&1，加入 | (1<<i)，lowbit mask&-mask。 位运算 含义 mask >> i & 1 第 i 位是否在集合里 mask | (1 << i) 加入元素 i mask & ~(1 << i) 移出元素 i mask & (mask - 1) 去掉最低位的 1 mask & -mask 只留最低位 1（lowbit） for (s = mask; s; s = (s-1) & mask) 枚举非空子集，合计 O(3ⁿ) 子集枚举是 O(3ⁿ) 不是 O(4ⁿ)：每个元素三种角色——不在 mask、在 mask 但不在 s、在 s。用来做「把集合划成若干组」（最少几辆车、集合覆盖）。"},{"a":"s8","h":"数位 DP","x":"08 数位 DP 「统计 [L, R] 内满足条件的数」的标准解法 统计 [L,R]：差分 f(R)−f(L−1)。按位填一个不超过 x 的数 上界 x = 2 5 3 limit = true 贴着天花板，当前位 ≤ x[pos] limit = false 某位已填小，后面 0–9 自由 只能缓存 !limit 且已开始的状态 limit=true 依赖具体的 x，每个上界只有一条这样的路径；缓存会跨查询污染。 前导零用 started：012 和 12 是同一个数。「不含连续相同」在前导零阶段通常不生效。 memo 维必须覆盖所有影响后续决策的信息。漏维会混状态；多一维只是浪费。 像填车牌：一直贴上限就不能超位；任意一位填小，后面彻底自由——自由态才可复用。 面试题里有完整记忆化骨架。这里只记三件事：差分、limit、只缓存自由态。 数位 DP 像 按位填写不超过上限的车牌 。前面几位都和上限一样（ limit=true ），当前位就不能超过上限对应的那一位。任意一位填小，后面彻底自由（ limit=false ），可以填 0–9——自由态与具体上限无关，才允许缓存。换一个 x 再跑时，贴天花板的那条路径完全不同，所以 limit=true 绝不能写进 memo。"},{"a":"s9","h":"优化四件套","x":"09 优化四件套 每一种都有明确的适用判据"},{"a":"s9-1","h":"9.1 滚动数组","x":"9.1 滚动数组"},{"a":"s9-2","h":"9.2 单调队列优化","x":"9.2 单调队列优化"},{"a":"s9-3","h":"9.3 斜率优化","x":"9.3 斜率优化"},{"a":"s9-4","h":"9.4 决策单调性与分治","x":"9.4 决策单调性与分治 四种优化的判据见下图四格。点 TOC 对应小节会落到图上方。 ① 滚动数组 只依赖上一层 → 压成 1～2 行 空间 O(n·m)→O(m)，时间不变 代价：旧层覆盖，不能还原方案 要输出方案就别滚，或另记决策 ② 单调队列 · O(n) 窗口 [i−k, i) 求 max/min + w(i) 判据：候选优劣与 i 无关 谁差谁永久丢掉（单调序列） 前缀和 / BIT 同属「查询交给结构」 ③ 斜率 / 凸包 · O(n) 转移含 i×j 乘积项 判据：优劣依赖 i（查询斜率） 维护下凸壳；斜率不单调 → 李超 / 二分 和单调队列常被混，看是否与 i 有关 ④ 决策单调 + 分治 · O(n log n) opt[i] 单调，但不是窗口形式 solve(lo,hi,optL,optR) 找 mid 再劈 四边形不等式；实践打表看 opt[i] 区间 DP 可把 O(n³) 收到 O(n²) 优化前先问：状态能否少一维？该不该换算法族（贪心 / DAG 最长路）？换族通常比抠转移更赚。不要提前优化——没优化的版本是对拍基准。 单调队列 vs 斜率 ，一句话：候选优劣是否与 i 无关？无关 → 单调序列、永久丢弃；有关（乘积项）→ 凸壳、按斜率查。斜率不单调用李超或凸壳二分。决策单调要验证四边形不等式，竞赛里常打表看 opt[i] 是否单调——不单调立刻知道此优化不适用。前缀和加速区间求和、值域树状数组加速「a[j]<a[i] 的前缀 max」，和单调队列是同一类：内层循环在做能交给结构的查询。"},{"a":"s9-5","h":"9.5 优化对照（交互）","x":"9.5 优化对照（交互） 五个 DP 族的状态设计与优化 点击类型，看状态怎么定、循环顺序为什么这样、以及能用哪种优化。 背包 区间 树形 状压 四种优化"},{"a":"s10","h":"陷阱清单","x":"10 陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 01 背包 j 正序 变成完全背包，答案偏大 01 倒序、完全正序（§4.1） 组合数 / 排列数循环搞反 计数结果错 物品在外 = 组合；容量在外 = 排列 分组背包物品循环不在最内层 变成「每组可选多个」 三层顺序：组 → 容量倒序 → 物品 区间 DP 没按长度递增 用到还没算好的值 len 在最外层（它是阶段） Floyd 的 k 不在最外层 只做了一次中转 同上：阶段必须最外层 「恰好装满」初值用 0 把不可达状态当成合法解 用 −∞ （求最大）或 +∞ （求最小） −∞ 用 INT_MIN dp[j-w] + v 溢出（UB） 用 INT_MIN/2 或 -1e9 数位 DP 缓存了 limit=true 跨查询污染，答案错 只缓存 !limit && started 数位 DP 没处理前导零 「012」被当成三位数 加 started 标记 LIS 把 tails 当答案序列 输出的不是合法子序列 tails 只是「各长度的最小结尾」；要方案得另记前驱 LIS 严格/非严格用错 bound 长度差 1 或更多 严格 → lower_bound ；非严格 → upper_bound Kadane 初值用 0 全负数时答案错 初始化为 a[0] 树形 DP 递归爆栈 链状树 + n 大时 SIGSEGV 迭代 DFS 或按 BFS 序倒序处理 状压 1 << n 溢出 n ≥ 31 时错 写 1LL << n 滚动后还想输出方案 拿不到决策路径 要方案就别滚动，或额外记决策 状态维度漏了一维 不同状态被混为一谈，答案错 问「后续决策还依赖什么信息」； 宁可多加一维 循环顺序和状态漏维都不报错、只给错答案，小数据还常碰巧对。 防线是暴力对拍 ：写一个指数搜索版，小数据随机比对几千组，DP 题的对拍成本很低。怀疑维度不够时，构造两个「当前状态相同但历史不同」的输入，看后续最优决策是否一样——不一样就该加维。不确定时宁可多加一维（只是慢），少加是错。溢出用 -fsanitize=undefined ，见 Sanitizers 实战 。"},{"a":"s11","h":"面试题集（12 道）","x":"11 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s11-1","h":"11.1 基础（必答）","x":"11.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 什么问题适合用 DP？和分治、贪心、回溯怎么区分？ DP 需要 两个前提 ： ① 最优子结构 最优解可以由子问题的最优解组合出来 —— 这是能「递推」的前提。 ② 重叠子问题 同一个子问题在递归展开中被反复遇到 —— 这是「记下来」有价值的前提。 情形 用什么 有最优子结构 + 无 重叠子问题 分治 （归并排序、快速选择） 有最优子结构 + 有 重叠子问题 DP 局部最优即全局最优（且能证明） 贪心 ——可以看成「不需要记状态的 DP」 没有 最优子结构 回溯 + 剪枝 ，或换一个有最优子结构的建模 DP 的本质 「把指数级的搜索空间折叠成多项式个状态」 。而状态设计就是在回答： 要记住哪些信息，才足以让后面的决策不依赖更早的历史？ 多记浪费（状态数爆炸），少记就错（后面的决策需要它）。 写 DP 的建议流程 先用 记忆化递归 写（和「状态定义 + 转移」一一对应，最不容易想错），确认对了再改 自底向上 （消栈深），最后按需 压缩空间 。 直接写自底向上容易在循环顺序上出错，而循环顺序错 不报错、只给错答案 。 基础 01 背包的一维写法为什么 j 要倒序？完全背包为什么正序？ 因为一维数组把「上一轮」和「本轮」压在了同一块内存里， j 的方向决定了 dp[j - w[i]] 读到的是旧值还是新值 。 dp[j] = max(dp[j], dp[j - w[i]] + v[i]); 倒序（j: W → w[i]）= 01 背包 dp[j - w[i]] 下标更小， 还没被本轮更新 ⟹ 它仍是「上一轮」的值，即「 不含 第 i 个物品」的最优解 ⟹ 第 i 个物品最多被用一次。 正序（j: w[i] → W）= 完全背包 dp[j - w[i]] 已被本轮更新过 ⟹ 它可能已经含第 i 个物品 ⟹ 该物品可以重复使用。 所以这不是要背的口诀 它是「滚动数组里下标更小的位置是新值还是旧值」的 直接推论 。理解了这一点，遇到新的背包变体也能自己推出方向。 还有第二组顺序也决定语义 「物品在外层 vs 容量在外层」决定组合数还是排列数 （求方案数时）： · 物品在外 → 每种物品只在自己那一轮被考虑 → (1,2) 和 (2,1) 算 同一种 → 组合数 · 容量在外 → 每个容量下都重新考虑所有物品 → 算 两种 → 排列数 「零钱兑换 II」求组合（物品在外）；「组合总和 IV」名字叫组合但其实求排列（容量在外）——这是个著名的命名陷阱。 基础 LIS 的 O(n log n) 解法是什么？ tails 数组是最长上升子序列吗？ 贪心 + 二分 。维护 tails[k] = 长度为 k+1 的上升子序列的 最小可能结尾 。 for ( int x : a) { auto it = std:: lower_bound (tails. begin (), tails. end (), x); if (it == tails. end ()) tails. push_back (x); // 能接长 else *it = x; // 替换成更小的结尾 } return tails. size (); 为什么 tails 递增 若 tails[k] ≥ tails[k+1] ，那长度 k+2 的序列去掉最后一项就得到一个长度 k+1、结尾更小的序列，与 tails[k] 的最小性矛盾。 为什么替换是安全的 把 tails[k] 换成更小的 x， 不会让任何已有长度变短 （长度 k+1 仍可达），但 为将来留下更多接长的可能 。 tails 不是答案序列（本题最常见的误解） 它只是「各长度的最小结尾」的记录， 不对应任何一个具体的上升子序列 。 要输出具体序列，需额外记录「每个元素被放到哪个位置」以及它的前驱，最后回溯。 严格 vs 非严格 严格上升 （ < ）→ lower_bound （相等时替换掉）。 非严格 （允许相等）→ upper_bound （相等时可接长）。 「最长不下降 / 下降 / 不上升」四种变体靠「取反数组」和「换 bound」组合。 写之前先确认题面要哪一种。 基础 「恰好装满」和「不超过容量」的背包差别在哪？ 只差初值。 转移方程完全一样。 问题 dp[0] dp[1..W] 不超过容量，求最大价值 0 0 （空背包合法，价值 0） 恰好装满，求最大价值 0 −∞ 恰好装满，求最小代价 0 +∞ 求方案数 1 （空方案算一种） 0 为什么 −∞ 能表达「恰好」 −∞ 表示「这个容量 无法 恰好装满」。因为它极小，任何从它转移来的状态也会是极小值， 不会被误当成合法解 。 而初值全 0 意味着「任何容量都可以什么都不装地『装满』」——那就变成了「不超过容量」的语义。 −∞ 不要用 INT_MIN dp[j - w] + v 会 有符号溢出（UB） ，可能变成一个很大的正数从而污染答案。 用 INT_MIN / 2 或 -1e9 （并确保后续加法不会再溢出）。 这是个高频且难查的 bug ——用 -fsanitize=undefined 能直接抓到。 最后判断答案时也要小心 如果 dp[W] 仍是 −∞ （或小于某个阈值），要输出「无解」而不是那个负数。"},{"a":"s11-2","h":"11.2 中级（区分熟练度）","x":"11.2 中级（区分熟练度） 中级 区间 DP 为什么必须按区间长度从小到大循环？ 因为 dp[l][r] 依赖 dp[l][k] 和 dp[k+1][r] ，它们的 区间长度都严格小于 r−l+1 。所以「长度」是这个 DP 的 阶段维度 ，必须在最外层。 for (len = 2; len <= n; ++len) ← 阶段 for (l = 0; l + len - 1 < n; ++l) { r = l + len - 1; for (k = l; k < r; ++k) ← 枚举分界点 dp[l][r] = min(dp[l][r], dp[l][k] + dp[k+1][r] + cost); } 复杂度 O(n³) 和 Floyd 完全同理 Floyd 的 k （允许经过哪些中间点）也是阶段维度，必须最外层。 「DP 的阶段必须在最外层」是一条通用规律 ，同样适用于背包的物品维、状压 DP 的 mask 递增。 等价的另一种写法 倒序枚举 l、正序枚举 r （ for l = n-1 downto 0; for r = l+1 to n-1 ），隐式保证了「更短的区间先算」。两种写法都对，但 按长度显式循环更不易出错 。 一个重要技巧：反向思考「最后一步」 「戳气球」正向想「先戳哪个」会导致子问题不独立（戳掉一个后左右合并了）； 反向想「最后戳哪个」则左右两段完全独立 ——因为最后戳它时，两边的边界是固定的原始边界。 区间 DP 卡住时，试试把「第一步」换成「最后一步」，或把「删除」换成「插入」。 优化到 O(n²) 如果代价函数满足 四边形不等式 ，可以用 决策单调性 把 k 的枚举范围限制在 [opt[l][r-1], opt[l+1][r]] ，石子合并就能从 O(n³) 降到 O(n²)。 中级 多重背包的二进制拆分为什么是对的？ 把「k 个相同物品」拆成 1, 2, 4, …, 2^t, r 这几个「不同物品」，然后跑标准 01 背包。复杂度从 O(W·Σkᵢ) 降到 O(W·Σlog kᵢ) 。 int k = cnt[i]; for ( int p = 1 ; p <= k; p *= 2 ) { items. push_back ({p * w[i], p * v[i]}); k -= p; } if (k > 0 ) items. push_back ({k * w[i], k * v[i]}); // 剩余 正确性：这组数「能且只能」凑出 0 到 k 「能凑出」 ： 1,2,4,…,2^t 能凑出 0 到 2^(t+1)−1 的任意整数（就是二进制表示）；加上剩余 r 就覆盖到 k。 「只能凑出」 ：所有数之和恰好是 k，不会超过。 于是「取 0 到 k 个」这个选择被 编码成 log k 个独立的 0/1 选择 。 「用二进制压缩选择空间」是个通用技巧 同族的还有： 快速幂 （把 a^n 拆成 log n 次平方）、 稀疏表 （用 2^k 长度的区间覆盖任意区间）、 倍增法 （LCA、树上跳 k 步）。 共同点：把「连续的 0..k 范围」表示成 log k 个二进制位，从而把线性的枚举变成对数的组合。 还有更优的做法 单调队列优化 可以做到 O(W·n) ：按 j mod w[i] 的余数分组，同一组内的转移就变成了「窗口内求最大值」（窗口大小 k），用单调队列 O(1) 摊销。 代码更长，但复杂度更好。竞赛里 k 很大时用它。 中级 数位 DP 里为什么只能缓存 limit == false 的状态？ 因为 limit == true 的状态 依赖具体的上界 x ，不能跨查询（甚至不能跨不同的高位选择）复用。 limit 的含义 limit == true 表示「前面填的每一位都 和上界 x 完全相同 」——此时当前位只能填 0..x[pos] 。 limit == false 表示「前面已有某位填得比 x 小」——此时后面 彻底自由 ，可以填 0..9。 为什么 limit=true 不能缓存 对每个上界 x， limit == true 的路径 只有一条 （沿着 x 的各位走下来）。缓存它毫无收益，而且 换一个 x 时这个值就错了 ——因为它算的是「在 x 的约束下」的结果。 而 limit == false 的状态与 x 无关 ，只取决于「还剩几位」和「前面留下的约束信息」，所以可以放心缓存和复用。 if (!limit && started && memo[pos][prev] != - 1 ) return memo[pos][prev]; // ... 计算 ... if (!limit && started) memo[pos][prev] = res; // 只在这个条件下写缓存 第二个必踩坑：前导零 「012」和「12」是同一个数，但按位构造时是不同路径。用 started （或 lead ）标记「是否已开始填有效数字」。 很多题的约束在前导零阶段不应生效 ——比如「不含连续相同数字」，前导零不算数字。 第三个坑：memo 维度漏了 memo 的维度必须覆盖 所有影响后续决策的信息 。漏一维会把不同状态混为一谈（答案错）；多一维只是浪费空间。 不确定时宁可多加。 整体框架 差分 ： ans(L,R) = f(R) − f(L−1) ，把区间问题变成「统计 0 到 x」。然后按位构造 + limit 标记 + 记忆化。 中级 单调队列优化和斜率优化怎么区分？ 一句话判据：「候选之间的优劣关系是否与 i 无关？」 无关 → 单调队列 转移形如 dp[i] = max{dp[j]} + w(i) ，其中 w(i) 只依赖 i。 此时比较两个候选 j₁、j₂ 只需比 dp[j₁] 和 dp[j₂] —— 与 i 无关 。 ⟹ 「谁支配谁」是绝对的 ⟹ 被支配者可以永久丢弃 ⟹ 单调队列。 再加上「候选集是随 i 单调右移的窗口」，就得到 O(n)。 有关 → 斜率优化 / 凸包技巧 转移形如 dp[i] = min{dp[j] + (S[i] − S[j])²} ，展开后有 −2·S[i]·S[j] 这种 i 与 j 的乘积项 。 此时哪个候选更优 取决于 i （相当于取决于查询的斜率）⟹ 不能简单丢弃 ⟹ 要维护一个 下凸壳 ，按斜率查询。 单调队列 斜率优化 维护的不变式 单调序列 凸壳 丢弃是否永久 是 否（要看斜率区间） 复杂度 O(n) O(n)（斜率单调）/ O(n log n)（不单调） 斜率不单调时 — 用 李超线段树 或凸壳上二分 还有第三种：决策单调性 + 分治 如果最优决策点 opt[i] 关于 i 单调不减 ，但转移不是窗口形式（单调队列用不上），可以用 分治优化 做到 O(n log n)。 怎么判断 ：理论上验证代价函数满足 四边形不等式 ；实践中常用 打表验证 ——写朴素 O(n²) 版，输出每个 i 的最优决策点，看它是否单调。 不严谨但有效，而且不单调时你立刻知道这个优化不适用。"},{"a":"s11-3","h":"11.3 高级（体现深度）","x":"11.3 高级（体现深度） 高级 拿到一道你没见过的 DP 题，你的思考流程是什么？ 五步，顺序不能乱。 ① 先看 n 的范围反推复杂度 n ≤ 20 → 状压或回溯；n ≤ 500 → O(n³) 区间 DP；n ≤ 5000 → O(n²)；n ≤ 10⁵ → 必须 O(n log n) 或 O(n)，说明需要优化；值域大但位数少 → 数位 DP。 这一步反推出「状态大概有几维」 ，比盲想快得多。 ② 定义状态：问「要记住哪些信息」 关键问题是 「后面的决策还依赖哪些历史信息？」 ——那些就是状态。 看题面里的关键词找信号：「容量/预算」→ 加一维；「区间 [l,r]」→ 两维；「子树」→ 树形；「n≤20 + 哪些已用」→ 位掩码；「最多 k 次操作」→ 加一维 k；「持有/不持有」→ 状态机。 宁可多加一维 ——多加只是慢，少加是错。 ③ 写转移：站在「最后一步」 问 「最后这一步有几种选择？」 ——每种选择对应一项转移。 爬楼梯：走 1 级还是 2 级；01 背包：最后一个物品选不选；LCS：最后两字符相等吗；区间 DP：最后合并的分界点在哪。 如果正向想不通，试试反向（「戳气球」枚举最后戳的而不是先戳的）。 ④ 定初值与循环顺序 初值：「无解」用 ±∞ 还是 0？「空」是合法状态吗？ 循环顺序： DP 的「阶段」必须在最外层 （区间长度、Floyd 的 k、背包的物品）。滚动数组时正序/倒序决定语义。 ⑤ 最后才优化 滚动数组 / 单调队列 / 斜率优化 / 决策单调性。 不要提前优化 ——优化会掩盖状态设计的问题，而且没优化的版本正是你对拍的基准。 还有一条元建议：先写记忆化递归 它和「状态定义 + 转移」 一一对应 ，最不容易想错，而且不用操心循环顺序。确认对了再改自底向上。 然后一定要暴力对拍 ——DP 的两类最难查的 bug（循环顺序错、状态漏维）都 不报错、只给错答案 ，而且常在小数据上碰巧对。暴力版通常十几行，对拍成本极低。 高级 DP 的状态设计怎么判断「维度够不够」？举一个漏维的例子。 判据： 「给定当前状态，后续的最优决策是否完全确定，不再依赖任何更早的历史？」 如果还依赖，就说明维度不够。 漏维例子一 · 股票买卖含冷却期 「买卖股票，卖出后有 1 天冷却期」。 只用 dp[i] （第 i 天的最大收益） 不够 ——后续决策还依赖「我现在持有股票吗」和「昨天是否刚卖出」。 正解 ： dp[i][0/1/2] = 第 i 天结束时「持有 / 不持有且不在冷却 / 冷却中」的最大收益。 这是状态机 DP。 漏维例子二 · 打家劫舍 III（树上） 只用 dp[u] （u 子树的最大值） 不够 ——父节点的决策依赖「u 自己有没有被选」。 正解 ： dp[u][0/1] 。 漏维例子三 · 「最多交易 k 次」 只用 dp[i][持有] 不够——还依赖「已经交易了几次」。 正解 ： dp[i][j][持有] ，j 是已用交易次数。 自检方法：构造反例 怀疑维度不够时， 构造两个「当前状态相同但历史不同」的输入 ，看它们的最优后续决策是否相同。 不同 → 说明有信息没被记进状态 → 需要加维。 这比盯着方程想更有效，因为它把抽象问题变成了具体检验。 反过来：维度过多的代价 多加一维只是空间和时间变差（可能超限），但 答案是对的 。所以不确定时宁可多加，跑通后再想能不能去掉。 去掉维度的常见理由： ① 那一维的值可以由其他维推出（冗余）； ② 那一维只有最后一层有用（可以滚动）； ③ 那一维的取值范围极小可以枚举。 高级 树上背包的复杂度为什么是 O(n·m) 而不是 O(n·m²)？ 关键在 合并时按子树大小限制枚举上界 ——这是一个「看起来只是小优化，实际改变了复杂度量级」的经典结论。 // 树上背包：dp[u][j] = 在 u 子树里选 j 个的最优值 void dfs ( int u) { size[u] = 1 ; for ( int v : children[u]) { dfs (v); // 合并 v 的子树： 枚举上界用 size 而不是 m for ( int j = std:: min (size[u], m); j >= 0 ; --j) for ( int k = 0 ; k <= std:: min (size[v], m - j); ++k) dp[u][j+k] = std:: max (dp[u][j+k], dp[u][j] + dp[v][k]); size[u] += size[v]; // ← 合并后才更新 size } } 证明：每对点只在它们的 LCA 处被合并一次 内层双重循环的执行次数是 size[u] × size[v] ——这恰好是「u 已合并部分的点」与「v 子树的点」的 配对数 。 而任意一对点 (x, y)，只会在 它们的最近公共祖先 那一次合并中被计数一次。 所以 总执行次数 = 点对总数 = O(n²) 。 如果还有容量上限 m，则是 O(n·m) （每个点最多和 m 个「已选数量」配对）。∎ 写成 O(n·m²) 的错法 for ( int j = m; j >= 0 ; --j) // ❌ 上界用 m 而不是 size[u] for ( int k = 0 ; k <= m - j; ++k) 这样每个节点都做满 m² 次，总共 O(n·m²)。 而且它不会报错，只是慢 ——所以很多人写出来 TLE 却不知道差在哪。 还有一个必须注意的顺序 size[u] += size[v] 必须在合并 之后 ——因为循环上界用的是「已合并部分的大小」。写在前面会让上界偏大，虽然答案还对但复杂度退化。 这类分析的一般名字 它属于 「启发式合并 / 小到大合并」 的复杂度分析家族，同族的还有： DSU on tree （树上启发式合并，O(n log n)）、 线段树合并 （O(n log n)）、 并查集按大小合并 （深度 O(log n)）。 共同的分析套路是「换个角度数总量」 ——不数「每次操作多少」，而数「某个量总共变化多少」。这和 摊销分析 的势能法是同一个思想。 高级 一个 O(n²) 的 DP 在 n = 10⁵ 时超时，有哪些优化路径？ 先定位 「n² 是从哪来的」 ——通常是「外层枚举 i，内层枚举所有候选 j」。优化就是 把内层的 O(n) 查询变成 O(1) 或 O(log n) 。按适用条件排序： ① 候选集是滑动窗口 + 求 max/min → 单调队列，O(n) 转移形如 dp[i] = max{dp[j] : i−k ≤ j < i} + w(i) 。 判据：候选优劣只依赖 j（与 i 无关） ⟹ 被支配者可永久丢弃。 ② 转移里有 i×j 的乘积项 → 斜率优化，O(n) 如 dp[i] = min{dp[j] + (S[i]−S[j])²} 。 判据：候选优劣依赖 i ⟹ 不能简单丢弃，要维护 下凸壳 按斜率查询。斜率不单调时用 李超线段树 （O(n log n)）。 ③ 转移是区间求和 → 前缀和，O(n) dp[i] = Σ dp[j]（j 在某区间） → 维护 dp 的前缀和，O(1) 查区间和。 最简单也最容易被忽略。 ④ 候选集由「值」而非「位置」界定 → 树状数组 / 线段树，O(n log n) dp[i] = max{dp[j] : a[j] < a[i]} （LIS 的形式）→ 用 值域树状数组 维护前缀 max，先离散化。 ⑤ 最优决策点单调 → 分治优化，O(n log n) opt[i] 关于 i 单调不减，但转移不是窗口形式。 怎么判断 ：验证四边形不等式，或 打表看 opt[i] 是否单调 （实践中的常用手段）。 但优化前先问两个问题 ①「状态设计能不能简化？」 ——有时 O(n²) 是因为状态多了一维不必要的信息。重新想状态定义可能直接降到 O(n)。 ②「有没有更好的算法族？」 ——比如某些 DP 其实可以用贪心（如区间调度）、或者转成图论问题（DAG 最长路）。 换算法族的收益通常大于优化转移。 这五种优化的共同思路 「把对候选集的聚合查询交给一个支持增量更新的数据结构」 。 识别信号很统一： 内层循环在做一个「可以用数据结构回答的查询」 （区间最值、区间和、前缀 max、凸壳上的最优线）。 看到这个形状，就该问「哪个结构能 O(1) 或 O(log n) 回答它」。 ⬅️ 最短路与最小生成树 ➡️ 贪心与回溯 📊 单调栈与前缀和 🔤 字符串匹配 📚 目录 🏠 主页 动态规划 — 五步方法论 · LIS 的 O(n log n) · 背包全家桶与循环顺序 · 区间与树形与状压与数位 DP · 四种优化 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/graph_traversal_and_topology.html","t":"图遍历与拓扑排序","c":"算法与数据结构","s":[{"a":"","h":"","x":"图遍历与拓扑排序 · 三种图表示 / DFS 时间戳与四种边 / 环检测 / 0-1 BFS / Kahn 与逆后序 / Tarjan SCC Kavin Tech Notes ← 主页 目录 30 min ◐ 图遍历与拓扑排序 图的表示决定一切 三种表示的取舍 与其他章节的关系 DFS 模板与三色标记 时间戳与四种边 环检测 BFS 与它的四个变体 无权图最短路 多源 BFS 双向 BFS 0-1 BFS 拓扑排序 Kahn：按入度剥离 DFS 逆后序 字典序最小 工程用途 连通性 无向图：连通分量 割点与桥 有向图：SCC 缩点成 DAG 二分图 判定 最大匹配 工程场景 场景对照（交互） 陷阱清单 面试题集（12 道） 9.1 基础 9.2 中级 9.3 高级 Algorithms · Graph Traversal · 2026 图遍历与拓扑排序 门槛不在十行 DFS/BFS，而在 「这是不是图问题」 和 「图怎么存」 。表示选错，复杂度从 O(V+E) 变成 O(V²)。下面用图把遍历、拓扑、连通性和缩点讲清，正文只留几份骨架代码，其余题型收进表。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~30 min 前置 递归 · 并查集 三种图表示 DFS 时间戳与四种边 0-1 BFS Kahn 与逆后序 Tarjan SCC 二分图"},{"a":"s1","h":"图的表示决定一切","x":"01 图的表示决定一切 选错表示，复杂度直接差一个量级"},{"a":"s1-1","h":"1.1 三种表示的取舍","x":"1.1 三种表示的取舍 邻接矩阵 邻接表（vector 套 vector） 链式前向星（CSR） 空间 Θ(V²) Θ(V + E) + 每个 vector 的开销 Θ(V + E)，两个扁平数组 遍历某点的所有边 Θ(V) （要扫整行） Θ(deg) Θ(deg)，连续内存 判断 (u,v) 是否有边 Θ(1) Θ(deg) Θ(deg) cache 友好度 好（连续）但数据量大 差 （每个 vector 独立分配） 最好 适合 稠密图 、Floyd、V ≤ 几千 通用，代码最短 竞赛/高性能 ，V、E 很大 同一张图 · 矩阵扫整行，表只扫 deg A B C D A→B,C B→D C→D D→A 邻接矩阵 Θ(V²) · A B C D A 0 1 1 0 B 0 0 0 1 C 0 0 0 1 D 1 0 0 0 查边 O(1) · 扫邻居 O(V) 邻接表 Θ(V+E) A → B, C B → D C → D D → A 扫邻居 O(deg) · 查边 O(deg) 稀疏图（道路、依赖）用表；V≤500 且要全源最短路才上矩阵。CSR 是把所有表订成两个扁平数组，cache 更好。 默认邻接表 。E 落在 [0, V²] ：稀疏（E = O(V)）走 O(V+E)；稠密（E ≈ V²）走矩阵，此时 Floyd / 朴素 Dijkstra 的 O(V²) 反而更省堆常数。V = 10⁵ 绝不能开矩阵。 三种表示像 三种记通话记录的方式 。矩阵是一张 V×V 大表：查「A 打过 B 吗」一眼看到，但列出 A 的通话对象要扫整行——一万人就要一亿格。邻接表是每人一本小册子，只记真实通话；CSR 把所有册子订成一本连续的书，再加「从第几页开始」的索引，内容一样，cache 更好。"},{"a":"s1-2","h":"1.2 与其他章节的关系","x":"1.2 与其他章节的关系 递归与分治 —— DFS 是递归；深图爆栈改迭代或 BFS 并查集与区间结构 —— 无向连通性的在线路线，分工见 §5.1 最短路与最小生成树 —— BFS = 无权最短路；0-1 BFS 是 Dijkstra 特例 动态规划 —— DAG 上 DP 必须按拓扑序转移 堆与 Top-K —— Dijkstra 的优先队列 多工具并行执行 —— 拓扑排序在 Agent 调度里的应用"},{"a":"s2","h":"DFS","x":"02 DFS 十行代码，但时间戳理论能解一整类问题"},{"a":"s2-1","h":"2.1 模板与三色标记","x":"2.1 模板与三色标记 // 三色：有向环检测必须用。WHITE 未访 / GRAY 在栈 / BLACK 已完成 enum { WHITE, GRAY, BLACK }; std::vector< int > color; bool dfs ( int u) { // 返回是否发现环 color[u] = GRAY; for ( int v : g[u]) { if (color[v] == GRAY) return true ; // 回边 if (color[v] == WHITE && dfs (v)) return true ; } color[u] = BLACK; return false ; } DFS 栈 = 当前路径 · GRAY 还在栈上 1 2 3 4 BLACK GRAY 栈中 GRAY 栈顶 WHITE 调用栈（顶在上） 3 ← 正在扩展 2 1 已完成，已弹出 GRAY = 从 3 走回 2 就是环 只用 visited 会把「已完成的 BLACK」误判成环。反例： 1→2, 1→3, 2→3 是 DAG。 为什么必须三色 ： visited == true 盖住两种完全不同的状态—— 还在当前递归栈里 （GRAY，走过去就是环）和 已经整棵子树处理完 （BLACK，只是跨边或前向边）。连通分量、桥、SCC 都建立在「栈上 vs 已完成」这个区分上；深图递归深度 = 最长路径，Linux 默认栈约 8 MB，链状 10⁶ 必崩，能换 BFS 就换，必须 DFS 就改显式栈并加深度上限。"},{"a":"s2-2","h":"2.2 时间戳与四种边","x":"2.2 时间戳与四种边 进入时间 tin 、离开时间 tout 把边分成四类，是桥 / 割点 / SCC 的共同语言。分类只看访问 v 时它是什么颜色：WHITE 是树边，GRAY 是回边（有环），BLACK 再比 tin 分前向边和跨边。 DFS 森林与四种边（有向图） 1 2 3 4 5 树边 回边 4→1 前向边 1→5 跨边 5→4 按 (tin, tout) 的关系分类边 u → v 树边 v 是 WHITE DFS 实际走过的边 回边 v 是 GRAY 存在环 ⟺ 存在回边 前向边 v 是 BLACK 且 tin[u] < tin[v] 跨边 v 是 BLACK 且 tin[u] > tin[v] v 是 u 的后代 ⟺ tin[u] < tin[v] < tout[v] < tout[u] （括号定理：两个节点的时间区间要么嵌套，要么不相交） 无向图只有 树边和回边 两种（没有前向/跨边） 四种边的分类完全由「访问 v 时 v 是什么颜色」决定，而这又对应时间戳的嵌套关系。 括号定理 ：任意两点的 [tin, tout] 要么完全嵌套（一个是另一个的后代），要么完全不相交——不可能部分重叠。 v 是 u 的后代 ⟺ tin[u] < tin[v] && tout[v] < tout[u] 。这把「是不是祖先」变成 O(1)，把子树问题变成区间问题，再用线段树处理（见 区间结构 ）。无向图没有前向边和跨边，只剩树边与回边。"},{"a":"s2-3","h":"2.3 环检测","x":"2.3 环检测 有向环 ⟺ 回边（走到 GRAY） A B C C→A 时 A 仍是 GRAY → 有环 无环 DAG（跨边） 1 2 3 2→3 时 3 已是 BLACK，不是环 环检测已写进上面的三色 DFS。无向图另跳「来路」；有重边必须按边 id 跳，否则平行边被漏判。 图类型 判据 陷阱 有向 走到 GRAY；或 Kahn 出队数 < n visited 把跨边当环 无向 vis[v] 且 v 不是来路 用父节点跳会漏掉平行边环 无向 + 重边 传边 id： if (eid == in_edge) continue 两条平行边本身就是环 读题先问清有没有自环和重边——它同时影响环检测、桥判定和最短路。生产里发现环还要把环上的节点打出来（沿 GRAY 栈回溯），光返回 false 不够诊断。"},{"a":"s3","h":"BFS 与它的四个变体","x":"03 BFS 与它的四个变体 队列一换，能力就变"},{"a":"s3-1","h":"3.1 无权图最短路","x":"3.1 无权图最短路 // BFS 天然给出 无权图 的单源最短路 std::vector< int > bfs ( int s, int n) { std::vector< int > dist (n, - 1 ); std::queue< int > q; dist[s] = 0 ; q. push (s); while (!q. empty ()) { int u = q. front (); q. pop (); for ( int v : g[u]) if (dist[v] == - 1 ) { // ← 入队时 就设 dist dist[v] = dist[u] + 1 ; q. push (v); } } return dist; } BFS 按层：第一次碰到 = 最短 s d=0 a b d=1 c d e d=2 入队时标记 出队才标 → 重复入队 队列胀到 O(E)，dist 偏大 边权不同时层数 ≠ 距离 改 Dijkstra / 0-1 BFS 第一次访问就是最短，前提是边权相同。多源：所有源点一起入队，等价于连一个权 0 的超级源。 必须在入队时标记并写 dist ，不是出队时。出队才标会让同一节点反复入队：稠密图上队列胀到 O(E)，症状是「答案对但超时」或 dist 偏大。边权不同时「层数少」不等于「距离短」，改 Dijkstra 或 0-1 BFS。"},{"a":"s3-2","h":"3.2 多源 BFS","x":"3.2 多源 BFS 「到最近一个 1 的距离」：全部 1 先入队，再跑标准 BFS，仍是 O(V+E)，不是 O(k(V+E))。正确性：所有源点连到一个权 0 的虚拟超级源，问题变回单源。「任意一个 / 至少一个 / 多汇」都是同一招——网络流里的源汇也是这么造出来的。 网格四连通 · 格子就是节点 上 左 (i,j) 右 下 int d[4][2] = {{-1,0},{1,0},{0,-1},{0,1}}; 八连通再加四个对角；障碍 / 越界 continue 岛屿、迷宫、01 矩阵距离 = 图上的 BFS 多源：所有「源格子」先入队，dist=0 再按层扩，得到每个格子到最近源的距离 网格题先定四连通还是八连通，再套 BFS。方向数组写一次，越界和障碍在循环里跳过。 格子题本质是隐式图：节点 = 格子，边 = 相邻可走。不要真的建 vector<vector<int>> ，在 BFS 里用方向数组生成邻居即可。多源场景（01 矩阵到最近的 1、多入口迷宫）仍然是「源格子先入队」。"},{"a":"s3-3","h":"3.3 双向 BFS","x":"3.3 双向 BFS 双向 BFS · 两边各搜一半 · 总是扩较小侧 s 遇 t 单向 ≈ b^d 双向 ≈ 2·b^(d/2) b=10,d=6：快约 500 倍 前提：已知终点、能反向、 边权相同。单词接龙 / 八数码 有向图要建反图。不知道确切终点（任意满足条件）就不能双向。 三个前提： 已知确切终点 、 能反向遍历 （有向图建反图）、边权相同。「总是扩较小一侧」让两边前沿平衡，避免一边爆炸。典型题：单词接龙、最小基因变化、八数码。"},{"a":"s3-4","h":"3.4 0-1 BFS","x":"3.4 0-1 BFS 边权只有 0/1 时用双端队列：权 0 压队首、权 1 压队尾，O(V+E)，不必上堆。识别信号是「某些移动免费、某些花 1」：打通格子、最少翻边（正向 0 反向 1）、转弯有代价直走免费。 // 权 0 压队首（同层），权 1 压队尾（下一层） std::vector< int > bfs01 ( int s, int n) { std::vector< int > dist (n, INT_MAX); std::deque< int > dq; dist[s] = 0 ; dq. push_back (s); while (!dq. empty ()) { int u = dq. front (); dq. pop_front (); for ( auto [v, w] : g[u]) { if (dist[u] + w < dist[v]) { dist[v] = dist[u] + w; if (w == 0 ) dq. push_front (v); else dq. push_back (v); } } } return dist; } deque 里最多两层：d 在前，d+1 在后 d d d+1 d+1 权 0 → 队首（仍是 d） 权 1 → 队尾（变成 d+1） 信号：打通格子花 1 / 直走免费 最少翻边：正向 0、反向 1 小范围整数权 → 桶队列 Dial 队首永远最小，等于 Dijkstra 的「取最小」被队列结构免费提供。 为什么对 ：deque 里任意时刻最多两个距离值（ d 和 d+1 ），队首总是最小——这就是 Dijkstra 的选取规则，只是不用堆。权 0 不增加距离，仍属当前层 → 队首；权 1 是下一层 → 队尾。复杂度从 O((V+E) log V) 降到 O(V+E)。边权是小范围整数时用桶队列 / Dial：每个距离一个桶，O(V + E + C·V)。"},{"a":"s4","h":"拓扑排序","x":"04 拓扑排序 两种写法，各有独门本事"},{"a":"s4-1","h":"4.1 Kahn：按入度剥离","x":"4.1 Kahn：按入度剥离 // Kahn：反复取出入度为 0 的节点 std::vector< int > topo_kahn ( int n) { std::vector< int > indeg (n, 0 ), order; for ( int u = 0 ; u < n; ++u) for ( int v : g[u]) ++indeg[v]; std::queue< int > q; for ( int u = 0 ; u < n; ++u) if (indeg[u] == 0 ) q. push (u); while (!q. empty ()) { int u = q. front (); q. pop (); order. push_back (u); for ( int v : g[u]) if (--indeg[v] == 0 ) q. push (v); } if (( int )order. size () != n) return {}; // 有环 return order; } Kahn · 入度归零才能出队 · 出队数 < n 就是环 第 0 层 A B indeg=0 第 1 层 C D 第 2 层 E 剩下的点入度永远 > 0 互相等待 = 有向环 层数 = DAG 最长路 = 并行调度的关键路径 同层互无依赖，可并行 字典序最小 队列换成最小堆 每步贪心选编号最小 不能先随便排再调整 「让 x 尽量早」是另一题 一次遍历同时得到「有没有环」和「没有的话顺序是什么」。默认用 Kahn：不爆栈、能分层、能换堆求字典序。 Kahn 的层 就是 DAG 的最长路：同层节点互无依赖，可以并行；层数是「再多机器也不能更快」的下界。实际调度往往不卡死在层上，而是谁的入度先归零谁就跑（工作队列 + 计数器），并行度更高。环的报错必须可诊断：对剩余节点跑三色 DFS，沿栈打印整个环。"},{"a":"s4-2","h":"4.2 DFS 逆后序","x":"4.2 DFS 逆后序 后序位置 push，最后 reverse，就是拓扑序。任意边 u→v ：v 是 WHITE 则先递归完 v；BLACK 则已完成；GRAY 则有环。故所有边上 tout[v] < tout[u] ，按 tout 降序即「u 在 v 前」。默认仍用 Kahn：不爆栈、能分层、能换堆求字典序。DFS 逆后序只在两处必要——Kosaraju 第一遍，或你同时需要时间戳。 Kahn DFS 逆后序 环检测 order.size() < n 发现 GRAY 字典序最小 队列换堆 不能 分层 / 关键路径 按层剥离 不直接 栈深 无 深图爆栈 Kosaraju 第一遍 不适用 必须"},{"a":"s4-3","h":"4.3 字典序最小","x":"4.3 字典序最小 Kahn 的队列换成最小堆，O(V log V + E)。必须在过程中每步贪心选最小，不能先随便排出一个拓扑序再调整。「字典序最小拓扑序」和「让某点尽量靠前」是两道题：后者通常要反向拓扑再反向贪心。题面这两个词出现时要分清。"},{"a":"s4-4","h":"4.4 工程用途","x":"4.4 工程用途 构建 / 包管理 / Airflow / Excel 重算 / 死锁等待图：节点是任务或事务，边是先后或等待。环 = 不可调度或死锁。拓扑的工程价值有三面： 顺序 （按什么执行）、 环检测 （配置有没有问题，很多系统里这才是主用途）、 层数 = DAG 最长路 = 理论最短完成时间。场景对照见 §7，Agent 调度见 多工具并行执行 。"},{"a":"s5","h":"连通性","x":"05 连通性 从「连通分量」到「割点、桥、SCC」"},{"a":"s5-1","h":"5.1 无向图：连通分量","x":"5.1 无向图：连通分量 连通分量 · 必须从每个未访问点各起一次 1 2 3 分量 A 4 5 分量 B 6 分量 C 只从 0 出发会漏 B、C 边一次给全 → DFS/BFS 边陆续到、只问连通 → 并查集（不在本页展开） 要路径、距离、割点用遍历；只要连通且边在线到达，用并查集。删边两者都不直接支持。 选型只问一句： 边是一次给全还是陆续到来？ 一次给全且还要路径/割点 → DFS/BFS。陆续到、只问是否连通 → 并查集（不用建图，见 并查集 ）。要删边：并查集离线逆向，或 LCT。 DFS / BFS 并查集 复杂度 O(V+E) O(E·α(V)) 建图 要 不要 在线加边 否 是 分量节点列表 遍历时直接得到 要再收集 顺带能求 路径、桥、SCC 只有连通"},{"a":"s5-2","h":"5.2 割点与桥","x":"5.2 割点与桥 low[u] = 子树经至多一条回边能到的最小 tin u v 桥？ 桥 low[v] > tin[u] 子树绕不回 u，这条边是唯一通道 割点 low[v] ≥ tin[u]（u 非根） 根是割点 ⟺ ≥ 2 个树边孩子 求桥用边 id 跳来路，回边 min tin[v] 一个符号之差：能回到 u 本身时边不是桥，但点仍可能是割点。平行边永远不是桥。 求桥必须用边 id 跳来路：父节点会把平行边误判成桥。回边更新是 min(low[u], tin[v]) 不是 low[v] ——回边只能跳到 v，不能接着用 v 的 low。根的割点要另判：树边孩子 ≥ 2。"},{"a":"s5-3","h":"5.3 有向图：SCC","x":"5.3 有向图：SCC 强连通分量 ：集合内任意两点互相可达。Kosaraju 两遍 DFS：原图求逆后序，再在 反图 上按该顺序 DFS，每棵树一个 SCC——好讲，但要建反图。Tarjan 一遍完成：栈里是「还没确定归属」的节点， low[u]==tin[u] 时弹出一个 SCC。关键是 else if (on_stack[v]) ：v 已弹出说明它属于 已经确定的另一个 SCC ，那条跨边绝不能拉低当前 low，否则多个分量会被错误合并。面试讲 Kosaraju 更清楚，生产更常用 Tarjan。"},{"a":"s5-4","h":"5.4 缩点成 DAG","x":"5.4 缩点成 DAG 缩点：互相能到的城市合成都市圈，圈与圈只剩单向 a b c SCC 1 d e SCC 2 f SCC 3 最长路 / 最大权 → DAG DP 加最少边使强连通 = max(入度 0 点数, 出度 0 点数) 2-SAT：x 与 ¬x 同 SCC 无解 Tarjan 编号 = 逆拓扑序 圈与圈若还能互相到，本来就是同一个 SCC。缩点恢复偏序，拓扑和 DP 才能用。 缩点像把「互相都能到」的城市合成都市圈。圈内部有环，谈不上先后；圈与圈之间一定单向——否则它们本就是同一个圈。Tarjan 给出的编号恰好是缩点后 DAG 的 逆拓扑序 ，可以直接拿来做 DP，不必再跑一遍 Kahn。"},{"a":"s6","h":"二分图","x":"06 二分图 一个判定，一个匹配"},{"a":"s6-1","h":"6.1 判定","x":"6.1 判定 二分图 ⟺ 无奇环 · 相邻必须异色 L L R R 合法：组内无边 1 2 3 三角形 = 奇环 → 冲突 染色：能给方案、能吐奇环 必须扫所有分量 边在线到来 → 扩展域并查集 （实现见并查集篇，本页不展开） col[v] = col[u] ^ 1 染色冲突就是奇环。外层 for s 漏分量是最高频坑，连通分量 / SCC / 拓扑多个源点同样适用。 二分图 ：节点能分成两组，所有边跨组。等价于 没有奇环 。染色能给出方案也能输出一个奇环；扩展域并查集（每个点拆成 x 与 x+n）支持边陆续到来，但拿不到方案。在线操作选并查集，给定整图要方案就染色。"},{"a":"s6-2","h":"6.2 最大匹配","x":"6.2 最大匹配 匈牙利靠增广路：每次给一个左部点找一条「未匹配边与已匹配边交替」的路，找到就翻转，匹配数 +1。每轮必须重置 used，否则匹配数偏小。数据大换 Hopcroft-Karp（O(E√V)）。 算法 / 定理 要点 匈牙利增广路 O(V·E)；每轮重置 used；右部 match[v]=左部 Hopcroft-Karp O(E√V)，数据大时换它 König 二分图最小点覆盖 = 最大匹配 最大独立集 V − 最大匹配（仅二分图） DAG 最小路径覆盖 拆点建二分图，答案 = V − 最大匹配 带权匹配 KM 或最小费用最大流 看到「最少点覆盖所有边 / 最多互不相邻 / DAG 最少路径覆盖」，第一反应是二分图匹配。König：最小点覆盖 = 最大匹配。带权用 KM 或最小费用最大流，后者更好写。网络流建模见 最短路与最小生成树 。"},{"a":"s7","h":"工程场景","x":"07 工程场景 「这是个图问题」往往是最难的一步 场景 节点 / 边 算法 构建系统 / Airflow / Excel 目标 → 依赖 Kahn；环 = 循环依赖；层数 = 关键路径 数据库死锁 事务 → 等待的锁 有向环；沿栈取出环再选牺牲者 微服务发布 服务 → 调用 SCC 找循环依赖；拓扑定顺序 网络可靠性 路由器 → 链路 桥 / 割点 = 单点故障 Agent 工具编排 调用 → 参数依赖 拓扑 + 按层并行 状态空间（八数码 / 接龙） 整个状态 → 合法一步 不显式建图；双向 BFS / A* 识别信号三条：出现「依赖 / 先后 / 前置」→ 有向图 + 拓扑；「连通 / 可达 / 分组」→ 无向用遍历或并查集，有向用 SCC；「最少步数」→ BFS / 0-1 BFS / Dijkstra。最容易错的是 节点是什么 ——不一定是题面里的对象：八数码的节点是整盘状态，且图不显式建出来；点容量或路径覆盖要 拆点 ；「最多免费坐 k 次车」把节点变成 (位置, 已用次数) 。边的方向也取决于你要什么：求执行顺序是「被依赖的指向依赖者」，求影响范围则反过来。"},{"a":"s7-1","h":"7.1 场景对照（交互）","x":"7.1 场景对照（交互） 五个工程场景的图建模 点击场景，看节点和边怎么定、用什么算法、以及环意味着什么。 构建系统 死锁检测 网络单点故障 Agent 工具编排 状态空间搜索"},{"a":"s8","h":"陷阱清单","x":"08 陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 只从节点 0 开始遍历 图不连通时漏掉其他分量 外层 for s 从每个未访问节点各起一次 BFS 在出队时标记 同一节点多次入队，超时或距离偏大 入队时 就标记并设 dist 有向图环检测只用 visited 把跨边误判成环 三色标记：GRAY = 在递归栈中 无向图用父节点跳过来路 有重边时漏判环 / 误判桥 用 边 id 跳过 Tarjan 求桥时混用 low 和 tin 结果错 回边只能 min(low[u], tin[v]) Tarjan SCC 漏了 on_stack 检查 SCC 划分错 else if (on_stack[v]) 必不可少 桥用 ≥、割点用 > 判定反了 桥 low[v] > tin[u] ，割点 low[v] ≥ tin[u] 忘了根节点的割点特判 根被误判 / 漏判 根是割点 ⟺ 有 ≥ 2 个树边孩子 DFS 递归在深图上爆栈 SIGSEGV，只在大数据下出现 改 BFS，或显式栈，或加深度断言 匈牙利每轮忘重置 used 匹配数偏小 每个左部点开始前重置 邻接矩阵存稀疏大图 MLE（V=10⁵ 时要 10¹⁰ 格） 改邻接表 / CSR 把自环 / 重边当合法输入没处理 各种诡异结果 读题时问清；必要时先去重 拓扑排序没检查 order.size() 有环时静默返回错误的部分序 必须比对 order.size() == n 漏分量和出队才标记最常见，且呈「答案接近但不对」。手推三个用例：单点、两个分量、一个环。爆栈用 ASan 的 stack-overflow 抓，见 Sanitizers 实战 。图来自不可信输入时，深链 DFS 是 DoS，必须加深度上限，不能只靠加大栈。"},{"a":"s9","h":"面试题集（12 道）","x":"09 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s9-1","h":"9.1 基础（必答）","x":"9.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 邻接矩阵和邻接表怎么选？复杂度差别在哪？ 邻接矩阵 邻接表 空间 Θ(V²) Θ(V+E) 遍历某点所有边 Θ(V) （扫整行） Θ(deg) 判 (u,v) 是否有边 Θ(1) Θ(deg) 判据：稀疏还是稠密 E 的范围是 [0, V²]。 稀疏图 （E = O(V)：道路网、社交网络、依赖图）→ 邻接表，O(V+E) 远优于 O(V²)。 稠密图 （E ≈ V²：完全图、距离矩阵）→ 邻接矩阵，而且此时 O(V²) 的算法（朴素 Dijkstra、Floyd）反而更好，省掉堆的常数。 真实世界的图绝大多数是稀疏的 ，所以默认邻接表。 高性能场景还有第三选择：CSR / 链式前向星 两个扁平数组（ start[] 存每点边的起始位置， to[] 存所有边的终点），空间 Θ(V+E) 且 完全连续 。 对比 vector<vector<int>> ：后者每个内层 vector 独立分配，遍历时指针追逐、cache 不友好。CSR 是顺序访问，预取友好。 V、E 到百万量级时差别明显。 读题信号 看给定的 V、E 范围：V ≤ 500 且要求全源最短路 → 直接邻接矩阵 + Floyd。V = 10⁵ → 必须邻接表（矩阵要 10¹⁰ 格，MLE）。 基础 有向图环检测为什么需要三种颜色？只用 visited 会怎样？ visited == true 只说明「访问过」，但这有 两种截然不同的情况 ： GRAY：还在当前递归栈里 从 u 走到 v 意味着从 v 能回到 v → 有环 。 BLACK：已经完全处理完了 只是一条 跨边或前向边 ， 不是环 。 只用 visited 的反例 1→2, 1→3, 2→3 。从 1 出发：DFS 到 2，2 到 3（3 变 visited 并完成），回到 1，1 再看 3 —— 若只看 visited 就 误报有环 。而这是个 DAG。 enum { WHITE, GRAY, BLACK }; bool dfs ( int u) { color[u] = GRAY; // 进栈 for ( int v : g[u]) { if (color[v] == GRAY) return true ; // 回边 → 环 if (color[v] == WHITE && dfs (v)) return true ; } color[u] = BLACK; // 出栈 return false ; } 另一条路：Kahn 拓扑排序 如果最终出队节点数 < n，说明剩下的节点入度永远降不到 0 → 有环。 一次遍历同时得到「有没有环」和「如果没有，顺序是什么」 ，比单独跑环检测更实用。 无向图不一样 无向图只有树边和回边（没有前向/跨边），所以只需 visited + 跳过来路 。但要用 边 id 而非父节点跳——否则有重边时会漏判（两条平行边本身构成环）。 基础 为什么 BFS 能给出无权图的最短路？BFS 有什么实现坑？ 因为 BFS 按 层 扩展——先访问所有距离 1 的，再距离 2 的……所以一个节点 第一次被访问时，走的必然是最短路径 。 这个论证依赖「每条边权重相同」 边权不同时「层数少」不等于「距离短」，BFS 失效 → 要用 Dijkstra。 而 0-1 BFS 是两者之间的折中：边权只有 0/1 时用双端队列，O(V+E)。 最高频的实现坑：出队时才标记 必须在「入队时」标记并设 dist ： for ( int v : g[u]) if (dist[v] == - 1 ) { // 检查 dist[v] = dist[u] + 1 ; // 入队时 就设 q. push (v); } 出队时标记会让同一节点 多次入队 ——稠密图上队列膨胀到 O(E)，而且 dist 可能被写成非最短值。 症状是「答案对但超时」或「答案偏大」。 第二个坑：只从节点 0 开始 图可能不连通。要计数连通分量、或确保全覆盖时，必须 外层从每个未访问节点各起一次 。 这个坑在所有图遍历题里都存在（连通分量、SCC、二分图染色、拓扑排序的多个起点）。 多源 BFS 「从任意一个起点出发的最短距离」不需要跑 k 次—— 把所有起点一起放进初始队列 ，复杂度仍是 O(V+E)。 正确性：等价于「加一个虚拟超级源点，连所有起点，边权 0」。 看到「任意一个 / 至少一个」就该想到加虚拟节点。 基础 写出拓扑排序，并说明怎么顺带检测环。 Kahn 算法 ：反复取出入度为 0 的节点。 std::vector< int > topo ( int n) { std::vector< int > indeg (n, 0 ), order; for ( int u = 0 ; u < n; ++u) for ( int v : g[u]) ++indeg[v]; std::queue< int > q; for ( int u = 0 ; u < n; ++u) if (!indeg[u]) q. push (u); while (!q. empty ()) { int u = q. front (); q. pop (); order. push_back (u); for ( int v : g[u]) if (--indeg[v] == 0 ) q. push (v); } if (( int )order. size () != n) return {}; // ← 环检测 return order; } 环检测 如果最终 order.size() < n ，说明剩下的节点入度永远降不到 0——它们互相依赖，构成环。 必须检查这一行 ，否则有环时会静默返回错误的部分序。 另一种写法：DFS 逆后序 DFS 后序的 逆序 就是拓扑序。证明：对任意边 u→v ，若 v 是 WHITE 则先递归完 v（ tout[v] < tout[u] ）；若 v 是 BLACK 则显然 tout[v] < tout[u] ；若 v 是 GRAY 则有环。所以按 tout 降序排列满足所有边的约束。 Kahn DFS 逆后序 求字典序最小 能 （队列换最小堆） 不能 求「最少几轮完成」 能 （按层剥离） 不直接 栈深风险 无 深图爆栈 用于 Kosaraju 求 SCC 不适用 必须用它 默认用 Kahn 更直观、无爆栈、能扩展到字典序最小和分层。只在做 Kosaraju 或需要时间戳时用 DFS 逆后序。"},{"a":"s9-2","h":"9.2 中级（区分熟练度）","x":"9.2 中级（区分熟练度） 中级 什么是 0-1 BFS？为什么它能用双端队列代替堆？ 当边权 只有 0 和 1 两种时，不需要 Dijkstra 的堆——用双端队列即可，复杂度从 O((V+E) log V) 降到 O(V+E) 。 for ( auto [v, w] : g[u]) { // w ∈ {0, 1} if (dist[u] + w < dist[v]) { dist[v] = dist[u] + w; if (w == 0 ) dq. push_front (v); // 同层 → 队首 else dq. push_back (v); // 下一层 → 队尾 } } 为什么对 双端队列在任意时刻 最多包含两个不同的距离值 （ d 和 d+1 ），且 队首的距离总是最小的 ——这正好复现了 Dijkstra「每次取距离最小的点」的性质，而不需要堆来维护。 权 0 的边不增加距离 → 新节点属于「当前层」→ 压队首；权 1 → 「下一层」→ 压队尾。 本质是 Dijkstra 的特例 ：「取最小」这个操作被队列结构免费提供了。 识别信号 「某些移动免费、某些移动花费 1」 ： · 网格最短路，某些格子可以「打通」（打通花 1，直接走花 0） · 「最少翻转多少条边使 s 能到 t」→ 正向边权 0、反向边权 1 · 迷宫中「转弯花代价、直走免费」 更一般的情形 边权只有 {0, w} 两种也可以（把 w 当 1，最后乘回去）。 边权是 小范围整数 [0, C] 时用 桶队列 / dial's algorithm ：每个距离一个桶，O(V + E + C·V)。 这一族方法的共同思路是：用「结构」代替「堆」来提供有序性——只要距离值的取值范围足够小或足够规整。 中级 怎么求无向图的桥和割点？两者的判据差别在哪？ 都用 Tarjan 的 low 值 ： low[u] = 从 u 的子树（含 u）通过 至多一条回边 能到达的最小 tin 。 桥 树边 (u,v)：low[v] > tin[u] （v 的子树无法绕过这条边回到 u 或更早） 割点 树边 (u,v)：low[v] ≥ tin[u] 且 u 不是根 u 是根时：u 有 ≥ 2 个树边孩子 为什么一个是 > 一个是 ≥ 桥 关心的是「这条 边 能不能被绕过」——如果 v 的子树能回到 u 本身（ low[v] == tin[u] ），那说明有另一条路径连接 u 和 v，这条边不是桥。 割点 关心的是「删掉这个 点 后 v 的子树是否失联」——如果 v 的子树最多只能回到 u（而回不到 u 的祖先），那删掉 u 就会让 v 的子树孤立，所以 low[v] == tin[u] 时 u 就是割点。 三个实现坑 ① 求桥 必须用边 id 跳过来路 ，不能用父节点——否则有重边时会把平行边误判成桥（平行边永远不是桥）。求割点用父节点是安全的。 ② 回边更新必须是 low[u] = min(low[u], tin[v]) ， 不是 low[v] ——回边只能「跳到」v，不能继续用 v 的 low。混用这两个是最常见的错。 ③ 根节点的割点判据不同 （要数树边孩子个数），忘了特判会误判根。 工程意义 桥和割点就是 单点故障 ：网络拓扑里的桥是「断了就分区的链路」，割点是「宕了就分区的路由器」。 进一步： 边双连通分量 （去掉桥后的连通块）内部任意两点有至少两条边不重复的路径 → 冗余区域。这是网络可靠性分析的基础。 中级 Tarjan 求 SCC 里的 on_stack 检查为什么必不可少？ 因为它区分了两种「v 已被访问」的情况，而它们对 low[u] 的影响完全不同。 for ( int v : g[u]) { if (!tin[v]) { // 树边 tarjan (v); low[u] = std:: min (low[u], low[v]); } else if (on_stack[v]) { // ← 关键 low[u] = std:: min (low[u], tin[v]); } // v 已访问但 不在栈上 → 指向已完成的 SCC， 不能 更新 low } 在栈上 vs 不在栈上 在栈上 ：v 属于「当前正在探索、还没确定归属」的节点集合 → u 和 v 可能 在同一个 SCC → 应该更新 low。 不在栈上 ：v 所属的 SCC 已经确定并弹出了 → u 和 v 一定 不 在同一个 SCC（否则 v 的 SCC 不会先被确定）→ 这条跨边不应影响 u 的 low 。 去掉检查会怎样 对所有已访问的 v 都更新 low，会让 u 的 low 被 无关的、已完成的 SCC 拉低 → low[u] == tin[u] 的判定失效 → SCC 划分错误（通常是把多个 SCC 错误地合并）。 对比 Kosaraju Kosaraju 两遍 DFS：① 原图求逆后序；② 反图 上按该顺序 DFS，每棵树就是一个 SCC。 易理解易写，但要建反图 （内存双份）。Tarjan 一遍完成、不需反图，代价是逻辑更绕。 面试里 Kosaraju 更容易讲清，生产里 Tarjan 更常用。 Tarjan 的一个额外好处 它给出的 SCC 编号顺序恰好是 缩点后 DAG 的逆拓扑序 ——可以直接用来做 DP，不必再跑一遍拓扑排序。 而缩点的意义是 「消除环，恢复偏序」 ：把每个 SCC 缩成一个点后必然是 DAG（否则那些 SCC 会合并），于是拓扑排序和 DAG 上的 DP 就都能用了。 中级 判断二分图有哪两种方法？各自的适用场景？ 二分图 ⟺ 不存在奇环 。这个等价性是两种算法的共同依据。 方法一 · BFS/DFS 染色 相邻节点染不同色，冲突则有奇环。 for ( int s = 0 ; s < n; ++s) { // 必须遍历所有分量 if (col[s] != - 1 ) continue ; // BFS 染色，col[v] = col[u] ^ 1 // if (col[v] == col[u]) return false; } 能给出染色方案，也能输出一个奇环。 方法二 · 扩展域并查集 每个点拆成 x 和 x+n 两个域。对每条边 (u,v) ：先查 find(u) == find(v) （已被推断为同组 → 矛盾），否则 unite(u, v+n) 且 unite(u+n, v) 。 支持在线 ——边可以陆续到来，随时能回答。 染色 扩展域并查集 给出染色方案 能 否 输出奇环 能 否 在线处理 需先建全图 支持 代码量 中等 更短 「在线 vs 离线」是第一个分岔口 题面说「依次处理 m 个操作，每次输出答案」→ 必须并查集。说「给定一张图，判断/输出方案」→ 染色更合适。 染色法的高频遗漏 必须遍历所有连通分量 。图可能不连通，只从节点 0 开始会漏掉其他分量。这个坑在所有图遍历题里都存在。"},{"a":"s9-3","h":"9.3 高级（体现深度）","x":"9.3 高级（体现深度） 高级 设计一个构建系统的依赖调度器，怎么做？循环依赖怎么报错？ 模型： 节点 = 构建目标，边 = 依赖关系 （A 依赖 B ⟹ 边 B→A，表示 B 必须先完成）。核心是拓扑排序。 ① 用 Kahn 而不是 DFS 三个理由： 无爆栈风险 （依赖链可能很长）； 顺带环检测 ；最重要的是—— 按层剥离天然给出并行度 。 ② 按层并行执行 Kahn 的每一「层」（同时入度为 0 的节点）内部 互无依赖，可以并行 。 层数 = DAG 的最长路径长度 = 关键路径 ，它决定了理论最短完成时间——再多机器也不能更快。这是评估「加机器有没有用」的关键指标。 实际实现通常不严格按层，而是 「谁的依赖先满足谁就开跑」 （用一个工作队列 + 计数器），并行度更高。 ③ 循环依赖的报错必须可诊断 光说「检测到循环依赖」没用——用户需要知道 是哪几个目标构成的环 。 做法：Kahn 结束后若 order.size() < n ，对 剩下的节点 跑一次 DFS 三色标记，遇到 GRAY 时 沿递归栈回溯打印整个环 。 这是「工程实现」和「刷题实现」的关键差别：刷题只要返回 false，生产必须给出可操作的错误信息。 ④ 还要处理的现实问题 增量构建 ：只重建「变了的目标 + 它的所有下游」→ 从变更点做 正向可达性搜索 （BFS/DFS）。 缓存命中 ：用「输入内容哈希 + 命令哈希」做 key（bazel 的做法），能跨机器复用。 失败处理 ：一个目标失败后，它的所有下游都要标记为「跳过」而不是「失败」——同样是正向可达性。 动态依赖 ：某些依赖只有构建后才知道（如 C 的 #include ）→ 需要边构建边扩展图。 ⑤ 这个模型的其他化身 Airflow / DAG 工作流引擎、Excel 公式重算、编译器的类型推导顺序、 Agent 工具调用编排 、包管理器依赖解析（但版本约束求解本质是 SAT，比拓扑排序难）。 共同点：都要同时解决「顺序」和「环检测」两件事，而且环检测的错误信息质量往往比算法效率更重要。 高级 数据库怎么检测死锁？检测到之后怎么办？ 建 等待图（wait-for graph） ：节点 = 事务，边 T1 → T2 表示「T1 在等 T2 持有的锁」。 死锁 ⟺ 等待图中有环 。 ① 检测：有向图环检测 用 DFS 三色标记（GRAY = 在当前递归栈中）。发现回边时 沿栈回溯得到整个环 ——这一步是必要的，因为要从环里选牺牲者。 触发时机有两种： 周期性检测 （每隔几百毫秒扫一遍，MySQL 早期）或 等待时立即检测 （每次一个事务开始等待就从它出发找环，InnoDB 的做法，更及时）。 ② 选牺牲者的策略 不能随便选——要让代价最小且避免 活锁 （同一个事务反复被选中回滚，永远无法完成）： · 回滚代价最小 ：已修改的行数最少、持有的锁最少、已执行时间最短（InnoDB 用「undo log 长度」近似） · 避免活锁 ：把「已被回滚次数」计入权重，或按事务开始时间给老事务优先权 · 不选系统关键事务 ③ 另一条路：预防而非检测 超时 （ innodb_lock_wait_timeout ）：最简单，但 会误杀只是慢的事务 ，且超时值难调（太短误杀、太长死锁持续久）。 按固定顺序加锁 ：从根上避免环（比如「总是按主键升序加锁」）。这是 应用层最有效的手段 ，但要求所有代码都遵守。 Wait-Die / Wound-Wait ：按事务时间戳决定「等待还是回滚」，无环可证，但回滚率高于检测法。 ④ 分布式场景更难 单机的等待图在一个进程内，扫一遍就行。分布式下等待关系 跨节点 ，要么用 集中式死锁检测器 （收集各节点的局部等待图拼成全局图，有单点和延迟问题），要么用 边追逐（edge chasing） 算法传递探测消息。 而且 网络延迟会导致「假死锁」 ——检测到环时它可能已经解开了。所以分布式系统更倾向用 超时 + 重试 这种简单粗暴但鲁棒的方案。 这是「理论最优」在分布式下让位于「可运维」的又一个例子。 高级 一个 DFS 在生产数据上爆栈了，有哪些解法？各自的取舍？ DFS 递归深度 = 图的最长路径，而 Linux 默认栈 8 MB、每帧几十到上百字节 → 安全深度约 10⁴–10⁵ 。V = 10⁶ 的链状图必崩。 ① 换 BFS（最简单，优先考虑） 队列在堆上， 天然不爆栈 。 但要确认 问题是否允许 ：求最短路、连通分量、拓扑排序（Kahn）、二分图判定都可以换。 不能换的是依赖「后序」的问题 ——求 SCC、桥、割点、子树聚合、时间戳，这些必须 DFS。 ② 显式栈改写 DFS 把递归改成「栈里存 (节点, 已处理到第几个邻居)」，模拟递归的执行状态。 能保留 DFS 的全部性质（包括后序和时间戳），代价是 代码可读性明显下降 ，而且容易写错（尤其是后序位置）。 这就是 递归转迭代 里的「状态机」方法——栈里存的那个「已处理到第几个」就是程序计数器。 ③ 加大栈（临时止血，不是根治） ulimit -s unlimited 、 pthread_attr_setstacksize 、或在 Windows 上用链接器选项。 缺点 ：只是把上限推高，深度再翻倍还是会崩；而且在容器/受限环境里可能改不了；多线程时每个线程都要这么大栈，内存浪费。 可以作为应急，但要同时安排根治。 ④ 深度断言 + 拒绝（安全视角） 如果图来自 不可信输入 （用户上传的依赖文件、外部 API 返回的关系），这不只是性能问题而是 DoS 漏洞 ——攻击者构造深链就能打崩服务。 必须 加显式深度上限并优雅拒绝 ，而不只是改大栈。这和 递归下降解析器 的爆栈是同一类问题。 ⑤ 决策顺序 问题允许换 BFS 吗？ → 换（最省事）。 必须 DFS 但图可信？ → 显式栈（一次性成本，之后不用再管）。 必须 DFS 且图不可信？ → 显式栈 + 深度上限 （安全要求）。 只是临时救火？ → 加大栈 + 记一个技术债。 另外用 -fsanitize=address 能把爆栈报成明确的 stack-overflow 而不是裸 SIGSEGV，定位快得多。 高级 怎么把一个实际问题识别成图问题？建模时最容易错在哪？ 三个识别信号： ① 「依赖 / 先后 / 前置」 → 有向图 + 拓扑排序。构建系统、任务调度、类型推导、Excel 重算。 ② 「连通 / 可达 / 分组」 → 连通分量。无向图用并查集（在线）或 DFS（要更多信息）；有向图用 SCC。 ③ 「最少步数 / 最短」 → BFS（无权）、0-1 BFS（权只有 0/1）、Dijkstra（正权）、Bellman-Ford（有负权）。 最容易错的是「节点是什么」 节点 不一定是题面里显式给的对象 ： · 状态空间搜索 ：节点是 整个状态 （八数码里是整个棋盘、单词接龙里是整个单词）。这类图 不显式建出来 ——邻居是「按规则生成的」，图可能是无穷大的。 · 拆点 ：一个对象拆成「入点」和「出点」。用于点容量限制（网络流）、DAG 最小路径覆盖（二分图匹配）。 · 分层图 ：节点是 (位置, 附加状态) 。「最多可以免费坐 k 次车的最短路」→ 节点变成 (城市, 已用免费次数) ，图规模乘以 k+1。 · 边变节点 ：某些问题里边才是主体（如「边的着色」）。 第二个易错点：边的方向和权重 「A 依赖 B」建成 A→B 还是 B→A ？ 取决于你要什么 ——求「执行顺序」要 B→A （B 先完成），求「影响范围」要 A→B （改了 B 影响谁）。 两个方向的图都建出来往往更方便 。 「最少翻转多少边」这类题里，边权是 你构造出来的 （正向 0、反向 1），不是题面给的。 建模比套算法难，也更值得练 算法就那几个（DFS/BFS/拓扑/Dijkstra/并查集），而 建模的变化是无穷的 。 练法：拿到题先问三句—— 「节点是什么？边是什么？我要的答案对应图上的什么？」 把这三句用自然语言写清楚，算法通常就自明了。反过来，如果这三句说不清，直接开始写代码一定会绕弯路。 ⬅️ 并查集与区间结构 ➡️ 最短路与最小生成树 🌲 树与平衡树 📐 动态规划 📚 目录 🏠 主页 图遍历与拓扑排序 — 三种图表示 · DFS 时间戳与四种边 · 环检测 · 0-1 BFS · Kahn 与逆后序 · Tarjan SCC — Kavin Tech Notes · 2026"}]},{"u":"algorithms/greedy_and_backtracking.html","t":"贪心与回溯","c":"算法与数据结构","s":[{"a":"","h":"","x":"贪心与回溯 · 交换论证 / 拟阵直觉 / 区间调度与 Huffman / 回溯三要素与去重 / N 皇后位运算 / 分支限界 Kavin Tech Notes ← 主页 目录 30 min ◐ 贪心与回溯 两种极端策略 与其他章节的关系 贪心的正确性证明 交换论证 前缀最优（归纳） 拟阵：会不会堵死后面 经典贪心 区间调度 区间覆盖与合并 Huffman 编码 排序不等式与反悔贪心 贪心失效时 两个经典反例 失效的判据 回溯框架 做选择与撤销 子集 / 组合 / 排列的模板差异 排列 vs 组合去重 剪枝 五种剪枝手段 N 皇后：冲突即剪枝 分支限界与工程近似 分支限界 遇到 NP-hard 的出路 选型对照 模板对照（交互） 陷阱清单 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Greedy & Backtracking · 2026 贪心与回溯 贪心和回溯是搜索策略的两个极端： 贪心每步只走一条路，赌它一定对；回溯每步都试所有路，靠剪枝活下来 。所以这两个话题的核心都不是模板——贪心的核心是 证明 （交换论证、拟阵），回溯的核心是 剪枝和去重 。这一篇把这两件事讲透，并给出「贪心什么时候会失效」的可操作判据。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~30 min 前置 排序 · 递归 · DP 交换论证 拟阵直觉 区间调度与 Huffman 回溯三要素与去重 N 皇后位运算 分支限界"},{"a":"s1","h":"两种极端策略","x":"01 两种极端策略 贪心是「不记状态的 DP」，回溯是「没有重叠子问题的 DP」 贪心 动态规划 回溯 每步的选择 只选一个 ，不回头 枚举所有选择，记下最优 试所有选择 ，失败就撤销 需要的性质 贪心选择性质 + 最优子结构 最优子结构 + 重叠子问题 无（但需要可剪枝才实用） 复杂度 通常 O(n log n) 多项式 指数 难点 证明它对 设计状态 剪枝与去重 写错的表现 安静地给出次优解 安静地给出错误答案 超时，或答案重复/遗漏 三者是一条连续谱 ：贪心 = 状态维度为 0 的 DP（每步最优不依赖历史）；DP 在贪心不成立但子问题重叠时接手，把指数折叠成多项式；回溯在状态太大、无法枚举时只能真搜。写错的表现也不同：贪心安静给次优解，DP 安静给错答案，回溯则超时或重复遗漏。拿到题先问「能贪心吗」——最快；不能再问「能 DP 吗」；再不行才搜。 贪心像 不带地图爬山，每步朝最陡的方向走 ：快，但可能停在半山腰。只有地形保证「一路向上就是最高峰」时才对—— 证明这一点就是贪心的全部难度 。回溯像 系着绳子探洞 ：走不通就退回换路。剪枝是「一眼看出此路不通，直接不进」——不改最坏复杂度，实际数据上却差几个数量级。 与 DP 何时换：三问定策略，n 的范围直接给答案 拿到一道题 先别动手写搜 ① 局部 ⟹ 全局？ 交换论证说得清 贪心 · O(n log n) 区间调度 / Huffman ② 子问题重叠？ 状态能枚举完 DP · 多项式 01 背包 / 区间 DP 回溯 + 剪枝 n 直接告诉你 n ≤ 11 → 排列穷举 n ≤ 20 → 子集 / 状压 n ≤ 5000 → O(n²) DP n ≤ 10⁵ → 贪心或 O(n log n) DP 更大 → 近似 / 求解器 问不出「换过来为什么不变差」，就不要赌贪心；状态爆炸就不要硬 DP。n 的量级往往比感觉更可靠。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 动态规划 —— 贪心是「不记状态的 DP」；贪心失效时退回 DP 排序全景 —— 绝大多数贪心的第一步是排序；「按什么排」就是贪心策略本身 堆与 Top-K —— Huffman、任务调度类贪心靠堆维护「当前最优候选」 最短路与最小生成树 —— Dijkstra、Kruskal、Prim 都是贪心，正确性靠交换论证 复杂度与分析 —— 遇到 NP-hard 的五条出路 递归与分治 —— 回溯的 RAII 式状态恢复、深度爆栈"},{"a":"s2","h":"贪心的正确性证明","x":"02 贪心的正确性证明 写不出证明就不该用贪心"},{"a":"s2-1","h":"2.1 交换论证","x":"2.1 交换论证 交换论证（exchange argument） 四步：① 假设最优解 OPT 与贪心解 G 不同；② 找到 第一个不同处 （贪心选 g，OPT 选 o）；③ 把 OPT 里的 o 换成 g，证明新解 合法且不更差 ；④ 反复换，直到 G 本身就是最优。∎ 第 ③ 步说不清，贪心就是错的。 交换论证的结构（以区间调度为例） 贪心 G 最早结束 … 最优 OPT 结束更晚 … 第一个不同处 第 ③ 步：换完为什么仍然合法、且不变差 贪心选的区间结束更早 ⟹ 给后面留下的空间不少于 OPT 选的那个 ⟹ 把 OPT 的第一个换成它，后面所有区间仍然合法，区间数不变（仍最优） ⟹ 新解与贪心多一致一个 ⟹ 反复应用即得贪心最优 ∎ 核心就一句：贪心这一步给未来留下的余地不少于任何其他选择，所以换过来不会变差。 面试被问「怎么知道贪心是对的」，不要说「感觉对」或「试了几个例子」。至少要能讲出第 ③ 步：「贪心选的这个 X，在 Y 这个维度上不比别的差，所以换过来不会让后面变糟」；或者排除一个具体反例。能说出一句形式化论证，比多做十道贪心题更能体现水平。"},{"a":"s2-2","h":"2.2 前缀最优（归纳）","x":"2.2 前缀最优（归纳） 另一种证法 ：归纳证明「贪心每一步前缀都是该规模下的最优」。 适合逐个处理元素 （Kadane、加油站、跳跃游戏）。 交换论证更适合「先排序再按序选」 （区间调度、Huffman）。"},{"a":"s2-3","h":"2.3 拟阵：前面选错会不会堵死后面","x":"2.3 拟阵：前面选错会不会堵死后面 拟阵 是「贪心一定对」的充分条件：独立集满足 遗传性 （子集仍独立）和 交换性 （|A| < |B| 时，B 里总能找出一个元素加入 A 仍独立）。定理：按权降序、能加就加，一定得到最大权独立集——Kruskal 就是图拟阵上的这个算法。 拟阵两条公理像 组队规则 。遗传性：合法队伍去掉几个人还合法。交换性：人少的队总能从人多的队里抽一个人过来仍不违规。满足这两条时，「按能力从高到低挑、能加就加」一定能组出最强队——因为前面挑得不好也能补救。01 背包不满足交换性：两件都不超重的集合，人少的那个未必能从人多的那边加进任何一件。 可操作判据：前面贪心选完，后面会不会无路可走？ 不会堵死 → 贪心有戏 区间调度：换上结束更早的，后面更宽裕 Kruskal：少一条边的森林总能从 另一棵树里补一条不形成环的边 交换性成立，前面选「错」也能补救 会堵死 → 改 DP 01 背包：先塞进一件大的，剩余容量 装不下两件小的，而那两件合起来更优 「不可分割 + 全局容量」破坏交换性 前面选错就卡死，贪心给出次优解 不必真去验证拟阵公理。问一句「前面选完会不会把后面堵死」比背定义有用。"},{"a":"s3","h":"经典贪心","x":"03 经典贪心 「按什么排序」就是贪心策略本身"},{"a":"s3-1","h":"3.1 区间调度（活动选择）","x":"3.1 区间调度（活动选择） 最多安排多少个互不重叠的会议？贪心选择性质是： 每次选结束最早的那个 ——它给后面留下的时间最多。交换论证的第 ③ 步就是这句话：把最优解的第一个换成「结束最早的」，可用时间只增不减，后面全部仍然合法。按开始时间或按时长都是错的。 活动选择：三个会议 [0,10]、[1,2]、[3,4] 时间 0 2 4 10 A [0,10] B [1,2] C [3,4] 按开始时间（错） 先占 A，B、C 都重叠 局部「最早开工」→ 全局只 1 个 按结束时间（对） 先占 B（最早结束），再占 C 给后面留的空隙最大 → 2 个 按时长也错：先选最短的 [4,6]，会挡住 [0,5] 和 [5,10] 这一对。互斥选择按结束排；合并/覆盖按开始排。 // 最多能安排多少个互不重叠的会议？→ 按结束时间升序贪心 int max_non_overlap (std::vector<std::pair< int , int >> iv) { std:: sort (iv. begin (), iv. end (), []( auto & a, auto & b) { return a.second < b.second; }); int cnt = 0 , last_end = INT_MIN; for ( auto & [s, e] : iv) if (s >= last_end) { ++cnt; last_end = e; } return cnt; } 问题 按什么排序 / 策略 最多不重叠区间数 结束时间 升序 最少删除多少区间使不重叠 同上（总数 − 最多不重叠数） 最少需要几个会议室 扫描线 或最小堆维护各室结束时间 用最少的点覆盖所有区间 结束时间升序，在当前右端放点 区间合并 开始时间 升序，然后依次合并 最少区间数覆盖 [0, T] 开始时间升序，能接上的里面选 右端最远"},{"a":"s3-2","h":"3.2 区间覆盖与合并","x":"3.2 区间覆盖与合并 「用最少的 X 覆盖/到达 Y」（跳跃游戏、加油站、视频拼接）都是 在能接上的里面选最远的 ：按开始排，双指针 i 全程只前进，O(n) 而不是 O(n²)。 「选最多个」（互斥）按结束排 ，要给未来留余地； 「合并 / 覆盖」（推进）按开始排 ，要按顺序处理。读题先分清是选择型还是推进型，不能一上来就按结束时间排。"},{"a":"s3-3","h":"3.3 Huffman 编码","x":"3.3 Huffman 编码 反复取两个最小的合并（最小堆）。交换论证：频率最小的两个 x、y 一定可以是最深层兄弟——任意最优树最深层的兄弟 a、b，把 x↔a、y↔b 换过去，加权路径长度不会增加（小频率换到更深）。于是先合并 x、y 成超级节点，规模减一，递归即得算法。同族：合并果子、连接绳子。 只能合并相邻 （石子合并）时这个论证失效，必须区间 DP——「能否任意合并」是分界线。 同一堆 [4, 1, 1, 4]：约束一变，贪心就翻车 可任意合并（Huffman） 1+1=2 → 2+4=6 → 6+4=10 总代价 18，这就是最优 「两个最小的在最深处」成立 只能合并相邻（石子） 贪心先合中间 1+1 → 总代价 18 最优先合两端 → 总代价 16 位置约束破坏交换论证 → DP Huffman 骨架看起来能套的题，先问「能不能任意挑两堆」。不能，就不要贪心。"},{"a":"s3-4","h":"3.4 排序不等式与反悔贪心","x":"3.4 排序不等式与反悔贪心 问题 贪心策略 证明要点 最小化平均等待时间 按处理时长升序（SJF） 交换相邻，短的在前总代价更小 两数组配对求内积 一升一降 / 同序 排序不等式 拼接数字使结果最大 比较器 a+b > b+a 必须满足传递性 带截止时间的任务 按截止排，堆里退掉最差的 反悔贪心：先全收，违规再退最长 反悔贪心 ：先按某种顺序全部接受，一旦违规就从已接受的里面用堆退掉最差的那个。退掉耗时最长的，既解决违规，又最大程度为后面腾时间，且任务数只减 1。这是交换论证在动态过程中的应用——每次退货都是一次局部交换。加油站「先开、油不够再回头选之前最大的站」也是同一套路。"},{"a":"s4","h":"贪心失效时","x":"04 贪心失效时 局部最优不是全局最优"},{"a":"s4-1","h":"4.1 两个经典反例","x":"4.1 两个经典反例 贪心错了不会报错，只会安静地给次优解。下面两例都是「每步看起来最香，整体更差」——比值最高、面额最大，都是局部最优。 局部最优 ≠ 全局最优 01 背包 W=10，按价值/重量 (6,7) 密度 1.17 (5,5)×2 密度 1.00 先拿 7 剩 4，装不下 → 结果 7 最优：两件 5+5 = 10 分数背包能切开，贪心才对 零钱 [1, 3, 4] 凑 6 贪心先拿最大面额 4 + 1 + 1 = 3 枚 最优：3 + 3 = 2 枚 人民币面额碰巧可以贪心 「试了几个例子都对」不可靠。零钱兑换的反例要「大面额不是小面额的倍数」，随机测试都可能撞不上。"},{"a":"s4-2","h":"4.2 失效的判据","x":"4.2 失效的判据 四个信号，看到就警惕 ： 全局容量 + 物品不可分割 → 01 背包，用 DP（可分割则分数背包，贪心对）。 位置/顺序约束 （只能合并相邻、必须连续）→ 区间 DP。 「恰好」类约束 （恰好 k 个、和恰好 T）→ 贪心很难卡准，通常 DP。 前面的选择会堵死后面 （没有拟阵交换性）→ DP 或搜索。 最实用的验证是 小数据暴力对拍 ：写十几行枚举，随机跑几千组。贪心的错往往只在精心构造的小反例上暴露。想到反例就立刻放弃，不要修补策略——补出来的贪心通常还有别的反例，而且越来越难验证。此时退回 DP 更快：DP 的正确性只依赖「状态定义 + 转移完备」，比「全局最优性」好验证得多。"},{"a":"s5","h":"回溯框架","x":"05 回溯框架 做选择、撤销、剪枝；候选集合决定模板"},{"a":"s5-1","h":"5.1 三要素：做选择 / 撤销","x":"5.1 三要素：做选择 / 撤销 做选择 → 递归 → 撤销：必须严格配对 path [] make → [1] make → [1,2] undo → [1] undo → [] 忘了撤销，或 early return 跳过撤销 → 路径被污染，答案多出或错乱。 只留一个出口，或用 RAII（析构里 pop）。记录结果必须拷贝 path，存引用会让所有答案变成最后一次状态。 push / pop 是一对括号。少写一次 pop，后面所有分支都在脏状态上跑。 // 回溯骨架：做选择 / 递归 / 撤销。可行性剪枝一行即可 void backtrack (State& st) { if ( is_complete (st)) { record (st); return ; } for ( auto & choice : candidates (st)) { if (! valid (st, choice)) continue ; // 可行性剪枝：已违规就不往下走 make (st, choice); backtrack (st); undo (st, choice); // 撤销 ← 关键 } } 搜索树：对 [1,2] 每个元素「选 / 不选」 {} {1} {} 选 1 不选 1 {1,2} {1} {2} {} 四个叶子 = 2² 个子集。n=20 就是一百万，再大就不能穷举 回溯就是在这棵树上 DFS。剪枝是砍掉整棵子树，不是把复杂度从指数变成多项式。 三要素 ：① 路径 （已做的选择，通常一个 vector，边走边 push/pop）；② 候选集合 （当前还能选什么——三种模板的唯一差别）；③ 终止条件 （路径够长 / 候选为空 / 到达目标）。短例子只留子集一种：每个节点都记录；组合改成只在 path.size()==k 时记录；可重复选则递归传 i 而不是 i+1 。 void dfs ( int start) { res. push_back (path); // 子集：每个节点都记录 for ( int i = start; i < n; ++i) { path. push_back (a[i]); dfs (i + 1 ); // i+1：不回头 path. pop_back (); } }"},{"a":"s5-2","h":"5.2 子集 / 组合 / 排列的模板差异","x":"5.2 子集 / 组合 / 排列的模板差异 三种模板的唯一区别：候选集合怎么定 子集 / 组合 for (i = start; i < n; ++i) 传 start = i + 1，不回头 每元素只考虑一次，无顺序 排列 for (i = 0; i < n; ++i) used[] 标记，每次从头扫 同元素不同位置算不同方案 可重复选 for (i = start; i < n; ++i) 递归传 start = i（不是 i+1） 同一元素可以再选 子集 2ⁿ（n≤20） 组合 C(n,k) 排列 n!（n≤10~11，10!≈3.6×10⁶） n 更大就不能穷举，改 DP 或贪心。骨架相同，改的是 for 从哪起、递归传什么 排列完整代码见面试题；这里只记「从头扫 + used」，不要和子集模板混 写错模板的典型症状：子集出现了 [1,2] 和 [2,1] 两份，或排列漏了一种顺序。"},{"a":"s5-3","h":"5.3 去重：排列 vs 组合","x":"5.3 去重：排列 vs 组合 输入含重复元素时先 排序 让相同值相邻。组合/子集用「同一层不选相同值」；排列用「相同值必须按下标从小到大用」——那个感叹号 !used[i-1] 写反会漏解。 输入已排序 [1, 1, 2]：两种去重不是同一句话 组合 / 子集 · 同一层 if (i > start && a[i]==a[i-1]) skip 本层起点是 start，第二个 1 和 第一个 1 在同一层 → 跳过 不涉及 used，因为根本不回头 效果：{1,2} 只出现一次 排列 · 也是同一层，但看 used if (i>0 && a[i]==a[i-1] && !used[i-1]) !used[i-1]：前一个 1 还没用 → 正在本层跳过它直接选后一个 → 重复，砍 used[i-1]：前一个已在上层用了 → 这是合法的「第二个 1」，不能砍 写成 used[i-1] 会漏解 记不清条件时，同一层用局部 set 记已选过的值——慢一点，但不依赖预排序，也不容易写反。 n≤20 时不必回溯：二进制枚举 2ⁿ 个子集 mask bit2=c bit1=b bit0=a 子集 000 0 0 0 { } 001 0 0 1 { a } 101 1 0 1 { a, c } 111 1 1 1 { a, b, c } 第 i 位为 1 ⟺ a[i] 在子集里 循环 mask ∈ [0, 2ⁿ) 无需递归、无撤销、无去重坑 n>20 时 2ⁿ 爆炸，改 DP / 剪枝搜 能用掩码就别上回溯：没有递归栈，也没有「忘了 pop」这种坑。 // n ≤ 20：二进制枚举子集，比回溯更直 for ( int mask = 0 ; mask < ( 1 << n); ++mask) { std::vector< int > sub; for ( int i = 0 ; i < n; ++i) if (mask & ( 1 << i)) sub. push_back (a[i]); }"},{"a":"s6","h":"剪枝","x":"06 剪枝 不改变最坏复杂度，但决定实际能不能跑完"},{"a":"s6-1","h":"6.1 五种剪枝手段","x":"6.1 五种剪枝手段 组合总和 target=7，候选已排序 [2, 3, 6, 7] 0 +2 +3 +6 +7 砍掉 砍掉 sum+a[i]>target 用 break 不是 continue：后面 a[j]≥a[i]，一次剪掉整个尾部 可行性剪枝在模板里就是那一行 if (!valid) continue 。排序之后，「当前不行 ⇒ 后面都不行」才能升级成 break。剪枝像找工作先看硬门槛：不剪是每个岗位都面完才知道不合适；排过序再 break，等于门槛不够后面更高的整批跳过。 剪枝 做法 典型场景 可行性 已违反约束 → 返回 N 皇后冲突、数独行列宫 最优性（限界） 当前代价 ≥ 已知最优 → 返回 TSP、装箱 上界估计 剩下全选也达不到 → 返回 组合总和、选 k 个 排序 + 提前退出 不可能时 break 而非 continue 升序后 sum + a[i] > target 对称性 等价分支只搜一个 N 皇后第一行只搜一半"},{"a":"s6-2","h":"6.2 N 皇后：冲突即剪枝","x":"6.2 N 皇后：冲突即剪枝 同一列、同一主对角线（ row+c ）、同一副对角线（ row−c ）不能再放。这就是可行性剪枝的具体化——不必再写第三份长代码。朴素写法用三个数组标记占用，放皇后时置 1、返回时清 0，还是 make / undo。位运算版把三组占用压进寄存器： avail 一次算出可放位， avail & -avail 取最低位 1，对角线随行下移而左右移位，参数传值天然回溯，通常再快 5–10 倍。细节见面试题。 4 皇后：第 0 行放在列 1 之后，斜线与整列都是禁区 Q ╱ ╲ 同列占用 col[c] 对角线 diag1[row+c] / diag2[row−c+n] 可放：下一行只试这些格子 对称性剪枝：第一行只放前 n/2 列，计数乘 2。 要输出具体方案时镜像还原更麻烦，先确认题目要计数还是方案。 位运算版快 5–10 倍：avail 一次算出可放位，参数传值天然回溯。 三个布尔数组（或三个位掩码）就是状态。放皇后 = make，递归返回 = 把标记清零，还是那套撤销。"},{"a":"s7","h":"分支限界与工程近似","x":"07 分支限界与工程近似 穷举跑不完、问题本身又难的时候"},{"a":"s7-1","h":"7.1 分支限界","x":"7.1 分支限界 分支限界 = 回溯 + 上下界估计 + 优先搜索顺序 。最小化问题里，若「已花 + 下界 ≥ 已知最优」，整枝剪掉。下界必须是 乐观估计 （不能高于真值），否则会剪掉最优解——和 A* 的可采纳性 是同一条要求。用优先队列先搜最有希望的分支，好解出现得越早，剪得越狠。 best = 12 之后：左枝已花 8、下界 5，8+5=13 ≥ 12 → 整枝丢掉 0 8+5 7 砍掉（≥12） 继续，可能刷新 best 常用下界 TSP：剩余点 MST / 每点最小出边之和 装箱：⌈总体积 / 箱容量⌉ 整数规划：LP 松弛（去整数约束） 图着色：最大团大小 把「必须是整数」拿掉，问题变成多项式可解的线性规划，解一定不差于整数解，于是天然是下界。再在非整数变量上分支（x ≤ ⌊v⌋ 或 x ≥ ⌈v⌉）。这就是 Gurobi / CPLEX / CBC 的核心框架。工程上先试求解器，往往比自己写搜索快，也更好维护。"},{"a":"s7-2","h":"7.2 遇到 NP-hard 的出路","x":"7.2 遇到 NP-hard 的出路 策略 保证 适合 先质疑需求 — 「95% 够不够？」这一步收益最大 量真实规模 — n 常年 ≤ 30 时求解器几毫秒出最优 近似算法 有界近似比 结果要对外承诺（计费、合规） ILP / SAT 求解器 最优，时间不保证 离线批处理、有时间窗口 启发式 无保证 只影响内部效率 自己从零写遗传算法，通常打不过 Gurobi / CBC / OR-Tools，还留下无人能维护的代码。结果要对外承诺（计费、合规、配额）用近似算法，因为需要写进文档的界；只影响内部效率就启发式加监控；离线批处理有时间窗口，直接上求解器。可解释性、稳定性和可干预这三点，往往比「解好 2%」更决定项目成败。完整讨论见 复杂度与分析 。"},{"a":"s8","h":"选型对照","x":"08 选型对照 拿到题先问三句 三个问题定策略 ：① 局部最优能推全局？（交换论证成立）→ 贪心 ，O(n log n)。② 不能贪心，子问题重叠且状态能枚举完？→ DP ，多项式。③ 状态太大或无最优子结构 → 回溯 + 剪枝 ；再搜不完 → 近似 / 启发式 / 求解器。n 通常直接给答案：n ≤ 11 排列穷举；n ≤ 20 子集 / 状压；n ≤ 5000 可 O(n²) DP；n ≤ 10⁵ 贪心或 O(n log n) DP。见 复杂度与分析 。"},{"a":"s8-1","h":"8.1 模板对照（交互）","x":"8.1 模板对照（交互） 五种策略的模板与判据 点击策略名，看骨架、正确性依赖什么、以及什么时候会失效。 贪心证明 经典贪心 回溯三模板 去重与剪枝 NP-hard 出路"},{"a":"s9","h":"陷阱清单","x":"09 陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 贪心没证明就用 安静地给出次优解 写下交换论证；用暴力对拍 区间调度按开始时间排 结果偏小 「选最多个」按结束排；「合并/覆盖」按开始排 01 背包用贪心 次优解 不可分割 + 容量 → DP 只能合并相邻却用 Huffman 次优解 有位置约束 → 区间 DP 自定义比较器不满足传递性 std::sort 越界（UB） 验证严格弱序（见 排序全景 ） 回溯忘了撤销 / early return 跳过撤销 路径被污染 push/pop 配对；只留一个出口或 RAII 排列去重写成 used[i-1] 漏解 是 !used[i-1] 去重前忘了排序 去重条件失效 相同元素必须相邻 剪枝用 continue 而非 break 答案对但慢几倍 已排序且后面都不行时用 break 记录结果时存了引用 所有结果都变成最后一个状态 存拷贝 分支限界的下界高估 剪掉最优解 下界必须是乐观估计 回溯深度过大爆栈 SIGSEGV 加深度上限 这张表分两类： 贪心的错都是「安静地次优」 （前四条）， 回溯的错都是「重复 / 遗漏 / 超时」 。前者的防线是暴力对拍——写十几行枚举，随机跑几千组，这是验证贪心的唯一可靠手段。后者的防线是手推最小用例：空输入、单元素、全相同元素（专测去重）。C++ 里「存引用而非拷贝」特别常见，症状很迷惑（所有答案都一样），值得单独记。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 贪心、DP、回溯三者怎么选？它们的关系是什么？ 三者是一条连续谱 ，按「需要记多少状态」区分： 贪心 = 不需要记状态的 DP 每步的最优选择 不依赖任何历史信息 ，所以状态维度为 0。需要「贪心选择性质」——局部最优能导出全局最优。O(n log n)。 DP 贪心不成立时接手：需要记一些状态，但 子问题重叠 ，所以能把指数折叠成多项式。 回溯 DP 也不成立时接手：子问题不重叠，或状态空间太大无法枚举 → 只能真的搜，指数复杂度，靠剪枝活下来。 贪心 DP 回溯 难点 证明它对 设计状态 剪枝与去重 写错的表现 安静地给次优解 安静地给错答案 超时 / 重复 / 遗漏 思考顺序 能贪心吗？（最快）→ 能 DP 吗？→ 只能搜吗？ 而 n 的范围通常直接给出答案 ：n ≤ 11 → 排列穷举；n ≤ 20 → 子集/状压；n ≤ 5000 → O(n²) DP；n ≤ 10⁵ → 贪心或 O(n log n)。 基础 区间调度（最多不重叠区间数）为什么按结束时间排序？ 因为 结束最早的区间给后面留下的时间最多 。 std:: sort (iv. begin (), iv. end (), []( auto & a, auto & b) { return a.second < b.second; // 按结束时间升序 }); int cnt = 0 , last_end = INT_MIN; for ( auto & [s, e] : iv) if (s >= last_end) { ++cnt; last_end = e; } 交换论证 设最优解 OPT 的第一个区间不是「结束最早的那个 g」。因为 g 结束更早， 它给后面留下的可用时间不少于 OPT 选的那个 ⟹ 把 OPT 的第一个换成 g 后， 后面所有区间仍然合法 ⟹ 新解区间数不变（仍最优）且与贪心多一致一个。 反复应用 ⟹ 贪心解本身就是最优解。∎ 另两种排序的反例 按开始时间 ： [0,10], [1,2], [3,4] —— 先选 [0,10] 只能选 1 个，实际能选 2 个。 按时长 ： [0,5], [4,6], [5,10] —— 先选最短的 [4,6] 只能选 1 个，实际能选 2 个。 但「按开始排」也有它的题 「选最多个」（互斥选择）→ 按结束排 ，因为要给未来留余地。 「合并 / 覆盖」（连续推进）→ 按开始排 ，因为要按顺序处理。 读题时先分清是「选择型」还是「推进型」。 基础 写出回溯的通用框架，说明三要素。 void backtrack (State& st) { if ( is_complete (st)) { record (st); return ; } // ① 终止条件 for ( auto & choice : candidates (st)) { // ② 候选集合 if (! valid (st, choice)) continue ; // 剪枝 make (st, choice); // ③ 做选择 backtrack (st); undo (st, choice); // 撤销 （关键） } } 三要素 ① 路径 （已做的选择）——通常一个 vector，边走边 push/pop。 ② 候选集合 （当前还能选什么）——这是三种模板的 唯一区别所在 。 ③ 终止条件 ——路径达到目标长度 / 候选为空 / 达到目标状态。 最常见的两个坑 ① 忘了撤销 ，或者 early return 跳过了撤销 。 可靠写法： 只留一个出口 （不许 early return），或用 RAII ： struct Guard { std::vector< int >& p; Guard (std::vector< int >& p_, int v) : p(p_) { p. push_back (v); } ~ Guard () { p. pop_back (); } // 任何出口（含异常）都撤销 }; ② 记录结果时存了引用 res.push_back(path) 必须是 拷贝 。存指针或引用会导致 所有结果都变成最后一个状态 ——这个 bug 的症状很迷惑（所有答案都一样），在 C++ 里特别常见。 基础 子集、组合、排列三种回溯模板的区别在哪？ 骨架完全相同， 唯一区别是「候选集合怎么定」 。 子集 / 组合：传 start，不回头 for ( int i = start; i < n; ++i) { path. push_back (a[i]); dfs (i + 1 ); // ← i+1 path. pop_back (); } 不产生顺序，每元素只考虑一次 排列：用 used，每次从头扫 for ( int i = 0 ; i < n; ++i) { // ← 从 0 if (used[i]) continue ; used[i] = true ; path. push_back (a[i]); dfs (); path. pop_back (); used[i] = false ; } 同元素在不同位置算不同方案 三个变体 子集 ：每个递归节点都 record （2ⁿ 个）。 组合 C(n,k) ：只在 path.size() == k 时 record。 可重复选 （组合总和）：递归传 dfs(i) 而不是 dfs(i+1) 。 状态数决定 n 的上限 子集 2ⁿ → n ≤ 20 可行；排列 n! → n ≤ 10~11 可行（10! ≈ 3.6×10⁶）。 看到 n ≤ 20 想子集/状压，n ≤ 11 想排列，n 更大就不能穷举 ——要 DP 或贪心。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 什么是交换论证？用它证明一个贪心。 交换论证 是证明贪心最常用的方法，四步模板： 模板 ① 假设存在最优解 OPT，它与贪心解 G 不同。 ② 找到 第一个不同的位置 ——贪心选 g，OPT 选 o。 ③ 证明：把 OPT 里的 o 换成 g（并适当调整后面），得到的新解 不比 OPT 差 。 ④ 于是存在「与贪心一致得更多」的最优解。反复应用 ⟹ 贪心解本身最优。∎ 关键在第 ③ 步 要说清「 换完之后为什么仍然合法，且不变差 」。这一步说不清，贪心就是错的。 区间调度的第 ③ 步就是一句话： 「贪心选的区间结束更早 ⟹ 它给后面留下的空间不少于 OPT 选的那个 ⟹ 后面所有区间仍然合法」 。 Huffman 的交换论证 设频率最小的两个字符是 x、y。 断言 ：存在一棵最优树，x 和 y 是最深层的一对兄弟。 论证 ：取任意最优树 T，设它最深层的一对兄弟是 a、b。因 x、y 频率最小，把 x↔a、y↔b 交换后 加权路径长度不会增加 （频率小的换到更深、频率大的换到更浅）。 所以可以先把 x、y 合并成一个超级节点，规模减一， 递归应用 即得 Huffman。∎ 另一种证法：前缀最优（归纳） 证明「贪心的每一步前缀都是该规模下的最优解」。 适合「逐个处理元素」的贪心 （Kadane、加油站、跳跃游戏）；而交换论证更适合「先排序再按序选」的形态。 面试时不要说这两句 「感觉上应该对」「试了几个例子都对」。 至少要给出交换论证的第 ③ 步 ，或者一个反例的排除。 能说出一句形式化的论证，比多做十道贪心题更能体现水平。 中级 为什么 01 背包不能贪心，但分数背包可以？ 01 背包的反例 容量 W=10，物品 (w=6,v=7) 比值 1.17、 (w=5,v=5) 比值 1.00、 (w=5,v=5) 比值 1.00。 按比值贪心：先拿 7，剩余容量 4 装不下 → 结果 7 。 最优：拿后两个 → 10 。 分数背包可以贪心 物品能切开时，按「价值/重量」比降序装，装满为止。 最后一个可以只装一部分 ——所以容量总能恰好用完，不会出现「剩下 4 但装不下 5」的浪费。 交换论证：任何最优解里，如果比值高的没装满而比值低的装了，把后者换成前者一定不差。∎ 用拟阵的视角看差别 拟阵的 交换性 要求：若 A、B 都是独立集且 |A| < |B| ，则 B 中存在元素可以加入 A 而保持独立。 01 背包不满足 ：两个都不超重的物品集合，元素少的那个未必能从元素多的那个里加进 任何 一件（可能都超重）。 所以「前面选错会堵死后面」——这正是贪心失效的本质。 可操作的判据 问自己： 「如果我前面贪心地选了一些，会不会导致后面『无路可走』而其他选法能走通？」 不会 （总能继续扩充）→ 贪心有戏。 会 → 贪心很可能错，考虑 DP。 这个问法比背拟阵定义实用得多。 中级 排列去重的条件为什么是 !used[i-1] 而不是 used[i-1] ？ 前提：先排序 （让相同元素相邻）。然后： if (i > 0 && a[i] == a[i- 1 ] && ! used[i- 1 ]) continue ; 解释这个感叹号 !used[i-1] 为真 = 「前一个相同的值 还没被用 」 ⟹ 说明我们正在 同一层 尝试「跳过前一个相同值，直接选 a[i]」 ⟹ 这和「选前一个相同值」产生的方案 完全相同 （值一样嘛）→ 重复，跳过。 反之 used[i-1] 为真 = 前一个相同值 已在上层被用了 ⟹ 现在选 a[i] 是合法的「第二个相同值」→ 不能跳 。 效果 相同的值只能按下标从小到大依次使用 ，从而每组相同值只产生一种相对顺序。 写成 used[i-1] 会怎样 也能「去重」，但会把 正确的那一种也跳掉 → 漏解 。这是本题最容易犯的错。 对比：组合/子集的去重条件 if (i > start && a[i] == a[i-1]) continue; 用 i > start 而不是 i > 0 ——因为组合里 start 标记了本层的起点， 只需保证「同一层不选相同值两次」 ，不涉及 used。 记不清时的稳妥做法 在同一层用一个 局部 unordered_set 记录「本层已选过哪些值」。常数变大，但 不需要预排序也不容易写错 。 面试时可以先写这个版本，再说明「排序 + 条件判断更快」。 中级 回溯剪枝有哪些手段？ break 和 continue 的区别为什么重要？ 剪枝 做法 可行性 已违反约束 → 返回（N 皇后冲突检查、数独行列宫） 最优性（限界） 当前代价 ≥ 已知最优 → 返回 上界估计 「剩下全选也达不到目标」→ 返回 排序 + 提前退出 先排序，遇到不可能的 break 对称性 等价分支只搜一个（N 皇后第一行只搜一半） break vs continue 候选 已排序 且「当前不行意味着后面都不行」→ 用 break ， 一次剪掉整个尾部 。 只是当前这个不行、后面可能行 → 用 continue 。 例 ：组合总和里数组升序后， if (sum + a[i] > target) break; ——后面的 a[j] ≥ a[i] 也一定超。 写成 continue 答案对但白跑整个尾部 ，实际数据上常常是几倍到几十倍的差距。 这也解释了「先排序」为什么是剪枝的前置步骤 排序把「零散的不可行」变成「一个连续的不可行尾部」，从而能一次剪掉一整批。 类比：找工作时把岗位按门槛排序，一旦发现达不到某个门槛，后面更高的都不用看了。 剪枝不改变最坏复杂度 它只影响 实际数据上的表现 。所以： ① 别指望剪枝把指数变成多项式； ② 但对真实数据往往是几个数量级的差距，值得花时间做； ③ 如果剪枝后还是跑不完，说明问题规模超出了搜索的能力范围，要换近似或求解器。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 怎么判断一个贪心策略是错的？如果发现反例该怎么办？ 四个「贪心可能失效」的信号 （看到任何一个就警惕）： ① 有全局资源约束且物品不可分割 「容量 / 预算 / 总和上限」+ 不能切开 → 01 背包类， 用 DP 。 （可切开则贪心成立——分数背包。） ② 选择之间有位置或顺序约束 「只能合并相邻」「必须连续」→ 区间 DP 。 反例：石子合并 [4,1,1,4] ，贪心（每次合并最小的相邻两堆）得 18，最优（先合并两端）得 16。 「相邻」这个约束破坏了 Huffman 的前提。 ③ 「恰好」类约束 恰好选 k 个、和恰好为 T → 贪心很难满足「恰好」，通常要 DP。 ④ 前面的选择会「堵死」后面 不满足拟阵的交换性 → DP 或搜索。 最实用的验证手段：暴力对拍 写一个 O(指数) 的暴力枚举（小数据），随机生成几千组测试比对贪心解和暴力解。 贪心的错误往往只在「精心构造的小反例」上暴露 ，而随机对拍恰好擅长找这类反例。 「试了几个例子都对」是最不可靠的证据——零钱兑换需要「大面额不是小面额倍数」这种特定结构，随机测试都可能撞不上。所以既要对拍也要写下交换论证。 发现反例后：立刻放弃贪心，不要修补 修补出来的贪心通常还有别的反例，而且 越来越难验证 。 此时退回 DP 是更快的路 ——DP 的正确性只依赖「状态定义 + 转移完备」，比贪心的「全局最优性」容易验证得多。 工程上也是这个道理：一个需要五条特殊规则才对的贪心，维护成本远高于一个直白的 DP。 高级 N 皇后的位运算优化为什么能快 5–10 倍？ void dfs ( int row, int col, int d1, int d2) { if (row == n) { ++cnt; return ; } int avail = (( 1 << n) - 1 ) & ~(col | d1 | d2); while (avail) { int p = avail & -avail; // lowbit：取最低位的 1 avail ^= p; dfs (row + 1 , col | p, (d1 | p) << 1 , (d2 | p) >> 1 ); } } 四个原因 ① avail 一次算出所有可放位置 ，不需要逐列检查（省掉内层 n 次判断）。 ② avail & -avail 直接取最低位的 1 ——只遍历真正可行的位置，跳过所有被占用的列。 ③ 对角线的移位是关键洞察 ：往下走一行时，主对角线约束向左移一位（ << 1 ）、副对角线向右移一位（ >> 1 ）。这样就不需要用 row+c / row−c 去索引数组， 全部在寄存器里完成 。 ④ 参数传值天然回溯 ——没有数组访问、没有显式的状态恢复。 对角线移位为什么对 主对角线上的格子满足 row + col 相同。从第 row 行走到第 row+1 行时，同一条主对角线对应的列号 减少 1 → 在位掩码里就是 左移一位 。副对角线（ row − col 相同）则列号增加 1 → 右移一位。 把「二维的对角线约束」编码成「一维掩码的移位」，这是本优化最精妙的地方。 还能再快近一倍：对称性剪枝 第一行只放前 n/2 列，结果乘 2（n 为奇数时中间列单独算）。 但注意 ：如果要输出所有 具体方案 而不只是计数，对称性剪枝需要额外镜像还原，代码复杂度上升。 先确认题目要「计数」还是「方案」。 同族技巧 「用位运算把状态压进寄存器」还出现在： 状压 DP 、数独求解（每行/列/宫一个掩码）、图的邻接矩阵用 bitset （传递闭包 O(V³/64)）、 Swiss Table 的 SIMD 控制字节。 高级 什么是分支限界？它和普通回溯、A* 有什么关系？ 分支限界 = 回溯 + 上下界估计 + 优先搜索顺序。 下界（对最小化问题） 估算「从当前状态出发，最少还要花多少代价」。若 已花代价 + 下界 ≥ 已知最优 ， 整个分支剪掉 。 优先搜索顺序 用优先队列先搜「最有希望」的分支（best-first），比 DFS 更早找到好解 ，从而让限界剪得更狠。 这是一个正反馈：好解越早出现，剪枝越有效。 和 A* 的关系 下界必须是「乐观估计」 （不能高于真实值），否则会剪掉最优解—— 这和 A* 要求启发函数「可采纳」（不高估）是完全同一个条件 。 实际上 A* 可以看成分支限界在「最短路」这个特定问题上的实例： f = g + h 里的 h 就是下界。 问题 常用下界 TSP 剩余点的 MST 权重、或每点最小出边之和 装箱 / 调度 ⌈总体积 / 箱容量⌉ 整数规划 LP 松弛 （去掉整数约束后的解） 图着色 最大团的大小 LP 松弛值得单独说 把「变量必须是整数」去掉，问题变成线性规划（ 多项式可解 ），它的解一定不差于整数解 → 天然是一个下界 。 然后在某个非整数变量上分支（ x ≤ ⌊v⌋ 或 x ≥ ⌈v⌉ ），递归求解。 这就是所有商业 ILP 求解器（Gurobi、CPLEX、CBC）的核心框架。 工程上遇到组合优化问题，先试求解器往往比自己写搜索快得多，也更容易维护。 高级 生产上遇到一个需要「最优排班/装箱/路径规划」的需求，怎么落地？ 这类需求几乎都是 NP-hard。 但动手写算法之前，有两步收益更大。 ① 先质疑需求（收益最大的一步） 很多「最优」是把业务约束 抽象过头 的结果。「给 1000 个任务排最优班」是 NP-hard，但业务上往往只要「 95% 的任务在 SLA 内 + 最坏不超过 2 倍 」——那就变成一个贪心能解的问题。 动手前确认真的需要最优解。 ② 量真实输入规模 NP-hard 说的是 最坏情形随 n 增长 的行为。如果生产数据里 n 常年 ≤ 30， ILP 求解器几毫秒就返回最优解 ——这个「NP-hard」在工程上根本不是问题。 先看数据，再谈复杂度类。 确认必须放弃精确解后，按「保证强度」排序的出路： 策略 保证 适合 近似算法 有界近似比 结果要 对外承诺 （计费、合规、配额） ILP / SAT 求解器 最优解，时间不保证 离线批处理，有几小时窗口 限定参数（FPT） k 小时精确 某个参数天然很小 启发式 无保证 结果只影响 内部效率 别掉进的坑 自己从零写启发式（尤其遗传算法这类）往往打不过成熟求解器 ，还留下无人能维护的代码。 先试通用求解器（Gurobi / CBC / OR-Tools），打不过再自己写。 而且求解器通常还免费给你「当前解距离最优的上界」，这个信息对业务沟通极有价值。 还要考虑算法之外的三件事 ① 可解释性 ：排班结果要给人看，「为什么小王被排了周末」必须能回答。一个复杂启发式的输出常常无法解释，这在实际推行时是致命的。 ② 稳定性 ：输入微小变化不应导致排班大幅重排（用户会抗议）。这通常要在目标函数里加「与上一版的差异」惩罚项。 ③ 可干预 ：业务方需要能手工锁定某些安排，然后让算法优化其余部分。 这三点往往比「解好 2%」更决定项目成败。 ⬅️ 动态规划 ➡️ 字符串匹配 🔀 排序全景 📐 复杂度与分析 📚 目录 🏠 主页 贪心与回溯 — 交换论证 · 拟阵直觉 · 区间调度与 Huffman · 回溯三要素与去重 · N 皇后位运算 · 分支限界 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/hash_tables_deep_dive.html","t":"哈希表内核","c":"算法与数据结构","s":[{"a":"","h":"","x":"哈希表内核 · 哈希函数标准 / 拉链与开放寻址 / Robin Hood / Swiss Table 的 SIMD 探测 / 增量 rehash / Hash-DoS Kavin Tech Notes ← 主页 目录 35 min ◐ 哈希表内核 本质：用值算地址 与其他章节的关系 哈希函数 好哈希的三个标准 std::hash 的实现与陷阱 取模 vs 位掩码 组合多个字段 滚动哈希 拉链法 unordered_map 的真实结构 标准为什么逼出这个实现 开放寻址 线性探测与聚簇 二次探测与双重哈希 Robin Hood 删除的墓碑问题 Swiss Table 控制字节与 SIMD 探测 快 2–3 倍的来源 代价：迭代器与引用失效 负载因子与 rehash 探测长度曲线 增量式 rehash 尾延迟 哈希碰撞 DoS 2011 年的事故 SipHash 与随机种子 选型决策表 实现对照（交互） 分布式场景 一致性哈希环 布隆过滤器 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Hash Tables · 2026 哈希表内核 哈希表的 O(1) 是 期望、摊销、且依赖哈希函数质量 的。三个限定词各自对应一类事故：碰撞攻击、rehash 停顿、哈希选错。拉链 / 开放寻址、滚动哈希、一致性哈希和布隆过滤器都以图为主，代码只留探测与哈希更新。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~35 min 前置 复杂度与分析 哈希函数三标准 拉链 vs 开放寻址 Robin Hood Swiss Table SIMD 增量 rehash Hash-DoS"},{"a":"s1","h":"本质：用值算地址","x":"01 本质：用值算地址 以及那个 O(1) 的三个限定词 数组下标是 O(1)，因为地址能直接算。哈希表把这件事推广到任意键： 槽 = h(key) mod 桶数 。键空间远大于桶数， 冲突是必然的 ——工程复杂度全在怎么处理冲突。 桶与碰撞：无限键空间 → 有限桶数组 键 apple apricot banana h(key) 整数指纹 桶数组（8 个） 0 1 2 3 4 5 6 7 碰撞 apple 与 apricot 都进桶 2 banana → 桶 5 无冲突，一次访问即命中 两种解法：拉链（同桶串成链）或开放寻址（占用相邻空槽）→ §3 / §4 O(1) 三个限定：期望均匀 · 摊销（rehash 是 O(n)）· 不含 h(key) 本身（长串是 O(len)） 冲突不可避免。后面整篇都在回答：冲突后走链，还是在数组里接着探。 「哈希表是 O(1)」的三个限定词 ： 期望 ——假设哈希均匀。不成立就退化 O(n)，攻击者可以主动制造（§7）。 摊销 ——单次可能触发 O(n) rehash，那次停顿就是 p99（§6）。 不含哈希计算 ——长键上 h(key) 是 O(len)，计算本身可以是瓶颈。 面试里主动说这三条，比背「平均 O(1) 最坏 O(n)」有用。 像图书馆按首字母分区：找书只去那一区。分区不均 → 一区排长龙（坏哈希）；扩馆要把书全部重上架（rehash，当天谁来都得等）；恶意捐赠同一字母的书 → 碰撞攻击。防御不是换书架，而是让分区规则每次开馆随机变化，让攻击者猜不到该捐什么。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 复杂度与分析 —— 摊销 vs 平均的区别、rehash 的尾延迟、p99 视角 单调栈与前缀和 —— 「前缀和 + 哈希表」是哈希表最高频的算法用途 排序全景 —— 哈希去重 vs sort + unique 的分工 STL 容器深度剖析 —— unordered_map 与 map 的接口层对照 存储与数据架构 —— 一致性哈希、LRU 缓存、跳表 缓存穿透与布隆过滤器 —— 布隆 / Cuckoo 过滤器的完整展开"},{"a":"s2","h":"哈希函数","x":"02 哈希函数 整个数据结构的性能地基"},{"a":"s2-1","h":"2.1 好哈希的三个标准","x":"2.1 好哈希的三个标准 标准 含义 不满足的后果 均匀性 输出在值域上近似均匀分布 桶倾斜 → 链表变长 → 退化 O(n) 雪崩效应 输入改 1 位，输出 约一半 的位翻转 相似键映射到相邻桶 → 聚簇（开放寻址下尤其致命） 速度 短键几个周期，长键接近内存带宽 哈希计算本身成为瓶颈 （可选） 抗碰撞 攻击者无法构造碰撞 Hash-DoS（§7） 雪崩不够：h(x)=x、桶数 1024、掩码 0x3FF 时，1000 的倍数挤成一段 键 1000 2000 3000 4000 x & 0x3FF 1000 976 952 928 步长恒为 −24 → 主聚簇，开放寻址下探测长度爆炸 修法：finalizer 打散（见下方 mix） 输入改 1 位，输出约一半翻转 素数取模能「搅」高位，位掩码不能 所以 2 的幂桶必须配有雪崩的哈希 absl / hashbrown 都内置了这层混淆 均匀不够，还要打散 恒等哈希 + 位掩码会把低位规律直接变成桶倾斜。素数取模能救命，现代实现却都选位掩码——前提是哈希本身够好。 哈希函数 速度（长键） 抗碰撞 典型用途 FNV-1a 慢（逐字节） 否 短键、代码简单优先 MurmurHash3 快 否 曾是事实标准，仍广泛使用 xxHash3 极快（接近内存带宽） 否 大数据、校验、非对抗场景首选 CityHash / FarmHash 快 否 Google 内部，abseil 默认 wyhash 极快 否 新一代，很多 Rust/C++ 库采用 SipHash-1-3 中等 是（带密钥） 处理不可信输入时必选 （Rust/Python 默认） SHA-256 / BLAKE3 慢 密码学级 内容寻址、签名， 不要用于哈希表 哈希表不要用 SHA / MD5——慢几十倍，而哈希表不需要抗第二原像。抗攻击用 SipHash （专为「密钥未知则不可构造碰撞」设计）。反过来也不要用 xxHash 做内容寻址或去重指纹，它有意不抗碰撞。选哈希的第一个问题永远是「输入是否可信」。"},{"a":"s2-2","h":"2.2 std::hash 的实现与陷阱","x":"2.2 std::hash 的实现与陷阱 类型 libstdc++ 的 std::hash 问题 int / 整型 恒等函数 （直接返回值） 零雪崩 。配位掩码桶时低位倾斜的键会严重聚簇 T* 指针 reinterpret 成整数 低位恒为 0 （对齐），同上 std::string MurmurHash 变体（逐字节） O(len)； 无随机种子 → 可被攻击 float / double 按位哈希（0 特判） NaN != NaN 导致 NaN 键永远查不到 std::pair / tuple 没有特化 编译报错，必须自己写 libstdc++ 用素数桶，恒等哈希勉强能用；自己写 2 的幂 + 位掩码 时必须加一层混淆，否则页对齐指针、按 1000 递增的 ID 都会挤成一段。absl / hashbrown 已内置这层 finalizer。 // splitmix64 finalizer：3 条指令，雪崩很好 inline uint64_t mix ( uint64_t x) { x += 0x9E3779B97F4A7C15ULL ; x = (x ^ (x >> 30 )) * 0xBF58476D1CE4E5B9ULL ; x = (x ^ (x >> 27 )) * 0x94D049BB133111EBULL ; return x ^ (x >> 31 ); }"},{"a":"s2-3","h":"2.3 取模 vs 位掩码","x":"2.3 取模 vs 位掩码 素数取模 h % p 整数除法 20–40 周期 哈希再弱也能搅进高位 libstdc++ unordered_map 走这条 责任在「桶映射」 2 的幂 + 位掩码 h & (n−1) 1 条 AND，1 周期；扩容只看 1 个新位 只用低位 → 必须雪崩充分 absl / folly / Rust HashMap 走这条 责任在「哈希函数」 复杂度从桶映射转移到哈希函数。折中：Fibonacci 哈希 (h * 黄金比) >> (64 − log₂ n) ，一次乘法 + 移位，用高位。 现代实现几乎全选位掩码，因为 xxHash / wyhash 已经够快、雪崩充分，用 1 条 AND 换 20 周期的除法划算。自己造表时绝不能再配弱哈希——责任已经全部压在哈希函数上。"},{"a":"s2-4","h":"2.4 组合多个字段","x":"2.4 组合多个字段 std::hash 没有 pair / tuple 特化。异或是最常见的错法：对称（ (a,b) 与 (b,a) 碰撞）且自逆（ a==b 结果恒 0，所有对角元素挤进一个桶）。相加同样对称；高位拼接会丢信息。结构体整块哈希还要小心 padding 字节未定义——两个逻辑相等的对象可能因垃圾值不同而哈希不同。 template < class T> inline void hash_combine ( size_t & seed, const T& v) { seed ^= std::hash<T>{}(v) + 0x9E3779B97F4A7C15ULL + (seed << 6 ) + (seed >> 2 ); } 捷径： absl::flat_hash_map 的 AbslHashValue ，或逐字段 combine。不要对带 padding 的结构体做整块字节哈希。"},{"a":"s2-5","h":"2.5 滚动哈希","x":"2.5 滚动哈希 定长窗口从位置 i 滑到 i+1 时，不必重算 m 个字符：减掉离开的、乘基数、加上进入的。前缀哈希能 O(1) 取任意子串，比定长滚动更常用——这才是它在最长公共子串、二分判定里的价值。完整匹配与碰撞处理见 字符串匹配 。 滚动哈希窗口：H = a·B³ + b·B² + c·B + d s a b c d e f 窗口 i=0 H′ = (H − a·B³) · B + e O(1)，不是 O(m) a b c d e f 窗口 i=1 记住三件事 前缀哈希能 O(1) 取任意子串，比定长滚动更常用 自然溢出 (mod 2⁶⁴) 可被构造碰撞 竞赛：随机基数或双哈希；不可信输入还要真比较 char 要转 unsigned，否则非 ASCII 变负数 这和 Hash-DoS 是同一原理：平均可被攻击 窗口右移只动离开的字符和进入的字符。任意子串相等用前缀哈希；哈希命中后，不可信输入必须再比一遍原文。 // 窗口右移 1：O(1) 更新 H = (H - s[i] * p) * B + s[i + m]; // p = B^{m-1}"},{"a":"s3","h":"拉链法","x":"03 拉链法 std::unordered_map 走的路，以及标准怎么把它逼出来的"},{"a":"s3-1","h":"3.1 unordered_map 的真实结构","x":"3.1 unordered_map 的真实结构 libstdc++ 的 unordered_map：桶数组 + 一条全局单链表 （不是每桶一条） 桶数组 0 1 2 3 4 5 桶存的是「前驱指针」，不是链表头 全局链表 k1 (桶1) k2 (桶1) k3 (桶3) k4 (桶5) k5 (桶5) 为什么是「一条全局链表」 标准要求 begin()/end() 遍历是 O(size) ， 不能是 O(桶数)。若每桶一条独立链表， 遍历就得跳过所有空桶 → O(桶数)。 所以必须把所有节点串成一条。 代价 · 每个节点一次单独 malloc（ 指针追逐 ） · 节点额外 8~16 B（next 指针 + 缓存的 hash） · 查找 = 1 次桶访问 + N 次链表跳转， 每次都可能 cache miss unordered_map 的「桶 + 全局单链表」结构不是实现者的偏好，而是标准的接口要求逼出来的。"},{"a":"s3-2","h":"3.2 标准为什么逼出这个实现","x":"3.2 标准为什么逼出这个实现 C++11 把桶接口、引用稳定性、 max_load_factor > 1 写进契约， 任何合规实现都只能是拉链 。这是标准库设计的著名教训：接口暴露了多少实现细节，直接决定以后还能不能换内核。abseil / folly / Boost.Unordered 主动放弃引用稳定，才换得开放寻址的 2–3 倍。 标准要求 后果 引用与指针在 rehash 后仍有效（只有迭代器失效） 元素必须单独堆分配，不能放连续数组 bucket_count() / bucket(key) / 局部迭代器 必须真有「桶」，结构被锁成拉链 负载因子可 > 1 开放寻址物理上做不到 全表遍历 O(size) 节点必须串成一条全局链，不能每桶独立 插入不使已有引用失效 禁止 Robin Hood 这类「移动已有元素」 拉链的真实优势不是「更慢所以没人要」：引用稳定（可持有 &map[k] 跨插入）、α 可 > 1、删除无墓碑、Java 8 单桶 > 8 可转红黑树把最坏从 O(n) 收到 O(log n)。选型问的是「要不要引用稳定」，不是「谁更快」（§8）。 链 vs 探测：同一组键 {A,B,C} 都哈希到槽 2 拉链 0 1 2 3 A B C 指针追逐 · α 可 > 1 · 引用稳定 开放寻址（线性探测） 0 1 A B C 5 理想槽 +1 +2 连续数组 · cache 友好 · α 必须 < 1 删除不能标空，否则切断探测链（§4.4） 线性探测理论上聚簇最重，实测常最快（预取） 同一碰撞，两种落点。左边付指针与分配，右边付聚簇与墓碑。代码只留下面这一段探测。"},{"a":"s4","h":"开放寻址","x":"04 开放寻址 所有元素放在一个连续数组里"},{"a":"s4-1","h":"4.1 线性探测与聚簇","x":"4.1 线性探测与聚簇 冲突时顺着数组找下一个空位。探测序列连续，符合 cache line 与硬件预取：探 8 个槽可能只 1 次 miss；二次 / 双重哈希每步随机跳，每步都可能 miss。所以线性探测理论聚簇最重，实测常最快——又一个 「渐进相同、常数不同」 。 // 线性探测：唯一保留的查找代码 int find_slot ( const Key& k) { size_t i = hash (k) & mask; while (slots[i].state != EMPTY) { if (slots[i].state == FULL && slots[i].key == k) return i; i = (i + 1 ) & mask; } return - 1 ; // 碰到 EMPTY 才说明不存在 } 1 3 5 10 25 50 期望探测次数 0 0.5 0.7 0.85 0.95 1.0 负载因子 α 查找命中 查找未命中 / 插入 α ≈ 0.85 线性探测的期望探测次数 命中 ： (1 + 1/(1−α)) / 2 未命中： (1 + 1/(1−α)²) / 2 α→1 时 发散 ：α=0.99 未命中要探测 ~5000 次 探测长度在 α > 0.85 之后急剧上升，且「未命中」比「命中」恶化得快得多——这决定了扩容阈值。 未命中曲线按 1/(1−α)² 恶化。以「查不到」为主的负载（缓存 miss、去重）对 α 更敏感——阈值要更低，或前面加布隆过滤器（§9.2）。"},{"a":"s4-2","h":"4.2 二次探测与双重哈希","x":"4.2 二次探测与双重哈希 探测方式 第 i 次位置 聚簇 cache 线性 h + i 主聚簇 最好 二次 h + (i+i²)/2 次聚簇 中 双重哈希 h1 + i·h2(k) 几乎无 每步随机跳 Robin Hood 线性但会抢位 方差极小 最好 二次探测的式子必须能覆盖全部槽，否则表还没满就找不到空位——看起来能跑，高负载才爆。2 的幂桶配 h+(i+i²)/2 可以遍历全部 2^k 个槽；朴素 h+i² 只能覆盖约一半。"},{"a":"s4-3","h":"4.3 Robin Hood","x":"4.3 Robin Hood Robin Hood：劫「离家近」的去帮「离家远」的 槽上数字 = probe distance（实际 − 理想） 0 1 1 4 空 新键 pd=3 撞上 pd=1 → 踢它，自己坐下 不变式：pd 沿探测序列单调不减 查找时可提前终止：当前槽 pd 已小于 查找距离 → 目标不可能在后面 均值仍由 α 决定，但长尾被削平 → p99 好 插入会移动已有元素 → 标准库不能用 不改善平均探测长度，只削平长尾。Rust HashMap 在 1.36 前用它，后改 Swiss Table。 插入时若当前占用者比自己更接近理想位置，就把它踢走、自己坐下，再为被踢者继续找位。不变式让未命中不必走到 EMPTY。代价是插入会移动已有元素，违反 unordered_map 的引用稳定，所以标准库不能用；自造表且对 p99 敏感时仍值得考虑。"},{"a":"s4-4","h":"4.4 删除的墓碑问题","x":"4.4 删除的墓碑问题 删除：EMPTY 切断探测链；DELETED 才能继续走 插入 A→3，B 冲突占 4，C 再占 5 2 A B C 6 ❌ 删 B 标 EMPTY：查 C 在槽 4 停住，误判不存在 ✅ 标 DELETED：查找继续；插入可复用该槽 墓碑不减探测长度，必须计入 α；超阈值 rehash 才能清掉 写多删多 → 选拉链 墓碑表 size 很小，探测却很长 极端：整表墓碑，未命中扫全表 拉链删除只是摘链，无残留 三态 EMPTY / FULL / DELETED。负载因子用 (size + tombstones) / capacity。 墓碑不减少探测长度，只减少 size() 。反复插删会让表看起来很空、探测却很长；极端时整表墓碑，未命中扫全表变成 O(n)。所以必须把墓碑计入 α，超阈值 rehash 清掉。写多删多的负载下拉链反而更稳——选型要问删除频率，不只读写比。"},{"a":"s5","h":"Swiss Table","x":"05 Swiss Table abseil / Rust hashbrown 的做法：用 SIMD 一次探测 16 个槽"},{"a":"s5-1","h":"5.1 控制字节与 SIMD 探测","x":"5.1 控制字节与 SIMD 探测 元数据与数据分开：每槽 1 字节控制信息，16 字节 = 一条 SIMD 寄存器。H1 定位组，H2（7 位）当指纹筛候选。 Swiss Table：每个槽位对应 1 字节控制信息，16 个控制字节 = 1 条 SIMD 寄存器 控制字节 E 7A 3C 5F E 1B D 5F … 共 16 个 = 128 bit E = 空(0x80) D = 墓碑(0xFE) 其余 = 哈希高 7 位 （H2） 一次 SIMD 比较，得到 16 位掩码 m = _mm_movemask_epi8( _mm_cmpeq_epi8(ctrl, set1(H2))) → 掩码里每个 1 表示「这个槽的 H2 匹配」 只对掩码里的位去访问真正的槽位 H2 有 7 位 → 假阳性率约 1/128 ⟹ 绝大多数情况只需比较 1 次 完整 key 而完整 key 的比较才是真正贵的那一步 与传统开放寻址的对比 传统：每探测一个槽 → 访问槽位（可能 cache miss） → 比较完整 key（string 要 memcmp） 探测 8 次 = 8 次 miss + 8 次 memcmp Swiss：控制字节数组极小 16 字节 = 1/4 条 cache line 1 次 miss 拿到 64 个槽的元信息 1 条 SIMD 指令筛掉 127/128 的候选 → 通常只需 1 次完整 key 比较 控制字节把「探测」和「比较」解耦：探测在极小的元数据数组上批量做，只有通过筛选的才去访问真正的键值。 「大概率排除」和「精确确认」分两步 ：H1 定位组，H2（7 位）当指纹。SIMD 在连续 16 字节上一次比完，假阳性约 1/128，通常只比 1 次完整 key。与布隆过滤器同源——区别是控制字节后面就是真数据（§9.2）。传统开放寻址每探一槽都要访问槽位 + memcmp；Swiss 把探测从「大对象数组」挪到「1/4 条 cache line」。"},{"a":"s5-2","h":"5.2 快 2–3 倍的来源","x":"5.2 快 2–3 倍的来源 因素 std::unordered_map absl::flat_hash_map 内存布局 节点散落（每个一次 malloc） 一块连续数组 查找的 cache miss 1（桶）+ N（链表跳转） 通常 2（控制字节 + 槽位） 每元素额外内存 next 指针 + 缓存 hash + malloc 头 ≈ 32 B 1 字节控制字节 桶映射 素数取模（整数除法 ~20 周期） 位掩码（1 周期） key 比较次数 链表上每个节点都比 SIMD 筛完通常只比 1 次 哈希函数 std::hash （整型是恒等） 内置混淆 + 随机种子 分配次数 每次插入一次 仅扩容时 2–3 倍是七项累积，不是换个 std::hash 就能拿到：连续数组、位掩码、1B 元数据、SIMD、内置混淆、仅扩容时分配、更高 α。数据量小（几百）时差距不大，甚至 unordered_map 更好（无 SIMD 开销、无扩容抖动）；随机大表才明显。必须在自己的数据上测，见 性能优化指南 。"},{"a":"s5-3","h":"5.3 代价：迭代器与引用失效","x":"5.3 代价：迭代器与引用失效 元素在数组里，扩容会 移动 —— &map[k] 跨一次可能 rehash 的操作就是悬垂引用。 unordered_map 没有这个问题。需要稳定时用 absl::node_hash_map 。迁移时这是唯一必须审查的点：全仓库搜「持有 map 元素引用跨越后续操作」，并把 ASan 挂进 CI。另外两条：value 必须可移动；迭代顺序刻意随机化，测试挂了先怀疑隐藏的顺序依赖。"},{"a":"s6","h":"负载因子与 rehash","x":"06 负载因子与 rehash 摊销 O(1) 背后的那次停顿"},{"a":"s6-1","h":"6.1 探测长度曲线","x":"6.1 探测长度曲线 装载因子触发 rehash：α 超阈值 → 容量翻倍、全部键重映射 旧表 8 槽 · 已用 7 · α = 0.875 空 插入第 8 个 → 停顿 O(n) 新表 16 槽 · α 回到 ~0.44 · 墓碑一并清掉 拉链 α=1 只是平均链长 1；开放寻址 α→1 探测发散。Swiss 用 SIMD 压低探测常数，才敢用 0.875。 摊销 O(1) 的那一次停顿就发生在这里。低延迟服务要 reserve(峰值) 或渐进式搬迁。 实现 默认 max_load_factor 扩容策略 std::unordered_map （libstdc++） 1.0 下一个素数（约 2 倍） absl::flat_hash_map 约 0.875（7/8） 2 倍 Rust HashMap （hashbrown） 约 0.875 2 倍 Java HashMap 0.75 2 倍；单桶 > 8 转红黑树 Python dict 约 0.66（2/3） 约 3 倍（按已用槽数） 拉链 α=1 只是平均链长 1，还可以 > 1；开放寻址的探测是 1/(1−α) 量级，α=0.9 未命中约 50 次，α→1 发散。Swiss 用 0.875 是因为 SIMD 把「探 16 槽」做成「探 1 槽」的成本，于是可以承受更高占用、更省内存。"},{"a":"s6-2","h":"6.2 增量式 rehash","x":"6.2 增量式 rehash 一次性 rehash 是 O(n) 停顿：分配新表、重算所有键、搬移。n 很大时是几十到几百毫秒。Redis 把它切碎：双表并存，每次增删查改顺带搬一个桶；查找先旧后新，插入只写新表，保证旧表只减不增。 Redis 的渐进式 rehash（rehashidx 从 0 逐步推进到 ht[0] 末尾） ht[0] 旧表 ✓ ✓ → k k k rehashidx ✓ = 已搬空 k = 还有数据 ht[1] 新表 容量是旧表的 2 倍 rehash 期间的读写规则 查找 / 删除： 先查 ht[0]，没找到再查 ht[1] 插入： 一律写 ht[1] （保证 ht[0] 只减不增） 每次增删查改顺带搬 1 个桶；另有定时任务批量搬 效果 单次操作最坏从 O(n) 降到 O(1) + 一个桶 代价：rehash 期间内存占用是两份， 且每次操作要查两张表（常数变大） 渐进式 rehash 把一次长停顿摊成很多次微小开销——总工作量不变，但尾延迟大幅改善。 总工作量不变，但没有单次 O(n)。代价是过渡期内存双份、每次操作查两张表——和增量 GC、在线 DDL、LSM compaction 同一笔交易：用平均成本换尾延迟。"},{"a":"s6-3","h":"6.3 尾延迟","x":"6.3 尾延迟 那次 O(n) 落在哪个请求上，那个请求就是 p99。均值上的摊销完美，对低延迟服务却是抖动来源。 手段 做法 代价 预留容量 启动时 reserve(峰值) ，运行期永不扩容 要能估出峰值；内存常驻 渐进式 rehash 把搬移分摊到后续操作（Redis 做法） 实现复杂；过渡期内存双份、常数变大 换稳定结构 用红黑树（ std::map ）： 永远 O(log n)，无抖动 平均更慢 分片 拆成 N 个小表，rehash 只影响 1/N 跨片操作变复杂 后台 rehash 另起线程建新表，切换时原子替换 需要并发安全设计，内存双份 reserve(n) 保证能装 n 个而不 rehash，实现按 n / max_load_factor 算桶数—— reserve(1000) 在 α=0.875 时会开到约 2048 槽。不要用 rehash(n) 代替，它的参数是桶数不是元素数。完整清单见 低延迟编程 。"},{"a":"s7","h":"哈希碰撞 DoS","x":"07 哈希碰撞 DoS 把「平均 O(1)」变成「最坏 O(n)」的攻击"},{"a":"s7-1","h":"7.1 2011 年的事故","x":"7.1 2011 年的事故 Hash-DoS：公开确定性哈希 → 离线算出碰撞集 → 单桶退化成链 0 1 2 k1 k2 … kn 插入 n 个碰撞键：O(n²) 2011：几 MB POST 打满一核数分钟 同一原理的亲戚 ReDoS · 快排锯齿输入 · zip bomb · 深嵌套 JSON 共同点：某资源维度无上界，且由攻击者控制 处理不可信输入：逐个维度问上界在哪 PHP / Java / Python / Ruby / ASP.NET / V8 当时都中招。问题不是哈希表有 bug，而是平均复杂度可被攻击。 2011 年 28C3 上公布：Web 框架把 POST 字段 / 查询参数 / JSON 键放进哈希表。攻击者离线算出大量哈希相同的字符串当参数名，全部落入同一桶。插入 n 个碰撞键从 O(n) 变成 O(n²) （每次都要沿链查重）。几 MB 的 POST 就能打满一核数分钟。哈希公开且确定，碰撞集成本几乎为零。教训见 复杂度与分析 ：平均复杂度可被攻击。"},{"a":"s7-2","h":"7.2 SipHash 与随机种子","x":"7.2 SipHash 与随机种子 修法不是换数据结构，而是让攻击者 算不出 碰撞： h(secret, key) ，密钥在进程启动时随机。SipHash 的保证就是「密钥未知则不可构造碰撞」。 语言 / 库 现在的默认 Python 3.3+ SipHash-1-3 + 随机种子（ PYTHONHASHSEED 可固定，仅用于调试） Rust HashMap SipHash-1-3 + 随机种子（默认就安全） Ruby / Perl / PHP 7+ SipHash 或加盐 Java HashMap 没换哈希，改成 单桶超 8 个转红黑树 （最坏 O(log n)） absl::flat_hash_map 内置混淆 + 进程级随机种子 std::unordered_map 无随机化， std::hash 完全确定 std::hash 无随机化，是标准层面的缺口。不可信键（HTTP 参数名、JSON 键）不要直接进 unordered_map 。三条修法：① 自定义带 random_device 种子的 hasher（SipHash-1-3）；② 换 absl::flat_hash_map ；③ 同时限制键的个数与长度——即使哈希安全，资源也要有上界。 Java 转红黑树改的是最坏复杂度（O(n)→O(log n)）；其他语言换哈希改的是攻击可行性。可以叠加。转树对用户写的烂 hashCode 也有效。"},{"a":"s8","h":"选型决策表","x":"08 选型决策表 按约束而不是按「哪个快」来选 需求 选什么 理由 通用、无特殊要求 absl::flat_hash_map / ankerl::unordered_dense 2–3 倍性能，内存更省 需要引用/指针稳定 std::unordered_map / absl::node_hash_map flat 版扩容会移动元素 需要有序遍历 / 范围查询 std::map （红黑树） 哈希表无序；要 lower_bound 只能用树 需要稳定的最坏延迟 std::map 或预留容量的哈希表 树永远 O(log n)；哈希 rehash 会抖 键来自不可信输入 带随机种子的 hasher 或 absl std::hash 无随机化，可被 Hash-DoS 元素少（< 几十个） std::vector + 线性查找 连续内存 + 无哈希计算，实测常更快 键是小整数且稠密 std::vector 直接下标 不需要哈希，真正的 O(1) 写多删多 拉链法 开放寻址的墓碑会累积（§4.4） 只需判断「存在过吗」，容忍误判 布隆过滤器 空间小几十倍 多线程高并发读写 folly::ConcurrentHashMap / 分片加锁 标准容器都不是线程安全的 选型按顺序问三件事 ： 元素有多少？ 几十个以内， vector 线性扫描往往打败任何哈希表。 有没有硬约束？ 引用稳定、有序遍历、最坏延迟、线程安全——有约束先满足约束。 键可信吗？ 不可信就必须带随机种子。 三个都过了才比性能。「哪个更快」是最后一个问题。"},{"a":"s8-1","h":"8.1 实现对照（交互）","x":"8.1 实现对照（交互） 五种实现的内部结构与取舍 点击实现名，看它的内部结构、性能来源、以及必须接受的代价。 unordered_map 开放寻址 Robin Hood Swiss Table std::map"},{"a":"s9","h":"分布式场景","x":"09 分布式场景 同一个哈希，换成分片的地址 「哈希 → 桶」换成「哈希 → 机器」就是分片。朴素 hash % N 在 N 从 3 变 4 时约 3/4 的键要迁移——缓存命中率瞬间归零，下游被打穿。一致性哈希把值域看成环，加节点只搬它接管的那段弧，迁移量 ≈ 1/N。"},{"a":"s9-1","h":"9.1 一致性哈希环","x":"9.1 一致性哈希环 一致性哈希环：加一台机器只搬 ≈ 1/N，不是 N/(N+1) A B C D? key key 顺时针遇到的第一个节点 = 归属 加 D 时只搬 C→A 之间落到 D 弧上的键 其余键的归属不变 虚拟节点：每台 100–200 个点撒在环上 大数定律压方差；也可按机器性能加权 Rendezvous / Jump / Redis 16384 槽：见下表 热点键不是一致性哈希能解决的： 爆款 key 仍打爆一台 → 本地缓存或加随机后缀 迁移期读写：Redis 用 ASK/MOVED 让客户端参与 环解决「节点数变化时迁多少」。负载均衡靠虚拟节点；访问量倾斜要另做热点处理。 方案 加/减节点的迁移量 负载均衡 典型使用者 取模 ≈ N/(N+1) 完美 节点数固定的场景 一致性哈希 + 虚拟节点 ≈ 1/N 好（靠虚拟节点数） Cassandra、Dynamo、memcached 客户端 Rendezvous（HRW）哈希 ≈ 1/N 很好 实现更简单，无需维护环 Jump Consistent Hash 最优 完美 Google；但 只支持在末尾增删节点 槽位映射（slot） 可控（按槽迁移） 完美 Redis Cluster（16384 槽） Redis Cluster 不用环，而是把键空间固定切成 16384 槽（ CRC16(key) % 16384 ）再分配给节点。好处是迁移粒度可观测、可暂停、可回滚——你能确切知道「槽 5000 正在从 A 迁到 B」。一致性哈希的迁移是隐式的，出问题难定位。这是「显式间接层」换可运维性。完整讨论见 存储与数据架构 。"},{"a":"s9-2","h":"9.2 布隆过滤器","x":"9.2 布隆过滤器 只需「存在过吗」、允许假阳性时，不必存真实键。k 个哈希把比特数组对应位置 1；查询时有一位是 0 则 一定不在 ，全是 1 则 可能在 （别的键的并集碰巧盖住了）。假阳性率约 (1 − e^{−kn/m})^k ，m 是位数、n 是元素数。完整展开见 缓存穿透与布隆过滤器 。 布隆过滤器：假阳性来自「别的键的比特并集」 bit[16] 0 1 1 3 1 5 6 1 8 9 10 1 12 1 1 15 插入 x → 位置 2, 7, 13 插入 y → 位置 4, 11, 14 查询 z（从未插入） 看 2, 4, 13：全是 1 假阳性：并集碰巧盖住了 z 的指纹 查询 w，位 9 = 0 有一位是 0 → 一定不在 无假阴性；假阳性率由 m、k、n 决定 缓存穿透前置一层布隆，挡住「一定没有」的请求。不能直接清比特删除（会误伤别人）——要删用 Counting Bloom 或 Cuckoo。 Swiss Table 的控制字节是同一思想的精确版：摘要挡住无效访问，后面跟着真数据。布隆不存键，所以没有「再比一次」这条退路，假阳性必须靠把 m 开够来压。缓存场景里用它挡穿透，命中后再查真实存储。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 哈希表的查找是 O(1) 吗？把这句话说完整。 不完整。准确说法是： 期望、摊销 O(1)，且不含哈希计算本身；最坏 O(n) 。三个限定词各自对应一类真实故障。 ① 期望（expected） 建立在「哈希值均匀分布」的假设上。假设不成立就退化到 O(n)——而 攻击者可以主动制造这个不成立 （Hash-DoS）。 ② 摊销（amortized） 单次插入可能触发 rehash ，代价 O(n)。均值上无所谓，但那次停顿落在哪个请求上，那个请求就是 p99。 ③ 不含哈希计算 渐进分析把 h(key) 当 O(1)，但对长字符串它是 O(len) 。键很长时哈希计算本身就是瓶颈。 和红黑树对比时该怎么说 std::map 是 确定性的 O(log n)：没有假设、没有抖动、没有攻击面。所以「哈希表比树快」只在 平均意义 上成立；需要 稳定最坏延迟 时树反而是对的选择。 而且 n 小时（几十到几百）树常常更快——log n 只有 6~8，而哈希要算 hash + 一次随机地址访问。 基础 解决哈希冲突有哪两大类方法？各自的优缺点？ 拉链法（separate chaining） 开放寻址（open addressing） 做法 每桶挂一条链表/树 冲突时在同一数组里找下一个空位 负载因子 可 > 1 必须 < 1 ，且 >0.85 后急剧恶化 引用稳定性 稳定 （元素单独分配） 扩容会移动元素 cache 友好 差 （指针追逐） 好 （连续内存） 每元素额外内存 ≈ 32 B （指针+hash+malloc 头） 0~1 B 删除 简单摘链 需要墓碑 分配次数 每次插入一次 malloc 仅扩容时 谁在用 拉链 ： std::unordered_map 、Java HashMap （单桶超 8 个转红黑树）。 开放寻址 ： absl::flat_hash_map 、Rust HashMap 、Python dict 。 选型判据不是「哪个快」 先问 「需不需要引用稳定性」 （能否持有 &map[k] 跨插入）和 「删除频繁吗」 （开放寻址的墓碑会累积）。两个都不需要，再选开放寻址求性能。 基础 什么是负载因子？它为什么决定了扩容时机？ 负载因子 α = 元素数 / 桶数（或槽数） 。它是「有多挤」的度量，直接决定查找的期望比较次数。 拉链法：α = 平均链长；期望比较 ≈ 1 + α/2 α=1 时约 1.5 次 → 完全可接受， α 可以 > 1 开放寻址：α = 占用率 命中 ： (1 + 1/(1−α)) / 2 未命中： (1 + 1/(1−α) ² ) / 2 ← 恶化更快 α=0.9 未命中约 50 次 α=0.99 未命中约 5000 次 （发散） 所以阈值差别很大 unordered_map （拉链）默认 1.0 ； flat_hash_map （开放寻址）约 0.875 ；Java 0.75；Python 约 0.66。 「未命中恶化更快」的工程含义 以「查不到」为主的负载—— 缓存 miss 检查、去重判断、存在性验证 ——对负载因子更敏感。这类场景该把阈值设更低，或前面加一层布隆过滤器挡掉不存在的键。 Swiss Table 为什么敢用 0.875 因为 SIMD 让「探测 16 个槽」的成本接近「探测 1 个槽」—— 它把探测长度的常数压得极低，于是能承受更高负载因子，从而省内存 。这是个漂亮的连锁收益。 基础 为什么 std::unordered_map 的 std::hash<int> 是恒等函数也能用？什么时候会出问题？ 因为 libstdc++ 的 unordered_map 用 素数桶数取模 。取模会把哈希值的高位信息「搅」进结果，所以即使哈希函数很弱（恒等），分布也还算均匀。 出问题的场合：自己造 2 的幂 + 位掩码的表 位掩码 h & (n-1) 只用低位 。此时恒等哈希会暴露键的低位规律： // 键都是 1000 的倍数，桶数 1024 1000 & 0x3FF = 1000 2000 & 0x3FF = 976 3000 & 0x3FF = 952 // 步长恒定 → 聚簇成一段 指针做键更糟—— 对齐让低位恒为 0 。 这是一个责任转移的取舍 素数取模 把「保证分布」的责任放在桶映射上（哈希函数可以弱），代价是每次一次整数除法（~20 周期）。 位掩码 把责任放在哈希函数上，桶映射只需 1 周期，但 哈希函数必须有雪崩效应 。 现代实现全选后者——因为好的快哈希（xxHash、wyhash）已经足够。 修法 自己加一层混淆（splitmix64 的 finalizer，3 条指令，雪崩很好）： x += 0x9E3779B97F4A7C15 ; x = (x ^ (x>> 30 )) * 0xBF58476D1CE4E5B9 ; x = (x ^ (x>> 27 )) * 0x94D049BB133111EB ; return x ^ (x>> 31 ); 或者直接用 absl::flat_hash_map ——它内置了这层混淆。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 为什么 std::unordered_map 必须是拉链法实现？标准做了什么约束？ 标准把太多 实现细节 写进了接口契约，这些要求联合起来 排除了开放寻址 ： 标准要求 后果 引用与指针在 rehash 后仍有效 元素必须单独堆分配， 不能存在连续数组里 bucket_count() / bucket(key) / 局部迭代器 begin(n) 必须真的有「桶」，暴露了拉链结构 max_load_factor 可 > 1 开放寻址的负载因子 物理上不可能 > 1 全表遍历 O(size) 节点必须串成 一条全局链表 （否则要跳过空桶 → O(桶数)） 插入不使已存在元素的引用失效 禁止 Robin Hood 这类「移动已有元素」的方案 这是标准库设计的一个著名教训 C++11 定接口时把桶接口、引用稳定性、负载因子 > 1 都写进了契约，结果 任何符合标准的实现都必须是拉链法 ，未来无法换成更快的方案。 这就是 abseil、folly、Boost.Unordered 新版都提供 不完全兼容标准接口 的替代品的原因——它们主动放弃引用稳定性换 2–3 倍性能。 「接口暴露了多少实现细节」直接决定未来的优化空间 ——这条经验对设计任何库都成立。 但拉链法也有真优势 ① 引用稳定（可安全持有 &map[k] ，在缓存/对象池/图结构里很有用）； ② 负载因子可 > 1（内存吃紧时不必扩容）； ③ 删除简单，无墓碑； ④ 可按桶退化成树 ——Java 8 单桶超 8 个转红黑树，把最坏从 O(n) 降到 O(log n)，这是拉链法独有的能力。 中级 开放寻址的删除为什么需要「墓碑」？墓碑又带来什么问题？ 因为直接标 EMPTY 会 切断探测链 ： // A→槽3, B→槽3冲突占4, C→槽3冲突占5 // 槽: [.., .., .., A, B, C, ..] // 删 B 标 EMPTY → [.., .., .., A, _, C, ..] // 查 C：槽3(A 不匹配) → 槽4 是 EMPTY → 判定「不存在」 // 但 C 明明在槽5！ 墓碑（三态） 状态改为 EMPTY / FULL / DELETED ： · 查找遇到 DELETED 继续探测 （不终止） · 插入遇到 DELETED 可以复用 该槽 · 只有遇到 EMPTY 才判定不存在 墓碑带来的新问题 墓碑不减少探测长度，只减少元素个数。 反复插入删除的表会不断累积墓碑，导致： · size() 很小但探测很长（有效负载因子被墓碑撑高） · 极端情况整表全是墓碑 → 任何未命中查找都要扫全表 → O(n) 标准解法 把墓碑计入负载因子 ： load = (size + tombstones) / capacity ，超阈值就 rehash——rehash 会自然清掉所有墓碑。 选型含义 这是开放寻址在 「写多删多」负载 下的固有弱点。这类场景拉链法反而更稳（删除只是摘链，无残留）。 选型时要问的不只是读写比，还有删除频率 ——这一点经常被忽略。 中级 Swiss Table（ absl::flat_hash_map ）为什么比 unordered_map 快 2–3 倍？ 核心创新是 元数据与数据分离 ：一个紧凑的「控制字节数组」（每槽 1 字节）+ 一个槽位数组。哈希值拆成两部分用： H1 定位组， H2 （7 位）存进控制字节当 指纹 。 m = _mm_movemask_epi8(_mm_cmpeq_epi8(ctrl, set1(H2))) 一条 SIMD 指令比较 16 个 控制字节 → 得到 16 位掩码 只对掩码里的位去访问真正的槽位 H2 有 7 位 ⟹ 假阳性率约 1/128 ⟹ 通常只需比较 1 次完整 key 关键是「探测」和「比较」解耦 传统开放寻址每探测一个槽都要：访问槽位（可能 cache miss）+ 比较完整 key（string 要 memcmp）。 Swiss Table 的探测发生在 极小的连续元数据数组 上——16 字节只占 1/4 条 cache line，一次 miss 能拿到 64 个槽的元信息。 但 2–3 倍不来自单个技巧，是七项累积 连续内存（vs 节点散落）、位掩码（vs 素数取模的整数除法 ~20 周期）、1 字节元数据（vs 每元素 ~32 B 额外内存）、SIMD 筛选、内置哈希混淆、仅扩容时分配、更高的负载因子。 所以「只把 std::hash 换成 xxHash」提升有限 ——真正的瓶颈是内存布局和分配。 代价：引用与迭代器失效 元素直接存在数组里，扩容会 移动元素 ： auto & v = map[ \"a\" ]; map[ \"b\" ] = 1 ; // 可能扩容 → v 悬垂 v = 2 ; // use-after-free 需要引用稳定就用 absl::node_hash_map 。 迁移时唯一要审查的就是这一点 ——全仓库搜「持有 map 元素引用跨越后续 map 操作」，并把 ASan 挂进 CI。 还有两个次要差异 value 必须 可移动 ；迭代顺序 刻意随机化 （防止代码依赖遍历顺序）——迁移后测试挂了，先怀疑是不是有隐藏的顺序依赖。 同源思想 控制字节 = 一个小的、可能假阳性的摘要，用来挡住绝大多数无效访问——这和 布隆过滤器 完全同源。区别只是布隆不存真实数据。 中级 自己给 std::pair 写 hasher，为什么不能用异或组合？ 因为 异或是对称且自逆的 ，会产生两类系统性碰撞： ① 对称 → 顺序信息丢失 h(a) ^ h(b) == h(b) ^ h(a) ⟹ (1,2) 和 (2,1) 必然碰撞 。用 pair 表示「有向边」或「坐标」时这是灾难。 ② 自逆 → 对角线全归一桶 a == b 时 h(a) ^ h(a) == 0 ⟹ 所有 (x, x) 挤进同一个桶。 相加也有同样的对称问题；简单移位 (h1 << 32) | h2 在 h2 超过 32 位时冲突。 正确写法：Boost 风格 hash_combine template < class T> void hash_combine ( size_t & seed, const T& v) { seed ^= std::hash<T>{}(v) + 0x9E3779B97F4A7C15ULL // 黄金比例常数 + (seed << 6 ) + (seed >> 2 ); // 混入已有 seed } 它 顺序敏感 且有雪崩效应。 两条捷径 ① 用 absl::flat_hash_map ——abseil 为 pair / tuple /容器都提供了正确的 AbslHashValue ； ② 逐字段拼成字节序列后整体 xxHash 。 整体字节哈希的隐藏坑 结构体的 padding 字节内容是未定义的 ——两个逻辑相等的对象可能因 padding 里的垃圾值不同而哈希不同。要么 static_assert 确认无 padding，要么逐字段 combine。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 什么是 Hash-DoS？为什么 C++ 的 std::unordered_map 有风险，怎么修？ 2011 年 28C3 公布的攻击：Web 框架把 HTTP 参数名 / JSON 键放进哈希表，攻击者 预先离线算出大量哈希相同的字符串 发过去，全部落进同一桶。 插入 n 个碰撞键的总代价：O(n) → O(n²) （每次插入都要遍历整条链查重） 当时的数字：几十 KB ~ 几 MB 的 POST 就能打满一个 CPU 核数分钟 受影响的有 PHP、Java、Python、Ruby、ASP.NET、V8——都因为用了 确定性的、算法公开的 哈希函数。 C++ 的问题在标准层面 std::hash<std::string> 没有随机化 ：同一字符串在同一实现上永远返回同一值，算法还是公开的。 所以 把不可信输入（参数名、JSON 键、上传字段名）直接当键放进 std::unordered_map 是有风险的 。 修法一 · 带随机种子的 hasher struct SeededHash { static const uint64_t seed; // 启动时从 random_device 取 size_t operator ()(std::string_view s) const { return siphash13 (seed, s. data (), s. size ()); } }; SipHash 的安全性正是「密钥未知时不可构造碰撞」 ——这是它相对 xxHash/Murmur 的唯一优势，也是它存在的理由。 修法二 · 换 absl（内置随机种子） absl::flat_hash_map 自带进程级随机种子 + 哈希混淆。 修法三 · 限制键数量与长度（必须同时做） if (params.size() > kMaxParams) reject(); ——即使哈希安全也该做。 这挡住的是一整类「资源无上界」问题 ，不只哈希碰撞。 Java 的另一条路 Java 没换哈希，而是 单桶超 8 个转红黑树 。这改的是「最坏复杂度」（O(n)→O(log n)，攻击仍能发生但危害可控）；换哈希改的是「攻击可行性」（最坏仍 O(n) 但构造不出来）。 两者可叠加。转树还对 非恶意 的坏哈希有效（比如用户自己写了糟糕的 hashCode ）。 一般性原理 教训不是「哈希表有 bug」，而是 「平均复杂度可被攻击」 。同类漏洞：ReDoS（正则回溯指数级）、快排复杂度攻击（针对三点取中构造锯齿输入）、zip bomb、递归解析爆栈。 共同点：某个资源维度没有上界，而它由攻击者控制。 写处理不可信输入的代码时，逐个资源维度问「上界在哪、谁保证」。 高级 一个哈希表在线上偶发 200ms 的延迟尖刺，怎么排查和消除？ 「偶发」+「尖刺」+ 哈希表，几乎可以直接指向 rehash ：单次插入触发 O(n) 的重建（分配新表 + 重算所有键的桶 + 搬移 + 释放旧表）。 ① 确认 尖刺频率应该随元素数呈 指数递减 （每翻倍一次 rehash）；用 perf 抓尖刺时的栈，会看到 _M_rehash / resize / operator new 。也可以打点记录 bucket_count() 的变化时刻与尖刺时刻对齐。 别忽略另外两个嫌疑 ① 如果表很大且元素是 单独 malloc 的节点（ unordered_map ），rehash 时的 内存分配器行为 （可能触发 mmap 、页错误、甚至 THP 整理）比搬移本身更贵； ② 释放旧表时的大量 free 也是一笔。用 heap profiler 确认。 按性价比排序的消除手段： ① 预留容量（首选） 启动时 reserve(峰值) ，运行期永不扩容——把摊销 O(1) 变成 最坏 O(1)。 注意 reserve(n) 的参数是元素数 （内部按 n / max_load_factor 算桶数），而 rehash(n) 的参数是 桶数 ——这是高频误用。 ② 分片 拆成 N 个小表（ shard = hash(k) % N ），rehash 只影响 1/N 的数据，单次停顿降到 1/N。顺带还能降低锁竞争。代价是跨片操作（如全表遍历、批量原子操作）变复杂。 ③ 渐进式 rehash（Redis 的做法） 双表并存， rehashidx 逐步推进：查找/删除先查旧表再查新表， 插入一律写新表 （保证旧表只减不增），每次操作顺带搬 1 个桶。 单次最坏从 O(n) 降到 O(1)+一个桶。代价： 过渡期内存双份 ，且每次操作要查两张表（常数变大）。 ④ 换稳定结构 std::map 永远 O(log n)、 无抖动 。平均更慢，但如果需求是「p99 稳定」而不是「均值最快」，这是对的选择。 这是一笔通用的交易 「用平均成本换尾延迟」——渐进式 rehash、GC 的增量回收、数据库的在线 DDL、LSM 的分层 compaction，全是同一笔交易。 做决定前先确认目标是「均值」还是「p99」 ：扇出 100 次的服务里，单次 p99=200ms 意味着 63% 的用户请求会撞上它（见 复杂度与分析 §8.2）。 高级 你要给一个高频交易系统选 key-value 容器，怎么决策？ 高频交易的核心约束是 最坏延迟（p99.9）而不是平均延迟 ，这会让常规的「哈希表最快」结论完全翻转。 先排除的：默认的 unordered_map 三个理由： ① rehash 造成不可预测的停顿； ② 每次插入一次 malloc —— malloc 自身的尾延迟不受控 （可能触发 mmap、页错误、锁竞争）； ③ 指针追逐导致 cache miss 不可预测。 ① 首选：预分配的开放寻址表 启动时 reserve 到峰值容量， 运行期零分配、零 rehash 。连续内存 + 位掩码，访问模式可预测。 如果键是 稠密小整数 （instrument id、order id 段），直接用 std::vector 下标——真正的 O(1)，无哈希计算，最可预测。 这一条最常被跳过，但往往是最优解。 ② 元素少时用线性扫描的数组 几十个以内（比如订阅的合约列表）， vector<pair<K,V>> + 线性查找往往打败任何哈希表：连续内存、无哈希计算、无分支预测失败、可 SIMD。 先量一下真实规模再谈结构。 ③ 需要有序（价格档位、时间序） 不要用哈希表。价格档位这类要 lower_bound 的场景用 排序数组 + 二分 （比 std::map 快得多，因为连续内存），或者干脆用 按价格直接下标的数组 （价格离散化后值域有限）。 ④ 别忘了内存局部性之外的约束 NUMA ：表要分配在访问它的核所在的 NUMA 节点上； 大页 ：大表用 hugepage 减少 TLB miss； 禁用 THP 的整理 （可能造成停顿）； 预热 ：启动后先跑一遍把页真正映射进来（首次触碰的页错误也是尖刺）。 见 低延迟编程 与 NUMA 与内存 。 决策顺序（这才是答案） ① 量真实规模和访问模式 → 可能根本不需要哈希表； ② 列出硬约束（最坏延迟、有序性、单线程还是并发）； ③ 消除所有运行期动态分配； ④ 在 真实数据和真实延迟分布 上测，看 p99.9 而不是均值。 「哪个容器快」是最后一个问题 ——前三步做对了，容器选择的影响反而变小。 高级 把「哈希 → 桶」换成「哈希 → 机器」，会遇到什么新问题？ 核心新问题是： 桶数（机器数）会变，而变化的代价是数据迁移而不是内存重分配 。 朴素取模的灾难 node = hash(key) % N 。N 从 3 变 4 时， 约 N/(N+1) 的键都要迁移 ——缓存场景下命中率瞬间归零，全部打到后端 → 雪崩 。 对比单机哈希表：rehash 也要重算所有键，但那只是内存操作；分布式里这是跨网络搬 TB 级数据。 一致性哈希 把哈希值域看成一个 环 ，节点落在环上若干点，key 顺时针找到的第一个节点就是归属。 加节点时只有它接管的那段键要迁移，迁移量 ≈ 1/N。 虚拟节点为什么必需 每台机器只占一个点时，N 台把环切成 N 段， 段长方差很大 ——某台机器可能负责 3 倍于平均的键。虚拟节点（每机 100–200 个点）用大数定律把方差压下来。 额外好处： 可以按机器性能分配不同数量的虚拟节点 ，实现加权分片。 方案 迁移量 特点 取模 ≈N/(N+1) 仅适合节点数固定 一致性哈希+虚节点 ≈1/N Cassandra、Dynamo、memcached Rendezvous（HRW） ≈1/N 实现更简单，无需维护环 Jump Consistent Hash 最优 但 只支持末尾增删节点 固定槽位 可控 Redis Cluster（16384 槽） Redis Cluster 的选择值得单独学 它 不用 一致性哈希，而是把键空间固定切成 16384 槽（ CRC16(key) % 16384 ），再把槽分配给节点。 好处是 迁移的粒度和进度完全可控可观测 ——你能确切知道「槽 5000 正在从 A 迁到 B」，能暂停、能回滚、能监控进度。一致性哈希的迁移是隐式的，出问题很难定位。 代价是多了一层槽→节点的映射要维护和同步。 这是「显式的间接层」换「可运维性」的典型选择 ——在分布式系统里，可运维性常常比理论最优更值钱。 还有两个必须处理的问题 热点键 ：一致性哈希解决的是「键的数量」均衡，不解决「访问量」均衡。一个爆款商品的 key 会打爆它所在的那台机器——要靠本地缓存、key 加随机后缀分散、或单独处理热点。 迁移期间的一致性 ：数据正在从 A 搬到 B 时，读写该去哪？Redis Cluster 用 ASK / MOVED 重定向，本质是让客户端参与解决。见 存储与数据架构 。 ⬅️ 单调栈与前缀和 ➡️ 堆与 Top-K 📐 复杂度与分析 🗄️ 存储与数据架构 📚 目录 🏠 主页 哈希表内核 — 哈希函数标准 · 拉链与开放寻址 · Robin Hood · Swiss Table 的 SIMD 探测 · 增量 rehash · Hash-DoS — Kavin Tech Notes · 2026"}]},{"u":"algorithms/heap_and_topk.html","t":"堆与 Top","c":"算法与数据结构","s":[{"a":"","h":"","x":"堆与 Top-K · 建堆 Θ(n) 证明 / priority_queue 的坑 / Top-K 四解法选型 / 对顶堆 / 可删除堆 / 定时器堆与时间轮 Kavin Tech Notes ← 主页 目录 35 min ◐ 堆与 Top-K 本质：只承诺最值 与其他章节的关系 二叉堆手写 数组表示 上浮与下沉 建堆是 Θ(n) 的证明 STL heap 算法族 priority_queue 的坑 比较器方向反直觉 不能改、不能删任意元素 Top-K 四解法选型 四种做法对照 分布式 Top-K 流式与在线 对顶堆求中位数 滑动窗口中位数 近似 heavy hitters 可删除堆的三条路 延迟删除 索引堆 换 multiset 堆的变体 d-ary 堆 斐波那契堆与配对堆 K 路归并与败者树 定时器：堆 vs 时间轮 三种方案对照 选型（交互） 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Heap & Top-K · 2026 堆与 Top-K 堆 只承诺根是最值 ：放弃全序，换 O(log n) 插删和 O(1) 取最值。Top-K、调度、归并够用；要删任意元素、中位数、有序遍历就不够。下面讲建堆为何是 Θ(n)、 priority_queue 比较器为何反着、Top-K 四种解法怎么选，以及定时器何时该换时间轮。 核心难度 ★★★☆☆ 面试出现率 极高 阅读时长 ~35 min 前置 复杂度 · 排序 建堆 Θ(n) 证明 priority_queue 三坑 Top-K 四解法 对顶堆 可删除堆 堆 vs 时间轮"},{"a":"s1","h":"本质：只承诺最值","x":"01 本质：只承诺最值 放弃全序，换来插删的 O(log n) 排序给全序但插入 O(n)；哈希 O(1) 查找但完全无序。 堆只维护「父 ≥ 子」 ：取最值 O(1)，插删沿一条路径 O(log n)。它刻意做得不够有序——够用时（Top-K、调度、归并）是最优时空权衡，不够用时（删任意、中位数、有序遍历）就要换结构或打补丁。 大顶堆 ：完全二叉树 + 每个节点 ≥ 其子。没承诺的：兄弟无序、第二大只知是根的某个孩子、中序不是有序序列、查/删任意元素 O(n)。 这些「不承诺」才让插删只要走一条路径。 操作 堆 排序数组 平衡树 哈希表 取最值 O(1) O(1) O(log n) O(n) 插入 O(log n) O(n) O(log n) O(1) 删最值 O(log n) O(1) （末尾） O(log n) O(n) 删任意元素 O(n) 找 + O(log n) 调 O(n) O(log n) O(1) 查任意元素 O(n) O(log n) O(log n) O(1) 有序遍历 否 O(n) O(n) 否 建立 Θ(n) O(n log n) O(n log n) O(n) 大顶堆只承诺一件事：根是最值。其余顺序一律不维护 50 30 40 O(1) 取最值 30 与 40 谁大？堆不保证 第二大在哪 一定是根的某个孩子，不知哪个 中序 ≠ 有序 和 BST 完全不同，别拿来遍历 查 / 删任意元素 O(n) 找位置 → 见 §6 三条补法 插删只要一条路径 树高 ⌊log₂ n⌋ → O(log n) 堆像急诊分诊台：只保证下一个最危急；不问「第 37 危急是谁」，按姓名移除也很贵。 分诊台不给所有病人排一到一百的号——那太贵。来了新人只需和上级比几次（O(log n)）；想知道第 37 危急是谁？ 做不到 。有人自己走了？得先 O(n) 按姓名找出来。 §6 的三条补法就是在给这张台加索引。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 最短路与最小生成树 —— Dijkstra 的延迟删除 / decrease-key 在那一页展开，本文不重复写一份 排序全景 —— 堆排序、 partial_sort 、 nth_element 的对照 复杂度与分析 —— 建堆 Θ(n) 的求和技巧、摊销分析 单调栈与前缀和 —— 单调队列与堆在「窗口极值」上的分工 哈希表内核 —— 索引堆需要哈希表做「元素 → 位置」映射 STL 容器深度剖析 —— priority_queue 的接口层 C++ / Linux 系统设计 —— 定时器与时间轮的完整设计题"},{"a":"s2","h":"二叉堆手写","x":"02 二叉堆手写 用数组表示一棵树"},{"a":"s2-1","h":"2.1 数组表示","x":"2.1 数组表示 完全二叉树除最后一层全满、最后一层左对齐，数组才没有空洞。下标全是算术： parent=(i−1)/2 、 left=2i+1 、 right=2i+2 。零指针、cache 友好——这是堆常数远小于平衡树的原因。树高 ⌊log₂ n⌋，所以插删都是 O(log n)。 大顶堆：完全二叉树 ⟹ 可以无缝压进数组，无需指针 50 30 40 10 20 35 i=0 1 2 3 4 5 数组 50 30 40 10 20 35 0 1 2 3 4 5 下标关系（0-based） parent(i) = (i − 1) / 2 left(i) = 2i + 1 right(i) = 2i + 2 纯算术，无指针 ⟹ 零额外内存、cache 友好 「完全二叉树」这个前提很关键 只有除最后一层外全满、最后一层左对齐， 才能保证数组里 没有空洞 。 树高 = ⌊log₂n⌋ ⟹ 插删都是 O(log n) 堆用「完全二叉树 + 数组下标算术」代替指针——这是它常数因子远小于平衡树的根本原因。前提必须是完全二叉树，否则数组里会有空洞，下标公式全部失效。"},{"a":"s2-2","h":"2.2 上浮与下沉","x":"2.2 上浮与下沉 插删都只走一条路径：上浮对新叶，下沉对新根 sift-up · 插入 45 先放末尾，再和父比，大就换 50 30 45 ↑ 与 50 比后停 sift-down · 删根 末尾补到根，挑较大孩子下沉 10 30 40 ↓ 跟 40 换 实现用「取出待放值、沿路只搬父/子、最后一次落位」，比反复 swap 少约 2/3 赋值——libstdc++ 也是这样。 template < class T, class Cmp = std::less<T>> class BinaryHeap { std::vector<T> a_; Cmp less_; // less_(x, y) 为真表示 x 优先级更低 void sift_up ( size_t i) { // 插入后用：新元素往上冒 T v = std:: move (a_[i]); while (i > 0 ) { size_t p = (i - 1 ) / 2 ; if (! less_ (a_[p], v)) break ; // 父不比 v 小 → 就位 a_[i] = std:: move (a_[p]); // 父下移（不是交换：省一半赋值） i = p; } a_[i] = std:: move (v); } void sift_down ( size_t i) { // 删根后用：新根往下沉 size_t n = a_. size (); T v = std:: move (a_[i]); while ( true ) { size_t l = 2 * i + 1 ; if (l >= n) break ; // 挑较大的孩子 size_t c = (l + 1 < n && less_ (a_[l], a_[l + 1 ])) ? l + 1 : l; if (! less_ (v, a_[c])) break ; // v 不比孩子小 → 就位 a_[i] = std:: move (a_[c]); i = c; } a_[i] = std:: move (v); } public : const T& top () const { return a_[ 0 ]; } bool empty () const { return a_. empty (); } void push (T v) { a_. push_back (std:: move (v)); sift_up (a_. size () - 1 ); } void pop () { a_[ 0 ] = std:: move (a_. back ()); // 末尾补到根 a_. pop_back (); if (!a_. empty ()) sift_down ( 0 ); } void build (std::vector<T> v) { // Θ(n) 建堆 a_ = std:: move (v); if (a_. size () < 2 ) return ; for ( size_t i = a_. size () / 2 ; i-- > 0 ; ) sift_down (i); } }; 实现上两处细节让手写版比朴素 swap 快：先取出待放值、沿路只搬父/子、最后一次落位，赋值少约三分之二；下沉先挑较大孩子再比，每层 2 次比较。这就是 libstdc++ 的 __push_heap / __adjust_heap 。比较器 less_(x,y) 表示 x 优先级更低，与 priority_queue 同向。"},{"a":"s2-3","h":"2.3 建堆是 Θ(n) 的证明","x":"2.3 建堆是 Θ(n) 的证明 面试陷阱：n 个元素建堆是 O(n) 还是 O(n log n)？ 取决于怎么建。 自底向上 sift-down：数量最多的叶子几乎不动 → 总和有界 h 1 个 × h 层 1 1 n/4 个 × 1 层 0 0 0 0 叶子 n/2 个 × 0 层 从最后一个非叶往前 sift-down ✅ 自底向上 · Θ(n) T ≤ Σ 2^d · (h−d) = 2^h · Σ k/2^k Σ k/2^k → 2，故 T ≤ 2n ❌ 逐个 push · Θ(n log n) sift-up 处理最深节点 一半叶子都要爬满 log n 层 优化要对着数量最多的那部分：堆里最多的是叶子，让叶子什么都不做，就赢掉一个 log。堆排序第二阶段仍是 n 次 sift-down，所以仍是 Θ(n log n)。 逐个 push 像让每个新人从底层走完晋升：大多数人本就该在底层，却被逼爬满 log n 层。自底向上只从非叶开始问「手下有没有比你强的」——叶子没下属、天然合法。 建堆 Θ(n) 推不出堆排序 O(n) ：第二阶段 n 次「取根 + 下沉」省不掉，这是同一道题的常见追问。"},{"a":"s2-4","h":"2.4 STL heap 算法族","x":"2.4 STL heap 算法族 算法 作用 复杂度 std::make_heap(f, l) 把区间整理成堆 Θ(n) （自底向上） std::push_heap(f, l) 假定 [f, l-1) 已是堆，把 *(l-1) 并入 O(log n) std::pop_heap(f, l) 把最大值 换到末尾 ， [f, l-1) 仍是堆 O(log n) std::sort_heap(f, l) 对堆做堆排序 → 升序 O(n log n) std::is_heap(f, l) 检查是否满足堆性质 O(n) 堆排 = make_heap Θ(n) + n 次「顶换到尾 + sift-down」→ 仍 Θ(n log n) ① 已是大顶堆 50 30 40 10 ② pop_heap：50 ↔ 10 10 30 40 50 已就位 ③ 对新根 10 sift-down，堆缩小 1 40 30 10 50 pop_heap 不删元素 只换到末尾，自己 pop_back 直接用 heap 算法族能拿到底层 vector：可遍历、可 reserve、必要时重建。priority_queue 这些都不给（§3）。 pop_heap 不删元素，只把顶换到末尾再下沉，真正删除要自己 pop_back() ——和 std::remove 一样，算法只有迭代器、改不了容器大小。区间构造内部走 make_heap ，循环 push 实测能差 3–5 倍。 std::make_heap (a. begin (), a. end ()); // Θ(n) std::sort_heap (a. begin (), a. end ()); // 升序；第二阶段仍 Θ(n log n)"},{"a":"s3","h":"priority_queue 的坑","x":"03 priority_queue 的坑 三个高频误用"},{"a":"s3-1","h":"3.1 比较器方向反直觉","x":"3.1 比较器方向反直觉 top() 返回的是比较器下「最大」的那个：less → 大顶，greater → 小顶 ❌ 想要小顶却写了 less priority_queue<int> // 大顶 priority_queue<int, vec, less> less(a,b)=a<b → 小的后出 和默认一样，还是大顶堆 ✅ 小顶堆用 greater priority_queue<int, vec, greater> greater 下「最大」= 数值最小 自定义：deadline 小的先出 return a.deadline > b.deadline; Compare 语义是「a 优先级低于 b」，和 std::sort 的「a 应排在 b 前」正好反着——C++ 里最容易混的一对接口。 记忆方法 ： top() 返回的是这个比较器下「最大」的元素。传 greater 时，按 greater 定义的「最大」就是数值最小的 → 小顶堆。自定义类型同样反着读：想让 deadline 小的先出，写 return a.deadline > b.deadline; 。"},{"a":"s3-2","h":"3.2 不能改、不能删任意元素","x":"3.2 不能改、不能删任意元素 top() 返回 const 引用，没有 begin() ，没有 reserve 。封装换来的是「用法极简单」；一旦要改优先级、删任意元素或遍历，这笔账立刻亏。 你想做的 priority_queue 支持吗 该怎么办 修改某个元素的优先级 否 （ top() 返回 const 引用） 延迟删除，或用索引堆 删除任意元素 否 同上，或换 multiset 遍历所有元素 否 （没有 begin() ） 用 make_heap + 裸 vector 预留容量 否 （无 reserve ） 用带初值的构造，或裸 vector Θ(n) 批量建堆 是 （区间构造函数） priority_queue(v.begin(), v.end()) 取出底层容器 受保护（ c 是 protected） 继承后暴露，或干脆别用它 堆 vs 有序集：只要最值用堆；要删任意 / 两端 / 遍历就换 multiset priority_queue / 裸堆 取最值 O(1) · 插删最值 O(log n) 数组连续，常数小，可 Θ(n) 建 不能改优先级 / 删任意 / 遍历 无 reserve；底层 c 是 protected 区间构造 = make_heap，别循环 push multiset / set begin / rbegin 同时取最小最大 erase(find(x)) 删一个 · O(log n) 能遍历、能按值定位 红黑树：指针追逐 + 每节点分配 erase(x) 会删光所有等于 x 的 生产里优先 vector + heap 算法族，用法极简单才用 priority_queue。需要删任意元素见 §6：能用 multiset 就用，除非 profile 打到常数。 底层 c 是 protected 不是 private ，继承后暴露是合法但丑的技巧。生产更倾向裸 vector + heap 算法族：多写两行，换 reserve 、遍历和必要时重建。"},{"a":"s4","h":"Top-K 四解法选型","x":"04 Top-K 四解法选型 最常见的堆应用，也最常选错"},{"a":"s4-1","h":"4.1 四种做法对照","x":"4.1 四种做法对照 方法 时间 空间 前提 / 特点 ① 全排序取前 k O(n log n) O(1) 最直观，也最浪费 ② partial_sort O(n log k) O(1) 前 k 个 有序 ③ nth_element + 局部 sort O(n) O(1) 最快 ；但会 打乱原数组 ④ size-k 最小堆 O(n log k) O(k) 唯一能处理流式 / 数据装不下 求最大 k 个用 size=k 的最小堆：堆顶是门槛，不是冠军 当前候选（k=3） 7 门槛 9 12 流来 8 8 > 7 → 踢掉门槛 push 8，堆仍是 3 个 流来 4 → 4 ≤ 7，丢掉 为什么不能用最大堆 堆顶会是候选里最大的 那信息没用：不知道踢谁 规则：堆顶 = 最易淘汰的 能装内存且可打乱 → nth_element O(n) 最快；流式 / 装不下 → 只有 size-k 堆。k 接近 n 时直接 sort。 选型按顺序问：① 数据能全装内存吗？不能 → 只有 ④。② 数组能打乱吗？不能 → ② 或 ④，或先拷一份再用 ③。③ 前 k 个要有序吗？只要「是这 k 个」→ ③ 单独用更快。④ k 接近 n？直接 sort （log k ≈ log n）。n=10⁷、k=100 时全排序和 nth_element 能差 20 倍以上——这一步经常被跳过。 // 流式：求最大 k 个，维护 size=k 的最小堆 std::priority_queue< int , std::vector< int >, std::greater< int >> pq; for ( int x : stream) { if (( int )pq. size () < k) pq. push (x); else if (x > pq. top ()) { pq. pop (); pq. push (x); } } nth_element 平均 O(n)，最坏可退化（libstdc++ 用 introselect 兜底）。它会打乱原数组：若原序还要给别人用，先拷一份，或改用堆 / partial_sort 。求最小 k 个则改成 size-k 的 最大堆 ——规则不变：堆顶应是最容易被淘汰的那个。"},{"a":"s4-2","h":"4.2 分布式 Top-K","x":"4.2 分布式 Top-K 各分片先出局部 Top-K 再归并：全局 Top-K 必然是某分片的 Top-K 之一，网络从 O(n) 降到 O(M×k)。 只对「值在单分片内就确定」成立。 「按总点击取 Top-100」不能局部 Top-K 归并——要先按 key shuffle 分片 A 商品 X 排第 101 分片 B 商品 X 排第 101 分片 C 商品 X 排第 101 局部都不报 X 加总却可能第 1 可加指标（max / 单值）→ 局部归并对。要聚合（sum、点击）→ reduceByKey 必须在 top 之前，或 Count-Min Sketch 近似。 不想 shuffle 全量：各分片报 Top-(k×c) 加误差界，或用可合并的 sketch。 判据只有一句：元素的最终值能否在单个分片内确定。能（最大的 100 个数）→ 局部归并对；不能（按总点击）→ 必须先 shuffle 或接受近似。 可加指标（max、单值比较）→ 局部归并对：若某元素连本分片前 k 都进不去，本分片已有 k 个比它大，全局更不可能。要跨分片聚合（总点击）→ reduceByKey 必须在 top 之前。不想 shuffle 全量：各分片报 Top-(k×c) 加误差界，或 Count-Min Sketch（可合并，§5.3）。"},{"a":"s5","h":"流式与在线","x":"05 流式与在线 数据一条条来，答案要随时可取"},{"a":"s5-1","h":"5.1 对顶堆求中位数","x":"5.1 对顶堆求中位数 「数据流的中位数」是堆最漂亮的应用：用两个堆把数据切成两半，两个堆顶恰好夹住中位数。插入 O(log n)，取中位数 O(1)。 对顶堆：左半用大顶堆，右半用小顶堆，两个堆顶就在中位数两侧 lo： 大顶堆 （存较小的一半） [1, 3, 5, 7, 9] 的堆顶 = 9 堆顶是左半的 最大值 = 整体的下中位数候选 hi： 小顶堆 （存较大的一半） [11, 13, 15, 17] 的堆顶 = 11 堆顶是右半的 最小值 = 整体的上中位数候选 中位数 在这里 两条不变式（必须同时维持） ① lo 的每个元素 ≤ hi 的每个元素 ② |lo.size() − hi.size()| ≤ 1 取中位数 O(1) size 为奇：返回较大那个堆的堆顶 size 为偶：两个堆顶的平均值 两个堆背靠背，堆顶恰好夹住中位数。插入 O(log n)、取中位数 O(1)。奇数返回较大堆的顶，偶数取两顶平均；约定 lo 可以多一个。 class MedianFinder { std::priority_queue< int > lo_; // 大顶堆：较小的一半 std::priority_queue< int , std::vector< int >, std::greater< int >> hi_; // 小顶堆：较大的一半 public : void add ( int x) { // 先无脑塞进 lo_，再把 lo_ 的最大值移给 hi_ → 保证不变式 ① lo_. push (x); hi_. push (lo_. top ()); lo_. pop (); // 再平衡大小 → 保证不变式 ②（约定 lo_ 可以多一个） if (lo_. size () < hi_. size ()) { lo_. push (hi_. top ()); hi_. pop (); } } double median () const { if (lo_. size () > hi_. size ()) return lo_. top (); return (lo_. top () + hi_. top ()) / 2.0 ; } }; add 不判断 x 该进哪边：先塞 lo、把 lo 顶挪到 hi、再平衡大小。空堆边界被流程吃掉。任意分位数可改两个堆的大小比；生产流式分位用 t-digest / DDSketch（可合并），对顶堆 O(n) 且不能合并。 「能不能合并」是流式统计结构的分水岭 ——决定它能不能用在多实例汇总。"},{"a":"s5-2","h":"5.2 滑动窗口中位数","x":"5.2 滑动窗口中位数 窗口里元素会离开，堆不能删任意元素。常用：两个 multiset （对顶堆思路但能删）、或对顶堆 + 延迟删除。删一个必须 erase(find(x)) ， erase(x) 会删光所有重复值。 方案 复杂度 做法 multiset + 中间迭代器 O(n log k) 维护一个指向中位数的迭代器，插删后按需前后移一步 对顶堆 + 延迟删除 O(n log k) 用哈希表记「待删元素」，堆顶若在待删表里就先弹掉 两个 multiset O(n log k) 对顶堆的思路，但换成能删任意元素的容器 树状数组 + 值域二分 O(n log 值域) 值域小时最快；需要离散化 维护「中间迭代器」插删后要按落点左右移，琐碎易错——两个 multiset 更稳，逻辑和对顶堆几乎一样。树状数组 + 值域二分在值域小或可离散化时更快。窗口中位数的本质难点不是「求中位数」，而是「堆不能删任意元素」——所以这条题和 §6 是同一块短板。"},{"a":"s5-3","h":"5.3 近似 heavy hitters","x":"5.3 近似 heavy hitters 「频次最高的 K 个」在存不下每个 key 的计数时，堆也不够——你连次数都没有。 方案 空间 保证 哈希表计数 + size-k 堆 O(不同元素数) 精确 Count-Min Sketch + 堆 O(1/ε · log(1/δ)) 频次 只会高估 ，误差有界 Space-Saving / Misra-Gries O(k/ε) 保证找出所有频次 > εn 的元素 采样 + 精确统计 O(样本量) 概率保证 Count-Min Sketch ：d 个哈希 + d×w 计数矩阵，插入各 +1，查询取 min（碰撞只高估）。再配 size-k 堆存候选。这是「精确但无界 → 近似但有界」：用可控误差换亚线性空间，和 HyperLogLog / t-digest / 布隆同一族。动手先问「误差多少可接受」——没这个数字就没法选。展开见 Redis 与缓存 。"},{"a":"s6","h":"可删除堆的三条路","x":"06 可删除堆的三条路 堆最大的短板，以及三种补法 堆不知道元素在哪，删任意 / decrease-key 要 O(n) 找。Dijkstra、天际线、窗口中位数、可取消定时器都撞这面墙—— 最短路 里 Dijkstra 的延迟删除是同一招。"},{"a":"s6-1","h":"6.1 延迟删除","x":"6.1 延迟删除 三条补法：登记待删 · 真映射下标 · 换有序集 ① 延迟删除 erase 只 ++pending top 时 prune 失效顶 摊销仍 O(log n) size 要自己维护 膨胀则 2× 时重建 重复值用 (值, id) ② 索引堆 id → 数组下标 每次落位都更新映射 真 O(log n) decrease-key erase 后 up+down 都试 漏一处映射只给错结果 常数大，代码多 ③ multiset 标准库，代码最少 两端 O(1)，能删能遍历 红黑树常数大 erase(x) 会删光重复 能用就用，除非 profile 打到它 只删失效元素（定时器取消、窗口过期）→ 延迟删除。真要 decrease-key 且热路径 → 索引堆。其余默认 multiset。 延迟删除摊销仍是 O(log n) ：每元素最多 push 一次、被 prune 一次，内层总次数 ≤ 总插入（ 势能法 ）。三个坑： size() 必须自己维护（ pq.size() 含失效，判空会全错）；删多查少会胀到 O(总插入)， pq.size() > 2 * size_ 时整体重建；按值登记只在值唯一时安全，否则用 (值, id) 。"},{"a":"s6-2","h":"6.2 索引堆","x":"6.2 索引堆 索引堆额外维护「id → 堆下标」，从而真正 O(log n) 任意删除和 decrease-key。sift 里每一次赋值都要写回映射——漏一处不会立刻崩，只给错结果；末尾补位后 sift_up 和 sift_down 都试一遍（一大一小未知）。常数明显变大，除非热路径真要 decrease-key，否则优先延迟删除。"},{"a":"s6-3","h":"6.3 换 multiset","x":"6.3 换 multiset 三条路的常数差在「要不要哈希映射、要不要红黑树节点」。延迟删除数组连续、代码少，但堆可能膨胀；索引堆每次移动多一次哈希写； multiset 每元素约 40B 指针节点。 延迟删除堆 索引堆 multiset 删任意元素 O(1) 登记 + 摊销 O(log n) O(log n) decrease-key 不支持 （只能删+插） O(log n) 删+插 O(log n) 同时取最大和最小 需两个堆 需两个 O(1) （ begin / rbegin ） 常数因子 小 （数组连续） 中 大 （红黑树指针追逐 + 每节点一次分配） 空间 可能膨胀 O(n) + 映射 O(n)，每元素约 40 B 代码量 少 多 最少（标准库） 决策：能用 multiset 就用。decrease-key 且热（稠密图 Dijkstra）→ 索引堆。只删失效 → 延迟删除。"},{"a":"s7","h":"堆的变体","x":"07 堆的变体 理论最优与实测最优往往不是同一个"},{"a":"s7-1","h":"7.1 d-ary 堆","x":"7.1 d-ary 堆 把二叉堆的「每节点 2 个孩子」改成 d 个：树高 log_d n 。sift-up 变快（爬得少），sift-down 每层要比 d 个孩子。d=4 时高度减半，而 4 个孩子在数组里连续，一次 64B cache line（16 个 int）几乎拿齐，「每层多比 3 次」几乎不额外内存访问。decrease-key 只用上浮，所以 Dijkstra 松弛密集时 d-ary 最赚。 d=4 通常实测最优：高度减半，4 个孩子连续，几乎不额外 cache miss 二叉堆 高 log₂ n 每层比 2 个孩子 孩子不一定同 cache line 4-ary 高 log₄ n（减半） 4i+1…4i+4 连续 64B line 一次拿齐 decrease-key 纯赚 适用 Dijkstra 松弛密集 调度里大量提优 d 再大，每层比太多 d=4 是经验甜点 Fibonacci 堆让 Dijkstra 变成 O(E+V log V)，但链表森林 + consolidate 实测很差。实践用 4-ary 或配对堆（Boost.Heap）。"},{"a":"s7-2","h":"7.2 斐波那契堆与配对堆","x":"7.2 斐波那契堆与配对堆 堆 insert find-min delete-min decrease-key 实测 二叉堆 O(log n) O(1) O(log n) O(log n) 很好 d-ary 堆（d=4） O(log_d n) O(1) O(d log_d n) O(log_d n) 最好 斐波那契堆 O(1) O(1) O(log n) 摊销 O(1) 摊销 很差 配对堆 O(1) O(1) O(log n) 摊销 o(log n) 摊销 好 斐波那契堆是理论最优、实测很差 ：它让 Dijkstra 从 O((V+E) log V) 降到 O(E + V log V) ，稠密图上渐进成立。但每节点 6 个指针字段，摊销 O(1) decrease-key 的账记在 delete-min 的 consolidate 上，cache 极差。实践用 4-ary 或配对堆（Boost.Heap 有，最接近这个理论优势而常数小得多）。展开见 复杂度与分析 。"},{"a":"s8","h":"K 路归并与败者树","x":"08 K 路归并与败者树 外部排序里为什么不用普通堆 K 路归并：size-K 最小堆存各路队首，每条记录 O(log K)，总 O(N log K)。外部排序更常用败者树——同样渐进，比较次数约一半，且路径固定、访问可预测。I/O 已被缓冲成接近顺序读时，CPU 侧的比较常数就是瓶颈。完整流程见 排序全景 。 普通堆每次 pop+push ≈ 2 log K 次比较；败者树沿固定叶子路径只需 log K 二叉堆 K 路归并 末尾补到根，再一路下沉 每层：挑孩子 + 判就位 约 2 log K 次比较 访问模式不固定 败者树 / tournament 内部节点存败者，根上是胜者 从该叶子往上走固定路径 只需 log K 次比较 I/O 顺序读时，CPU 常数就是瓶颈 工程上更致命的是：不要两两归并（每遍全量 I/O）；K 太大则每路缓冲变小、顺序读退化成随机读——K 必须实测。 不要写成两两归并：需要 log₂K 遍， 每遍完整读写全量数据 ，1 TB 下 I/O 差约 7 倍。K 也不是越大越好——内存要在 K+1 个缓冲区之间分，K 太大则每路缓冲变小，磁盘从顺序读退化成随机读。机械盘常取几十到几百，NVMe 可更大， 必须实测 。这个模式还出现在 LSM compaction、Spark shuffle、数据库落盘归并。"},{"a":"s9","h":"定时器：堆 vs 时间轮","x":"09 定时器：堆 vs 时间轮 最经典的「堆够不够用」判断题"},{"a":"s9-1","h":"9.1 三种方案对照","x":"9.1 三种方案对照 定时器是「堆够不够用」的经典判断题。epoll 内部和 nginx 用红黑树：取消是一等公民。内核 / Netty / Kafka 用时间轮：添加取消都 O(1)，用精度换吞吐。libuv 用堆：实现简单，取消不频繁时够用。 最小堆 红黑树 时间轮（分层） 添加定时器 O(log n) O(log n) O(1) 取消定时器 O(n) 找 （或延迟删除 / 索引堆） O(log n) O(1) （链表摘除） 取最近到期 O(1) O(1) （最左节点） O(1) 推进时钟 / 触发 O(log n) / 个 O(log n) / 个 O(1) 摊销 / 个 精度 任意 任意 受槽粒度限制 内存 紧凑 每节点指针开销 槽数组 + 链表 实现复杂度 最简单 用标准库即可 最复杂 （分层、级联） 典型使用者 libuv、多数应用层 epoll 内部、nginx Linux 内核、Netty、Kafka 分层时间轮：像钟表的时分秒针，高层轮到期时把任务「级联」到低层轮 秒轮 (60 槽, 粒度 1s) … 每槽挂一条任务链表 ↑ 指针 分轮 (60 槽, 粒度 60s) 存「1 分钟后才到期」的任务 级联 时轮 (24 槽, 粒度 1h) 存「1 小时后才到期」的任务 添加 O(1)（算出该进哪个轮的哪个槽）· 取消 O(1)（双向链表摘除）· 推进时只处理当前槽的链表，而不是全局排序 时间轮用「按到期时间分桶」代替「全局排序」，把 O(log n) 降到 O(1)，代价是精度受槽粒度限制。连接超时、心跳、重试完全够用；微秒级场景不要用它。 时间轮用分桶代替排序 ：任务丢进「该到期的槽」，和哈希算地址同理。分层用 60+60+24=144 槽覆盖 24h×1s（单层要 86400 槽），代价是高层到期要级联——和数字进位一样，用几个小位数表示大范围。精度受槽粒度限制（槽 100ms 就最多晚 100ms）。 几千以内、精度高、少取消 → 最小堆。每连接一个超时且几乎都会取消 → 时间轮或红黑树：堆的 O(n) 查找是致命的，延迟删除会胀堆。海量 + 精度可放宽 → 分层时间轮（Netty / Kafka / 内核）。微秒级场景（交易、音视频）用堆 + timerfd 。优先 timerfd + epoll 或 libuv，特殊需求再自写。设计题见 C++ / Linux 系统设计 。"},{"a":"s9-2","h":"9.2 选型（交互）","x":"9.2 选型（交互） 五个场景的堆选型 点击场景，看该用哪种结构、为什么，以及不选其他方案的理由。 Top-K 流式中位数 Dijkstra 定时器 K 路归并"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 堆是什么结构？它承诺了什么、 没有 承诺什么？ 堆是一棵 完全二叉树 ，且每个节点的值不小于（大顶堆）它所有子节点的值。因为是完全二叉树，可以直接用数组表示，无需指针： parent(i) = (i − 1) / 2 left(i) = 2i + 1 right(i) = 2i + 2 ← 纯算术，零额外内存、cache 友好 承诺 根是最值（O(1) 取），插入删除 O(log n)， 建堆 Θ(n) ，空间 O(1) 额外。 没有承诺（这些才是考点） · 兄弟之间无任何顺序 （左孩子可能比右孩子大也可能小） · 不知道第二大是谁 （只知道「一定是根的某个孩子」） · 中序遍历不是有序序列 （和二叉搜索树完全不同） · 查/删任意元素是 O(n) （不知道元素在哪） 这些「不承诺」正是它快的原因 堆 刻意放弃全序 ，只维护一条约束，所以插删只需沿一条路径调整。 对比：排序数组给全序但插入 O(n)；平衡树给全序且插删 O(log n)，但常数大（指针追逐 + 每节点一次分配）；哈希表给 O(1) 查找但完全无序。 堆在「取最值」这一个维度上做到了最优的时空权衡。 基础 把 n 个元素建成堆，是 O(n) 还是 O(n log n)？ 取决于怎么建 ——这是本题的全部考点。 逐个 push：Θ(n log n) for (T x : v) heap. push (x); // Σ log i ≈ log(n!) ≈ n log n 自底向上 sift_down：Θ(n) for ( size_t i = n/ 2 ; i-- > 0 ; ) sift_down (i); // 从最后一个非叶节点往前 证明 树高 h = ⌊log₂n⌋，深度 d 的节点至多 2^d 个， 每个最多下沉 (h − d) 层： T(n) ≤ Σ(d=0..h) 2^d·(h−d) = 2^h · Σ(k=0..h) k/2^k （换元 k = h−d） ≤ 2^h · 2 （Σ k/2^k 收敛到 2） ≤ 2n ∎ 直觉（比公式更该记住） 一半的节点在最底层，而它们几乎不用动。 · sift_up （逐个 push）处理的是 最深 的节点 → 一半元素都要爬满 log n 层 → 贵 · sift_down （自底向上）处理的节点越靠底层，能下沉的层数 越少 → 数量最多的那部分几乎免费 优化要针对「数量最多的那部分」 ——这是可迁移的思路。 常见追问 「建堆 O(n)，那堆排序也是 O(n) 吗？」 不是。 堆排序第二阶段要做 n 次「取根 + sift_down」，每次 O(log n)，那部分省不掉，所以堆排序仍是 Θ(n log n)。 实践对应 std::make_heap 和 priority_queue 的 区间构造函数 都是 Θ(n)；循环 push 是 Θ(n log n)。实测能差 3–5 倍。 基础 std::priority_queue 默认是大顶堆还是小顶堆？怎么改？ 默认大顶堆 （和 std::sort 默认升序的直觉相反）。改成小顶堆要传 std::greater ： std::priority_queue< int > maxHeap; // 大顶堆 std::priority_queue< int , std::vector< int >, std::greater< int >> minHeap; // 小顶堆 为什么方向是反的 Compare 的语义是「 a 的优先级低于 b 」—— cmp(a,b)==true 时 a 排在 b 后面 出队。传 less （ a<b 时 a 优先级低）意味着「小的后出」= 大顶堆。 记忆方法 「 top() 返回的是在这个比较器下『最大』的元素 」。传 greater 时，按 greater 定义的「最大」就是数值最小的 → 小顶堆。 自定义类型时也照这个读：想让 deadline 小的先出，就写 return a.deadline > b.deadline; 。 和 std::sort 的 Compare 语义相反 std::sort 的 cmp(a,b) 是「a 应排在 b 前 」； priority_queue 是「a 优先级 低 」。 这是 C++ 里最容易搞混的一对接口 ，写的时候多测一个用例。 顺带：三个不支持的操作 不能 修改元素优先级（ top() 是 const 引用）、 不能 删任意元素、 不能 遍历（无 begin() ）、 不能 reserve 。 需要这些就直接用 std::vector + make_heap/push_heap/pop_heap ——多写两行，换完全的控制权。 基础 从 10 亿个数里找最大的 100 个，怎么做？ 关键先问一句： 数据能全装进内存吗？ 10 亿个 int 是 4 GB——如果能装，用 nth_element ；如果是流式或装不下，用 size-k 堆。 方案 A · 能全装内存 → nth_element，O(n) std:: nth_element (v. begin (), v. begin ()+ 100 , v. end (), std::greater<>{}); std:: sort (v. begin (), v. begin ()+ 100 , std::greater<>{}); 最快 ，总 O(n)。前提是数组 可以被打乱 。 方案 B · 流式 / 装不下 → size-100 的最小堆，O(n log k)、空间 O(k) std::priority_queue< int , std::vector< int >, std::greater< int >> pq; for ( int x : stream) { if (pq. size () < 100 ) pq. push (x); else if (x > pq. top ()) { pq. pop (); pq. push (x); } } 「求最大 k 个用最小堆」必须能解释 堆顶是这 100 个候选里 最小 的，也就是 门槛 。新元素只要超过门槛，就把门槛踢出去、自己进来。 用最大堆的话，堆顶是候选里最大的—— 这个信息毫无用处 ，你无法知道该踢谁。 推论：求最小 k 个用最大堆。规则是「堆顶应该是最容易被淘汰的那个」。 别用的方案 全排序 O(n log n) 白排了 999 999 900 个不需要的数。n=10⁹ 时这是 20 倍以上的差距—— 而这一步经常被跳过 。 如果是分布式 各分片先出局部 Top-100，再归并这些 Top-100。正确性：全局 Top-K 的每个元素必然是它所在分片的 Top-K 之一。网络传输从 O(n) 降到 O(M×k)。 但这只对「可比较的单值」成立 ——若是「按总点击数取 Top-100 商品」这类需要跨分片 聚合 的，必须先 shuffle（见高级题）。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 怎么在数据流中实时求中位数？ 对顶堆 ：用两个堆把数据切成两半，两个堆顶恰好夹住中位数。 结构 lo = 大顶堆 ，存较小的一半（堆顶是左半最大值） hi = 小顶堆 ，存较大的一半（堆顶是右半最小值） 两条不变式（必须同时维持） ① lo 的每个元素 ≤ hi 的每个元素 ② |lo.size() − hi.size()| ≤ 1 void add ( int x) { lo_. push (x); // 先无脑塞左边 hi_. push (lo_. top ()); // 把左边最大的挪到右边 → 保证 ① lo_. pop (); if (lo_. size () < hi_. size ()) { lo_. push (hi_. top ()); hi_. pop (); } // 保证 ② } double median () { if (lo_. size () > hi_. size ()) return lo_. top (); return (lo_. top () + hi_. top ()) / 2.0 ; } 插入 O(log n)，取中位数 O(1) 。 这个 add 的写法值得学 它 不判断 x 该进哪个堆 ，而是「先塞左边 → 把左边最大的挪到右边 → 平衡大小」三步无条件执行。 这样就不需要 if (x < lo.top()) 的分类讨论，也不用处理「堆为空时 top() 怎么办」。 用无条件的规整操作代替分类讨论，消掉了一整类边界 bug ——维护多个相互约束的结构时都可以这么想。 滑动窗口中位数难一档 因为元素会 离开窗口 ，而堆不支持删任意元素。要用：两个 multiset （对顶堆思路但能删）、或对顶堆 + 延迟删除 。 注意 multiset::erase(x) 会删 所有 等于 x 的元素，删一个必须 erase(find(x)) 。 生产上通常不用它 流式分位数在生产里用 t-digest 或 DDSketch ：空间 O(log 值域)、 支持合并 （多实例的 sketch 可汇总）、有相对误差保证。对顶堆是 O(n) 空间且 无法合并 。 「能不能合并」是流式统计结构的关键分水岭 ——决定了它能否用在分布式环境。 中级 堆不支持删除任意元素，有哪几种补救办法？各自的取舍？ 三条路，按「性价比」排序： ① 延迟删除（最常用） 不真删，用一个哈希表记「待删元素」；取用时先把堆顶的失效元素弹掉。 void erase ( int x) { ++pending_[x]; --size_; } // O(1)，只登记 int top () { prune (); return pq_. top (); } 摊销仍 O(log n) ：每元素最多 push 一次、被 prune 弹出一次，prune 的总次数 ≤ 总 push 次数（势能法）。 延迟删除的三个坑 ① size() 必须自己维护—— pq_.size() 含失效元素，用它判空会全错； ② 堆可能 膨胀到 O(总插入量) （删多查少时），解法是 pq_.size() > 2*size_ 时整体重建； ③ 按「值」登记只在值唯一时安全，有重复值要用 (值, 唯一 id) 。 ② 索引堆（需要 decrease-key 时） 额外维护「元素 id → 堆数组下标」的映射，支持真正 O(log n) 的任意删除和 decrease-key。 两个实现细节最容易错 ：每次元素落位都要更新映射（漏一处就全乱，且不会立刻崩）； erase 后要 同时尝试 sift_up 和 sift_down （末尾补上来的元素可能更大也可能更小）。 ③ 换 multiset（能用就用它） 标准库、代码最少、功能最全（还能 O(1) 同时取最大最小）。代价是常数大（红黑树指针追逐 + 每节点一次分配）。 决策 能用 multiset 就用 ，除非 profile 显示它的常数是瓶颈。 需要 decrease-key 且性能敏感（稠密图 Dijkstra、大规模调度）→ 索引堆。 只需要「删除已失效元素」（定时器取消、天际线、窗口过期）→ 延迟删除 ，性价比最高。 中级 斐波那契堆的 decrease-key 是 O(1) 摊销，为什么实践中几乎不用？ 因为它是 理论最优但实测很差 的典型——渐进复杂度赢了，常数因子输得更多。 理论优势是真的 它让 Dijkstra 从 O((V+E) log V) 降到 O(E + V log V) 。稠密图（E ≈ V²）上这是实打实的渐进改进。 但三个实践问题 ① 结构是一堆链表连成的森林 ，每节点有 parent/child/left/right/degree/mark 六个字段 → 内存开销大、 指针追逐严重 ； ② 「摊销 O(1)」的代价藏在 delete-min 里——那次要做大量树合并（consolidate）， cache 行为极差 ； ③ 代码复杂，容易写错。 实践该用什么 d-ary 堆（d=4） ：树高 log_4 n （比二叉堆减半），而 4 个孩子在数组里 连续 ——一次 cache line 就能全拿到，所以「每层多比 3 次」几乎不额外花钱。 对 decrease-key 密集的场景收益最大 （decrease-key 只用 sift_up，纯赚）。 配对堆 ：实测里最接近斐波那契堆的理论优势而常数小得多，Boost.Heap 有现成实现。 这是「渐进 vs 实测」的最极端例子 它和 排序 里「堆排序理论完美却实测最慢」、 复杂度 里「list 的 O(1) 插入输给 vector 的 O(n)」是同一类现象： 渐进分析基于 RAM 模型（每次内存访问代价相同），而真实机器上 L1 与主存差 100 倍。 正确的结论不是「渐进分析没用」，而是 渐进分析决定 n 大时的量级（不可违抗），常数因子决定同量级内的胜负（可优化 10 倍） 。 中级 K 路归并用堆是 O(N log K)，为什么外部排序更常用「败者树」？ 两者渐进复杂度相同，差别在 比较次数的常数 ——而外部排序里 I/O 已被缓冲优化到接近顺序读， CPU 侧的比较开销就成了瓶颈 。 普通堆的每次「出一个、进一个」 把末尾补到根，然后 一路下沉 。每层要先「挑较大的孩子」（1 次比较）再「判是否就位」（1 次比较）→ 约 2 log K 次比较。 败者树（loser tree / tournament tree） 完全二叉树， 内部节点存「败者」 （比较中输的一方），根之上单独存全局胜者。 取出胜者后，从 它原来所在的叶子 往上走一条 固定路径 ，逐个和路上的败者比较—— 只需 log K 次比较 。 比较次数约是普通堆的 一半 ，而且路径固定、访问模式可预测。 又是「同样渐进、常数决定选型」 和 introsort 的 cutoff、d-ary 堆的 d=4、基数排序打败 std::sort 是同一类判断： 渐进分析选出候选，常数因子决定最终选择。 但 K 路归并真正的工程要点在别处 ① 不要写成两两归并 ——那需要 log₂K 遍， 每遍都完整读写一次全量数据 ，I/O 是 K 路的 log₂K 倍。1 TB 数据下是 7 倍差距。 ② K 不是越大越好 ——内存要在 K+1 个缓冲区之间分，K 太大则每路缓冲太小，磁盘从 顺序读退化成随机读 。机械盘上 K 常取几十到几百，NVMe 可更大， 必须实测 。 算法选型（堆 vs 败者树）的影响远小于这两条。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 设计一个支持百万级定时器的系统，选堆还是时间轮？ 先问三个问题，答案完全由它们决定： 定时器数量？精度要求？取消频率？ 最小堆 红黑树 分层时间轮 添加 O(log n) O(log n) O(1) 取消 O(n) 找 O(log n) O(1) 链表摘除 触发 O(log n)/个 O(log n)/个 O(1) 摊销 精度 任意 任意 受槽粒度限制 使用者 libuv epoll 内部、nginx 内核、Netty、Kafka 百万级定时器场景下堆的致命问题是「取消」 典型场景是「每个连接一个超时定时器，请求正常返回就取消」—— 绝大多数定时器都会被取消而不是触发 。堆的 O(n) 查找完全不可接受。 用延迟删除能缓解，但堆会膨胀到 O(总添加量)；用索引堆可以，但不如时间轮简单。 所以选分层时间轮 本质是「用分桶代替排序」 ——不维护全局顺序，只把任务丢进「它该到期的那个槽」，和哈希表用值算地址同一个思想。 分层是为了时间跨度 ：单层要支持 24h × 1s 精度需要 86400 个槽；分层后 60+60+24=144 个槽覆盖同样范围，代价是高层任务到期时要 级联 到低层。 和数字的进位表示同理——用几个小位数表示大范围。 时间轮的代价必须接受 精度受槽粒度限制 （槽粒度 100ms 就意味着定时器最多晚 100ms 触发）。对连接超时、心跳、重试这类需求完全够用；对 需要精确到微秒 的场景（高频交易、音视频同步）不行，那要用堆 + timerfd 。 但真正的答案是「先别自己写」 Linux 上 timerfd + epoll 让内核帮你管（ 内核内部就是时间轮 ）；应用层直接用 libuv / Boost.Asio。 自己实现只在有特殊需求时才值得 ——比如需要百万级定时器且不想每个都占一个 fd，或者需要自定义的批量触发语义。 面试时能说出「先评估现成方案」比直接开始画时间轮更能体现工程判断。 高级 分布式环境下求「Top-100 热门商品（按总点击数）」，各分片先出局部 Top-100 再归并对吗？ 不对。 这是分布式 Top-K 最大的陷阱，也是「局部 Top-K 归并」这个正确结论被误用的典型。 反例 某商品在 每台机器上都排第 101 ，各分片的 Top-100 都不含它。但把 M 台的点击数 加起来 后，它可能是全局第 1。 局部 Top-K 归并会完全漏掉它。 「局部 Top-K 归并」什么时候才对 只有当 元素的最终值能在单个分片内确定 时才对。比如「求最大的 100 个数」——每个数的值就是它本身，不需要跨分片聚合。 正确性论证：全局 Top-K 的每个元素必然是它所在分片的 Top-K 之一（若它连本分片前 k 都进不去，说明本分片就有 k 个比它大的，全局更不可能）。 需要聚合时的正确做法 先按 key 重分区（shuffle） ，让同一商品的所有数据聚到同一台机器 → 聚合出该商品的全局总数 → 再取 Top-K。 这就是 MapReduce/Spark 里 reduceByKey 必须发生在 top 之前的原因 ，不是随便写的顺序。 不想 shuffle 全量数据时的两条近似路 ① 各分片报 Top-(k×c) （c 为放大系数）并附带「本分片第 k×c 名的值」作为误差界——归并后能给出「结果最多偏差多少」的保证。 ② 用 Count-Min Sketch ：d 个哈希函数 + d×w 计数矩阵，插入时对应格子 +1，查询取 d 个格子的 最小值 （碰撞只会高估，取 min 最接近真值）。 sketch 可以合并 ，所以各分片的 sketch 汇总后再配一个 size-k 堆即可。 背后的一般判据：这个指标可加吗 sum / count / min / max 可加 → 能预聚合、能局部归并。 avg 要拆 → 存 sum 和 count 分别聚合，最后再除。 distinct count 和分位数不可加 → 必须换成可合并的近似结构（ HyperLogLog 、t-digest）。 「这个指标可加吗」决定了整个分布式方案的形态 ——这个问题应该在设计的第一步就问。 高级 一个用堆做优先级调度的服务，出现了「低优先级任务永远得不到执行」的问题，怎么解决？ 这是 饥饿（starvation） ，堆的纯优先级语义的必然结果：只要高优先级任务持续到来，低优先级任务的位置永远排在后面。 这不是 bug，是需求没被表达清楚。 先确认这真的是问题 某些场景饥饿是 正确 的（比如「紧急告警必须永远优先于日报生成」）。 先问业务：低优先级任务有 SLA 吗？ 没有 SLA 就不是问题；有 SLA 才需要下面的方案。 ① 老化 / 动态提优（aging，最常用） 把优先级从「静态值」改成「 静态优先级 + 等待时长 × 老化系数 」。等得越久，有效优先级越高，最终一定能超过新来的高优先级任务。 实现要点 ：不要真去修改堆里所有元素的 key（那是 O(n)）。而是把入队时间放进比较器， effective = base_priority + (now - enqueue_time) * rate —— 但这会让比较器随时间变化，破坏堆的不变式 。 正确做法 ：改成比较 deadline = enqueue_time - base_priority / rate 这类 不随时间变化 的量，让堆序稳定。这是个容易写错的地方。 ② 多级队列 + 配额（更可控） 每个优先级一个独立队列，调度器按 加权轮询 取（如高:中:低 = 7:2:1）。 好处是 配额显式、可观测、可调 ——你能直接回答「低优先级至少能拿到 10% 的处理能力」，而 aging 方案很难给出这种保证。这也是 Linux CFS 之前的 O(1) 调度器和多数 QoS 系统的做法。 ③ 预留通道 固定留 N 个 worker 只处理低优先级任务，与高优先级完全隔离。最简单、最可预测，代价是资源利用率下降。 「隔离优于共享 + 复杂调度」——这在故障隔离上也成立。 别忽略：饥饿常常是容量问题的症状 如果高优先级任务多到能持续压满所有 worker，那真正的问题是 容量不足 或 上游没限流 。这时任何调度算法都只是在决定「谁先饿死」。 先看队列长度和积压趋势 ：如果队列在持续增长，调度算法救不了，要么扩容要么限流（见 限流·熔断·降级 ）。 选型判据 需要可证明的公平性保证 （对外承诺 SLA）→ 多级队列 + 配额，因为它的保证是显式且可计算的。 只要「最终会被执行」 → aging，实现更简单。 低优先级任务很少但绝不能丢 → 预留通道。 高级 一个 priority_queue 在长时间运行后内存持续增长，可能是什么原因？ 「持续增长」而不是「一次性变大」，指向三类原因，按可能性排序： ① 延迟删除导致的堆膨胀（最常见） 如果代码用了延迟删除（不真删，只登记待删），而 删除远多于 top() 调用 ——失效元素永远没机会被 prune 掉，堆会膨胀到 O(总插入量) 。 典型场景：大量定时器被取消但没人去取堆顶；或者只在特定条件下才 top() 。 修法 ：当 pq.size() > 2 * logical_size 时 整体重建 （把有效元素拷出来 make_heap ）。这个阈值让重建的摊销代价保持 O(1)。 ② 生产快于消费（真正的积压） 入队速率持续大于出队速率。 这是容量问题不是内存问题 ——加内存只是延后崩溃时间。 诊断 ：打点记录队列长度随时间的变化。如果是单调增长的趋势线而不是波动，就是这一类。 修法 ： 有界队列 + 明确的溢出策略 （拒绝新任务 / 丢弃最低优先级 / 阻塞生产者）。无界队列是分布式系统里最常见的设计错误之一——它把「立刻失败」变成了「几小时后 OOM」。 ③ 元素本身持有资源 堆里存的不是简单值，而是持有 shared_ptr 、连接、缓冲区的对象。 堆没膨胀，但每个元素很大 ，或者形成了循环引用导致资源不释放。 诊断 ：heap profiler 看内存去向——如果 vector<Item> 本身不大但总内存涨，就是元素引用的对象。 排查顺序 ① 打点 pq.size() 和逻辑 size，看两者是否分叉（分叉 → 延迟删除膨胀）； ② 看队列长度趋势（单调增 → 积压）； ③ heap profiler 看内存是在 vector 本身还是在元素引用的对象上。 三个信号能直接区分三种原因。 设计层面的教训 任何队列都应该有界。 无界队列把背压（backpressure）问题隐藏起来，直到 OOM 才暴露——而那时故障范围已经从「部分请求失败」扩大到「整个进程死亡」。 正确做法是 让容量上限成为一个显式的、可监控的、有明确溢出行为的配置项 。这和 Hash-DoS 那一类「资源维度没有上界」的问题是同一个原则（见 哈希表内核 §7）。 ⬅️ 哈希表内核 🔀 排序全景 📊 单调栈与前缀和 🏗️ C++/Linux 系统设计 📚 目录 🏠 主页 堆与 Top-K — 建堆 Θ(n) 证明 · priority_queue 的坑 · Top-K 四解法选型 · 对顶堆 · 可删除堆 · 定时器堆与时间轮 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/monotonic_and_prefix.html","t":"单调栈与前缀和","c":"算法与数据结构","s":[{"a":"","h":"","x":"单调栈与前缀和 · 单调栈不变式 / 柱状图最大矩形 / 单调队列 / 前缀和加哈希表 / 二维容斥 / 差分与扫描线 Kavin Tech Notes ← 主页 目录 35 min ◐ 单调栈与前缀和 本质：只留有用的候选 与其他章节的关系 单调栈 不变式与模板 下一个更大元素 接雨水 柱状图最大矩形 推广到二维 单调队列 滑动窗口最大值 与优先队列的分工 DP 的决策单调优化 前缀和 一维与 STL 对应 前缀和 + 哈希表 二维前缀和与容斥 前缀异或与其他可逆运算 差分数组 区间加、单点查 二维差分 扫描线 四者选型对照 决策表 模板对照（交互） 工程里的前缀和 单调计数器与 rate 直方图与分位数 SQL 窗口函数 陷阱清单 面试题集（12 道） 9.1 基础 9.2 中级 9.3 高级 Algorithms · Monotonic & Prefix · 2026 单调栈与前缀和 这四个工具解决的是同一类问题： 把「每次查询都重新扫一遍」换成「维护一个可增量更新的状态」 。单调栈和单调队列丢掉永远不会成为答案的候选；前缀和与差分把区间操作变成两个端点的加减。它们的共同代价也一样—— 都需要先证明「被丢掉的东西真的没用」或「这个运算真的可逆」 。 核心难度 ★★★★☆ 面试出现率 高 阅读时长 ~35 min 前置 复杂度 · 双指针 单调栈不变式 柱状图最大矩形 单调队列 前缀和 + 哈希表 二维容斥 差分与扫描线"},{"a":"s1","h":"本质：只留有用的候选","x":"01 本质：只留有用的候选 两条不同的省法，同一个动机 这一篇里的四个工具分成两族，动机相同但手法相反： 族 工具 手法 关键前提 丢候选 单调栈（§2） 新元素让某些旧元素 永远不可能 再成为答案 → 直接弹出 「被遮挡」关系可判定 单调队列（§3） 同上，外加「过期」也要从另一端弹 同上 + 窗口按序滑动 预处理 前缀和（§4） 区间查询 → 两个端点相减 运算 可逆 （有逆元） 差分（§5） 区间修改 → 两个端点加减 同上，且修改与查询分阶段 合法性就两句 ：弹出 x 时必须证明——对所有还没处理的位置， x 都不可能是答案。通常理由是新来的 y 在各方面都不比 x 差、且更靠后，于是 x 被 y 完全支配 。前缀和则要求运算有 逆元 才能 S[r]−S[l−1] ：加 / 异或 / 非零乘法可以， max / min / gcd 不行 ——没有逆元就去稀疏表或线段树。这条判据比记模板重要。 单调栈像 排队看演出的视线 ：你站在队尾往前看，只能看到「比前面所有人都高」的人——一旦被更高的挡住，对后面所有人永远不可见，可以从名单里永久删除。栈里剩下的正是「从当前位置往回还能看见的人」，天然递减；每人最多进队一次、出队一次，所以总代价 O(n)。前缀和像 累计里程表 ：第 30 到第 80 公里跑了多远，看两个读数相减即可。前提是只增且可减——记「最高时速」相减毫无意义。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 双指针与滑动窗口 —— 窗口极值必须靠单调队列；窗口对负数失效时改用前缀和 复杂度与分析 —— 「每个元素进出各一次」的 O(n) 论证用的是那里的势能法 堆与 Top-K —— §3.2 讲单调队列与优先队列的分工 并查集与区间结构 —— 一维前缀 / 差分的代码与「改查交错上树」在那边；本篇只留图 + 二维 / 哈希 排序全景 —— 扫描线的第一步永远是按坐标排序 SQL 从基础到高级 —— §7.3 的窗口函数就是前缀和的声明式版本"},{"a":"s2","h":"单调栈","x":"02 单调栈 一个模板，五道经典题 单调栈维护「还没找到答案的候选」。新元素一来，把被它完全支配的旧元素全部结算并永久移出。下面先看栈里为什么必须单调，再看弹出的那一刻为什么就是写答案的时刻。"},{"a":"s2-1","h":"2.1 不变式与模板","x":"2.1 不变式与模板 栈里只留「从 i 往回还能看见的」——底大顶小，严格递减 2 1 5 6 2 3 栈底 当前 i 3 看不见 5：被 6 挡住 当前栈（存下标） 6 底 3 顶 底→顶递减 = 往回看的天际线 被挡住的已弹出 每人进出各一次 → O(n) 求「下一个更大」用递减栈：栈顶永远是「还没找到右边更大者」里最近的那个。 // 每个位置「右边第一个更大」的下标；没有则为 n。存下标。 std::vector< int > next_greater ( const std::vector< int >& a) { int n = ( int )a. size (); std::vector< int > res (n, n), st; for ( int i = 0 ; i < n; ++i) { while (!st. empty () && a[st. back ()] < a[i]) { res[st. back ()] = i; // 弹出时才写答案 st. pop_back (); } st. push_back (i); } return res; } 三条约定 ：① 存 下标 不存值（求宽度必须有下标）。② 求「更大」递减栈，「更小」递增栈。③ 答案写在 pop 时 ，不是 push 时。O(n)：每下标进出各一次，内层 while 总次数 = 总 pop ≤ n，与 双指针 同一套势能。"},{"a":"s2-2","h":"2.2 下一个更大元素","x":"2.2 下一个更大元素 弹出时机：a[i] 一来，把栈里所有被它支配的结算掉（答案写在 pop 时） 2 1 5 6 2 3 a[i] 0 1 2 3 4 5 i=0 [2] 直接入栈 i=1 [2, 1] 1 < 2，不弹，入栈（栈内递减 ✓） i=2 [5] 5 弹出 1 和 2 → res[1]=2, res[0]=2 i=3 [6] 6 弹出 5 → res[2]=3 i=4 [6, 2] 2 < 6，入栈 i=5 [6, 3] 3 弹出 2 → res[4]=5；栈底 6 无解 → res[3]=n 为什么弹出是安全的 弹出 x 时，a[i] > a[x] 且 i > x。 对任何 j > i，若 a[j] > a[x]， 那 a[i] 更近、也更大 → x 的答案 已经是 i，不可能是 j。 ⟹ x 被 a[i] 完全支配 ，可永久移出 每个下标进出各一次 ⟹ O(n) 最终 res = [2,2,3,6,5,6] 弹出 x 时 a[i] 更大且更近：后面不可能再成为 x 的答案。答案写在 pop，不是 push。 四种变体只需改两处—— 比较符号 和 遍历方向 ： 要求 遍历方向 while 条件 栈内单调性 右边第一个更大 左 → 右 a[st.back()] < a[i] 递减 右边第一个更小 左 → 右 a[st.back()] > a[i] 递增 左边第一个更大 右 → 左 a[st.back()] < a[i] 递减 左边第一个更小 右 → 左 a[st.back()] > a[i] 递增 循环数组 ：下标 i % n 扫 2n 轮，第二轮只弹不压——第一轮已经把「绕一圈能看到的更大元素」写进答案，再压会破坏「每个下标只进一次」。"},{"a":"s2-3","h":"2.3 接雨水","x":"2.3 接雨水 三种解法代表三种角度。双指针版常被追问「凭什么 O(1) 空间」：矮的那一侧水位已经被对侧更高的墙钉死，不必知道对面的确切高度。单调栈版则按层结算横条。 解法 时间 / 空间 角度 预处理左右最大值 O(n) / O(n) 按列 ： min(左max, 右max) − h[i] 对撞双指针 O(n) / O(1) 同上，左右 max 边走边维护 单调栈 O(n) / O(n) 按层 ：弹出时结算一条横向水条 h = [0,1,0,2,1,0,1,3] 左：按列 右：按层（单调栈弹出时） 按列：水位取左右最高的较矮者 按层：弹出 bottom 时 左墙 = 新栈顶，右墙 = i 宽 = i − left − 1 高 = min(h[left], h[i]) − h[bottom] 栈空 → 左边没墙，存不住，break 双指针：lmax < rmax 时左水位已被钉死 按列看水位、按层看横条，水量相同。单调栈版弹出后栈空必须 break，否则访问空栈是 UB。 双指针凭什么 O(1) ： lmax < rmax 时，左指针右侧至少有一根 ≥ rmax 的柱，所以左格水位就是 lmax ——不必知道右侧最大的精确值。单调栈版弹出 bottom 后若栈空，左边没墙，必须 break，否则访问空栈是 UB。"},{"a":"s2-4","h":"2.4 柱状图最大矩形","x":"2.4 柱状图最大矩形 这是单调栈的标志性题目：求柱状图中能勾出的最大矩形。难点不在栈，而在 枚举「哪根柱是矩形的高」 而不是枚举左右端点。对每根柱，宽度由两侧第一个更矮者决定，而这正是单调栈的标准输出。 h = [2, 1, 5, 6, 2, 3]，以每根柱为高，宽度扩展到两侧第一个更矮者 2 1 5 6 2 3 最大矩形 = 5 × 2 = 10 高 5 的柱子：左界=下标1（值1），右界=下标4（值2）→ 宽 = 4−1−1 = 2 枚举方式的选择决定了难度 ❌ 枚举左右端点 → O(n²) 对，但慢 ✅ 枚举「哪根柱子是矩形的高」 对每根柱 i，宽度上限由两侧 第一个 更矮 的柱子决定： width = right[i] − left[i] − 1 而「两侧第一个更矮」正是 单调栈的标准输出 ⟹ O(n) 关键洞察：换枚举对象，问题就变简单 难点不在单调栈，而在「枚举高而不是枚举端点」这个视角转换。 // 首尾高度 0：栈永不空 + 全部弹出结算 int largest_rectangle (std::vector< int > h) { h. insert (h. begin (), 0 ); h. push_back ( 0 ); std::vector< int > st; int best = 0 ; for ( int i = 0 ; i < ( int )h. size (); ++i) { while (!st. empty () && h[st. back ()] > h[i]) { int top = st. back (); st. pop_back (); best = std:: max (best, h[top] * (i - st. back () - 1 )); } st. push_back (i); } return best; } 哨兵 ：左 0 保证栈永不空（ st.back() 合法）；右 0 把剩余柱子全部弹出结算，省掉循环后再清算。链表哑头、区间 [−∞,+∞] 、字符串终止符都是同一手法——循环后还要再处理一遍剩余状态时，先想能不能加哨兵。"},{"a":"s2-5","h":"2.5 推广到二维","x":"2.5 推广到二维 「01 矩阵最大矩形」= 逐行压成柱状图，复用 §2.4，O(m·n)。以第 r 行为地面，往上连续 1 的个数就是柱高；遇 0 清零。这是处理二维问题的通用套路：不去直接想三维，而是对每一层问「如果这一层是地面，往下能挖多深」——二维前缀和、最大子矩阵和（枚举上下界 + 一维 Kadane）、二维差分都是同一降维。 以第 r 行为底：遇 1 则 heights[c]++，遇 0 则清零，再跑一遍柱状图 矩阵 1 0 1 1 1 1 1 0 1 1 1 1 ← 当前底 heights 3 2 3 1 然后 largest_rectangle(heights) —— 二维问题变 m 个一维 降维 + 复用：二维前缀和、最大子矩阵和（枚举上下界 + Kadane）、二维差分都是同一套路。"},{"a":"s3","h":"单调队列","x":"03 单调队列 单调栈 + 一个「过期」出口 单调队列 = 单调栈 + 从另一端弹出「过期」元素。窗口求和能加减是因为加法可逆；求最大值不行——max 没有逆元，移出最大值后次大值丢失。它要求元素 按下标顺序进入、按下标顺序过期 ，这个更强的假设才换来 O(n)。"},{"a":"s3-1","h":"3.1 滑动窗口最大值","x":"3.1 滑动窗口最大值 下面用滑动窗口最大值把两个出口走一遍。队尾弹「被支配」、队头弹「过期」，队首恒为当前窗口最大。 a = [1, 3, −1, −3, 5, 3, 6, 7]，k = 3 窗口滑到含 5 时 1 3 −1 −3 5 3 6 7 当前窗口 [−1, −3, 5] deque（下标，值递减） −1 过期? ← 头：滑出窗口才弹 −3 支配 5 入尾 ← 尾：被新来的支配就弹 两个出口，两种理由 尾部 pop_back：被支配，永久删 头部 pop_front：过期，资格没了 所以必须是双端队列 队首 = 当前窗口最大值 过期用 if 即可（每轮最多一个滑出）。求最大值用 <= 弹相等项——旧的更先过期，留着没用。 // 定长 k 窗口最大值，O(n) 摊销。存下标，值递减。 std::vector< int > max_sliding_window ( const std::vector< int >& a, int k) { std::deque< int > dq; std::vector< int > res; for ( int i = 0 ; i < ( int )a. size (); ++i) { while (!dq. empty () && a[dq. back ()] <= a[i]) dq. pop_back (); dq. push_back (i); if (dq. front () <= i - k) dq. pop_front (); if (i >= k - 1 ) res. push_back (a[dq. front ()]); } return res; } 两个出口，两种理由 ：尾部 pop_back 是被支配（新元素更大且更靠后，旧的永不可能再是最大，永久删）；头部 pop_front 是过期（没被支配，只是滑出窗口）。必须用 deque。不变式：下标递增、值递减，队首 = 窗口最大。求最大值用 <= 弹相等项——旧的更先过期；要统计最大值出现次数才用 < 。头部用 if 即可，每轮最多一个过期。"},{"a":"s3-2","h":"3.2 与优先队列的分工","x":"3.2 与优先队列的分工 单调队列 堆 multiset 窗口最大值 O(n) 摊销 O(n log n) O(n log k) 删任意元素 否 需延迟删除 能 同时取最大最小 要两个队列 要两个堆 能 前提 按序进出 无 无 单调队列用「按序进出」换 O(n)：被支配者一定比支配者先过期，才能永久丢。堆没有这个前提，必须保留全部（延迟删除还要一张表判断堆顶是否过期）。窗口不是定长滑动时改延迟删除堆；同时要最大和最小用两个单调队列；要中位数见 堆与 Top-K 。"},{"a":"s3-3","h":"3.3 DP 的决策单调优化","x":"3.3 DP 的决策单调优化 dp[i] = max{ dp[j] + w(i) : i − k ≤ j < i } 候选窗口随 i 整体右移 → 就是定长窗口最大值 → 单调队列 O(n) 识别：转移对 j 求 max/min，且候选区间随 i 单调移动。典型题是跳跃游戏 VI：每步最多跳 k 格， dp[i] = max(dp[i-k..i-1]) + a[i] ，队首就是窗口内 dp 最大者。权重含 i·j 乘积项（如 (a[i]−a[j])² ）则是 斜率优化 ：谁更优取决于 i，不能永久丢，要维护下凸壳。一句话：候选之间的优劣是否与 i 无关——无关则单调队列，有关则凸包。展开见 动态规划 。"},{"a":"s4","h":"前缀和","x":"04 前缀和 把区间查询变成两个端点相减 前缀和把「每次查询都扫一遍」换成预处理。一维模板和差分数组与 并查集与区间结构 §4 重复，本篇只留图，把那边没有的二维容斥和「前缀和 + 哈希表」写清楚。共同前提仍是：运算可逆，且修改与查询不交错。"},{"a":"s4-1","h":"4.1 一维与 STL 对应","x":"4.1 一维与 STL 对应 一维前缀和 / 差分的代码与「改查交错上树」见 并查集与区间结构 §4 。这里只留图： S[0]=0 长度 n+1， sum(l,r)=S[r+1]−S[l] ，让 l=0 不用特判。输出必须 long long —— partial_sum 的累加类型由 输出容器 决定， vector<int> 就会溢出。 partial_sum 是 inclusive，公式要 exclusive，C++17 用 exclusive_scan(..., 0LL) 。 S[0]=0，S[i]=a[0]+…+a[i−1] ⟹ sum[l..r] = S[r+1] − S[l] a 3 1 4 2 5 1 要 [2,4] = 4+2+5 S 0 3 4 8 10 15 16 S[5]=15 S[2]=4 15 − 4 = 11 不能做的 区间 max / min / gcd 没有逆元，减不掉 改查交错 → 每次重建 O(n) 改用 BIT / 线段树 见并查集与区间结构 哨兵位 S[0]=0 换掉 l=0 的特判。max 减不掉，所以前缀和管不了区间最值。"},{"a":"s4-2","h":"4.2 前缀和 + 哈希表","x":"4.2 前缀和 + 哈希表 含负数时窗口失效（和不单调）。枚举右端，查有多少左端使 S[r]−S[l]=k ，即查 S−k 的次数。三处必错： cnt[0]=1 （空前缀，否则漏掉从 0 起的子数组）； 先查后加 （否则 k=0 匹配自己）；存次数不是存「是否出现」。 a=[1, 2, −1, 1]，k=2 走到 S=3 时查 3−2=1，命中两次 S 0 1 3 2 3 第一次 S=3 再一次 S=3 三处必错点（小数据测不出） ① cnt[0]=1：空前缀，否则漏掉从下标 0 起的子数组 ② 先查 S−k 再 ++cnt[S]：否则 k=0 会匹配自己 ③ 存次数不是存「是否出现」——含负数时同一 S 会反复出现 自问：能否定义一个可累加可相减的量，使「区间满足条件」≡ 两个前缀相等或相差 k？ 变体 key / value 转化 和为 k 的个数 前缀和 → 次数 查 S−k 和为 k 的最长 前缀和 → 最早下标 key 不存在才插入 能被 k 整除 S mod k → 次数 ((S%k)+k)%k 0/1 数量相等最长 (1 数−0 的数) → 最早下标 0 当 −1，化和为 0 真正要学的是 转化 ：把 0 当 −1、数负号奇偶、对 k 取模，本质都是找到一个可累加且可相减的量。自问：能否定义一个前缀量，使「区间满足条件」等价于两个前缀相等或相差 k？"},{"a":"s4-3","h":"4.3 二维前缀和与容斥","x":"4.3 二维前缀和与容斥 二维前缀和 S[i][j] = 左上角 (0,0) 到 (i−1, j−1) 的矩形和 构建（容斥） a S[i][j] = S[i−1][j] ← 上面一块 + S[i][j−1] ← 左边一块 − S[i−1][j−1] ← 重叠部分减掉 + a[i−1][j−1] ← 当前格 「加二减一加一」 查询（容斥） 目标 S[r2+1][c2+1] − S[r1][c2+1] − S[r2+1][c1] + S[r1][c1] 「减二加一」 为什么维度取 (m+1) × (n+1) 且首行首列全 0 和一维取 S[0]=0 同理：让 r1=0 或 c1=0 时不需要任何特判。否则查询公式要写四个分支。 代价是「下标偏移 1」——查询原矩阵的 [r1..r2][c1..c2] 要用 S 的 [r1..r2+1][c1..c2+1]，这是 off-by-one 高发点。 构建是「加二减一加一」，查询是「减二加一」。两个公式都来自同一个容斥原理。 // S 取 (m+1)×(n+1)，首行首列 0。查询原矩阵 [r1..r2][c1..c2] for ( int i = 1 ; i <= m; ++i) for ( int j = 1 ; j <= n; ++j) S[i][j] = S[i- 1 ][j] + S[i][j- 1 ] - S[i- 1 ][j- 1 ] + a[i- 1 ][j- 1 ]; long long query2d ( int r1, int c1, int r2, int c2) { return S[r2+ 1 ][c2+ 1 ] - S[r1][c2+ 1 ] - S[r2+ 1 ][c1] + S[r1][c1]; } k 维容斥 2^k 项：二维好用，三维勉强（查询 8 项），再高换 k-d 树或分块。维度取 (m+1)×(n+1) 且首行首列全 0，与一维 S[0]=0 同理，否则 r1=0 或 c1=0 要写四个分支。代价是下标偏移 1——查原矩阵 [r1..r2][c1..c2] 要用 S 的 [r1..r2+1][c1..c2+1]，这是 off-by-one 高发点。"},{"a":"s4-4","h":"4.4 前缀异或与其他可逆运算","x":"4.4 前缀异或与其他可逆运算 本质要求是 可逆 。异或最优雅：逆运算是自己，无溢出无符号；「区间内每个数出现偶数次 ⟺ 区间异或为 0」。max/min/gcd 没有逆元，换稀疏表 / 线段树，见 并查集与区间结构 。有趣的对称：求和能前缀和不能稀疏表（重叠会加两次）；max 能稀疏表不能前缀和。 运算 逆 前缀查询 加 / 异或 减 / 自身 S[r+1]−S[l] / X[r+1]^X[l] 乘（无 0）/ 模乘 除 / 逆元 含 0 则乘法失效 max / min / gcd 无 稀疏表或线段树"},{"a":"s5","h":"差分数组","x":"05 差分数组 前缀和的对偶：把区间修改变成两点加减 差分和前缀和互为逆运算：a 的差分的前缀和 = a。前缀和擅长区间查、改要重建；差分擅长区间改、查要先还原。两者都要求修改与查询分阶段——交错就上树。"},{"a":"s5-1","h":"5.1 区间加、单点查","x":"5.1 区间加、单点查 一维差分代码见 并查集与区间结构 §4.2 。 d[l]+=v; d[r+1]-=v ，最后前缀还原。长度必须 n+1（ r 可以是 n−1）。「O(1) 改」的前提是 所有修改都在查询之前 ；改查交错则差分套 BIT 或线段树懒标记。 区间 [2,5] += 3：d[2]+=3，d[6]-=3；还原 a[i] = a[i-1]+d[i] 下标 1 2 3 4 5 6 d 改 2 +3 1 0 -1 -3 左端 +v 右端后 −v a 还 2 5 6 6 5 2 两次单点 = 一次区间加 前缀把 +v 带到末尾 −v 在 r+1 截断 m 次加 + 一次还原 = O(m+n)，不是 O(mn) 边加边查 → 差分套 BIT 差分像门口记进出：只在区间两端记账，前缀和还原任意时刻人数。"},{"a":"s5-2","h":"5.2 二维差分","x":"5.2 二维差分 子矩阵加 v：四角 + − − + ，全部打完再做二维前缀和。符号与二维查询相同——互为逆运算。记住其中一个就能推出另一个。矩阵同样要多留一行一列，否则 r2+1 / c2+1 越界，而且小数据下常常不崩。 给 [r1..r2][c1..c2] 加 v：四个角，然后二维前缀还原 +v −v −v +v 目标矩形 d[r1][c1] += v d[r1][c2+1] -= v d[r2+1][c1] -= v d[r2+1][c2+1] += v 右下被减两次，必须加回来 k 维要 2^k 个角。矩阵也要多留一行一列，否则 r2+1 / c2+1 越界。"},{"a":"s5-3","h":"5.3 扫描线","x":"5.3 扫描线 值域稀疏时不开全数组，只记变化点，排序后扫。会议室 = 最大重叠：开始 +1、结束 −1。同刻顺序由开闭语义决定：左闭右开 [s,e) 先减后加（ pair 字典序 -1<+1 自动做到）；闭区间在端点相交则先加后减。写反只在「刚好相接」上错。 [1,3) 与 [3,5) 不冲突：时刻 3 先处理 −1 再 +1 [1, 3) [3, 5) 1 3 5 事件：(1,+1) (3,−1) (3,+1) (5,−1)。pair 字典序让 −1 排在 +1 前。 闭区间 [1,3]∩[3,5] 在点 3 相交 → 同刻先加后减，或结束写成 e+1。 写反只在「刚好相接」上错，随机测很难命中。 扫描线 = 排序 + 可增量状态。计数用整数；要删任意高度用 multiset（天际线）；覆盖长度用线段树。 题型 事件 状态 会议室 (开始,+1) (结束,−1) 当前重叠 max 天际线 (左,−h) (右,+h) 高度 multiset 矩形面积并 竖边入/出 y 覆盖长度（线段树） 二维数点 按一维排序 另一维树状数组 扫描线 = 排序 + 一个可增量维护的状态。难度全在状态用什么结构：只要计数 → 一个整数（会议室）；要最大值且需删任意元素 → multiset 或延迟删除堆（天际线）；要区间覆盖长度 → 线段树（矩形面积并）；要「有多少已插入值小于 x」→ 树状数组（二维数点 / 逆序对）。识别信号：题目涉及区间 / 线段 / 事件，且答案与某时刻的聚合状态有关。"},{"a":"s6","h":"四者选型对照","x":"06 四者选型对照 按「修改 / 查询」的组合选工具"},{"a":"s6-1","h":"6.1 决策表","x":"6.1 决策表 需求 工具 预处理 单次操作 备注 区间和查询， 无修改 前缀和 O(n) O(1) 最简单，首选 区间加， 最后统一查 差分数组 — O(1) 改 还原一次 O(n) 单点改 + 区间和查 树状数组 O(n) O(log n) 代码最短的平衡选择 区间改 + 区间和查 线段树 + 懒标记 O(n) O(log n) 通用但代码长 区间 最值 查询，无修改 稀疏表 O(n log n) O(1) max/min 无逆元，不能用前缀和 定长窗口 最值 单调队列 — O(1) 摊销 要求元素按序进出 「两侧第一个更大/更小」 单调栈 O(n) — 一遍扫出全部答案 和为 k 的子数组（含负数） 前缀和 + 哈希表 — O(1) 均摊 滑动窗口在此失效 先问 改查是否交错 ：不交错（先全改再全查，或根本不改）→ 前缀和 / 差分 / 稀疏表，O(1)；交错 → BIT 或线段树，O(log n)。再问 有没有逆元 ：有（加、异或）→ 前缀和族；没有（max、min、gcd）→ 稀疏表或线段树。定长窗口最值还有第三条路：按序进出 → 单调队列。这两个问题答完，工具基本就定了——直接上线段树常常是过度设计。"},{"a":"s6-2","h":"6.2 模板对照（交互）","x":"6.2 模板对照（交互） 五个工具的模板、不变式与前提 点击工具名，看它的不变式、模板骨架、复杂度论证，以及什么时候不能用。 单调栈 单调队列 前缀和 前缀和+哈希 差分 / 扫描线"},{"a":"s7","h":"工程里的前缀和","x":"07 工程里的前缀和 监控、直方图、SQL 里的同一个东西"},{"a":"s7-1","h":"7.1 单调计数器与 rate","x":"7.1 单调计数器与 rate Prometheus 的 counter 就是前缀和： rate = (c(t2)−c(t1))/(t2−t1) 。存累计值而不是每秒增量： 为什么存累计 说明 抗采样丢失 丢点只影响时间精度，下一个点仍能算出正确总量；存增量则那段永远丢了 抗采样间隔变化 15s 改 30s 时累计语义不变，增量语义会变 客户端无状态 只需原子自增，不用维护上次上报时刻 可任意重采样 同一份数据能算 1 分钟或 1 小时的 rate 进程重启会归零，必须用 rate() / increase() 检测 reset，不要手写相减——发版时会出现巨大负尖刺。见 可观测性 。"},{"a":"s7-2","h":"7.2 直方图与分位数","x":"7.2 直方图与分位数 桶计数的前缀和是 CDF，上面二分估 p99，桶内线性插值。Prometheus histogram 直接存累积（ le=\"…\" ），桶可跨实例相加后再算分位数——累积计数可加，这是它能汇总的原因。代价是精度受桶宽限制：p99 落在很宽的桶里时插值偏差大。要「任意分位数相对误差 ≤ 1%」用 t-digest / DDSketch，用近似换 O(log 值域) 空间。distinct count 同样不可加，要换成 HyperLogLog。"},{"a":"s7-3","h":"7.3 SQL 窗口函数","x":"7.3 SQL 窗口函数 SUM() OVER (ORDER BY … ROWS UNBOUNDED PRECEDING) 是前缀和； ROWS 6 PRECEDING 是定长窗口； LAG 是差分。 ROWS 按行、 RANGE 按值——缺日期时「最近 7 天」必须用 RANGE INTERVAL '6' DAY ，否则前 6 条记录可能跨过一周。详见 SQL 从基础到高级 。"},{"a":"s8","h":"陷阱清单","x":"08 陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 栈里存值不存下标 求宽度 / 距离时无从下手 一律存下标 单调栈方向搞反 答案完全错 求「更大」用递减栈，求「更小」用递增栈 接雨水漏判栈空 访问空栈 back() ，UB 弹出后若栈空则 break 柱状图未加哨兵 要在循环后再写一段清算逻辑，易漏 首尾各加高度 0 的哨兵 单调队列只用一个出口 过期元素没清，结果偏大 尾部弹「被支配」，头部弹「过期」，两者都要 前缀和不留 S[0]=0 l=0 要特判，容易漏 长度取 n+1，首位为 0 partial_sum 输出用 int 大数组求和溢出（UB） 输出容器用 long long ，或用 exclusive_scan 带初值 inclusive / exclusive 混用 结果整体偏移一位 用名字明确的 exclusive_scan 忘了 cnt[0] = 1 漏掉「从下标 0 开始」的子数组 哈希表初始化时放入空前缀 先加后查 k = 0 时多算自己 先查 S − k ，再 ++cnt[S] 负数取模 「能被 k 整除」类题答案错 ((S % k) + k) % k 差分数组少留一位 d[r+1] 越界写，隐蔽 长度取 n+1 扫描线同刻事件顺序 「首尾相接」的用例错 先想清区间是开还是闭；左闭右开 → 先减后加 对 max/min 用前缀和 逻辑上根本不成立 无逆元 → 改稀疏表或线段树 一半坑是少一个哨兵位： S[0] 、 d[n] 、柱状图两端 0、 cnt[0]=1 。凡是用两端差表达区间，都要多留一个边界。写之前先想清数组开多长。溢出 / 越界用 sanitizer 抓，见 Sanitizers 实战 。"},{"a":"s9","h":"面试题集（12 道）","x":"09 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s9-1","h":"9.1 基础（必答）","x":"9.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 什么是单调栈？写出「下一个更大元素」的模板，并说明为什么是 O(n)。 单调栈是一个 栈内元素保持单调 的栈。它的作用是维护「还没找到答案的候选集」——新元素一来，就把被它 完全支配 的旧元素全部结算并永久移出。 std::vector< int > next_greater ( const std::vector< int >& a) { int n = a. size (); std::vector< int > res (n, n), st; // st 存 下标 for ( int i = 0 ; i < n; ++i) { // 不变式：栈内值严格递减 while (!st. empty () && a[st. back ()] < a[i]) { res[st. back ()] = i; // 弹出时才确定答案 st. pop_back (); } st. push_back (i); } return res; } O(n) 的论证 每个下标最多 push 一次、 pop 一次。内层 while 的 总 执行次数等于总 pop 次数 ≤ n。取势能 Φ = 已 pop 的元素数（单调增、上界 n）——和双指针是同一套论证。 弹出为什么安全 弹出 x 时， a[i] > a[x] 且 i > x 。对任何 j > i ， a[i] 比 a[j] 离 x 更近且已经更大 ⟹ x 的答案必然是 i，不可能是 j ⟹ x 被 完全支配 ，可永久移出。 三条约定 ① 栈里存 下标 不存值（下标能反查值，且求宽度必须有下标）； ② 求「更大」用递减栈、求「更小」用递增栈； ③ 答案在 pop 时写入 ，不是 push 时——这是单调栈题的统一节奏。 基础 前缀和怎么做区间和查询？为什么数组长度要取 n+1？ S[0] = 0, S[i] = a[0] + … + a[i−1] sum(l, r) = S[r+1] − S[l] （闭区间 [l, r]） 预处理 O(n)，此后每次查询 O(1) 为什么长度取 n+1 且 S[0]=0 让 l = 0 时 不需要特判 。否则公式要写成 l == 0 ? S[r] : S[r] - S[l-1] ，多一个分支就多一处出错机会。 这和二分用半开区间是同一类设计： 多留一个哨兵位置，换掉一整类边界判断。 溢出 S 必须用 long long 。10⁵ 个 10⁴ 量级的 int 相加就溢出，而有符号溢出是 UB。 用 STL 时更隐蔽： std::partial_sum 的累加类型由 输出容器 的元素类型决定——给 vector<int> 输出就是 int 累加。 STL 对应 std::partial_sum 是 inclusive （含当前项）；区间查询公式要的是 exclusive 。C++17 起用名字明确的 std::exclusive_scan(first, last, out, 0LL) ——顺带还能显式指定累加类型，并支持 std::execution::par 并行。 基础 差分数组解决什么问题？它和前缀和是什么关系？ 差分解决 「多次区间加，最后统一查询」 ：把 O(n) 的区间修改压成 O(1) 的两点加减。 d[l] += v; d[r+1] -= v; ← 区间 [l, r] 全加 v， O(1) 最后对 d 求一次 前缀和 ，得到最终数组 m 次操作总代价 O(m + n)，而不是朴素的 O(m·n) 两者互为逆运算 a 的差分的前缀和 = a 。所以它们是同一枚硬币的两面： · 前缀和： 区间查询 O(1)，但修改要 O(n) 重建 · 差分： 区间修改 O(1)，但查询要先还原 O(n) 生活类比 差分像「只在门口记进出的人数」——要让 10 点到 12 点的每个人都算一次，不用给这两小时每分钟都记一笔，只在 10 点记 +1、12 点零一分记 −1。想知道任意时刻屋里多少人，把从开门到那一刻的进出 累加 （前缀和）即可。 两个坑 ① d 的长度必须是 n+1 —— r 可以等于 n−1 ，此时要写 d[n] 。少留一位就是越界写，而且小数据下常常不崩（写进了容量冗余），非常隐蔽。 ② 「O(1) 修改」的前提是 所有修改都在查询之前 。修改与查询交错时差分不适用，要换树状数组或线段树+懒标记。 基础 为什么滑动窗口求「最大值」不能像求「和」那样加一个减一个？ 因为 移出的可能正是最大值，而你不知道次大值是谁 。求和时「减掉移出的元素」是精确可逆的；求最大值时，移出最大值后你手上没有任何信息能恢复出新的最大值。 本质原因： max 没有逆元 。加法可以减回来，max 不能「max 回去」。这条判据同时解释了为什么区间最值不能用前缀和（见 §4.4）。 正解：单调队列，O(n) 摊销 std::deque< int > dq; // 存下标，对应值递减 for ( int i = 0 ; i < n; ++i) { while (!dq. empty () && a[dq. back ()] <= a[i]) dq. pop_back (); // ① 尾部：被支配 dq. push_back (i); if (dq. front () <= i - k) dq. pop_front (); // ② 头部：过期 if (i >= k - 1 ) res. push_back (a[dq. front ()]); } 两个出口，两个不同理由 尾部弹出（被支配） ：新元素既更大、位置又更靠后 ⟹ 旧元素永远不可能再是最大值 ⟹ 永久删除。 头部弹出（过期） ：下标滑出窗口。它没被支配，只是不再有资格参赛。 正因为有两个出口，容器必须是双端队列而不是栈。 替代方案 multiset （O(n log k)，但能删任意元素、能同时取最大最小）； 延迟删除堆 （O(n log n)，但当窗口不是定长滑动而是任意区间时反而唯一可行）。"},{"a":"s9-2","h":"9.2 中级（区分熟练度）","x":"9.2 中级（区分熟练度） 中级 「和恰好为 k 的子数组个数」怎么做（数组可含负数）？有哪些必须注意的细节？ 滑动窗口在含负数时 失效 （和不随窗口变长单调），正解是 前缀和 + 哈希表 ：枚举右端点 r，查「有多少个左端点 l 使 S[r] − S[l] = k 」，即查 S[r] − k 出现过几次。 int subarray_sum ( const std::vector< int >& a, int k) { std::unordered_map< long long , int > cnt; cnt[ 0 ] = 1 ; // ① 空前缀 long long S = 0 ; int ans = 0 ; for ( int x : a) { S += x; auto it = cnt. find (S - k); if (it != cnt. end ()) ans += it->second; // ② 先查 ++cnt[S]; // 后加 } return ans; } ① cnt[0] = 1 是必需的 它代表「空前缀」，对应「子数组从下标 0 开始」。漏掉它，所有以 0 开头的合法子数组都会被漏算。 ② 必须先查后加 如果先 ++cnt[S] 再查 S − k ，那么 k = 0 时当前前缀会 匹配到自己 ，多算一个长度为 0 的「子数组」。 ③ 存次数而非「是否出现」 含负数时同一个前缀和会反复出现，每次对应一个不同的左端点，必须累加次数。 变体只需换 key/value 的语义 · 求 最长 而非个数 → value 存「最早出现的下标」，且 只在 key 不存在时插入 （保留最早的） · 能被 k 整除 的子数组 → key 存 ((S % k) + k) % k （负数取模要修正） · 0 和 1 数量相等 的最长子数组 → 把 0 当 −1，转化成「和为 0」 · 乘积为正 的最长段 → 数负号个数的奇偶，转化成前缀奇偶性 这些题的共同点是「找到一个可累加且可相减的量」——这才是前缀和题的真正技巧。 中级 「柱状图中最大矩形」怎么用单调栈解？哨兵技巧解决了什么？ 关键不在单调栈，而在 视角转换 ：不枚举矩形的左右端点（O(n²)），而是 枚举「哪根柱子是矩形的高」 。 对每根柱 i：宽度上限由两侧 第一个更矮 的柱子决定 width = right[i] − left[i] − 1 面积 = h[i] × width 而「两侧第一个更矮」正是单调栈的标准输出 ⟹ O(n) int largest_rectangle (std::vector< int > h) { h. insert (h. begin (), 0 ); // 左哨兵 h. push_back ( 0 ); // 右哨兵 std::vector< int > st; // 递增栈 int best = 0 ; for ( int i = 0 ; i < ( int )h. size (); ++i) { while (!st. empty () && h[st. back ()] > h[i]) { int top = st. back (); st. pop_back (); best = std:: max (best, h[top] * (i - st. back () - 1 )); } st. push_back (i); } return best; } 哨兵消掉了两类边界判断 左哨兵 0 ：比任何柱子都矮，永不被弹出 ⟹ 栈永不为空 ⟹ st.back() 总是合法，不用判空。 右哨兵 0 ：会把栈里剩下的所有柱子都弹出 ⟹ 不需要在循环后再写一段「清算剩余栈」的重复代码 。 哨兵是个通用手法 链表题的哑头节点、区间题的 [−∞, +∞] 边界、字符串题末尾补终止符，都是同一个东西。 识别信号：如果你在写「循环后还要再处理一遍剩余状态」，通常加个哨兵就能合并掉。 二维推广 「01 矩阵中全 1 的最大矩形」= 逐行把二维压成柱状图（ heights[c] = g[r][c]=='1' ? heights[c]+1 : 0 ），每行调一次上面的函数，O(m·n)。 「降维 + 复用已有解法」是二维问题的通用套路。 中级 为什么区间 max 不能用前缀和？该用什么？ 因为前缀和的本质要求是 运算可逆（有逆元） ——只有能「减掉不要的部分」，才能用 S[r] − S[l] 还原区间。 max 没有逆元 ：知道 max(a[0..r]) 和 max(a[0..l-1]) ，推不出 max(a[l..r]) 。 运算 有逆元？ 能否前缀和 加法 减法 能 异或 异或自身 能 （最优雅，无溢出无符号问题） 乘法（无 0） 除法 能， 含 0 则失效 模乘（模质数） 乘逆元 能 max / min / gcd 无 不能 替代方案（按需求选） 无修改 + 要 O(1) 查询 → 稀疏表 （sparse table）：O(n log n) 预处理，O(1) 查询。它利用 max 的「可重叠合并」性质——两个有重叠的区间取 max 仍正确，所以用两个长度为 2^k 的区间覆盖任意区间即可。 有修改 → 线段树 ：O(n) 建、O(log n) 查改，通用但代码长。 定长窗口滑动 → 单调队列 ：O(1) 摊销，但要求元素按序进出。 为什么 gcd 能用稀疏表但不能用前缀和 稀疏表只要求运算 可结合 + 可重叠合并 （idempotent），不要求可逆。 gcd(gcd(x,y), gcd(y,z)) = gcd(x,y,z) 成立，所以能用。 而 求和不能用稀疏表 （重叠部分会被加两次），却能用前缀和。 两个结构的适用条件恰好互补 ——这是区间查询问题里一个很有启发的对称性。 中级 扫描线解「会议室 II」，同一时刻的事件该按什么顺序处理？ 把每个会议拆成两个事件： (开始, +1) 和 (结束, −1) ，排序后累加，过程中的最大值就是所需会议室数。 for ( auto & [s, e] : iv) { ev. emplace_back (s, + 1 ); ev. emplace_back (e, - 1 ); } std:: sort (ev. begin (), ev. end ()); // pair 字典序：同刻时 -1 排在 +1 前 int cur = 0 , best = 0 ; for ( auto & [t, d] : ev) { cur += d; best = std:: max (best, cur); } 顺序由区间开闭语义决定 左闭右开 [s, e) （会议时间段的常规语义）： [1,3) 与 [3,5) 不 冲突 ⟹ 同刻 先减后加 。上面的代码靠 pair 字典序（ -1 < +1 ）自动实现。 闭区间 [s, e] （线段覆盖）： [1,3] 与 [3,5] 确实 在点 3 相交 ⟹ 同刻 先加后减 ，要反转排序键，或把结束时刻改成 e + 1 。 为什么这个坑难发现 写反了只在「 刚好首尾相接 」的用例上错，随机测试很难命中。 动手前先在纸上写清「首尾相接算不算重叠」 ——这是扫描线第一大坑。 扫描线 = 排序 + 一个可增量维护的状态 难度全在「状态用什么结构维护」： · 只要计数 → 一个整数（会议室） · 要最大值且需删任意元素 → multiset 或延迟删除堆（ 天际线问题 ） · 要区间覆盖长度 → 线段树（ 矩形面积并 ） · 要「有多少已插入值小于 x」→ 树状数组（ 二维数点 / 逆序对 ） 识别信号 ：题目涉及区间 / 线段 / 事件，且答案与「某时刻的聚合状态」有关。"},{"a":"s9-3","h":"9.3 高级（体现深度）","x":"9.3 高级（体现深度） 高级 单调队列还能优化 DP，怎么识别这类题？它和斜率优化的区别是什么？ 识别信号： DP 转移的候选区间是一个随 i 单调移动的窗口，且转移里对 j 求 max/min 。 dp[i] = max{ dp[j] + w(i) : i − k ≤ j < i } 朴素：每个 i 扫 k 个候选 → O(n·k) 观察：候选集 {i−k, …, i−1} 随 i 整体右移 ⟹ 就是「定长滑动窗口最大值」 ⟹ 单调队列 → O(n) // 跳跃游戏 VI：每步最多跳 k 格，求最大得分 for ( int i = 1 ; i < n; ++i) { if (dq. front () < i - k) dq. pop_front (); // 过期 dp[i] = dp[dq. front ()] + a[i]; // 队首是窗口内 dp 最大者 while (!dq. empty () && dp[dq. back ()] <= dp[i]) dq. pop_back (); // 被支配 dq. push_back (i); } 与斜率优化的关键区别 看 转移权重能否拆成「只依赖 j 的部分」 ： 单调队列适用 ： dp[i] = max{dp[j]} + w(i) ——候选的 比较 只依赖 j（比 dp[j] 大小），与 i 无关。所以「谁支配谁」是绝对的，可以永久丢弃。 斜率优化适用 ： dp[i] = min{dp[j] + (a[i] − a[j])²} ——展开后有 −2·a[i]·a[j] 这种 i 与 j 的乘积项 ，谁更优 取决于 i 。此时不能简单丢弃，要维护一个 下凸壳 （convex hull trick），按斜率查询。 一句话判据 「候选之间的优劣关系是否与 i 无关」 ——无关则单调队列（丢弃是永久的），有关则凸包/李超线段树（丢弃要看斜率区间）。 这两者常被混为一谈，但复杂度都是 O(n)，区别在维护的不变式：一个是单调序列，一个是凸壳。 还有一类：决策单调性 + 分治 如果最优决策点 opt[i] 关于 i 单调不减（但转移不是窗口形式），可以用 分治优化 （divide and conquer optimization）做到 O(n log n)。这三类优化会在批次 4 的 动态规划 里系统对比。 高级 Prometheus 的 counter 为什么设计成「只增的累计值」而不是「每个周期的增量」？ 因为 counter 就是前缀和 ，而 rate() 就是区间查询 S[r] − S[l] 。这个设计的好处正是前缀和相对于「存增量」的好处。 ① 抗采样丢失 存增量时丢一个采样点就 永久丢了那段数据 ；存累计值时丢点只影响时间精度， 下一个点仍能算出正确的总量 。监控系统的采集必然会偶尔失败，这一条是决定性的。 ② 抗采样间隔变化 采集周期从 15s 改成 30s 时，累计值的语义不变；增量的语义会变（「每 15s 多少」变成「每 30s 多少」），历史数据无法直接对比。 ③ 客户端无状态 只需一个原子自增，不需要维护「上次上报时刻」和窗口状态。这让客户端库极简，也避免了多线程下的窗口同步问题。 ④ 可任意重采样 同一份数据能算 1 分钟、5 分钟、1 小时的 rate；增量存储只能向上聚合，且聚合后无法还原更细粒度。 代价：counter reset 进程重启让计数归零，此时 counter(t2) − counter(t1) 变成 负数 。所以 rate() 的实现必须 检测重置 ——读数下降就假定发生重启，那一段按「从 0 开始」处理。 这就是为什么应该用 rate() / increase() 而不要手写 a - b ：手写版不处理重置，会在每次发版时产生一个巨大的负值尖刺，进而触发误告警。 同一族设计 histogram 也存 累积 形式（每个 bucket 记「≤ 阈值的样本数」），这样桶可以 跨实例相加 后再算分位数——累积计数是可加的。代价是精度受桶边界限制， histogram_quantile 只能在桶内线性插值。想要更好精度就换 t-digest / DDSketch（用相对误差保证换 O(log 值域) 空间）。 见 可观测性 。 高级 一个「查询任意时间区间内的请求量」的接口，随着数据量增长越来越慢，怎么设计？ 先定位：如果实现是「扫描原始日志表按时间过滤求和」，那复杂度是 O(区间内记录数)——数据量涨，查询必然线性变慢。这是 「每次查询都重新扫一遍」的典型症状 ，也正是前缀和要解决的问题。 ① 先问清查询模式 这决定了全部方案： 区间端点是否任意 （还是只查「最近 N 分钟」）？ 需要多细的粒度 （秒级还是分钟级）？ 写入是否还在持续 （历史数据是否会变）？ 能接受多大误差 ？ ② 历史数据不变 → 预聚合 + 前缀和 按分钟预聚合成一张表，再对它建 累计和 （前缀和）。任意区间查询变成两行相减，O(1)。 数据量从「每请求一行」降到「每分钟一行」——降了几个数量级。 这是最划算的一步。 ③ 写入持续 → 分层聚合（roll-up） 秒级保留 1 小时、分钟级保留 7 天、小时级保留 1 年。查询时按区间长度 自动选层 ：查最近 10 分钟用秒级，查上个月用小时级。 这就是时序数据库（Prometheus 的 recording rules、VictoriaMetrics、InfluxDB 的 continuous query）的标准做法—— 不要自己造，直接用 。 ④ 需要边写边查任意区间 → 树状数组 / 线段树 前缀和不支持高效修改（要 O(n) 重建）。修改与查询交错时，用 树状数组 （单点改 + 区间和查，O(log n)，代码最短）。 「修改和查询是否交错」是这类选型的第一判据 ：不交错 → 前缀和 O(1)；交错 → 树状数组 O(log n)。 ⑤ 别忽略基数问题 如果要按维度（接口、状态码、地域）分别统计，预聚合的行数是 各维度基数的乘积 。维度里混进了 user_id 或 trace_id 这类高基数字段，预聚合表会比原始数据还大。 预聚合的前提是维度组合可控 ——这是实践中最常翻车的地方。 再往上一层 如果查询的是「去重用户数」而不是「请求量」， 预聚合不成立 ——去重计数不可加（两个小时各 100 个 UV，合起来不是 200）。此时要用 HyperLogLog ：它的 sketch 可以合并，用约 1.5% 的相对误差换 O(1) 空间的可加性。 「这个指标可加吗」决定了能不能预聚合 ：sum/count/min/max 可加；avg 要存 sum 和 count 分别聚合； distinct count 和分位数不可加 ，必须换成可合并的近似结构（HLL、t-digest）。见 Redis 与缓存 。 高级 「天际线问题」为什么不能只用一个整数维护当前高度？该用什么？ 因为天际线要的是 当前所有活跃建筑高度的最大值 ，而建筑的结束顺序 与开始顺序无关 ——一个先开始的高楼可能最后才结束。 为什么整数不够 会议室问题维护的是「重叠 数量 」，加减是可逆的，一个整数够了。天际线维护的是「高度 最大值 」—— max 没有逆元 ，移除一个建筑时你无法从「当前最大值」推出「新的最大值」。 这和 §9.1 第 4 题（窗口最大值不能加一个减一个）是同一个根因。 为什么单调队列也不够 单调队列要求元素 按序进入、按序过期 。天际线里建筑的结束时刻是任意的——先入的可能后出，所以「被支配就永久丢弃」这个论证不成立。 方案一 · multiset（最直观） 事件为 (x, −h) 表示开始、 (x, +h) 表示结束。扫描时开始就 insert(h) 、结束就 erase(find(h)) （ 注意用 find 只删一个 ，直接 erase(h) 会删掉所有等于 h 的），取 *rbegin() 为当前高度。O(n log n)。 事件排序技巧：把开始事件的高度存成 负数 ，这样同一 x 处「先处理开始（负数小，排前面）再处理结束」，且「多个开始时高的先处理」——一次排序解决全部同刻顺序问题。 方案二 · 延迟删除的大顶堆 堆不支持删任意元素。做法是 只入不删 ，取堆顶时先循环丢弃「已过期」的堆顶（用一个 unordered_map<高度, 待删次数> 或比较结束时刻判断）。 优点是常数比 multiset 小（数组连续 vs 红黑树指针追逐）；代价是堆可能膨胀到 O(n)。 这是「可删除堆」这一类需求 同类问题还有：滑动窗口中位数、带删除的 Top-K、Dijkstra 的 decrease-key。通用手段有三种—— ① 换能删任意元素的结构（ multiset 、平衡树）； ② 延迟删除 （懒惰堆，最常用）； ③ 索引堆 （额外维护「元素 → 堆内位置」的映射，支持真正的 O(log n) 任意删除与 decrease-key）。 三者的取舍见 堆与 Top-K 。 ⬅️ 双指针与滑动窗口 ➡️ 哈希表内核 📐 并查集与区间结构 🗃️ SQL 窗口函数 📚 目录 🏠 主页 单调栈与前缀和 — 单调栈不变式 · 柱状图最大矩形 · 单调队列 · 前缀和加哈希表 · 二维容斥 · 差分与扫描线 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/recursion_and_divide_conquer.html","t":"递归与分治","c":"算法与数据结构","s":[{"a":"","h":"","x":"递归与分治 · 栈帧与尾递归 / 递归转迭代 / 记忆化 / 分治范式 / 快速选择与 Karatsuba / 并行分治 Kavin Tech Notes ← 主页 目录 35 min ◐ 递归与分治 递归的一句话本质 与其他章节的关系 递归的机器视角 栈帧里到底有什么 爆栈：不是异常，是信号 尾调用消除为何不保证 写对递归的三件套 三个必要条件 状态往哪里放 递归转迭代三条路 尾递归改写 显式栈 状态机（后序的难点） 记忆化：从指数到线性 重叠子问题 三种写法对照 自顶向下 vs 自底向上 分治范式 三步骨架 复杂度落在哪一情形 分治案例集 归并求逆序对 快速选择与 introselect 快速幂 最近点对 Karatsuba 大数乘法 案例对照（交互） 分治的工程形态 cutoff：小规模切走 并行分治与任务窃取 递归陷阱清单 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Recursion & Divide-Conquer · 2026 递归与分治 递归难，不是因为概念复杂，而是因为它要求你 同时相信两件事 ：函数还没写完，但它已经能正确处理更小的输入。这一篇先把递归拆到栈帧层面（它为什么会崩、为什么改不成循环），再把「分治」这个最重要的递归模式讲透——从归并求逆序对到 Karatsuba，最后到生产代码里的 cutoff 与并行分治。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~35 min 前置 复杂度与分析 栈帧与爆栈 尾调用消除 递归转迭代 记忆化 分治五案例 并行分治"},{"a":"s1","h":"递归的一句话本质","x":"01 递归的一句话本质 把「怎么做」换成「假设小一号的已经做完了」 写循环是 过程式 ：维护变量，一步步推进。写递归是 归约式 ：只描述「大问题怎么由小问题拼出来」，并 假设小问题已经解决 。难处不在语法，而在于你必须同时相信两件事：函数还没写完，但它已经能正确处理更小的输入。 递归的信任契约 ：写 f(n) 时可以无条件假设更小的调用已经正确。你只负责两件事：每次调用 真的缩小 ；最小规模 直接给答案 。归纳法会保证其余。 递归像 层层授权 。总监接到「统计部门人数」，不会自己去数每个工位——给各主管同一条指令「报你手下有多少人」，再把数字加起来加 1。关键是： 你不必知道下属怎么统计 ，他们也在用同一条指令授权给更下层。 基线 是没有下属的员工直接答「1」； 规模缩小 要求组织是有限层的树，循环汇报就是无限递归。 两条主线： 机制 （§2–§4）讲栈帧、爆栈、去递归； 范式 （§5–§8）讲记忆化、分治，以及生产里的 cutoff 与并行。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 复杂度与分析 —— §6 用到的主定理与递归树都在那里 排序全景 —— 归并与快排是分治最重要的两个实例 二分查找 —— 只有一个子问题的「退化分治」 C++ 协程 —— 协程给了另一种把递归状态搬出栈的方式 操作系统基础 —— 栈的映射方式与 guard page 机制"},{"a":"s2","h":"递归的机器视角","x":"02 递归的机器视角 「函数调用自己」在硬件上是什么"},{"a":"s2-1","h":"2.1 栈帧里到底有什么","x":"2.1 栈帧里到底有什么 CPU 层面没有「递归」这个概念——只有函数调用。而函数调用的全部魔法在于 栈帧 （stack frame）：每次调用把「这次调用的私有状态」压到栈上，返回时弹掉。递归之所以能工作，纯粹是因为 同一个函数的多次调用各有一份独立的栈帧 。深度就是此刻叠了多少帧。 高地址 低地址 栈向低地址增长 main 的栈帧 调用 fact(4) fact(4) 返回地址 → main | 参数 n=4 | 局部变量 fact(3) 返回地址 → fact(4) | 参数 n=3 | 局部变量 fact(2) 返回地址 → fact(3) | 参数 n=2 | 局部变量 fact(1) ← 基线条件，直接返回 1 返回地址 → fact(2) | 参数 n=1 ← rsp（栈顶） 每帧固定开销 · 返回地址 8 B（call 指令压入） · 保存的 rbp / 被调用者保存寄存器 · 参数（前 6 个走寄存器，多的上栈） · 局部变量 + 对齐填充（16 B 对齐） 典型 32~128 B / 帧 递归深度 = 栈帧数 Linux 默认线程栈 8 MB 64 B/帧 → 约 13 万层 128 B/帧 → 约 6.5 万层 所以： O(log n) 深度 → 永远安全 O(n) 深度 + n≥10⁵ → 必崩 栈向低地址生长：深度 = 帧数。8 MB / 64 B ≈ 13 万层——这是选递归还是迭代的硬上限。 三个数字： Linux 线程栈 8 MB （Windows 主线程 1 MB）、 每帧 32–128 B 、安全深度约 10⁴–10⁵ 。帧里有 int buf[1024] 立刻掉到几千层；换 -O0 或开 sanitizer，上限再掉档。别拿一次实测当结论。"},{"a":"s2-2","h":"2.2 爆栈：不是异常，是信号","x":"2.2 爆栈：不是异常，是信号 栈溢出 不是 C++ 异常。内核在栈末端放不可访问的 guard page ； rsp 踩上去触发页错误，内核发 SIGSEGV 。 try/catch 拦不住。典型场景：n=10⁶ 的链表递归反转（深度 = n，48 B×10⁶ ≈ 48 MB > 8 MB）；解析深度嵌套的 JSON。O(n) 深度 + n 来自外部输入 = 可被打成 DoS。迭代反转只要三个指针，O(1) 空间。 main f(n) · 帧 k f(n−1) · 帧 k+1 …… 继续压帧 guard page · SIGSEGV 不是异常，是信号 · try/catch 无效，进程直接死 · gdb bt：同一函数上万帧 · 出错地址紧邻 rsp / 栈下界 · ASan 报 stack-overflow O(n) 深度 + n 由输入决定 → 必须去递归或加 depth 上限 爆栈现场：同一函数重复上万帧、地址贴着 rsp。生产里用 depth 断言赶在 SIGSEGV 之前倒下。 怎么从现场认出是爆栈而不是普通野指针： 线索 爆栈的表现 普通空指针 / 野指针 gdb 的 bt 同一个函数重复上万帧 ，或直接报 Backtrace stopped 帧数正常，栈顶是具体的业务函数 出错地址 紧邻当前 rsp ，且落在栈区下界 通常是 0x0 附近或明显的垃圾值 ASan 报告 stack-overflow SEGV on unknown address / use-after-free 给递归加 assert(depth < N) ：在踩 guard page 之前给你干净回溯。解析不可信输入时这是安全项，不只是调试技巧。"},{"a":"s2-3","h":"2.3 尾调用消除为何不保证","x":"2.3 尾调用消除为何不保证 尾调用 ：最后一个动作是调用并直接返回其结果。 TCO ：复用当前帧， call 换成 jmp ，栈深恒为 1。这是优化，不是 C++ 语言保证。 return n * fact(n−1) fact(4) 必须留着 n=4 fact(3) 必须留着 n=3 fact(2) … 帧一直堆 返回后再乘 → 不能丢帧 深度 = n return fact(n−1, acc*n) 同一帧原地改 n、acc call → jmp，rsp 不动 栈深恒为 1 累加器把「返回后的乘法」搬进参数 C++ 里仍是优化：有析构 / -O0 / 虚调用都会失效 尾递归可复用帧：把返回后的工作搬进累加器。C++ 不要拿 TCO 当深递归方案——有析构函数的局部对象会让「看起来像尾调用」的 return 其实不在尾位置。 把「递归返回后要做的事」搬进参数（累加器），非尾递归就变成尾递归。GCC/Clang 在 -O2 下常会消掉累加器版；Scheme / Lua 在标准里强制消除。C++ 失效的常见原因： 原因 为什么阻止 TCO 局部对象有析构函数 析构必须在 return 之后跑，所以那个「调用」其实不在尾位置。这是 C++ 特有的、最常见的原因。 编译在 -O0 / -Og 调试友好优先，不做栈帧复用 函数是虚函数 / 通过函数指针调用 目标不确定（除非能去虚化） 返回大对象（走 sret 隐藏指针） 调用约定要求当前帧提供返回值空间 参数需要在栈上传递且布局不同 无法原地复用帧 开了 sanitizer / -fno-optimize-sibling-calls 需要完整栈信息 最隐蔽的一条： 局部对象有析构 。析构必须在 return 之后跑，所以 std::string tmp = log(n); return f(n-1); 看起来像尾调用，其实不是。RAII 用得越好越容易撞上。需要 O(1) 栈深就手写循环（§4）；需要 O(log n) 上限就只递归小半边。C++ 里不要把「尾递归 + 编译器优化」当成深递归方案。 C++ 没有 [[tail_call]] 。Clang 的 __attribute__((musttail)) 做不到就 编译报错 ，适合解释器 dispatch；代价是不可移植。"},{"a":"s3","h":"写对递归的三件套","x":"03 写对递归的三件套 递归写不出来，通常是这三件里漏了一件"},{"a":"s3-1","h":"3.1 三个必要条件","x":"3.1 三个必要条件 要素 问自己 漏掉的后果 ① 规模严格缩小 每次递归调用的参数，是否 一定 比当前更接近基线？ 无限递归 → 爆栈 ② 基线条件完备 所有能到达的最小情形都覆盖了吗（含空、单元素、负数）？ 越过基线继续递归 → 爆栈或越界 ③ 归约关系正确 假设子调用返回正确答案，我拼出的结果对吗？ 结果错但不崩，最难查 「缩小」不等于「参数变了」。 f((n+1)/2) 在 n=1 时还是 f(1) 。写完手推最小的两三个输入。 ① 基线 if (!r) return 0 空 / 单元素 / n≤1 树题用「空」不用「叶」 ② 缩小 depth(left), depth(right) 每次更接近基线 漏了 → 无限递归 ③ 组合 1 + max(L, R) 假设子调用已正确 错了不崩，最难查 三件齐了，归纳法替你保证全部输入 工程还要问第四件：深度是 O(log n) 还是 O(n) 三件套：基线放最前、参数真缩小、组合关系对。树递归基线永远选空节点，叶子和单孩子自动统一。 int depth (TreeNode* r) { if (!r) return 0 ; // 基线 return 1 + std:: max ( depth (r->left), // 缩小 depth (r->right)); // 组合 } 无限递归 · 缺基线 f(1) → f(1) → f(1) → … 参数没变，或 n=1 时 n/2 仍是 1 bt 里参数一直不变 最终踩 guard page 修好 if (n ≤ 1) return … 下一步必须更接近基线 手推 n=0 / 1 / 2 互递归：每条边都要缩小 缺基线或「缩小」是假的：栈回溯里参数冻住。整数除法取整方向是最高发原因。"},{"a":"s3-2","h":"3.2 状态往哪里放","x":"3.2 状态往哪里放 方式 写法 优点 代价 值参数 f(node, depth) 每层独立，回溯自动完成 每帧一份拷贝 引用参数 f(node, path&) 零拷贝，能收集结果 push 后必须 pop 捕获式 lambda [&](auto&& self, int u) 状态在外层，签名干净 C++20 前要 self / function 纪律：push 在第一行、pop 在最后一行、中间不许 early return。要用提前返回就上 RAII guard——有析构会挡住 TCO，O(n) 深递归本来就该迭代。"},{"a":"s4","h":"递归转迭代三条路","x":"04 递归转迭代三条路 按难度从易到难：累加器、显式栈、状态机"},{"a":"s4-1","h":"4.1 尾递归改写","x":"4.1 尾递归改写 最简单的一类：递归已是最后一个动作（或能用累加器变成最后一个）。改写规则机械： 基线 → 循环退出（取反）；递归实参 → 循环末尾对参数赋值。 gcd 的 gcd(b, a%b) 变成 while 里三变量轮换； fact(n, acc*n) 变成 while (n>1) acc *= n, --n 。需要 O(1) 栈深就手写 while ，不要赌 TCO（§2.3）。"},{"a":"s4-2","h":"4.2 显式栈","x":"4.2 显式栈 多个递归调用、或返回后还有工作时，把编译器替你维护的栈自己写出来。显式栈 = 手动 DFS：右子先压，才能左子先出。 递归调用栈 visit(A) · 待进左 visit(B) · 左孩子 帧里藏着「返回后走右」 编译器记住 PC + 局部量 = 堆上 vector（LIFO） 栈顶 → B（先 visit） 其下 → C（A 的右子，后出） pop → visit → 右先 push、左后 push 顺序与递归前序一致 用 vector 当栈：连续内存、可 reserve。后序还要在栈里存 stage（返回地址显式化） C++20 协程做的是同一件事：把帧搬到堆上 显式栈就是手动 DFS。深度 O(log n) 用递归；n 由外部输入决定时必须去递归（防 DoS）。 void pre (TreeNode* r) { if (!r) return ; std::vector<TreeNode*> st{r}; while (!st. empty ()) { TreeNode* n = st. back (); st. pop_back (); visit (n); if (n->right) st. push_back (n->right); // 右先入 → 左先出 if (n->left) st. push_back (n->left); } } 用 vector 当栈而不是 std::stack （默认底层 deque）：连续内存、可 reserve ，竞赛和高性能代码的默认选择。"},{"a":"s4-3","h":"4.3 状态机（后序的难点）","x":"4.3 状态机（后序的难点） 前序好改：访问在递归 之前 ，节点只被「碰」一次。后序难：同一节点要等两棵子树都完成，会被回到多次。栈里除了指针还要存 stage ：0 还没进左、1 左完进右、2 两边完再 visit。这就是通用去递归化—— (局部状态, 程序计数器) ，stage 扮演栈帧里的返回地址。任意递归都能机械改写，可读性会很差。C++20 协程做的是同一件事：把帧搬到堆上。 深度有 O(log n) 保证 → 用递归，30 层怎么都不会崩。深度可能 O(n) 且 n 来自外部 → 必须去递归，不是为了性能，是防 DoS。第三条路是换算法：Morris 遍历用空右指针线索化，O(1) 空间；图用 BFS，队列在堆上。"},{"a":"s5","h":"记忆化：从指数到线性","x":"05 记忆化：从指数到线性 递归的最大浪费，和一行代码的修复"},{"a":"s5-1","h":"5.1 重叠子问题","x":"5.1 重叠子问题 朴素的 Fibonacci 递归是 O(φⁿ) ≈ O(1.618ⁿ)，慢得离谱。原因不是递归本身，而是 同一个子问题被反复计算 ： fib(5) fib(4) fib(3) fib(3) fib(2) fib(2) fib(1) fib(2) fib(1) fib(1) fib(0) fib(1) fib(0) fib(1) fib(0) fib(3) 算了 2 次 fib(2) 算了 3 次 基线（共 8 个叶子） 只有 6 个 不同 的子问题（fib0..fib5），却做了 15 次调用 → 指数浪费 fib(n) 递归树 O(φⁿ) 个节点，不同子问题只有 n+1 个。记忆化就是把树压成 DAG。 fib5 fib4 fib3 fib3 fib2 fib1 命中 memo，边合并 每个 n 一格：O(n) 节点，不再指数展开 记忆化 DAG：虚线是「查表直接返回」。子问题重叠 → DP；子问题不相交 → 分治，不必 memo。 重叠子问题 ：同一子问题在树的不同位置反复出现——这是 DP 信号。 分治 的子问题互不相交（归并左右两半不共享），所以不需要记忆化。 「是否重叠」是分治 vs DP 的唯一判据。"},{"a":"s5-2","h":"5.2 三种写法对照","x":"5.2 三种写法对照 ❌ 朴素递归 O(φⁿ) long long fib ( int n) { if (n < 2 ) return n; return fib (n- 1 ) + fib (n- 2 ); } // n=50 要算约 2×10¹⁰ 次 → 分钟级 ✅ 记忆化 O(n) long long fib ( int n, std::vector< long long >& memo) { if (n < 2 ) return n; if (memo[n] != - 1 ) return memo[n]; // ← 一行 return memo[n] = fib (n- 1 , memo) + fib (n- 2 , memo); } // 每个 n 只算一次 → n=50 瞬间 // 但栈深仍是 O(n) 通用路径： 朴素 → memo（消重复）→ 自底向上（消栈）→ 滚动变量（消空间） 。Fibonacci 还能矩阵快速幂 O(log n)。注意：加了 memo 之后 栈深仍是 O(n) ，n=10⁶ 照样爆栈——记忆化不是去递归。"},{"a":"s5-3","h":"5.3 自顶向下 vs 自底向上","x":"5.3 自顶向下 vs 自底向上 记忆化递归 迭代 DP 写法 跟递归定义一致 要先排计算顺序 栈深 O(深度)，可能爆 0 无用状态 不算 全表都填 稀疏状态 哈希表优势明显 浪费 空间压缩 难 滚动数组容易 稠密且要全填 → 自底向上（纯循环、好压空间）；稀疏或拓扑序难定 → 记忆化。竞赛默认前者；状态是字符串/集合时几乎只能后者。自顶向下像按需查族谱，只走关心的一支；自底向上像从始祖把整本族谱填完。"},{"a":"s6","h":"分治范式","x":"06 分治范式 子问题互不重叠时的标准骨架"},{"a":"s6-1","h":"6.1 三步骨架","x":"6.1 三步骨架 Divide 把规模 n 的问题拆成 a 个规模 n/b 的 独立 子问题 Conquer 递归解决每个子问题（规模足够小时直接算） Combine 把子问题的解合并成原问题的解，代价 f(n) T(n) = a · T(n/b) + f(n) 「独立」是定义性前提：子问题之间没有共享的子子问题。归并把数组切成左右两半，左半完全不需要右半——所以不 memo。这是分治与 DP 的分界（§5.1）。 分治 · 不相交 左半 [0,m) 右半 [m,n) 合并 O(n)，无共享子问题 不必记忆化 DP · 重叠 fib(n) 依赖 fib(n−1) 与 fib(n−2) 两棵子树共享 fib(n−2) 以下 同一格会被走进多次 必须 memo / 填表 切开后互不相干 → 分治；会重复走进同一状态 → DP。不要给归并加 memo，也不要给 fib 只写分治骨架。 设计决策只有三个： 怎么切 （a、b）、 切到多小停 （基线 / cutoff）、 合并花多少 （f(n)）。三者一定，复杂度就定了。骨架实现见 §7.1 的归并求逆序对。"},{"a":"s6-2","h":"6.2 复杂度落在哪一情形","x":"6.2 复杂度落在哪一情形 用主定理（ 复杂度与分析 §5.2）：c* = log_b a，拿 f(n) 和 n^c* 比大小。 算法 a b f(n) c* 结果 二分查找 1 2 Θ(1) 0 Θ(log n) 归并排序 2 2 Θ(n) 1 Θ(n log n) 快速幂 1 2 Θ(1) 0 Θ(log n) 快速选择（理想划分） 1 2 Θ(n) 0 Θ(n) 最近点对 2 2 Θ(n) 1 Θ(n log n) Karatsuba 3 2 Θ(n) ≈1.585 Θ(n^1.585) 朴素分治大数乘 4 2 Θ(n) 2 Θ(n²)（无收益） 最后两行：f(n) 和 b 相同，只把 a 从 4 降到 3，就从 Θ(n²) 掉到 Θ(n^1.585)。情形一（叶子主导）里， 降低子问题个数是唯一杠杆 。情形三才值得砸合并步；cutoff 永远只影响常数。 思考顺序：先减 a（Karatsuba / Strassen——用更多加减换更少乘法），再降合并代价（只在情形三有用），最后调 cutoff。顺序反了就是在错的地方使劲。"},{"a":"s7","h":"分治案例集","x":"07 分治案例集 逆序对是骨架；其余用图和表对照"},{"a":"s7-1","h":"7.1 归并求逆序对","x":"7.1 归并求逆序对 模式： 在合并步顺便统计跨界信息 。逆序对 i < j 且 a[i] > a[j] ，朴素 O(n²)。合并两个已排序半段时，从右半取出 a[j] ，左半剩余全部比它大—— 左半剩余个数 一次加完。 [8 4 2 1] cnt=6 [8 4] 内部 1 [2 1] 内部 1 8 4 2 1 合并：右半取出 2 时，左半还剩 8、4 → +2 每层 Θ(n)，log n 层 → Θ(n log n)。等号 a[i]≤a[j] 同时管正确性与稳定性 归并树：内部逆序对在子问题里算完，跨界的在 Combine 一次加完。同一模板：翻转对、区间小于 x 的个数。 long long merge_count (std::vector< int >& a, int lo, int hi, std::vector< int >& buf) { if (hi - lo < 2 ) return 0 ; int mid = lo + (hi - lo) / 2 ; long long cnt = merge_count (a, lo, mid, buf) + merge_count (a, mid, hi, buf); int i = lo, j = mid, k = lo; while (i < mid && j < hi) { if (a[i] <= a[j]) { buf[k++] = a[i++]; } else { cnt += mid - i; // ← 左半剩余全部与 a[j] 构成逆序对 buf[k++] = a[j++]; } } while (i < mid) buf[k++] = a[i++]; while (j < hi) buf[k++] = a[j++]; std:: copy (buf. begin () + lo, buf. begin () + hi, a. begin () + lo); return cnt; } a[i] <= a[j] 的等号同时决定：相等不算逆序对、相等时取左边（稳定性）。写成 < 两件事一起坏。同一模板还能解翻转对（a[i] > 2·a[j]）、区间内小于 x 的个数、三元逆序对。识别信号：要统计的量能在「左半 × 右半」跨界配对上一次算清。"},{"a":"s7-2","h":"7.2 快速选择与 introselect","x":"7.2 快速选择与 introselect 第 k 小：排序后取是 O(n log n)。快选每次划分后 只递归一边 ，期望 O(n)。最坏仍 O(n²)。 n · 随机 pivot 就位 丢掉的一侧 pivot 只递归含 k 的一侧 ≈ 3n/4 再切 → 9n/16 … n + 3n/4 + 9n/16 + … < 4n → 期望 Θ(n) 快排两边都要：2T(n/2)+Θ(n) = Θ(n log n)。情形三：f(n)=Θ(n) 压过 n⁰ 期望线性来自「丢掉常数比例」。std::nth_element = introselect：快选走常数，超深切 median-of-medians / 堆，最坏 O(n)。 方案 最坏 期望 实测常数 用在哪 随机化快选 O(n²) O(n) 小 绝大多数场景 median-of-medians O(n) O(n) 大（约 5–10×） 需要最坏保证时 introselect O(n) O(n) 小 std::nth_element std::nth_element = introselect：默认走快选（常数小），监控递归深度，超过约 2 log n 就切 median-of-medians 或堆，拿到最坏 O(n)。和 std::sort 的 introsort 是同一个设计—— 快路径求常数，慢路径兜底最坏 。完整剖析见 排序全景 §9，接口对照见 STL 算法库 。"},{"a":"s7-3","h":"7.3 快速幂","x":"7.3 快速幂 a^n = (a^(n/2))² ，O(log n) 次乘。迭代按二进制展开： a^13 = a^8·a^4·a^1 （13=1101₂）。任何满足结合律的运算都能这样加速。 「乘法」换成 能解什么 矩阵乘 线性递推第 n 项（Fibonacci 的 O(log n)） 布尔矩阵乘 恰好 k 步可达 (min, +) 乘 恰好 k 条边的最短路 加法（快速乘） 大模数下 a*b%m 防溢出 r*a%m 在 m 接近 2⁶³ 时溢出 long long ——中间结果放不下。用 __int128 中转。这是生产里最高发的快速幂 bug， 只在大模数暴露 ，小数据完全测不出来。"},{"a":"s7-4","h":"7.4 最近点对","x":"7.4 最近点对 平面 n 点求最近一对。朴素 O(n²)。精妙全在合并：d = min(d_L, d_R) 之后，跨界更近点只可能落在中线两侧各 d 的带内；带内按 y 排后每点最多比 7 个邻居。 左半：递归得 d_L 右半：递归得 d_R x = mid 带宽 2d d = min(d_L, d_R) → 跨界的更近点对只可能落在中线两侧各 d 的带内 关键引理 带内点按 y 排序后，每个点 只需和后面 最多 7 个 点比较 （否则会有两点距离 < d， 与 d 的定义矛盾）→ 合并 O(n) 合并靠几何引理压到 O(n)：2d×d 矩形里最多 8 个互相距离 ≥ d 的点。每层重排 y 会变成 O(n log² n)，要预先按 y 排、回溯时归并维护。 为什么是 7 ：带切成 d/2×d/2 格，每格最多 1 点（对角线 d/√2 < d）；目标点邻域 2×4=8 格，除去自己剩 7。常数是 6/7/8 不重要，重要的是它是常数——合并才是 O(n) 而不是 O(n²)。 每层都对带内点按 y 重排，合并变成 O(n log n)，总复杂度退化为 O(n log² n) 。要真正 O(n log n)，必须预先按 y 排一份，回溯时像归并一样维护 y 序。很多教材示例其实是 log² 版——面试能指出是加分项。"},{"a":"s7-5","h":"7.5 Karatsuba 大数乘法","x":"7.5 Karatsuba 大数乘法 竖式 O(n²)。切高低两半：x=x₁B+x₀，展开要 4 次子乘。主定理 c*=log₂4=2， 和竖式一样，白干 。Karatsuba 用一次加减把乘法降到 3 次：P₁=x₁y₁，P₂=x₀y₀，P₃=(x₁+x₀)(y₁+y₀)，中间项 = P₃−P₁−P₂。 T(n) = 3 T(n/2)+O(n) → Θ(n^log₂3) ≈ Θ(n^1.585) 情形一里复杂度只由 a 决定：a 从 4→3，指数从 2 掉到 1.585。优化合并步在这里毫无收益。 像 用三次称重代替四次 ：称 A、称 B、再称 A+B，第四个数算出来。乘法比加减贵，所以这笔交易永远划算。Strassen 是同一笔账（8 次子矩阵乘 → 7 次，代价 18 次矩阵加减）。 再快有 Toom-Cook（切 k 份）和 FFT 乘法（Schönhage–Strassen）。GMP 按位数分段：竖式 → Karatsuba → Toom → FFT。OpenBLAS 的 GEMM 不用 Strassen——分块 + SIMD 的常数优势有 10×，而且数值稳定性更差。渐进不等于实用。"},{"a":"s7-6","h":"7.6 案例对照（交互）","x":"7.6 案例对照（交互） 分治五案例：拆法 / 合并 / 复杂度 点击查看每个案例的 Divide-Conquer-Combine 三步分别做了什么，以及复杂度落在主定理哪一情形。 归并求逆序对 快速选择 快速幂 最近点对 Karatsuba"},{"a":"s8","h":"分治的工程形态","x":"08 分治的工程形态 教科书版本和生产版本差两处改造"},{"a":"s8-1","h":"8.1 cutoff：小规模切走","x":"8.1 cutoff：小规模切走 生产归并不递归到单元素：到 16–32 就切插入排序。理由叠在一起：小 n 时函数调用比比较还贵；插入排序顺序访问、零分配、近乎有序时几乎无误预测；32 个 int 整块在 L1。最有说服力的一条：二叉递归树里 叶子数约等于内部节点数 ，砍掉最底几层等于消掉一半调用。 n n/2 n/2 n ≤ CUTOFF insertion_sort n ≤ CUTOFF insertion_sort n ≤ CUTOFF insertion_sort n ≤ CUTOFF insertion_sort 叶子在 L1、插入排序零分配。CUTOFF 用真实数据测 16/24/32/48，别照抄 另：if (a[mid−1] ≤ a[mid]) 跳过合并 → 近乎有序时 Θ(n)。TimSort 把 run 识别做到极致 小规模切插入排序：优化叶子等于优化一半调用。libstdc++ 的 std::sort 用 16，那是为 int 调的。"},{"a":"s8-2","h":"8.2 并行分治与任务窃取","x":"8.2 并行分治与任务窃取 子问题独立所以能并行；每层开线程会在 n=10⁶ 时创建百万 OS 线程（每线程 8 MB 栈）直接 OOM。 n > PAR_CUTOFF 才值得并行 fork 一半 async / spawn 当前线程做另一半 自己不闲着 join / sync → merge n ≤ SEQ_CUTOFF → 插入排序（串行叶子） PAR ≫ SEQ。顶层 merge 是 Amdahl 瓶颈，真并行归并要把 merge 也切开 fork-join：fork 一个、自己干另一个。任务窃取（Cilk / TBB / ForkJoinPool / Rayon）让不均衡分治自动均衡。 任务窃取：每个线程自己的双端队列，从 队尾 取自己的任务（LIFO，刚生成的子任务还在 cache），空闲时从 别人队头 偷（FIFO，偷到最大最老的块，摊薄偷取开销）。快排划分不均时也能自动均衡，不必预先知道任务大小。Cilk、TBB、Java ForkJoinPool、Rayon 都是这个模型。 C++17 std::execution::par 可少写线程池（libstdc++ 要 -ltbb ）。三条前提： par_unseq 允许向量化交错，可调用体 不能有同步 （加锁、分配都可能死锁）； reduce 要求可结合可交换——浮点加法不满足，结果可能与 accumulate 不同；粒度不够时并行版更慢。取舍见 并发模式 。"},{"a":"s9","h":"递归陷阱清单","x":"09 递归陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 规模没真的缩小 栈溢出，且 bt 里参数一直不变 手推最小的 2–3 个输入；注意整数除法的取整方向 基线不完备 空指针崩溃 / 越界 树递归以「空节点」为基线；数组递归先判 lo >= hi 深度 O(n) 且 n 来自外部输入 可被构造成 DoS 改迭代，或加 depth 断言 + 上限拒绝 重叠子问题未记忆化 指数级慢，但结果对 加 memo；判据是「同一参数是否被多次调用」 引用参数忘回溯 结果多出/污染，不崩 push/pop 严格配对，或用 RAII guard 按值传大对象 慢且吃内存，profile 里全是拷贝构造 改 const& ；只在确实需要每层独立副本时按值传 依赖尾调用消除 -O0 或加了 RAII 后突然爆栈 C++ 里不要依赖 TCO；要么手写循环，要么保证深度 O(log n) 递归里做 I/O 或加锁 死锁 / 极慢 递归收集，出来后统一 I/O；锁在递归外层拿（或用递归锁，但那通常是设计问题） 相互递归漏了终止 isEven/isOdd 式互调无限循环 把「规模缩小」的义务落在 每一条 调用边上 快速幂中间溢出 大模数下结果错，小数据测不出 __int128 中转，或用快速乘 前三条可以用工具兜住：ASan 的 stack-overflow 把爆栈报成可读诊断（不是裸 SIGSEGV）；模糊测试专门找「深度嵌套把递归解析器打崩」。工具用法见 Sanitizers 实战 。处理不可信输入时，还要逐个资源维度问「上界在哪」——递归深度只是其中一例，同类还有 zip bomb、ReDoS。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 写一个正确的递归需要哪几个要素？各自漏掉会怎样？ 三个要素，缺一不可： ① 规模缩小 每次递归调用的参数必须 严格 更接近基线。漏掉 → 无限递归 → 爆栈。常见错误是「参数变了」但没变小，比如整数除法里 f((n+1)/2) 在 n=1 时还是 f(1) 。 ② 基线完备 所有可达的最小情形都要覆盖（空、单元素、0、负数）。漏掉 → 越过基线继续递归 → 崩溃或越界。 树递归的通用技巧：以「空节点」而不是「叶子节点」做基线 ，这样所有分支自动统一，能消掉一半边界 bug。 ③ 归约关系正确 假设子调用返回正确答案，你拼出的结果必须对。漏掉 → 不崩但结果错 ，最难查。 满足①②后，数学归纳法就替你保证了正确性——这也是为什么递归可以「相信自己」：基线是归纳基础，归约关系是归纳步。 额外一条（工程） 还要问「深度会是多少」。O(log n) 深度永远安全；O(n) 深度且 n 由外部输入决定时， 这是个安全问题而不只是性能问题 ——攻击者可以用深度嵌套的输入把爆栈变成 DoS。 基础 递归为什么会爆栈？爆栈时程序表现是什么，能被 catch 吗？ 每次函数调用都要在栈上压一个 栈帧 ，存返回地址、保存的寄存器、参数和局部变量，典型 32–128 字节。递归深度 = 栈帧数，深度太大就超出栈空间。 数量级 Linux 默认线程栈 8 MB （Windows 主线程 1 MB）。按 64 B/帧算约 13 万层。 所以安全深度大约 10⁴–10⁵ ；帧里有大数组时会骤降到几千层。 不能 catch 栈溢出 不是 C++ 异常。内核在栈末端放了不可访问的 guard page ，越过时触发页错误，内核判定不是合法栈扩展，发 SIGSEGV 。程序当场死， try/catch 完全拦不住。 怎么认出是爆栈 ：gdb 的 bt 里同一个函数重复上万帧（或直接 Backtrace stopped ）；出错地址紧邻 rsp ；ASan 报 stack-overflow 而不是 SEGV on unknown address 。 实用技巧 给递归函数加 depth 参数并 assert(depth < N) 。它在爆栈 之前 触发，给出干净可读的回溯，而不是一堆重复帧。处理外部输入（嵌套 JSON、递归下降解析器）时这几乎是必须的。 基础 朴素 Fibonacci 递归为什么是指数级？加一行代码怎么变成 O(n)？ 因为存在 重叠子问题 ： fib(5) 展开后 fib(3) 被算 2 次、 fib(2) 被算 3 次，越往下重复越多。递归树有 O(φⁿ)≈O(1.618ⁿ) 个节点，但本质不同的子问题只有 n+1 个。 记忆化 ——加一行查表： long long fib ( int n, std::vector< long long >& memo) { if (n < 2 ) return n; if (memo[n] != - 1 ) return memo[n]; // ← 这一行 return memo[n] = fib (n- 1 , memo) + fib (n- 2 , memo); } 每个 n 只算一次 → O(n) 时间。 但栈深仍是 O(n) ，n=10⁶ 照样爆栈。 继续优化的四步路径 朴素递归 → 记忆化 （消重复） → 自底向上迭代 （消栈深） → 滚动变量 （消空间，O(1)）。这四步是绝大多数 DP 题的通用优化顺序。Fibonacci 还能用矩阵快速幂做到 O(log n)。 判据 「子问题是否重叠」是区分 该用 DP 还是 该用分治 的唯一标准。归并排序的左右两半完全不相交，所以不需要记忆化；Fibonacci 的两个子调用共享大量子子问题，所以必须记忆化。 基础 什么是分治？它的三步是什么？举两个例子。 分治是把规模 n 的问题拆成若干规模更小、 互不重叠 的子问题，递归解决后合并。 Divide 拆成 a 个规模 n/b 的独立子问题 Conquer 递归解决（规模小于 cutoff 时直接算） Combine 合并子解，代价 f(n) T(n) = a·T(n/b) + f(n) → 用主定理求解 例一 · 归并排序 ：切成左右两半（a=2, b=2），递归排序，然后 O(n) 归并 → T(n)=2T(n/2)+Θ(n) = Θ(n log n)。 例二 · 二分查找 ：切成两半但 只需递归一半 （a=1, b=2），合并代价 Θ(1) → T(n)=T(n/2)+Θ(1) = Θ(log n)。 分治 vs DP 关键区别是 子问题是否独立 。分治的子问题不相交（左半排序不需要右半的任何信息），所以不需要记忆化。DP 的子问题重叠，必须记忆化。看到「子问题会重复出现」就该想 DP，看到「切开后互不相干」就该想分治。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 什么是尾递归？为什么在 C++ 里不能依赖尾调用消除？ 尾调用 指函数的最后一个动作是调用另一个函数并 直接返回其结果 ，中间没有任何额外运算。编译器可以复用当前栈帧、把 call 换成 jmp ，递归就变成了循环，栈深恒为 1。 不是尾递归 return n * fact (n- 1 ); // 返回后还要乘 → 帧必须保留（存 n） 是尾递归 return fact (n- 1 , acc*n); // 用累加器把「返回后的工作」搬进参数 C++ 里不能依赖它，因为 TCO 是优化而非语言保证 （Scheme、Lua 在标准层面强制要求，C++ 没有）。失效原因： 最隐蔽的一条 局部对象有析构函数 。析构必须在 return 之后执行，所以那个调用其实不在尾位置： int f ( int n) { std::string tmp = log (n); // 有析构 return f (n- 1 ); // 看着是尾调用，实际不是 } 这是 C++ 特有的，RAII 越用得好越容易撞上。 其他原因 -O0 / -Og 、虚函数或函数指针调用（目标不确定）、返回大对象走 sret、开了 sanitizer 或 -fno-optimize-sibling-calls 。 正确做法 需要 O(1) 栈深 → 手写循环 ；需要 O(log n) 上限 → 只递归小半边 。Clang 的非标准 __attribute__((musttail)) 会在做不到 TCO 时 编译报错 ，把玄学变成确定性，代价是不可移植。 中级 怎么把任意递归机械地改成迭代？以二叉树后序遍历为例。 通用方法： 自己维护栈，栈里存 (局部状态, 执行到哪一步) 。后者就是把编译器帮你记住的「返回地址」显式化。 前序好改，因为访问发生在递归 之前 ，一个节点只需被「碰」一次： std::vector<TreeNode*> st{r}; while (!st. empty ()) { auto * n = st. back (); st. pop_back (); visit (n); if (n->right) st. push_back (n->right); // 右先入 → 左先出 if (n->left) st. push_back (n->left); } 后序难，因为节点要 等两棵子树都完成 ，会被回到多次。所以栈里必须记录阶段： std::vector<std::pair<TreeNode*, int >> st{{r, 0 }}; while (!st. empty ()) { auto & [n, stage] = st. back (); if (stage == 0 ) { ++stage; if (n->left) st. push_back ({n->left, 0 }); } else if (stage == 1 ) { ++stage; if (n->right) st. push_back ({n->right, 0 }); } else { visit (n); st. pop_back (); } } 这就是去递归化的本质 stage 扮演的正是栈帧里那个「返回地址」。任何递归都能这样机械改写，代价是可读性直线下降。 C++20 协程做的事本质相同 ——把局部状态和「执行到哪一行」打包搬到堆上。 工程取舍 深度有 O(log n) 保证时 就用递归 ，可读性完胜。深度可能 O(n) 且 n 由外部输入决定时 必须去递归 （防 DoS）。还有第三条路： 换算法 ——Morris 遍历用叶子的空右指针线索化，O(1) 空间且无栈；图遍历用 BFS 代替 DFS，队列在堆上天然不爆栈。 中级 怎么用归并排序在 O(n log n) 内统计逆序对？说清关键那一步。 把统计 嵌进归并的合并步 。关键洞察：合并两个 已排序 半段时，若从右半取出 a[j] ，说明左半剩下的所有元素都比它大——那么左半剩余个数就是以 a[j] 为右端点的逆序对数，一次加完。 int i = lo, j = mid, k = lo; while (i < mid && j < hi) { if (a[i] <= a[j]) buf[k++] = a[i++]; else { cnt += mid - i; // ← 关键一行 buf[k++] = a[j++]; } } 总复杂度就是归并排序本身：T(n)=2T(n/2)+Θ(n) = Θ(n log n)。 别写错等号 a[i] <= a[j] 里的等号同时决定两件事： ① 相等元素不被算成逆序对（正确性）； ② 相等时优先取左边（ 稳定性 ）。写成 < 两者同时坏掉。 模式识别 这属于「 在分治的合并步顺便统计跨界信息 」这一类。识别信号是： 要统计的东西可以在「左半 × 右半」的跨界配对上一次算清 。同一套模板还能解「翻转对（a[i] > 2·a[j]）」「区间内小于 x 的个数」「三元逆序对」。 替代解法 树状数组（BIT）+ 离散化也是 O(n log n)，从右往左扫，每次查询「已插入的比当前小的个数」。分治版不需要离散化、常数更小；BIT 版更容易扩展成在线查询。 中级 快速选择为什么是期望 O(n) 而快排是 O(n log n)？ std::nth_element 怎么保证最坏 O(n)？ 差别只有一处： 快排划分后两边都要递归，快选只需递归一边 （第 k 小只可能在一边）。 快排：T(n) = 2 T(n/2) + Θ(n) → Θ(n log n) 快选：T(n) = 1 T(n/2) + Θ(n) → Θ(n) 主定理情形三：f(n)=Θ(n) 压过 n^log₂1 = n⁰ = 1 更直观地：随机 pivot 时每次期望切掉常数比例，代价是几何级数 n + 3n/4 + 9n/16 + … < 4n 。 注意是「期望」不是「最坏」 ——最坏（每次 pivot 都取到极值）仍是 O(n²)。 std::nth_element 用 introselect ： 快路径 随机化/启发式 pivot 的快速选择，常数小。 慢路径兜底 监控递归深度 ，超过阈值（约 2 log n）就切换到有最坏 O(n) 保证的算法（median-of-medians 或改用堆）。 方案 最坏 实测常数 随机化快选 O(n²) 小 median-of-medians O(n) 大 5–10× introselect O(n) 小 这是个可复用的设计模式 「 快路径求常数，慢路径兜底最坏，用一个廉价的监控量在两者间切换 」—— std::sort 的 introsort 也是这个结构（超深转堆排）。它的价值在于：你既拿到了随机化算法的实测速度，又拿到了确定性算法的最坏保证，代价只是一个深度计数器。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 Karatsuba 为什么能把 O(n²) 降到 O(n^1.585)？这个思路的一般形式是什么？ 把 n 位数切成高低两半 x = x₁B + x₀ 、 y = y₁B + y₀ ，展开得： x·y = x₁y₁·B² + (x₁y₀ + x₀y₁)·B + x₀y₀ 朴素分治要算 4 个子乘积 → T(n)=4T(n/2)+O(n) ，c*=log₂4=2 → Θ(n²)，完全白干 。 Karatsuba 用一次额外加减把 4 次降到 3 次 ： P₁ = x₁y₁, P₂ = x₀y₀, P₃ = (x₁+x₀)(y₁+y₀) x₁y₀ + x₀y₁ = P₃ − P₁ − P₂ ← 中间项用减法得到 T(n) = 3T(n/2) + O(n) → Θ(n^log₂3) ≈ Θ(n^1.585) 为什么降 a 这么值钱 在主定理 情形一（叶子主导） 里，复杂度 = Θ(n^log_b a)，完全由 a 决定，f(n) 无关。所以 a 从 4→3 就把指数从 2 降到 1.585。 优化合并步（f）在情形一里毫无收益 ——这是设计分治算法时最重要的一条判断。 一般形式 用便宜的运算换贵的运算 。乘法比加减贵得多（大数上尤其），所以多做几次加减去省一次乘法总是划算。同一交易的其他实例： Strassen 矩阵乘 （8 次子矩阵乘 → 7 次，代价 18 次矩阵加减，得 Θ(n^2.807)）、 Toom-Cook （切 k 份，用多项式插值省乘法）、 FFT 乘法 （把卷积搬到频域，Schönhage–Strassen 达 O(n log n log log n)）。 渐进不等于实用 GMP 的策略是 按位数分段选算法 ：小数走竖式、中等 Karatsuba、更大 Toom-Cook、超大 FFT。因为 Karatsuba 的递归 + 临时空间常数远大于竖式，交叉点在几十个 limb。同理 OpenBLAS/MKL 的 GEMM 不用 Strassen——分块 + SIMD + FMA 的常数优势有 10–20×，而且 Strassen 数值稳定性更差 ，这是渐进分析完全看不见的成本。 高级 为什么生产级归并排序不递归到单元素？cutoff 该怎么定？ 因为小规模时 递归本身的开销超过了算法优势 。四条理由，最后一条最有说服力： 递归开销占比 n=4 时，函数调用 + 栈帧建立 + 边界判断的成本超过实际的比较与移动。 插入排序常数极小 顺序访问、零分配、分支高度可预测（近乎有序时几乎无分支误预测）。 小数组整块在 L1 32 个 int = 128 字节 = 两条 cache line，全程命中。 叶子占一半节点 二叉递归树里 叶子数约等于内部节点数 。砍掉最底几层等于消掉 一半 的函数调用——这是收益最大的地方。 cutoff 怎么定 ：不要照抄，也不要凭感觉。它取决于元素大小、比较器成本、cache 大小。 唯一正确的方式是用真实数据分布测 16/24/32/48/64 看曲线。 libstdc++ 的 std::sort 用 16（ _S_threshold ），那是为 int 调的；比较器很贵时最优值更小。 另一个必加的优化 if (a[mid- 1 ] <= a[mid]) return ; // 两半已首尾有序 → 跳过整个合并 对已排序或近乎有序的输入，这一行把 Θ(n log n) 变成 Θ(n)。 TimSort （Python/Java）把这个思路做到极致：主动识别输入里天然存在的有序段（run）并直接合并，对真实世界的部分有序数据快得多——真实数据几乎从不是均匀随机的，这是个被低估的优化方向。 高级 并行化一个分治算法，你会怎么做？会踩哪些坑？ 分治天生适合并行（子问题独立），但 每层无脑开线程会立刻崩 ——n=10⁶ 时试图创建 10⁶ 个 OS 线程，每个默认 8 MB 栈，直接 OOM。 正确做法是 双阈值 + 当前线程参与 ： if (n <= SEQ_CUTOFF) { insertion_sort (...); return ; } if (n > PAR_CUTOFF) { // 只在大块时并行 auto fut = std:: async (std::launch::async, [&]{ psort (lo, mid); }); psort (mid, hi); // 当前线程做另一半，不闲着 fut. get (); } else { psort (lo, mid); psort (mid, hi); } 坑一：粒度太细 任务切分和同步开销在 μs 量级。 PAR_CUTOFF 要远大于 SEQ_CUTOFF （经验值：子任务至少几万元素、几十 μs 工作量）。粒度不够时并行版比串行版 更慢 。 坑二：fork 两个自己等着 浪费一整个线程。正确是 fork 一个、自己做另一个（Cilk 的 spawn/sync 语义）。 坑三：合并步是串行瓶颈 归并的 merge 是 O(n) 串行的。按 Amdahl 定律 ，顶层那一次 O(n) 合并单独就把加速比压在有限值上——哪怕下面全并行了。真正的并行归并排序必须用 并行 merge ：在一半里二分查找另一半的划分点，把合并也切开。 为什么用任务窃取而不是静态划分 分治的子任务大小 事先不可知 （快排划分可能极不均）。 任务窃取 让每个线程有自己的双端队列：从队尾取自己的（LIFO，刚生成的子任务数据还在 cache 里）、空闲时从别人队头偷（FIFO，偷到最大最老的任务，摊薄偷取开销）。这样不均衡的分治也能自动负载均衡。Cilk / TBB / ForkJoinPool / Rayon 都是这个模型。 用标准库时的三个前提 par_unseq 允许向量化交错，可调用体 不能有任何同步 （加锁、分配都可能死锁）； std::reduce 要求运算 可结合可交换 ——浮点加法不满足结合律，所以并行结果可能与 accumulate 不同，甚至每次运行都不同；libstdc++ 需要 -ltbb 。 高级 线上一个递归下降的配置解析器被一条请求打崩了，怎么定位和根治？ 症状（ SIGSEGV 、只在特定输入下复现、Debug 版可能不复现）指向 栈溢出 ，而且是被外部输入触发的——这是个 安全问题 ，不只是 bug。 ① 确认是爆栈 gdb 的 bt 里同一个 parseValue / parseObject 重复上万帧，或直接 Backtrace stopped ；出错地址紧邻 rsp 。用 -fsanitize=address 重跑，ASan 会明确报 stack-overflow （而不是 SEGV on unknown address ）。 ② 找触发输入 攻击载荷通常是 深度嵌套 ： [[[[[...]]]]] 或 {\"a\":{\"a\":{\"a\":...}}} 。几十 KB 的请求体就能造出几万层嵌套。 用 fuzzer 复现 ——libFuzzer/AFL 特别擅长找这类问题。 止血（当天） ：在解析入口加深度计数，超限直接拒绝请求： if (++depth_ > kMaxDepth) throw ParseError { \"nesting too deep\" }; 注意 限制要在解析时逐层检查 ，不能只检查输入长度——短输入也能造出深嵌套。 根治（这是真正的答案） 光加个上限只是把崩溃换成拒绝，还有三件事要做： ① 改成显式栈的迭代解析 （栈在堆上，深度只受内存限制且可优雅报错），成熟 JSON 库（simdjson、RapidJSON 的迭代模式）都是这么做的； ② 把「 所有处理不可信输入的递归必须有显式深度上限 」写进 review 清单，并搜一遍全仓库其他递归下降代码（协议解析、表达式求值、模板渲染、XML/YAML）—— 同类代码几乎一定不止一处 ； ③ 把 fuzzing 挂进 CI，专门喂嵌套结构的语料，让下次回归在合并前就被拦住。 再往上一层看 这类事故的共性是「 输入的某个维度没有上界，而代码里有个隐式的资源随它线性增长 」。递归深度 vs 栈只是其中一例；同构的问题还有：数组长度字段没校验 → 一次分配 4 GB、正则回溯 → ReDoS、解压缩比没限制 → zip bomb。 写「解析不可信输入」的代码时，应该逐个资源维度问「上界在哪，谁保证」。 ⬅️ 复杂度与分析 ➡️ 排序全景 🎯 二分查找 🧵 并发模式 📚 目录 🏠 主页 递归与分治 — 栈帧与尾递归 · 递归转迭代 · 记忆化 · 分治范式 · 快速选择与 Karatsuba · 并行分治 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/shortest_path_and_mst.html","t":"最短路与最小生成树","c":"算法与数据结构","s":[{"a":"","h":"","x":"最短路与最小生成树 · Dijkstra 正确性证明 / 负权为何失效 / Bellman-Ford 与负环 / Floyd 的 DP 视角 / 割性质 / 最大流最小割 Kavin Tech Notes ← 主页 目录 35 min ◐ 最短路与最小生成树 松弛：所有最短路算法的原子操作 与其他章节的关系 Dijkstra 算法与实现 正确性证明 负权为何失效 堆优化与延迟删除 Bellman-Ford 与 SPFA 为什么是 n−1 轮 负环检测 SPFA 与它的最坏 Floyd-Warshall DP 视角与循环顺序 传递闭包与最小环 A* 与启发式 可采纳与一致性 双向与分层预处理 最短路选型表 算法对照（交互） 最小生成树 割性质：两个算法的共同基础 Kruskal Prim 变形与推广 网络流入门 最大流最小割 Dinic 建模的三个套路 陷阱清单 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Shortest Path & MST · 2026 最短路与最小生成树 最短路共用同一个原子操作—— 松弛 ，差别只在顺序和轮数；MST 共用同一个定理—— 割性质 。抓住这两点，Dijkstra / Bellman-Ford / Floyd / Kruskal / Prim 就不再是五套互不相干的模板。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~35 min 前置 图遍历 · 堆 · 并查集 Dijkstra 正确性证明 负权与负环 Floyd 的 DP 视角 A* 可采纳性 割性质 最大流最小割"},{"a":"s1","h":"松弛：所有最短路算法的原子操作","x":"01 松弛：所有最短路算法的原子操作 四个算法的差别只在「按什么顺序松弛」 松弛（relax） 对边 (u, v, w)： if (dist[u] + w < dist[v]) dist[v] = dist[u] + w; 含义：「 经过 u 再到 v 」比当前已知的到 v 更短，就更新 松弛是所有最短路算法的原子操作：只比较「经 u 到 v」和「已知到 v」 成功：dist[v] 被改小 u dist=3 w=2 v 8 → 5 3+2 < 8，改成 5 失败：已知已经更好 u dist=3 w=2 v dist=4 3+2 ≮ 4，不动 一次松弛只处理一条边。算法之间的差别不在「怎么松弛」，而在「按什么顺序、松弛几轮」。 算法 松弛顺序 几轮 负权 BFS 按层 每边一次 边权必须相同 Dijkstra 当前 dist 最小的点 每点出队一次 否 Bellman-Ford 任意顺序扫全部边 n−1 轮 是 ，能测负环 Floyd 按允许的中间点集合 V 轮 是 （无负环） A* dist + 启发值最小 每点出队一次 否 松弛顺序决定一切 ：最短路有最优子结构（任意前缀也是最短路）。只要松弛边 (u,v) 时 dist[u] 已是终值，一次就够。 Dijkstra 用「每次取当前 dist 最小的点」保证这一点——但这个保证 完全依赖非负权 （§2.3）。 Bellman-Ford 不做任何顺序保证，用「重复 n−1 轮」覆盖所有可能的边数。 Floyd 换成另一个维度：按「允许经过哪些中间点」递增地松弛。四个算法不是四套互不相干的模板，而是「松弛顺序」这一个设计空间里的四个点。 松弛像不断更新「到某地的最优报价」。报价表初始全是 ∞，有人说「A 到 B 只要 50」，你就检查「我到 A 的报价 + 50」是否更便宜。 Dijkstra 是精明采购：先确认最便宜的城市（报价不可能再降），再用它更新邻居。 Bellman-Ford 把所有报价单反复过 n−1 遍，笨但稳。 Floyd 逐个开放中转城市：允许经 1 号？允许经 1、2 号？ 负权 等于「走过去还倒给你钱」——精明采购的前提「已确认最便宜」立刻被打破，这不是实现 bug，是算法本身在负权图上就是错的。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 图遍历与拓扑排序 —— BFS 是无权最短路；DAG 上按拓扑序松弛一遍即可 堆与 Top-K —— Dijkstra 的优先队列、延迟删除与索引堆 并查集与区间结构 —— Kruskal 完全建立在并查集上； find / unite 实现见该页 §2.2，不要在本页再写一份 动态规划 —— Floyd 和 Bellman-Ford 本质都是 DP 贪心与回溯 —— Dijkstra 和 Prim/Kruskal 都是贪心，正确性靠交换论证 图数据库 —— 大规模图上最短路的工程实现 本页把「按什么顺序松弛」和「割上的最小边」两条主线讲透；堆的 decrease-key / 配对堆细节、并查集的路径压缩证明，分别放在堆与并查集那两篇，避免把最短路页写成三篇的合订本。"},{"a":"s2","h":"Dijkstra","x":"02 Dijkstra 最常用，也最容易在负权上翻车"},{"a":"s2-1","h":"2.1 算法与实现","x":"2.1 算法与实现 堆优化 Dijkstra：小顶堆按 dist 弹出，过期条目用「d > dist[u]」跳过 S A B 1 4 2 优先队列（小顶堆）逐步变化 ① 压 (0,S) 堆：[(0,S)] ② 弹 S，松弛邻居 → 压 (1,A)(4,B) ③ 弹 A（dist 最小=1）→ 认定 A 已确定 ④ 再松弛 A 的出边；同一点可能再压入更小值 延迟删除：弹出时若 d > dist[u]，这是过期条目，跳过 不改堆内旧条目，直接压新的——代码短，稀疏图够用 前提：边权非负，弹出即终值 堆里存的是「候选距离」不是「已确定距离」。弹出最小值那一刻，才把该点标成终值，并用它去松弛邻居。 // 堆优化 Dijkstra + 延迟删除。正权图；负权见 §2.3 std::vector< long long > dijkstra ( int s, int n) { const long long INF = LLONG_MAX / 4 ; std::vector< long long > dist (n, INF); std::priority_queue<std::pair< long long , int >, std::vector<std::pair< long long , int >>, std::greater<>> pq; dist[s] = 0 ; pq. push ({ 0 , s}); while (!pq. empty ()) { auto [d, u] = pq. top (); pq. pop (); if (d > dist[u]) continue ; // 过期条目 for ( auto [v, w] : g[u]) { if (dist[u] + w < dist[v]) { dist[v] = dist[u] + w; pq. push ({dist[v], v}); // 不改旧条目，压新的 } } } return dist; } if (d > dist[u]) continue 省了不影响正确性（后续松弛都不会成功），但会白扫邻居，稠密图上明显变慢。 INF 不能用 LLONG_MAX ， dist[u]+w 会有符号溢出（UB）；用 LLONG_MAX/4 或先判 dist[u]==INF 。邻接表存 (邻居, 权重) ；稠密图（E≈V²）改用朴素 O(V²) 每轮扫最小，堆的常数反而更亏。"},{"a":"s2-2","h":"2.2 正确性证明","x":"2.2 正确性证明 不变式：弹出 u 时 dist[u] 已是终值。反证依赖「S 外第一跳之后的路径 ≥ 0」 已确定 S s p 未取出 x u 横切边 ≥ 0 ? 若 u 不是最短 更短路必先走出 S 第一跳出点 x： 真实(x) ≤ 真实(u) 这一步用到非负权 x 应比 u 更先弹出（dist[x] 更小），与「x 还在堆外」矛盾。面试能指出「哪一步用了非负性」，比只说「不能负权」深一层。 不变式 ：每次从堆取出的 u， dist[u] 就是最终最短距离。反证：若取出时不是最短，则存在更短路 s→…→x→…→u ，x 是路上第一个还没被取出的点。因为 边权非负 ，真实距离(x) ≤ 真实距离(u) < dist[u]——这一步用到「x 到 u 那段 ≥ 0」。x 之前的点都已取出，所以 dist[x] 已正确且 < dist[u]，堆应按 dist 先弹出 x，矛盾。∎ 负权让「那段 ≥ 0」不成立，整条论证链断掉。"},{"a":"s2-3","h":"2.3 负权为何失效","x":"2.3 负权为何失效 最小反例：三个节点，一条负权边——没有负环，Dijkstra 仍然错 S A B 1 4 −4 Dijkstra 的执行 取出 S → dist[A]=1, dist[B]=4 取出 A（dist=1 最小）→ 认定 A 已确定 取出 B → 4+(−4)=0 < 1，但 A 已出队 真实答案 S→B→A 的 dist[A] = 0 算法给出 1 —— 不是实现 bug 「取出即终值」依赖后续路径 ≥ 0。负权让这一步不成立，改 visited / 加常数都救不了。 加常数会额外惩罚边数多的路径；无负环也不能用；问题是过早确定，不是漏访问。 正确做法：有负权用 Bellman-Ford；多次全源查询用 Johnson（一次 Bellman-Ford 出势能，重赋权成非负后再跑 Dijkstra）。 三个常见的错误补救： 「所有边权加一个大常数变正」 ——错。这会额外惩罚边数多的路径：3 条边共 5 变成 5+3C，2 条边共 6 变成 6+2C，C 够大时后者反而更短。只有「所有路径边数相同」才成立。 「负权但无负环就能用」 ——错。上面的反例没有负环。 「加 visited 不重复访问」 ——错。问题不在漏访问，而在 过早确定 。 有负权就换 Bellman-Ford；需要全源且负权固定，用 Johnson ：加虚拟源跑一次 Bellman-Ford 得到势能 h，令 w'(u,v)=w(u,v)+h[u]−h[v] （三角不等式保证非负），再对每个源跑 Dijkstra，最后还原 dist(u,v)=dist'(u,v)−h[u]+h[v] 。同一对端点的所有路径被平移同一个常数，最短路的相对顺序不变。势能法的精髓是「只加只依赖节点的项」——A* 的一致性 h(u)≤w+h(v) 也是同一件事。"},{"a":"s2-4","h":"2.4 堆优化与延迟删除","x":"2.4 堆优化与延迟删除 实现 复杂度 适用 朴素扫最小 O(V²) 稠密图 反而最优 二叉堆 + 延迟删除 O(E log E) 默认 ，代码最短 索引堆 decrease-key O(E log V) 稠密且内存紧 斐波那契堆 O(E + V log V) 理论最优，实测差 桶队列 / Dial O(E + maxW·V) 边权是小范围整数 延迟删除不维护「节点→堆位置」，代码短一半、常数更小（索引堆每次元素移动都要额外写映射）。堆可能涨到 O(E)，所以弹出是 O(log E) 而非 O(log V)，复杂度写成 O(E log E)；稀疏图 E=O(V) 两者同阶。只在稠密且内存紧时才上索引堆。斐波那契堆的指针追逐和 consolidate 把 O(E+V log V) 的纸面优势吃掉，实践用配对堆或 d-ary 堆（见 堆与 Top-K ）。"},{"a":"s3","h":"Bellman-Ford 与 SPFA","x":"03 Bellman-Ford 与 SPFA 能处理负权，代价是不再贪心"},{"a":"s3-1","h":"3.1 为什么是 n−1 轮","x":"3.1 为什么是 n−1 轮 无负环时最短路最多 n−1 条边。第 k 轮覆盖「边数 ≤ k」的最短路 第 1 轮 s 的直接出边 第 2 轮 两条边的路径 第 n−1 轮 最长简单路径 归纳：s→…→u→v 有 k+1 条边 ⇒ 前缀 s→…→u 有 k 条边，第 k 轮后 dist[u] 已对 第 k+1 轮扫到 (u,v) 即得正确 dist[v]。实践加 if (!changed) break，很少跑满 边的扫描顺序只影响收敛速度，不影响正确性。按 BFS 序通常更快——这是 SPFA 的动机，但最坏仍是 O(V·E)。 为什么 n−1 轮足够 ：无负环时任意最短路最多 n−1 条边（否则重复访问节点成环；非负环可去掉，负环则无最短路）。归纳：第 k 轮结束后，所有「最多 k 条边的最短路」都已正确。k=1 是 s 的直接出边；k→k+1 时前缀 s→…→u 有 k 条边，dist[u] 已对，本轮扫到 (u,v) 即得 dist[v]。∎ 实践里 if (!changed) break 极重要——真实图很少跑满 n−1 轮。 std::vector< long long > bellman_ford ( int s, int n, const std::vector<Edge>& edges) { const long long INF = LLONG_MAX / 4 ; std::vector< long long > dist (n, INF); dist[s] = 0 ; for ( int round = 0 ; round < n - 1 ; ++round) { bool changed = false ; for ( auto & [u, v, w] : edges) if (dist[u] != INF && dist[u] + w < dist[v]) { dist[v] = dist[u] + w; changed = true ; } if (!changed) break ; } for ( auto & [u, v, w] : edges) // 第 n 轮：还能松弛 ⇒ 负环 if (dist[u] != INF && dist[u] + w < dist[v]) return {}; return dist; }"},{"a":"s3-2","h":"3.2 负环检测","x":"3.2 负环检测 负权 ≠ 负环。负环从源可达时，绕圈可使距离趋于 −∞ S A B C 2 1 1 −3 环权 1+1+(−3)=−1，可无限绕 第 n 轮还能松弛？ 否 → 无可达负环，dist 可用 是 → 最短路无定义，必须报错 只测 s 可达的环；测全图负环 把所有 dist 初始化为 0 （等价于超级源连所有点） 负权但无负环：最短路存在，用 Bellman-Ford。有负环但从 s 不可达：不影响 s 的答案。题目若可能有负环，先问清「有环时输出什么」。 负权 ≠ 负环 。负权无负环 → 最短路有定义，正常求。有负环且从 s 可达 → 最短路 无定义 （绕圈趋于 −∞），必须报错而不是返回某个值。有负环但 s 不可达 → 不影响。Bellman-Ford 第 n 轮只能测 s 可达的环；要测全图，把所有 dist 初始化为 0（超级源）。Floyd 看 d[i][i] < 0 能定位哪个点在环上。"},{"a":"s3-3","h":"3.3 SPFA 与它的最坏","x":"3.3 SPFA 与它的最坏 SPFA 只把「dist 刚变小」的点入队再松弛出边，平均快， 最坏仍是 O(V·E) 且可被构造出来 ——竞赛界说「关于 SPFA，它死了」，指出题人会专门造反例。选型因此很硬： 非负权一律 Dijkstra ，不要因为「平均更快」用 SPFA；有负权才用 SPFA 或 Bellman-Ford（没得选，但要知道最坏）；要全源且负权固定，走 Johnson（一次 Bellman-Ford 出势能 + V 次 Dijkstra），稀疏图上远优于 V 次 Bellman-Ford 的 O(V²E)。Johnson 的势能重赋权也出现在最小费用最大流里：增广后费用可能变负，用势能拉回非负就能继续用 Dijkstra 找增广路。A* 的一致性条件本质同样是重赋权后非负。"},{"a":"s4","h":"Floyd-Warshall","x":"04 Floyd-Warshall 五行代码解全源最短路，但循环顺序不能错"},{"a":"s4-1","h":"4.1 DP 视角与循环顺序","x":"4.1 DP 视角与循环顺序 d[k][i][j] = i 到 j、只允许经过编号 < k 的中间点。k 是阶段，必须最外层 k 未开放 只能走直达边 i —w→ j 等价于邻接矩阵 允许中转 k min(不经过 k, 经过 k：i→k→j) 滚动后可原地写 ❌ k 放最内层 对每个 (i,j) 试一遍 中转点 = 只松弛一次 两次以上中转就错 记法：DP 的阶段在最外层——背包的物品维、区间 DP 的长度维，同一条 k 维可省：到 k 的路径不会因「允许经过 k」再变短，所以原地更新安全 小图上把 k 放内层可能碰巧对，bug 极难查。V≤400～500 时 Floyd 常数极小（三层纯循环、cache 友好、易 SIMD），往往快过「跑 V 次 Dijkstra」——后者稠密图上是 O(V³ log V)。 状态 d[k][i][j] = i 到 j、只允许经过编号 < k 的中间点的最短距离。转移：不经过 k 则沿用 d[k][i][j]，经过则 d[k][i][k]+d[k][k][j]。k 维可滚动省掉，因为到 k 自己的路径不会因「允许经过 k」再变短，原地更新安全。把 k 放到内层，就变成「对每个点对试一遍中转」——只考虑一次跳，需要两次以上中转的路径全错。 写法 含义 对不对 for k / for i / for j 按阶段扩展允许的中转点 对 for i / for j / for k 每个点对试一次中转 错 （多跳路径漏） 转移 min(d[i][j], d[i][k]+d[k][j]) 不经过 / 经过 k"},{"a":"s4-2","h":"4.2 传递闭包与最小环","x":"4.2 传递闭包与最小环 变体 把 min/+ 换成 用途 传递闭包 或/与 可达性； bitset 可到 O(V³/64) 最小环 更新前统计 d[i][k]+d[k][j]+w(j,i) ≥3 个点的最小环 负环 跑完看 d[i][i] < 0 能定位哪个点在环上 最大瓶颈路 max(a, min(b,c)) 路径上最小边权最大 恰好 k 条边 (min,+) 矩阵快速幂 O(V³ log k) 骨架适用任何 (⊕, ⊗) 半环上的路径问题： (min,+) 最短路， (max,min) 瓶颈路（路径上最小边权最大）， (or,and) 可达性（bitset 可到 O(V³/64)）， (+,×) 计数或概率。识别出这个抽象，一份三重循环解四类题；同样的骨架配矩阵快速幂就是「恰好 k 步」。"},{"a":"s5","h":"A* 与启发式","x":"05 A* 与启发式 用「对未来的估计」引导搜索方向"},{"a":"s5-1","h":"5.1 可采纳与一致性","x":"5.1 可采纳与一致性 Dijkstra 按 g 排序，均匀扩散；A* 按 f=g+h，朝目标偏置。h≡0 即退回 Dijkstra Dijkstra · 水波 s t 四面八方，与 t 无关 A* · 可采纳 h 永不高估 s t 少扩 一致：h(u) ≤ w+h(v) ⇒ 出队即确定 只有可采纳也可能要 reopen 已关闭节点；几何启发（曼哈顿 / 欧氏）在对应移动规则下都一致。h 乘 ε>1（weighted A*）更快但放弃最优，导航里常见，面试里要说清。 可采纳 ： h(n) ≤ 真实距离(n→目标) ，永不高估 ⇒ 保证最优解。 一致 / 单调 ： h(u) ≤ w + h(v) （三角不等式）⇒ 每个节点只需扩展一次，像 Dijkstra 那样出队即确定。一致 ⇒ 可采纳，反之不成立。实践里几何启发都一致，很少需要实现 reopening。weighted A* 把 h 乘 ε>1，搜索快一个数量级，但只保证「解 ≤ ε×最优」——游戏和导航可接受 5% 路径差，不能一边用它一边宣称最优。 场景 h 一致？ 网格四连通 曼哈顿 |dx|+|dy| 是 网格八连通 对角 max(|dx|,|dy|) 是 任意方向 欧氏距离 是 h 乘系数 > 1 weighted A* 不可采纳"},{"a":"s5-2","h":"5.2 双向与分层预处理","x":"5.2 双向与分层预处理 双向搜索不是「相遇即最优」：相遇点不一定在最优路径上，要等两边已扩展节点的最小 dist 之和 ≥ 当前最优，才停。无权双向 BFS 没这问题（层数单调），从 BFS 迁到 Dijkstra 时最容易踩。全国路网约 10⁷ 节点，在线 Dijkstra 扫大半张图，单次查询到秒级、QPS 上万撑不住——关键观察是路网静态、查询海量，应该 预处理换查询速度 。Contraction Hierarchies 按重要性收缩节点并加捷径，查询只往更重要的方向走，全国微秒级，是 OSRM 一类引擎的核心；ALT（地标+三角不等式）实现简单、加速 10–100 倍；Hub Labeling 查询最快但标签占空间。实时路况会让捷径失效，用 Customizable CH 把「拓扑」和「权重」拆成两阶段预处理。"},{"a":"s6","h":"最短路选型表","x":"06 最短路选型表 三个问题定算法 三个问题定算法：边权 → 单源/全源 → 是否 DAG 边权什么样 全相同 → BFS 0/1 → 0-1 BFS 非负 → Dijkstra 有负 → Bellman-Ford 全源且 V 小 Floyd O(V³) V 大+负权 → Johnson 图是 DAG？ 拓扑序松弛一遍 O(V+E) 且可负权 DAG 最容易被忽略：任务依赖、状态转移、时间推进往往无环，不必上堆。 静态图 + 海量查询 → 预处理（CH）；边权小整数 → 桶队列。 拿到题先问这三句，再动手写模板。稠密非负图用朴素 O(V²) Dijkstra，别无脑上堆。DAG 那条最容易被忽略：任务依赖、状态转移、时间推进往往无环，「绕环变小」不可能发生，拓扑序松弛一遍就是最优，还天然支持负权——此时上 Dijkstra 或 Bellman-Ford 都是浪费。 情形 算法 复杂度 无权 / 0-1 BFS / 0-1 BFS O(V+E) 非负 · 稀疏 堆 Dijkstra O(E log V) 非负 · 稠密 朴素 Dijkstra O(V²) 负权单源 / 测负环 Bellman-Ford O(V·E) 全源 V≤500 Floyd O(V³) 全源 V 大 + 负权 Johnson O(V·E log V) DAG 拓扑序松弛 O(V+E) ，可负权 静态图海量查询 CH / Hub Labeling 预处理换查询 // DAG：按拓扑序松弛一遍即可，O(V+E)，天然支持负权（无环） for ( int u : topo) for ( auto [v, w] : g[u]) if (dist[u] != INF) dist[v] = std:: min (dist[v], dist[u] + w); 0-1 BFS 是 Dijkstra 在边权只有 0/1 时的退化：双端队列，权 0 压队首、权 1 压队尾，任意时刻队内最多两个距离值，O(V+E) 且不需要堆。识别信号是「某些移动免费、某些花费 1」。边权是小范围整数时用桶队列 / Dial，O(E + maxW·V)。共同思路：距离取值足够规整时，用结构代替通用优先队列。"},{"a":"s6-1","h":"6.1 算法对照（交互）","x":"6.1 算法对照（交互） 五个算法的适用条件与失效方式 点击算法名，看它的松弛策略、正确性依赖什么、以及在什么输入上会失效。 Dijkstra Bellman-Ford Floyd A* MST 两算法"},{"a":"s7","h":"最小生成树","x":"07 最小生成树 两个算法，一个定理"},{"a":"s7-1","h":"7.1 割性质：两个算法的共同基础","x":"7.1 割性质：两个算法的共同基础 割性质：横跨任意割的最小边一定属于某棵 MST 割 A B C 集合 S D E F 集合 V∖S 2 ← 最小 7 5 横切边 A-D(7)、B-D(5)、B-F(2) → 权 2 必在某棵 MST。交换论证：加入它形成的环上必另有横切边可换掉。 Prim 的割是「已选 vs 其余」；Kruskal 扫到连接两块的边时，更小的边都处理过，所以它就是这两块之间某个割的最小边。 割性质 ：把点集分成任意两个非空部分 S 与 V∖S，横跨这个割的最小边一定属于某棵 MST。交换论证：设 e 是该最小边，若某棵 MST T 不含 e，把 e 加入 T 形成环，环要从 S 出去还要回来，所以环上必另有横切边 f。由 e 的最小性 w(e)≤w(f)，用 e 换 f 得到的仍是生成树且不更重 ⇒ 也是 MST 且含 e。∎ Kruskal 和 Prim 都在找「某个割上的最小横切边」，只是割的取法不同。"},{"a":"s7-2","h":"7.2 Kruskal","x":"7.2 Kruskal Kruskal：排序后扫边，unite 成功收下，同根（已连通）丢弃 A B C D 1 ✓ 2 ✓ 3 ✓ 4 ✗ 成环 边按权 1 A–B unite ✓ 2 A–C unite ✓ 3 B–D unite ✓ 4 A–D 同根，丢 已 n−1 条，停 并查集在干什么 find 相同 = 已连通 unite 返回 false = 成环 实现见并查集页 路径压缩 + 按秩 复杂度被排序主导 unite / 路径压缩 / 按秩合并的实现见 并查集 §2.2 ；DSU 在 Kruskal 里的角色见该页 §3.3 。这里不重复写 DSU。 // 排序 + 并查集判环。DSU 实现见 union_find_and_range.html#s2-2 long long kruskal ( int n, std::vector<Edge> edges) { std:: sort (edges. begin (), edges. end (), []( const Edge& a, const Edge& b) { return a.w < b.w; }); DSU dsu (n); long long total = 0 ; int used = 0 ; for ( auto & [u, v, w] : edges) { if (dsu. unite (u, v)) { // false = 已同根，加了会成环 total += w; if (++used == n - 1 ) break ; } } return used == n - 1 ? total : - 1 ; // 边不足 n−1 → 不连通 } unite 返回 false 表示两端已同根，加上会成环，这正是 Kruskal 需要的布尔。必选边（已有光纤）先 unite 再扫候选；禁选边直接跳过。最终边数不足 n−1 时图不连通：返回最小生成森林还是报错，取决于题目，但 一定要检查 ，否则会把部分和当成答案。"},{"a":"s7-3","h":"7.3 Prim","x":"7.3 Prim Prim 切边：割 = 已选 S vs 其余，每次把横切最小边对端点收进 S 已选 S A B 已在树里 割 未选 C D 切边 2 ← 收 C 5 和 Dijkstra 的差别 Dijkstra 压 dist[u]+w 累计路径长 Prim 压单条边权 w 接入 S 的那条切边 代码极像 Dijkstra，写混堆里的键会得到完全错的树。稠密图用朴素 O(V²) Prim；不连通时要对每个分量各跑，Kruskal 则自然得到森林。边已排好序时 Kruskal 退化成 O(E·α)，几乎是线性。 Kruskal Prim 堆 Prim 朴素 复杂度 O(E log E) O(E log V) O(V²) 适合 稀疏 / 边已排序 稀疏 稠密 结构 排序 + 并查集 堆 无 不连通 最小生成森林 每分量各跑 同"},{"a":"s7-4","h":"7.4 变形与推广","x":"7.4 变形与推广 问题 解法 最大生成树 / 最小瓶颈树 降序或取反；MST 本身就是最小瓶颈生成树 次小生成树 / 唯一性 枚举非树边换树上路径最大边；等权替代则不唯一 必选 / 禁选边 必选先 unite；禁选跳过 有向「MST」 最小树形图 ，朱刘算法，Kruskal/Prim 不适用 只需连通指定点 Steiner 树 ， NP-hard 读题先确认两件事： 无向吗？要连通全部点还是指定的一部分？ 有向光纤是最小树形图，朱刘算法，Kruskal/Prim 完全不适用。「只需 k 个机房连通、可借道其他点」是 Steiner 树，一字之差从多项式变成 NP-hard。MST 得到的是树——任何一条边断了就分区，真实网络要冗余时模型已经不够。"},{"a":"s8","h":"网络流入门","x":"08 网络流入门 建模比算法难"},{"a":"s8-1","h":"8.1 最大流最小割","x":"8.1 最大流最小割 最大流 = 最小割容量。残量里从源可达的点集，就是一刀最小割 S A B C D T 4 5 3 ← 割 2 ← 割 最小割 = 3+2 = 5 最大流也是 5 流量必过每一刀 上界可达即定理 找瓶颈 = 算一次最大流 工程上「能传多少」和「瓶颈在哪」是同一个计算。求出最大流后，残量网络中从源可达的点集就是最小割。实践首选 Dinic（分层 + 多路增广 + 当前弧，当前弧不能在 DFS 内重置）；单位容量图 O(E√E)，所以能替代匈牙利做二分图匹配。 算法 复杂度 何时用 Edmonds-Karp O(V·E²) 小数据、好写 Dinic O(V²E)；单位容量 O(E√E) 默认 ，可替代匈牙利 费用流 依赖实现 带权匹配、调度"},{"a":"s8-2","h":"8.2 Dinic","x":"8.2 Dinic 分层图只沿「层数 +1」增广，保证每次走的都是残量网络里的最短路，且同一张层次图上可以增广多次。当前弧记住试到第几条出边，整张层次图的多路增广期间不能重置——去掉或在 DFS 内清掉都会退化、直接 TLE。反向边必须成对存储，用 e^1 更新残量 cap[e]-=f; cap[e^1]+=f ；无向边两个方向都给满容量，不是一边 0。每轮 BFS 都要重置层数，到不了汇就结束。"},{"a":"s8-3","h":"8.3 建模的三个套路","x":"8.3 建模的三个套路 套路 做法 解决什么 超级源汇 虚拟 S/T 连所有起终点 多源多汇 拆点 v_in → v_out 容量 = 点容量 点有上限 二分图 S→左→右→T 容量 1 最大匹配 = 最大流 最小割 = 二选一 选 A/B 的代价建成割 项目选择、闭合子图 算法几十行可以背，难的是把应用题翻译成管道：「每个工人最多做 3 个任务」→ 拆点，中间边容量 3；多仓多店 → 超级源汇；「选 A 还是 B 各有代价」→ 建成割。König（二分图最小点覆盖 = 最大匹配）是最大流最小割的特例；最大独立集 = V − 最大匹配（二分图）。看到「最少选多少个覆盖 / 最多选多少个互不冲突」就该想到这一族。匹配细节见 图遍历与拓扑排序 。"},{"a":"s9","h":"陷阱清单","x":"09 陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 负权图上用 Dijkstra 答案偏大，小图可能碰巧对 换 Bellman-Ford / SPFA； 加常数变正是错的 Floyd 的 k 不在最外层 只做了一次中转，多次中转的路径错 k 是 DP 阶段，必须最外层 INF 用 LLONG_MAX dist[u] + w 溢出（UB） 用 LLONG_MAX/4 ，或先判 dist[u] == INF Dijkstra 漏了 d > dist[u] 检查 正确但慢（白做大量无用遍历） 补上延迟删除的跳过 Prim 压入累计距离 算出的不是 MST Prim 压 单条边权 ，Dijkstra 压 累计距离 MST 忘检查连通性 不连通时返回错误的部分和 检查选中边数 == n−1 双向 Dijkstra「相遇即返回」 不是最优解 要等「两边最小 dist 之和 ≥ 当前最优」 A* 用了不可采纳的 h 找到的不是最优解 确认 h ≤ 真实距离 ；weighted A* 要明说放弃最优性 忽略 DAG 的特殊性 在 DAG 上用 Dijkstra 或 Bellman-Ford 拓扑序松弛一遍即可，O(V+E) 且支持负权 SPFA 用在非负权图上 可能被构造数据卡到 O(V·E) 非负权一律 Dijkstra 网络流的反向边处理错 流量算错 成对存储用 e^1 ；无向边两个方向都给满容量 Dinic 少了当前弧优化 TLE 当前弧不能在 DFS 内重置 把有向图当 MST 问题 Kruskal/Prim 完全不适用 那是最小树形图，用朱刘算法 负权用 Dijkstra、Floyd 的 k 不在最外层，都会在小数据上碰巧对——这让它们特别难查。防线是动手前问「边权可能为负吗？图是 DAG 吗？」，并把答案写成注释；再用三个节点一条负边对拍。溢出用 -fsanitize=undefined 抓，见 Sanitizers 实战 。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 写出堆优化的 Dijkstra，并说明「延迟删除」那一行的作用。 std::vector< long long > dijkstra ( int s, int n) { const long long INF = LLONG_MAX / 4 ; // ← 不用 LLONG_MAX，防溢出 std::vector< long long > dist (n, INF); std::priority_queue<std::pair< long long , int >, std::vector<std::pair< long long , int >>, std::greater<>> pq; dist[s] = 0 ; pq. push ({ 0 , s}); while (!pq. empty ()) { auto [d, u] = pq. top (); pq. pop (); if (d > dist[u]) continue ; // ← 延迟删除 for ( auto [v, w] : g[u]) if (dist[u] + w < dist[v]) { dist[v] = dist[u] + w; pq. push ({dist[v], v}); } } return dist; } 延迟删除是什么 堆不支持修改已有元素的优先级（decrease-key）。所以不改旧条目， 直接压入新的 (更小距离, v) ——同一节点可能在堆里有多个条目。 if (d > dist[u]) continue; 跳过那些 已经被更好路径更新过的过期条目 。 省掉这一行会怎样 不影响正确性 （后续松弛都不会成功），但会白做大量无用的邻居遍历——稠密图上明显变慢。 INF 的选择 不能用 LLONG_MAX —— dist[u] + w 会 有符号溢出（UB） 。用 LLONG_MAX / 4 ，或在松弛前先判 dist[u] == INF 。 为什么延迟删除是实践首选 对比索引堆（真 decrease-key）：延迟删除 不需要维护「节点→堆内位置」映射 ，代码短一半、常数更小（索引堆每次元素移动都要额外写映射表）。 堆可能膨胀到 O(E)，但稀疏图上 E = O(V)，无所谓。 只在稠密图 + 内存紧张时才值得上索引堆。 基础 Dijkstra 为什么不能处理负权边？给一个反例。 因为它的不变式 「每次从堆取出的节点，其 dist 已是最终值」 依赖非负权。 最小反例 S →(1) A S →(4) B B →(−4) A Dijkstra：取出 S → dist[A]=1, dist[B]=4 ；取出 A（dist=1 最小）→ 认定已确定 ；取出 B，松弛 B→A 得 4−4=0 < 1 ——但 A 已经出队， 不会再被处理 。 真实 dist[A] = 0 ，Dijkstra 给出 1。 注意这个图没有负环。 证明的哪一步用到了非负性 反证时需要「x 到 u 的那段路径长度 ≥ 0」，从而推出 真实距离(x) ≤ 真实距离(u) 。负权让这一步不成立，整个论证链断掉。 能指出这一点比只说「不能处理负权」深一层。 三个常见的错误补救 ①「所有边权加一个大常数」 ——错。这会 额外惩罚边数多的路径 ：3 条边共 5 变成 5+3C，2 条边共 6 变成 6+2C，C 大时后者反而更短。只有「所有路径边数相同」才成立。 ②「无负环就能用」 ——错，上面反例就没负环。 ③「加 visited 不重复访问」 ——错，问题不在重复访问而在 过早确定 。 正确做法 单源 → Bellman-Ford / SPFA （O(V·E)）。 需要全源且负权固定 → Johnson 算法 ：先用一次 Bellman-Ford 算出一组「势能」把边权重新赋成非负，再对每个源跑 Dijkstra。 基础 Bellman-Ford 为什么松弛 n−1 轮？怎么检测负环？ 因为 无负环时任意最短路最多 n−1 条边 （否则会重复访问节点形成环；非负环可去掉，负环则无最短路）。 归纳证明 第 k 轮结束后，所有「最多 k 条边的最短路」都已正确算出。 · k=1：从 s 直接出发的边，第一轮必被松弛。 · k→k+1：最短路 s→…→u→v 有 k+1 条边 ⟹ s→…→u 有 k 条边，归纳假设 dist[u] 已正确 ⟹ 第 k+1 轮松弛 (u,v) 得到正确的 dist[v] 。∎ 负环检测：跑第 n 轮 n−1 轮后 再跑一轮 ，如果还能松弛成功 → 存在负环（因为无负环时 n−1 轮已收敛）。 for ( auto & [u,v,w] : edges) if (dist[u] != INF && dist[u] + w < dist[v]) return {}; // 有负环 「有负环」和「有负权」是两件事 负权但无负环 → 最短路存在，正常求。 有负环且从 s 可达 → 最短路 无定义 （绕环可趋于 −∞），必须报错而不是返回值。 有负环但 s 不可达 → 不影响，正常返回。 题目可能有负环时，必须先问清「有负环时输出什么」。 两个实用细节 ① 加 if (!changed) break; ——真实图通常几轮就收敛，很少跑满 n−1 轮。 ② 要检测 任意 负环（不只是 s 可达的），把所有 dist 初始化为 0（等价于加超级源连所有点）。 基础 Floyd 的三层循环为什么 k 必须在最外层？ 因为 k 是 DP 的「阶段」维度 ，而 i、j 是状态维度。 状态定义 d[k][i][j] = 从 i 到 j、只允许经过编号 < k 的中间点的最短距离 转移 d[k+1][i][j] = min( d[k][i][j], // 不经过 k d[k][i][k] + d[k][k][j] ) // 经过 k k 这一维可以滚动省掉（因为 d[k][i][k] == d[k+1][i][k] ，原地更新安全），于是变成熟悉的三层循环： for ( int k = 0 ; k < n; ++k) // ← 阶段，必须最外层 for ( int i = 0 ; i < n; ++i) for ( int j = 0 ; j < n; ++j) d[i][j] = min(d[i][j], d[i][k] + d[k][j]); k 放内层会怎样 for (i) for (j) for (k) // ❌ 这变成「对每个 (i,j) 试所有中转点」， 只考虑了一次中转 。需要两次以上中转的路径就算错。 而且它在小图上可能碰巧对 ——这让 bug 特别难查。 记法 「DP 的阶段必须在最外层」 。这条规律对所有类似 DP 都成立：背包的物品维、区间 DP 的长度维、状压 DP 的状态维。 Floyd 骨架的推广 它适用于任何「(⊕, ⊗) 半环」上的路径问题——只需换掉 min 和 +： · (min, +) → 最短路 · (max, min) → 最大瓶颈路 （路径上最小边权最大） · (or, and) → 可达性（传递闭包，可用 bitset 优化到 O(V³/64)） · (+, ×) → 路径计数 / 概率 一份代码解四类问题。 什么时候该用 Floyd 它的 O(V³) 常数 极小 （三层纯循环、cache 友好、易并行），而且代码只五行。 V ≤ 400~500 时 Floyd 往往比「跑 V 次 Dijkstra」更快 ——后者在稠密图上是 O(V³ log V)。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 Kruskal 和 Prim 的共同正确性基础是什么？两者怎么选？ 共同基础是 割性质（cut property） ：把节点集分成任意两个非空部分 S 和 V∖S，则 横跨这个割的最小边一定属于某棵 MST 。 证明（交换论证） 设 e 是横跨割的最小边，假设某棵 MST T 不含 e。把 e 加入 T 形成一个环，这个环必然 至少还有另一条边 f 也横跨这个割 （要出去还要回来）。由 e 的最小性 w(e) ≤ w(f) ，用 e 换 f 得到 T'，仍是生成树且 w(T') ≤ w(T) ⟹ T' 也是 MST 且含 e。∎ 两个算法都在找「横跨某个割的最小边」 Prim ：割是「已选节点集 vs 其余」，每次选横跨它的最小边。 Kruskal ：按权排序扫边，当前边若连接两个不同连通块，它就是横跨「这两块之间某个割」的最小边（更小的边都已处理过）。 Kruskal Prim（堆） Prim（朴素） 复杂度 O(E log E) O(E log V) O(V²) 适合 稀疏图 稀疏图 稠密图 不连通图 自然得到最小生成森林 要对每个分量各跑 同 边已排序 O(E·α)，极快 无优势 无优势 Prim 和 Dijkstra 的关键区别 代码极像，但压入堆的东西不同： Dijkstra：pq.push({ dist[u] + w , v}); // 累计距离 Prim ：pq.push({ w , v}); // 单条边权 因为 Dijkstra 求「源到 v 的路径总长」，Prim 求「把 v 接入已选集合的 那条边 的代价」。 写混了结果完全错 ，而两份代码相似度极高。 别忘检查连通性 选中边数 < n−1 说明图不连通，不存在生成树。 中级 A* 的启发函数需要满足什么条件？「可采纳」和「一致」有什么区别？ Dijkstra：按 g(n) 排序 g = 起点到 n 的已知距离 A* ：按 f(n) = g(n) + h(n) h = n 到终点的 估计 距离 h ≡ 0 → 退化成 Dijkstra；h 越准 → 搜索范围越小 可采纳（admissible） h(n) ≤ 真实距离(n → 目标) ，即 永不高估 。 ⟹ 保证找到 最优解 。 一致 / 单调（consistent） 对任意边 (u,v,w)： h(u) ≤ w + h(v) （类似三角不等式）。 ⟹ 保证 每个节点只需扩展一次 （不需重新打开已关闭的节点）。 一致 ⟹ 可采纳 （反之不成立）。 实践含义 只有「可采纳」时 A* 仍找到最优解，但可能需要 重新打开已关闭节点 （reopening），实现更复杂。 「一致」时就能像 Dijkstra 那样「出队即确定」。 常见的几何启发（曼哈顿、欧氏、对角距离）在对应移动规则下都是一致的 ，所以实践中很少需要考虑 reopening。 weighted A*：主动放弃最优性 把 h 乘上 ε > 1，搜索快得多，但只保证「解 ≤ ε × 最优解」。 游戏寻路和地图导航常这么做（用户感知不到 5% 的路径差，但 200ms → 20ms 很明显）。 但必须明确知道自己放弃了最优性 ——不能一边用 weighted A* 一边宣称最优。 双向 A*/Dijkstra 的终止条件坑 不是「两边相遇就得到最优解」 。正确条件是「两边已扩展节点的最小 dist 之和 ≥ 当前找到的最优路径长度」。 相遇点不一定在最优路径上——可能有一条经过别处的更短路还没被发现。标准做法是相遇后 继续搜索并维护当前最优 ，直到条件满足。 无权图的双向 BFS 没这个问题（层数单调），所以从 BFS 迁移到 Dijkstra 时容易踩。 中级 什么情况下不需要 Dijkstra 就能求最短路？ 四种情况，按「被忽略的频率」排序： ① 图是 DAG（最容易被忽略） 按拓扑序松弛一遍即可，O(V+E) ，而且 天然支持负权 （没有环，「绕环变小」不可能发生）。 很多问题的图其实是 DAG：任务依赖、状态转移、时间推进、分层图。 此时用 Dijkstra 是多余的，用 Bellman-Ford 更是浪费。 ② 边权全相同（无权图） BFS ，O(V+E)。BFS 按层扩展，节点第一次被访问时走的必然是最短路。 ③ 边权只有 0 和 1 0-1 BFS ：双端队列，权 0 压队首、权 1 压队尾，O(V+E)。 双端队列在任意时刻最多含两个距离值（d 和 d+1）且队首最小——正好复现 Dijkstra 的性质而不需要堆。 识别信号： 「某些移动免费、某些花费 1」 。 ④ 边权是小范围整数 桶队列 / dial's algorithm ：每个距离一个桶，O(V + E + maxW·V)。 共同思路 这四种都是 用「结构」代替「堆」来提供有序性 ——只要距离值的取值范围足够小或足够规整，就不需要通用的优先队列。 而 DAG 那一条更本质 ：它用「问题本身的偏序」代替了「按距离排序」。 拿到最短路问题的第一个动作应该是问「这图是 DAG 吗」。 中级 最大流最小割定理说了什么？它有什么工程价值？ 网络的最大流量 = 最小割容量 （把源和汇分开所需删除的最小边容量之和）。 直觉 流量必须全部通过每一个割 ⟹ 最大流 ≤ 任意割的容量 。定理说这个上界是 可达 的。 工程价值：「能传多少」和「瓶颈在哪」是同一个问题 求出最大流后， 残量网络中从源点可达的点集 就给出了最小割——这让「找瓶颈」从直觉判断变成一次可执行的计算。 应用：网络容量规划（哪条链路要扩容）、供应链瓶颈分析、图像分割（graph cut）。 它统一了一批看似无关的问题 König 定理 （二分图最小点覆盖 = 最大匹配）是它在二分图上的特例。 最大独立集 = V − 最大匹配 （二分图）。 最小路径覆盖（DAG）= V − 最大匹配 。 看到「最少选多少个覆盖」「最多选多少个互不冲突的」就该想到这一族。 算法选择 实践首选 Dinic （分层 + 多路增广 + 当前弧优化 ），实测远好于理论界 O(V²E)； 单位容量图上是 O(E√E) ，这是它能替代匈牙利算法做二分图匹配的原因。 当前弧优化 不是可选的 ——去掉后复杂度会退化，而且它不能在 DFS 内部重置。 难点在建模不在算法 算法几十行可以背，难的是「怎么把题目翻译成管道图」。三个套路： 超级源汇 ：多源多汇 → 单源单汇。 拆点 ： v_in → v_out ，中间边容量 = 点容量（用于「每个中转站最多过 k 个」）。 最小割 = 二选一代价 ：把「选 A 还是 B」建成割（最大权闭合子图、项目选择）。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 要给一个全国路网做导航，直接跑 Dijkstra 行吗？工业界怎么做？ 不行。全国路网约 10⁷ 量级的节点 ，一次 Dijkstra 要扫遍大半张图——单次查询几百毫秒到秒级，而导航服务的 QPS 可能上万。 ① 先算清瓶颈 Dijkstra 的问题不是复杂度不对（O(E log V) 已经很好），而是 常数太大且无法复用 ：每次查询都从零开始扫。 关键观察： 路网是静态的 （几周才更新一次），而查询是海量的 ⟹ 应该用预处理换查询速度 。 ② 主流方案：Contraction Hierarchies（CH） 按「重要性」逐个 收缩 节点：删掉一个节点时，如果它的邻居之间的最短路必须经过它，就 加一条捷径边（shortcut） 补上。 查询时做 双向 Dijkstra，且只往「重要性更高」的方向走 ——搜索范围极小。 效果：全国路网单次查询能到微秒级 ，预处理几分钟到几十分钟。这是 OSRM 等开源导航引擎的核心。 ③ 其他方案 ALT （A* + Landmarks + 三角不等式）：预选若干地标，预计算所有点到地标的距离，用三角不等式得到比直线距离强得多的 h。实现简单，加速 10–100 倍。 Hub Labeling ：每点预存一组「枢纽 + 距离」，查询时求交集—— 查询最快 （几乎 O(1)），但存储开销大（每点几十到几百个标签）。 分层图 （高速/国道/市内分层）：工业界早期做法，直观但加速比不如 CH。 ④ 但真实导航还有 CH 处理不了的东西 实时路况 ：边权动态变化 → CH 的捷径边失效。解法是「Customizable CH」（把预处理分成「拓扑相关」和「权重相关」两阶段，后者可以几秒内重算）。 转向限制 （不能左转、单行道）→ 要在 边 而不是点上建图，或用「拆点」表达。 多目标 （最快 vs 最短 vs 最少收费）→ 每个目标一套预处理，或用多目标 Pareto 最短路。 路线偏好、避开拥堵、途经点 → 都会破坏纯粹的最短路模型。 ⑤ 可迁移的判断 「图是静态的 + 查询是海量的」→ 预处理换查询速度 。这个判断适用于所有类似场景：搜索引擎的倒排索引、数据库的索引、编译期计算。 反过来， 如果图会频繁变化，重预处理的成本可能超过收益 ——此时要么增量更新预处理结果，要么退回在线算法。 动手前先量「更新频率 vs 查询频率」。 高级 「有负权但要多次查询全源最短路」怎么做？ 朴素做法有两条，各有问题：跑 V 次 Bellman-Ford 是 O(V²E)；用 Floyd 是 O(V³)——V 大时都不行。 Johnson 算法：重赋权 + V 次 Dijkstra 核心思想是 把负权图变成等价的非负权图 ，从而能用 Dijkstra。 步骤： ① 加一个 虚拟源点 q ，向所有点连权 0 的边； ② 用 Bellman-Ford 从 q 求出 h[v] （顺带检测负环——有负环则无解）； ③ 重赋权 ： w'(u,v) = w(u,v) + h[u] − h[v] ； ④ 对每个源跑 Dijkstra （在 w' 上）； ⑤ 还原 ： 真实距离(u,v) = dist'(u,v) − h[u] + h[v] 。 为什么 w' 非负 Bellman-Ford 的结果满足三角不等式 h[v] ≤ h[u] + w(u,v) （否则还能继续松弛），移项即得 w(u,v) + h[u] − h[v] ≥ 0 。∎ 为什么最短路的「相对顺序」不变 对任意路径 p: v₀ → v₁ → … → v_k ： w'(p) = Σ [w(vᵢ,vᵢ₊₁) + h[vᵢ] − h[vᵢ₊₁]] = w(p) + h[v₀] − h[v_k] ← 中间项全部抵消 同一对 (起点, 终点) 的所有路径，w' 长度都比 w 长度多出同一个常数 h[v₀] − h[v_k] ⟹ 最短路不变。这就是「势能法重赋权」的精髓。 复杂度 O(V·E log V) （一次 Bellman-Ford + V 次 Dijkstra），稀疏图上远优于 Floyd 的 O(V³)。 稠密图（E ≈ V²）上则和 Floyd 相当甚至更差，此时直接用 Floyd（代码五行、常数小）。 「势能重赋权」是个通用技巧 同样的思路用在 最小费用最大流 里：每轮增广后费用可能变负，用势能重赋权后就能用 Dijkstra 代替 SPFA 找增广路（Primal-Dual 方法），复杂度更好。 核心是「加一个只依赖节点的项，让所有路径同步偏移，从而不改变相对优劣」 ——这个想法也出现在 A* 的启发函数（一致性条件本质就是重赋权后非负）里。 高级 一个「最少加多少条网线让所有机房连通，且成本最低」的需求，怎么建模？ 看起来是 MST，但 读题要确认四件事 ——它们决定了这到底是不是 MST 问题。 ① 已有部分连接吗？ 如果某些机房之间已经连通，把这些边 先并入并查集 （代价 0），然后对剩余候选边跑 Kruskal。 这是「必选边」的标准处理。 ② 要连通「所有」还是「指定的一部分」？ 所有点 → MST。 只需连通指定的 k 个点、可以借道其他点 → 那是 Steiner 树，NP-hard （k 小时用状压 DP）。 这一字之差决定了问题的复杂度类，必须问清。 ③ 图是无向的吗？ 无向 → Kruskal/Prim。 有向（比如「单向光纤」）→ 那是最小树形图，要用朱刘算法（Edmonds），Kruskal/Prim 完全不适用。 ④ 目标是「总成本最小」还是「最贵的那条最便宜」？ 总成本最小 → MST。 「最贵的那条最便宜」→ 最小瓶颈生成树 ——好消息是 MST 恰好也是最小瓶颈生成树 （MST 上任意两点路径的最大边权最小），所以同一个算法。 建模确认后：Kruskal + 并查集 // 已有连接先并入 for ( auto & [u,v] : existing) dsu. unite (u, v); // 候选边按成本排序后扫 for ( auto & [u,v,w] : sorted_candidates) if (dsu. unite (u, v)) { total += w; ++used; } // 检查连通性：used + existing_merges == n-1 为什么选 Kruskal 而非 Prim： 能自然处理「已有边」和「图可能不连通」 ，而且候选边通常是稀疏的（不是所有机房两两之间都能拉线）。 工程上还要考虑的（MST 模型覆盖不到的） 可靠性 ：MST 是树 → 任何一条边断了就分区 。真实网络要求冗余，实际是「求一个 2-边连通的最小子图」（更难的问题），或者在 MST 基础上加环。 容量 ：网线有带宽上限 → 变成网络流或多商品流问题。 施工约束 ：某些路径不能施工、成本随时间变化。 面试时指出「MST 得到的是树，没有任何冗余」这一点，比只写出 Kruskal 更有价值。 高级 为什么说网络流「建模比算法难」？举一个建模的例子。 因为算法（Dinic）是 固定的几十行，可以背 ；而建模是 把一道自然语言描述的问题翻译成管道图 ，变化无穷且没有机械方法。 三个基本套路 超级源汇 ：多源多汇 → 加虚拟 S 连所有起点、虚拟 T 连所有终点。 拆点 ：点有容量限制时，把 v 拆成 v_in → v_out ，中间边容量 = 点容量。 最小割 = 二选一 ：「选 A 还是选 B，各有代价」建成一个割，割掉哪边就是选哪边。 建模例子：项目选择 / 最大权闭合子图 题 ：有若干项目（有收益）和若干设备（有成本），每个项目需要若干设备，设备可共用。选一些项目使「总收益 − 总成本」最大。 建模 ： · S → 每个项目，容量 = 该项目的收益 · 每个设备 → T，容量 = 该设备的成本 · 项目 → 它需要的设备，容量 = ∞ 答案 = 总收益 − 最小割 。 为什么对 ：容量 ∞ 的边 不可能被割 ，所以任何有限割要么割掉「S→项目」（= 放弃该项目的收益），要么割掉「设备→T」（= 支付该设备的成本）。最小割就是「最小的放弃+支付」，于是总收益减去它就是最大净收益。 这个例子体现的建模思路 把「二选一的代价」编码成边容量，把「必须同时选」编码成 ∞ 边。 然后「最优决策」就变成「最小割」。 同族问题：图像分割（前景/背景二选一，相邻像素倾向同类 → ∞ 或大权边）、文理分科、最大密度子图。 为什么这类题的难度分布极不均 想到建模 → 剩下是套模板（10 分钟）；想不到 → 完全无从下手。 没有中间状态。 所以练法不是刷更多题，而是 积累「建模模式库」 ：看到「点有容量」想拆点，看到「二选一有代价」想最小割，看到「一对一分配」想二分图匹配，看到「每人做几件事有上限」想容量限制。 面试里网络流出现频率不高（建模难、代码长），但知道「最大流最小割定理」和「二分图匹配 = 最大流」很有价值——因为 König 定理这类结论会以「最少选多少个点覆盖所有边」的形式出现。 ⬅️ 图遍历与拓扑排序 ➡️ 动态规划 🔗 并查集与区间结构 🔺 堆与 Top-K 📚 目录 🏠 主页 最短路与最小生成树 — Dijkstra 正确性证明 · 负权与负环 · Floyd 的 DP 视角 · A* 可采纳性 · 割性质 · 最大流最小割 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/sorting_deep_dive.html","t":"排序全景","c":"算法与数据结构","s":[{"a":"","h":"","x":"排序全景 · Ω(n log n) 下界 / introsort / 稳定性 / 外部排序 Kavin Tech Notes ← 主页 目录 35 min ◐ 排序全景 排序是算法思想的博物馆 与其他章节的关系 三个 O(n²) 冒泡与选择 插入排序凭什么最快 希尔排序：唯一的悬案 归并排序 两种递归方向 稳定性从哪里来 原地归并的代价 快速排序 分区三法 pivot 怎么选 退化与爆栈 堆排序：理论完美、实测最慢 Ω(n log n) 下界证明 决策树模型 证明 突破下界 计数排序 基数排序 桶排序 std::sort：introsort introsort 三段结构 stable_sort 与其他兄弟 严格弱序：真实事故 工程场景 外部排序与多路归并 TimSort 与 pdqsort 并行排序 选型决策表 八大排序对照（交互） 面试题集（12 道） 12.1 基础 12.2 中级 12.3 高级 Algorithms · Sorting · 2026 排序全景 排序是 算法设计思想的博物馆 ：分治、堆、增量、桶、下界、自适应、多算法切换都能在这里找到最干净的实例。本篇用图讲清比较下界、快排退化、归并稳定、堆下标和 introsort；代码只留 Hoare 分区、堆下沉和计数数组，不堆五种完整实现。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~35 min 前置 复杂度 · 递归与分治 决策树下界 introsort 三段 严格弱序 UB 外部排序 TimSort / pdqsort"},{"a":"s1","h":"排序是算法思想的博物馆","x":"01 排序是算法思想的博物馆 为什么一个「已经被库解决」的问题值得学透 生产里几乎永远写 std::sort 。学排序不是为了以后手写八种实现，而是 它把几乎所有通用算法技巧都装进一个足够小的问题 ：分治、堆、增量、分桶、下界证明、自适应、多算法切换、外部内存——每一种都能在排序里找到最干净的实例。 技巧 在排序里的化身 在别处的样子 分治 归并、快排 最近点对、Karatsuba、线段树 堆（优先队列） 堆排序 Dijkstra、Top-K、定时器 增量构造 插入排序 在线算法、增量凸包 值域分桶 计数、基数、桶排序 哈希、直方图、LSD 字典树 下界证明 决策树 → Ω(n log n) 不可能性结论的通用方法 自适应 TimSort 识别天然有序段 自适应压缩、JIT profile 优化 多算法切换 introsort（快排/堆排/插排） nth_element 、GMP 大数乘、BLAS 外部内存算法 多路归并 LSM compaction、MapReduce shuffle 学排序像 练几道基础菜 ：控火、调味是在这里练出来的，没人指望你以后天天手写快排。只会背「快排平均 O(n log n)」却讲不出 std::sort 为何小区间切插入、深度超限为何切堆排，就像只会背菜谱不会控火——熟菜能应付，新菜不行。 组织： §2–§6 机制与边界 （O(n²) → 希尔 → 归并稳定 → 快排分区与退化 → 堆下标）， §7–§8 下界与如何绕过 （决策树 → 计数/基数）， §9–§11 工业实现 （introsort、外部排序、选型）。代码只留分区、下沉、计数数组三块，其余用图和表。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 复杂度与分析 —— Ω 记号、主定理、cache 局部性都在那里 递归与分治 —— 归并/快排的分治骨架、cutoff 与并行分治 二分查找 —— 排序的下游用途，以及 lower_bound 的实现 STL 算法库 —— sort / stable_sort / partial_sort / nth_element 的接口层对照 STL 容器深度剖析 —— priority_queue 与 heap 算法族 Spark 内核 —— shuffle 阶段的外部排序与溢写"},{"a":"s2","h":"三个 O(n²)","x":"02 三个 O(n²) 其中两个只有教学价值，第三个是工业级实现的一部分"},{"a":"s2-1","h":"2.1 冒泡与选择","x":"2.1 冒泡与选择 两个 O(n²) 只值记住性质，不必各写一份实现： 比较 交换 稳定 记住什么 冒泡 最坏 Θ(n²)，最好 Θ(n) = 逆序对数 是 每次相邻交换消恰好 1 个逆序对；逆序对 = 排好所需的最少相邻交换次数 选择 固定 Θ(n²)，不自适应 O(n) 否 元素极大、移动极贵而比较极便宜时才有用（几 MB 结构体又不能间接排序） 稳定性谁破坏：看会不会做「长距离交换」 输入 [2a, 2b, 1] —— 两个 2 原本 2a 在前 2a 2b 1 选择：min=1 与 2a 对换 1 2b 2a 2a、2b 相对顺序反了 → 不稳定 插入 / 冒泡 / 归并 只做相邻后移或按序追加 相等时停在右侧（用 > 不用 ≥） → 相对顺序不变，稳定 快排 / 堆排 / 选择：跨区交换 → 相等元素顺序被打乱 凡是长距离交换的排序基本都不稳定（选择、快排、堆排、introsort）。想稳定就只能相邻移动或按序追加（冒泡、插入、归并、计数、基数、TimSort）。"},{"a":"s2-2","h":"2.2 插入排序凭什么最快","x":"2.2 插入排序凭什么最快 渐进同样 Θ(n²)，却是 唯一写进工业实现的 O(n²) 算法 ：libstdc++ 的 std::sort 、Java 的 Arrays.sort 、Python 的 TimSort 都在小区间用它。真实复杂度是 Θ(n + I) （I = 逆序对数），不是固定 n²——已有序时内层一次都不进，就是线性。 近乎有序：已排好的前缀 + 少量新键，每个 key 只后移几格 1 3 4 7 9 2 新键 1 2 3 4 7 9 内层：a[j] > key 才后移（1 次写，不是交换 3 次） 相等就停 → 稳定；写成 ≥ 会翻序 I = 2 的逆序 → 这一轮 Θ(1) 次移动 已排好再插入少量新元素 ≈ 线性 introsort 收尾：块间有序后 I = O(n·cutoff) 后移而非交换、只碰相邻、分支可预测——所以小 n 和近乎有序都极快。内层必须是 a[j] > key ，写成 ≥ 既丢掉稳定性又白做移动。 相对冒泡：后移是 1 次写而不是交换 3 次；内层通常连续进入或不进入，分支可预测；只碰相邻，全程 cache 命中；稳定、原地、可在线接收。这正是 introsort 收尾能成立的原因：快排把数组分成「块内无序、块间有序」后，逆序对数被压到 O(n × cutoff)，最后一遍插入是线性的。"},{"a":"s3","h":"希尔排序：唯一的悬案","x":"03 希尔排序：唯一的悬案 一个渐进复杂度至今没有定论的经典算法 插入排序一次只能挪 一格 ——末尾的最小元素要走 n 步。希尔的想法： 先跨大步插入，再逐步缩小步长 （Knuth：h = 3h+1）。最后一轮 h=1 就是普通插入，但此时数组已近乎有序，那一轮接近 Θ(n)。 像整理散落的书：先粗分成几堆（大步长），再在堆内细分，最后才精确排。每轮都让元素离目标更近。 增量序列 已证明的复杂度 备注 Shell 原始：n/2, n/4, …, 1 Θ(n²) 最差的选择 （偶数步长互不干涉） Knuth：(3^k−1)/2 → 1, 4, 13, 40 O(n^1.5) 实现最简单，实用 Hibbard：2^k−1 → 1, 3, 7, 15 Θ(n^1.5) 已证明的紧界 Sedgewick O(n^4/3) 实测最好的几个之一 Pratt：2^i·3^j Θ(n log²n) 理论最好，但序列太长、常数大 最优序列 未知 希尔排序的平均复杂度至今是开放问题 原始序列（不断 ÷2）最差 ：步长全是偶数，h=1 之前奇偶位从未比较过。构造「小的全在奇数位、大的全在偶数位」，前面所有轮次都白做。好序列要让相邻步长 互质 ，不同轮次才能互相帮忙。 希尔排序今天几乎不用于生产（ std::sort 全面更好）。留下两件事： ① 教科书里唯一渐进复杂度未定论的经典算法，说明「算法分析」本身有难度； ② 原地、无递归、几行代码，适合几 KB RAM、不允许递归、不能带 std 的嵌入式。"},{"a":"s4","h":"归并排序","x":"04 归并排序 唯一同时做到「稳定 + 最坏 O(n log n)」的比较排序"},{"a":"s4-1","h":"4.1 两种递归方向","x":"4.1 两种递归方向 自顶向下 自底向上 形态 切半 → 递归 → 合并；cutoff≤32 切插入 宽度 1→2→4… 逐层合并 栈 O(log n) 零栈深 优化 好加 cutoff； a[mid-1]≤a[mid] 可跳过合并 链表首选（不用随机访问找中点） 两个必加的优化：叶子占了一半节点，cutoff 切插入把常数打下来；若左段最大值 ≤ 右段最小值，整段已有序，跳过 O(n) 合并——近乎有序输入可以从 Θ(n log n) 掉到接近 Θ(n)。"},{"a":"s4-2","h":"4.2 稳定性从哪里来","x":"4.2 稳定性从哪里来 稳定性 完全由合并时那一个比较的方向决定 。相等取左边 → 原顺序保留；取右边就翻序。 合并 [lo, mid) 与 [mid, hi) → 相等时取谁，决定稳不稳 左段已有序 3 5a 8 右段已有序 4 5b 9 i → 5a j → 5b ✅ a[j] < a[i] 才取右；相等取左 5a 写出 … 3, 4, 5a, 5b … 相对顺序不变 ❌ a[j] ≤ a[i] 相等也取右 5b 写出 … 3, 4, 5b, 5a … 顺序被反转 为什么要稳定：多关键字可以分步排 先按薪资、再按部门稳定排一遍 = SQL ORDER BY 部门, 薪资。不稳定就必须写复合 comparator。 归并是唯一同时做到「稳定 + 最坏 O(n log n)」的比较排序。稳定性就这一处比较。 稳定 ：比较意义上相等的元素保持它们在输入中的相对顺序。它让 多关键字可以分步排 ——先按薪资降序、再按部门升序稳定排一遍，第二遍不打乱第一遍。SQL 的 ORDER BY a, b 、表格多列点击都靠这个。不稳定排序必须写把所有关键字合起来的复合 comparator。"},{"a":"s4-3","h":"4.3 原地归并的代价","x":"4.3 原地归并的代价 归并的短板是 O(n) 缓冲。存在原地归并，但旋转法变成 Θ(n log²n)，块归并常数极大。链表归并只重接指针， 不需要临时数组 ——所以 std::list::sort 用归并而不是快排：链表没有随机访问，快排分区做不了。 方案 额外空间 时间 实用性 标准归并（全缓冲） O(n) Θ(n log n) 默认选择 只缓冲一半 O(n/2) Θ(n log n) 常见优化 ，把短的一半拷出去 旋转法原地归并 O(1) Θ(n log²n) 慢一个 log 块归并（Block merge） O(1) Θ(n log n) 常数极大 ，代码复杂 链表归并 O(log n)（仅栈） Θ(n log n) 链表排序的最优解 std::stable_sort ： 能分到 n 个缓冲就 Θ(n log n)，分不到就退化成原地归并 Θ(n log²n) 。这是标准里少见的「复杂度取决于能否分配内存」的接口——内存紧时会静默变慢一个 log，不要默认拿它当「更安全的 sort」。"},{"a":"s5","h":"快速排序","x":"05 快速排序 平均最快，也最容易写错"},{"a":"s5-1","h":"5.1 分区三法","x":"5.1 分区三法 快排的全部工作在 分区（partition） ：选一个 pivot，把数组重排成「≤ pivot 的在左、≥ pivot 的在右」。合并是空操作。三种主流写法的不变式不同：Lomuto 单指针推进、Hoare 双指针对撞、三路把「等于 pivot」单独隔出来。 ① Lomuto（单向扫描） < pivot ≥ pivot 未处理 pivot i j → 简单好记 · 相等元素全归一侧 · 全等数组退化 O(n²) ② Hoare（双向对撞） ≤ pivot 未处理 ≥ pivot i → ← j 交换次数约少一半 · 全等数组也能均分 · 边界易写错 ③ 三路（Dijkstra 荷兰国旗） < pivot == pivot 未处理 > pivot lt i gt 大量重复键时唯一正确选择：相等元素一次到位，不再进入递归 三种分区的不变式不同：Lomuto 单指针推进，Hoare 双指针对撞，三路把「等于 pivot」单独隔出来。 只留 Hoare：交换少、全等也能均分。Lomuto 在全等数组上 a[j] < pivot 永假 → 切成 (0, n−1) → Θ(n²)。三路把「== pivot」隔出来，k 个不同值时 Θ(n log k)，枚举/小整数键才必须用。 int partition_hoare ( int * a, int lo, int hi) { int pivot = a[lo + (hi - lo) / 2 ]; int i = lo - 1 , j = hi; while ( true ) { do { ++i; } while (a[i] < pivot); do { --j; } while (a[j] > pivot); if (i >= j) return j; std:: swap (a[i], a[j]); } } // 返回值不是 pivot 就位下标 → 递归写 [lo, j+1) 与 [j+1, hi) // pivot 必须是数组里真实存在的元素，否则 do-while 可能越界 返回的 j 不是 「pivot 已就位」的下标，只是分界点。递归必须写 [lo, j+1) 与 [j+1, hi) ——不能像 Lomuto 那样跳过中间那个元素，否则会丢掉一个。大量重复键（枚举、布尔、截断时间戳）才必须上三路：等于 pivot 的一段一次到位，复杂度从 Θ(n log n) 降到 Θ(n log k)。"},{"a":"s5-2","h":"5.2 pivot 怎么选","x":"5.2 pivot 怎么选 策略 最坏输入 代价 评价 固定取首/末位 已排序 / 倒序数组 0 绝不要用 ——最坏输入太常见 取中位下标 特意构造的锯齿数组 0 比取端点好得多，但仍可构造 随机选 无（攻击者无法预测） 1 次随机数 简单有效 ；期望 O(n log n) 三点取中（首/中/末） 可构造（Bentley-McIlroy 反例） 3 次比较 实测最好的启发式 九点取中（ninther） 更难构造 ~13 次比较 大数组时用（pdqsort、BSD qsort） median-of-medians 无，最坏 O(n) 常数 5–10× 只作为兜底，不作主路径 随机化 不改善最坏渐近 （最坏仍 Θ(n²)），而是把「最坏何时发生」从攻击者可控变成不可控——这是安全性质。introsort 用深度计数器 检测 退化并切堆排，才把最坏 真正 拉回 O(n log n)。两者可叠加：生产实现就是三点取中 + 深度超限转堆排。凡是「输入由外部提供 + 算法有可构造的最坏」，都要当安全问题处理。 随机化 / 三点取中：解决的不是「最坏复杂度」，而是「谁能构造最坏」 取首/末位 已排序 / 倒序 = 每次极值 树退化成链，Θ(n²) 最坏输入太常见 三点取中 · 随机选 首/中/末取中位数，或随机下标 期望均衡，实测最好 最坏仍 Θ(n²)，只是难构造 introsort 深度检测 超 2⌊log₂n⌋ 层 → 切堆排 最坏真正 O(n log n) 随机化是安全性质，这个才是性能保证 2011 年有过针对三点取中的复杂度攻击：锯齿输入让每次中位数都是极值。 外部输入 + 可构造最坏 = 安全问题。防御：熵种子随机 pivot，或深度兜底。 随机化不改善最坏渐近；introsort 用廉价计数器把最坏拉回 O(n log n)。两者可叠加。"},{"a":"s5-3","h":"5.3 退化与爆栈","x":"5.3 退化与爆栈 两个独立维度：时间 Θ(n²)（慢，但不崩）和栈深 O(n)（ SIGSEGV ，比慢严重得多）。很多人只记得第一个。栈深的修法 不依赖任何概率假设 ：无论 pivot 多差，「较短的那一半」必然 ≤ n/2，所以只递归短边、长边转循环，栈深上界 log₂n 是确定性的。时间退化另用 introsort 深度检测，两件事别混。 快排树：均衡 vs 最坏链 均衡 · 每层切半 · 高 log₂n n n/2 n/2 n/4 n/4 n/4 n/4 总工作每层 Θ(n) × log n 层 = Θ(n log n) 最坏 · 每次切掉 1 个 · 高 n n n−1 n−2 已排序 + 取端点 或 Lomuto + 全等 时间 Σk = Θ(n²) 朴素递归栈深 O(n) → 崩 修栈深（无条件） 只递归较短一半 较长一半转 while 短边 ≤ n/2 栈 ≤ log₂n 时间退化另用 introsort 深度检测（见 §9），两件事别混。 链状树既慢又爆栈。栈深可以无条件压住，这一点比随机化更可靠；时间最坏必须靠切换算法才能真正拉回来，见 §9。"},{"a":"s6","h":"堆排序：理论完美、实测最慢","x":"06 堆排序：理论完美、实测最慢 最坏 O(n log n) + O(1) 空间，却几乎不被单独使用 纸面完美： 最坏 Θ(n log n) 、O(1) 空间、无递归。定位是 introsort 的降落伞，不是主力。 // 下沉：把 a[i] 沉到正确位置，堆大小为 n void sift_down ( int * a, int i, int n) { while ( true ) { int l = 2 * i + 1 , r = l + 1 , m = i; if (l < n && a[l] > a[m]) m = l; if (r < n && a[r] > a[m]) m = r; if (m == i) return ; std:: swap (a[i], a[m]); i = m; } } 数组下标即树：parent i → 左 2i+1、右 2i+2 0 1 2 3 4 5 6 下标 0 1 2 3 4 5 6 下沉走 0→1→3→… 步长 ×2，超出 L2 后几乎每层一次 miss，预取器失效 自底向上建堆 Θ(n)：底层节点几乎不用下沉。逐个 push 才是 Θ(n log n)。 建堆是 Θ(n) 不是 Θ(n log n) ：深度 d 的节点最多下沉 (h−d) 层，深度 d 上有 2^d 个节点。求和 Σ 2^d·(h−d) ≤ 2n 。关键是 大部分节点在底层，几乎不用下沉 。反过来「逐个 push」每次 sift-up 的都是最深节点，才是 Θ(n log n)。面试高频陷阱。 堆的访问像在楼里找人、每次只能问两个下属：下标 2i+1 是指数跳跃，预取器猜不到下一站。快排和归并沿着走廊走——操作次数未必更少，但 cache 命中好得多。所以「比较次数少」不等于跑得快。 实测慢：cache 差（步长指数增长，超出 L2 后几乎每层一次 miss）、比较约快排 2 倍、分支近 50% 误预测、不稳定、不自适应（升序甚至是最坏建堆输入）。所以只做兜底——最坏有保证、原地、无递归，恰好补齐快排短板。堆作为优先队列的用途远大于堆排序（Dijkstra / Top-K / 定时器 / K 路归并），见 STL 容器 。"},{"a":"s7","h":"Ω(n log n) 下界证明","x":"07 Ω(n log n) 下界证明 为什么比较排序不可能更快"},{"a":"s7-1","h":"7.1 决策树模型","x":"7.1 决策树模型 把「只靠比较」的排序抽象成二叉树：内部节点是 a[i]:a[j] ，叶子是一种输出排列。 路径长度 = 该输入的比较次数，树高 = 最坏比较次数 。插入、归并、快排、堆排的差别只是树的形状不同。 a₁ : a₂ < ≥ a₂ : a₃ a₂ : a₃ < ≥ < ≥ ① a₁a₂a₃ a₁ : a₃ a₁ : a₃ ⑥ a₃a₂a₁ < ≥ < ≥ ② a₁a₃a₂ ③ a₃a₁a₂ ④ a₂a₁a₃ ⑤ a₂a₃a₁ n=3：必须有 3! = 6 个叶子 → 二叉树至少 ⌈log₂6⌉ = 3 层 → 最坏至少 3 次比较 推广到任意 n：叶子 ≥ n! → 树高 ≥ log₂(n!) = Θ(n log n) n=3 的决策树。叶子必须覆盖全部 6 种排列，二叉树的高度因此不可能低于 log₂6。"},{"a":"s7-2","h":"7.2 证明","x":"7.2 证明 证明只有三步，每一步都很短： 第一步 叶子数 ≥ n! 算法必须能输出全部 n! 种排列（否则存在某个输入它答错）。 每个叶子只对应一个固定的输出排列，故叶子数 ≥ n!。 第二步 高度为 h 的二叉树，叶子数 ≤ 2^h 所以 2^h ≥ n! → h ≥ log₂(n!) 第三步 用 Stirling 公式展开 log₂(n!) n! ≥ (n/e)^n log₂(n!) ≥ n·log₂(n/e) = n·log₂n − n·log₂e = Θ(n log n) ∴ 任何比较排序的最坏比较次数是 Ω(n log n) 。∎ 像二十个问题：每个是/非最多把候选 减半 ，要从 N 个候选里锁定一个，至少问 log₂N 次。排序要区分的候选是 n! 种排列，每次比较只能问一个是/非——所以 log₂(n!) ≈ n log n 是 信息论下界 ，跟算法多聪明无关。想突破只能改问法：不问「谁大」，问「你的值是多少」（§8 计数与基数）。 推论 含义 归并渐进最优 Θ(n log n) 恰好达到下界，比较模型内只能抠常数 平均也是 Ω(n log n) 同一证明可改成「叶子平均深度 ≥ log₂(n!)」，不存在平均更快的比较排序 只约束比较模型 能用元素值做索引/位运算的算法不受限——这是模型边界，不是漏洞 同一套路能证「有序数组查找至少 log₂n 次比较」，见 二分查找 。 用决策树数叶子是证明下界的通用方法。"},{"a":"s8","h":"突破下界","x":"08 突破下界 不比较，直接用值做下标 值能 直接当下标 就能跳过比较，做到线性。Ω(n log n) 只约束「只靠比较获取信息」的算法；这条路对输入加了限制：键必须是范围已知的整数、可按位拆的定长键，或近似均匀的实数。"},{"a":"s8-1","h":"8.1 计数排序","x":"8.1 计数排序 k = O(n) 时 Θ(n+k)；k ≫ n 不可行（百万个 32 位整数要 16 GB 计数数组）。 倒序回填才稳定 ——正序会把相同值按倒序填入。稳定性在这里不是可选项：LSD 基数每一轮都是一次稳定计数，写成正序会让前面几轮的结果被打乱，整段基数排序出错。 // a = [2, 0, 2, 1, 0]，k = 3 // ① 计数 cnt = [2, 1, 2] // ② 前缀和 cnt = [2, 3, 5] ← 每个值的结束下标 for ( int i = n - 1 ; i >= 0 ; --i) // ③ 倒序回填 → 稳定 out[--cnt[a[i]]] = a[i]; // → [0, 0, 1, 2, 2]"},{"a":"s8-2","h":"8.2 基数排序","x":"8.2 基数排序 大值域拆成 d 组小值域，逐位做稳定计数。32 位按 8 位一组 → d=4、K=256，顺序访问、零比较、零分支误预测，实测常比 std::sort 快 2–3×——这是唯一能明确打败 introsort 的通用排序。列式数据库、GPU（cub::DeviceRadixSort）、「按定长整数 key 排百万级记录」都在用。 字符串：LSD 走完全长；MSD 按前缀分桶，短前缀就能停 LSD · 先排末字符，再排倒数第二……必须每轮稳定 cat car cap 第 1 遍按末位 p/r/t 第 2 遍按中间 a…（稳定，不打乱上轮） 定长整数 / 定长串：循环 d 遍即可 MSD · 先按首字符分桶，再递归排每个桶 c* d* t* 变长串、前缀差异大时桶很快只剩 1 个 → 提前结束 不依赖稳定性；递归 + 小桶常数大 工程：定长整数用 LSD；字典/URL/变长串用 MSD（或 trie 思想） 桶排序同族：均匀才线性，偏斜就退化——分布式 range partition 必须处理倾斜 LSD 完全依赖每轮稳定计数；MSD 用前缀把问题缩小。列存 / GPU 排序大量用 LSD 整数基数。 有符号整数（补码）最高位是 1，按无符号排会跑到正数后面——先翻最高位（ x ^ 0x80000000 ）。IEEE 754 正数可按位比， 负数的位模式是倒序 ：正数翻符号位、负数全位取反。漏掉这步的 bug 很隐蔽：正数测试全过，一上负数就错。 算法 时间 空间 稳定 前提 计数 Θ(n+k) Θ(n+k) 是 小整数键 基数 LSD Θ(d(n+K)) Θ(n+K) 是 定长可按位拆 桶 期望 Θ(n) Θ(n) 依桶内 近似均匀"},{"a":"s8-3","h":"8.3 桶排序","x":"8.3 桶排序 期望 Θ(n)，但 依赖均匀分布 ，一偏斜就 Θ(n²)。真正留下的不是当排序用，而是「按范围分桶再独立处理」——分布式 range partition、Spark 的 rangePartitioner 、外部排序的分区阶段都是它。它们也必须解决同一个问题：数据倾斜时怎么办（通常靠采样 + 动态调整分界点）。"},{"a":"s9","h":"std::sort：introsort","x":"09 std::sort：introsort 快排主路径 + 堆排兜底 + 小段插入，不要去背库源码"},{"a":"s9-1","h":"9.1 introsort 三段结构","x":"9.1 introsort 三段结构 std::sort 是 introsort （Musser 1997）：快排为主，深度超限切堆排，最后全局插入。短板互不重叠，用一个 depth_limit 串起来。 ① 快排主路径 三点取中选 pivot Hoare 风格分区 递归右半 + 左半转循环 → 常数最小，平均最快 深度超限 2·⌊log₂n⌋ ② 堆排兜底 __partial_sort = make_heap + 逐个 pop 最坏 O(n log n) 保证 → 慢，但不会退化成 n² ③ 最后一遍插入排序（对整个数组，不是逐块） 此时数组「块内无序、块间有序」，每个元素离最终位置 ≤ 16 → 逆序对数 = O(16n) → 插入排序是 Θ(n) ，而且全程 cache 命中 为什么这三段能凑成一个 快排：常数小、平均快 但最坏 O(n²)、栈深 O(n) 堆排：最坏有保证、原地、无递归 但 cache 差、常数大 插排：小规模无敌、自适应 但 O(n²) 三者的短板互不重叠 → 用一个深度计数器串起来 结果：平均拿到快排的常数，最坏拿到 O(n log n) 保证，栈深 ≤ O(log n) 代价只有一个 depth_limit 整数——这是「多算法按运行时特征切换」的教科书范例 introsort 的价值不在任何单个算法，而在于三者短板互补，用一个廉价的监控量在它们之间切换。 「快路径求常数 + 廉价监控量 + 慢路径兜底最坏」到处可见： nth_element 的 introselect、GMP 按位数切乘法、JIT 解释→热点编译、TCP 快重传→超时、DB 的 nested-loop → hash join 回退。用一个 int 计数器就把快排/堆排/插排串起来。 工程：小 n 插入 —— cutoff=16，最后一遍是全局插入不是逐块排 块 ≤16 块 ≤16 块 ≤16 块 ≤16 块间已有序 每元素离最终位 ≤16 为什么 cutoff 是 16 而不是 2 或 64 太小：递归/分区开销吃掉插入的优势；太大：块内无序，I 不再是 O(n) 全局插一次：逆序对 O(16n) → Θ(n)，顺序扫 cache 全中。分块排要多次进出。 unguarded_partition 省掉边界检查，前提是比较器满足严格弱序，否则指针冲出数组。 深度上限 2⌊log₂n⌋：理想只需 log n 层，超了说明 pivot 系统性差，切堆排。unguarded_partition 省掉边界检查，前提是三点取中保证 pivot 不是最值——这也是比较器一旦不自洽就会越界的原因。"},{"a":"s9-2","h":"9.2 stable_sort 与其他兄弟","x":"9.2 stable_sort 与其他兄弟 接口 算法 复杂度 稳定 额外空间 std::sort introsort O(n log n) 最坏（C++11 起标准要求） 否 O(log n) 栈 std::stable_sort 归并（缓冲）/ 原地归并（无缓冲） O(n log n) 或 O(n log²n) 是 尝试 O(n)，失败则 O(1) std::partial_sort 堆（建 k 大小的堆再筛） O(n log k) 否 O(1) std::nth_element introselect O(n) 平均，最坏 O(n) 否 O(log n) 栈 std::sort_heap 堆排序 O(n log n) 否 O(1) std::is_sorted_until 线性扫描 O(n) — O(1) 选型：全排且不在乎相等顺序 → sort ；要稳定（多关键字分步、UI 多列） → stable_sort ；只要前 k → partial_sort （O(n log k)）；只要第 k 个、或前 k 内部无序 → nth_element （O(n)，最快）；边来边取最大 → priority_queue 。求 Top 100 写全量 sort 是浪费： nth_element 把前 100 挪到前面再局部 sort 才是 O(n)。n=10⁷ 时比全排快 20 倍以上。"},{"a":"s9-3","h":"9.3 严格弱序：真实事故","x":"9.3 严格弱序：真实事故 违反严格弱序是 UB ，典型表现不是顺序错，而是 unguarded_partition 指针冲出数组、几分钟后在别处崩。三条：反自反（ comp(a,a) 为假）、非对称且传递、「等价」可传递。用 < 不用 <= 。 错法 破坏 修 return a <= b 反自反 用 < 多字段 if 链写漏 可能 a<b 且 b<a std::tie / <=> 数据含 NaN 等价不可传递 先 erase_if(isnan) 比较器有副作用 两次比较结果不同 无副作用；外部键先物化成 pair []( const P& a, const P& b) { return std:: tie (a.x, a.y) < std:: tie (b.x, b.y); } Debug 开 -D_GLIBCXX_DEBUG / _ITERATOR_DEBUG_LEVEL=2 会主动校验。review 见到比较器里的 <= 一律当 bug。见 Sanitizers 。之所以会越界而不只是顺序错：unguarded 内层省掉了边界检查，依赖「pivot 不是最值、指针一定会停下」——比较器不自洽时 while (comp(*i, pivot)) ++i 可能永远为真，往堆上乱写，几分钟后在无关代码里崩。"},{"a":"s10","h":"工程场景","x":"10 工程场景 数据装不进内存、数据本来就有序、以及多核"},{"a":"s10-1","h":"10.1 外部排序与多路归并","x":"10.1 外部排序与多路归并 1 TB 数据、16 GB 内存时走 外部排序 ：分块内排生成有序 run，再 K 路归并。核心不是算法（归并都会），而是让磁盘 I/O 尽可能顺序。 阶段一 · 分块内排序（生成有序 run） 1 TB 输入 顺序读 读满 8 GB → std::sort → 写出一个 run run 1 run 2 run 3 ··· run 128 每个 run 内部有序，磁盘上共 128 个文件 阶段二 · K 路归并（一遍扫完） run 1 → run 2 → run 3 → ⋮ run 128 → 每路一个 输入缓冲区 buf_size = RAM / (K+1) 缓冲越大 → 顺序 读越多 → 越快 最小堆（size = K） 堆里放每路的当前队首 pop 最小 → 写输出 → 从那一路补一个进来 每条记录 O(log K) 1 TB 有序输出 顺序写 K 的取舍 K 大 → 归并 遍数少 K 大 → 每路 缓冲小 → 随机 I/O 变多 要实测 外部排序的核心不是算法，而是让磁盘 I/O 尽可能顺序。K 的选择是「归并遍数」与「每路缓冲大小」的权衡。 K 路用最小堆，每条 O(log K)；别两两归并（I/O × log₂K）。K 太大则每路缓冲变小、顺序读变随机读——机械盘几十到几百，NVMe 可更大，必须实测。置换选择用持续吞吐的堆代替「读满一批排一批」，期望生成 2× 内存 的 run，对已部分有序才明显。LSM compaction / Spark shuffle / DB ORDER BY 溢出都是这套，见 存储架构 、 Spark 内核 。"},{"a":"s10-2","h":"10.2 TimSort 与 pdqsort","x":"10.2 TimSort 与 pdqsort 真实数据几乎从不是均匀随机的——日志按时间近似有序、数据库结果集部分有序、多个有序流拼接后是分段有序。教科书假设均匀随机， std::sort 基本不自适应。TimSort 和 pdqsort 就是冲着真实分布设计的。 TimSort（Python / Java 对象数组） pdqsort（Rust / Boost / 现代 C++ 提案） 基础 归并排序（稳定） 快排（不稳定） 核心思想 识别输入里天然的有序段（run） ，用栈维护并按规则合并 检测「坏模式」并主动打破 已有序输入 Θ(n) （一个 run 就扫完） Θ(n) （有专门的有序检测） 特色优化 galloping merge（一路远大于另一路时用指数搜索跳过） partial insertion sort、遇到重复键多时切三路、遇到反复不均分时随机打乱 最坏 Θ(n log n) Θ(n log n)（沿用 introsort 的深度兜底） 稳定 是 否 额外空间 O(n) O(log n) 自适应：复杂度随无序程度变。插入对逆序对 I 自适应（Θ(n+I)）；TimSort 对天然有序段数 R 自适应（Θ(n log R)）； std::sort 基本不自适应，所以「已排好又追加一小批」时 TimSort 能快一个数量级。C++ 里不必换算法：追加一小段 → 局部 sort + inplace_merge ；多路有序 → merge / 堆；先问有没有序 → is_sorted 。"},{"a":"s10-3","h":"10.3 并行排序","x":"10.3 并行排序 C++17： std::sort(std::execution::par, …) （libstdc++ 需 TBB）。加速比通常远低于核数：顶层那次 O(n) 合并串行（Amdahl 上限）、排序是 memory-bound 多核先撞带宽墙、快排划分不均。并行版内存占用也更高。10⁷ 个 int 单线程约 0.6s——如果 ②③ 已经压到 1 秒内，并行可能不必要。先换算法（基数、排索引而非大对象、 nth_element ），再上并行。"},{"a":"s11","h":"选型决策表","x":"11 选型决策表 八大排序的完整对照，以及生产选型顺序 算法 平均 最坏 最好 空间 稳定 自适应 实用定位 冒泡 Θ(n²) Θ(n²) Θ(n) O(1) 是 是 只有教学价值 选择 Θ(n²) Θ(n²) Θ(n²) O(1) 否 否 交换次数 O(n)，移动极贵时 插入 Θ(n²) Θ(n²) Θ(n) O(1) 是 是 小区间 / 近乎有序 / 在线 希尔 依增量序列 O(n^1.5)~ Θ(n log n) O(1) 否 部分 极受限的嵌入式环境 归并 Θ(n log n) Θ(n log n) Θ(n log n) O(n) 是 加优化后是 需要稳定 / 链表 / 外部排序 快排 Θ(n log n) Θ(n²) Θ(n log n) O(log n) 否 否 通用默认（配 introsort 兜底） 堆排 Θ(n log n) Θ(n log n) Θ(n log n) O(1) 否 否 兜底算法 / 内存极紧 计数 Θ(n+k) Θ(n+k) Θ(n+k) O(n+k) 是 否 小值域整数键 基数 Θ(d(n+K)) Θ(d(n+K)) Θ(d(n+K)) O(n+K) 是 否 定长整数键，实测最快 桶 Θ(n)* Θ(n²) Θ(n) O(n) 依桶内 否 * 需均匀分布假设 生产选型四步 （按收益从大到小）： 先问要不要全排。 只要第 k 个 → nth_element （O(n)）；只要前 k 个 → partial_sort （O(n log k)）；只要知道有没有序 → is_sorted 。这一步经常直接省掉一个 log。 再问键是什么。 定长整数/定长字符串 → 试基数排序，单线程常能快 std::sort 2–3 倍。枚举/小整数 → 计数或三路快排。 再问要不要稳定。 要 → stable_sort ；不要 → sort 。别为了「保险」默认用 stable_sort ，它慢且吃 O(n) 内存。 最后才并行 ，而且先减少数据移动（排索引而非排对象）。"},{"a":"s11-1","h":"11.1 八大排序对照（交互）","x":"11.1 八大排序对照（交互） 排序算法逐个体检 点击算法名，看它的机制、优缺点、以及「什么时候真的该用它」。 插入 归并 快排 堆排 基数 std::sort"},{"a":"s12","h":"面试题集（12 道）","x":"12 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s12-1","h":"12.1 基础（必答）","x":"12.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 什么是稳定排序？它为什么重要？哪些常见排序是稳定的？ 稳定 指排序后， 比较意义上相等 的元素保持它们在输入中的相对顺序。 为什么重要 稳定性让 多关键字排序可以分步做 。要按「部门升序、部门内薪资降序」排，只需先按薪资降序排一遍，再按部门升序 稳定 排一遍——第二遍不打乱第一遍。这在 SQL 的 ORDER BY a, b 、表格 UI 的多列点击排序里是刚需。不稳定排序想做同样的事，必须写一个把所有关键字合起来的复合 comparator。 稳定 不稳定 冒泡、插入、 归并 、计数、基数、TimSort 选择、希尔、 快排 、堆排、introsort 一个记忆线索 凡是会做「 长距离交换 」的排序基本都不稳定（选择排序把远处的最小值换过来、堆排把堆顶和末尾换、快排跨区交换）。只做相邻移动或按序追加的排序才稳定。 C++ 里的细节 std::sort 不保证稳定； std::stable_sort 保证，但它 尝试分配 O(n) 缓冲 ——分配失败时退化成原地归并，复杂度从 O(n log n) 变成 O(n log²n) 。这是标准里罕见的「复杂度取决于能否分配内存」的接口。 基础 快排、归并、堆排的复杂度与空间各是多少？各自的适用场景？ 平均 最坏 额外空间 稳定 快排 Θ(n log n) Θ(n²) O(log n) 栈 否 归并 Θ(n log n) Θ(n log n) O(n) 缓冲 是 堆排 Θ(n log n) Θ(n log n) O(1) 否 快排 常数最小、cache 友好（顺序分区），是 通用默认 。缺点是最坏 O(n²) 且朴素实现栈深 O(n)——所以生产实现必须配 introsort 的深度兜底 + 「只递归小半边」。 归并 唯一同时做到 稳定 + 最坏 O(n log n) 。用在：需要稳定、 链表排序 （不需要临时缓冲，只重接指针，所以 std::list::sort 用它）、 外部排序 （多路归并是唯一可行方案）。 堆排 纸面完美（最坏有保证 + O(1) 空间 + 无递归），但 实测最慢 ：下沉时访问下标 i→2i+1→4i+3，步长指数增长，cache miss 严重且预取器完全失效；分支「左孩子大还是右孩子大」基本随机，误预测率近 50%。所以它的定位是 兜底算法 ——它的三个优点恰好补齐快排的三个短板。 基础 为什么插入排序是唯一被写进工业级排序库的 O(n²) 算法？ 因为它在 小规模 和 近乎有序 两种情形下都极快，而这两种情形恰好是分治排序的收尾阶段会遇到的。 自适应 复杂度是 Θ(n + I) （I = 逆序对数），不是固定 Θ(n²)。已有序时内层一次都不进 → Θ(n)。 写操作少 用 后移 （1 次赋值）而不是 交换 （3 次赋值）。这是它比冒泡快好几倍的主因。 分支可预测 + 全程 cache 命中 只碰相邻元素，顺序访问；内层循环通常连续「进入」或连续「不进入」，分支预测器命中率高。 原地 + 稳定 + 在线 O(1) 空间，可以边接收数据边排。 在 introsort 里的角色 ：快排把数组分到每块 ≤ 16 后停止，此时数组「块内无序、块间有序」， 每个元素离最终位置不超过 16 → 总逆序对数 O(16n) → 最后一遍全局插入排序是 Θ(n) ，而且顺序扫描 cache 全命中。 实现细节 内层条件必须是 a[j] > key 而 不是 >= 。用 >= 会在相等时继续前移，既丢掉稳定性，又白做无用移动。 基础 计数排序 / 基数排序为什么能做到 O(n)？它们的前提是什么？ 因为它们 不通过比较获取信息 ，而是 直接用元素的值当下标 。Ω(n log n) 下界只约束「只靠比较」的算法，所以这条路径不受它限制。 计数排序 Θ(n+k) 统计每个值出现几次 → 求前缀和得到每个值的结束位置 → 倒序 回填（倒序才稳定）。 前提：键是范围 [0,k) 的整数，且 k = O(n)。 给 100 万个 32 位整数排序时 k=2³²，计数数组要 16 GB → 不可行。 基数排序 Θ(d(n+K)) 把大值域拆成 d 组小值域（如 32 位拆成 4 组 8 位）， 逐位做稳定的计数排序 。LSD（低位到高位） 完全依赖每轮的稳定性 ——某一轮不稳定，前面几轮的结果就被打乱了。 基数排序的隐藏坑 有符号整数和浮点数不能直接按位排 ，因为位序和数值序不一致：补码负数最高位是 1（会排到正数后面），IEEE 754 负数的位模式是倒序的。修法是先做位变换（正数翻符号位、负数全位取反）。 漏掉这步的 bug 很隐蔽——正数测试全过，一上负数就错。 桶排序 期望 Θ(n)，但 依赖「键近似均匀分布」的假设 ，数据一偏斜就退化 Θ(n²)。它真正的价值不是当排序用，而是「先按范围分桶再各桶独立处理」这个模式——分布式系统的 range partition 就是它。"},{"a":"s12-2","h":"12.2 中级（区分熟练度）","x":"12.2 中级（区分熟练度） 中级 证明比较排序的最坏比较次数是 Ω(n log n)。 用 决策树模型 ：把任何只靠比较获取信息的排序算法抽象成一棵二叉树——内部节点是一次比较 a[i]:a[j] ，两条出边对应两个结果，叶子是最终输出的排列。 树高 = 最坏比较次数。 ① 叶子数 ≥ n! 算法必须能输出全部 n! 种排列，每个叶子只对应一个固定排列。 ② 高度 h 的二叉树叶子数 ≤ 2^h ⟹ 2^h ≥ n! ⟹ h ≥ log₂(n!) ③ Stirling：n! ≥ (n/e)^n log₂(n!) ≥ n·log₂(n/e) = n log₂n − n log₂e = Θ(n log n) ∎ 直觉版 像「二十个问题」猜谜：每个是/非问题最多把候选 减半 ，要从 n! 个候选里锁定一个，至少需要 log₂(n!) ≈ n log n 次。这是 信息论下界 ，与算法多聪明无关。 三个推论： 归并排序渐进最优 它的 Θ(n log n) 恰好达到下界 → 比较模型内不存在渐进更好的算法，优化只能在常数上做。 平均情形也是 Ω(n log n) 同一论证可改成「叶子平均深度 ≥ log₂(n!)」，所以「平均更快」的比较排序也不存在。 下界只约束比较模型 它 不 约束能用元素值直接做索引/位运算的算法——这不是漏洞，而是模型边界。计数、基数、桶排序正是从这里出去的。 「用决策树数叶子」是证明下界的通用套路 ：同样能证明「有序数组查找至少 log₂n 次比较」。 中级 快排的三种分区方式有什么区别？给一个全部元素相等的数组，各自表现如何？ Lomuto Hoare 三路（荷兰国旗） 指针 单向推进 双向对撞 三指针 lt / i / gt 交换次数 多 约少一半 中等 全等数组 Θ(n²) 且栈深 O(n) 中点均分，Θ(n log n) Θ(n) 一遍搞定 边界易错度 低 高 中 Lomuto 为什么在全等数组上崩 条件 a[j] < pivot 永假 → i 一直不动 → 划分成 (0, n−1)，每次只切掉一个元素。时间 Θ(n²)、栈深 O(n)。 Hoare 为什么不会 它的两个指针在遇到「等于 pivot」时都会停下并交换，所以全等数组时 i、j 各走一步就相遇 → 中点划分。 Hoare 的两个坑 ① 返回的下标 不是 「pivot 已就位」的位置，只是分界点 → 递归必须写 [lo, j+1) 和 [j+1, hi) ，不能像 Lomuto 那样跳过中间元素； ② do{++i}while(a[i]<pivot) 依赖 pivot 是数组里 真实存在 的元素做哨兵，取平均值之类会越界。 什么时候必须用三路 按枚举字段排序 （状态、类型、省份）、按布尔或小整数排、按截断时间戳排——这些场景不同值可能只有几十个而元素上百万。三路把复杂度从 Θ(n log n) 降到 Θ(n log k) （k = 不同值个数），因为等于 pivot 的那一段一次到位，不再进入递归。 中级 逐行讲一遍 libstdc++ 的 std::sort 做了什么，以及为什么这样设计。 它是 introsort （Musser 1997），三段结构： void __sort (It first, It last, Comp comp) { if (first == last) return ; __introsort_loop (first, last, __lg (last-first) * 2 , comp); // ①② __final_insertion_sort (first, last, comp); // ③ } void __introsort_loop (It first, It last, Size depth_limit, Comp comp) { while (last - first > 16 ) { // 小区间留给 ③ if (depth_limit == 0 ) // ② 退化 → 堆排 { __partial_sort (first, last, last, comp); return ; } --depth_limit; It cut = __unguarded_partition_pivot (first, last, comp); // ① __introsort_loop (cut, last, depth_limit, comp); // 递归右半 last = cut; // 左半转循环 } } ① 快排主路径 三点取中选 pivot + Hoare 风格分区。 常数最小，平均最快。 「递归一半 + 另一半转循环」把栈深压到 O(log n)。 ② 堆排兜底 深度上限 2⌊log₂n⌋ （理想只需 log₂n，留 2 倍余量）。超限说明 pivot 选择在这个输入上 系统性地 差，不是运气 → 切堆排，把最坏 真正 拉回 O(n log n)。 ③ 全局插入排序 注意是对 整个数组 做一遍，不是逐块排。因为块间已有序、每个元素离最终位置 ≤16 → 逆序对总数 O(16n) → 这一遍是 Θ(n) 且顺序扫描 cache 全命中。分块排反而要多次进出。 为什么这三段能凑成一个 它们的 短板互不重叠 ：快排常数小但最坏 O(n²)；堆排最坏有保证、原地、无递归但 cache 差；插排小规模无敌但 O(n²)。 用一个 int 计数器就把三者串起来 ——平均拿到快排的常数，最坏拿到 O(n log n) 保证。 这个「快路径求常数 + 廉价监控量 + 慢路径兜底」的模式在系统软件里到处可见： nth_element 的 introselect、GMP 按位数切换乘法、JIT 的解释→热点编译、TCP 快重传→超时重传、DB 的 nested-loop→hash join 回退。 还有一处细节 unguarded_partition 是「无哨兵检查」的分区：三点取中保证 pivot 不是最值，指针一定会停下，于是 内层循环省掉了边界判断 。这在最内层循环里收益显著——但它也是下一题那个 UB 会导致越界的原因。 中级 要从 10⁷ 条记录里取 Top 100，你会怎么做？为什么不直接 sort？ sort 再取前 100 是 O(n log n)，白排了 999 万条不需要的记录。三个更好的方案： 方案一 · nth_element + sort（最快，O(n)） std:: nth_element (v. begin (), v. begin ()+ 100 , v. end (), cmp); // O(n) std:: sort (v. begin (), v. begin ()+ 100 , cmp); // O(100 log 100) 总共 O(n) 。n=10⁷ 时比 sort 快 20 倍以上。 前提是数据全在内存且可以被打乱顺序。 方案二 · partial_sort（O(n log k)） std::partial_sort(v.begin(), v.begin()+100, v.end()) 。比方案一略慢（多了堆的 log k），但一步到位且前 100 直接有序。 方案三 · 大小为 k 的最小堆（O(n log k)，空间 O(k)） 唯一能处理 流式数据 / 数据不能全装进内存 的方案：维护一个 size=100 的最小堆，新元素比堆顶大就替换堆顶。空间只要 O(k)。 std::priority_queue<T, std::vector<T>, std::greater<>> pq; for ( auto & x : stream) { if (pq. size () < 100 ) pq. push (x); else if (x > pq. top ()) { pq. pop (); pq. push (x); } } 选型判据 数据全在内存、可打乱 → nth_element （O(n) 最快）。流式 / 内存装不下 / 分布式各分片先出局部 Top-K → 堆 。k 接近 n → 直接 sort （此时 log k ≈ log n，没有优势了）。 分布式场景还有一层： 各分片各出 Top-K，再归并这些 Top-K ——这是正确的（全局 Top-K 必然是某个分片的 Top-K），而且把网络传输从 O(n) 降到 O(分片数 × k)。"},{"a":"s12-3","h":"12.3 高级（体现深度）","x":"12.3 高级（体现深度） 高级 线上服务有个偶发崩溃，最终定位到一个 std::sort 的比较器。这是怎么发生的，怎么根治？ 比较器违反了 std::sort 要求的 严格弱序 ，这是 未定义行为 ——而且它的表现通常不是「顺序错」，而是 数组越界写入，然后在完全无关的地方崩溃 。 为什么会越界 introsort 的 __unguarded_partition 内层循环 省掉了边界检查 ，它依赖「pivot 不是最值，所以指针一定会在某个元素处停下」——这个前提由严格弱序保证。比较器不自洽时 while (comp(*i, pivot)) ++i; 可能永远为真， 指针一路冲出数组往堆上乱写 。 典型症状： 排序本身没报错，几分钟后在别的模块崩溃 ，因为被踩坏的是别人的数据。这也是它极难定位的原因。 严格弱序的三条要求 （记 comp(a,b) = 「a 排在 b 前」）：反自反（ comp(a,a) 必为 false）、非对称且传递、以及「等价关系」 !comp(a,b) && !comp(b,a) 必须可传递。 四种真实见过的错法： ① 用了 <= return a <= b; 违反反自反。 review 里看到比较器出现 <= 一律当 bug。 ② 多字段 if 链写漏 if (a.x < b.x) return true ; return a.y < b.y; // ← a.x > b.x 时也可能返回 true → a<b 且 b<a 正确写法： return std::tie(a.x, a.y) < std::tie(b.x, b.y); 或 C++20 的 <=> 。 ③ 数据含 NaN NaN 与任何值比较都是 false → 「等价」不可传递（a≡NaN、NaN≡b，但 a≢b）。排序前 erase_if(isnan) 或归一化。 ④ 比较器有副作用 / 依赖可变外部状态 [&](int a,int b){ return score[a]++ < score[b]; } ——同一对元素两次比较结果不同。 比较器必须无副作用且幂等。 需要按外部键排序时先物化成 pair 再排。 根治（不是修一处） ① Debug 构建开 -D_GLIBCXX_DEBUG （libstdc++）或 _ITERATOR_DEBUG_LEVEL=2 （MSVC），它们会 主动校验严格弱序 并明确报错； ② 全仓库搜比较器里的 <= 和手写 if 链，统一改成 std::tie / <=> ； ③ 把「排序前检查 NaN」写进浮点数据处理的规范； ④ 把这条加进 review 清单——这类错误 几乎从不只有一处 ，它是习惯问题。 高级 要排序 1 TB 数据但只有 16 GB 内存，怎么做？关键的工程权衡在哪？ 外部排序 ，两阶段： 阶段一 · 生成有序 run 顺序读入，每读满约 8 GB（留一半内存给缓冲和输出）就 std::sort 一遍，写出一个有序文件。1 TB → 约 128 个 run。 阶段二 · K 路归并 为每个 run 开一个输入缓冲区，用一个 size=K 的最小堆 存各路当前队首。pop 最小值写输出，再从那一路补一个进堆。每条记录 O(log K) ，全量数据只需再读写一遍。 关键权衡都在 K 上： 别写成两两归并 两两归并需要 log₂K 遍， 每遍都完整读写一次全量数据 → I/O 是 K 路归并的 log₂K 倍。1 TB 数据下这是 7 倍的差距。 K 也不是越大越好 内存要在 K+1 个缓冲区之间分。 K 太大 → 每路缓冲太小 → 磁盘从顺序读退化成随机读 。机械盘上 K 通常取几十到几百，NVMe 上可以更大。 这个值必须实测 ，因为它取决于存储介质的顺序/随机性能比。 进一步的优化 置换选择（replacement selection） ：用一个持续吞吐的堆代替「读满一批排一批」，期望能生成 2× 内存大小 的 run，从而把 run 数减半。代价是实现复杂，且对已部分有序的输入收益才明显。 还有 压缩 run （用 CPU 换 I/O，通常划算）和 只排 key + 偏移量 （记录很大时，避免搬运整条记录）。 这个模式无处不在 LSM-Tree 的 compaction 就是多路归并； MapReduce / Spark 的 shuffle 是「map 端分块排序 + reduce 端归并」；数据库的 ORDER BY 在结果集超过 sort_buffer_size 时也走磁盘归并。 所以外部排序的核心不是算法（归并大家都会），而是让磁盘 I/O 尽可能顺序。 高级 什么是自适应排序？TimSort 和 pdqsort 各解决了什么问题？在 C++ 里怎么吃到这个红利？ 自适应排序 的复杂度随输入的「无序程度」变化，而不是只看 n。常用度量：逆序对数 I、天然有序段数 R。 动机很实际： 真实数据几乎从不是均匀随机的 ——日志按时间近似有序、数据库结果集部分有序、多个有序流拼接后是分段有序。而 std::sort 基本 不 自适应。 TimSort pdqsort 基础 / 稳定 归并 / 稳定 快排 / 不稳定 核心思想 识别天然有序段（run） ，用栈维护并按规则合并 检测坏模式并主动打破 特色优化 galloping merge：一路远大于另一路时用指数搜索跳过 重复键多 → 切三路；反复不均分 → 随机打乱；近乎有序 → partial insertion sort 已有序输入 Θ(n) Θ(n) 额外空间 O(n) O(log n) C++ 里怎么吃到红利 不必换排序算法，而是 主动利用已知的有序性 ： · 已排好的 v 追加了一小段 → 对新段单独 sort + std::inplace_merge ，而不是整体重排； · 多个有序序列合并 → std::merge 或最小堆做 K 路归并； · 只想知道是否有序 → std::is_sorted 是 O(n)，比排一遍便宜太多。 再往上一层 「 为真实数据分布而不是最坏情形优化 」是个通用思路，不只在排序里：JIT 按运行时 profile 重编译热点、压缩算法先建字典再编码、数据库的自适应哈希索引、CPU 的分支预测器。 它们的共同代价是 需要一个「检测阶段」，而检测本身有开销、也可能被对抗性输入欺骗 ——pdqsort 遇到反复不均分时主动随机打乱，正是为了防这种对抗。 高级 一个「排序 5000 万条日志按时间戳」的接口太慢，你的优化顺序是什么？ 先换算法和数据布局，最后才考虑并行。 按收益从大到小： ① 先问需不需要全排序 这是最大的一笔收益，而且经常被跳过。只要最近 N 条 → partial_sort （O(n log k)）；只要中位数或某个分位 → nth_element （O(n)）；只是要分页展示前 100 条 → nth_element + 局部 sort 。 问清需求经常直接省掉一个 log。 ② 减少数据移动量 如果记录很大（比如每条日志几百字节）， 不要排结构体本身 ——排 (timestamp, index) 的 pair，或者干脆排 index 数组。这把每次交换从「拷贝几百字节」变成「拷贝 8–16 字节」。 注意间接排序会引入一次随机访问的重排，所以要实测： 记录大到一定程度才划算 。 ③ 换基数排序 时间戳是 定长无符号整数 ——这是基数排序的理想输入。64 位时间戳按 8 位分 8 遍（或先归一化到相对秒数压到 32 位、4 遍）。 单线程常能比 std::sort 快 2–3 倍 ，因为它顺序访问、无比较、无分支误预测。这是唯一能明确打败 std::sort 的通用排序。 ④ 利用已有的有序性 日志天然按时间近似有序。先 std::is_sorted_until 探一下有序前缀有多长——如果数据是「几个有序段拼起来的」（多机日志合并的典型形态），那正确做法是 K 路归并 （O(n log K)）而不是重新排序（O(n log n)）。 ⑤ 最后才是并行 并行排序的加速比通常远低于核数： 合并步串行 （Amdahl 上限）、 内存带宽饱和 （排序是 memory-bound，多核同时搬数据会先撞带宽墙）、 负载不均 。而且内存占用更高。先算一笔账：5×10⁷ 个 int 单线程 std::sort 约 3 秒——如果 ②③ 已经把它压到 1 秒内，并行可能不必要。 还有一层：能不能不在这里排 如果这个接口反复对同一批数据排序，真正的答案可能是 把有序性前移 ：写入时就按时间分区落盘（时序数据库的做法）、或者建一个按时间戳的索引。 「反复排同一批数据」是个架构信号，不是算法问题。 参见 存储与数据架构 里时序数据库那一节。 ⬅️ 递归与分治 ➡️ 二分查找 📐 复杂度与分析 🧰 STL 算法库 📚 目录 🏠 主页 排序全景 — Ω(n log n) 下界 · introsort · 稳定性 · 外部排序 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/string_algorithms.html","t":"字符串匹配","c":"算法与数据结构","s":[{"a":"","h":"","x":"字符串匹配 · KMP 的 border 理论 / Z 函数 / Rabin-Karp 双哈希 / Manacher / AC 自动机 / 后缀结构 / ReDoS Kavin Tech Notes ← 主页 目录 30 min ◐ 字符串匹配 本质：复用已比较的信息 与其他章节的关系 朴素匹配与它的浪费 KMP 与 border 理论 border 是什么 next 数组的构造 匹配过程 border 的其他用途 Z 函数（扩展 KMP） Rabin-Karp 与滚动哈希 滚动哈希 碰撞与双哈希 它真正的价值 Manacher：线性求回文 AC 自动机：多模式匹配 后缀结构简介 后缀数组 后缀自动机与后缀树 工程场景 正则与 ReDoS grep / 全文检索 / diff 算法对照（交互） 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · String Matching · 2026 字符串匹配 共同主题是 「不要浪费已经比较过的信息」 。失配后朴素匹配从头再来；KMP 用 border 决定回退到哪；Z 函数复用已知匹配段；滚动哈希用一次加减乘更新指纹。六个算法是同一思想的六种实现。 核心难度 ★★★★☆ 面试出现率 中高 阅读时长 ~30 min 前置 哈希 · Trie · BFS border 理论 Z 函数 双哈希 Manacher AC 自动机 ReDoS"},{"a":"s1","h":"本质：复用已比较的信息","x":"01 本质：复用已比较的信息 六个算法，一条主线 算法 复用什么信息 复杂度 解决什么 朴素匹配 什么都不复用 O(nm) 基准 KMP 模式串的 border （前缀=后缀的最长长度） O(n+m) 单模式匹配 + 周期性问题 Z 函数 之前算出的 匹配段 [l, r] O(n) 「每个位置能和开头匹配多长」 Rabin-Karp 上一个窗口的 哈希值 O(n+m) 期望 多次比较、二维匹配、去重指纹 Manacher 已知回文的 对称性 O(n) 所有回文子串 AC 自动机 Trie + 失配指针 （KMP 的多串版） O(n + Σm + 命中数) 多模式同时匹配 后缀数组 / SAM 所有后缀的 有序关系 / 等价类 O(n log n) / O(n) 任意子串的复杂查询 读这张表时抓住一条： 已经比过的信息，能不能变成下一次比较的起点 。KMP / Z / Manacher 把答案存在「已知区间」里；滚动哈希把窗口收成一个整数；AC 和后缀结构把多串、全后缀预建成可转移的状态。选型先问「要复用什么」，再问复杂度。 核对长号码时，错了一位就从第一位重新念——这是朴素匹配。 KMP 说：已经对了 7 位，而这 7 位的前 3 = 后 3，所以退到第 3 位继续。「前缀和后缀重合多少」就是 border ，也是 KMP 的全部内容。 滚动哈希 走另一条路：不逐位比，给窗口算指纹；右移时减最左、加最右，一次加减乘而不是重算 7 位。 Z 函数 / Manacher 则记住「已经算出的一段」，新位置先抄对称处的答案再往外扩。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 哈希表内核 —— 滚动哈希的碰撞概率、双哈希、以及「输入可信吗」这个判据 树与平衡树 —— AC 自动机建在 Trie 上；后缀树是压缩 Trie 图遍历与拓扑排序 —— AC 自动机的失配指针用 BFS 构造 动态规划 —— 编辑距离、最长公共子串都是 DP；AC 自动机上还能做 DP 递归与分治 —— 正则回溯的爆栈与 ReDoS 搜索引擎基础 —— 倒排索引与全文检索的完整讨论"},{"a":"s2","h":"朴素匹配与它的浪费","x":"02 朴素匹配与它的浪费 先看清浪费在哪，才知道 KMP 省了什么 朴素匹配失配后 i 前进 1、 j 归零——刚才比对过的信息全部丢掉。最坏 AAAAAB 配 AAAB ：每个窗口都要把已经确认的 AAA 再比一遍。复杂度 O(nm)，在「模式几乎全是同一个字符」时打满。 s = \"AAAAAB\"，p = \"AAAB\" —— 朴素匹配的最坏情形 s A A A A A B i=0 A A A B 失配（比了 4 位） i=1 A A A B 又失配（又比 4 位） i=2 A A A B 匹配成功 浪费在哪 i=0 时已经知道 s[1..3] = \"AAA\"，但 i=1 时又把这三个 A 重新比了一遍 。KMP 的洞察： 这些信息本来就在手里 朴素匹配的浪费是「失配后把已经确认的部分信息全部丢掉」——而这些信息完全可以从模式串本身推出来。 关键洞察 ：失配时已经知道 s[i..i+j-1] == p[0..j-1] 。文本接下来长什么样， 完全等价于模式前 j 个字符的结构 ——后者可以预处理，与文本无关。KMP 就是把「失配后该退到哪」预先在模式串上算好：O(m) 预处理 + O(n) 扫描。预处理只依赖模式，同一个 p 扫多份文本时 next 可以复用。"},{"a":"s3","h":"KMP 与 border 理论","x":"03 KMP 与 border 理论 理解 border，KMP 就不用背了"},{"a":"s3-1","h":"3.1 border 是什么","x":"3.1 border 是什么 border （失配函数 / 前缀函数 / next）：既是 t 的 真前缀 又是 真后缀 的串。 next[i] = p[0..i] 的最长 border 长度。 例： p = \"ababa\" → next = [0, 0, 1, 2, 3] （整串的最长 border 是 \"aba\" ）。 border 的含义：p[0..i] 的最长「前缀 = 后缀」的长度 p = ababa a b a b a 前缀 \"aba\" a b a 后缀 \"aba\" —— 与前缀相同，长度 3 ⟹ next[4] = 3 失配时为什么能跳到 next[j−1] 已知 s 的最近 j 位 = p[0..j−1] 而 p[0..j−1] 的 后缀 \"aba\" = 前缀 \"aba\" ⟹ s 的最近 3 位已经匹配上了 p 的前 3 位，可以直接从 j=3 继续 border 的传递性（本篇最有用的一条） next[i] 的 border 也是 p[0..i] 的 border ⟹ 沿 next 链 i → next[i]−1 → … 能枚举出所有 border border 把「文本接下来的信息」变成「模式串自身的结构」——这是 KMP 的全部智慧。 失配回退：s = AAAAAB，p = AAAB，next = [0, 1, 2, 0] s A A A A A B i 停在这里 j=3 失配 A A A B A ≠ B，j = next[2] = 2 回退后 A A A B 前缀 AA 仍匹配，从 j=2 继续比 s[i] 和朴素匹配的差别 i 全程只前进；j 沿 next 链回退。 已确认的前缀不用重比 ——O(n) 就来自这里。 失配只动 j、不动 i。next 告诉你「已经对上的那段里，还有多长的前缀可以接着用」。"},{"a":"s3-2","h":"3.2 next 数组的构造","x":"3.2 next 数组的构造 构造就是「用 p 匹配自己」；匹配是同一套回退。势能 Φ = len （或匹配中的 j ）：外层每轮最多 +1（总增量 ≤ n），内层 while 每次让它严格减小且 ≥ 0，所以总减量也 ≤ n，两层循环仍是 O(m) / O(n)。和 双指针 、「维护已知区间」的 Z / Manacher 是同一套论证。 // next 构造 + 匹配：同一套回退。i / 外层下标只增不减 std::vector< int > build_next ( const std::string& p) { int m = p. size (); std::vector< int > nxt(m, 0 ); for ( int i = 1 , len = 0 ; i < m; ++i) { while (len > 0 && p[i] != p[len]) len = nxt[len - 1 ]; if (p[i] == p[len]) ++len; nxt[i] = len; } return nxt; } std::vector< int > kmp_search ( const std::string& s, const std::string& p) { auto nxt = build_next (p); std::vector< int > hits; int m = p. size (); for ( int i = 0 , j = 0 ; i < ( int )s. size (); ++i) { while (j > 0 && s[i] != p[j]) j = nxt[j - 1 ]; if (s[i] == p[j]) ++j; if (j == m) { hits. push_back (i - m + 1 ); j = nxt[j - 1 ]; } } return hits; }"},{"a":"s3-3","h":"3.3 匹配过程","x":"3.3 匹配过程 把「已匹配长度」看成自动机状态：匹配就沿绿边 +1；失配沿红虚线 fail（即 next[j-1] ）回退， i 不动。到达终态即命中，再沿 fail 找重叠匹配。这和真正的 DFA 一样：每个字符一次转移；区别是失配边在实现里往往用 while 沿 next 走，而不是把 256 条边都补全——AC 自动机的「补全」才把 while 消掉。 KMP 自动机直觉 · p = ABABC 状态 = 已匹配前缀长度 0 1 2 3 4 5 命中 A B A B C fail / next：1→0 2→0 3→1 4→2 （5 命中后再沿 fail 找重叠） 绿边吃一个字符前进；红虚线失配回退。 i 不走回头路，所以扫描是 O(n) 状态机视角：next 就是 fail 函数。找到匹配后写 j = nxt[j-1] 而不是 0，才能抓住重叠命中。两种 next 下标定义不要混用。 i 只前进 是 O(n) 的本质。 命中后 j = nxt[j-1] 才能找重叠（ \"aaaa\" 里找 \"aa\" 应得 3 个）；题目要求不重叠才归零。另一种 next（ next[0]=-1 、下标偏 1）也对， 不要混用 。"},{"a":"s3-4","h":"3.4 border 的其他用途","x":"3.4 border 的其他用途 有长 k 的 border ⟹ 整串右移 n−k 后重叠部分吻合 ⟹ n−k 是周期；若还能整除，就是整数次重复。拼接 a + '#' + b 把「两串关系」收成「一串的自身结构」（分隔符必须两串都不含）。 问题 用 border 怎么解 最小周期 n − next[n-1] ；能整除则是整数次重复 所有 border 沿 next 链枚举（border 的传递性） 重复的子字符串 n % (n − next[n-1]) == 0 且 next[n-1] > 0 最短回文添加 s + '#' + reverse(s) 的 next 末项 = 最长回文前缀 最长公共前后缀 a + '#' + b 的 next 末项 循环同构 在 s+s 里找 t 最小周期 = n − next[n−1] · \"abcabcabc\" n=9，next[8]=6 → 周期 3 a b c a b c a b c 周期 abc 周期 abc 周期 abc border 长度 6：前 6 = 后 6（abcabc） 同一套 next 还能做 所有 border：沿 next 链枚举 最短回文添加：s + '#' + reverse(s) 循环同构：在 s+s 里找 t 9 % 3 = 0 → 整数次重复 「abcabca」next 末项是 4，周期仍是 3，但 7 不能整除 3——最后一段不完整，不是整数次重复。"},{"a":"s4","h":"Z 函数（扩展 KMP）","x":"04 Z 函数（扩展 KMP） 比 KMP 更直观，很多题更好用 Z 函数 ： z[i] = s[i..] 与 s 本身的最长公共前缀长度（ z[0] 常取 n）。例： s = \"aabxaab\" → z = [7, 1, 0, 0, 3, 1, 0] 。 Z-box · s = aabxaab 已算出 z[4]=3，已知段 [l, r) = [4, 7) 前缀 a a b x z[0..2] 已算过 s a a b x a a b l=4 i=5 r=7 Z-box 对齐前缀 aab 算 z[5]：i 落在盒内，对称点是 i−l = 1 z[5] ← min(r−i, z[1]) = min(2, 1) = 1，再尝试往外扩。 盒外才真正比字符 KMP 构造、Z 函数、Manacher 是同一框架：维护已知区间 → 区间内复用 → 区间外扩展 → 势能证 O(n)。匹配时对 p+'#'+s 求 Z， z[i]==m 即命中。 // Z 函数：维护已知匹配段 [l, r)（Z-box） std::vector< int > z_function ( const std::string& s) { int n = s. size (); std::vector< int > z(n); z[ 0 ] = n; for ( int i = 1 , l = 0 , r = 0 ; i < n; ++i) { if (i < r) z[i] = std:: min (r - i, z[i - l]); while (i + z[i] < n && s[z[i]] == s[i + z[i]]) ++z[i]; if (i + z[i] > r) { l = i; r = i + z[i]; } } return z; } 势能 Φ = r ：while 每次成功都让 r 增大， r ≤ n 故 O(n)。对 p + '#' + s 求 Z，凡 z[i] == m 就是一次匹配。 情形 选谁 纯匹配 等价；KMP 不用拼接，省一点空间 每个位置与开头匹配多长 Z 函数（这就是定义） 周期性 / border KMP 的 next 好写好记 Z：「复用对称位置 + 暴力扩展」比 next 链直观"},{"a":"s5","h":"Rabin-Karp 与滚动哈希","x":"05 Rabin-Karp 与滚动哈希 它的价值不在单模式匹配"},{"a":"s5-1","h":"5.1 滚动哈希","x":"5.1 滚动哈希 多项式哈希 H(s[0..m-1]) = s[0]·B^(m-1) + … + s[m-1] (mod M) 滚动 H_new = (H_old − s[i]·B^(m-1)) · B + s[i+m] (mod M) 前缀 sub(l, r) = h[r+1] − h[l]·pw[r−l+1] 任意子串 O(1) 窗口右移：H = a·B³ + b·B² + c·B + d → 一次加减乘得到下个指纹 i=0 a b c d e 减 a·B³ 加 e i=1 a b c d e 前缀哈希更常用 任意子串相等变成 O(1) 整数比较 才能配二分做最长公共 / 重复子串 char 转 unsigned，否则非 ASCII 变负数 单模式匹配请用 KMP，哈希的价值不在这 定长滚动是特化；前缀哈希能取任意区间。细节与碰撞见 哈希表内核 。"},{"a":"s5-2","h":"5.2 碰撞与双哈希","x":"5.2 碰撞与双哈希 自然溢出（mod 2⁶⁴）可被 Thue-Morse 串打穿任意基数，竞赛里专门卡单哈希。防御按强度： 运行时随机基数 （挡住离线构造）→ 大质数模 + 随机 B （注意乘法溢出 / __int128 ）→ 双哈希 （两组 (B,M) 都相等才认）。生日悖论：mod 1e9+7 比较 10⁵ 对时碰撞约 0.5% ，双哈希后才可忽略。 不能只信哈希的时候 ：输入不可信（用户上传、外部 API）→ SipHash 或哈希命中后再真比较；用于去重 / 鉴权 / 内容寻址 → SHA-256、BLAKE3。竞赛或内部工具、输入可信 → 双哈希 + 随机基数够用。这和 哈希表 的 Hash-DoS 同一原理：平均情形可被攻击。"},{"a":"s5-3","h":"5.3 它真正的价值","x":"5.3 它真正的价值 单模式匹配请用 KMP（确定、无碰撞、不用模数）。哈希的价值是 把字符串比较变成 O(1) 整数比较 ，从而能用 set、二分、DP 状态、图节点标识。前缀哈希比定长滚动更常用：预处理 h[] 、 pw[] 后任意子串 O(1)，这才是最长公共 / 重复子串里「二分 + 判定」能成立的原因。字节必须转 unsigned char ，否则有符号 char 会把非 ASCII 变成负数。 场景 为何哈希比 KMP 合适 任意两子串是否相等 前缀哈希 O(1)；KMP 只回答「模式在哪」 二分 + 判定 任意长度子串比较：最长公共 / 重复子串 二维匹配 每行先哈希，再对行哈希序列做一维匹配 等长多模式 模式哈希进 unordered_set ，扫一遍文本 树哈希 / 指纹 结构编码成整数；SimHash 是相似不是精确"},{"a":"s6","h":"Manacher：线性求回文","x":"06 Manacher：线性求回文 复用「已知回文的对称性」 最长回文子串：中心扩展 O(n²)（ 2n−1 个中心 ：n 个字符 + n−1 个间隙）代码五行，n ≤ 1000 面试首选；二分 + 哈希 O(n log n) 能当场写。Manacher 的 O(n) 只在 n 到 10⁶、或需要 每个位置的半径 时才必要。插 # 把奇偶统一成奇数，首尾哨兵 ^ ≠ $ 让扩展自动停——和 柱状图最大矩形 的哨兵同一思路。面试策略：先写中心扩展，再口述「有 O(n) 的 Manacher，复用已知回文的对称性」。 回文中心 · \"abba\" 必须枚举 2n−1 个中心，漏掉间隙就找不到偶数回文 奇数 a b b a 中心在字符上 → \"a\" / \"b\" 偶数 a b b a 间隙中心 从 bb 的缝向外扩 → abba Manacher 的预处理 \"abba\" → ^ # a # b # b # a # $ 全部变成奇数回文，只扩一种 哨兵 ^ ≠ $，扩展碰到就停 插 # 后 p[i] 恰是原串回文长度 间隙中心处理 abba 这类偶数回文。n ≤ 1000 写中心扩展；现场硬写 Manacher 容易在哨兵和下标上翻车。 Manacher 复用：已知以 c 为中心、右界到 r 的大回文，内部像镜子 已知回文 [2c−r, r] c i′ i r i′ = 2c − i p[i] 起点 = min(r−i, p[i′]) 镜像结论只在大回文内部有效，所以要 min。 先复用再扩展 —— 和 Z-box 同一结构 不写完整实现：记住「镜像 + min(右界剩余, 对称半径)」就够在面试里讲清 O(n)。需要所有回文半径时才值得写；只求最长一条，中心扩展更稳。"},{"a":"s7","h":"AC 自动机：多模式匹配","x":"07 AC 自动机：多模式匹配 Trie + KMP 的失配指针 k 个模式同时扫一段文本：朴素是 k 次 KMP；AC 自动机是 Trie + fail ，O(n + Σm + 命中数)，文本只走一遍。 fail[v] 指向「是 v 的后缀、又是某模式前缀」的最长节点——KMP next 的多串版。空边补成 ch[fail[u]][k] 后，查询每个字符恰好一次转移。fail 依赖父节点，必须 BFS 按深度建。 Trie · 插入 he / she / his。公共前缀合并成一条路径 0 h s e* i h h s e i h he 终点 his 再向下 s* she 再向下 e* 查找 / 插入都是 O(键长)，与词库规模无关——这是和 BST / 哈希的本质差别。 AC 自动机建在这棵 Trie 上。词库与前缀匹配的工程形态见 树与平衡树 。 // Trie：最短 insert / search（节点 0 为根） void insert ( const std::string& w) { int u = 0 ; for ( char c : w) { int k = c - 'a' ; if (!ch[u][k]) { ch[u][k] = ch. size (); ch. push_back ({}); } u = ch[u][k]; } ++cnt[u]; } bool search ( const std::string& w) { int u = 0 ; for ( char c : w) { u = ch[u][c - 'a' ]; if (!u) return false ; } return cnt[u] > 0 ; } fail = KMP next 的多串版：指向「是自己的后缀、又是某模式前缀」的最长节点 0 h e* s h′ e* h e s h e fail：sh → h she → he（匹配 she 时顺带命中 he） BFS 按深度建 fail；空边补成 ch[fail[u]][k] 后查询不必 while 回退。 查询若沿 fail 链累加 cnt ，模式有 a, aa, aaa… 这类嵌套后缀时会退化成 O(节点数)。只要总数：按 BFS 序预累加 sum[v] = cnt[v] + sum[fail[v]] ，查询 O(1)。要每条模式的次数：查询只打标记，最后在 fail 树上自底向上加。fail 指针构成一棵树，AC 上的 DP 也走这棵树。 应用 要点 敏感词 / WAF / 序列搜索 一遍扫出全部命中，可同时替换 AC 上的 DP 「长 n 且不含任何模式」：自动机状态当 DP 下标 词库 / 输入法 双数组 Trie 等压空间变体；内存往往比时间更紧 热更新词库 新建自动机后原子切换指针，旧对象引用归零再释放"},{"a":"s8","h":"后缀结构简介","x":"08 后缀结构简介 从「找一个模式」到「任意子串的任意查询」"},{"a":"s8-1","h":"8.1 后缀数组","x":"8.1 后缀数组 sa[i] = 排名第 i 的后缀起点； height[i] = 相邻两后缀的 LCP。倍增 O(n log n) 好写，DC3 / SA-IS 是 O(n) 但复杂；height 用 Kasai O(n)。 任意两后缀的 LCP = height 上这两名次之间的区间最小值 ，配稀疏表就是 O(1)。两串最长公共子串要拼 a+'#'+b 再取 max(height)，且两端 来自不同串 ——漏了这个条件会得到串内重复，答案偏大。 后缀数组直觉 · s = banana sa 按字典序排后缀，height 是邻居的 LCP i sa[i] 后缀 height 0 5 a — 1 3 ana 1 (a) 2 1 anana 3 (ana) ← max 3 0 banana 0 4 4 na 0 5 2 nana 2 (na) 从图上直接读 最长重复子串 = max(height) = ana 不同子串个数 = n(n+1)/2 − Σ height 模式是否出现：SA 上二分 任意 LCP：height 的 RMQ 面试优先二分 + 哈希，SA 作补充 排名相邻的后缀才可能有最长公共前缀。不要手写 SA；能讲清这张图和 max(height) 就够。 问题 SA + height 模式是否出现 SA 上二分，O(m log n) 最长重复子串 max(height[i]) 不同子串个数 n(n+1)/2 − Σ height 两串最长公共子串 拼接后取 max(height)，且两端 来自不同串 （漏了会算成串内重复） 至少出现 k 次 height 上滑窗最小值 任意两后缀 LCP height 的 RMQ（稀疏表 O(1)）"},{"a":"s8-2","h":"8.2 后缀自动机与后缀树","x":"8.2 后缀自动机与后缀树 结构 本质 何时用 后缀数组 后缀有序 + LCP 首选：好写、常数小 后缀树 压缩后缀 Trie 理论漂亮，代码极长 SAM 识别全部子串的最小 DFA 出现次数等 SA 别扭的查询；状态 ≤ 2n−1 SAM 按 endpos 等价类建状态（出现位置集合相同的子串归一类），状态数 ≤ 2n−1、转移 ≤ 3n−4，所以能线性构造。link（parent tree）上做 DP 能算出现次数、不同子串个数。构造约 40 行且有「克隆节点」分支，初学先 SA——能解八成题，也好调。SAM 是「必须要它」时再学。面试几乎不手写：先给二分+哈希，再补一句「更多子串查询可预处理 SA」。"},{"a":"s9","h":"工程场景","x":"09 工程场景 正则、grep、检索、diff"},{"a":"s9-1","h":"9.1 正则与 ReDoS","x":"9.1 正则与 ReDoS 回溯型 （PCRE / Java / Python re / JS / .NET）：反向引用、前后查找都支持，最坏指数。 自动机型 （RE2、Rust regex ）：保证 O(n·m) 线性，但反向引用超出了正则语言，做不了。用户可配置正则时，这些强特性本来就不该开放。 灾难性回溯 · (a+)+ 配 aaaX：同一段 a 有多种切法，X 对不上就全试一遍 aaa|X a + aa aa + a a+a+a 全部撞上 X 共同特征 嵌套量词 (x+)+ / (x*)* 同一输入多种切法 最后才失配 25 个字符就能卡死 CPU 2019 Cloudflare 全球宕机就是 WAF 正则 ReDoS。和 Hash-DoS、递归解析爆栈、快排复杂度攻击、zip bomb 同一原理：攻击者控制的维度没有上界。防御先问「上界在哪，谁保证」。 // 典型灾难性回溯 /^(a+)+$/ 配一长串 a 再加 X /(a|aa)+$/ 配 aaaa…a! /^(\\s*\\w+)*$/ 配长空白加非法字符 防御 代价 换 RE2 / Rust regex 线性保证；失去反向引用 不让用户提供正则 改成受限查询语法，最安全 超时熔断 + 限长 纵深；许多引擎不能中断，可能要丢独立进程 静态扫描危险模式 safe-regex / recheck ，有假阳假阴"},{"a":"s9-2","h":"9.2 grep / 全文检索 / diff","x":"9.2 grep / 全文检索 / diff 系统 算法 GNU grep Boyer-Moore：从模式 尾部 比，失配可跳多格，平均亚线性 ripgrep 自动机型 regex + SIMD memchr ES / Lucene 倒排索引，查询阶段不做字符串扫描 LIKE '%x%' B+ 树用不上，要全文索引或 trigram git diff Myers：编辑图上的 BFS 优化 拼写纠错 编辑距离 + Trie 剪枝，或 BK-tree / SymSpell 近似去重 MinHash / SimHash，相似不是精确匹配 BM 和 KMP 哲学相反：KMP「一次一格、绝不回退」有最坏保证；BM 从模式 尾部 比，坏字符 / 好后缀一次可跳多格，模式越长、字符集越大跳得越远，平均亚线性，最坏仍可能 O(nm)（除非 Galil）。grep 面对的是大文件里找短模式，「跳过大段」比「最坏保证」更值钱。ripgrep 对字面量直接 SIMD 扫，常数往往赢过精巧算法。全文检索把「在文档里找词」变成「在倒排里查词」——预处理换查询，和 Contraction Hierarchies 同一思路。 LIKE '%x%' 的 % 在前，B+ 树前缀比较用不上，这是数据库慢查询的常见根因。"},{"a":"s9-3","h":"9.3 算法对照（交互）","x":"9.3 算法对照（交互） 五个字符串算法的复用机制与适用场景 点击算法名，看它复用什么信息、O(n) 怎么证、以及什么时候该用它。 KMP / border Z 函数 滚动哈希 Manacher AC 自动机"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 KMP 的 next 数组是什么含义？手写它的构造。 next[i] = p[0..i] 的 最长 border 的长度。而 border 是「 既是真前缀、又是真后缀 」的字符串。 例： p = \"ababa\" → next = [0, 0, 1, 2, 3] （ \"ababa\" 的最长 border 是 \"aba\" ，长度 3）。 std::vector< int > build_next ( const std::string& p) { int m = p. size (); std::vector< int > nxt (m, 0 ); for ( int i = 1 , len = 0 ; i < m; ++i) { while (len > 0 && p[i] != p[len]) len = nxt[len - 1 ]; if (p[i] == p[len]) ++len; nxt[i] = len; } return nxt; } next 有什么用（这才是重点） 失配时我们 已经知道 s[i-j..i-1] == p[0..j-1] 。而 p[0..j-1] 的后缀 = 它的前缀（长度 next[j-1] ）⟹ s 的最近这几位已经匹配上了 p 的开头几位 ⟹ 可以直接从 j = next[j-1] 继续，不必退到 0。 构造为什么是 O(m) 势能法：取 Φ = len 。外层每轮 len 最多 +1（总增量 ≤ m）；内层 while 每次让 len 严格减小； len ≥ 0 ⟹ 总减量 ≤ 总增量 ≤ m ⟹ 内层总次数 ≤ m。∎ 和双指针「两层循环仍是 O(n)」是同一套论证。 两种 next 定义不要混用 还有一种流行定义是下标偏移 1、 next[0] = -1 。两种都对，但 混用会导致下标错乱 。选一种记牢。 基础 KMP 的匹配过程为什么是 O(n)？ 因为 i （文本串指针）全程只前进，从不回退 ——这是它相对朴素匹配的本质区别。 for ( int i = 0 , j = 0 ; i < n; ++i) { // i 只增 while (j > 0 && s[i] != p[j]) j = nxt[j- 1 ]; // j 回退 if (s[i] == p[j]) ++j; if (j == m) { record (i - m + 1 ); j = nxt[j- 1 ]; } } 势能论证 取 Φ = j 。外层每轮 j 最多 +1 ⟹ 总增量 ≤ n。内层 while 每次让 j 严格减小，且 j ≥ 0 ⟹ 内层总执行次数 ≤ n 。 总代价 = n（外层）+ n（内层总量）= O(n) 。∎ 对比朴素匹配的浪费 朴素匹配失配后 i 前进 1、 j 归零—— 刚才比对的 j 位信息全部丢弃 。 s = \"AAAAAB\", p = \"AAAB\" ：i=0 时已知 s[1..3] = \"AAA\" ，但 i=1 时又把这三个 A 重新比了一遍。 而这些信息本来就在手里 。 两个实现细节 ① 找到匹配后写 j = nxt[j-1] （不是 j = 0 ），才能找到 重叠的匹配 （在 \"aaaa\" 里找 \"aa\" 应得 3 个）。题目要求不重叠才写 j = 0 。 ② KMP 分两阶段：O(m) 预处理模式串 + O(n) 扫文本串。 预处理只依赖模式串 ，所以同一个模式串匹配多个文本时可以复用 next 数组。 基础 什么是滚动哈希？它有什么风险？ 多项式哈希 H(s[0..m-1]) = s[0]·B^(m-1) + … + s[m-1] (mod M) 滚动（窗口右移一位） H_new = (H_old − s[i]·B^(m-1)) · B + s[i+m] (mod M) ↑ 减掉最左位 ↑ 左移 ↑ 加最右位 O(1) 完成，而不是重算 m 位 前缀哈希比滚动更常用 预处理 h[] 和 pw[] 后， 任意 子串的哈希是 h[r+1] − h[l]·pw[r-l+1] ，O(1)。 这让「比较任意两个子串是否相等」变成 O(1)，从而能配合 二分 解「最长公共子串」「最长重复子串」。 风险一：碰撞 单哈希 mod 1e9+7、比较 n 对字符串时，由生日悖论碰撞概率约 n²/(2M) —— n = 10⁵ 时约 0.5% ，不算低。 风险二：可被构造攻击 用「自然溢出」（mod 2^64）时，可以构造 Thue-Morse 串 使任意基数下都碰撞。竞赛里这被用来卡单哈希选手。 三种防御 ① 随机基数 （运行时随机取 B，而非固定 131）——挡住离线构造反例； ② 大质数模数 + 随机 B； ③ 双哈希 （两组不同的 (B, M)，都相等才认为相等）。 什么时候不能只信哈希 输入不可信 （用户上传、外部 API）→ 用带随机密钥的哈希，或 哈希相等后再做真实比较 。 用于安全决策 （去重、鉴权、内容寻址）→ 必须用密码学哈希或真实比较。 这和 Hash-DoS 是同一个原理：平均情形可被攻击。 另外别忘 (unsigned char) 强转—— char 有符号时非 ASCII 字节会变负数。 基础 求「最长回文子串」有哪几种方法？分别什么复杂度？ 方法 复杂度 代码量 适用 中心扩展 O(n²) 五行 n ≤ 1000， 面试首选 DP dp[i][j] O(n²) 时间 O(n²) 空间 中等 需要判断所有子串是否回文时 二分 + 哈希 O(n log n) 中等 思路直接，能当场写 Manacher O(n) 较长 n 很大，或需 所有位置的回文半径 中心扩展的要点 枚举中心（ 2n−1 个 ：n 个字符中心 + n−1 个间隙中心），向两边扩展。 「间隙中心」是处理偶数长度回文的关键 ，漏掉它会错过 \"abba\" 这类。 Manacher 的两个技巧 ① 插入分隔符统一奇偶 ： \"abba\" → \"^#a#b#b#a#$\" ，所有回文变成奇数长度，只需处理一种情况。 而且插入后 p[i] 恰好等于原串中回文的长度。 ② 首尾哨兵 （ ^ 和 $ ，互不相同）：让扩展 自动在边界停下 ，不需要越界判断。 和柱状图最大矩形的哨兵是同一思路。 Manacher 复用什么信息 已知「以 c 为中心、右边界到 r」是回文。要算 i（在 c 右边）时， 因为大回文内部对称 ，i 的镜像位置 2c−i 的结果已经算过，可以直接拿来当起点。 限制是 镜像结论只在大回文内部有效 → 取 min(r−i, p[2c−i]) 。 这个「先复用、再扩展」的结构和 Z 函数一模一样。 面试策略 先写中心扩展（五行、不易错），然后说「有 O(n) 的 Manacher，思路是复用已知回文的对称性」。 这比硬写 Manacher 更稳 ——现场写 Manacher 很容易在哨兵和下标上出错。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 怎么用 KMP 的 next 数组求字符串的最小周期？ 最小周期 = n − next[n−1] 若 n % (n − next[n−1]) == 0 且 next[n−1] > 0 ⟹ 整个串由这个周期 整数次 重复而成 直觉 如果 p[0..n-1] 有长度 k 的 border，说明「 前 k 个 = 后 k 个 」 ⟹ 把整个串 右移 n−k 位后，重叠部分完全吻合 ⟹ n−k 是一个「周期」（允许最后一段不完整）。 而当 n 能被 n−k 整除时，最后一段刚好完整 ⟹ 整个串是这个周期的 整数次重复 。 例子 \"abcabcabc\" ，n=9， next[8] = 6 （border 是 \"abcabc\" ）→ 最小周期 = 9−6 = 3 ，且 9 % 3 == 0 ⟹ 由 \"abc\" 重复 3 次。 \"abcabca\" ，n=7， next[6] = 4 → 周期 3，但 7 % 3 ≠ 0 ⟹ 不是整数次重复（最后一段不完整）。 border 的传递性（同样重要） next[i] 对应的 border 的 border 也是 p[0..i] 的 border ⟹ 沿 next 链 i → next[i]−1 → … 能枚举出所有 border 。 这用于「求所有周期」「统计每个前缀出现次数」等问题。 拼接技巧：把两串关系转成一串的自身结构 对 a + '#' + b 求 next，末尾值 = a 的后缀与 b 的前缀的最长匹配 。 同理可解：最短回文添加（对 s + '#' + reverse(s) 求 next）、循环同构判定（在 s+s 里找 t ）。 分隔符必须是两串都不含的字符 ，否则会产生跨界的假匹配。 中级 Z 函数是什么？它和 KMP 怎么选？ z[i] = s[i..] 与 s 本身的 最长公共前缀长度 。 例： s = \"aabxaab\" → z = [7, 1, 0, 0, 3, 1, 0] 。 for ( int i = 1 , l = 0 , r = 0 ; i < n; ++i) { if (i < r) z[i] = std:: min (r - i, z[i - l]); // 复用对称位置 while (i + z[i] < n && s[z[i]] == s[i + z[i]]) ++z[i]; // 暴力扩展 if (i + z[i] > r) { l = i; r = i + z[i]; } // 更新 Z-box } 它复用什么信息 维护一个已知匹配段 [l, r] （Z-box）——这段等于 s 的一个前缀。若 i 落在这段内， 它对应的信息在 z[i-l] 里已经算过 ，可以直接拿来当起点。 O(n) 的证明：势能 Φ = r ，内层 while 每次成功都让 r 严格增大， r ≤ n 。∎ 用 Z 函数做匹配 对 p + '#' + s 求 Z 函数，凡 z[i] == m 的位置就是一个匹配。 情形 选谁 纯匹配 等价；KMP 省一点空间（不用拼接） 「每个位置与开头匹配多长」 Z 函数 （这就是它的定义） 周期性 / border 相关 KMP 的 next 好写好记 Z 函数 （「复用对称位置 + 暴力扩展」比 next 链回退直观） 三个算法共用一套框架 KMP 构造、Z 函数、Manacher 都是「 维护一个已知区间 + 在区间内复用 + 区间外暴力扩展 + 势能法证 O(n) 」。 认出这个框架，三个算法就变成了一个模式的三种实例。 中级 AC 自动机怎么工作？fail 指针和 KMP 的 next 什么关系？ AC 自动机 = Trie + fail 指针 ，解决「k 个模式串同时匹配一个文本串」，复杂度 O(n + Σmᵢ + 命中数) ——文本只扫一遍（对比「对每个模式串跑一次 KMP」的 O(k·n)）。 fail 指针 = KMP 的 next 的多串版 fail[v] 指向「 是 v 所代表字符串的后缀，且是某个模式串的前缀 」的最长节点。 作用完全等同于 KMP 失配后的回退——只不过 KMP 在一个串上回退，AC 自动机在整棵 Trie 上回退。 用 BFS 构造 + 「补全」Trie if (v) { fail[v] = ch[fail[u]][k]; q. push (v); } else { v = ch[fail[u]][k]; } // ← 补全：空边指向 fail 的对应边 为什么用 BFS ： fail[v] 依赖 fail[父节点] ，所以必须 按深度从小到大 处理。 「补全」的价值 ：查询时 不需要 while 回退 ——每个字符恰好一次状态转移。这一步把「Trie + fail」真正变成了「自动机」。 查询的复杂度陷阱 for (int t = u; t; t = fail[t]) total += cnt[t]; 这个内层循环最坏是 O(节点数)。模式串有大量嵌套后缀（ a, aa, aaa, … ）时会退化。 优化 ：只需总数时，把 cnt 沿 fail 树 预先向上累加 （ sum[v] = cnt[v] + sum[fail[v]] ，按 BFS 序），查询变 O(1)；需要每个模式串的次数时，查询只打标记，最后在 fail 树上做一次自底向上累加。 「fail 指针构成一棵树」是重要性质 很多进阶应用基于它：fail 树上的 DP、子树求和、 AC 自动机上的 DP （「构造长度 n 的串使不含任何模式串」→ 在自动机状态上做 DP）。 典型应用 ：敏感词过滤、WAF 规则匹配、生物序列多模式搜索、输入法词库。 中级 怎么求「两个字符串的最长公共子串」？（注意是子串不是子序列） 先区分清楚 ：子串（substring）必须 连续 ；子序列（subsequence）可以不连续。这两个问题的解法完全不同。 方法 复杂度 要点 DP dp[i][j] O(nm) dp[i][j] = 以 a[i] 、 b[j] 结尾 的公共子串长度；不等时 置 0 （不是取 max！） 二分 + 哈希 O((n+m) log) 二分长度 L，把 a 的所有长 L 子串哈希放 set，查 b 的 后缀数组 O(n log n) 拼接后求 max(height[i]) ，且 sa[i-1] 、 sa[i] 来自不同串 后缀自动机 O(n) 对 a 建 SAM，用 b 在上面跑 DP 版最常见的错 字符不相等时 必须置 0 （ dp[i][j] = 0 ），而不是像 LCS（最长公共 子序列 ）那样取 max(dp[i-1][j], dp[i][j-1]) 。 因为子串要求连续 ——一旦断开就得重新开始。 这一行的差别就是「子串 vs 子序列」的全部区别。 二分 + 哈希是面试的最佳答案 思路直接（「存在长度 L 的公共子串」关于 L 单调 → 可以二分），代码短，能当场写出来。 但要说明单调性 ：如果存在长度 L 的公共子串，那它的任意子串（长度 < L）也是公共子串 ⟹ 单调成立。 后缀数组版的易漏条件 拼接 a + '#' + b 后求 max(height[i]) ， 必须同时要求 sa[i-1] 和 sa[i] 来自不同的原串 。 漏掉这个条件会得到「同一个串内部的重复」， 答案偏大 。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 什么是 ReDoS？线上有一条用户可配置的正则，怎么防？ ReDoS （正则拒绝服务）： 回溯型 正则引擎在某些模式 + 输入组合下会指数爆炸。 // 典型的「灾难性回溯」 /^(a+)+$/ 配 \"aaaaaaaaaaaaaaaaaaaaaaaaX\" /(a|aa)+$/ 配 \"aaaa…a!\" /^(\\s*\\w+)*$/ 配一长串空格加非法字符 共同特征 嵌套的量词 （ (x+)+ 、 (x*)* ）+ 有多种方式匹配同一段输入 + 最后失配 。 引擎必须尝试所有拆分方式，数量指数级。 25 个字符就能让 CPU 跑几秒。 2019 年 Cloudflare 的全球宕机就是一条 WAF 正则引发的 ReDoS。 两种引擎的本质差别 回溯型 （PCRE、Java、Python re 、JS、.NET）：支持反向引用等强特性，但 最坏指数 。 自动机型 （RE2、Rust regex ）： 保证 O(n·m) 线性 ，但 不支持反向引用 （那超出了正则语言的表达能力）。 防御，按优先级： ① 换自动机型引擎（根治） RE2 / Rust regex 。代价是失去反向引用、前后查找等特性—— 但如果正则来自用户，这些特性本来就不该开放 。 ② 不让用户提供正则（更根治） 改成 受限的查询语法 （如只支持前缀/后缀/包含），后端翻译成安全的匹配。功能受限但最安全。 「用户可配置正则」本身就是一个值得质疑的需求。 ③ 纵深防御（应该同时做） 超时 + 熔断 （但很多引擎不支持中断，可能要放到独立进程/线程里跑）； 限制输入长度 ； 静态扫描危险模式 （ safe-regex 、 recheck 等工具，有假阳性但能挡住明显的）。 这和其他几类漏洞是同一个原理 「某个资源维度没有上界，而它由攻击者控制」 ： · Hash-DoS （碰撞把 O(1) 拖成 O(n)） · 递归解析爆栈 （深度嵌套 JSON） · 快排复杂度攻击 （构造锯齿输入） · zip bomb（解压比无上限） 共同防御思路：对每个资源维度问「上界在哪，谁保证」。 高级 为什么 GNU grep 用 Boyer-Moore 而不是 KMP？ 因为 Boyer-Moore 的平均复杂度是「亚线性」的 ——它可以 跳过 文本中的字符而不检查，而 KMP 必须逐个字符看过去。 核心差别：从尾部开始比 BM 从模式串的 尾部 开始比较 。失配时用两条规则决定跳多远： 坏字符规则 ：文本里那个不匹配的字符，如果在模式串里根本不存在 → 直接跳过整个模式串长度 。 好后缀规则 ：已匹配的后缀在模式串里还出现在别处 → 对齐到那个位置。 模式串越长、字符集越大，跳得越远 —— 平均可以做到 O(n/m)。 对比 KMP 的哲学 KMP： 「一次前进一格，但绝不回退」 ——O(n) 有确定性保证，且预处理只依赖模式串。 BM： 「尽可能一次跳很远」 ——平均亚线性，但 最坏仍是 O(nm) （除非用 Galil 规则改进）。 grep 面对的是「在大文件里找短模式」，此时「跳过大段文本」的收益远大于「最坏保证」。 现代实现走得更远 ripgrep ：Rust regex （自动机型，线性保证）+ SIMD 加速的字面量搜索 （ memchr 一次比较 16–32 字节）。 对「查找一个固定字符串」这个最常见的情形， SIMD 暴力扫描往往打败所有精巧算法 ——因为它把「每字符一次比较」变成「每 32 字节一条指令」。 这又是「渐进相同、常数决定胜负」的例子。 更高层的洞察：全文检索根本不做字符串匹配 ES / Lucene 把「在文档里找词」变成「 在倒排索引里查词，得到文档列表 」—— 预处理换查询速度 ，和 Contraction Hierarchies 是同一思路。 而 LIKE '%x%' 用不了 B+ 树索引 （B+ 树只支持前缀比较）→ 要用全文索引（倒排）或 trigram 索引 （把字符串切成三字组）。 这是数据库慢查询的常见根因。 高级 要做一个敏感词过滤系统，怎么设计？ 核心算法是 AC 自动机 （Trie + fail 指针），一遍扫描找出所有命中。但真实系统的难点 几乎都不在算法上 。 ① 算法层：AC 自动机 + 预累加 建 Trie → BFS 构造 fail 并补全 → 文本扫一遍。 关键优化 ：把 cnt 沿 fail 树预先向上累加，查询时 O(1) 而不是沿 fail 链走。否则词库里有 a, aa, aaa 这类嵌套后缀时会退化。 词库几万到几十万条时，内存是主要约束 → 用 双数组 Trie 或按字符集压缩节点。 ② 真正的难点是「变体绕过」 攻击者会用： 拼音替换 、 形近字 （O↔0、l↔1）、 插入无意义字符 （「敏*感*词」）、 全角/半角 、 繁简转换 、 零宽字符 、 emoji 分隔 。 解法是「归一化预处理」 ：统一转小写、全角转半角、繁转简、删除零宽字符和标点、可选地转拼音。 把归一化做好比优化算法重要十倍。 ③ 第二个难点是「误杀」 「三个代表」「毛主席」这类词在正常语境里合法。纯字符串匹配无法区分语境。 解法 ： 白名单 （精确短语豁免）、 上下文窗口 （周围出现某些词则放行）、 分级 （高危直接拦、中危送人审、低危只记录）。 「宁可漏一个也不能误杀一片」还是反过来，是业务决策不是技术决策——必须先问清。 ④ 工程要求 热更新词库 ：不能重启服务 → 构建新自动机后 原子指针切换 （旧的等引用归零再释放）。 性能 ：AC 自动机是 O(n)，但归一化和多套索引（原文/拼音/形近）会乘上常数。要压测 最坏输入 （超长文本、大量命中）。 可观测性 ：记录命中了哪些词、误杀申诉率—— 这些数据是调优词库的唯一依据 。 ⑤ 什么时候该换方案 如果绕过手段持续升级，字符串匹配会陷入「打补丁」的军备竞赛。此时应该 补一层语义模型 （文本分类 / embedding 相似度），用「字符串匹配挡明显的、模型挡变体的」两层结构。 但字符串匹配层不能去掉 ——它快、确定、可解释、可审计，这些是模型给不了的。 「用确定性方法挡住已知的，用模型兜住未知的」是内容安全的通用架构。 高级 面试被问「最长重复子串」，你会怎么答？ 先问清定义 ：「重复」是指 出现至少两次 吗？允许 重叠 吗？要返回长度还是具体串？——这些会影响解法。 最佳面试答案：二分 + 哈希，O(n log n) 思路 ：二分答案长度 L，判定「是否存在长度 L 的子串出现两次」——用前缀哈希把所有长 L 子串的哈希放进 unordered_set ，出现重复就说明可行。 单调性论证 （必须说）：若存在长度 L 的重复子串，则它的任意长度 < L 的子串也重复出现 ⟹ 可行性关于 L 单调 ⟹ 可以二分。 优势 ：思路直接、代码二十行、能当场写完、能讲清正确性。 补充：后缀数组，O(n log n) 求出 SA 和 height 数组后， 答案就是 max(height[i]) ——因为排名相邻的两个后缀有最长的公共前缀。 要「至少出现 k 次的最长子串」→ height 数组上的 滑动窗口最小值 （窗口大小 k−1）。 说出这个能体现知识广度，但不要主动提议现场手写 SA。 如果不允许重叠 二分 + 哈希时要额外记录每个哈希值 最早出现的位置 ，要求两次出现的位置差 ≥ L。 后缀数组版则需要在 height 上滑窗时同时维护 max(sa) 和 min(sa) ，要求差 ≥ L。 这个条件很容易漏。 哈希方案要说明碰撞风险 单哈希 mod 1e9+7、n = 10⁵ 时碰撞概率约 0.5%（生日悖论）。 要提一句「用双哈希或随机基数降低碰撞」 ，或者「哈希相等后再做一次真实比较」。 主动提出这一点比等面试官问更好——它说明你知道哈希方案的风险边界。 面试策略总结 ① 先问清定义（重叠？次数？返回什么）； ② 给出能当场写的方案（二分+哈希）并说明单调性； ③ 主动提碰撞风险和缓解； ④ 补充「如果需要更多子串查询，可以预处理后缀数组，答案是 max(height)」。 这个结构（问清 → 可实现方案 → 风险 → 更优方案的存在性）适用于绝大多数算法面试题。 ⬅️ 贪心与回溯 🌲 树与平衡树（Trie） 🔑 哈希表内核 🔍 搜索引擎基础 📚 目录 🏠 主页 字符串匹配 — KMP 的 border 理论 · Z 函数 · Rabin-Karp 双哈希 · Manacher · AC 自动机 · 后缀结构 · ReDoS — Kavin Tech Notes · 2026"}]},{"u":"algorithms/trees_and_balanced_trees.html","t":"树与平衡树","c":"算法与数据结构","s":[{"a":"","h":"","x":"树与平衡树 · 三序遍历 / BST 有序 / AVL 与红黑旋转 / B 树阶 / LCA 倍增 / 树形 DP Kavin Tech Notes ← 主页 目录 30 min ◐ 树与平衡树 本质：用层级换对数 与其他章节的关系 二叉树遍历 三种递归序 二叉搜索树 插入与查找 为什么会退化 平衡的两种流派 旋转 AVL：严格平衡 红黑树：放松平衡 对照与选型 红黑树在工程里 五条性质保证了什么 std::map / epoll / CFS B 树与 B+ 树 又矮又胖的动机 B 与 B+ 的差别 跳表：用随机代替旋转 最近公共祖先 倍增骨架 选型与树形 DP 结构对照（交互） 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Trees · 2026 树与平衡树 树的价值是 把 n 个元素叠成 log n 层，每次操作只走一条路径 。保矮有四条路：AVL 卡高度差、红黑树卡颜色、B+ 把节点加宽、跳表用随机层高。遍历 / BST / 旋转用图讲；AVL 与红黑 不写完整实现 。LCA 留倍增骨架；树形 DP 转到 动态规划 。 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~30 min 前置 递归 · 复杂度 三序遍历 BST 有序 旋转 / AVL / 红黑 B 树阶 LCA 倍增 树形 DP"},{"a":"s1","h":"本质：用层级换对数","x":"01 本质：用层级换对数 以及「保证 log n 层」的四条路 线性扫描是 O(n)。树引入 层级 ：每下一层丢掉一大块候选，复杂度变成 O(树高)。「树高 = log n」不是免费的——二叉搜索树最坏会退化成链，此时比数组还差。核心问题不是查找（那很简单），而是 插删之后树还矮不矮 。 同一组 {1,2,3,4,5,6,7}：形状决定树高 按序插入 → 链 1 2 3 ⋮ h = n 平衡后 → 完全二叉树 4 2 6 1 3 5 7 h = ⌊log₂ n⌋ 四条保矮的路 AVL：高度差 ≤ 1 红黑：颜色约束 B+：节点变宽 跳表：随机层高 查找永远走一条根到叶的路。生产里必须保证这条路是 O(log n)，而不是 O(n) 的链。 四种答案只争一件事：插删后如何保矮。 AVL ：左右高度差 ≤ 1，靠旋转。 红黑树 ：黑高相等 + 无连续红，h ≤ 2 log(n+1)。 B / B+ ：节点变宽，树高 log_B n（磁盘上 3–4 层）。 跳表 ：随机层高，期望 log n，无旋转。 AVL 是最严的 HR：两部门层级差不能超过 1，一超就重组——查询最快、重组最勤。 红黑树 允许临时工，但不能连续两个，正式编制人数沿每条路相同——查询稍慢，重组少。 B+ 把每个部门做到一页（几百 key），跨页沟通（磁盘）比页内贵一千倍。 跳表 入职掷骰子定级，期望自然是对数。 四种策略只争一件事：插删之后树还矮不矮。朴素 BST 在按序插入时会变成链，比数组还差——既是 O(n) 又有指针追逐。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 递归与分治 —— 遍历是递归最自然的场景 动态规划 · 树形 DP —— 后序把子树答案合并到父节点 并查集与区间结构 —— 线段树、树状数组是为区间查询设计的树 二分查找 —— B+ 页内二分、跳表层级跳跃 堆与 Top-K —— 堆也是树，但不维护全序 STL 容器 —— std::map 的红黑树与迭代器失效 存储与数据架构 —— B+ / LSM / 跳表在数据库里"},{"a":"s2","h":"二叉树遍历","x":"02 二叉树遍历 三种序只差 visit 的位置；选序看信息往哪流"},{"a":"s2-1","h":"2.1 三种递归序","x":"2.1 三种递归序 同一棵树 · 三种序 = visit 相对左右递归的位置 1 2 3 4 5 先序 根→左→右 1 2 4 5 3 信息从上往下传 中序 左→根→右 4 2 5 1 3 BST 上是有序序列 后序 左→右→根 4 5 2 3 1 信息从下往上收 选序判据 路径和 / 序列化 / 深拷贝 → 前序（参数往下传） 高度 / 删除整棵 / LCA / 子树聚合 → 后序（返回值往上收） 直径：后序收高度 + 一个引用记全局。层序 = BFS。 先序把根印在左右之前，中序夹在中间，后序印在最后。BST 的中序就是排序。 // 三种序的区别只在 visit(root) 的位置 void preorder (Node* r) { if (!r) return ; visit (r); preorder (r->l); preorder (r->r); } void inorder (Node* r) { if (!r) return ; inorder (r->l); visit (r); inorder (r->r); } void postorder (Node* r) { if (!r) return ; postorder (r->l); postorder (r->r); visit (r); } 序 信息方向 典型题 前序 从上往下 序列化、路径和、深拷贝 中序 BST 有序 验证 BST、第 k 小 后序 从下往上 高度、LCA、子树聚合、树形 DP 层序 按层 最小深度、每层最大值 选序的判据只有一条： 信息是从上往下给的，还是从下往上收的？ 路径和、序列化、深拷贝是参数往下传 → 前序。高度、是否平衡、子树节点数、LCA、树形 DP 是返回值往上收 → 后序。直径两者都要：后序收高度，再用引用记全局最大值。BST 上要有序序列才用中序。层序是 BFS，按距根的层数处理。 迭代版把递归栈显式化即可：前序一次入栈，后序每个节点入栈两次（第一次展开、第二次访问）。 expanded 标记就是程序计数器。Morris 用空右指针当临时线索换 O(1) 空间，但 遍历中改树 ——不能并发、visit 抛异常会留下坏树、树必须可写。一般用显式栈（O(h)，平衡时就是 O(log n)）。"},{"a":"s3","h":"二叉搜索树","x":"03 二叉搜索树 左 < 根 < 右；中序即有序；按序插入会退化"},{"a":"s3-1","h":"3.1 插入与查找","x":"3.1 插入与查找 BST：每个节点都满足「左子树所有值 < 根 < 右子树所有值」 4 2 6 1 3 5 7 中序：1 2 3 4 5 6 7 查找 5：4 → 6 → 5 只比父子会漏全局 5 的左孩子 3 < 5，3 的右孩子 6 > 3，局部都对 但 6 在 5 的左子树里 → 不是 BST 验证要传 (min,max) 区间，或中序检查递增 删除：叶子直接摘；单孩子用孩子顶替； 双孩子用中序后继（右子最左）的值顶替再删它 有序性是整棵子树的约束，不是「左孩子 < 根」。查找和插入都只走一条路。 // 查找 / 插入都只走一条路；相等则查找命中、插入为 no-op Node* find (Node* r, int k) { while (r && r->val != k) r = k < r->val ? r->l : r->r; return r; } Node* insert (Node* r, int k) { if (!r) return new Node(k); if (k < r->val) r->l = insert (r->l, k); else if (k > r->val) r->r = insert (r->r, k); return r; } 删除三种情形：叶子直接摘；单孩子用孩子顶替（子树整体上移，仍是 BST）；双孩子不能直接拆——找 中序后继 （右子树最左）或前驱（左子树最右），把值拷到当前节点，再递归删它。后继没有左孩子，递归必落到前两种，不会无限展开。总用后继会让树偏向一侧；平衡树实现里常按子树高度选侧。 情形一 · 叶子 5 7 父指针置空 情形二 · 单孩子 5 8 孩子整体上移 情形三 · 两个孩子 直接删会留下两棵孤立子树 中序后继 = 右子树最左节点 把它的值拷过来，再递归删它 后继最多一个孩子 → 落到一或二 验证 BST：传 (min,max)，或中序检查递增 双孩子删除的正确性：后继是「比当前值大的最小者」，放回去仍满足左 < 根 < 右。"},{"a":"s3-2","h":"3.2 为什么会退化","x":"3.2 为什么会退化 按序插入 1,2,…,n 得到右链，所有操作变 O(n)。自增 ID、时间戳、排好序的导入都是这种输入。平均 O(log n) 依赖随机顺序，可被构造攻击（与快排平均复杂度同一类假设，见 复杂度与分析 ）。朴素 BST 生产上基本不可用。 思路 代表 保证 插删后主动调整 AVL、红黑树 最坏 O(log n) 随机化结构 跳表、Treap 期望 O(log n) 按访问频率调整 Splay 摊销 O(log n) 加宽节点 B / B+ 最坏 O(log_B n)"},{"a":"s4","h":"平衡的两种流派","x":"04 平衡的两种流派 旋转保中序；AVL 卡高度差；红黑卡颜色。不写完整实现"},{"a":"s4-1","h":"4.1 旋转","x":"4.1 旋转 旋转： 中序序列不变，只改形状 。所以旋转后仍是合法 BST。 右旋前 y x A B C 中序：A x B y C 右旋 右旋后 x A y B C 中序：A x B y C 不变 改 3 个 指针： y->l=B x->r=y 返回 x 左旋对称。AVL 还要更新高度；红黑树还要改颜色。下面不写完整的插删修复。 // 右旋：把左孩子提为根。左旋对称。不是完整 AVL/红黑实现 Node* rotate_right (Node* y) { Node* x = y->l; y->l = x->r; x->r = y; return x; } 失衡 形态 旋转 LL 左子的左子过高 一次右旋 RR 右子的右子过高 一次左旋 LR 左子的右子过高 左子先左旋，再右旋 RL 右子的左子过高 右子先右旋，再左旋 记法： 失衡方向决定旋转方向；两个字母不同就先转成相同。 LL/RR 一次转，LR/RL 先把「拐弯」转直再转。插入最多两次旋转就能复衡；删除在 AVL 里可能沿路径一路转。 四种失衡（高的那一侧用暖色标出） LL 右旋 RR 左旋 LR 左子先左旋 再右旋 RL 右子先右旋 再左旋 两个字母相同 = 单旋；不同 = 先把内侧转成外侧，再单旋。"},{"a":"s4-2","h":"4.2 AVL：严格平衡","x":"4.2 AVL：严格平衡 不变式：每个节点 BF = h(左) − h(右) ∈ {−1, 0, +1} 。斐波那契最坏形状给出 h ≈ 1.44 log₂(n+2)，非常接近完美平衡的 log₂ n。|BF| = 2 就按上表旋转。插入从新叶往上走，碰到第一个 |BF|=2 的节点转一次（或两次）即可，因为旋转后整条路径复衡。删除可能把失衡传到父亲，最坏一路转到根。 AVL：节点上的数字是平衡因子 BF = hL − hR 插入 1 后 y 的 BF = +2（LL） y +2 x +1 1 C 0 hL=2 hR=1 右旋 修复后 |BF| ≤ 1 x 0 1 y 0 C h ≈ 1.44 log n · 删除可能沿路径一路旋转 |BF|≤1 让 AVL 最矮；代价是删除最坏 O(log n) 次旋转。"},{"a":"s4-3","h":"4.3 红黑树：放松平衡","x":"4.3 红黑树：放松平衡 不直接卡高度，用颜色间接卡。真正干活的是性质 4（无连续红 → 红最多占一半层级）和 5（黑高相等 → 最短路 ≥ 黑高），合起来 h ≤ 2 log₂(n+1)。性质 1–3 把根和 NIL 都定为黑，让黑高定义干净、插入情形更少。 红黑性质：无连续红 + 黑高相等 ⇒ h ≤ 2 log₂(n+1) 11 黑 2 红 14 黑 1 7 15 NIL NIL NIL 五条性质 1. 节点非红即黑 2. 根是黑 3. NIL 叶是黑 4. 红节点孩子必须黑（无连续红） 5. 任一节点到后代 NIL 黑高相同 4 ⇒ 红最多占一半层级 5 ⇒ 最短路 ≥ 黑高 ⇒ h ≤ 2·bh 「两倍」松弛换来删除最多 3 次旋转（其余改颜色）。这是比 AVL 改得快的原因。 高度上界 黑高 bh：性质 5 ⇒ 任何路径至少 bh 个黑节点；性质 4 ⇒ 路径长度 ≤ 2·bh。 黑节点至少构成完美二叉树 ⇒ n ≥ 2^bh − 1 ⇒ bh ≤ log₂(n+1)。 ∴ h ≤ 2·bh ≤ 2 log₂(n+1) 性质 4 和 5 像「正式员工 + 实习生」：每条路经过的正式编制人数必须相同（组织不会一支特别深）；实习生下面不能再带实习生（临时工最多占一半层级）。最深最多是最浅的两倍——这份松弛让红黑树少转很多圈。"},{"a":"s4-4","h":"4.4 对照与选型","x":"4.4 对照与选型 AVL 红黑树 高度 ≈1.44 log n 2 log n 查找 更快 稍慢 插入旋转 ≤ 2 ≤ 2 删除旋转 最坏 O(log n) ≤ 3 额外存储 高度或 2 bit BF 1 bit 颜色 使用者 读多写少的内存索引 std::map 、CFS、epoll 读远多于写 → AVL（树矮约 30%，查找更快）；读写均衡或写多 → 红黑（最坏修改代价更小）。标准库选红黑，因为通用容器无法预知读写比。 完整插删修复不在本页展开 ——记住旋转表和这两条不变式，能讲清取舍即可。"},{"a":"s5","h":"红黑树在工程里","x":"05 红黑树在工程里 选它几乎从不是因为「查找最快」"},{"a":"s5-1","h":"5.1 五条性质保证了什么","x":"5.1 五条性质保证了什么 工程愿意接受 2 log n 而不是 1.44 log n，是因为 修改代价可控 。这张表里没有一条理由是「查找最快」。真实理由分三类：要有序 / 范围查询（哈希做不到）；要确定性延迟（哈希会 rehash 抖）；要迭代器 / 引用稳定。三条都不需要，就该用哈希表。 场景 真实理由 std::map / set 插入不失效迭代器；删除只失效被删的那个 Linux CFS 按 vruntime 频繁插删 + 取最小；最左节点可缓存成 O(1) epoll 兴趣列表 fd 增删频繁，要 O(log n) 确定性（哈希会 rehash 抖） 内核 VMA 范围查询：「地址落在哪个 VMA」 nginx 定时器 取最近到期 + 频繁取消（堆找不到任意元素）"},{"a":"s5-2","h":"5.2 std::map / epoll / CFS","x":"5.2 std::map / epoll / CFS libstdc++ 把颜色存在独立字段（可移植、好调试）；内核把颜色塞进 parent 指针最低位（节点 4 字节对齐，低 2 位必为 0）。通用库和内核的约束不同，同一结构会做出不同取舍。 n 较小时（几十到几百）map 常比哈希表快：树的上层节点被反复访问、常驻 cache；哈希要先算 hash（string 键扫全串）再做一次几乎必 miss 的随机访存。交叉点大约在 n 几百到一千，依赖 key 类型。小容器上先别急着换。如果数据只读且要有序，排序数组 + lower_bound 比任何树都快（连续内存、无每节点分配）。"},{"a":"s6","h":"B 树与 B+ 树","x":"06 B 树与 B+ 树 阶决定扇出；磁盘上一次跳转比一次比较贵一千倍"},{"a":"s6-1","h":"6.1 又矮又胖的动机","x":"6.1 又矮又胖的动机 内存跳转 ~100 ns（一次 cache miss），磁盘跳转 ~100 µs（SSD）～ 10 ms（HDD）， 差一千倍以上 。优化目标从「少比较」变成「少跳转」。节点做成一个磁盘页（4–16 KB），每页放几百个 key：页内二分 8–10 次比较全在内存里，换掉的是二十多次磁盘 I/O。BST：log₂(10⁹)≈30 次 I/O；B+（B≈500）：3–4 次。这是 二分查找 里「两层不同代价单位」的完整版本。 阶 m：每个节点最多 m 个孩子、m−1 个 key（图中 m=4） 10 20 30 内部 2 5 8 12 15 18 22 25 28 32 35 40 B+ 叶子存全部数据，内部只存分隔 key → 扇出更大、树更矮 叶子链表 → 范围扫描是顺序 I/O BST 30 次页跳 vs 阶 500 的 B+ 只需 3~4 次。页内再二分，比较很便宜。 阶越大扇出越大、树越矮。B+ 把数据全放叶子并串成链表，所以数据库索引几乎都用它。"},{"a":"s6-2","h":"6.2 B 与 B+ 的差别","x":"6.2 B 与 B+ 的差别 B 树 B+ 树 数据 内部也存 只有叶子存 扇出 较小 更大 → 更矮 叶子 无连接 有序链表 范围扫描 回树中序 顺序 I/O B+ 胜在 BETWEEN / ORDER BY / GROUP BY ：叶子链表让范围扫描变成顺序 I/O，磁盘上比随机 I/O 快一到两个数量级。B 树「单点查找可能在内部节点命中」的优势相比之下微不足道，还让树变高了。 覆盖索引 = 查询所需列都在索引叶子里，不用回表（回表 = 又一轮 B+ 查找 + 随机 I/O）。写密集会页分裂、随机写 → LSM 把随机写变顺序写（内存表 + 顺序落盘 + 后台 compaction），代价是读放大和写放大。「B+ vs LSM」本质是读优化 vs 写优化，见 存储架构 与 关系型数据库 。"},{"a":"s7","h":"跳表：用随机代替旋转","x":"07 跳表：用随机代替旋转 同样的 O(log n)，代码只有平衡树的三分之一 跳表是多层有序链表：上层稀疏、跳得远，底层是完整序列。每个节点插入时 掷硬币决定层高 （p=1/2 或 1/4），期望层高 log n，期望查找 O(log n)， 完全不需要旋转 。「保持平衡」被外包给随机数，代价是保证从「最坏 O(log n)」降成「期望 O(log n)」——攻击者控不了你的骰子，这和 Treap 是同一类思路。 跳表：多层有序链表，上层跳得远，逐层下降逼近目标 L3 L2 L1 head 42 61 查找 61：L3 从 head 跳到 42（<61，前进）→ 42 后面是 NIL，下降 L2 从 42 跳到 560 位置的节点（>61，退回）→ 下降 → L1 逐个到 61 每个节点插入时掷硬币决定层高（p=1/2 或 1/4）→ 期望层高 log n，期望查找 O(log n)， 无需任何旋转 跳表把「保持平衡」这个难题外包给了随机数——期望上层级分布正好是对数的。 跳表 红黑树 保证 期望 O(log n) 最坏 O(log n) 代码 约 1/3 删除情形多 范围扫描 底层就是链表 中序遍历 并发 局部 CAS 旋转牵连多节点 使用者 Redis ZSet、RocksDB memtable std::map 、内核 Redis 选跳表（antirez 亲述，都不是「更快」）： ① ZRANGE / ZRANGEBYSCORE 是核心，底层链表天然顺序扫； ② 无旋转无颜色，代码约红黑树 1/3，长期维护是实质优势； ③ 前进指针记 span 就能按排名查（ ZRANK ），红黑树要额外维护子树大小。Redis 单线程，并发友好不是它的理由。 ConcurrentSkipListMap 和 RocksDB memtable 选它，却正是因为并发：插入只需在每层对「前驱的 next」做一次 CAS，各层可独立完成。 即使只完成部分层，结构仍然合法 （底层链表始终有序，上层只是索引）——「中间状态也是合法状态」是无锁化的关键，平衡树的旋转中途是非法树。同一个结构在不同系统里被选中的原因可以完全不同。"},{"a":"s8","h":"最近公共祖先","x":"08 最近公共祖先 树上路径交点；静态 LCA 用倍增，O(n log n) 预处理 + O(log n) 查询 树上 u 到 v 的路径必过它们的 LCA。距离 = dep[u] + dep[v] − 2·dep[LCA]。求法很多：两次 DFS 的栈交点（在线、O(n) 每次）；Tarjan 离线 + 并查集；欧拉序转 RMQ（ST 表 O(1) 查询）。工程和竞赛里最常用的是 倍增 ：预处理每个点的 2^k 祖先，查询先把深的抬到同一层，再一起跳。树链剖分把重链压成序列，查询跳的次数更少，但代码更长——本页只留倍增骨架。 LCA(u,v) = 从 u、v 爬到根时最后的交点 1 2 3 4 5 6 LCA(6, 5) = 2 LCA(6, 3) = 1 树上 u→v 路径 = u→LCA + LCA→v 距离 = dep[u]+dep[v]−2·dep[LCA] 在线 DFS 栈 / Tarjan 离线并查集 / 欧拉序 + RMQ。下面只留倍增骨架。 任意两点路径必过 LCA。先把两点抬到同一深度，再一起跳。"},{"a":"s8-1","h":"8.1 倍增骨架","x":"8.1 倍增骨架 up[k][u] = u 的第 2^k 个祖先 查询从大 k 跳到小 k u up[0]=p up[1]=p² up[2]=p⁴ up[k]=p^(2^k) ① 深的那个先跳到与浅的同层（二进制拆 dep 差） ② 若已相遇就是 LCA；否则从大到小：祖先不同才跳 ③ 最后停在 LCA 的两个孩子上，返回 up[0] 树剖把重链压成序列再跳，查询更短；骨架更长，这里不展开。 预处理 DFS 填 up 表。树链剖分是另一条路，代码更长，需要时再写。 // 倍增 LCA：up[k][u] = 2^k 祖先。LOG = 20 够 n≤1e6 void dfs ( int u, int p) { up[ 0 ][u] = p; for ( int k = 1 ; k < LOG; ++k) up[k][u] = up[k- 1 ][up[k- 1 ][u]]; for ( int v : g[u]) if (v != p) { dep[v] = dep[u]+ 1 ; dfs (v, u); } } int lca ( int u, int v) { if (dep[u] < dep[v]) std:: swap (u, v); for ( int k = LOG- 1 ; k >= 0 ; --k) if (dep[up[k][u]] >= dep[v]) u = up[k][u]; if (u == v) return u; for ( int k = LOG- 1 ; k >= 0 ; --k) if (up[k][u] != up[k][v]) u = up[k][u], v = up[k][v]; return up[ 0 ][u]; } 约定根的父亲是 0， dep[0] 取 −1，避免跳到树外。树剖把轻边一刀、重链压成序列，查询沿轻边跳的次数是 O(log n)；骨架更长，路径聚合、子树修改才值得上。点分治处理路径统计，见 树形 DP 同页的「直径 / 重心」提示。"},{"a":"s9","h":"选型与树形 DP","x":"09 选型与树形 DP 按查询能力选结构；树上后序聚合转到 DP 篇 树形 DP = 后序遍历 + 子树状态。选 / 不选 u 时子树最优 u dp[u][0/1] 先 dfs 完所有孩子 v₁ 已是整棵子树答案 v₂ 选或不选 v₃ 再往上合并 不选 u：孩子 max(选,不选) 相加。选 u：孩子必须不选。 完整状态（树上背包 / 换根 / 链状树防爆栈）→ 动态规划 · 树形 DP 后序从下往上收，和 §2 选序判据是同一件事。展开见 动态规划 §6 树形 DP ，这里不重复骨架。 树形 DP 的状态几乎总是「子树 + 该节点的选择」。选/不选（最大独立集、打家劫舍 III）用 dp[u][0/1] ；树上背包用 dp[u][j] ，合并上界取 size[u] 而不是 m，实际 O(n·m)；换根要两遍 DFS（后序 down、前序 up），max 不可减时要用前缀+后缀。链状树 DFS 深度 = n，n=10⁵ 必须改迭代，否则爆栈。 Trie：按键的字符分层。只要「等于」用哈希；要前缀才用它 * c a t r d cat / car 共享 ca 复杂度 O(键长)，与词数无关 空间是死穴 → 基数树 / ART 压缩 AC 自动机 = Trie + 失配指针 见 字符串匹配 篇 前缀查询是 Trie 族的独占能力。补全、路由、敏感词都落在这条路上。 朴素 Trie 每节点 26～256 个指针，空间是死穴。压缩三条路：路径压缩（基数树 / Patricia，单链合并）；自适应节点（ART 的 Node4/16/48/256，Node16 用 SIMD 一次比 16 个 key，和 Swiss Table 控制字节同源）；双数组 Trie（分词、输入法）。要前缀才用它；只要等于，哈希表更省心。多模式匹配见 字符串匹配 的 AC 自动机。 结构 查找 插删 有序 前缀 范围扫描 典型用途 哈希表 O(1) 期望 O(1) 摊销 否 否 否 点查为主 排序数组 O(log n) O(n) 是 否 最快（连续） 只读索引 AVL O(log n) 最快的树 O(log n)，删除旋转多 是 否 O(n) 中序 读多写少 红黑树 O(log n) O(log n)，删除最多 3 转 是 否 O(n) 中序 std::map 、内核 跳表 O(log n) 期望 O(log n) 期望 是 否 底层链表 Redis ZSet、并发场景 B+ 树 O(log_B n) I/O O(log_B n) 是 否 叶子链表顺序 I/O 磁盘索引 Trie / ART O(键长) O(键长) 是 是 是 前缀查询、路由 堆 O(n) O(log n) 否 否 否 只要最值 决策顺序： ① 数据在磁盘还是内存？磁盘 → B+ 族（或 LSM）。 ② 要什么查询？只要等于 → 哈希表；要有序/范围 → 树或跳表；要前缀 → Trie 族（唯一选择）。 ③ 会变吗？不变 → 排序数组 + 二分，连续内存比任何树都快。 ④ 高并发写 → 跳表比平衡树容易做无锁。「哪个快」是最后才问的——前三步定下来，候选通常只剩一个。"},{"a":"s9-1","h":"9.1 结构对照（交互）","x":"9.1 结构对照（交互） 五种树形结构的取舍 点击结构名，看它的平衡策略、真实使用者，以及被选中的具体理由。 AVL 红黑树 B+ 树 跳表 Trie / ART"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 前序、中序、后序分别在什么时候用？怎么快速判断该用哪个？ 三种序的代码差别只在 visit(root) 那一行的位置。选哪个取决于 信息的流动方向 。 判据（比记名字管用） 「我需要的信息是从上往下给的，还是从下往上收的？」 · 从上往下传（参数）→ 前序 ：序列化、路径和、深拷贝 · 从下往上收（返回值）→ 后序 ：求高度、判平衡、LCA、子树聚合 · BST 上要有序序列 → 中序 ：验证 BST、第 k 小 · 按层处理 → 层序 BFS ：最小深度、每层最大值 两者都要的情况 比如「求二叉树直径」：既需要子树高度（往上收），又要更新全局答案。写法是 后序 + 一个引用参数记全局值 ： int depth (Node* r, int & best) { if (!r) return 0 ; int L = depth (r->l, best), R = depth (r->r, best); best = std:: max (best, L + R); // 用子树信息更新全局 return 1 + std:: max (L, R); // 往上返回自己的高度 } 另一个通用技巧 树递归的 基线一律选「空节点」而不是「叶子节点」 。选叶子要写 if (!r->l && !r->r) ，还得单独处理只有一个孩子的情况；选空节点则所有分支自动统一。 这条规则能消掉树题里一半的边界 bug。 基础 怎么验证一棵树是不是 BST？只比较父子为什么不对？ 只比较父子的反例 5 / \\ 3 7 \\ 6 // 3 < 5 ✓，7 > 5 ✓，6 > 3 ✓ 每对父子都合法，但 6 在 5 的左子树里却比 5 大 ——BST 要求的是「左子树 所有 值 < 根」，不是「左孩子 < 根」。 解法一 · 往下传区间（前序） bool ok (Node* r, long long lo, long long hi) { if (!r) return true ; if (r->val <= lo || r->val >= hi) return false ; return ok (r->l, lo, r->val) && ok (r->r, r->val, hi); } // 调用：ok(root, LLONG_MIN, LLONG_MAX) 注意用 long long ——如果节点值可能是 INT_MIN / INT_MAX ，用 int 边界会误判。这是本题最常见的隐藏 bug。 解法二 · 中序遍历检查递增（更简洁） Node* prev = nullptr ; bool ok (Node* r) { if (!r) return true ; if (! ok (r->l)) return false ; if (prev && prev->val >= r->val) return false ; prev = r; return ok (r->r); } 好处是 避开了边界值问题 ，而且能灵活处理「是否允许重复值」的语义（改 >= 为 > ）。 基础 BST 删除一个有两个孩子的节点怎么做？为什么这样是对的？ 删除分三种情形，只有第三种需要技巧： 情形一 · 叶子 直接删，父指针置空。 情形二 · 只有一个孩子 用唯一的孩子 顶替 自己（整棵子树上移，仍满足 BST）。 情形三 · 两个孩子 找一个「能站在这个位置」的值来顶替： 中序后继 （右子树的 最左 节点）或 中序前驱 （左子树的 最右 节点）。把它的 值 拷到当前节点，然后 递归删除它 。 Node* succ = r->r; while (succ->l) succ = succ->l; r->val = succ->val; r->r = erase (r->r, succ->val); 为什么是对的（两个理由） ① 后继的值确实能站在这个位置 ：它是「所有大于当前值的元素里最小的那个」，所以放到当前位置后，左子树全部 < 它、右子树全部 > 它，BST 性质保持。 ② 递归不会无限展开 ：后继是右子树的最左节点，所以它 必然没有左孩子 ⟹ 最多只有一个孩子 ⟹ 递归删除它必然落到情形一或二。 一个细节 总是用后继（或总是用前驱）会让树 逐渐偏向一侧 。有些实现交替使用，或按子树高度选较高的那侧，以延缓退化。朴素 BST 里意义不大（反正会退化），但实现平衡树时会影响旋转次数。 基础 什么时候该用 std::map 而不是 unordered_map ？ 三个正当理由， 没有一个是「map 更快」 ： ① 需要有序性 / 范围查询 lower_bound 、 upper_bound 、按序遍历、找「最接近 x 的元素」——哈希表根本做不到。这是最常见的理由。 ② 需要确定性延迟 树永远 O(log n)， 无 rehash 抖动、无攻击面 。哈希表是「期望 O(1)、最坏 O(n)、且偶尔一次 O(n) 停顿」。对 p99 敏感的服务、实时系统，可预测性比平均速度值钱。 ③ 需要迭代器 / 引用稳定性 std::map 插入不失效任何迭代器，删除只失效被删的那个。 unordered_map 的 rehash 会失效所有迭代器（引用还有效）； absl::flat_hash_map 连引用都失效。 顺带纠正一个常见误解 「树一定比哈希表慢」不成立。 n 较小时（几十到几百） std::map 常常更快：树的 上层节点常驻 cache （被反复访问），而哈希表要先算 hash（string 键要扫全串）再做一次 随机地址访问 （几乎必然 cache miss）。 交叉点约在 n 几百到一千，依赖 key 类型与哈希成本。 小容器上先别急着换。 还有更好的第四选项 如果数据 只读且要有序 ，用 排序数组 + std::lower_bound ——连续内存，比红黑树快得多（无指针追逐、无每节点一次分配）。这个选项经常被忽略。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 AVL 和红黑树的区别是什么？为什么 std::map 选红黑树？ AVL 红黑树 不变式 左右子树 高度差 ≤ 1 黑高相等 + 无连续红节点 高度上界 ≈1.44 log n 2 log n 查找 更快（树矮 ~30%） 稍慢 插入旋转 ≤ 2 次 ≤ 2 次 删除旋转 最坏 O(log n) 次 ≤ 3 次 额外存储 高度或平衡因子 1 bit 颜色 关键差异在删除 AVL 删除后可能需要 沿整条路径一路调整 （每层一次旋转）；红黑树的删除修复 最多 3 次旋转 ，其余通过改颜色解决。 所以 std::map 选红黑树 通用容器 无法预知读写比 ，而红黑树的「最坏修改代价」更小、更可控。这是保守但正确的默认。 选型判据： 读远多于写 → AVL （树矮，查找快）； 读写均衡或写多 → 红黑树 。 红黑树高度上界的推导（可能追问） 设黑高 bh： · 性质 5（黑高相等）⟹ 任何路径至少 bh 个黑节点 · 性质 4（无连续红）⟹ 任何路径长度 ≤ 2·bh · 黑节点至少构成完美二叉树 ⟹ n ≥ 2^bh − 1 ⟹ bh ≤ log₂(n+1) ∴ h ≤ 2·bh ≤ 2 log₂(n+1) 「无连续红」保证红节点最多占一半层级，这个『两倍』的松弛正是它修改更快的原因。 中级 什么是 Morris 遍历？它的代价是什么？ Morris 遍历用 O(1) 空间 完成中序遍历（递归和显式栈都要 O(h)）。办法是 临时借用叶子的空右指针 指回当前节点，走完再断开。 while (cur) { if (!cur->l) { visit (cur); cur = cur->r; } else { Node* pre = cur->l; while (pre->r && pre->r != cur) pre = pre->r; // 找中序前驱 if (!pre->r) { pre->r = cur; cur = cur->l; } // 建线索，下潜 else { pre->r = nullptr ; visit (cur); cur = cur->r; } // 断线索，访问 } } 复杂度仍是 O(n)——每条边最多被走 3 次（下潜、找前驱、返回）。 三个真实限制（这才是考点） ① 遍历中修改了树结构 （虽然结束会恢复）⟹ 不能并发遍历 ，也不能被其他线程同时读到； ② 遍历中抛异常会留下损坏的树 （线索没断开），之后任何操作都可能死循环—— visit 可能抛异常时不要用； ③ 树必须 可写 （不能是 const ）。 实际用途很窄 只在「树极深（递归会爆栈） 且 内存极紧」时才值得。一般用显式栈就够（O(h) 空间，h 通常是 log n）。 但它是体现「知道有 O(1) 方案」的好谈点，也是「换算法消掉栈」这一思路的漂亮例子——对比 递归与分治 里的三条去递归路径。 中级 为什么数据库索引用 B+ 树而不是红黑树？B 树和 B+ 树的区别？ 因为 磁盘上「一次节点跳转」比「一次比较」贵一千倍以上 ，优化目标从「减少比较次数」变成「减少跳转次数」。 内存跳转 ≈ 100 ns（cache miss） 磁盘跳转 ≈ 100 µs（SSD）～ 10 ms（HDD） 二叉搜索树：log₂(10⁹) ≈ 30 次 I/O B+ 树 (B≈500)：log₅₀₀(10⁹) ≈ 3~4 次 I/O 做法是把节点做成 一个磁盘页大小 （4–16 KB），每页放几百个 key。 用页内的廉价二分（8–10 次内存比较）换掉 26 次磁盘 I/O。 B 树 vs B+ 树： B 树 B+ 树 数据存哪 内部节点也存 只有叶子存 每页 key 数 较少 更多 → 树更矮 叶子之间 无连接 按序链成链表 范围扫描 要回树中序遍历 顺着叶子链表顺序 I/O B+ 胜出的决定性理由是范围扫描 SQL 的 BETWEEN 、 ORDER BY 、 GROUP BY 都是范围操作，叶子链表让它们变成 顺序 I/O ——磁盘上比随机 I/O 快一到两个数量级。 B 树「单点查找可能在内部节点命中」的优势相比之下微不足道，而且它还让树变高了。 顺带解释了覆盖索引和 LSM 覆盖索引 ：查询需要的所有列都在索引叶子里 → 不用回表（回表 = 又一轮 B+ 树查找 + 随机 I/O）。 LSM-Tree 存在的理由 ：B+ 树在写密集下会页分裂 + 随机写；LSM 把随机写变成顺序写（内存表 + 顺序落盘 + 后台 compaction），代价是读放大和写放大。 「B+ vs LSM」本质是「读优化 vs 写优化」。 中级 Redis 的 ZSet 为什么用跳表而不是红黑树？ 作者 antirez 给过三个理由，都不是「跳表更快」： ① ZSet 需要范围查询 ZRANGE 、 ZRANGEBYSCORE 是核心操作。 跳表的最底层就是一条有序链表 ，范围扫描是天然的顺序访问；红黑树要做中序遍历（指针跳来跳去）。 ② 实现和调试简单得多 没有旋转、没有颜色、删除逻辑直白——代码量约是红黑树的 1/3。 对一个要长期维护的开源项目，这是实质性优势。 ③ 容易支持「按排名查找」 在每个前进指针上记一个跨度（span）即可实现 ZRANK / ZRANGE by index 。红黑树要做这个得额外维护子树大小。 跳表的平衡策略：用随机代替旋转 每个节点插入时 掷硬币决定层高 （p=1/2 或 1/4），期望层高 log n ⟹ 期望查找 O(log n)。 完全不需要任何旋转 ——「保持平衡」这个难题被外包给了随机数。 代价是保证从「最坏 O(log n)」降级成「期望 O(log n)」。 同一个结构在不同系统里的理由可以完全不同 Redis 选它是为了 范围查询 + 实现简单 （它单线程，不关心并发）。 ConcurrentSkipListMap （Java）和 RocksDB 的 memtable 选它是为了 并发友好 ——跳表的修改是局部的（只改几个指针），容易做无锁；而平衡树的旋转会影响多个节点，无锁化极难。 这个对比比记「跳表 vs 红黑树谁快」有价值得多。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 要实现一个搜索框自动补全，用什么数据结构？怎么支持「按热度排序」和百万级词库？ 「前缀查询」这个需求直接排除哈希表和平衡树—— 只有 Trie 族能做 。但朴素 Trie 有三个问题要解决。 ① 基础结构：Trie 查找 O(键长)， 与词库大小无关 。走到前缀节点后遍历子树收集候选。 ② 空间问题必须解决 朴素 Trie 每节点 26 个指针 = 208 字节（64 位），实际可能只用 1–2 个。百万词、平均长 8 → 节点数几百万 → 几百 MB 。 三种压缩： 路径压缩 （基数树，单链节点合并）、 自适应节点 （ART：Node4/16/48/256 按需长大，Node16 用 SIMD）、 双数组 Trie （两个整数数组编码整棵树，最省空间且 cache 友好，中文输入法常用）。 ③ 按热度排序：在节点上预存 Top-K 不要「收集所有后代再排序」——热门前缀（如 a ）的后代可能有几十万个。 在每个节点上预计算并缓存「以此为前缀的 Top-10」 ，查询变成 O(键长) + O(1)。这是空间换时间，也是所有生产补全系统的做法。 热度更新时只需沿路径向上更新受影响的节点（O(键长)）。 ④ 实际系统还要什么 拼写容错 （编辑距离 ≤ 1 的候选）→ Trie 上做带剪枝的模糊搜索，或用 BK-tree / SymSpell。 拼音/首字母 （中文）→ 建多套索引。 个性化 → 全局 Top-K 与用户历史加权融合。 实时性 → 热度用滑动窗口或 EWMA 而不是全量重算。 工程上先考虑现成方案 Elasticsearch 的 completion suggester （基于 FST，比 Trie 更省空间）、Redis 的 ZRANGEBYLEX （有序集合按字典序范围查，可以模拟前缀）。 自己实现只在有特殊约束时才值得 ——比如需要嵌入到客户端、或词库有特殊结构。 面试时先说「评估现成方案」再讲自研设计，比直接开始画 Trie 更能体现工程判断。 高级 ART（自适应基数树）解决了 Trie 的什么问题？它和 Swiss Table 有什么共同点？ ART 解决的是 Trie 的 空间浪费 问题，而它的解法思路和 Swiss Table 同源。 Trie 的空间问题根源 节点大小固定为字符集大小 （256 个指针 = 2 KB），而实际孩子数分布 极度不均 ——靠近根的节点孩子多，靠近叶子的往往只有 1–2 个。绝大多数指针是空的。 ART 的两个改进 ① 自适应节点大小 ：按孩子数选四种表示—— · Node4 ：4 个 key 字节 + 4 个指针（线性查找） · Node16 ：16 个 key 字节 + 16 个指针（ SIMD 一次比较 16 个 ） · Node48 ：256 字节索引表 + 48 个指针 · Node256 ：直接 256 个指针（数组下标即字节） 节点 按需长大/缩小 ，既省空间又保留 O(键长)。 ② 路径压缩 ：单链路径合并（继承基数树）。 与 Swiss Table 的共同点 「用 SIMD 在一小块连续数组上批量筛选」 ： · Swiss Table：16 个 控制字节 （哈希高 7 位）一条 SIMD 指令比完，筛掉 127/128 的候选。 · ART 的 Node16：16 个 key 字节 一条 SIMD 指令比完，直接定位孩子。 两者都是「把元数据压缩到极小的连续数组里，让一次 cache line 读取 + 一条 SIMD 指令完成大批量筛选」。 这是现代数据结构的共同手法 ，同族还有：B 树式的 cache 内索引（一个 cache line 放 16 个 key）、布隆过滤器（小摘要挡住无效访问）。 识别出这个模式，你在设计新结构时就多了一个工具。 使用者与定位 HyPer、DuckDB、部分内存数据库的主索引。它的定位是 「内存数据库里替代 B+ 树的选项」 ——O(键长) 而非 O(log n)，且天然支持前缀和范围。 代价是实现复杂度高（四种节点 + 转换逻辑），且对 长键 不如 B+ 树（键长 100 字节时 O(100) 未必优于 O(log n)）。 高级 一个用 std::map 做内存索引的服务，QPS 上不去，profile 显示时间都在 map 查找上。怎么优化？ 红黑树的性能问题几乎总是 指针追逐 + 每节点一次分配 造成的，而不是 log n 这个次数。按性价比排序： ① 先问需不需要有序 如果只做 点查 ，换 absl::flat_hash_map ——连续内存 + SIMD，通常快 3–5 倍。 这一步收益最大，但经常被跳过 ：很多代码用 map 只是因为「习惯」或「以前需要有序，现在不需要了」。 ② 需要有序但数据只读 → 排序数组 + lower_bound 连续内存，二分的前几步会 cache miss 但后几步全命中；没有每节点一次分配、没有指针追逐。 通常比红黑树快 2–3 倍 ，内存也省一半以上（每元素省掉两个指针 + 颜色 + malloc 头）。 如果偶尔要更新，可以「批量重建」（攒一批改动后重排一次）。 ③ 需要边读边写 + 有序 → 考虑跳表或 B 树式结构 cache 内 B 树 （一个 cache line 放 16 个 key，SIMD 比较）能把树高从 log₂n 降到 log₁₆n，cache miss 次数降 4 倍。ART 也是候选。 高并发写场景 → 跳表 （局部修改，易做无锁）。 ④ 不换结构也能改善的两件事 自定义分配器 ： std::map 每个节点一次 malloc ，节点散落在堆上。用 arena / pool allocator 让节点连续分配，能显著改善 cache 局部性。 缩小 key ：如果 key 是长 string，比较成本高且节点大。改成存哈希值 + 短标识，或用 string_view 指向统一的字符串池。 但先确认 profile 没骗你 「时间都在 map 查找上」可能有两个假象： ① 实际瓶颈是 key 的构造/比较 （构造临时 std::string 做查找 → 每次一次分配），改用 map 的透明比较器（C++14 的 std::less<> ）或 string_view key 就能解决； ② 实际是 内存带宽饱和 ，此时任何结构都受限，得先减少数据量。 测量方法见 性能优化指南 。 高级 为什么平衡树很难做无锁并发，而跳表相对容易？ 根本原因是 「一次修改影响多少个节点」 。 平衡树的困难 旋转是一个涉及多个节点的原子性变更 ：一次旋转要同时改 3–4 个指针，而且可能 沿路径向上连锁触发 （AVL 删除最坏 O(log n) 次旋转）。 要让并发读者始终看到一致的树，就得： · 用 读写锁 （写者独占整棵树 → 并发度低），或 · 用 细粒度锁 + 严格的加锁顺序 （极易死锁，且旋转要锁多个节点），或 · 用 RCU （每次修改 复制 受影响的路径，读者无锁——但写放大严重，且要延迟回收旧节点）。 跳表为什么容易 修改是局部的 ：插入一个节点只需在每一层做一次「前驱的 next 指针」的 CAS，各层可以 独立地、逐层完成 。 关键在于 即使只完成了部分层，结构仍然是「正确但不够优」的 ——底层链表始终有序完整，上层只是索引。读者顺着走一定能找到，只是可能慢一点。 这个「中间状态也是合法状态」的性质是无锁化的关键 ，平衡树没有（旋转中途的树是非法的）。 实践中的方案对照 ConcurrentSkipListMap （Java）：无锁跳表，CAS + 标记删除。 RocksDB memtable ：无锁跳表（单写多读，写者也无锁）。 Linux 内核的 RCU + 红黑树 ：读无锁（RCU 保护），写加锁——因为内核里读远多于写。 B+ 树的 latch coupling / B-link 树 ：数据库用的方案，靠「节点内加短锁 + 右兄弟指针」让读者能追上分裂中的节点。 可迁移的一般原则 「设计并发数据结构时，先问『部分完成的修改是否仍是合法状态』」 。 是 → 可以无锁化（跳表、Michael-Scott 队列、Swiss Table 的部分操作）。 不是 → 需要锁、RCU 或整体复制。 这也解释了为什么 无锁结构常常要重新设计数据布局 ，而不是给现有结构加 CAS——布局本身决定了能否无锁。相关展开见 并发模式 。 📐 树形 DP ⬅️ 堆与 Top-K ➡️ 并查集与区间结构 🕸️ 图遍历与拓扑排序 🗄️ 存储与数据架构 📚 目录 🏠 主页 树与平衡树 — 三序遍历 · BST 有序 · AVL 与红黑旋转 · B 树阶 · LCA 倍增 · 树形 DP — Kavin Tech Notes · 2026"}]},{"u":"algorithms/two_pointers_sliding_window.html","t":"双指针与滑动窗口","c":"算法与数据结构","s":[{"a":"","h":"","x":"双指针与滑动窗口 · O(n) 的摊销论证 / Floyd 判圈证明 / 变长窗口统一模板 / 恰好 K 的拆解 / 限流与 TCP 窗口 Kavin Tech Notes ← 主页 目录 30 min ◐ 双指针与滑动窗口 本质：单调推进 与其他章节的关系 为什么两层循环是 O(n) 对撞指针 快慢指针 同向读写指针 滑动窗口统一模板 窗口的三种约束类型 模板对照（交互） 工程里的滑动窗口 陷阱清单 面试题集（12 道） 10.1 基础 10.2 中级 10.3 高级 Algorithms · Two Pointers & Sliding Window · 2026 双指针与滑动窗口 这一类技巧的共同点只有一句： 让某个指针全程只往一个方向走，绝不回退 。回退是 O(n²) 的来源，不回退就是 O(n)。所以真正要练的不是模板，而是「 凭什么可以不回退 」——每道题都要能说出那个让你敢丢掉一半候选的性质。本篇把这个论证讲清，再把三类指针和窗口的四步骨架收成一套可迁移的写法。 核心难度 ★★★☆☆ 面试出现率 极高 阅读时长 ~30 min 前置 复杂度与分析 势能法证 O(n) Floyd 判圈 窗口四步骨架 恰好 K 的拆解 限流与 TCP 窗口"},{"a":"s1","h":"本质：单调推进","x":"01 本质：单调推进 不回退，所以线性 「双指针」描述的是 形式 （两个下标），不是 机制 。真正把 O(n²) 压到 O(n) 的是：每个下标 只沿一个方向走 。 单调推进 ：下标全程单向移动、总移动量 O(n)，嵌套循环的 总 执行次数仍是 O(n)。前提是每走一步都能 确定性地排除 一批候选，且永不再看。模板谁都会背， 凭什么能排除 才决定写得对不对。 对撞像 两人从走廊两端找门牌 。左边号码太小，就能确定这一间及其左侧都不是答案——因为号码递增，所以安心右移、永不回头。号码乱排时排除不了任何东西： 「数组有序」不是装饰，它就是解法本身。 快慢指针像操场上的龟兔：相对速度为 1，圈上距离逐格缩小，不可能跳过相遇点。读写指针像整理抽屉：左手（写）左边已经放好，右手（读）只往前掏，掏完抽屉并没有变短——还得自己把后面空壳扔掉。 本篇按「指针怎么动」分四类。模板谁都会背，真正要练的是每一步凭什么排除候选： 类型 指针怎么动 靠什么性质排除候选 典型题 对撞 （§3） 两端向中间 有序性 / 单调的目标函数 两数之和、三数之和、盛水容器 快慢 （§4） 同向不同速 相对位移的周期性 环检测、找中点、倒数第 k 个 同向读写 （§5） 同向同速，读快于写 写指针左侧已是最终结果 原地去重、移除、分区 滑动窗口 （§6） 右扩左缩，都不回退 约束关于窗口长度单调 最长无重复子串、最小覆盖子串"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 复杂度与分析 —— §2 的 O(n) 论证用的正是那里的势能法 单调栈与前缀和 —— 窗口极值要靠单调队列，「和为 K 的子数组」要靠前缀和 + 哈希表 二分查找 —— 「第 k 小的距离对」是「二分答案 + 双指针计数」的组合 排序全景 —— 对撞指针的前提通常由排序提供；三路分区就是三个同向指针 限流 · 熔断 · 降级 —— §8 的四种限流算法在那里有完整的生产讨论"},{"a":"s2","h":"为什么两层循环是 O(n)","x":"02 为什么两层循环是 O(n) 指出一个单调有界的量 内层指针声明在外层之外、全程只增不减时， ++j 的总次数被 j 的上界卡住——看起来像 O(n²)，摊销后是 O(n)。这就是 势能法 ：Φ = j，内层每执行一次 Φ 加 1，总增量 ≤ n。要点不是「看起来像双指针」，而是能指出这个单调有界的量。j 若每轮重置到 i，Φ 每次从 i 重新涨，总增量变回 Σ(n−i)=O(n²)。约束不单调时内层还可能来回抖——那更不是双指针。 错误数法：外层 n × 内层最坏 n = O(n²) —— 上界成立但太松。 正确数法：外层恰好 n 次 + 内层 ++j 总量 ≤ n = O(n) 。 对撞 Φ = r−l 每轮至少减 1；窗口 Φ = l+r 两者只增。 外层 i 走 n 步 · 内层 j 只增不减 · 总量 ≤ 2n i 0 1 2 … n−1 恰好 n 次 j 0 只向右挪 → 至多到 n（永不回退） ++j 总量 ≤ n ❌ 每轮 j = i 重置 j 的总移动变回 O(n²)——只是两个变量，不是双指针 ❌ 约束不单调（含负数的积/和） 缩小窗口不保证「更容易满足」→ 窗口不适用 分开数：外层 n 次 + 内层 ++j 总量 ≤ n = O(n)。指不出这个单调有界的量，复杂度论证就不成立。 写完后的自检 对撞：Φ = r − l ，每轮至少减 1，初值 n ⟹ 至多 n 轮。 窗口：Φ = l + r ，两者只增、各至多 n ⟹ 总移动 ≤ 2n。 读写：Φ = 读指针位置，只增 ⟹ n 次。 滑动窗口还要求 约束关于伸缩单调 ： [l,r] 合法 ⇒ 子区间也合法（或反向）。数组 全正 时和/积随窗口变长单调； 含负或 0 就断了，改前缀和 + 哈希表（见 单调栈与前缀和 ）。题面没说正整数，面试里要主动问。乘积题含 0 还会除零；「和恰好为 K」哪怕全正，直接开窗也不对——「恰好」本身就不单调，要做差或换前缀和。"},{"a":"s3","h":"对撞指针","x":"03 对撞指针 两端向中间，每步排除一整行或一整列 有序数组找两数之和为 target：朴素 O(n²)，对撞 O(n)。每步用有序性砍掉整行或整列。 std::pair< int , int > two_sum_sorted ( const std::vector< int >& a, int target) { int l = 0 , r = ( int )a. size () - 1 ; while (l < r) { int s = a[l] + a[r]; if (s == target) return {l, r}; if (s < target) ++l; // 和太小 → 换更大的左端 else --r; // 和太大 → 换更小的右端 } return {- 1 , - 1 }; } 正确性的关键是 排除性论证 ，必须能说清： 候选空间 = 所有 (l, r) 数对，对撞指针每步排除一整行/列 l=0 l=1 l=2 r=4 r=0 ① a[0]+a[4] < target → a[0] 配任何更小的右端都更小 → 整行 l=0 全部排除 ，++l ② a[1]+a[4] > target → a[4] 配任何更大的左端都更大 → 整列 r=4 全部排除 ，--r 每步排除一整行或一整列 ⟹ 至多 2n 步覆盖 n² 的候选空间 对撞指针不是「试遍所有对」，而是每步用有序性砍掉整行或整列——这才是 O(n) 的来源。 排除性论证 ： a[l] + a[r] < target 时， a[r] 已是剩余最大右端。左端配它都不够，配任何更小的右端更不够 ⟹ 含下标 l 的候选整行丢弃， ++l 不漏解。和太大时 --r 对称。∎ 候选空间是 n² 个数对，每步砍掉整行或整列，所以至多 2n 步覆盖完——O(n) 来自排除，不是「试得快」。 无序两数之和没有可排除的性质，用 哈希表一遍扫 。若还要原始下标，排序会打乱——排 (值, 原下标) 或直接哈希。 同族： 三数之和 = 固定一个 + 对撞，O(n²)。去重必须和「已处理的前一个」比（ a[i]==a[i-1] ），和后一个比会把 [0,0,0] 跳掉；命中后 ++l; --r; 再跳过相同值。 盛水容器 min(h[l],h[r])×(r-l) ：宽度必减，高度被短板卡住，所以只换较矮一侧——换高板宽度变小、高度仍被短板卡，还可能丢掉高板配别的左端。面试一句话： 宽度必然减小，只有换短板才可能抬高上限。"},{"a":"s4","h":"快慢指针","x":"04 快慢指针 同向不同速，用相对位移做检测 链表判环：哈希表 O(n) 空间； Floyd 判圈 O(n) 时间 O(1) 空间 。慢指针 1 步、快指针 2 步，无环时 fast 先到空，有环时必然相遇。进入环后相对距离每轮恰好减 1，是环长内的非负整数，不可能跳过 0。步长差若为 2（走 3 步），偶环长加奇距离会在奇数间跳变，永不相遇。找入口：相遇后一头放回 head，两人同速走，再次相遇即入口——因为 a = (k−1)(b+c)+c 。 bool has_cycle (Node* head) { Node* slow = head; Node* fast = head; while (fast && fast->next) { slow = slow->next; fast = fast->next->next; if (slow == fast) return true ; } return false ; } 进入环后：相对距离每轮恰好 −1 → 必相遇 head 入口 slow fast 环内距离 d 相对位移 fast 每轮比 slow 多 1 步 d ← d − 1（模环长） 步长差 = 1 ⟹ 不会跳过 0 差为 2（走 3 步）时 偶环长 + 奇距离 → 永不相遇 相遇靠的是「距离逐个递减」。找入口：一头放回 head，两人同速走，再次相遇即入口（下图推导）。 head 入口 E a 相遇点 M b c 环长 = b + c 相遇时两者走过的距离 slow = a + b fast = a + b + k(b+c) k ≥ 1 fast 恰好是 slow 的 2 倍： 2(a+b) = a + b + k(b+c) a + b = k(b+c) a = k(b+c) − b = (k−1)(b+c) + c ⟹ 从 head 走 a 步 ≡ 从 M 走 c 步（+整数圈）→ 都到 E a = (k−1)(b+c) + c ：从头走 a 步和从相遇点走 c 步落在同一点。找中点 / 倒数 k 是同族（固定间距），偶数长度用手推 n=2 定前/后中点；删除节点加 dummy head。 同一套数学解「寻找重复数」（ i → a[i] 隐式链表，重复数=环入口）和「快乐数」。识别信号：确定性后继 f(x) + 有限状态 → Floyd，空间 O(1)。找中点 / 倒数第 k 是同族：固定间距。偶数长度用手推 n=2 定前/后中点；删除节点加 dummy head，否则删头要特例。"},{"a":"s5","h":"同向读写指针","x":"05 同向读写指针 STL 的 remove / unique 就是这个模式 读指针扫一遍，写指针左侧已是最终结果。 std::remove / unique 都是这个不变式。 原地移除 3：write 左侧全是保留值 1 2 4 3 5 3 已写入 write read → 不变式：[0, write) 合法 remove：x ≠ val 才写入 unique：与 write−1 不同才写 返回 write = 新逻辑末尾 不改 size，后面是空壳 写指针左侧已是最终结果；读指针只往前走。 std::remove 不改 size ：只把保留元素搬到前面，返回新逻辑末尾，后面是被 move 走的空壳。必须 v.erase(remove(...), end) ，或 C++20 std::erase 。原因是算法只拿到迭代器、拿不到容器，无法调用 erase ——这是 STL「算法与容器解耦」的代价。 template < typename It, typename T> It remove (It first, It last, const T& value) { first = std:: find (first, last, value); if (first == last) return first; It write = first; for (It read = ++first; read != last; ++read) if (!(*read == value)) *write++ = std:: move (*read); return write; // unique 同构：if (write==first || *read != *(write-1)) } 任务 写指针左侧 STL 移除等于 v [0, write) ≠ v remove 去相邻重复 无相邻重复 unique （全局去重先 sort） 分区 / 移零 全满足谓词 / 全非零且保序 partition / 手写 unique 只去 相邻 重复，全局去重要先 sort。 sort+unique 是 O(n log n) 时间 O(1) 空间且有序；哈希去重 O(n) 时间 O(n) 空间且不保序。有序数组「每个值最多留 k 个」可写成 write < k || a[write-k] != x ——用已写入部分当状态，前提仍是有序。读写指针的不变式是「写指针左侧已是最终结果」：remove 判断「当前值要不要」，unique 判断「当前值和已写入最后一个是否不同」。两者都只 move 一次每个元素。分区把「满足谓词」和「不满足」分成两段；移零要保序时不能 swap 到末尾——那会打乱相对顺序，必须把非零紧凑到前面再补零。"},{"a":"s6","h":"滑动窗口统一模板","x":"06 滑动窗口统一模板 四步骨架，覆盖绝大多数窗口题 变长窗口四步：① 右扩 ② while 收缩 ③ 统计 ④ r 前进、l 不回退。求最长 / 最短只改②③的关系。求最长时收缩条件是「不合法」，缩到刚好合法再统计（while 之后）；求最短时收缩条件是「已合法」，一边缩一边记（while 之内）。写错位置是变长窗口第一大 bug。收缩必须用 while：一次右扩可能要连缩多次，if 只缩一次会残留非法窗口。 窗口 = [l, r]，两端都只向右 2 1 3 4 7 l r ++l 移出 ++r 扩入 定长：右进一个、左出一个 移出用 r ≥ k，统计用 r ≥ k−1（差 1） 恰好 K = 至多 K − 至多 (K−1) 「恰好」不满足子区间合法，不能直接开窗 l、r 各自至多走 n 步，总移动 ≤ 2n。个数题： ans += r−l+1 （以 r 结尾的合法子区间个数）。 恰好 K = 至多 K − 至多 (K−1) 「恰好」不满足子区间合法，不能直接开窗。atMost 用标准变长窗口，每步 ans += r − l + 1 。 k=0 时 atMost(−1) 必须返回 0。 ① 右扩：r 前进一格，把 a[r] 加入窗口状态 ② 收缩：while (窗口不合法) 移出 a[l]，++l 注意是 while 而不是 if —— 一次右扩可能需要多次收缩 ③ 统计：此刻 [l, r] 是「以 r 结尾的合法窗口」 求最长 → max(r−l+1)；求个数 → += (r−l+1) ④ 循环：r 继续前进，l 永不回退 ⟹ 总移动 ≤ 2n ⟹ O(n) 求最长 vs 求最短：收缩条件正好相反 求最长（如「最多 K 个不同字符」） 收缩条件 = 窗口 不合法 → 缩到刚好合法，然后在这里统计 → 统计放在 while 之后 求最短（如「最小覆盖子串」） 收缩条件 = 窗口 已合法 → 一边缩一边统计，缩到不合法为止 → 统计放在 while 之内 写错位置是变长窗口第一大 bug 来源： 先问自己「我要的是最长还是最短」 「求最长」和「求最短」的收缩条件与统计位置完全相反。落笔前先确定是哪一种。 // 四步骨架：最长/最短只改 while 条件与统计位置 for ( int r = 0 ; r < n; ++r) { add (a[r]); // ① 右扩 while ( need_shrink ()) // ② while 不是 if remove (a[l++]); // ③ 最长：统计在 while 之后（此时刚好合法） // 最短：统计在 while 之内（一边缩一边记） use (l, r); } 定长窗口右进左出：移出用 r ≥ k ，统计用 r ≥ k−1 ，两个条件差 1，写混会漏第一个窗口。求个数时 ans += r−l+1 不是长度——收缩后 [l,r] 是以 r 结尾的最长合法区间，子区间也合法所以一共 r−l+1 个。这是所有「求子数组个数」题的框架，前缀和+哈希也是「枚举右端、查合法左端个数」。"},{"a":"s7","h":"窗口的三种约束类型","x":"07 窗口的三种约束类型 状态用什么维护，取决于约束长什么样 状态结构跟约束走。每种窗口题一份代码没有必要——四步骨架不变，换的是「窗口里记什么、何时算合法」。覆盖是计数的变体：不要每轮扫 need 数组，用 formed （已满额种数）把 O(k) 检查压成一次比较；加入只在 have==need 时 +1，移出必须先判再减。和/积只在全正时单调；窗口最值必须单调队列，因为移出的可能正是当前最大值。 计数 cnt[] / map 维护种类、重数 例：无重复子串 覆盖型加 formed 满额种数 O(1) 判断 和 / 积 一个标量 前提：数组全正 例：和 ≥ target 最短 含负 → 前缀和+哈希 窗口不再单调 最值 不能只存一个数 移出的可能是当前 max 例：滑动窗口最大值 单调队列 O(n) 队头即窗口最值 计数 / 和 / 最值三种约束对应三种状态。覆盖是计数的变体：用 formed 把 O(k) 检查压成一次比较。 窗口 k=3，数组 1 3 −1 −3 5 · 队列存下标，值递减 1 3 −1 −3 5 窗口 入队：弹出队尾所有 ≤ 新值的 出窗：队头下标过期则弹出 队头 = 当前窗口最大值 每个元素至多进/出一次 → O(n) 窗口最值不能只留一个变量。单调队列细节见 单调栈与前缀和 。 约束类型 状态结构 例题 计数（至多 K 种 / 无重复） cnt[128] / map；ASCII 用 unsigned char 下标 最长无重复子串 覆盖（含重数） need/have + formed （满额种数） 最小覆盖子串 和 / 积（全正） 标量；定长则右进左出 和 ≥ target 最短 恰好 K 至多 K − 至多 (K−1) 恰好 K 个不同整数 窗口最值 单调队列（下标递增、值单调） 滑动窗口最大值 和不单调（含负） 窗口不适用 和为 K → 前缀和+哈希 分界线只有一条： 数组是否全正 。含负则和/积不再随窗口伸缩单调。题面没说正整数，面试里主动问。 char 做下标是 UB，必须 (unsigned char) ；UTF-8 不要按字节开窗。 覆盖型不要每轮扫 128 格：维护 formed （已满额种数），加入时 have==need 才 +1，移出时 先判再减 。窗口最值不能只存一个数，移出的可能是当前 max，要用单调队列。"},{"a":"s7-4","h":"7.4 模板对照（交互）","x":"7.4 模板对照（交互） 五类窗口题的模板与判据 点击题型，看状态怎么维护、收缩条件是什么、统计放在哪一行，以及适用前提。 求最长（计数型） 求最短（覆盖型） 求个数（恰好 K） 定长窗口 窗口不适用时"},{"a":"s8","h":"工程里的滑动窗口","x":"08 工程里的滑动窗口 限流、TCP、流式聚合都是同一个词的不同含义 刷题窗口管的是「一段下标」；工程窗口管的是「一段时间 / 一段未确认字节」。左右端都只前进，但约束和失败模式不同。限流看突刺与内存：固定窗口会在边界打出两倍配额，滑动日志无突刺但吃 O(QPS) 内存，漏桶平滑却不允许突发，令牌桶用 (last, tokens) 两个字段做惰性补充，允许受控突发，是生产默认。TCP 窗口大小是 min(cwnd, rwnd)，零窗口要 persist timer，否则死锁。 ① 固定窗口 · 边界突刺问题 窗口 [0s, 1s) 窗口 [1s, 2s) 0.9s 打满 100 + 1.1s 再打 100 → 0.2s 内通过 200， 超限 2 倍 ② 滑动窗口 · 无突刺但吃内存 任意 1s 区间内计数 ≤ 100 滑动日志：存每个请求时间戳 → O(QPS) 内存 滑动计数：切成 N 个小格，只存格计数 → 精度换内存 ③ 漏桶 · 平滑输出，不允许突发 固定速率漏出；桶满则丢弃 → 输出绝对平滑， 但无法应对合理突发 ④ 令牌桶 · 允许受控突发（最常用） 按速率产令牌，攒到 burst 上限 → 平时平滑， 攒够可一次放行一批 工程默认选 令牌桶 ：实现只需 (上次时间, 当前令牌数) 两个字段，O(1) 空间、按需惰性补充，且能表达「允许合理突发」这个真实需求 四种算法都是 O(1) 判定。差别在突刺、内存、能否突发。生产讨论见 限流 · 熔断 · 降级 。 令牌桶 · 两个字段 (last, tokens)，调用时才补 ① 惰性补充 Δt × rate 封顶 burst 单调时钟 ② 判定 tokens ≥ need 够 → 扣减放行 不够 → 拒绝 ③ 工程坑 墙上时钟会被 NTP 拨乱 多副本各限一份 → 配额 ×N 分布式要把状态放 Redis 且原子 不必后台定时器。代码骨架在面试题里；这里只记状态量和失败模式。 令牌桶三点工程约束：时钟必须单调（ steady_clock / CLOCK_MONOTONIC ），墙上时间会被 NTP 拨乱，Δt 变负就把令牌算成负数；补充是惰性的——没有请求就不产令牌，省掉后台定时器；多副本各限一份等于配额乘 N，分布式必须把 (last, tokens) 放到共享存储并用原子脚本更新，否则限流形同虚设。 TCP 窗口 ≈ 算法题 [l, r]：两端只前进 已确认 已发送未确认 SND.UNA→NXT 还能发（rwnd） 禁止发送 l · ACK 推进 r · 发送推进 大小 = min(cwnd, rwnd)。零窗口要 persist timer 探测，否则死锁。 糊涂窗口：收一点就通告 → 小包风暴；Nagle + 延迟 ACK 会互相拖慢。 流处理还多一维乱序：水位线决定左端何时能安全前进。见网络 / Spark 篇。 算法题左端前进无条件安全；TCP / 流处理里它是正确性与延迟的权衡。 流处理多出来的维度是乱序。 算法题默认数据按序到达、不会迟到，所以 ++l 无条件安全。网络抖动、分区不均、客户端时钟偏差会让事件时间与到达时间脱节。水位线是「事件时间已推进到哪」的估计，越过窗口结束才触发计算；允许迟到则在水位线过后仍保留状态一段时间，再迟进侧输出。水位线越宽结果越准、延迟越高——这是正确性与延迟的旋钮，不是实现细节。 滚动窗口不重叠，滑动/跳跃窗口定长定步长可重叠，会话窗口按空闲超时切开。算法题里没有会话窗口的对应物：边界由数据间隙决定。状态还要能 checkpoint；迟到更新要求下游幂等。展开见 网络编程 与 Spark 内核 。"},{"a":"s9","h":"陷阱清单","x":"09 陷阱清单 review 时逐条扫一遍 右端先扩，再 while 缩 ① ++r，把 a[r] 加进状态 ② 这时窗口可能非法 ③ while 连缩多次，直到合法 先缩再扩会漏掉「以 r 结尾」的候选 收缩必须 while，一次 if 不够 空窗口 求最短：while 合法就 ++l 可能缩到 l = r+1（空） 此时 r−l+1 = 0，不能当答案 统计前判断 l ≤ r 定长：r≥k−1 才统计，同理 顺序反了会漏解；缩过头会把空区间当最短。手推 n=0,1,2 比背模板可靠。 陷阱 症状 修法 内层指针被重置 看着是双指针，实际 O(n²) 确认能指出一个单调有界的势能量（§2） 约束不单调却用窗口 结果错，且小数据可能测不出 问「数组是否全正」；不满足就换前缀和 + 哈希表 收缩用 if 而非 while 一次右扩需要多次收缩时漏缩 变长窗口的收缩一律用 while 统计位置放错 求最长/最短的结果偏差 1 或整体错 求最长 → 统计在 while 之后 ；求最短 → 在 while 之内 定长窗口 off-by-one 漏第一个窗口或多算一个 写清不变式；移出用 r >= k ，统计用 r >= k-1 char 直接做下标 非 ASCII 输入越界（UB） 强转 (unsigned char) ；多字节输入不要按字节开窗 三数之和去重方向错 [0,0,0] 这类解被跳掉 去重和「已处理的前一个」比，不和「后一个」比 Floyd 步长取 1 和 3 特定环长下永不相遇 步长差必须为 1，保证相对距离逐个递减 覆盖型每轮全量检查 复杂度变成 O(128n) 或 O(nk) 维护 formed 计数器增量判断 误用 std::remove 以为删掉了， size() 没变 配 erase ，或用 C++20 的 std::erase/erase_if 窗口和用 int 大数组求和溢出（UB） 累加量用 long long 链表边界未用哑头 删除头节点时需要特例分支 统一加 dummy head 前两条是「设计错」，其余多是「实现错」。设计错靠 写下势能量与单调性 来防，实现错靠 手推 n=0,1,2 的最小用例 来防。空窗口、先扩后缩、统计放错位置，是变长窗口的三大实现坑；约束不单调却套模板，是设计坑。三数之和去重方向、Floyd 步长差、remove 不配 erase，面试里同样高频。 累加溢出可用 -fsanitize=undefined 抓住——见 Sanitizers 实战 。"},{"a":"s10","h":"面试题集（12 道）","x":"10 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s10-1","h":"10.1 基础（必答）","x":"10.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 下面这段代码是两层循环，为什么复杂度是 O(n) 而不是 O(n²)？ int j = 0 ; for ( int i = 0 ; i < n; ++i) { while (j < n && ok (i, j)) ++j; use (i, j); } 因为 j 声明在循环外，全程只增不减 。不能用「外层 n 次 × 内层最坏 n 次」来数——那个上界成立但太松。 正确数法：分开数两件事 · 外层循环体：恰好 n 次 · 内层 ++j 的 总 次数：j 从 0 单调增到至多 n ⟹ 总量 ≤ n 总代价 = n + n = O(n) 这就是势能法 取势能 Φ = j（单调不减、上界 n）。内层每执行一次让 Φ 增加 1，而 Φ 总增量 ≤ n，所以内层总次数 ≤ n。 对照：什么时候不成立 如果写成 int j = i; （每轮重置）， j 的总移动量就变回 O(n²)。 那只是「两个变量」，不是双指针。 自检问题 写完双指针解法，问自己： 我能指出一个单调变化且有界的量吗 ？对撞指针是 r − l （每轮至少减 1）；滑动窗口是 l + r （都只增、各至多 n）。指不出来，就说明解法可能不是 O(n)。 基础 有序数组的两数之和用对撞指针，为什么移动指针不会漏掉解？无序数组呢？ 靠 排除性论证 ：设当前 a[l] + a[r] < target 。因为数组有序， a[r] 是剩余候选里 最大的右端 。 推理 既然 a[l] 配上最大的右端都还不够，那 a[l] 配任何剩余右端都不够 ⟹ 包含下标 l 的所有候选对都可丢弃 ⟹ ++l 不漏解。和太大时 --r 对称成立。∎ 几何直观 候选空间是 n² 个 (l, r) 对。每步排除 一整行或一整列 ，所以至多 2n 步就能覆盖完——这才是 O(n) 的来源，而不是「试遍所有对但试得快」。 无序数组不能用 没有可排除的性质： a[l] 配别的右端完全可能更大。此时标准解法是 哈希表一遍扫 （O(n) 时间 O(n) 空间）：遍历时查 target - a[i] 是否已出现过。 一个易忽略的分岔 如果题目要求返回 原始下标 而数组无序，排序会破坏下标。要么排 (值, 原下标) 的 pair，要么直接用哈希表。 「要不要保留原下标」决定了该排序还是该用哈希表。 基础 写出变长滑动窗口的模板。「求最长」和「求最短」的写法差别在哪？ 骨架四步： ① 右扩 → ② while 收缩 → ③ 统计 → ④ r 继续前进，l 永不回退 。差别全在②和③的关系。 求最长：统计在 while 之后 for (r...) { add (a[r]); while ( 不合法 ()) remove (a[l++]); best = max(best, r - l + 1 ); } 求最短：统计在 while 之内 for (r...) { add (a[r]); while ( 已合法 ()) { best = min(best, r - l + 1 ); remove (a[l++]); } } 收缩条件正好相反 求最长时收缩条件是「 不 合法」——缩到刚好合法再统计。求最短时收缩条件是「 已 合法」——一边缩一边统计，缩到不合法为止。 写错位置是变长窗口第一大 bug 来源 ，落笔前先确定要的是最长还是最短。 收缩必须用 while 而不是 if 一次右扩可能需要多次收缩（例如加入一个字符后窗口里出现了多处违规）。用 if 只缩一次，窗口会残留非法状态。 求「个数」时的统计式 ans += r - l + 1 。因为收缩完成后 [l,r] 是以 r 结尾的 最长 合法区间，而约束满足「子区间也合法」，所以 [l,r], [l+1,r], …, [r,r] 全合法，共 r−l+1 个。 基础 Floyd 判圈（快慢指针）怎么工作？为什么步长要取 1 和 2？ 慢指针每次 1 步、快指针每次 2 步。无环时快指针先走到 nullptr ；有环时两者必然相遇。O(n) 时间、 O(1) 空间 （对比哈希表法的 O(n) 空间）。 为什么一定相遇 两者都进入环后，考察 相对位移 ：快指针每轮比慢指针多走 1 步，所以两者的环内距离每轮 恰好减少 1 。距离是环长内的非负整数，每轮减 1 ⟹ 最多「环长」轮后必然变成 0，即相遇。∎ 为什么不能取 1 和 3 步长差为 2 时，相对距离每轮减 2 。当环长为偶数而初始距离为奇数时，距离只在奇数间跳变， 永远跨过 0 而不等于 0 → 永不相遇。 步长差必须为 1 ，才保证「距离逐个递减、不可能跳过 0」。 找环入口 相遇后把一个指针放回头部，两者 同速 前进，再次相遇处就是入口。推导：设头到入口 a、入口到相遇点 b、相遇点回到入口 c，则相遇时 slow 走 a+b 、fast 走 a+b+k(b+c) ，且 fast 恰是 slow 的两倍： 2(a+b) = a+b+k(b+c) ⟹ a = (k−1)(b+c) + c 所以「从头走 a 步」和「从相遇点走 c 步（+整圈）」落在同一点。 识别信号 题目里有一个 确定性的后继函数 f(x) 且状态空间有限 → 从任意起点必然进入循环，Floyd 就适用。同一套数学解「寻找重复数」（把 i → a[i] 看成隐式链表）和「快乐数」。"},{"a":"s10-2","h":"10.2 中级（区分熟练度）","x":"10.2 中级（区分熟练度） 中级 「盛最多水的容器」为什么要移动较矮的那一侧？给出证明。 蓄水量 = min(h[l], h[r]) × (r − l) 。设 h[l] < h[r] ，考虑所有以 l 为左端、右端在 r 内侧的候选 (l, r') （ r' < r ）： 宽度 r' − l < r − l ， 严格变小 。 高度 min(h[l], h[r']) ≤ h[l] = min(h[l], h[r]) ， 不会变大 ——因为已经被较矮的 h[l] 卡住了。 两个因子都不增 ⟹ 这些候选 全都不优于当前的 (l, r) ⟹ 包含 l 的候选可以整行丢弃 ⟹ ++l 不漏最优解。∎ 为什么不能移动较高的一侧 移动 r 时：高度仍被 h[l] 卡着（不会变大）、宽度还变小， 但你丢掉的那一列里可能藏着更优解 ——那个较高的 h[r] 配上别的左端可能更好。所以会错。 一句话版本（面试时说这句） 「宽度必然减小，所以唯一可能赚回来的是提升高度上限；而高度上限由 短板 决定，换长板毫无意义。」这就是木桶原理——桶能装多少由最短的板决定，桶变窄是纯损失，只有换掉短板才有机会。 中级 「恰好 K 个不同整数的子数组个数」怎么做？为什么不能直接开窗口？ 不能直接开窗口 ，因为「恰好 K 个不同元素」 不满足「子区间也合法」 ——把窗口缩小，不同元素数会掉到 K 以下。滑动窗口只能处理「至多 K」这类关于伸缩单调的约束。 恰好 K = 至多 K − 至多 (K−1) exactly(K) = atMost(K) − atMost(K−1) atMost(m) 用标准变长窗口，统计「以每个 r 结尾的合法子数组个数」： long long at_most ( const std::vector< int >& a, int m) { if (m < 0 ) return 0 ; // k=0 时 m=-1，必须守卫 std::unordered_map< int , int > cnt; long long ans = 0 ; int l = 0 ; for ( int r = 0 ; r < ( int )a. size (); ++r) { ++cnt[a[r]]; while (( int )cnt. size () > m) if (--cnt[a[l++]] == 0 ) cnt. erase (a[l- 1 ]); ans += r - l + 1 ; } return ans; } 为什么 ans += r−l+1 收缩完成后 [l,r] 是以 r 结尾的 最长 合法子数组。「至多 m」满足子区间也合法 ⟹ [l,r], [l+1,r], …, [r,r] 全合法，共 r−l+1 个。 通用框架 「 枚举右端点，统计以它结尾的合法区间数 」是所有「求子数组个数」题的共同框架，不只用于窗口——前缀和 + 哈希表也是这个框架（枚举右端点，查有多少个合法左端点）。 而「做差」技巧适用于任何「工具只会至多/至少，题目要恰好」的场合：恰好 K 个奇数、恰好 K 个不同字符、和恰好为 K。 中级 什么情况下滑动窗口 不能 用？举两个具体例子。 滑动窗口要求 约束关于窗口的伸缩单调 ——即「如果 [l,r] 合法，那么它的任意子区间也合法」（或反向单调）。不满足就不能用。 例一 · 数组含负数的「和」类题 「和恰好为 K 的子数组个数」，数组可含负数。和不随窗口变长单调递增： [2,-3,2] 的和是 1，去掉末尾变成 −1，再去掉变成 2—— 缩小窗口不保证和变小 。 正解 ：前缀和 + 哈希表。枚举右端点 r，查 prefix[r] − K 出现过几次，O(n)。 例二 · 含负数的「乘积」类题 「乘积 < K 的子数组个数」在 全正数 时可以开窗口（乘积随窗口变长单调递增）。一旦含负数，乘积的符号会翻转，单调性完全断裂；含 0 时还有除零问题。 正解 ：按 0 切段 + 前缀积/符号分类讨论。 隐藏前提 「数组是否全为正」是滑动窗口题的隐藏前提 。表面上题目只是给了一堆数，但和/积的单调性完全依赖这一点。 面试里如果题面没说「正整数」，一定要主动问一句 ——既避免写错，也体现你知道前提在哪。 另一类失效：约束是「恰好」 「恰好 K 个不同元素」也不满足子区间性质，要用「至多 K − 至多 K−1」做差（见上一题）。 替代工具速查 约束不单调时按题型换工具：求 个数 且和恰好 → 前缀和+哈希表；求 最短 且含负数 → 前缀和+单调队列；求 最大子数组和 → Kadane（DP）；求 窗口极值 → 单调队列 。 中级 「最小覆盖子串」里怎么 O(1) 判断「已覆盖目标集合」？ 不要每次都遍历计数数组检查（那是 O(128n) 或 O(nk)），而是维护一个整数 formed = 已经「满额」的字符种数 ，判断变成一次比较 formed == required 。 int need[ 128 ]={}, have[ 128 ]={}; for ( unsigned char c : t) ++need[c]; int required = 0 ; // t 中不同字符的种数 for ( int c= 0 ;c< 128 ;++c) required += (need[c] > 0 ); int formed = 0 ; // 加入： ++have[c]; if (need[c] > 0 && have[c] == need[c]) ++formed; // 恰好相等才 +1 // 移出： if (need[d] > 0 && have[d] == need[d]) --formed; // 先判再减！ --have[d]; 两条维护规则必须写对 ① 加入后只在 have[c] == need[c] （ 恰好相等 ）时 ++formed ——超额再加不能重复计数； ② 移出时先在 have[d] == need[d] 时 --formed ， 再 递减 have[d] 。 顺序反了就会漏判。 这题是「求最短」 所以收缩条件是「 已 合法」，统计放在 while 之内 （缩之前先记录当前长度）。 可迁移的手法 「 把一个需要 O(k) 检查的条件，压缩成一个可增量维护的计数器 」——这个手法在窗口题里反复出现：判「窗口内无重复」维护重复字符数、判「已覆盖」维护满额种数、判「所有条件达标」维护达标项数。 它的本质是把「检查」变成「在状态变更点做 O(1) 的差分更新」，和差分数组、Fenwick 树的思路同源。 别忘了 unsigned char char 在多数平台有符号，非 ASCII 字节会得到 负数下标 （UB）。必须写 (unsigned char)s[r] 。含多字节字符（UTF-8 中文）时更不能按字节开窗——一个字符占 1–4 字节，按字节滑动会切断字符。"},{"a":"s10-3","h":"10.3 高级（体现深度）","x":"10.3 高级（体现深度） 高级 设计一个限流器。固定窗口、滑动窗口、漏桶、令牌桶各有什么取舍？ 四种算法复杂度都是 O(1)，差别在 边界突刺、内存开销、是否允许突发 。 算法 边界突刺 内存 允许突发 固定窗口 有，可达 2 倍限额 O(1) 不受控 滑动日志 无 O(QPS) 不受控 滑动计数（分格） 近似无 O(格数) 不受控 漏桶 无 O(1) 不允许 令牌桶 无 O(1) 受控（burst 参数） 固定窗口的突刺 限额 100/秒。攻击者在 0.9s 打满 100，1.1s 窗口重置后再打 100 → 0.2s 内通过 200 。这是它不能用于严格限流的原因。 工程默认选令牌桶 状态只需 (上次时间, 当前令牌数) 两个字段， 惰性补充 （被调用时按经过时间算该补多少），不需要后台定时器，而且能表达「允许合理突发」这个真实业务需求（用户批量操作、重试风暴后的恢复）。 bool allow ( double now, double need = 1 ) { tokens_ = std:: min (burst_, tokens_ + (now - last_) * rate_); last_ = now; if (tokens_ >= need) { tokens_ -= need; return true ; } return false ; } 三个实现要点 ① 必须用 单调时钟 （ steady_clock ），墙上时钟会被 NTP 调整或闰秒搞乱； ② 惰性补充优于定时器——不需额外线程，限流器可按 key 动态增长； ③ 分布式限流 要把状态放 Redis 并保证原子性（Lua 脚本），否则 N 个副本各自限流会把总配额放大 N 倍。 再往上一层 限流只是「过载保护」的一环，单独用它会把问题推给上游。生产上要配 排队 + 超时 （被限流的请求是立即拒绝还是排队？排多久？）、 优先级 （限流时先丢哪类流量）、 自适应限流 （按实测延迟动态调整配额，而不是拍一个固定值）。见 限流 · 熔断 · 降级 · 隔离 。 高级 算法题的「滑动窗口」和流处理系统的「窗口」有什么本质区别？ 算法题的窗口有一个 隐含前提：数据按顺序到达、且不会迟到 。所以左边界前进是 无条件安全 的。流处理里这个前提不成立——这就是全部区别的来源。 乱序从哪来 网络抖动、分区消费速度不均、客户端时钟偏差、上游重试。事件的 产生时间（event time） 和 到达时间（processing time） 不一致，且没有上界。 水位线（watermark） 系统对「事件时间已推进到哪里」的估计。窗口在水位线越过其结束时间后才触发计算。 它把「左边界何时可以安全前进」从一个无条件动作变成了一个估计 。 允许迟到（allowed lateness） 水位线过去后仍保留窗口状态一段时间，迟到数据到达就 增量更新 已发出的结果；再迟则进侧输出流或丢弃。 这是一个正确性与延迟的权衡 水位线放得越宽 → 结果越准，但延迟越高、状态占用越久 。算法题里没有这个权衡（数据是完整且有序的），工程里它是核心设计决策。 而且它还牵出 幂等性 要求：迟到数据会导致同一个窗口的结果被多次发出，下游必须能处理「同一 key 的结果更新」而不是简单累加。 窗口类型也更丰富 滚动（不重叠定长）、滑动/跳跃（定长+定步长，可重叠）、 会话窗口 （按空闲超时自动切分，长度不定）、全局窗口+自定义触发器。会话窗口在算法题里根本没有对应物——它的边界由数据本身的间隙决定。 还有一个：状态要能容错 算法题的窗口状态在内存里，进程挂了重跑就行。流处理的窗口状态可能有几十 GB 且必须 可 checkpoint、可恢复、且恢复后语义一致 （exactly-once）。这是 Flink 状态后端和 Spark 结构化流的主要复杂度来源。见 Spark 内核 。 高级 为什么 std::remove 不删除元素？这个设计说明了 STL 的什么原则？ 因为算法只拿到 迭代器 ，拿不到容器—— 迭代器无法改变容器大小 。 std::remove 能做的只有「把要保留的元素搬到前面，返回新的逻辑末尾」。 It remove (It first, It last, const T& value) { first = std:: find (first, last, value); if (first == last) return first; It write = first; // 写指针 for (It read = ++first; read != last; ++read) // 读指针 if (!(*read == value)) *write++ = std:: move (*read); return write; // 新逻辑末尾 } 后果 size() 不变，返回值之后那段是 未指定的值 （被 move 走的空壳）。所以单独调用 std::remove(v.begin(), v.end(), 3) 什么都没删掉 。 正确写法 // erase-remove 惯用法 v. erase (std:: remove (v. begin (), v. end (), 3 ), v. end ()); // C++20 起（推荐） std:: erase (v, 3 ); std:: erase_if (v, []( int x){ return x % 2 ; }); 体现的 STL 原则：算法与容器解耦 算法只依赖 迭代器概念 ，所以一份 remove 能作用于 vector、deque、array、裸数组、甚至自定义容器。代价就是它 只能做「重排」不能做「增删」 。 同一原则的其他体现： std::unique 也不删除、 std::partition 只重排、 std::sort 要求随机访问迭代器所以对 list 不可用（ list 自带 sort 成员）。 C++20 补上短板的方式 std::erase / std::erase_if 是 针对容器 的自由函数重载（每个容器一份特化），而不是泛型算法——正是因为「改变大小」必须知道容器类型。这也印证了原来的设计不是疏忽，而是概念边界的必然结果。 相关的 STL 接口取舍见 STL 算法库 。 高级 线上一个「统计近 5 分钟错误率」的接口在流量高峰时内存暴涨，怎么排查和重构？ 「近 5 分钟」+ 「内存随流量增长」几乎可以直接定位：实现用的是 滑动日志 ——把每个请求的时间戳（甚至完整记录）存进一个队列，查询时扫一遍算 5 分钟内的数量。 ① 确认内存去向 看 RSS 增长是否与 QPS 线性相关；用 heap profiler（jemalloc/tcmalloc 的 profile、或 massif ）看是不是那个容器占主导。 滑动日志的内存是 O(QPS × 窗口秒数) ——QPS 从 1k 涨到 50k，5 分钟窗口就从 30 万条变成 1500 万条。 ② 顺带会发现第二个问题 滑动日志的查询通常是 O(窗口内条数)。内存涨的同时 查询延迟也在涨 ，两个症状同源。如果清理是「查询时惰性清理」，那高峰期第一次查询会一次性删掉几百万条 → 一个长停顿尖刺 。 重构：把「精确日志」换成「有界的近似聚合」。 方案一 · 分格滑动计数（推荐） 把 5 分钟切成 30 个 10 秒的格子， 每格只存一个计数 （成功数 + 失败数）。查询时把 30 个格子加起来。内存从 O(QPS×300) 降到 O(30) ，查询 O(30)。 代价是 边界精度损失 最多一格（10 秒）——对「错误率」这类监控指标完全可接受。格数就是精度与内存的旋钮。 方案二 · 指数衰减（EWMA） 只存一个数： rate = α·新样本 + (1−α)·rate 。内存 O(1)、更新 O(1)。 适合做告警和自适应限流 ，但它不是「严格的 5 分钟窗口」，而是一个加权平均——如果需求真的是「精确 5 分钟」（比如 SLA 计算），就不能用。 选型的判据是「这个数字给谁看」 驱动自动决策 （熔断、限流、扩缩容）→ EWMA 或分格计数都行，平滑性反而是优点。 对外承诺 / 计费 / SLA → 需要可解释、可复算的定义，用分格计数并把格粒度写进文档。 事后排查 → 不要在进程内保留，直接打点到时序数据库（Prometheus / VictoriaMetrics），让专门的系统去做降采样和长期存储。 这是一类通用的取舍 「 精确但无界 」换成「 近似但有界 」是流式统计的通用手段：计数 → 分格计数 / EWMA；去重计数 → HyperLogLog ；Top-K → Count-Min Sketch；分位数 → t-digest / DDSketch。 共同点是： 用一个可控的误差上界，换 O(1) 或 O(log n) 的空间 。反过来说，动手前必须先问清「误差多少可以接受」——没有这个数字，就没法选。 ⬅️ 二分查找 ➡️ 单调栈与前缀和 📐 复杂度与分析 🚦 限流与熔断 📚 目录 🏠 主页 双指针与滑动窗口 — 势能法证 O(n) · Floyd 判圈 · 窗口四步骨架 · 恰好 K 的拆解 · 限流与 TCP 窗口 — Kavin Tech Notes · 2026"}]},{"u":"algorithms/union_find_and_range.html","t":"并查集与区间结构","c":"算法与数据结构","s":[{"a":"","h":"","x":"并查集与区间结构 · 路径压缩与 α(n) / 扩展域与带权 / 树状数组 lowbit / 线段树懒标记 / 稀疏表可重叠合并 Kavin Tech Notes ← 主页 目录 30 min ◐ 并查集与区间结构 两类问题，一个动机 与其他章节的关系 并查集 连通分量 路径压缩与按秩合并 α(n) 变形与 Kruskal 带权并查集 离线与在线 Kruskal 前缀和与差分 前缀和 O(1) 差分数组 树状数组与线段树 树状数组 线段树区间 稀疏表 可重叠合并 三者适用条件 选型决策表 结构对照（交互） 陷阱清单 面试题集（12 道） 9.1 基础 9.2 中级 9.3 高级 Algorithms · Union-Find & Range · 2026 并查集与区间结构 这一篇讲四个「为特定查询定制的结构」： 并查集 回答「这两个东西连通吗」， 树状数组 和 线段树 回答「这段区间的聚合值是多少」， 稀疏表 回答「这段区间的最值是多少」。它们的复杂度都漂亮到不像话（α(n)、log n、O(1)），而每一个漂亮背后都有一个明确的 前提 ——只合并不分裂、可逆、可结合、可重叠。搞清前提，选型就不用猜。 核心难度 ★★★★☆ 面试出现率 高 阅读时长 ~30 min 前置 树 · 前缀和 路径压缩与 α(n) 扩展域并查集 树状数组 lowbit 线段树懒标记 稀疏表"},{"a":"s1","h":"两类问题，一个动机","x":"01 两类问题，一个动机 动机都是「别每次都重新扫一遍」 结构 回答什么问题 关键前提 复杂度 并查集 （§2） 「a 和 b 在同一集合吗」 「把 a 和 b 的集合合并」 只合并、不分裂 α(n)，实际 ≤ 4 前缀和 / 差分 （§4） 「不改的区间和 / 先改完再查」 运算 可逆 ；改查 不交错 O(1) 树状数组 / 线段树 （§5） 「改查交错的区间聚合」 BIT 要 可逆 ；线段树只需 可结合 O(log n) 稀疏表 （§6） 「区间最值查」， 不支持修改 运算 可重叠合并 O(1) 查，O(n log n) 建 选型先看前提，再背模板。 并查集只合并不分裂 ——路径压缩把树压平，父子关系回不去。要删边就 离线倒着加 （§3.2）。 树状数组要可逆 ——靠前缀相减拿区间，max/min 没逆元（与 前缀和 同一条判据）。 线段树只需可结合 ——所以能做 max/min/gcd，这是它比 BIT 通用的地方。 稀疏表要可重叠合并 —— max 不怕两段重叠； 求和不行 （中间会加两次）。 四个结构是四种档案索引。 并查集 像户籍合并：只记户主，合户容易、分家不行。 树状数组 像分层账本，改一天只动它所在的几层小计。 线段树 把仓库切成嵌套分区，改整区就在门口挂便条（懒标记），有人进来再往下传。 稀疏表 预先算好所有 2 的幂长度最值，两段盖住即可——最值不怕重复统计。"},{"a":"s1-1","h":"1.1 与其他章节的关系","x":"1.1 与其他章节的关系 单调栈与前缀和 —— 「可逆 / 可结合 / 可重叠」三个判据在那里首次出现；本篇的前缀/差分是那边的区间结构续篇 树与平衡树 —— 线段树是「为区间查询而设计的树」，结构固定，不需要旋转平衡 复杂度与分析 —— α(n) 的证明用的是势能法，也是「聚合法做不到」的典型 最短路与最小生成树 —— Kruskal 完全建立在并查集上，本篇只讲 DSU 在里面扮演的角色 图遍历与拓扑排序 —— 无向图连通性可以用并查集也可以用 DFS；在线加边走前者，要路径/奇环走后者"},{"a":"s2","h":"并查集","x":"02 并查集 森林 + 两个优化 → 摊销 α(n)"},{"a":"s2-1","h":"2.1 连通分量","x":"2.1 连通分量 每个集合是一棵树，根代表整个集合。 find 相同 ⟺ 同一连通分量； unite 把一棵树的根挂到另一棵下。前提是 只加边、不删边 。朴素沿父指针走，按插入顺序 unite 会退化成链， find 变成 O(n)——下面两张图分别解决「已经很长」和「不要先长成链」。 连通分量：find 相同 ⟺ 同一分量 · unite(3,4) 把右边整棵树挂过来 分量 A（根 1） 1 2 3 分量 B（根 4） 4 5 6 unite(3,4) 查 / 合 各一次 find(2)==find(3) → 已连通 find(2)!=find(5) → 未连通 unite 成功 → 连通块 −1 unite 失败 → 加这条边会成环 （Kruskal 就靠这个 bool） 连通性、连通块计数、判环，都是「根相不相同」这一个问题。无向图连通性也可以 DFS，但并查集支持 在线加边 ，且 Kruskal / 动态连通块计数几乎只能靠它。"},{"a":"s2-2","h":"2.2 路径压缩与按秩合并","x":"2.2 路径压缩与按秩合并 朴素 find 沿父指针走，链化后是 O(n)。两个优化各自独立：压缩把路径压平（查询本身在优化结构），按秩（或按大小）保证树不会先长成链。合起来才是 α(n)。生产里合成一块写： // find 路径压缩 + unite 按大小（秩）。两者同用 → 摊销 α(n) std::vector< int > p, sz; int find ( int x) { if (p[x] != x) p[x] = find (p[x]); // 回溯时直接挂到根 return p[x]; } bool unite ( int a, int b) { a = find (a); b = find (b); if (a == b) return false ; // 已连通 → 成环 if (sz[a] < sz[b]) std:: swap (a, b); // 小挂大 p[b] = a; sz[a] += sz[b]; return true ; } 路径压缩：find(5) 把路径上所有节点直接挂到根 压缩前 1 2 3 5 走 3 步 压缩 压缩后 1 2 3 5 之后 find 只需 1 步 写法 递归：短，深路径可能爆栈 迭代 / 路径减半：一趟 把节点挂到祖父 只压不按秩 → 用迭代 查询本身在优化结构：走过的路径被压平，下次常数步。递归版短但深路径可能爆栈；路径减半 p[x]=p[p[x]] 一趟完成，渐进同样是 α(n)，竞赛常用。 按秩 / 按大小：小树挂到大树下 · 深度只在「被挂」时 +1，且树至少翻倍 ✅ 小挂大（sz 3+1） A·3 2 3 B 深度不变，A 变成 4 下次再被挂，大小已翻倍 ❌ 大挂小 → 链 B A·3 2 3 深度 +1，且会越挂越长 为什么深度 ≤ log n 节点深度只在「整棵树 被挂到别人下面」时 +1 小挂大 ⇒ 被挂方大小 至少翻倍才会再 +1 翻倍次数 ≤ log₂ n 返回 bool 很有用：连通块 −1、Kruskal 判环、去重边，都靠「这次是否真合并」。按大小和按秩（近似树高）效果同类；路径压缩会打乱真实高度，所以按大小更常见、也更好维护。"},{"a":"s2-3","h":"2.3 α(n)","x":"2.3 α(n) 只用压缩：摊销 O(log n) 只用按秩：最坏 O(log n) 两者同时用 ：摊销 O(α(n)) α = 反 Ackermann，n < 2^2^2^2^16 时 ≤ 4 Fredman–Saks：指针实现做不到 O(1) 摊销 —— α(n) 是下界，不是「还没优化到常数」 证明走势能法 （Tarjan 1975，详见 摊销分析 ）：每个节点的势能由秩和到根的距离决定；一次 find 的实际代价加上势能变化不超过 O(α(n))。这是「操作交错、模式不规整，聚合法做不到」的典型。工程上直接当常数；竞赛里 只压不按秩 也常见（少一个数组），这时 find 用迭代或路径减半，避免递归在未压缩的深链上爆栈。反过来只按秩不压缩没必要——压缩几乎免费。"},{"a":"s3","h":"变形与 Kruskal","x":"03 变形与 Kruskal 边上带权、倒着加边、最小生成树"},{"a":"s3-1","h":"3.1 带权并查集","x":"3.1 带权并查集 除 parent 再存「到父节点的权」。压缩时沿路径累加/累乘，就能答「a 相对 b 差多少 / 比例多少」。公式必须自己推：写出压缩后已知关系仍成立，解出待求的 w。 带权边：已知 a/b=2、b/c=3 → 压缩后直接读 a/c = 6 压缩前（权在父边上） a b c ×2 ×3 w[a]=a/b w[b]=b/c 压缩 压缩后（都挂到根 c） c a b ×6 ×3 查询 同根才有 相对关系 a/c = 6 加法版（差值）把乘除换成加减。写错公式不会崩，只会给错答案。 // 除法求值：w[x] = x / parent[x]；压缩时沿路径累乘 int find ( int x) { if (p[x] == x) return x; int r = find (p[x]); w[x] *= w[p[x]]; // 先乘旧父的权，再改指针 return p[x] = r; } void unite ( int a, int b, double val) { // a/b = val int ra = find (a), rb = find (b); if (ra == rb) return ; p[ra] = rb; w[ra] = val * w[b] / w[a]; // 让合并后 a/b 仍 = val } 公式必须自己推，不能背。推导：合并后 a/b 仍应等于 val ，而 a/b = (w[a] · w[ra]) / w[b] ，令它等于 val 即得 w[ra] = val * w[b] / w[a] 。加法版（差值）把乘除换成加减。 写错不会崩，只会给错答案。 扩展域 （种类并查集）表达「敌对」：给 x 开 x 与 x+n 两个点。同组 unite(a,b) 且 unite(a+n,b+n) ；敌对 unite(a,b+n) 且 unite(a+n,b) 。矛盾： find(x)==find(x+n) 。食物链开三倍点。 并查集 ：在线、代码短，只答「有没有矛盾」，不给染色方案。 DFS/BFS 染色 ：能给出方案、能输出奇环，但要先建好整图（离线）。 题目说「边一条条来、每次都要输出」→ 并查集；「给定一张图，判断/输出方案」→ DFS。"},{"a":"s3-2","h":"3.2 离线与在线","x":"3.2 离线与在线 并查集 不能删边 。路径压缩把父子关系丢掉；即使不压，「从一个集合拆出一部分」也不是它能表达的操作。题目若要「依次删边、每次报连通块」，标准绕法是 离线倒过来 ：删除变添加。先读完所有删除得到最终图，再逆序把删掉的边 unite 回去，答案数组反转输出。典型题：打砖块、移除同行同列石头。 离线 vs 在线：并查集只会「加边」；删边必须先看完全部操作再倒放 在线：边一条条来，立刻答 t=1 unite(1,2) 连通？是 t=2 unite(2,3) 连通？是 t=3 问 1 与 4 否 边到达即可合并，α(n) 扩展域判二分图也走这条 离线：先读完，再倒着加回去 题目：依次删边，每次报块数 ① 得到最终图（全删完） ② 逆序把删掉的边 unite 回去 ③ 答案数组反转输出 必须离线。要在线删边 → LCT 倒放拆迁录像：拆楼是分裂，倒着放就是盖楼。必须能先看完整段录像（离线）。 可撤销并查集必须放弃路径压缩 （压缩不可逆），只用按秩合并 O(log n) + 栈记下每次改了什么，用于线段树分治：边挂在时间线段树上，DFS 加边、回溯撤销。真正在线加删边才需要 LCT——面试能说出名字和适用范围即可。"},{"a":"s3-3","h":"3.3 Kruskal","x":"3.3 Kruskal 最小生成树 的 Kruskal：边按权排序，依次尝试加入； unite 返回 true 就收下（还没连通），false 就丢掉（会成环）。并查集是判环的 α(n) 实现，不是可选优化。排序是 O(m log m)，DSU 可忽略。稀疏图、边可预先排序时 Kruskal 比 Prim（堆 + 切分）更干净；稠密图或只要点增量时 Prim 更合适。 Kruskal：排序后扫边，unite 成功收下，失败（已同根）丢弃 A B C D 1 ✓ 2 ✓ 3 ✓ 4 ✗ 成环 5 不必看 边按权 1 A–B unite ✓ 2 A–C unite ✓ 3 B–D unite ✓ 4 A–D 同根，丢 已 n−1 条，停 MST 权 = 1+2+3 并查集在干什么 每次 unite 就是「这条 边会不会把两棵树 连成环」 false → 跳过 复杂度 O(m log m) 排序主导，DSU 可忽略 Prim 走堆 + 切分；Kruskal 走排序 + 并查集。稀疏图、边可排序时 Kruskal 更干净。"},{"a":"s4","h":"前缀和与差分","x":"04 前缀和与差分 不交错时别上树：O(1) 查 / O(1) 改"},{"a":"s4-1","h":"4.1 前缀和 O(1) 区间","x":"4.1 前缀和 O(1) 区间 先全建再查、中间不改，区间和就是两次前缀相减。前提还是 可逆 ：知道 max(a[1..r]) 和 max(a[1..l−1]) ，推不出 max(a[l..r]) 。这条判据和树状数组是同一条——BIT 的 range 也是两次 prefix 相减。 前缀和：S[i] = a[1]+…+a[i] ⟹ sum[l..r] = S[r] − S[l−1] O(1) a 3 1 4 2 5 1 要的区间 [3,5] = 4+2+5 S 3 4 8 10 15 16 S[5] = 15 S[2] = 4 15 − 4 = 11 减掉 [1,2]，剩下 [3,5] 不能做的 区间 max / min 没有逆元，减不掉 查询与修改交错 → 前缀每次都要重建 改用 BIT / 线段树 建 O(n)、查 O(1)。修改和查询交错，这个结构立刻失效——每次改完都重建前缀是 O(n)，还不如一开始就上 BIT。"},{"a":"s4-2","h":"4.2 差分数组","x":"4.2 差分数组 差分是前缀和的逆：对原数组区间加，等价于差分数组两个单点改。全部加完再扫一遍还原。适合「先改完、最后统一查」。m 次区间加如果每次真去改 [l,r]，是 O(mn)；差分把它变成 O(m+n)。和查询交错时，把 d 丢进树状数组（§5）。 区间 [2,5] += 3：d[2]+=3，d[6]-=3；还原 a[i] = a[i-1]+d[i] 下标 1 2 3 4 5 6 d 原 2 0 1 0 -1 0 d 改 2 +3 1 0 -1 -3 左端 +v 右端后 −v a 还 2 5 6 6 5 2 两次单点 = 一次区间加 前缀和把 +v 一直带到末尾 −v 把它在 r+1 截断 m 次区间加 + 一次还原 = O(m + n)，不是 O(mn) 要边加边查 → 差分套 BIT 差分数组把「区间加」变成两个端点。和查询交错时，把 d 丢进树状数组（§5）：维护差分数组的 BIT，区间加仍是两次 add，单点查变成 prefix。再套一层 i·d 就能区间查。 // 前缀和区间查 + 差分数组区间加（1-based） for ( int i = 1 ; i <= n; ++i) S[i] = S[i- 1 ] + a[i]; // sum(l..r) ans = S[r] - S[l - 1 ]; // [l,r] += v，最后再扫一遍还原 d[l] += v; d[r + 1 ] -= v; for ( int i = 1 ; i <= n; ++i) a[i] = a[i- 1 ] + d[i];"},{"a":"s5","h":"树状数组与线段树","x":"05 树状数组与线段树 改查交错时上树；能用 BIT 就别写线段树"},{"a":"s5-1","h":"5.1 树状数组","x":"5.1 树状数组 tree[i] 管辖 a[i−lowbit(i)+1 .. i] 。查询 i -= lowbit 每次消掉一个二进制 1，更新 i += lowbit 向高位进位，都是 O(log n)。 必须 1-based ： lowbit(0)=0 会死循环。区间靠前缀相减，所以还是要可逆——max 不行。 树状数组： tree[i] 管辖 a[i−lowbit(i)+1 .. i] ，共 lowbit(i) 个元素 i=1 lowbit=1，管 a[1] i=2 lowbit=2，管 a[1..2] i=3 lowbit=1，管 a[3] i=4 lowbit=4，管 a[1..4] i=5 lowbit=1，管 a[5] i=6 lowbit=2，管 a[5..6] i=8 lowbit=8，管 a[1..8] lowbit(i) = i & (−i) 取出二进制 最低位的 1 6 = 0110 → 0010 = 2 8 = 1000 → 1000 = 8 沿二进制跳，最多 log n prefix(i)：i -= lowbit 7→6→4→0 add(i)：i += lowbit 3→4→8→16 差分套进去：1 个 BIT 做区间改+单点查；2 个 BIT（d 与 i·d）做区间改+区间查。 // 单点加 + 区间和。必须 1-based；range 依赖可逆 void add ( int i, long long v) { for (; i <= n; i += i & -i) t[i] += v; } long long prefix ( int i) { long long s = 0 ; for (; i > 0 ; i -= i & -i) s += t[i]; return s; } long long range ( int l, int r) { return prefix (r) - prefix (l - 1 ); } 需求 做法 BIT 个数 单点改 + 区间和 上面这一份 1 区间改 + 单点查 BIT 维护差分数组 d； add(l,v) 、 add(r+1,−v) ； a[i]=prefix(i) 1 区间改 + 区间和 维护 d 与 i·d 两个 BIT 2 「有多少个 ≤ x」 下标改成 值 （先离散化）；从右往左扫可求逆序对 1 a[i] = Σ(j≤i) d[j] ⟹ prefix_a(i) = (i+1)·Σ d[j] − Σ(j·d[j]) 区间 [l,r] += v：两个 BIT 各做两次 add。值域 BIT 大小 = 值域，10⁹ 必须先离散化。 必须 1-based ： lowbit(0)=0 会死循环。max/min 没有逆元，不要硬套 prefix(r)−prefix(l−1) 。能用 BIT 就用 BIT——代码是线段树的四分之一，常数快 2–3 倍。"},{"a":"s5-2","h":"5.2 线段树区间","x":"5.2 线段树区间 根管 [1,n] ，对半切到叶子。只需 可结合 ，所以 max/min/gcd、区间赋值都能做——这是它比 BIT 通用的地方。任意查询最多拆成 O(log n) 个「完全被包含」的节点：每一层最多 2 个部分相交（左右边界所在），只有它们继续往下。用数组存时开 4n ：n 不是 2 的幂时最底层会错开，2n 不够，这是最常见的 RE。 查询 [3,7]：完全覆盖的节点直接返回（黄），部分相交才继续往下 [1,8] 部分 [1,4] 部分 [5,8] 含 [5,7] [1,2] [3,4] ✓ [5,6] ✓ [7,8] 部分 [7] ✓ [8] [3,7] = [3,4] + [5,6] + [7] —— 三个完全覆盖节点，O(log n)。精确拆分、不重叠，所以求和 / 最值都能做。 线段树精确拆区间，既不「减掉不要的」也不「重叠盖住」。代价是代码和常数都比 BIT 大。面试能画出「查询如何拆成 O(log n) 段」比默写 60 行模板更有用。 区间 [2,5] 全部 +3：修改停在完全覆盖的节点，打 lazy，孩子先不动 [0,7] sum+=12 [0,3] sum+=6 [4,7] sum+=6 [0,1] [2,3] lazy=3 [4,5] lazy=3 [6,7] 孩子不动 孩子不动 节点自己的 sum 立刻更新；往下走之前 push_down。query 里也要 push——漏了读到过期值。区间和 apply 要乘长度。 懒标记让区间改也是 O(log n)。四个坑：① query 里也要 push_down （只在 update 里 push 是最常见的错）；② 区间和对「和」要乘长度，对最值不乘；③ 赋值覆盖加，打赋值必须清掉旧的加标记；④ 标记必须可合并——区间开平方不行，改用势能线段树暴力下到叶子。 变体 解决什么 动态开点 值域 10⁹ 但点稀疏；在线。能离线就离散化，常数更小 权值线段树 下标是值，答第 k 小 / 有多少 ≤ x 主席树 每次改只新建一条路径 → 区间第 k 小、历史版本 线段树上二分 把外层二分融进下降过程，O(log n) 而非 O(log² n) 线段树上二分 把「找第一个前缀和 ≥ target 的位置」从「外面二分 + 每次 query」（O(log² n)）收成一次根到叶的下降：每到一个节点，用左孩子的聚合值一次比较就决定往哪边走。前提是聚合值关于位置单调（前缀和要求元素非负）。同一技巧也适用于 BIT 上二分（从高位 lowbit 往低位试）、主席树求区间第 k 小、平衡树求排名。"},{"a":"s6","h":"稀疏表","x":"06 稀疏表 O(1) 查最值，代价是不能改"},{"a":"s6-1","h":"6.1 可重叠合并","x":"6.1 可重叠合并 st[k][i] = 区间 [i, i+2^k−1] 的聚合。查询取 k = ⌊log₂(r−l+1)⌋ ，用两个长度 2^k 的区间盖住 [l,r] ，允许中间重叠。前提叫 可重叠合并 （idempotent）： max(max(a,b), max(b,c)) = max(a,b,c) ，重复算 b 不影响结果。求和会加两次，异或会抵消，所以稀疏表不能求和。O(n log n) 预处理、O(1) 查；任何修改都要整体重建。 查 [2,8]：len=7，k=2（长度 4）· 两段 [2,5] 与 [5,8] 盖住，中间重叠不怕 下标 1 2 3 4 5 6 7 8 st[2][2] = [2,5] st[2][5] = [5,8] 重叠 5 能 / 不能 ✓ max min gcd 与或 ✗ 加（加两次） ✗ 异或（抵消） O(n log n) 预处理、O(1) 查、不支持修改。要改就只能线段树。 运算 可重叠？ 稀疏表 max / min / gcd / 与 / 或 是 能 加法 / 异或 否 不能"},{"a":"s6-2","h":"6.2 三者适用条件","x":"6.2 三者适用条件 要求依次放宽：可逆 → 可重叠 → 可结合；能力增强，代码和常数也变大 前缀和 / BIT 要求：可逆 ✓ 加、异或、乘 ✗ max / min / gcd 靠 S[r] − S[l−1] 前缀 O(1) / BIT 能改 稀疏表 要求：可重叠合并 ✓ max min gcd 与或 ✗ 加、异或 两段 2^k 盖住即可 O(1) 查，不能改 线段树 要求：仅可结合 ✓ 上面全部 精确拆分，不减不叠 O(log n) 查改 + 懒标记 代码最长、常数最大 前缀和能算的稀疏表算不了，稀疏表能算的前缀和算不了；线段树覆盖两者。前缀和像累计台账（能减才行），稀疏表像按 2/4/8 天预记最高温（重复统计不影响「最高」），线段树精确拆年、什么都能算，代价是每次走 log n 层。"},{"a":"s7","h":"选型决策表","x":"07 选型决策表 两个问题就能定下来 需求 结构 预处理 单次操作 代码量 区间和查， 无修改 前缀和 O(n) O(1) 3 行 区间最值查， 无修改 稀疏表 O(n log n) O(1) 15 行 区间加， 最后统一查 差分数组 — O(1) 改 3 行 单点改 + 区间 和 查 树状数组 O(n) O(log n) 10 行 区间改 + 区间 和 查 两个树状数组 O(n) O(log n) 25 行 区间改 + 区间 最值 查 线段树 + 懒标记 O(n) O(log n) 60+ 行 「第 k 小」/「有多少 ≤ x」 权值 BIT 或权值线段树 O(n log n) O(log n) 15–50 行 「区间第 k 小」/ 历史版本 可持久化线段树（主席树） O(n log n) O(log n) 80+ 行 连通性（只加边） 并查集 O(n) α(n) 15 行 连通性（要删边） 离线逆向 + 并查集，或 LCT — α(n) / O(log n) 20 / 200+ 行 两个问题定选型。 修改和查询是否交错？ 不交错（先全改再全查，或根本不改）→ 前缀和 / 差分 / 稀疏表，O(1)，代码三五行。这一步经常被跳过，直接上线段树是过度设计。 运算有什么性质？ 可逆（加、异或）→ 树状数组 ，短、常数快 2–3 倍。只可结合（max、min、gcd、区间赋值）→ 线段树 。 能用 BIT 就用它。线段树独有的是：不可逆运算、复杂懒标记、树上二分、动态开点 / 主席树。值域极大时，能离线就离散化，否则动态开点。"},{"a":"s7-1","h":"7.1 结构对照（交互）","x":"7.1 结构对照（交互） 四个结构的前提、模板与陷阱 点击结构名，看它的关键前提、核心代码、以及最容易写错的地方。 并查集 树状数组 线段树 稀疏表"},{"a":"s8","h":"陷阱清单","x":"08 陷阱清单 review 时逐条扫一遍 陷阱 症状 修法 树状数组用 0-based 死循环（ lowbit(0)=0 ） 一律 1-based，外部下标 +1 线段树数组只开 2n 越界（RE），n 非 2 的幂时必现 开 4n query 里漏 push_down 读到过期值，结果错但不崩 凡是要往下递归，先结清懒标记 apply 忘乘区间长度 区间加对「和」的影响算错 t[p] += v * (r-l+1) ；最值则不乘 多种懒标记顺序错 「区间赋值 + 区间加」混用时错 赋值优先级更高：打赋值标记时清掉已有的加标记 标记不可合并却用懒标记 结果错 如「区间开平方」→ 用势能线段树暴力递归 值域 BIT 忘了离散化 MLE（值域 10⁹） 先 sort + unique + lower_bound 压缩 并查集用了路径压缩又想撤销 回滚后结构错乱 可撤销版本 必须放弃路径压缩 ，只按秩合并 并查集递归 find 爆栈 SIGSEGV，只在大数据下出现 用迭代版或路径减半版 带权并查集公式背错 结果错不崩 写出「压缩后已知关系仍成立」的等式，解出来 稀疏表用来求和 重叠部分被加两次 求和不可重叠合并 → 换前缀和 / BIT 树状数组求区间 max 逻辑上不成立 max 无逆元 → 换线段树或稀疏表 聚合值溢出 大数据下结果错 一律 long long 三条「前提不满足却硬用」（稀疏表求和、BIT 求 max、压缩后想撤销）的共同特点： 能跑、结果安静地错 。靠 review 很难发现，最有效的防线是动手前问「这个运算可逆吗 / 可重叠吗」。其余多是实现细节（0-based BIT、数组开 2n、query 漏 push），用 暴力对拍 能全部抓到——小数据下把树的结果和 O(n) 循环比对，跑几千组随机。区间结构的题一定要对拍。"},{"a":"s9","h":"面试题集（12 道）","x":"09 面试题集（12 道） 按难度分三段，先自测再对照"},{"a":"s9-1","h":"9.1 基础（必答）","x":"9.1 基础（必答） 点击每道题展开参考答案（先自测，再对照）。 基础 写一个并查集，说明两个优化各自解决什么问题。 struct DSU { std::vector< int > p, sz; DSU ( int n) : p(n), sz(n, 1 ) { for ( int i = 0 ; i < n; ++i) p[i] = i; } int find ( int x) { // 路径减半（一趟，无递归） while (p[x] != x) { p[x] = p[p[x]]; x = p[x]; } return x; } bool unite ( int a, int b) { // 按大小合并 a = find (a); b = find (b); if (a == b) return false ; if (sz[a] < sz[b]) std:: swap (a, b); p[b] = a; sz[a] += sz[b]; return true ; // 「本次是否真的合并了」 } }; 朴素版的问题 unite(1,2), unite(2,3), unite(3,4)… 会把树变成一条链 → find 退化到 O(n)。 路径压缩 find 时顺便把路径上的节点 直接挂到根下 。它的妙处是 查询本身在优化数据结构 ——下次查同一条路径只需 1 步。单独用可得摊销 O(log n)。 按秩 / 按大小合并 总是把 小的挂到大的下面 。一个节点的深度只在它所在的树被挂到别人下面时才 +1，而按大小合并保证「树大小至少翻倍才会让深度 +1」⟹ 深度 ≤ log₂n。单独用可得最坏 O(log n)。 两者同时用 → α(n) α 是 反 Ackermann 函数 ，对所有现实的 n（哪怕 2^2^2^2^16）都 ≤ 4，工程上直接当常数。 证明用 势能法 （Tarjan 1975），而且 这个界是紧的 ——Fredman 和 Saks 证明了任何基于指针的实现都不可能做到 O(1) 摊销。 所以 α(n) 不是「还没优化到常数」，而是真实下界。 实践建议 只用路径压缩、不用按秩合并，绝大多数情况完全够用 （摊销 O(log n)），而且代码少一半。但这时 find 要用迭代版或路径减半版 ——递归版在还没压缩过的深路径上有爆栈风险。 基础 树状数组的 lowbit 是什么？为什么两个操作都是 O(log n)？ lowbit(i) = i & (−i) 取出 i 的二进制 最低位的 1 6 = 0110 → lowbit = 0010 = 2 8 = 1000 → lowbit = 1000 = 8 结构 tree[i] 管辖 a[i − lowbit(i) + 1 .. i]，共 lowbit(i) 个元素 void add ( int i, long long v) { for (; i <= n; i += i & -i) t[i] += v; } long long prefix ( int i) { long long s= 0 ; for (; i> 0 ; i -= i & -i) s += t[i]; return s; } 为什么是 O(log n) 查询 ： i -= lowbit(i) 每次消掉 i 的 一个二进制 1 ，而 i 最多有 log n 个 1 → 最多 log n 步（7→6→4→0）。 更新 ： i += lowbit(i) 每次让最低位的 1 向高位进位 ，最多 log n 步就超出 n（3→4→8→16）。 必须 1-based lowbit(0) = 0 ⟹ 用 0-based 会死循环。外部传 0-based 下标时记得 +1。 这是最高频的实现 bug。 依赖运算可逆 range(l,r) = prefix(r) − prefix(l−1) 要求运算 有逆元 。加法、异或可以； max/min 不行 （虽有「BIT 求前缀 max」的变体，但它只支持前缀查询且更新只能让值增大）。要区间最值就换线段树或稀疏表。 建树也能 O(n) 不必调 n 次 add （那是 O(n log n)）。先直接 t[i] = a[i] ，再 for i: if (i + lowbit(i) <= n) t[i + lowbit(i)] += t[i] 。 基础 线段树和树状数组该怎么选？ 两个问题就能定： ① 修改和查询是否交错？ 不交错 （先全改再全查，或根本不改）→ 用 前缀和 / 差分 / 稀疏表 ，O(1)，代码 3 行。 这一步经常被跳过，直接上线段树是过度设计。 交错 → 树状数组或线段树，O(log n)。 ② 运算有什么性质？ 可逆 （加法、异或）→ 树状数组 ：代码 10 行、常数快 2–3 倍。 只可结合 （max / min / gcd / 区间赋值 / 区间乘）→ 线段树 。 「能用 BIT 就用 BIT」 配上差分技巧，树状数组能覆盖八成常见需求： · 单点改 + 区间和 → 1 个 BIT · 区间改 + 单点查 → 1 个 BIT 维护差分数组 · 区间改 + 区间查 → 2 个 BIT（一个存 d[j] ，一个存 j·d[j] ） 而代码量只有线段树的四分之一。 线段树独有的能力 ① max/min/gcd 等不可逆运算； ② 复杂懒标记（区间赋值 + 区间加共存）； ③ 线段树上二分 ——O(log n) 找「第一个满足条件的位置」，而不是 O(log²n)； ④ 动态开点、可持久化、线段树合并等高级变体。 线段树的两个必踩坑 数组要开 4n （不是 2n，n 非 2 的幂时会越界）； query 里也要 push_down （凡是往下递归就要先结清懒标记）。 基础 为什么区间 max 不能用前缀和，但可以用稀疏表？ 因为两个结构要求的性质 不同，而且恰好互补 。 前缀和要求「可逆」 它靠 S[r] − S[l−1] 得到区间值，也就是 「减掉不要的部分」 。max 没有逆元——知道 max(a[0..r]) 和 max(a[0..l−1]) ，推不出 max(a[l..r]) 。 稀疏表要求「可重叠合并」 max(max(a,b), max(b,c)) = max(a,b,c) —— 重复计算 b 不影响结果 。 所以查询时可以偷懒：用 两个长度都是 2^k 的区间去「盖住」 [l,r] ，允许中间重叠，只需两次查表 → O(1) 。 int k = lg[r - l + 1 ]; return std:: max (st[k][l], st[k][r - ( 1 << k) + 1 ]); 反过来：稀疏表不能求和 重叠部分会被 加两次 。异或也不行（重叠会抵消）。 三者构成一个漂亮的对称 要求 能力 前缀和 / BIT 可逆 加、异或、乘 稀疏表 可重叠合并 max、min、gcd、与、或 线段树 仅需可结合 以上全部 要求依次 放宽 ，能力依次 增强 ，代价（代码量、常数）也依次增大。 前缀和能算的稀疏表算不了，稀疏表能算的前缀和算不了——两者恰好互补，而线段树覆盖两者。 但稀疏表不支持修改 O(n log n) 预处理、O(1) 查询，但 任何修改都要整体重建 。要修改就只能线段树。"},{"a":"s9-2","h":"9.2 中级（区分熟练度）","x":"9.2 中级（区分熟练度） 中级 什么是懒标记？它有哪些必须注意的地方？ 区间修改如果真去改每个叶子就是 O(n)。 懒标记 的思路是：改到「完全被查询区间覆盖」的节点就 停下 ，在那里挂一张便条，等真有人要往下走再传下去。 核心不变式 节点自己的聚合值立刻正确 （因为查询可能直接命中它）， 孩子的值「欠着」 ——等被访问时才结清。 void apply ( int p, int l, int r, long long v) { t[p] += v * (r - l + 1 ); // 区间和要 乘长度 ；最值则不乘 lazy[p] += v; } void push_down ( int p, int l, int r) { if (!lazy[p]) return ; int m = l + (r-l)/ 2 ; apply ( 2 *p, l, m, lazy[p]); apply ( 2 *p+ 1 , m+ 1 , r, lazy[p]); lazy[p] = 0 ; } 四个高频坑： ① query 里也要 push_down 查询要往下递归时同样必须先结清欠账，否则读到过期值。 只在 update 里 push_down 是最常见的错。 ② apply 忘乘区间长度 区间加 v 对「区间和」的影响是 v × 长度 ；对「区间最值」是 +v （不乘）。改聚合类型时容易忘改这里。 ③ 多种标记共存时顺序错 同时有「区间赋值」和「区间加」时， 赋值会覆盖之前的加 。 apply 里必须先处理赋值再处理加，且 打赋值标记时要清掉已有的加标记 。这是线段树最容易写错的地方。 ④ 标记必须「可合并」 懒标记要求「两次修改能合成一个标记」。区间加可以（ v1+v2 ）； 「区间开平方」不行 ——这类操作要用 势能线段树 ：暴力递归到叶子，靠「每个元素最多被开 log log 次就变成 1」来保证总复杂度。 中级 怎么用树状数组求逆序对？和归并排序的做法比有什么区别？ 把 BIT 的下标从「位置」换成「 值 」，它就能回答「已插入的元素中有多少个 ≤ x」。 // ① 离散化（必须！BIT 大小 = 值域） std::vector< int > s = a; std:: sort (s. begin (), s. end ()); s. erase (std:: unique (s. begin (), s. end ()), s. end ()); for ( int & x : a) x = lower_bound (s. begin (), s. end (), x) - s. begin () + 1 ; // ② 从右往左扫，查「右边有多少个严格小于当前值」 BIT bit (s. size ()); long long ans = 0 ; for ( int i = n - 1 ; i >= 0 ; --i) { ans += bit. prefix (a[i] - 1 ); bit. add (a[i], 1 ); } 离散化是必备前置 BIT 的大小等于值域。值域 10⁹ 时不离散化直接 MLE。 忘记离散化是这类题的第一大坑。 归并排序版 BIT 版 复杂度 O(n log n) O(n log n) 需要离散化 否 是 常数 更小 稍大 支持在线查询 否 是 扩展性 三元逆序对较难 易扩展 （二维数点、动态第 k 小） 为什么 BIT 版更好扩展 它把问题变成了「 维护一个可查询的值域计数结构 」，而这个抽象很容易叠加维度： · 二维数点 ：按一维排序（扫描线），另一维用 BIT · 动态第 k 小 ：在权值 BIT 上做「BIT 上二分」（从高位往低位试 lowbit） · 区间第 k 小 ：换成可持久化线段树（主席树） 归并版是「一次性算完」的，加不了这些。 中级 并查集能删边吗？如果题目要求「依次删除边，每次输出连通块数」怎么做？ 不能。 路径压缩把路径上所有节点直接挂到根， 原来的父子关系永久丢失 ——没法分裂。而且即使不压缩，「从一个集合里拆出一部分」本身就不是并查集能表达的操作。 标准绕法：离线 + 逆向处理 把时间倒过来——删除就变成了添加。 ① 先读完所有删除操作（离线），得到 最终 状态的图； ② 从最终状态开始， 逆序把删掉的边加回去 ，每次用并查集合并并记录答案； ③ 把答案数组 反转 输出。 类比 像 倒放拆迁录像 ：拆楼是分裂（难算），倒着放就变成盖楼（合并，并查集擅长）。算完所有帧再把答案序列反转回来。 唯一限制是必须能先看完整段录像（离线）。 典型题 「打砖块」（Bricks Falling When Hit）、「移除最多的同行同列石头」、各种离线动态连通性。 必须在线怎么办 ① 可撤销并查集 + 线段树分治 ：把「边在时间段 [l,r] 内存在」挂到一棵时间线段树上，DFS 时加边、回溯时撤销。 注意可撤销版必须放弃路径压缩 （只按秩合并，O(log n)），因为压缩不可逆。 ② LCT（Link-Cut Tree） ：真正支持在线加删边的动态树，O(log n) 摊销。但代码 200+ 行且极易写错—— 面试里能说出它的名字和适用范围就够了 ，不必现场写。 ③ 如果只需判断连通性而非计数 ，有基于欧拉回路树（ET-tree）的方案。 中级 什么是扩展域并查集？用它判二分图和 DFS 染色各有什么优劣？ 并查集只能表达「 同类 」关系。要表达「 敌对 」（a 和 b 一定 不 在同一组），做法是 把每个元素拆成两个域 。 扩展域（开两倍点） 为元素 x 建两个节点： x （「x 在集合 A」）和 x+n （「x 在集合 B」）。 · 「a 与 b 同组 」→ unite(a, b) 且 unite(a+n, b+n) · 「a 与 b 敌对 」→ unite(a, b+n) 且 unite(a+n, b) · 矛盾检测 ：若 find(x) == find(x+n) ，说明推出「x 既在 A 又在 B」→ 矛盾 典型题 ：判二分图、关押罪犯、食物链（三个域）、等式方程的可满足性。 扩展域并查集 DFS / BFS 染色 能否给出染色方案 否 （只答有无矛盾） 能 能否输出一个奇环 否 能 在线处理 支持 （边一条条来，随时可判） 需要先建好整图 代码量 更短 中等 「在线 vs 离线」是这类问题的第一个分岔口 题目说「依次处理 m 个操作，每次都要输出答案」→ 必须并查集。 题目说「给定一张图，判断/输出方案」→ DFS 染色更合适。 读题时先找这个信号，比纠结「哪个算法更好」有用。 相关变形：带权并查集 「敌对」是二元关系，如果关系是「a 比 b 大 3」这类 数值关系 ，就用 带权并查集 ：额外存「到父节点的权值」，路径压缩时沿路径累加。 权值更新公式必须自己推 （写出「压缩后已知关系仍成立」的等式解出来），不能背——写错不会崩，只会给错答案。"},{"a":"s9-3","h":"9.3 高级（体现深度）","x":"9.3 高级（体现深度） 高级 什么是「线段树上二分」？为什么它比「二分 + 区间查询」快一个 log？ 需求：找「第一个使某条件成立的位置」，比如「第一个前缀和 ≥ target 的下标」。 朴素做法：O(log²n) 在外面二分位置，每次调一次 O(log n) 的 query 验证 → log n × log n。 线段树上二分：O(log n) 把二分的决策直接放进树的下降过程里 ： int find_first ( int p, int l, int r, long long target) { if (t[p] < target) return - 1 ; // 整个子树都不够 if (l == r) return l; push_down (p, l, r); int m = l + (r-l)/ 2 ; // 用左子树的聚合值 一次比较 就决定往哪边走 if (t[ 2 *p] >= target) return find_first ( 2 *p, l, m, target); return find_first ( 2 *p+ 1 , m+ 1 , r, target - t[ 2 *p]); } 全程只走一条根到叶的路径 ——每层只做一次比较、只递归一个孩子。 这是个通用技巧 「 把外层的二分融进结构自身的下降过程 」，同样适用于： · 树状数组上二分 ：从最高位的 lowbit 往低位试，O(log n) 求「第 k 小」 · 可持久化线段树 ：求「区间第 k 小」（比较两个版本的左子树计数差） · 平衡树 ：维护子树大小后求排名 / 第 k 大 · Trie ：求「与 x 异或最大的数」（每层贪心选相反位） 共同特征是： 结构的每个节点都存着足够的聚合信息，让你一次比较就能排除一半 。识别出这个模式，很多 O(log²n) 的解法都能降一个 log。 前提 要求「聚合值关于位置单调」或至少「能从左子树的聚合值判断答案在哪边」。前缀和要求元素 非负 ；求第 k 小要求权值线段树的计数天然非负。 前提不满足时这个技巧不适用。 高级 为什么可撤销并查集必须放弃路径压缩？有什么替代方案？ 路径压缩和撤销在本质上不兼容 ：压缩把路径上 所有 节点直接挂到根，原来的父子关系永久丢失。要撤销就得记下「改了哪些节点的父指针以及原值」——而一次 find 可能改 O(log n) 甚至更多个节点，且这些修改和后续的 unite 交织在一起，无法干净地按操作回滚。 可撤销版的做法：只按秩合并 放弃压缩 ⟹ 最坏 O(log n)（而非 α(n)）。用一个栈记录每次 unite 改了什么： bool unite ( int a, int b) { a = find (a); b = find (b); // find 里 不 压缩 if (a == b) { history. push_back ({- 1 ,- 1 }); return false ; } if (sz[a] < sz[b]) std:: swap (a, b); history. push_back ({b, sz[a]}); // 记下要恢复什么 p[b] = a; sz[a] += sz[b]; return true ; } void rollback () { auto [b, old] = history. back (); history. pop_back (); if (b == - 1 ) return ; sz[p[b]] = old; p[b] = b; } 注意即使 unite 什么都没做（已在同一集合）也要压一条空记录 ，否则 rollback 次数会对不上。 主要用途：线段树分治 处理「边只在时间段 [l,r] 内存在」的问题：把每条边挂到时间线段树对应的 O(log T) 个节点上，然后 DFS 整棵时间树—— 进入节点时加边，回溯时撤销 。每条边最多被加/撤 O(log T) 次，总复杂度 O(m log T log n)。 这解决了一大类「动态图连通性」的离线问题。 其他方案 LCT （Link-Cut Tree）：真正在线支持加删边，O(log n) 摊销。但代码 200+ 行、极易写错。 离线逆向处理 ：如果操作只有删除（没有交错的加删），直接倒过来处理，删除变添加，可以用完整的压缩 + 按秩合并（α(n)）。 这是最优先考虑的方案 ——先看能不能离线。 一般化的教训 「优化」和「可回滚」常常冲突 ，因为优化往往通过 丢弃冗余信息 换性能，而回滚需要那些信息。 同类现象：数据库的 就地更新 vs MVCC （保留旧版本才能回滚/快照）、 压缩存储 vs 随机访问 、 路径压缩 vs 可撤销 。设计时如果知道将来要回滚，就得在一开始留出信息，而不是事后补。 高级 一个「实时统计任意时间区间内的指标聚合」的服务，用什么结构？ 先问三个问题，它们决定全部方案： 指标可加吗？修改和查询是否交错？区间端点任意还是只查最近 N 分钟？ ① 历史数据不变 → 预聚合 + 前缀和 按分钟预聚合，再对它建 累计和 。任意区间查询变成两行相减， O(1) 。数据量从「每请求一行」降到「每分钟一行」，降几个数量级。 这一步收益最大 ，而且是时序数据库（Prometheus recording rules、VictoriaMetrics）的标准做法—— 不要自己造 。 ② 边写边查任意区间 → 树状数组 前缀和不支持高效修改（要 O(n) 重建）。修改与查询交错时用 BIT ：单点改 + 区间和查，O(log n)，代码 10 行。 如果还要「区间批量修改」（如补数据、修正历史），用 两个 BIT （差分套 BIT）。 ③ 但先确认指标可加 sum / count / min / max 可加 → 能预聚合、能用 BIT/线段树。 avg 要拆 → 分别聚合 sum 和 count，查询时再除。 distinct count 和分位数不可加 → 必须换成 可合并的近似结构 ： HyperLogLog （去重计数，~1.5% 相对误差）、t-digest / DDSketch（分位数）。 「这个指标可加吗」决定整个方案的形态 ，应该在第一步就问。 ④ 别忽略维度基数 要按维度（接口、状态码、地域）分别统计时，预聚合的行数是 各维度基数的乘积 。混进 user_id / trace_id 这类高基数字段，预聚合表会比原始数据还大。 预聚合的前提是维度组合可控 ——这是实践中最常翻车的地方。 ⑤ 分层聚合是工程上的标准答案 秒级保留 1 小时、分钟级保留 7 天、小时级保留 1 年；查询时按区间长度 自动选层 。 这比「用一个精巧的数据结构」更重要—— 存储分层解决的是数据量问题，数据结构解决的是单次查询效率问题 ，前者的收益通常大一个数量级。 面试时先说分层再说 BIT，顺序反了会显得只看到局部。 高级 你要实现一个区间结构，怎么保证它是对的？ 区间结构（尤其是带懒标记的线段树）是 典型的「能跑但安静地错」的代码 ——一半的 bug 不会崩，只会给出错误结果。所以正确性保障必须是主动的。 ① 暴力对拍（最有效，不是可选项） 写一个 O(n) 的暴力实现（直接循环改/查数组），然后： for ( int test = 0 ; test < 10000 ; ++test) { int n = rand () % 20 + 1 ; // 小数据 才能暴力 // 随机 100 次操作，每次都比对两边结果 } 小 n 是关键 ——边界 bug（4n 越界、push_down 漏掉、区间端点）在 n=1,2,3 时最容易暴露，而大数据反而掩盖问题。 ② 写下前提并断言 本篇有三类「前提不满足却硬用」的错误，代码能跑但结果错：稀疏表求和、BIT 求 max、压缩后想撤销。 动手前明确写出：这个运算可逆吗？可重叠合并吗？修改与查询交错吗？ 把答案写成注释放在结构定义旁边。 ③ 不变式检查函数（debug 构建） 写一个 verify() ：递归检查每个节点的聚合值是否等于两个孩子的合并、懒标记是否与实际值一致。在每次操作后调用（只在 #ifndef NDEBUG 下）。 这能把「哪一步开始出错」定位到具体操作，而对拍只能告诉你「结果不对」。 ④ 覆盖这些具体边界 n=1；查询单点（l==r）；查询整个区间；查询空区间；区间左端=右端；操作序列里连续多次修改同一区间； 先改后查同一区间 （专门测 push_down）；懒标记叠加多次后再查。 ⑤ 工程上还要考虑 溢出 ：聚合值一律 long long ，并用 -fsanitize=undefined 抓有符号溢出。 越界 ： -fsanitize=address 能直接抓到「线段树只开 2n」这类问题。 把对拍挂进 CI ：区间结构的正确性依赖大量边界组合，一次性测过不代表以后改动还对。 见 Sanitizers 实战 。 ⬅️ 树与平衡树 ➡️ 图遍历与拓扑排序 📊 单调栈与前缀和 🛣️ 最短路与最小生成树 📚 目录 🏠 主页 并查集与区间结构 — 路径压缩与 α(n) · 扩展域与带权 · 树状数组 lowbit · 线段树懒标记 · 稀疏表可重叠合并 — Kavin Tech Notes · 2026"}]}];
