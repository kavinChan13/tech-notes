/* AUTO-GENERATED by scripts/build_search_index.py — do not edit by hand. */
(window.TN_SEARCH_SHARD=window.TN_SEARCH_SHARD||{})["stl"]=[{"u":"stl/concurrency_patterns_guide.html","t":"C++ 多线程编程模式完全指南","c":"现代 C++ / STL","s":[{"a":"","h":"","x":"C++ 多线程编程模式完全指南 — future/promise · 线程池 · Actor · 协程 Kavin Tech Notes ← 主页 目录 25 min ◐ 并发编程模式 · Roadmap 引言 · 为什么必学 并发原语全景 + 选型决策图 ⭐ future / promise / async shared_mutex 读写锁 call_once / latch / barrier / semaphore 线程池完整实现 V2 submit（返回值 + 异常） 为什么这里 forward、那里 move Producer-Consumer Reader-Writer 模式 Pipeline 流水线 Active Object / Actor Work-Stealing LMAX Disruptor ⭐ Sender/Receiver (C++26) ⭐ C++20 协程异步模式 跨线程异常传递 死锁防御与检测 真实工程实例 易错点速查 面试题集 速查总表 下一步 C++ STL · Concurrency · 2026 C++ 多线程编程模式 完全指南 原语总览在 并发深入 。本篇只讲 怎么组合 ：线程池 submit、有界队列、流水线、Actor、work-stealing、Disruptor、Sender/Receiver。死锁走 死锁调试 §2 ，false sharing 走 内存模型 ，协程机制走 协程深入 。 核心难度 ★★★★★ 面试出现率 高 阅读时长 ~25 min 前置 std::thread · 内存模型 future / promise / async 线程池 · 读写锁 Producer-Consumer · Actor 协程 · 跨线程异常"},{"a":"s1","h":"引言 — 多线程编程模式为何是高级岗硬骨头","x":"01 引言 — 多线程编程模式为何是高级岗硬骨头 本章目标 · 本章与已有资料的关系 mutex / cv 只是砖。高级岗考的是 模式 ：任务怎么投递、背压怎么做、状态怎么隔离、停服怎么干净退出。本页不重讲 mutex API。"},{"a":"s1-1","h":"本章目标","x":"本章目标 讲清 future 四件套的 角色分工 （实现细节见 并发深入 §5 ） 能写线程池 submit ： packaged_task 不可拷 → shared_ptr 投递，异常进 future 按场景选型：有界队列 / 流水线 / Actor / work-stealing / Disruptor 协程只讲「resume 在哪条线程」；机制见 协程 §12.1"},{"a":"s1-2","h":"本章与已有资料的关系","x":"本章与已有资料的关系 并发深入 ：原语（mutex / cv / jthread / 读写锁） 内存模型 ：atomic / 序 / false sharing 死锁调试 §2 ：定位与防御，本页不重写 HierarchicalMutex 本篇 ：把原语 组合 成可维护的并发架构"},{"a":"s2","h":"C++ 并发原语全景速览","x":"02 C++ 并发原语全景速览 2.1 全图 · 2.2 选型速查"},{"a":"s2-1","h":"2.1 四条路，先选路再选 API","x":"2.1 四条路，先选路再选 API 原语清单见 并发深入 。本页只记 模式层 的四条路：互斥、读写、无锁、消息。业务代码优先走右两列；库边界才下探 mutex / atomic。 先问「共享怎么碰」，再选叶子 互斥 读 ≈ 写 · 临界区可大 mutex + lock_guard 多锁 → scoped_lock 默认正确；争用时排队 读写 读 ≫ 写 · 临界区微秒+ shared_mutex COW / RCU-lite 纳秒级临界区反而更慢 无锁 可原子化 · 先 profile atomic / SPSC ring ABA → HP / EBR CAS 重试可能比锁慢 消息 状态不共享，只投递 队列 / Actor / 池 Pipeline / Disruptor 业务层首选"},{"a":"s2-2","h":"2.2 选型速查","x":"2.2 选型速查 需求 首选 别用错的时候 保护一块共享数据 mutex + lock_guard 读极多再考虑 shared_mutex / COW 一次性结果 + 异常 future / promise 流式数据用有界队列 大量短任务 线程池 + 有界队列 负载不均再上 work-stealing 有状态对象并发 Active Object / Actor 工作分发不该用 Disruptor 极低延迟广播 Disruptor / SPSC 先确认是广播不是抢任务 异步组合 + 取消 Sender/Receiver · 协程 线性流程协程就够"},{"a":"s2-3","h":"2.3 跨篇导航","x":"2.3 跨篇导航 主题 去哪篇 mutex / cv / jthread / 读写锁 API 并发深入 memory_order / ABA / 无锁结构 内存模型 §6 · §14 false sharing 内存模型 false sharing 协程调度 / 谁来 resume 协程 §12.1 死锁防御与检测 死锁调试 §2 TSan Sanitizers"},{"a":"s3","h":"future / promise / async / packaged_task — 异步四件套","x":"03 future / promise / async / packaged_task — 异步四件套 各自定位 · 关系图 · promise + future 经典模式 · std::async — 一行启动异步 · packaged_task — 让任意函数变成 future-able 的任务 · …"},{"a":"s3-1","h":"3.1 各自定位","x":"3.1 各自定位 组件 角色 典型用法 promise<T> 生产端 set_value / set_exception future<T> 消费端 get() 阻塞或重抛 packaged_task 可投递的 callable 线程池 submit（§6 的课） async 便捷启动 必须显式 launch::async shared_future 多次 get 多个等待者"},{"a":"s3-2","h":"3.2 关系图","x":"3.2 关系图"},{"a":"s3-3","h":"3.3 共享状态","x":"3.3 共享状态 共享状态：一边 set，一边 get promise<T> set_value / set_exception 写 shared state（堆） value 或 exception ready + mutex + cv refcount 管寿命 读 / 阻塞 future<T> get() / wait() promise.get_future() 拿到对应 future；worker 里 set，提交方 get 面试答「future 怎么实现」：堆上共享状态 + 引用计数。 set_value 加锁、写槽、置 ready、 notify ； get 等 ready 后返回或重抛。API 细节见 并发深入 §5 。"},{"a":"s3-4","h":"3.4 std::async — 临时对象析构 = 同步等待","x":"3.4 std::async — 临时对象析构 = 同步等待 不写策略时实现可选择 deferred ，根本不并发。更大的坑：返回的 future 析构会等任务结束 。两个临时对象 = 两次隐式 join = 串行。 临时 future 析构 = 隐式 wait ❌ async(work1); async(work2); 语句 1 结束 → 析构等 work1 再启动 work2 → 再析构等待 墙上时间 = T1 + T2 ✅ auto f1=…; auto f2=…; get 两个 future 都活着 两个任务重叠执行 墙上时间 ≈ max(T1, T2) // ❌ 临时 future 析构 = 同步等待 → 串行 std:: async (std::launch::async, work1); std:: async (std::launch::async, work2); // ✅ auto f1 = async(...); auto f2 = async(...); f1.get(); f2.get();"},{"a":"s3-5","h":"3.5 packaged_task","x":"3.5 packaged_task 把任意 callable 变成「可投递 + 自带 future」。不可拷贝，所以线程池用 shared_ptr<packaged_task> 包一层——这是 §6.2 的课，这里不重复实现。"},{"a":"s3-6","h":"3.6 shared_future","x":"3.6 shared_future future 只能 get 一次。多个等待者先 .share() 。典型：配置加载完成，N 个 worker 同时 get 同一份。"},{"a":"s4","h":"std::shared_mutex 读写锁 (C++17)","x":"04 std::shared_mutex 读写锁 (C++17) 用法 · 性能 — 反直觉 · 锁升级 / 降级 — 标准不支持 · 何时不要用 shared_mutex"},{"a":"s4-1","h":"4.1 读并发、写独占","x":"4.1 读并发、写独占 API 见 并发深入 §7 。这里只记模式： shared_lock 多读者并行， unique_lock 写独占。标准 不提供 读→写升级。 读写锁：并发读 · 独占写 · 禁止升级 读并发 R1 + R2 + R3 同时进 shared_lock 临界区要够长才划算 锁本身 ~5× mutex 写独占 W 进 → 读者全退 unique_lock 与任意读/写互斥 读写各半时不如 mutex 升级 = 死锁 两读者同时想升级 各持读锁等对方放 标准故意不提供 要改就一开始拿写锁"},{"a":"s4-2","h":"4.2 性能 — 反直觉","x":"4.2 性能 — 反直觉 临界区极短时，shared_mutex 的 lock/unlock 开销会吃掉「多读并行」的收益。读一个 atomic 变量别上读写锁。 原因是 共享锁本身要写一个共享计数器 。N 个「只读」线程各自 lock_shared() ，都在对同一条 cache line 做原子加减——从内存子系统看，这是 N 个 写者 在抢同一行，缓存行在各核之间来回弹跳。读得越并行，弹得越凶。 临界区长度 谁更快 为什么 读一个 int （几纳秒） atomic 完胜 锁的开销是临界区的几十倍 几十纳秒 mutex 通常更快 shared_mutex 的记账比 mutex 重，而并行度还来不及兑现 微秒级（查一棵树、算一段） shared_mutex 开始赢 并行读的收益终于盖过计数器争用 读极多写极少，且数据可整体替换 COW / RCU 更好 读者 一个原子指令都不用写 ，见 §8 与 安全回收 所以「读多写少就上读写锁」是个过度简化的口诀。真正的判据是 临界区够不够长 ：短临界区下读写锁的记账成本收不回来，长临界区下才谈得上并行收益。拿不准就两种都测——这类差异在 perf c2c 里会直接显示为那条计数器 cache line 的高频 HITM。"},{"a":"s4-3","h":"4.3 锁升级 / 降级 — 标准不支持","x":"4.3 锁升级 / 降级 — 标准不支持 先放读锁再拿写锁，中间有窗口，必须重新检查条件（类似 DCLP）。两个线程同时升级会死锁。需要「先看再改」→ 一开始就拿写锁，或改成单 writer 队列。"},{"a":"s4-4","h":"4.4 何时不要用","x":"4.4 何时不要用 单变量 / 纳秒级临界区 → atomic 读写接近 1:1 → mutex 读极多且数据可整体替换 → §8 的 COW 需要升级 → 重新设计，不要自己搓升级"},{"a":"s5","h":"call_once / latch / barrier / semaphore","x":"05 call_once / latch / barrier / semaphore 一次性初始化与 C++20 的四件同步原语 · 以及它们底下的 atomic::wait"},{"a":"s5-1","h":"5.1 std::call_once + once_flag","x":"5.1 std::call_once + once_flag 一次性初始化： call_once 能用，但 C++11 起 函数内 static （Magic Static）更短，标准保证线程安全。异常时 static 会重试， call_once 失败则 flag 仍未置位、下次再调。 原语 语义 典型场景 latch 倒数一次，不能重置 等 N 个 worker 就绪再开下一阶段 barrier 可重复的集合点，可带完成回调 多阶段迭代，每轮齐步 counting_semaphore 有上限的许可证 限并发（连接数、飞行中 RPC） binary_semaphore 0/1 信号 简单通知；复杂谓词仍用 cv 这四件的共同价值是 把「等待」这件事从 mutex + condition_variable + 谓词 的三件套里解放出来 。用 cv 表达「等 8 个线程都到齐」要自己维护计数器、自己写 while (cnt < 8) cv.wait(lk) 、还要记得 notify_all ；用 latch 就是两行，而且不可能写错谓词。"},{"a":"s5-2","h":"5.2 std::latch (C++20)","x":"5.2 std::latch (C++20) 一次性倒数门闩： count_down() 减一， wait() 阻塞到归零， arrive_and_wait() 是两者合一。 没有 mutex、没有谓词、不会虚假唤醒 ，因为「计数归零」本身就是完整的条件。 两种用法方向相反，都很常见： 收集 （主线程 latch done{N} ，每个 worker 完成时 count_down() ，主线程 wait() ）和 起跑枪 （ latch start{1} ，所有 worker 先 wait() ，主线程准备好后 count_down() 一次放行——压测里要让 N 个线程尽量同时开始，这是标准做法）。 两个硬约束： 不能重置 ，用完就废； count_down() 的总次数超过初始值是 UB ，不是「减到负数」也不是「忽略」。循环里每轮都要同步的场景，要的是下面的 barrier 而不是每轮 new 一个 latch。"},{"a":"s5-3","h":"5.3 std::barrier (C++20)","x":"5.3 std::barrier (C++20) barrier 是可重复使用的集合点：所有参与者 arrive_and_wait() ，最后一个到达时本 阶段（phase） 结束，计数自动重置、所有线程一起放行进入下一阶段。 它相对 latch 真正的增量是 完成函数（completion function） ：构造时传入的那个可调用体，会在 最后一个线程到达之后、任何线程被放行之前 执行，而且 只执行一次、在单线程上下文里 。这给了你一个天然安全的「阶段之间」窗口。 barrier 的一个 phase：等齐 → 完成函数 → 一起放行 T1 T2 T3 T4 计算 计算（最慢的那个） 计算 计算 先到的在等 集合点 完成函数（单线程） 交换缓冲 / 归约 / 判收敛 此刻 无人在跑用户代码 ，改共享状态是安全的 下一 phase 一起开始 代价：整组的节奏被 最慢的那个线程 决定——负载不均时 barrier 会把不均匀放大成串行 线程要提前退出，用 arrive_and_drop() 把自己从后续 phase 的参与者里摘掉，否则剩下的人永远等不齐"},{"a":"s5-4","h":"5.4 counting_semaphore / binary_semaphore","x":"5.4 counting_semaphore / binary_semaphore 信号量是「许可证池」： acquire() 取一张（没有就等）， release() 还一张。 counting_semaphore<N> 的模板参数是 编译期上限 ，不是初始值——初始值在构造函数里给。 信号量和 mutex 看起来都能「保护临界区」，但它 没有所有者概念 ：A 线程 acquire 、B 线程 release 是完全合法的。这正是它能做许可证的原因，也正是 不能拿它当 mutex 用 的原因——没有所有权就没有优先级继承（实时系统里会优先级反转）、没有递归检测、也没有「谁持有这把锁」可供调试器显示。要互斥就用 mutex ，要限并发才用信号量。"},{"a":"s5-5","h":"5.5 atomic 的 wait / notify：前面几个都是拿它搭的","x":"5.5 atomic 的 wait / notify：前面几个都是拿它搭的 C++20 给 所有 std::atomic<T> 加了 wait(old) / notify_one() / notify_all() 。语义是： 如果当前值仍等于 old 就阻塞 ，直到别人改了值并 notify。 std::atomic<bool> ready{false}; // 消费者：不需要 mutex，也不需要 condition_variable while (!ready.load(std::memory_order_acquire)) ready.wait(false, std::memory_order_acquire); // 值还是 false 就睡 // 生产者 ready.store(true, std::memory_order_release); ready.notify_all(); 实现上它就是 Linux 的 futex / Windows 的 WaitOnAddress ，和 mutex 、 condition_variable 走同一扇门（见 并发深入 §10 ），通常还会先自旋若干次再进内核。 latch 、 barrier 、 semaphore 的标准库实现基本都建立在它之上。 什么时候直接用它： 条件就是「某一个原子变量变了」 的时候。这种场景用 cv 要额外背一个 mutex，而 mutex 在这里保护不了任何东西——它只是 cv 的语法要求。反过来，条件涉及多个变量或复杂谓词时，仍然老老实实用 mutex + cv ：那个 mutex 是用来让「检查谓词」和「进入等待」变成一个原子步骤的，不能省。 注意外面那层 while 不能省： wait 只保证「值变了会醒」，不保证「醒来时值是你要的」——中间可能又被改回去，也允许实现层面的伪唤醒。"},{"a":"s6","h":"线程池 — 完整实现演化 ⭐⭐⭐","x":"06 线程池 — 完整实现演化 ⭐⭐⭐ 最简线程池 · 返回值 + 异常透传 · forward vs move · 关键设计要点 · 线程池大小 本页的课不是「再写一遍 mutex + cv 循环」，而是 submit ： packaged_task 不可拷，必须 shared_ptr 才能进队列；异常自动进 future。 线程池：submit → 队列 → worker；停服用 stop_token 提交方 submit → future 任务队列 有界 · 背压 / 防 OOM W0 W1 … Wn stop_token notify_all 叫醒 cv 停服语义二选一：drain（排空队列再退）或 abort（丢剩余任务） jthread 析构 = request_stop + join，不要 detach"},{"a":"s6-1","h":"6.1 结构：队列 + worker + 停止","x":"6.1 结构：队列 + worker + 停止 V1 用 function<void()> 只能扔副作用。生产级要返回值 + 异常：下面这块把两版合成 submit + worker 取任务。构造/析构按 jthread 常规写即可，不重复贴完整 class。"},{"a":"s6-2","h":"6.2 submit：packaged_task + shared_ptr","x":"6.2 submit：packaged_task + shared_ptr // packaged_task 不可拷 → shared_ptr 投递；submit 听调用方 forward，worker 里再 move template < class F, class ... Args> auto submit (F&& f, Args&&... args) -> std::future<std::invoke_result_t<F, Args...>> { using R = std::invoke_result_t<F, Args...>; auto task = std:: make_shared <std::packaged_task<R()>>( [f = std::forward<F>(f), args = std::make_tuple(std::forward<Args>(args)...)]() mutable { return std:: apply (std::move(f), std::move(args)); }); auto fut = task-> get_future (); { std::lock_guard lk{mu_}; tasks_. emplace ([task]{ (*task)(); }); } cv_. notify_one (); return fut; } void loop (std::stop_token st) { std::stop_callback cb(st, [ this ]{ cv_. notify_all (); }); while (!st. stop_requested ()) { std::move_only_function< void ()> task; { std::unique_lock lk{mu_}; cv_. wait (lk, st, [ this ]{ return !tasks_. empty (); }); if (st. stop_requested () && tasks_. empty ()) return ; task = std::move(tasks_. front ()); tasks_. pop (); } task (); // 异常已进 future } }"},{"a":"s6-2-1","h":"6.2.1 为什么这里 forward 、那里 move","x":"6.2.1 为什么这里 forward 、那里 move 口诀： forward 听调用方；move 抢已拥有且确定用完的东西 。 上门收件看运单才搬走（ forward ）；货进了你仓库、今晚必发，就无条件装车（ move ）。写成「暂借原件人的抽屉」（ forward_as_tuple ）→ 人走抽屉空 → 异步悬挂。 ① 进闭包：转发引用必须 std::forward F&& / Args&&... 是万能引用。左值拷进 tuple，右值才移。 make_tuple(forward<Args>(args)...) 存 值 。写成 forward_as_tuple 存的是引用—— submit 返回后调用方栈已结束，worker 解引用即 UAF。 一律 std::move 会掏空调用方左值。 forward_as_tuple 在异步边界必悬空。 ② 闭包里：具名成员是 lvalue init-capture 之后 f / args 有名字 → lvalue。 apply(f, args) 接不住 unique_ptr 形参。lambda 只跑一次，所以闭包内写 apply(std::move(f), std::move(args)) 。 ③ [task] 拷 shared_ptr 是共享所有权 packaged_task 不可拷。队列 lambda 拷贝 shared_ptr ，好让 get_future() 与 worker 同时活着。不是漏了 move 。 位置 写法 为什么 进闭包 std::forward<F>(f) / make_tuple(forward<Args>(args)...) 听调用方：左值拷、右值移；存值不存引用 闭包内 apply apply(std::move(f), std::move(args)) 具名成员是 lvalue；lambda 只跑一次，必须抢走 投递队列 [task]{ (*task)(); } 拷贝 shared_ptr ，共享 packaged_task 所有权 点击每道题展开参考答案（先自测，再对照）。 中级 线程池 submit 里为何不能一律 std::move ？ F&& / Args&&... 是转发引用。一律 move 会把调用方的 左值 也掏空： pool.submit(fn, payload) 之后 payload 变成 moved-from，调用方若还要用就是逻辑 bug。 std::forward 才是「条件移动」：左值保持左值（拷进 tuple），右值才移进 tuple。闭包 里面 才该无条件 move ——那时对象已经是闭包的具名成员，且 lambda 只执行一次。 口诀 forward 听调用方；move 抢已拥有且确定用完的东西。 中级 为什么这里必须 make_tuple 而不能 forward_as_tuple ？ forward_as_tuple 存的是 引用 （ tuple<Args&&...> ）。 submit 返回后，调用方实参的寿命已经结束，worker 线程稍后 apply 解引用 → 悬挂 / UAF。 make_tuple(std::forward<Args>(args)...) 把对象 拷或移进 tuple 的值成员 ，寿命跟闭包走，这才是异步边界该做的事。"},{"a":"s6-3","h":"6.3 V3 — 关键设计要点","x":"6.3 V3 — 关键设计要点 要点 实现 原因 worker 数量 hardware_concurrency() 或配置 避免过多线程切换 队列上限 BoundedQueue + cv 阻塞 push 背压 / 防 OOM 停止信号 jthread + stop_token RAII 自动停 + join 停止行为 \"drain or abort\" 选择 看业务语义 异常透传 packaged_task → future 用户能 catch 任务优先级 priority_queue + 优先级 tag SLA 敏感场景 多队列分片 每 worker 一队列 + 工作窃取 减少队列锁争抢 线程命名 pthread_setname_np 调试可见 任务延迟统计 提交时间戳 → 执行延迟直方图 容量规划"},{"a":"s6-4","h":"6.4 线程池大小选择公式","x":"6.4 线程池大小选择公式 Goetz： N ≈ N_cpu × U × (1 + W/C) 。CPU 密集 ≈ 核数；IO 密集按等待比放大。上线后看队列长度和任务延迟，别死背公式。"},{"a":"s7","h":"Producer-Consumer 模式","x":"07 Producer-Consumer 模式 BoundedQueue（生产级实现） · 使用模式 · 多 Producer / 多 Consumer · Backpressure (背压) 设计 有界队列把生产和消费解耦，容量就是背压。无界队列 = 慢性 OOM。 有界队列：满则阻塞生产，空则阻塞消费；close 叫醒所有人 P0 P1 P2 满 → wait cap = N not_full / not_empty close → notify_all push 失败 · pop 排空后 nullopt C0 C1 空 → wait 背压策略 Block / Drop / Reject MPMC 单锁会成为瓶颈：1↔1 用 SPSC ring；再热就分片或 work-stealing"},{"a":"s7-1","h":"7.1 BoundedQueue — push / pop / 停服","x":"7.1 BoundedQueue — push / pop / 停服 template < class T> class BoundedQueue { std::queue<T> q_; std::mutex mu_; std::condition_variable not_full_, not_empty_; size_t cap_; bool closed_ = false ; public : bool push (T v) { std::unique_lock lk{mu_}; not_full_. wait (lk, [ this ]{ return closed_ || q_. size () < cap_; }); if (closed_) return false ; q_. push (std::move(v)); not_empty_. notify_one (); return true ; } std::optional<T> pop () { std::unique_lock lk{mu_}; not_empty_. wait (lk, [ this ]{ return closed_ || !q_. empty (); }); if (q_. empty ()) return std::nullopt; T v = std::move(q_. front ()); q_. pop (); not_full_. notify_one (); return v; } void close () { std::lock_guard lk{mu_}; closed_ = true ; not_full_. notify_all (); not_empty_. notify_all (); } };"},{"a":"s7-2","h":"7.2 使用模式","x":"7.2 使用模式 生产者 push 失败就停；最后一个生产者 close() 。消费者 while (auto x = q.pop()) ，拿到 nullopt 就是排空且关闭。"},{"a":"s7-3","h":"7.3 多 Producer / 多 Consumer","x":"7.3 多 Producer / 多 Consumer mutex + 两把 cv 已经支持任意 P/C。单锁热了：SPSC → 无锁 ring；MPMC → 现成库；再热 → 分片 + §11 窃取。"},{"a":"s7-4","h":"7.4 背压","x":"7.4 背压 满了默认 Block。也可 Drop / DropOldest / Reject。没有反馈，生产长期快于消费，队列会把进程撑死。"},{"a":"s8","h":"Reader-Writer 模式 — 多种实现","x":"08 Reader-Writer 模式 — 多种实现 经典 shared_mutex · Copy-on-Write — 极致读性能 · RCU (Read-Copy-Update) — 极端读优化 · 选型"},{"a":"s8-1","h":"8.1 经典 shared_mutex","x":"8.1 经典 shared_mutex 读路径 shared_lock 、写路径 unique_lock 。锁语义见 §4，这里不重复 class。"},{"a":"s8-2","h":"8.2 Copy-on-Write — 配置 / 路由热更","x":"8.2 Copy-on-Write — 配置 / 路由热更 读端只 atomic<shared_ptr<const T>>::load ，几纳秒、不阻塞写。写端复制整份、原子替换。旧快照随最后一个读者的 shared_ptr 析构回收。数据集大就别整表复制，改分片。 COW：读 load 快照，写复制后 store 新指针 读者 × N current.load() 不阻塞写者 atomic<shared_ptr> 指向不可变 Map refcount 回收旧快照 写者（互斥） copy → 改 → store 大表复制会打爆内存 同一次请求里拿一份 snapshot，中间 reload 也不撕裂字段"},{"a":"s8-3","h":"8.3 RCU","x":"8.3 RCU 读端几乎零开销，写端复制 + 延迟回收。标准库没有，folly / userspace-rcu 才有。实现细节见 内存模型 §14 。"},{"a":"s8-4","h":"8.4 选型","x":"8.4 选型 场景 方案 读多、临界区微秒级 shared_mutex 读极多、表小、可整体替换 COW + atomic shared_ptr 读极多、表大 分片锁 单变量 atomic"},{"a":"s9","h":"Pipeline 流水线模式","x":"09 Pipeline 流水线模式 实现骨架 · 性能调优要点 阶段间用有界队列连接。吞吐 = 最慢那一段 。队列堆起来的阶段就是瓶颈，给它加 worker（若可并行），别给已经空转的段加线程。 流水线：总吞吐 = min(各段)；积压队列指向瓶颈 parse ×2 队列短 q validate ×4 队列最长 = 瓶颈 transform ×2 队列短 write ×1 常不可并行 每段 pop → transform → push；上游 close 后把下游也 close，整条链排空退出 大对象用 shared_ptr 过队列；队列对象隔开 cache line，避免 false sharing 假共享：队列对象挨太近会来回踢 cache line。隔离做法见 false sharing 。"},{"a":"s9-1","h":"9.1 实现骨架","x":"9.1 实现骨架 每段：从上游 pop ，失败则 out.close() ；成功则 push(transform(item)) 。不必再贴一份完整 Stage 模板——队列课在 §7。"},{"a":"s9-2","h":"9.2 调优","x":"9.2 调优 看每段队列长度，长的加 worker 批量 pop，减少 cv 唤醒 阶段间零拷贝：传 shared_ptr ，别拷大缓冲"},{"a":"s10","h":"Active Object / Actor — 异步消息驱动对象","x":"10 Active Object / Actor — 异步消息驱动对象 Active Object 实现 · Actor 与 Active Object 区别 状态只让一条线程碰：外人只往 mailbox 投递。内部无锁，排队延迟换正确性。 Active Object：外面投递，里面单线程碰状态 调用方 A 调用方 B mailbox 方法调用 → 消息 专属 worker 串行处理 只有它改 count_ future 回传结果 Actor 变体 调度器复用线程池"},{"a":"s10-1","h":"10.1 怎么投递","x":"10.1 怎么投递 post 的骨架与线程池 submit 相同： packaged_task + shared_ptr 进队列。区别是 一个对象一条线程 ，不是共享池。对象一多，线程数爆炸 → 换成 Actor 调度器。"},{"a":"s10-2","h":"10.2 Actor 与 Active Object","x":"10.2 Actor 与 Active Object Active Object Actor 消息 方法 + future 显式 send 位置 同进程 可跨节点 调度 每对象一线程 共享池 + 调度器 规模 对象少 百万 actor / 几十线程"},{"a":"s11","h":"Work-Stealing 工作窃取","x":"11 Work-Stealing 工作窃取 动机 · 关键设计 · 工程框架"},{"a":"s11-1","h":"11.1 动机","x":"11.1 动机 全局队列的锁会先热起来。每个 worker 一把双端队列：自己从 尾 进出（栈、缓存友好）；空了才从别人的 头 偷。 自己走尾，空了偷别人的头 W0 deque head → … → tail 自己 pop_back 被偷：pop_front W1 deque 忙 · 只碰自己的尾 大多数路径无锁 W2 空闲 steal W0.head 偷是少数事件 Chase-Lev deque 别手搓，用 TBB / folly"},{"a":"s11-2","h":"11.2 关键设计","x":"11.2 关键设计 两端冲突少：owner 只碰 tail，thief 只碰 head。生产用 TBB、folly::CPUThreadPoolExecutor，或 std::execution::par 后端。"},{"a":"s11-3","h":"11.3 工程框架","x":"11.3 工程框架 自写 Chase-Lev 极易在 ABA / 内存序上翻车。先用库，profile 再谈。"},{"a":"s11b","h":"LMAX Disruptor — 极致低延时的\"机械同情\"模式 ⭐","x":"11B LMAX Disruptor — 极致低延时的\"机械同情\"模式 ⭐ 定位 · 与 MPMC 的差别 · 环 + cursor · 协议 · 误用"},{"a":"s11b-1","h":"11b.1 定位","x":"11b.1 定位 LMAX 用环形 buffer 做到百万级订单/秒。它不是「更快的抢任务队列」： 所有消费者看见所有事件 ，slot 预分配复用。工作分发请用线程池 / work-stealing。"},{"a":"s11b-2","h":"11b.2 和 MPMC 队列差在哪","x":"11b.2 和 MPMC 队列差在哪 MPMC 队列 Disruptor 所有权 pop 取走 slot 复用，零分配 消费者 一条消息一个 广播 + barrier 依赖 同步 槽上 CAS 各自 cursor，隔 cache line 等待 混编 WaitStrategy 可插拔"},{"a":"s11b-3","h":"11b.3 环 + cursor","x":"11b.3 环 + cursor Ring 2^N · cursor 各占一行 cache 0 已发布 seq 0 1 已发布 seq 1 2 已发布 published=2 3 写入中 producer=3 空 … 环回 Producer cursor 申请到 3 Published ≤2 可读 Consumer A=1 可批量拉到 published Consumer B=0 最慢者决定回收 next ≤ min(消费者) + ring_size，否则等最慢消费者 WaitStrategy：BusySpin 换纳秒唤醒；普通服务用 BlockingWait"},{"a":"s11b-4","h":"11b.4 协议，不是 80 行框架","x":"11b.4 协议，不是 80 行框架 生产者 next() 等到环上最老 slot 已无读者 → 写 slot → publish 。消费者 wait_for(seq) 一次拿到一段连续可读区间，批量处理。cursor 用 alignas(64) 隔开，假共享见 内存模型 。"},{"a":"s11b-5","h":"11b.5 快在哪","x":"11b.5 快在哪 零 malloc、生产者不和消费者抢同一个 atomic、批量摊销屏障、BusySpin 可跳过 OS 调度。面试答「省了节点分配 + 假共享 + 逐条同步」即可。"},{"a":"s11b-6","h":"11b.6 生态与误用","x":"11b.6 生态与误用 C++ 侧 Disruptor-cpp；跨进程看 Aeron。工作分发（一个任务一个 worker）用 moodycamel 或 §11，不要 Disruptor。"},{"a":"s11c","h":"Sender/Receiver — C++26 结构化并发 (P2300) ⭐","x":"11C Sender/Receiver — C++26 结构化并发 (P2300) ⭐ 要解决什么 · 三大概念 · 管道 · 与协程"},{"a":"s11c-1","h":"11c.1 它要解决什么","x":"11c.1 它要解决什么 标准 future 不能组合；各家 future 互不兼容；协程解决了可读性，调度/取消/组合仍各写各的。P2300（ std::execution ，C++26）给异步操作 + 调度器 + 结构化取消一套代数。 组合：回调地狱 vs Sender 管道 ❌ then().then().then() 取消靠手埋；失败竞态 子任务可能比父活得久 标准 future 没有 then ✅ schedule | then | when_all 懒描述，sync_wait 才跑 set_value / error / stopped 父结束 → 子收到 stopped"},{"a":"s11c-2","h":"11c.2 三大概念","x":"11c.2 三大概念 概念 职责 Scheduler 在哪跑（池 / IO / GPU） Sender 懒的「做什么」，可组合 Receiver set_value / set_error / set_stopped"},{"a":"s11c-3","h":"11c.3 第一眼","x":"11c.3 第一眼 auto work = ex:: schedule (sched) | ex:: then ([]{ return load (); }) | ex:: then ([]( auto x){ return use (x); }); auto [n] = ex:: sync_wait (std::move(work)). value ();"},{"a":"s11c-4","h":"11c.4 组合算子","x":"11c.4 组合算子 then 变换； let_value 动态下一段； when_all / when_any ； upon_error / upon_stopped ； transfer 换调度器； bulk 并行扇出。"},{"a":"s11c-4b","h":"11c.4b completion signatures：这条管道的类型契约","x":"11c.4b completion signatures：这条管道的类型契约 上面那些算子能拼起来，靠的是每个 sender 都在 类型层面 声明了「我会以哪几种方式结束」。这份声明叫 completion signatures ，是 S/R 与 future 最本质的区别——future 只有「值或异常」两种结局，而 sender 把三种通道都写进了类型： 通道 签名形态 含义 set_value set_value_t(int, std::string) 成功，可以带 任意个 值（future 只能带一个） set_error set_error_t(std::exception_ptr) 、 set_error_t(std::error_code) 失败。 错误类型是管道类型的一部分 ，可以完全不用异常 set_stopped set_stopped_t() 被取消。 它不是错误 ——这是 S/R 的一个关键设计决定 把取消单独做成第三条通道，而不是塞进 set_error ，带来两个实际好处：取消 不需要构造异常对象 ，因此在热路径上是免费的；而且 upon_error 的处理逻辑不会被取消误触发——「服务正在优雅关闭」和「读磁盘失败」不该走同一段补偿代码。 类型契约还意味着 接错了是编译错误 ：把一个可能 set_error(error_code) 的 sender 接到只会处理 exception_ptr 的消费者上，编译期就挡住了。代价是错误信息会很长——这是所有重类型设计的共同账单。"},{"a":"s11c-4c","h":"11c.4c 取消是一棵树，不是一个 flag","x":"11c.4c 取消是一棵树，不是一个 flag jthread 的 stop_token 是 一个 令牌，你得自己传给每个需要它的地方。S/R 的取消是 结构化 的：子操作的 stop token 由父操作提供，于是取消沿着组合结构自动向下传播。 这正是「结构化并发」里「结构化」三个字的含义—— 异步操作的生命周期严格嵌套在它的父操作里 ，就像局部变量的生命周期嵌套在作用域里。具体后果是： when_all 里任何一个分支失败，其余分支 自动收到 set_stopped ，而且 when_all 保证在自己完成之前所有子操作都已结束。父操作不可能先于子操作析构，那类「回调回来时对象已经没了」的经典竞态从结构上被消掉了。 代价要说清楚：取消是 协作式 的。一个不检查 stop token 的长循环、或者一次阻塞的 read() 系统调用，照样会把整棵树卡住。S/R 消除的是「取消信号传不到」这类 结构性 问题，不是「代码不响应取消」这类 实现 问题。接入阻塞 API 时仍然要自己做超时或可中断的 IO。"},{"a":"s11c-5","h":"11c.5 并行从属","x":"11c.5 并行从属 when_all(read_a, read_b) | transfer(cpu) | then(merge) 。别在这里再贴一份 then 链，协程线性写法见下一章。"},{"a":"s11c-6","h":"11c.6 与协程","x":"11c.6 与协程 不是替代：sender 可 co_await ，Task 也可当成 sender。机制见 协程深入 。"},{"a":"s11c-7","h":"11c.7 结构化并发","x":"11c.7 结构化并发 子不能比父活得久；取消沿链传播； when_all 一人失败则取消其余再汇总。消灭 detach / 悬空回调。"},{"a":"s11c-8","h":"11c.8 现在就能用","x":"11c.8 现在就能用 NVIDIA stdexec、libunifex、Asio experimental sender。线性流程用协程即可，不必上 S/R。"},{"a":"s11c-9","h":"11c.9 何时上","x":"11c.9 何时上 跨 CPU/GPU/IO 的统一异步、复杂依赖图、要标准取消 → S/R。SPSC 低延迟走 Disruptor。遗留 future 在边界适配。"},{"a":"s12","h":"C++20 协程 — 异步模式新范式","x":"12 C++20 协程 — 异步模式新范式 与线程池/future 的关系 · 标准只提供机制 — 业务自定义 Task · 简化 Task 实现骨架 · 协程的真实开销 · 应用场景"},{"a":"s12-1","h":"12.1 本页只问：resume 在哪条线程","x":"12.1 本页只问：resume 在哪条线程 机制、帧、awaiter 见 协程深入 。模式层只记一件事： co_await 回来时跑在 谁 resume 了你 的那条线程上，不一定是当初挂起的那条。 挂起在 A，resume 在谁身上就在谁身上接着跑 线程 A 跑到 co_await IO return · 帧在堆上 完成回调 epoll / io_uring 谁回调谁 resume 线程 B（池 worker） handle.resume() TLS / 锁所属都变了 要固定线程：awaiter 里 post 回原 executor。调度对接见协程 §12.1 细节： 协程 §12.1 谁来 resume 。线性异步长得像同步，但不要自己搓 Task。"},{"a":"s12-2","h":"12.2 标准只给机制","x":"12.2 标准只给机制 关键字 + promise_type / handle / awaiter，没有官方 Task。用 cppcoro / folly::coro，或 C++26 sender 当底座。"},{"a":"s12-3","h":"12.3 不要手搓 Task","x":"12.3 不要手搓 Task 骨架课在协程篇。这里只记：挂起点活着的引用必须覆盖整个 suspend，否则 UB。"},{"a":"s12-4","h":"12.4 开销","x":"12.4 开销 帧默认堆分配；resume 约几十纳秒、不进内核。CPU 密集仍走线程池。"},{"a":"s12-5","h":"12.5 适合 / 不适合","x":"12.5 适合 / 不适合 适合万级挂起 IO、链式异步、generator。不适合 CPU 密集、要清晰内核栈的排障。gdb 看到的是调度器，不是协程步骤。"},{"a":"s13","h":"跨线程异常传递","x":"13 跨线程异常传递 std::exception_ptr — 通用桥梁 · std::future — get() 自动重抛 · packaged_task — 异常自动进 future · 多 worker 异常聚合 worker 抛的异常不会自己飞回提交方。三件工具：裸 exception_ptr 、 future.get() 自动重抛、 packaged_task 内部已经存进 future（§6）。"},{"a":"s13-1","h":"13.1 exception_ptr","x":"13.1 exception_ptr std::exception_ptr ep; try { do_work (); } catch (...) { ep = std:: current_exception (); } if (ep) std:: rethrow_exception (ep); // 在提交方线程重抛"},{"a":"s13-2","h":"13.2 future.get 自动重抛","x":"13.2 future.get 自动重抛 promise / async / packaged_task 都走共享状态。提交方 get() 即可，不必自己存 ptr。"},{"a":"s13-3","h":"13.3 packaged_task","x":"13.3 packaged_task callable 里 throw 会被抓住并 set_exception 。这是线程池 submit 能透传异常的原因。"},{"a":"s13-4","h":"13.4 多 worker 聚合","x":"13.4 多 worker 聚合 逐个 get ，把 current_exception() 推进 vector；一个直接 rethrow，多个包成 AggregateError。不要第一个失败就丢其余的错误。"},{"a":"s14","h":"死锁的预防与检测","x":"14 死锁的预防与检测 6 条防御规则 · 同时锁多个 mutex 的正确写法 · 锁层次 (Lock Hierarchy) · 死锁检测工具 定位与 gdb / TSan 走 死锁调试 §2 。这里只记设计侧四条，不重写 HierarchicalMutex。"},{"a":"s14-1","h":"14.1 6 条防御","x":"14.1 6 条防御 全局锁顺序（按地址也行） 多锁一起拿： scoped_lock 锁只要数据，不锁 IO RAII 守卫，禁止裸 lock/unlock 持锁不调未知回调 / 虚函数 除 cv.wait 外，持锁不阻塞"},{"a":"s14-2","h":"14.2 多锁","x":"14.2 多锁 A 然后 B、对面 B 然后 A → 死锁。C++17 scoped_lock{a,b} 内部避免。细节和案例见死锁篇。"},{"a":"s14-3","h":"14.3 锁层次","x":"14.3 锁层次 给每把锁一个 level，线程只能从高往低拿。实现与检测工具见 §2 。"},{"a":"s14-4","h":"14.4 工具","x":"14.4 工具 TSan 抓数据竞争；已死锁用 gdb thread apply all bt 。Helgrind / lockstat 补长时间等待。"},{"a":"s15","h":"REAL真实工程","x":"15 REAL真实工程 Reactor + 池 · 异步日志 · 配置热更 · 令牌桶 · 停服"},{"a":"s15-1","h":"15.1 Reactor + 线程池","x":"15.1 Reactor + 线程池 epoll 线程只做 IO。请求 submit 进业务池，算完 post_to_io 再 send。socket 不跨线程摸。"},{"a":"s15-2","h":"15.2 异步日志","x":"15.2 异步日志 就是 §7 有界队列：热路径只 push；后台线程批量 pop + flush。队列满 = 背压，别在日志里再 malloc 出天际。"},{"a":"s15-3","h":"15.3 配置热更","x":"15.3 配置热更 课在 §8.2： atomic<shared_ptr<const Config>> 。一次请求拿一份 snapshot，中间 reload 不撕裂字段。"},{"a":"s15-4","h":"15.4 令牌桶","x":"15.4 令牌桶 令牌桶：按速率补，按容量封顶，空则拒绝 rate 滴入 tokens / sec 桶 ≤ capacity try_acquire(n) 严格版用 CAS 循环 放行 空 → 拒 上面那种 load/store 简化版有丢更新。生产用 CAS 或现成限流库。"},{"a":"s15-5","h":"15.5 优雅停服","x":"15.5 优雅停服 停服四步：停接 → 排空 → 关连接 → join 1 停 accept 信号 → atomic 2 drain 有超时，队列 close 3 关连接 不再读写 socket 4 jthread 析构 request_stop + join 队列 close 必须 notify_all，否则 worker 会在 cv.wait 上睡死"},{"a":"s16","h":"易错点速查（10 个高频陷阱）","x":"16 易错点速查（10 个高频陷阱） 坑 症状 修法 async 临时 future 看起来并行，墙上时间相加 先接到 named future 再 get（§3.4） cv.wait 不带谓词 虚假唤醒后继续跑 永远带谓词；停服用 notify_all 持锁调用户回调 回调再拿锁 → 死锁 拷数据、放锁、再调；细节见 死锁 §2 thread 析构未 join std::terminate 用 jthread lambda 引用捕获跨线程 UAF / 随机崩 值捕获，或寿命覆盖 worker future.get 两次 二次 get → terminate 多人等用 shared_future 持锁做 IO / sleep 队列堆、延迟炸 锁只要数据 纳秒临界区用 shared_mutex 比 mutex 还慢 atomic / mutex / COW（§4、§8） 线程数远超核数 切换吃掉收益 Goetz 公式当起点，看队列延迟 乱写 memory_order_relaxed 看不见对方的写入 见 内存模型"},{"a":"s17","h":"面试题集（25 道分层）","x":"17 面试题集（25 道分层） 基础 · 中级 · 高级 · 速答示范（第 8 题）"},{"a":"s17-1","h":"17.1 基础","x":"17.1 基础 std::thread 和 std::jthread 区别? 为什么推荐 jthread? future / promise / packaged_task / async 各自定位? std::async 的两个启动策略? 默认是什么? condition_variable 为什么必须配 mutex 用? 什么是虚假唤醒? 怎么防? shared_mutex 与 mutex 怎么选? 什么是 RAII 锁守卫? 列出 4 种。"},{"a":"s17-2","h":"17.2 中级","x":"17.2 中级 从零实现一个线程池 (含返回值 + 异常透传) 实现一个 BoundedQueue 满足 producer-consumer + 干净停止 std::async 的 future 析构会做什么? 会有什么坑? 线程池大小怎么定? 给一个公式。 同时获取多个 mutex 怎么避免死锁? 多线程访问 STL 容器的规则? std::call_once 与 Magic Static 区别?"},{"a":"s17-3","h":"17.3 高级","x":"17.3 高级 实现 Active Object 模式 实现一个 4 阶段 pipeline，怎么找瓶颈? 什么是 work-stealing? 主要解决什么问题? std::future 的内部实现 (shared state)? 跨线程异常传递有几种方式? 怎么做配置热加载且对读端零开销? (COW + atomic shared_ptr) C++20 协程相比 future + then 有什么优势? 给一个限流器实现 (令牌桶 / 漏桶) 怎么实现优雅停服? 用 atomic 实现一个 lock-free SPSC queue shared_ptr 的引用计数为什么需要 atomic? 有没有不需要的场景?"},{"a":"s17-4","h":"17.4 速答示范（第 8 题）","x":"17.4 速答示范（第 8 题） 问 ：从零实现一个线程池，要支持任意 callable + 异常透传。 这题考的是 怎么说清楚 ，不是默写一整份 class。开口顺序： 结构：worker 池 + 有界队列 + mutex + cv submit 是变参模板，返回 future packaged_task 把 callable wrap 进去，异常自动进 future 它不可拷 ，所以队列 lambda 必须拷 shared_ptr<packaged_task> 进闭包用 forward 听调用方；闭包里再 move （§6.2.1） 停服： jthread + stop_token + notify_all 签名和 worker 取任务见 §6.2，不要在白板上默写构造/析构。"},{"a":"s18","h":"速查总表","x":"18 速查总表 同步原语选择 · 异步执行选择 · 死锁防御 6 条 · 并发模式速查"},{"a":"s18-1","h":"18.1 同步原语","x":"18.1 同步原语 选型表在 §2.2。mutex / 读写 / 无锁 / 消息四条路见图。"},{"a":"s18-2","h":"18.2 异步执行","x":"18.2 异步执行 场景 选 一次性任务 async （记住析构坑） 大量短任务 线程池 + future 有状态对象 Active Object / Actor 分阶段流 Pipeline 负载不均 work-stealing 可组合异步 协程 / Sender"},{"a":"s18-3","h":"18.3 死锁防御","x":"18.3 死锁防御 六条在 §14.1。定位走死锁篇，这里不复述。"},{"a":"s18-4","h":"18.4 并发模式速查","x":"18.4 并发模式速查 模式 核心 典型场景 线程池 worker + 队列 批量任务 Producer-Consumer 有界队列 + 阻塞 事件流处理 Pipeline 多阶段 + 中间队列 流式数据 Active Object 每对象一线程 状态隔离 Actor 调度器 + mailbox 百万 actor Work-Stealing per-worker 队列 高并发任务池 Reactor 事件循环 + 回调 IO 多路复用 协程 无栈, 编译期状态机 异步链式操作"},{"a":"s19","h":"下一步","x":"19 下一步 延伸阅读与练习 假共享 / cache line： 内存模型 TSan / ASan： Sanitizers atomic 与内存序： 内存模型 已死锁怎么定位： 死锁调试 §2 自己实现线程池时对照 §6.2 的 submit，benchmark 对比 std::async ← 上一篇 · 协程 相关 · 线程与原子操作 目录 主页 C++ 多线程编程模式完全指南 — future/promise · 线程池 · Actor · 协程 — 2026 ↑"}]},{"u":"stl/design_patterns_ppt.html","t":"C++ 设计模式 PPT","c":"现代 C++ / STL","s":[{"a":"","h":"","x":"C++ 设计模式 PPT — 工程案例 + Singleton / CRTP / PIMPL / Type Erasure / Visitor Kavin Tech Notes ← 主页 目录 70 min ◐ 设计模式 · Roadmap C++ 设计模式概览与选型 Singleton — 4 种实现演化 Factory — 工厂方法 + 注册表 Builder — 流式构造大型对象 Object Pool — 对象池 PIMPL — Pointer to Implementation 编译墙 CRTP — 静态多态 Type Erasure — std::function/std::any 风格接口 Adapter — 接口适配 Decorator — 链式包装行为 Observer / Signal-Slot — 事件订阅 Strategy — 策略模式（现代是 lambda） State — 状态机模式 Template Method — 固定流程骨架 HSM — Hierarchical State Machine 分层状态机 ⭐ Visitor — std::variant + std::visit 现代实现 Command — 把操作对象化（含 Undo） Iterator — 自定义容器的迭代器 Producer-Consumer — 经典并发模型 Active Object — 异步消息驱动对象 Sender / Receiver — C++26 结构化并发模式 ⭐⭐⭐ RAII + Scope Guard — C++ 第一原则 copy-and-swap 惯用法 Pass-by-Value + std::move 模式 Rule of Zero — 让编译器替你管 反模式集合 — 不要这么做 模式选型决策树（速查） 高频面试题（15 道 · 可折叠） 本专题结束 · 下一步 C++ Design Patterns · 2026 C++ 设计模式 不是 GoF 旧调重弹 — 是 用 C++ 现代特性重新表达 的设计模式 + C++ 独有惯用法。 各章节的 工程案例 抽象自两类常见的代码形态（C++「设备管理代理」与 Python「配置 / 校验 / 生成工具链」），均为 泛化示例 ：只保留模式骨架，不含真实路径、产品标识或逐文件拷贝。 核心难度 ★★★★☆ 面试出现率 高 阅读时长 ~70 min 前置 面向对象 · 模板 29 大章节 创建型 · 结构型 · 行为型 现代 C++ 惯用法 · 并发模式 单文件离线可读"},{"a":"slide-01","h":"C++ 设计模式概览与选型","x":"01 C++ 设计模式概览与选型 本 PPT 涵盖的模式 本 PPT 与 GoF 23 个模式的关系 覆盖 GoF 中 真正在 C++ 工程中常用 的模式 剔除 GoF 中已被语言特性取代的（如 Iterator → STL；Strategy → lambda） 新增 C++ 独有惯用法 ：CRTP、PIMPL、RAII、Type Erasure、copy-and-swap 每个模式给「 现代 C++ 写法 」而不是 1990 年代 OOP 重灾区 几条选型原则 能用 语言特性 解决 → 不要写设计模式（lambda 替代 Strategy/Functor） 能用 std::variant + visit → 不要用 Visitor 类层次 能用 std::function 接口 → 不要写 Observer 抽象类 能用模板 → 静态多态（CRTP）优于虚函数动态多态（性能更好） RAII 是 C++ 第一设计原则 ，先于一切其他模式 本 PPT 涵盖的模式 类别 模式 创建型 Singleton (4 种实现) · Factory · Builder · Object Pool 结构型 PIMPL · Adapter · Decorator · Type Erasure · CRTP 行为型 Observer/Signal · Strategy · State · Visitor · Command · Iterator 并发 Producer-Consumer · Active Object · Future/Promise 现代惯用法 RAII · Scope Guard · copy-and-swap · pass-by-value+move · Rule of Zero 反模式 God class · 单例滥用 · 多继承泛滥 · raw new/delete 工程案例总表 下表用 C++ 代理项目 表示设备侧代理风格的代码， Python 工具项目 表示配置与生成工具风格的代码；仅为教学对照，示例为泛化骨架。 数据流示意（虚线表示「形态启发」而非字面依赖） C++ 代理项目 场景 / 适配器 Python 工具项目 工厂 / 策略 本 PPT 泛化示例 模式 C++ 代理项目（启发点） Python 工具项目（启发点） Adapter 场景类依赖「配置会话端口」抽象，具体类封装协议栈与 XML/树操作 解析层把外部格式转成内部模型 Factory 按能力装配 unique_ptr 适配器 / 处理器 按节点类型选择 Paragraph / List / Complex 属性工厂 Strategy 运行期替换排序/校验算法时常用 std::function 文件生成：基类定义流程钩子，子类实现具体输出格式 Object Pool / 昂贵初始化 长生命周期会话与大块缓冲宜复用（与 RAII 组合） 模块级惰性解析缓存，避免重复装载同一份大文档 Singleton（慎用） 日志、指标注册表等进程级唯一资源 一次性缓存解析结果（注意测试可重置性） 所有工程案例只展示设计模式骨架， 不 包含版权头、产品型号或内部模块的真实命名 — 可放心用于公开分享。"},{"a":"slide-02","h":"Singleton — 4 种实现演化","x":"02 Singleton — 4 种实现演化 从经典到现代，一个模式问题反映了 C++ 30 年的并发演进。 ① 朴素实现（不线程安全） class Logger { static Logger * instance_; Logger () {} public : static Logger * get () { if (!instance_) instance_ = new Logger (); // ❌ 多线程下竞争 return instance_; } }; Logger * Logger ::instance_ = nullptr ; ② 双重检查锁（DCLP）— 历史方案 static std::atomic< Logger *> instance_; static std::mutex mu_; static Logger * get () { Logger * p = instance_. load (std::memory_order_acquire); if (!p) { std::lock_guard lk{mu_}; p = instance_. load (std::memory_order_relaxed); if (!p) { p = new Logger (); instance_. store (p, std::memory_order_release); } } return p; } // ⚠️ DCLP 的难点: C++11 之前无 atomic + memory order, 极易写错 ③ std::call_once（C++11，安全且通用） static std::once_flag flag_; static std::unique_ptr< Logger > instance_; static Logger & get () { std:: call_once (flag_, []{ instance_ = std:: make_unique < Logger >(); }); return *instance_; } ④ Meyers Singleton（C++11，最简最优 ⭐⭐⭐） class Logger { Logger () {} public : Logger ( const Logger &) = delete ; Logger & operator =( const Logger &) = delete ; static Logger & instance () { static Logger inst; // C++11 保证局部 static 线程安全初始化 (magic static) return inst; } }; // 使用 Logger :: instance (). log ( \"hello\" ); 结论 ：现代 C++ 单例 = Meyers Singleton 。① 自动线程安全；② 不需要显式删除指针；③ 析构顺序符合\"逆构造序\"；④ 代码最少。 能讲出 \"Magic Static\" 是 C++11 标准 [stmt.dcl]/4 强制保证的，编译器会插入 类似 call_once 的同步代码 → 满分回答。 不能 讲出\"DCLP 历史问题\" + \"为什么 atomic + memory order 是 C++11 才能正确实现 DCLP\" → 加分。 反模式警告 ：单例方便但 滥用 会让代码难测试、隐式依赖。优先依赖注入 (DI)，只有\"全局唯一\" + \"无状态依赖\" 才用单例（Logger / MetricsRegistry）。 四种实现一眼对比 方案 线程安全 代码量 析构 评价 ① 朴素裸指针 ❌ 竞争 少 泄漏 仅单线程玩具 ② DCLP ⚠️ 需 atomic+内存序 多 手动 历史方案，易写错 ③ call_once ✅ 中 自动 通用、可控初始化 ④ Meyers ✅ magic static 最少 逆序自动 首选 ⭐ 本质：多个调用方共享同一个实例 模块 A 模块 B 线程 C instance() 唯一对象 · 首次调用构造 所有调用方拿到的是同一个引用 —— 这也正是\"难以替换/测试\"的根源 到底该不该用单例？ ✅ 可以用 进程内 天然唯一 且无业务状态：日志器、指标注册表、配置只读快照 替代\"到处传同一个对象\"的样板，且 不影响测试 🚫 改用依赖注入 持有可变业务状态 / 外部资源（连接、设备会话） 单测需要替换或重置 → 单例的隐藏全局态会让用例互相污染 只是图方便少写一个参数 → 隐式依赖，后患无穷"},{"a":"slide-03","h":"Factory — 工厂方法 + 注册表","x":"03 Factory — 工厂方法 + 注册表 简单工厂方法 · 注册表式工厂（可插拔） 核心思想 把 对象的构造 和 使用 解耦。调用方只通过抽象接口/工厂得到对象，不关心具体类型。 什么时候用 对象类型由配置/运行时数据决定 可插拔的模块系统 测试需要替换实现 简单工厂方法 class Codec { public : virtual ~ Codec () = default ; virtual void encode ( const Message &, Buffer &) = 0; }; std::unique_ptr< Codec > make_codec ( const std::string& type) { if (type == \"json\" ) return std:: make_unique < JsonCodec >(); if (type == \"pb\" ) return std:: make_unique < PbCodec >(); throw std::invalid_argument(type); } 注册表式工厂（可插拔） class CodecRegistry { using Creator = std::function<std::unique_ptr< Codec >()>; std::unordered_map<std::string, Creator> map_; public : static CodecRegistry & instance () { static CodecRegistry r; return r; } void register_codec ( const std::string& name, Creator c) { map_[name] = std::move(c); } std::unique_ptr< Codec > create ( const std::string& name) { auto it = map_. find (name); return it == map_. end () ? nullptr : it->second(); } }; // 自动注册 — 在每个 codec 实现的 .cpp 中 struct JsonCodecRegister { JsonCodecRegister () { CodecRegistry :: instance (). register_codec ( \"json\" , []{ return std:: make_unique < JsonCodec >(); }); } }; static JsonCodecRegister _reg; // 静态对象在 main 之前自动注册 工程价值 ：插件 / Codec / Driver / Strategy 等机制都基于注册表 — 新增类型时 不需要改主代码 ，遵循开闭原则。 什么时候真的需要 Factory？（先想清楚再写） ✅ 适合 构造哪种对象由 运行时数据 （配置 / 报文 / 节点标签）决定 类型集合会 持续增长 ，希望新增时不改调用方 构造逻辑本身有分支 / 校验 / 默认值，不只是 new 测试需要 替换 具体实现（配合接口） 🚫 过度设计 只有一两种类型且基本不变 → 直接 if / make_unique 即可 为「将来可能扩展」预留的抽象工厂 → YAGNI 能用枚举 + 表驱动解决，却套了三层抽象类 选型顺序 ：① 一两种 → 自由函数 make_xxx() ；② 多种且固定 → 表驱动 / dict 分发 ；③ 需要第三方插件注册 → 注册表式工厂 。从简单往复杂走，不要一上来就抽象工厂层次。 CASE 工具项目：按「节点标签」分发的工厂（取材自真实校验器） 配置校验工具链解析 XML 时， 同一个 create() 入口 按节点的 tag 选择不同构造分支，返回对应领域对象 —— 这正是「表驱动工厂」的典型形态。下面在 保留结构 的前提下改写了名称、去掉了内部常量与产品细节。 # 形态取自真实 PropertiesFactory：静态 create + tag→builder 映射 class PropertyFactory : @staticmethod def create (node): builder = { \"paragraph\" : PropertyFactory._make_paragraph, \"list\" : PropertyFactory._make_list, }. get (node.tag) if builder is None : raise ValueError (f \"unsupported node: {node.tag}\" ) return builder(node) @staticmethod def _make_paragraph (node): return ParagraphProperty (_name(node), _value(node)) @staticmethod def _make_list (node): items = [PropertyFactory.create(c) for c in node.children] return ListProperty (_name(node), items) 为什么这样写而不是一堆 if/elif ？ ① 映射表一眼看全支持哪些类型；② 新增标签只加一行；③ create 递归处理嵌套结构（list 里再含 paragraph），结构清晰。 CASE 代理项目（C++）：同一问题的另一种语法 C++ 设备代理侧更常见的不是「字符串分发」，而是 接口 + 在 .cpp 里 make_unique 具体实现 ，把构造点收敛到一处、对调用方只暴露接口： // 调用方只看见 ISwmRpcAdapter；具体类型在 .cpp 决定，便于替换 / 打桩 class SwInventoryScenario { std::unique_ptr<adapter:: ISwmRpcAdapter > rpc_; public : explicit SwInventoryScenario (RadioContextPtr ctx) : rpc_(std:: make_unique < SwmRpcAdapter >(ctx)) {} // ← 构造点收敛于此 }; 两种写法 同一个目的 ：把「选哪种实现」与「怎么用」分开。Python 侧因鸭子类型用 dict 分发更轻；C++ 侧靠接口 + 智能指针获得可替换性与 RAII。"},{"a":"slide-04","h":"Builder — 流式构造大型对象","x":"04 Builder — 流式构造大型对象 典型 Builder · C++20 现代写法（聚合 + 指定初始化） 解决什么 构造函数参数 太多 （10+）+ 大多有默认值 → 调用方写得痛苦。 用 Builder 链式调用，可读性提升 10×。 C++20 替代方案：指定初始化 Foo{.a=1, .b=2} 大多数 builder 场景可以被取代（适用于聚合类型）。 典型 Builder class HttpRequest { /* 大量字段 */ }; class HttpRequestBuilder { HttpRequest req_; public : HttpRequestBuilder & method (std::string m) { req_.method = std::move(m); return * this ; } HttpRequestBuilder & url (std::string u) { req_.url = std::move(u); return * this ; } HttpRequestBuilder & header (std::string k, std::string v) { req_.headers. emplace (std::move(k), std::move(v)); return * this ; } HttpRequestBuilder & body (std::string b) { req_.body = std::move(b); return * this ; } HttpRequestBuilder & timeout (std::chrono::milliseconds t) { req_.timeout = t; return * this ; } HttpRequest build () && { return std::move(req_); } // rvalue ref qualifier }; // 使用 auto req = HttpRequestBuilder {} . method ( \"POST\" ) . url ( \"https://api.example.com/v1/users\" ) . header ( \"Authorization\" , \"Bearer ...\" ) . header ( \"Content-Type\" , \"application/json\" ) . body ( R\"({\"name\":\"alice\"})\" ) . timeout (5s) . build (); C++20 现代写法（聚合 + 指定初始化） struct HttpRequest { std::string method = \"GET\" ; std::string url; std::map<std::string, std::string> headers; std::string body; std::chrono::milliseconds timeout{2000}; }; auto req = HttpRequest { .method = \"POST\" , .url = \"https://...\" , .body = R\"(...)\", .timeout = 5s, }; 复杂构造（含校验/分支）仍用 Builder；纯字段集合用 C++20 designated initializer。 链式构造：每一步都返回自己 .method() .url() .header() .body() .build() → HttpRequest 每个 setter return *this → 可读性强； build() && 一次性 move 出成品 Builder 还是 C++20 指定初始化？ ✅ 用 Builder 构造含 校验 / 分支 / 计算 （如互斥字段、默认推导） 需要\"半成品不可见\"（只有 build() 后才拿到合法对象） 字段非常多、且常常只设其中几项 🚫 直接聚合初始化 纯数据结构、字段都有默认值 → Foo{.a=1,.b=2} 更短更直观 没有不变量需要在构造时强制 → Builder 是多余样板"},{"a":"slide-05","h":"Object Pool — 对象池","x":"05 Object Pool — 对象池 实现 — 通用 Object Pool · CASE：连接池 vs 惰性缓存 解决问题 对象构造 / 析构开销大（含系统调用 / 大块内存） 高 QPS 场景频繁创建 → GC / fragmentation 压力 典型： 连接池 / 线程池 / 大 Buffer 池 关键设计 用 RAII guard 自动归还（不依赖调用方调用 release） 线程安全 (mutex 或 lock-free) 高水位 / 超时回收 实现 — 通用 Object Pool template < class T> class ObjectPool : public std::enable_shared_from_this< ObjectPool <T>> { std::vector<std::unique_ptr<T>> pool_; std::mutex mu_; public : // 自定义 deleter — 归还到池而不是 delete using Handle = std::unique_ptr<T, std::function< void (T*)>>; Handle acquire () { std::unique_lock lk{mu_}; std::unique_ptr<T> obj; if (!pool_. empty ()) { obj = std::move(pool_. back ()); pool_. pop_back (); } lk. unlock (); if (!obj) obj = std:: make_unique <T>(); auto self = this -> shared_from_this (); return Handle(obj. release (), [self](T* p){ std::lock_guard lk{self->mu_}; self->pool_. emplace_back (p); }); } }; // 使用 — 离开作用域自动归还 auto pool = std:: make_shared < ObjectPool < Buffer >>(); { auto buf = pool-> acquire (); do_work (*buf); } // buf 析构 → deleter 把对象放回池 何时该上对象池？（多数情况答案是「不用」） ✅ 值得池化 构造成本 真的 高：建连接、握手、分配大块内存、加载模型 对象可 安全复用 ：能 reset 到干净状态 高频获取 / 释放，且并发争用可控 🚫 别池化 对象很轻（POD / 小 buffer）→ 直接构造更快也更安全 复用状态难清理 → 容易泄漏脏数据，得不偿失 用池只是为了\"省 new\" → 现代分配器已很快，先 profile 先问自己 ：构造开销是否进入了热路径？没量化过就别引入池 —— 它带来线程安全、生命周期、容量回收三类新复杂度。 CASE 连接池 + 「昂贵初始化」对照 左：经典数据库连接池 与 C++ 代理项目 中「长生命周期、系统调用密集」对象同一类动机 — 教学代码。 class DbConnectionPool { std::queue<std::unique_ptr< DbConn >> idle_; std::condition_variable cv_; std::mutex mu_; size_t max_size_; size_t active_ = 0; public : std::shared_ptr< DbConn > acquire (std::chrono::milliseconds timeout) { std::unique_lock lk{mu_}; if (!cv_. wait_for (lk, timeout, [ this ]{ return !idle_. empty () || active_ < max_size_; })) throw std::runtime_error{ \"timeout\" }; std::unique_ptr< DbConn > conn; if (!idle_. empty ()) { conn = std::move(idle_. front ()); idle_. pop (); } else conn = std:: make_unique < DbConn >(); ++active_; return std::shared_ptr< DbConn >(conn. release (), [ this ]( DbConn * p){ release (p); }); } void release ( DbConn * p) { std::lock_guard lk{mu_}; idle_. emplace (p); --active_; cv_. notify_one (); } }; 右：工具项目 — 惰性解析缓存（同族动机） 校验器对大配置文档只 objectify 解析一次，后续入口直接复用 —— 不是严格对象池，但共享 「昂贵初始化只做一次」 的核心动机（取材自真实的模块级缓存，名称已改写）。 # 形态取自真实的「模块级缓存全局量」 _doc_cache = None def get_doc (): global _doc_cache if _doc_cache is None : # 第一次才真正解析 _doc_cache = objectify. fromstring (_read_bytes()) return _doc_cache 这种「全局缓存」本质是 隐藏单例 ：单测必须留 reset 钩子，否则用例间状态互相污染。 两者的边界：复用「什么」 Object Pool 复用「活的」可变对象 取出 → 用 → reset → 归还 关注：并发 / 容量 / 清理 Lazy Cache 复用「不变的」解析结果 首次计算 → 此后只读 关注：失效 / 测试可重置"},{"a":"slide-06","h":"PIMPL — Pointer to Implementation 编译墙","x":"06 PIMPL — Pointer to Implementation 编译墙 C++ 经典编译墙 / 二进制兼容惯用法 解决什么 编译时间 ：头文件不暴露成员 → 改实现不用重编译用户代码 二进制兼容 (ABI) ：库的内部数据成员可以增删，sizeof 不变 编译墙 ：头文件不含第三方库 #include → 用户不需要那些依赖 隐藏实现 ：私有成员真的对用户不可见 代价 多一次堆分配（对象 + impl） 多一次指针跳转 不能 inline 实现 需要写转发函数（机械工作） 典型实现 // foo.h - 头文件 (干净) #pragma once #include <memory> class Foo { public : Foo (); ~ Foo (); Foo ( Foo &&) noexcept ; // 必须显式声明 (因为 unique_ptr<Impl> 中 Impl 不全) Foo & operator =( Foo &&) noexcept ; void do_something ( int ); std::string get_state () const ; private : struct Impl ; // 前置声明 std::unique_ptr< Impl > pImpl; }; // foo.cpp - 实现 #include \"foo.h\" #include <heavy_third_party.h> // 用户看不到 struct Foo :: Impl { HeavyThing data; Cache cache; Logger * log; }; Foo :: Foo () : pImpl(std:: make_unique < Impl >()) {} Foo ::~ Foo () = default ; // ⚠️ 必须在 .cpp 定义 (Impl 此时已知) Foo :: Foo ( Foo &&) noexcept = default ; Foo & Foo :: operator =( Foo &&) noexcept = default ; void Foo :: do_something ( int x) { pImpl->cache. put (x); } std::string Foo :: get_state () const { return pImpl->data. to_string (); } 坑 ：① 析构必须在 .cpp（否则 unique_ptr 删除不完整类型 → 编译错或 UB）；② 移动构造也要在 .cpp 默认。这两条踩过的人不在少数。 \"PIMPL 主要解决什么问题?\" — 答：① 编译时间 （头文件改动不影响用户）；② ABI 稳定性 （动态库版本演进）；③ 隐藏实现细节。 不 是为了\"封装\"（C++ 已有 private）。 编译墙：改实现，用户不用重编 用户代码 #include \"foo.h\" 只看见接口 foo.h Foo 接口 struct Impl;（前置） unique_ptr<Impl> —— 编译墙 —— foo.cpp struct Impl{...} heavy_3rd_party.h 所有重依赖 改这里只重编它 头文件不暴露成员 / 不含重依赖 → 改 Impl 时用户的 .o 无需重新编译；动态库 sizeof 不变保 ABI 什么时候值得 PIMPL？ ✅ 用 对外发布的 库 ，要保持 ABI / 头文件稳定 头文件被大量 TU 包含，编译时间是痛点 实现依赖一堆重型第三方头，不想传染给用户 🚫 不必 纯内部类、热路径小对象 → 多一次堆分配 + 指针跳转不值 只是想\"隐藏私有成员\" → C++ 的 private 已经够了 需要 inline / constexpr 性能 → PIMPL 挡住了内联"},{"a":"slide-07","h":"CRTP — 静态多态","x":"07 CRTP — 静态多态 静态多态示例 · 注入比较运算符 核心思想 派生类把 自己作为模板参数 传给基类： class Derived : public Base<Derived> {} 基类 编译期 知道派生类类型 → 可以静态调用派生类方法 → 无虚函数 / 无 vptr / 无运行时开销 。 典型用途 静态多态 (静态接口) 给派生类 注入通用功能 （如比较运算符、计数） Mixin 模式 std::enable_shared_from_this 就是 CRTP 静态多态示例 template < class Derived> class Shape { public : double area () const { return static_cast < const Derived*>( this )-> area_impl (); // 静态分发 } void print () const { std::cout << \"area=\" << area (); } }; class Circle : public Shape < Circle > { double r_; public : Circle ( double r) : r_(r) {} double area_impl () const { return 3.14159 * r_ * r_; } }; Circle {1.0}. print (); // area=3.14 — 编译器内联，零开销 注入比较运算符 template < class Derived> class Comparable { friend bool operator !=( const Derived& a, const Derived& b) { return !(a == b); } friend bool operator >( const Derived& a, const Derived& b) { return b < a; } friend bool operator <=( const Derived& a, const Derived& b) { return !(b < a); } friend bool operator >=( const Derived& a, const Derived& b) { return !(a < b); } }; class Money : public Comparable < Money > { int cents_; public : friend bool operator ==( const Money & a, const Money & b) { return a.cents_ == b.cents_; } friend bool operator <( const Money & a, const Money & b) { return a.cents_ < b.cents_; } }; // Money 自动获得 != / > / >= / <= ! // (C++20 起 spaceship 运算符让这种 mixin 大多没必要了) 性能优势 ：CRTP 编译期分发 → 编译器可以 完全 inline ，零运行时开销。虚函数有：① vptr 跳转；② 阻碍 inline 优化。 用法：\" 编译期就知道派生类 \" → CRTP；运行时不知道 → 虚函数。 静态分发 vs 动态分发 CRTP（静态多态） 编译期就知道 Derived 类型 base.area() → 直接 inline area_impl() 无 vptr · 可完全内联 · 零开销 virtual（动态多态） 运行期查 vtable 才知道实现 p->area() → load vptr → 间接 call 阻碍内联 · 一次间接跳转 同样是\"基类调派生实现\"，CRTP 把这步从运行期搬到编译期 该用 CRTP 还是虚函数？ ✅ 用 CRTP 类型在 编译期已知 、追求零开销（热路径、数值库） 给一批类 注入通用能力 （比较运算符、计数、 shared_from_this ） Mixin / 静态接口约束 🚫 用虚函数 需要 运行时 把不同子类装进同一容器（异构集合） 跨动态库边界、插件式扩展 代码可读性 > 极致性能，且不在热路径 C++20 起 operator<=> （spaceship）已能自动生成比较运算符 → \"注入比较\" 这类 CRTP mixin 大多不再需要。"},{"a":"slide-08","h":"Type Erasure — std::function/std::any 风格接口","x":"08 Type Erasure — std::function/std::any 风格接口 核心套路（手写一个简化 std::function） · REALstd::function 内部结构示意 核心思想 把\"任意符合接口的类型\" 统一存入 同一个容器/变量，但 不需要继承公共基类 。 典型代表： std::function 、 std::any 、 std::shared_ptr<void> 。 价值 解耦使用方与具体类型（无需共同基类） 第三方类型也能\"接入\"（不能改基类） API 接受任意 callable 而无需模板感染 核心套路（手写一个简化 std::function） class AnyCallable { // 内部抽象基类 struct Concept { virtual ~ Concept () = default ; virtual void invoke () = 0; virtual std::unique_ptr< Concept > clone () const = 0; }; // 模板化派生 — 包装具体类型 template < class F> struct Model : Concept { F f; Model (F f) : f(std::move(f)) {} void invoke () override { f (); } std::unique_ptr< Concept > clone () const override { return std:: make_unique < Model >(f); } }; std::unique_ptr< Concept > pImpl_; public : template < class F> AnyCallable (F f) : pImpl_(std:: make_unique < Model <F>>(std::move(f))) {} void operator ()() { pImpl_-> invoke (); } }; // 使用 — 任意 callable 都能装 AnyCallable a = []{ std::cout << \"lambda\\n\" ; }; struct F { void operator ()() { std::cout << \"functor\\n\" ; } }; AnyCallable b = F {}; void f () {} AnyCallable c = f; REAL std::function 内部结构示意 // std::function 实现 (简化) template < class Sig> class function; template < class R, class ... Args> class function<R(Args...)> { struct Storage { alignas ( void *) char sbo[24]; // SBO: 小对象不分配堆 }; // vtable 风格的 type erasure R (*invoke_)( Storage &, Args...); void (*destroy_)( Storage &); // ... }; 类型擦除是 运行时多态 的另一种实现 — 把 vtable 从类内部抽出来放在包装器中，让\"无关类型\"看起来一样。 Concept / Model：把\"无关类型\"统一起来 lambda functor 结构体 普通函数指针 互不相关、无公共基类 AnyCallable（包装器） Concept* （内部虚接口） Model<F> : Concept（按类型生成） 包装器内部藏一个虚接口 Concept ， Model<F> 为每个具体类型生成实现 → 外部看起来都是同一个类型 类型擦除 vs 模板 vs 继承 ✅ 用类型擦除 要把 无公共基类 的异构类型装进同一容器 / 变量 API 想接受\"任意 callable / 任意可打印\"而 不让模板感染 调用方 第三方类型无法改继承关系，但符合接口 🚫 别用 类型集合编译期已知 → std::variant 更省（无堆、无虚调用） 本就有公共基类 → 直接用虚函数更简单 极致性能热路径 → 一次间接调用 + 可能堆分配的代价要掂量"},{"a":"slide-09","h":"Adapter — 接口适配","x":"09 Adapter — 接口适配 把\"接口 A\"包成\"接口 B\"。在 C++ 中常见为 wrapper class 或 lambda。 例子：把 C 风格回调适配到现代 C++ 接口 // C 库的回调风格 extern \"C\" void register_callback ( void (*cb)( int , void * ctx), void * ctx); // 现代 C++ 想要 std::function 接口 class CCallbackAdapter { std::function< void ( int )> cb_; static void trampoline ( int x, void * ctx) { static_cast < CCallbackAdapter *>(ctx)->cb_(x); } public : explicit CCallbackAdapter (std::function< void ( int )> cb) : cb_(std::move(cb)) { register_callback (&trampoline, this ); } }; Adapter 何时用，和谁不要混淆 ✅ 用 Adapter 已有一个能干活、但接口\"不对味\"的类 / C 库 / 第三方 SDK 想让业务代码只依赖 自己定义的窄接口 ，便于替换与打桩 同一抽象需要 多种后端实现 （不同协议 / 传输） 🚫 不是 Adapter 只是想 加功能 （日志/重试）→ 那是 Decorator 只是想 简化一组子系统 调用 → 那是 Facade 接口本来就合适，纯转发 → 多余的间接层 CASE 代理项目：用「RPC 适配接口」隔离传输细节 设备代理里，几十个 场景 / Handler 都不直接碰 NETCONF/会话，而是依赖一个窄接口（下发 XML、读取上一次 RPC 状态）。具体适配器封装协议栈，并可在测试里换成假实现。下面接口形态 取自真实的 IGenericRpcAdapter （方法名保留、去掉了内部类型与命名空间）。 Scenario 业务编排 IRpcAdapter setWithXml / status （纯虚接口） RpcAdapter NETCONF / 树 FakeAdapter(测试) // 接口：方法名取自真实 IGenericRpcAdapter，去掉内部类型 class IRpcAdapter { public : virtual ~ IRpcAdapter () = default ; virtual RpcStatus getRpcStatus () const = 0; virtual bool setWithXml (std::string const & xml) const = 0; }; // 场景只持有接口；具体实现在 .cpp 里 make_unique（构造点收敛） class SwInventoryScenario { std::unique_ptr< IRpcAdapter > rpc_; public : explicit SwInventoryScenario ( RadioContextPtr ctx) : rpc_(std:: make_unique < SwmRpcAdapter >(ctx)) {} }; 收益 ：同一接口在软件清单、TLS、同步等场景复用 → 编排逻辑与传输细节解耦；单测注入 FakeAdapter 即可脱离真实设备跑用例。 容器适配器（STL 已有） // std::stack 把 deque 接口适配成 push/pop/top std::stack< int > s; // 内部用 deque s. push (1); s. top (); s. pop ();"},{"a":"slide-10","h":"Decorator — 链式包装行为","x":"10 Decorator — 链式包装行为 \" \"在不修改原对象的前提下添加新功能\" — 用 组合而不是继承 。 class Stream { public : virtual ~ Stream () = default ; virtual void write (std::string_view) = 0; }; class FileStream : public Stream { std::ofstream f_; public : FileStream ( const std::string& path) : f_(path) {} void write (std::string_view s) override { f_ << s; } }; // 装饰器: 添加压缩功能 class CompressStream : public Stream { std::unique_ptr< Stream > inner_; public : CompressStream (std::unique_ptr< Stream > s) : inner_(std::move(s)) {} void write (std::string_view s) override { inner_-> write ( compress (s)); } }; // 装饰器: 添加加密 class EncryptStream : public Stream { std::unique_ptr< Stream > inner_; public : EncryptStream (std::unique_ptr< Stream > s) : inner_(std::move(s)) {} void write (std::string_view s) override { inner_-> write ( encrypt (s)); } }; // 链式组合 auto stream = std:: make_unique < EncryptStream >( std:: make_unique < CompressStream >( std:: make_unique < FileStream >( \"data.bin\" ))); stream-> write ( \"hello\" ); // encrypt → compress → file 现实中比\"装饰器类\"更常用的是 函数组合 （pipeline）： auto pipeline = compose(encrypt, compress, file_write); 洋葱式包装：一层层加能力 EncryptStream（加密层） CompressStream（压缩层） FileStream（真正写文件） 核心对象 write(\"hello\") 自外向内：encrypt → compress → 落盘；每层只关心\"加一件事\"，不改原对象 Decorator 还是别的？ ✅ 用 Decorator 想在 不改原类 的前提下，按需叠加可组合的行为（压缩/加密/缓冲/限流） 组合是 运行时 决定的、顺序可变 用组合替代\"每种组合一个子类\"的继承爆炸 🚫 别混淆 是要 改接口形状 → Adapter，不是 Decorator 固定一串变换、无需多态 → 直接 函数组合 / pipeline 更轻 层数过深难调试 → 考虑显式 pipeline 表达"},{"a":"slide-11","h":"Observer / Signal-Slot — 事件订阅","x":"11 Observer / Signal-Slot — 事件订阅 现代 C++ 实现 — 用 std::function 替代 IObserver 接口 · RAII Connection — 自动断开 现代 C++ 实现 — 用 std::function 替代 IObserver 接口 template < class ... Args> class Signal { using Slot = std::function< void (Args...)>; std::vector<std::pair< size_t , Slot>> slots_; size_t next_id_ = 0; std::shared_mutex mu_; public : size_t connect (Slot s) { std::unique_lock lk{mu_}; auto id = next_id_++; slots_. emplace_back (id, std::move(s)); return id; } void disconnect ( size_t id) { std::unique_lock lk{mu_}; std:: erase_if (slots_, [id]( auto & p){ return p.first == id; }); } void emit (Args... args) { std::shared_lock lk{mu_}; for ( auto & [_, s] : slots_) s (args...); } }; // 使用 Signal < int , std::string> on_login; auto id = on_login. connect ([]( int uid, const std::string& n){ LOG( \"user {} ({}) logged in\" , uid, n); }); on_login. emit (42, \"alice\" ); RAII Connection — 自动断开 class ScopedConnection { Signal * sig_; size_t id_; public : ScopedConnection ( Signal * s, size_t id) : sig_(s), id_(id) {} ~ ScopedConnection () { if (sig_) sig_-> disconnect (id_); } // 禁拷贝, 允许 move ScopedConnection ( const ScopedConnection &) = delete ; ScopedConnection ( ScopedConnection && o) noexcept : sig_(o.sig_), id_(o.id_) { o.sig_ = nullptr ; } }; 真实生产建议用 boost::signals2 （成熟）或 sigslot 等库 — 含线程安全 + 自动断开 + 优先级。 一次 emit，广播给所有订阅者 Signal .emit(args) 日志 slot 指标上报 slot UI 刷新 slot 订阅者互不知道彼此存在；新增 / 移除一个 slot 不影响发射方 —— 这就是解耦的来源 Observer 何时用，要当心什么 ✅ 用 一处事件、 多个互不相关 的反应方（日志 / 缓存 / UI / 遥测） 发射方不该知道、也不想知道有谁在听 订阅关系运行时动态增减 ⚠️ 当心 生命周期 ：订阅者先析构却没退订 → 悬空回调（用 RAII Connection） 回调里做重活 / 再触发事件 → 卡发射线程、甚至重入死循环 只有一个固定订阅者 → 直接调用即可，别上事件系统 CASE 代理项目：通知 → 多订阅方 设备侧常有「网管下发 / 设备推送」异步事件：解析节点后分发给故障处理、状态缓存、遥测等模块。用 Signal / 消息总线 / 回调表均可，本质是 Observer ；关键仍是 生命周期 （谁先析构、谁取消订阅）。 环节 角色 源 协议栈 / 会话线程收到 XML 或内部事件 通道 窄接口回调或队列（避免在回调里做重活） 订阅方 场景对象、指标上报、告警门控（可多个）"},{"a":"slide-12","h":"Strategy — 策略模式（现代是 lambda）","x":"12 Strategy — 策略模式（现代是 lambda） 编译期 Strategy（模板）— 性能更好 GoF 旧写法（OOP 重灾区） class SortStrategy { public : virtual ~ SortStrategy () = default ; virtual void sort (std::vector< int >&) = 0; }; class QuickSort : public SortStrategy { /* ... */ }; class MergeSort : public SortStrategy { /* ... */ }; class Sorter { std::unique_ptr< SortStrategy > s_; public : void execute (std::vector< int >& v) { s_-> sort (v); } }; 现代 C++（lambda 替代） class Sorter { std::function< void (std::vector< int >&)> algo_; public : Sorter ( decltype (algo_) a) : algo_(std::move(a)) {} void execute (std::vector< int >& v) { algo_ (v); } }; // 使用 Sorter s1{[]( auto & v){ std:: sort (v. begin (), v. end ()); }}; Sorter s2{[]( auto & v){ std:: stable_sort (v. begin (), v. end ()); }}; 编译期 Strategy（模板）— 性能更好 template < class Algo> class Sorter { Algo algo_; public : void execute (std::vector< int >& v) { algo_ (v); } }; struct QuickSortAlgo { void operator ()(std::vector< int >& v) { std:: sort (v. begin (), v. end ()); } }; Sorter < QuickSortAlgo > s; // 编译期分发, 零开销 怎么选：lambda / 模板 / 还是要不要类层次？ ✅ 这样选 运行时切换、策略简单 → std::function + lambda 编译期已知、追求零开销 → 模板参数 + functor 策略 有状态、要注册、可热插拔 → 才考虑接口 + 工厂 🚫 别这样 为「一个 lambda 就够」的逻辑造三个 Strategy 子类 策略之间共享大量代码却用平行继承复制 Python 里没有 std::function 的烦恼：函数即对象，最轻的\"策略\"就是 传一个函数进去 。但当多个策略需要 共享流程骨架 时，基类 + 子类（Template Method）反而更清晰 —— 下面的真实例子正是这种情况。 CASE 工具项目：可插拔的「文件生成策略」（取材自真实生成器） IM 配置 → 产物文件时， Generator 持有一组 策略对象 ，逐个调用其 create_file_string() ；每种输出格式是一个子类。基类提供 register_config 这类公共钩子（Template Method），子类只实现\"怎么写\"。下面保留了真实的 装配方式 ，改写了名称、省略了产品细节。 # 基类：公共钩子；子类：具体输出格式 class FileGenerationStrategy : def __init__ ( self ): self .config = None def register_config ( self , config): # 公共步骤 self .config = config def create_file_string ( self , im_config): raise NotImplementedError # 由子类定义产物 class PythonStrategy ( FileGenerationStrategy ): def create_file_string ( self , im_config): # ... 把 im_config 渲染成 .py 文本 ... return { \"string\" : rendered} # 装配：Generator 不关心有哪些格式，只管挨个跑 class Generator : def __init__ ( self , config): self .config = config; self .strategies = [] def add_file_strategy ( self , strategy): strategy. register_config ( self .config) self .strategies. append (strategy) return self # 链式装配 def generate ( self , im): return [s. create_file_string ( self .im_mapping) for s in self .strategies] 这就是 Strategy 的价值所在 ： Generator 对\"有哪些输出格式\"一无所知 → 新增一种格式只需写一个新子类并 add_file_strategy ，主流程一行不改（开闭原则）。"},{"a":"slide-13","h":"State — 状态机模式","x":"13 State — 状态机模式 简单实现 — enum + switch · 用 std::variant 实现强类型状态机 简单实现 — enum + switch enum class ConnState : uint8_t { Init, Connecting, Established, Closing, Closed }; class Connection { std::atomic<ConnState> state_{ConnState::Init}; public : void on_event ( Event e) { switch (state_. load ()) { case ConnState::Connecting: handle_connecting (e); break ; case ConnState::Established: handle_established (e); break ; // ... } } bool try_transition (ConnState from, ConnState to) { return state_. compare_exchange_strong (from, to); } }; 用 std::variant 实现强类型状态机 struct Init {}; struct Connecting { std::chrono::steady_clock::time_point start; }; struct Established { std::string peer; int fd; }; struct Closed { int reason; }; using ConnState = std::variant< Init , Connecting , Established , Closed >; class Connection { ConnState state_{ Init {}}; public : void on_event ( Event e) { std:: visit ([ this , &e]( auto & st) { using T = std::decay_t< decltype (st)>; if constexpr (std::is_same_v<T, Init >) { if (e.type == EventType::Connect) state_ = Connecting { now ()}; } else if constexpr (std::is_same_v<T, Connecting >) { if (e.type == EventType::Done) state_ = Established {e.peer, e.fd}; } // ... }, state_); } }; 每个状态 携带各自需要的数据 （peer/fd 只在 Established 才有）→ 类型上禁止访问无关字段，比 enum + 类成员清晰得多。"},{"a":"slide-13b","h":"Template Method — 固定流程骨架","x":"13+ Template Method — 固定流程骨架 基类定下「步骤顺序」，子类只填空 —— 取材自真实场景基类 核心思想 基类把 流程顺序 写死（如：准备 → 执行 → 成功收尾 / 失败收尾），把其中 会变的步骤 留成虚函数让子类实现。 何时用 一批操作 骨架相同、细节不同 （典型：各种\"场景/事务\"） 想强制所有子类都走 同样的收尾 / 错误处理 注意：继承耦合强，步骤少时用「传函数」更轻 CASE 代理项目：所有「场景」共享同一生命周期 设备代理里几十个操作都建模成 Scenario ：基类 BaseScenario 固化了执行与收尾的骨架，并暴露 tearDownOk() / tearDownError() 两个钩子；每个具体场景只 override 自己的收尾逻辑。下面方法名取自真实基类，去掉了内部类型。 // 基类：固化\"成功收尾 / 失败收尾\"两个扩展点 class BaseScenario { public : virtual ~ BaseScenario () = default ; protected : virtual void tearDownOk () = 0; // 子类填空 virtual void tearDownError () = 0; // 子类填空 // run()/收尾顺序等公共骨架在基类里固定 }; // 具体场景：只关心自己的收尾；并遵守 Rule of Five 禁拷贝 class SwInventoryScenario final : public BaseScenario { public : explicit SwInventoryScenario ( RadioContextPtr ); SwInventoryScenario ( const SwInventoryScenario &) = delete ; SwInventoryScenario & operator =( const SwInventoryScenario &) = delete ; private : void tearDownOk () final ; // ← 只填这两步 void tearDownError () final ; }; 收益 ：① 所有场景的「成功/失败收尾」入口统一，不会有人漏掉错误处理；② 新增一个场景=继承+实现两个钩子；③ final 阻止再被继承、利于内联与意图清晰。 与 Strategy 的区别 ：Template Method 用 继承 在编译期固定\"变化点\"；Strategy 用 组合 在运行期注入变化点。步骤多且强约束顺序 → 前者；要运行时自由替换 → 后者。"},{"a":"slide-14","h":"HSM — Hierarchical State Machine 分层状态机 ⭐","x":"14 HSM — Hierarchical State Machine 分层状态机 ⭐ UML statechart / Miro Samek 的核心思想 — 解决\"扁平状态机状态爆炸\"问题 动机：扁平状态机的痛 普通 FSM (上一张) 有 N 个状态、M 个事件 → 最坏情况 N×M 个转移。一个电话机 HSM 的状态可能： 挂机 / 忙 / 振铃 / 拨号 / 通话中 / 来电中 / 静音 / 免提 ... \"按音量键\"在 每一个 非挂机状态下都要处理一遍 → 重复代码 HSM 把状态组织成 树 ：子状态继承父状态的事件处理。父状态处理\"按音量键\"一次即可。 扁平 FSM — 状态爆炸 switch (state) { case Idle: if (e==VolUp) ...; break ; case Dialing: if (e==VolUp) ...; break ; case Ringing: if (e==VolUp) ...; break ; case Talking: if (e==VolUp) ...; break ; // 每个 case 都重复音量处理 } HSM — 状态树继承 // 状态树 // Active (handles VolUp/VolDown) // ├── Idle // ├── OffHook // │ ├── Dialing // │ ├── Talking // │ └── Held // └── Ringing struct Active { handle (VolUp e); }; struct OffHook : Active { handle (HangUp); }; struct Talking : OffHook { handle (Mute); }; 核心机制：事件冒泡 + 进入/退出动作 (entry / exit) // 简化 HSM 实现 — 用 std::variant + 静态父子映射 enum class St { Active, Idle, OffHook, Dialing, Talking, Ringing }; // 父表 — 编译期常量 constexpr St parent_of[] = { St ::Active, // Active 自身是根 St ::Active, // Idle → Active St ::Active, // OffHook → Active St ::OffHook, // Dialing → OffHook St ::OffHook, // Talking → OffHook St ::Active, // Ringing → Active }; class Phone { St state_ = St ::Idle; public : void dispatch ( Event e) { St s = state_; while (s != St ::Active) { // 子→父冒泡 if ( handle (s, e)) return ; // 当前层吃了事件 s = parent_of[ static_cast < int >(s)]; // 向上传 } handle ( St ::Active, e); // 根层兜底 } void transit ( St target) { // 1. 计算 LCA (最近公共祖先) // 2. 从 state_ 一路 exit 到 LCA // 3. 从 LCA 一路 entry 到 target exit_path (state_, lca (state_, target)); entry_path ( lca (state_, target), target); state_ = target; } }; 典型应用 嵌入式 / RTOS 电话、UI、状态机协议解析。Miro Samek 的 QP/C++ 是工业标准。 游戏 AI NPC 行为树常用 HSM：Patrol → Combat (有 Aim/Reload/Flee 子状态) → Dead。 协议状态机 TLS、SIP、RTP、QUIC 都是天然的 HSM —— 标准文档里就是 statechart 形式。 C++ 生态库 库 风格 特点 Boost.SML 编译期 DSL 转移表用模板 + 操作符；零开销 sml2 更轻量、C++20 concepts 编译时检查、无虚函数 QP/C++ (Samek) 运行时 HSM 嵌入式实时；带 trace 工具 TinyFSM header-only 编译期分发，零虚函数 选型 ：状态 < 6 个、转移简单 → switch 或 variant；状态 > 10 / 多层 / 协议状态机 → HSM 库；安全关键系统（医疗、航空） → QP 或 Samek 风格的 trace-able HSM。 面试加分点：\"为什么 TCP 不用扁平 FSM？\" → 答 TIME_WAIT、CLOSE_WAIT 等都共享相同的 RST 处理 → 自然适合 HSM。"},{"a":"slide-15","h":"Visitor — std::variant + std::visit 现代实现","x":"15 Visitor — std::variant + std::visit 现代实现 更优雅：Overloaded helper GoF 旧写法 — 双重分派 class Visitor { public : virtual void visit ( Circle &) = 0; virtual void visit ( Square &) = 0; virtual void visit ( Triangle &) = 0; }; class Shape { public : virtual void accept ( Visitor &) = 0; }; class Circle : public Shape { public : void accept ( Visitor & v) override { v. visit (* this ); } }; 问题：① 加新形状必须改所有 Visitor；② 类层次膨胀；③ 调用复杂。 C++17 现代写法 struct Circle { double r; }; struct Square { double a; }; struct Triangle { double b, h; }; using Shape = std::variant< Circle , Square , Triangle >; double area ( const Shape& s) { return std:: visit ([]( auto && x) -> double { using T = std::decay_t< decltype (x)>; if constexpr (std::is_same_v<T, Circle >) return 3.14 * x.r * x.r; else if constexpr (std::is_same_v<T, Square >) return x.a * x.a; else return 0.5 * x.b * x.h; }, s); } 更优雅：Overloaded helper template < class ... Fs> struct Overloaded : Fs... { using Fs:: operator ()...; }; template < class ... Fs> Overloaded (Fs...) -> Overloaded <Fs...>; // CTAD double area ( const Shape& s) { return std:: visit ( Overloaded { []( const Circle & c) { return 3.14 * c.r * c.r; }, []( const Square & sq) { return sq.a * sq.a; }, []( const Triangle & t){ return 0.5 * t.b * t.h; } }, s); } std::visit 按\"当前装的类型\"分派 variant<Circle,Square,Tri> 当前活动: Square ✓ std::visit [](Circle&){ ... } [](Square&){ a*a } ← 命中 [](Triangle&){ ... } 编译期为每个可能类型生成分支，运行期按 variant 的活动下标选对应 lambda —— 无虚函数、无 accept 样板 variant+visit 还是经典 Visitor？ ✅ variant + visit 类型集合 封闭固定 （AST 节点、协议消息、状态） 常加 新操作 而很少加新类型 → 写个新 visitor 即可 想要值语义、无堆分配、编译期穷尽检查 🚫 用虚函数 / 类层次 类型由 第三方 / 插件 开放扩展（编译期不知道全集） 常加 新类型 而操作稳定 → 继承体系更顺 需要跨动态库边界多态"},{"a":"slide-16","h":"Command — 把操作对象化（含 Undo）","x":"16 Command — 把操作对象化（含 Undo） 现代 C++ 写法 — std::function 替代 \"操作\"本身变成一等对象 → 可保存、排队、撤销、重做、记日志。 class Command { public : virtual ~ Command () = default ; virtual void execute () = 0; virtual void undo () = 0; }; class SetTextCommand : public Command { TextEditor * ed_; std::string new_; std::string prev_; public : SetTextCommand ( TextEditor * e, std::string n) : ed_(e), new_(std::move(n)) {} void execute () override { prev_ = ed_-> text (); ed_-> set (new_); } void undo () override { ed_-> set (prev_); } }; class UndoStack { std::vector<std::unique_ptr< Command >> done_; std::vector<std::unique_ptr< Command >> redo_; public : void execute (std::unique_ptr< Command > c) { c-> execute (); done_. push_back (std::move(c)); redo_. clear (); } void undo () { if (done_. empty ()) return ; done_. back ()-> undo (); redo_. push_back (std::move(done_. back ())); done_. pop_back (); } void redo () { /* 对称 */ } }; 现代 C++ 写法 — std::function 替代 struct Action { std::function< void ()> do_; std::function< void ()> undo_; }; std::vector< Action > history_; history_. push_back ({ [ this ]{ apply_change (); }, [ this ]{ revert_change (); } }); execute / undo / redo：两个栈来回搬 done 栈（已执行） SetText(\"hi\") Insert(...) Delete(...) ← top redo 栈（已撤销） （空） undo：top 出 done → 入 redo（execute 时清空 redo） 把\"操作\"做成一等对象后，撤销/重做/录制/回放都只是 对栈的搬运 Command 何时用 ✅ 用 需要 撤销/重做、排队、延迟执行、录制回放、审计日志 把\"请求\"与\"执行者/执行时机\"解耦（如任务队列、事务） 每个操作都能描述自己的 逆操作 🚫 别用 只是立即调用一次、无需保存/撤销 → 直接调函数 简单回调用 std::function 的 Action{do,undo} 就够，不必造类层次"},{"a":"slide-17","h":"Iterator — 自定义容器的迭代器","x":"17 Iterator — 自定义容器的迭代器 STL 的 Iterator 是核心抽象 STL 的 Iterator 是 核心抽象 。给自定义容器加 begin/end/iterator → 自动获得 range-for / 算法支持。 class RingBuffer { std::vector< int > data_; size_t head_ = 0, size_ = 0, cap_; public : class iterator { RingBuffer * rb_; size_t i_; public : using iterator_category = std::forward_iterator_tag; using value_type = int ; using difference_type = std::ptrdiff_t; using reference = int &; using pointer = int *; iterator ( RingBuffer * rb, size_t i) : rb_(rb), i_(i) {} int & operator *() const { return rb_->data_[(rb_->head_ + i_) % rb_->cap_]; } iterator & operator ++() { ++i_; return * this ; } bool operator ==( const iterator & o) const { return i_ == o.i_; } bool operator !=( const iterator & o) const { return i_ != o.i_; } }; iterator begin () { return { this , 0}; } iterator end () { return { this , size_}; } }; // 立刻获得 range-for + STL 算法 RingBuffer rb; for ( int x : rb) {} std:: find (rb. begin (), rb. end (), 42); 现代 C++20 的 ranges + sentinel 让自定义迭代器更简单 — end() 可以返回不同类型的\"哨兵\"，常用于无穷序列、流式数据。 range-for 其实是这样展开的 for (int x : rb) 编译器自动展开 ↓ auto it = rb.begin(); for (; it != rb.end(); ++it) int x = *it; 只要提供 begin/end 和 * / ++ / != ，自定义容器立刻获得 range-for 与全部 STL 算法 要不要手写迭代器？ ✅ 值得写 自定义容器 / 视图，想接入 range-for 和 STL 算法 需要惰性 / 无穷序列、流式数据（配 C++20 sentinel） 遍历逻辑复杂（环形、跳跃、过滤），想对外隐藏 🚫 不必 内部就用 std::vector 等标准容器 → 直接复用它们的迭代器 只遍历一两次的私有数据 → 普通索引循环更直接 C++20 下优先考虑 views 组合而非从零手写"},{"a":"slide-18","h":"Producer-Consumer — 经典并发模型","x":"18 Producer-Consumer — 经典并发模型 用 用 有界队列 + mutex + cv 解耦生产者和消费者速率。 template < class T> class BoundedQueue { std::queue<T> q_; std::mutex mu_; std::condition_variable cv_not_full_, cv_not_empty_; size_t cap_; bool closed_ = false ; public : explicit BoundedQueue ( size_t cap) : cap_(cap) {} bool push (T v) { std::unique_lock lk{mu_}; cv_not_full_. wait (lk, [ this ]{ return closed_ || q_. size () < cap_; }); if (closed_) return false ; q_. push (std::move(v)); cv_not_empty_. notify_one (); return true ; } std::optional<T> pop () { std::unique_lock lk{mu_}; cv_not_empty_. wait (lk, [ this ]{ return closed_ || !q_. empty (); }); if (q_. empty ()) return std::nullopt; // closed + drained T v = std::move(q_. front ()); q_. pop (); cv_not_full_. notify_one (); return v; } void close () { std::lock_guard lk{mu_}; closed_ = true ; cv_not_empty_. notify_all (); cv_not_full_. notify_all (); } }; // 使用 BoundedQueue < Job > q{1024}; std::thread producer{[&]{ while (more) q. push ( make_job ()); }}; std::thread consumer{[&]{ while ( auto j = q. pop ()) process (*j); }}; 有界队列：用\"满/空\"两个条件变量解耦速率 Producer 满则阻塞 有界队列 (cap) Consumer 空则阻塞 push → notify 不空 · pop → notify 不满 · close 唤醒全部 队列把两端的 速率波动 吸收掉：快的一方被反压、慢的一方不空转 高性能场景考虑 lock-free MPMC queue（如 moodycamel::ConcurrentQueue）。"},{"a":"slide-19","h":"Active Object — 异步消息驱动对象","x":"19 Active Object — 异步消息驱动对象 把对象的每个公开方法变成\"投递到队列的消息\" 把对象的每个公开方法变成\"投递到队列的消息\"，由内部专属线程顺序执行 → 无需锁 就能保证状态一致。 class ActiveSession { std::queue<std::function< void ()>> q_; std::mutex mu_; std::condition_variable cv_; std::jthread worker_; bool stop_ = false ; // 内部状态 - 只在 worker 线程访问，不需要锁 int state_ = 0; public : ActiveSession () : worker_([ this ](std::stop_token st){ loop (st); }) {} // 同步接口 → 投递为异步消息 + future 等待 std::future< int > get_state () { auto p = std:: make_shared <std::promise< int >>(); auto f = p-> get_future (); enqueue ([ this , p]{ p-> set_value (state_); }); return f; } void increment () { enqueue ([ this ]{ ++state_; }); } private : void enqueue (std::function< void ()> f) { { std::lock_guard lk{mu_}; q_. push (std::move(f)); } cv_. notify_one (); } void loop (std::stop_token st) { std::stop_callback cb(st, [ this ]{ cv_. notify_all (); }); while (!st. stop_requested ()) { std::unique_lock lk{mu_}; cv_. wait (lk, [ this , &st]{ return !q_. empty () || st. stop_requested (); }); if (st. stop_requested () && q_. empty ()) return ; auto task = std::move(q_. front ()); q_. pop (); lk. unlock (); task (); } } }; 调用 = 投信；专属线程顺序处理邮箱 线程 A 调用 线程 B 调用 邮箱队列 enqueue(λ) worker 线程 顺序执行 状态只被那一个 worker 线程访问 → 无需加锁 ；同步结果通过 future 取回 类似 Erlang Actor / Akka — 每个 Active Object 一个邮箱 + 一个线程 + 自有状态。 没有锁 ，但有 future 延迟开销。"},{"a":"slide-20","h":"Sender / Receiver — C++26 结构化并发模式 ⭐⭐⭐","x":"20 Sender / Receiver — C++26 结构化并发模式 ⭐⭐⭐ P2300 (std::execution) · 取代 std::future · 通吃 CPU/GPU/IO 调度 动机：现有异步模型的痛 方案 痛点 裸 callback 嵌套地狱、错误传播靠手动、生命周期靠 shared_ptr 兜底 std::future 无 .then / 无组合算子（标准库里）；类型擦除性能开销 第三方 future（folly/Boost） 各成生态，互不兼容 C++20 协程 解决\"线性代码可读性\"，但调度器/取消/组合仍是裸的 P2300 （NVIDIA + Meta + Microsoft 推动，C++26）给出一套 异步执行的通用代数 ：Scheduler / Sender / Receiver 三个概念覆盖线程池、IO、GPU、协程框架。 三大概念（设计模式的\"配方\"） Scheduler \"在哪里跑\" 。线程池、IO 线程、GPU、协程框架的统一抽象。 auto sched = pool. get_scheduler (); Sender \"做什么\" — 异步操作的 描述符 ，懒求值，可组合。 auto s = ex:: schedule (sched) | ex:: then ([]{ return work (); }); Receiver \"结果交给谁\" — 三个回调入口： set_value / set_error / set_stopped 。 // 通常用户写 sender， // receiver 由组合算子隐式构造 典型组合算子（\"配方书\"） // 串行 auto chain = ex:: schedule (sched) | ex:: then ([]{ return step1 (); }) | ex:: then ([]( auto v){ return step2 (v); }); // 并行 fan-out + join auto both = ex:: when_all ( load_a (), load_b ()) | ex:: then ([]( auto a, auto b){ return merge (a, b); }); // 切换执行上下文（线程池 → GPU → CPU 回写） auto pipeline = read_file (p) | ex:: continues_on (gpu_sched) | ex:: bulk (N, kernel) | ex:: continues_on (cpu_sched) | ex:: then (write_back); // 错误恢复 auto safe = chain | ex:: upon_error ([](std::exception_ptr e){ return default_value; }); // 取消传播 — 自动！ // 父 stop_token 触发 → 整条管线收到 set_stopped 与 Active Object / Producer-Consumer 模式的关系 Active Object = \"把请求放进邮箱\" → S/R 是\"把请求 表达成 sender \"，可在任意 scheduler 上调度，更通用 Producer-Consumer = 双向数据流 → S/R 适合\" 单向请求 → 响应 \"型异步，流式建议 ranges + execution policy Reactor = 事件循环驱动 → S/R 可以以 Reactor 为 scheduler 后端 结构化并发 (Structured Concurrency) 契约 子任务生命周期不能超过父任务 —— 父结束自动取消所有未完成子 sender 取消是头等公民 —— stop_token 由 receiver 提供，沿组合链自动传播 无悬空 callback —— sender 不能\"逃出\"作用域，比 detach() 安全 错误聚合 —— when_all 任一失败，其他自动取消，统一上报 与 C++20 协程的关系 — 不是替代，是底座 // 任意 sender 都能 co_await，反之亦然 Task< int > orchestrate () { auto [a, b] = co_await ex:: when_all ( ex:: schedule (s) | ex:: then (step1), ex:: schedule (s) | ex:: then (step2)); co_return a + b; } 协程提供\"线性代码风格\"，S/R 提供\"调度与组合代数\" → 互补 。Eric Niebler 设计 P2300 时就明确\"为协程铺路\"。 现在就能用的实现 库 说明 NVIDIA stdexec P2300 参考实现，C++20 即可用，CUDA / GPU 后端 libunifex Eric Niebler 的原型，Meta 生产环境用 Asio 1.30+ 实验性 sender 适配 ( asio::experimental::use_sender ) 这就是 Trio / Kotlin Coroutines / Swift Structured Concurrency 引领的范式 ，C++26 终于跟上。它从根本上消除一类异步代码 bug：goroutine / detached thread / dangling callback 泄漏。Tech Lead 角度看，这是未来 5 年异步 C++ 库的通用语言。"},{"a":"slide-21","h":"RAII + Scope Guard — C++ 第一原则","x":"21 RAII + Scope Guard — C++ 第一原则 事务回滚模式 资源 (内存、锁、fd、连接) 的生命周期 = 对象的生命周期。 → 异常安全自动获得。 // Scope Guard — 通用清理 template < class F> class ScopeExit { F f_; bool active_ = true ; public : explicit ScopeExit (F f) : f_(std::move(f)) {} ~ ScopeExit () noexcept { if (active_) try { f_ (); } catch (...) {} } void dismiss () noexcept { active_ = false ; } ScopeExit ( const ScopeExit &) = delete ; }; template < class F> ScopeExit (F) -> ScopeExit <F>; // 使用 int fd = :: open ( \"x\" , O_RDONLY); ScopeExit close{[fd]{ :: close (fd); }}; // ... 任何抛点都能保证 close ... 事务回滚模式 void transfer ( Account & from, Account & to, double amt) { from. withdraw (amt); ScopeExit rollback{[&]{ from. deposit (amt); }}; to. deposit (amt); // 可能抛 → rollback 自动执行 rollback. dismiss (); // 成功 → 取消回滚 } 规则 ：所有\"作用域结束必须做 X\" 的场景都该用 RAII。 不要写 try/finally 风格 — C++ 没 finally，但你有 RAII。"},{"a":"slide-22","h":"copy-and-swap 惯用法","x":"22 copy-and-swap 惯用法 4 class String { char * data_ = nullptr ; size_t size_ = 0; public : // 1. 写好 swap (noexcept!) void swap ( String & o) noexcept { using std::swap; swap (data_, o.data_); swap (size_, o.size_); } // 2. 拷贝构造 (异常时不影响 *this) String ( const String & o) : data_( new char [o.size_]), size_(o.size_) { std:: memcpy (data_, o.data_, size_); } // 3. 唯一的赋值 — 同时实现拷贝赋值 + 移动赋值 String & operator =( String rhs) noexcept { // ⭐ 按值收 swap (rhs); return * this ; } }; // 调用方: String a, b; a = b; // 拷贝构造 rhs (可能抛) → swap (noexcept) → rhs 析构 (旧资源) a = std::move(b); // 移动构造 rhs (noexcept) → swap → rhs 析构 4 个好处 ： ① 强异常安全保证（任何抛都不影响 *this） ② 一份代码搞定拷贝赋值 + 移动赋值（重载消除） ③ 自动处理自赋值 (a = a) ④ 简洁、不易写错"},{"a":"slide-23","h":"Pass-by-Value + std::move 模式","x":"23 Pass-by-Value + std::move 模式 什么时候用 对于 需要存储 的参数（最终要保存到成员变量），用 按值收 + std::move 入成员 是最优做法。 class User { std::string name_; std::vector<std::string> tags_; public : // ❌ 旧写法: 两个重载 - 拷贝版和移动版 // User(const std::string& n) : name_(n) {} // User(std::string&& n) : name_(std::move(n)) {} // ✅ 现代写法 - 一份代码处理两种情况 User (std::string n, std::vector<std::string> tags) : name_(std::move(n)), tags_(std::move(tags)) {} }; // 调用方: std::string s = \"alice\" ; User u1{s, {}}; // s 拷贝构造 n → move 入 name_ (1 拷贝 + 1 移动) User u2{std::move(s), {}}; // s 移动构造 n → move 入 name_ (2 移动) User u3{ \"bob\" , {}}; // 临时构造 n → move 入 name_ (1 构造 + 1 移动) 什么时候用 ✅ 参数最终要 存入成员 （\"sink\" 参数） ✅ 类型有高效的移动构造（vector / string / unique_ptr） ❌ 仅\"读用\"的参数 → 用 const T& ❌ 类型移动开销 = 拷贝开销（int / 小 POD） → 直接按值传"},{"a":"slide-24","h":"Rule of Zero — 让编译器替你管","x":"24 Rule of Zero — 让编译器替你管 \"不写析构 拷贝 \" 不写 析构、拷贝、移动 — 让所有成员管好自己的资源\"。这是 C++ 现代代码的 金标准 。 // ✅ Rule of Zero — 所有成员都是 RAII 友好 class User { std::string name_; // 自管 std::vector< Order > orders_; // 自管 std::shared_ptr< Profile > profile_; // 自管 std::unique_ptr< Connection > conn_; // 自管 public : // 编译器自动生成: 默认构造、析构、拷贝(2)、移动(2) — 全都正确! }; // 如果有\"独占资源\"且没有现成 RAII 类 — 用 unique_ptr + 自定义 deleter 包装 class DbHandle { std::unique_ptr<sqlite3, decltype (&sqlite3_close)> db_; public : DbHandle () : db_( nullptr , &sqlite3_close) { /* open ... */ } // 仍是 Rule of Zero — unique_ptr 完成所有工作 }; 结论 ：99% 的业务类应该是 Rule of Zero。只有 包装裸资源 的底层类需要 Rule of 5。"},{"a":"slide-25","h":"反模式集合 — 不要这么做","x":"25 反模式集合 — 不要这么做 反模式 问题 正确做法 God Singleton \"什么都放进单例\" 难测试 / 隐式依赖 / 全局状态 依赖注入 (DI) / 工厂 裸 new / delete 异常泄漏 / 双重释放 make_unique / make_shared Java 风格继承层次 所有类都继承一个基类 菱形继承 / 滥用虚函数 / 难维护 组合优于继承 / Rule of Zero 用异常做控制流 性能差 / 难读 普通返回 / optional / expected 滥用 dynamic_cast 性能差 / 设计耦合 variant / 虚函数 / visitor 滥用 friend 破坏封装 / 高耦合 把功能改为成员或 free function 多继承泛滥 菱形 / 二义性 / 维护难 组合 / mixin (CRTP) 类成员到处暴露 setter 状态不变量难保证 只通过有意义的方法修改 OOP \"深\"层次 抽象类下 10 层派生 调试难 / 性能差 / 改动连锁 1-2 层 + 组合 + 模板 用 typedef 替代意图 类型安全弱 strong typedef (StrongId 模式) 面试反向问\"你见过哪些 C++ 反模式?\" — 能列出 5 个 + 给出修复方法 → 加分。"},{"a":"slide-26","h":"模式选型决策树（速查）","x":"26 模式选型决策树（速查） 需求 首选 备选 全局唯一对象 Meyers Singleton 依赖注入 构造复杂对象 C++20 designated init Builder 可插拔实现 注册表 + Factory 模板策略 对象生命周期管理 RAII (unique_ptr / shared_ptr) — 多种相关类型互转 std::variant + std::visit 虚函数 + Visitor 事件订阅 std::function + Signal boost::signals2 可替换算法 lambda / std::function 模板 functor 状态机 variant 状态 + visit / enum + switch state class 层次 异常安全 operator= copy-and-swap — 编译墙 / ABI 稳定 PIMPL 抽象基类 + 工厂 编译期多态 CRTP / 模板 — 运行时多态 (无共同基类) Type Erasure (std::function 风格) std::any 跨线程异步 future/promise / Active Object 裸线程 + cv 队列解耦 BoundedQueue (Producer-Consumer) lock-free 队列 类型集合封闭 variant + visit — 类型集合开放 (插件) 抽象基类 + 工厂 type erasure"},{"a":"slide-27","h":"高频面试题（15 道 · 可折叠）","x":"27 高频面试题（15 道 · 可折叠） 每题：关键词 + 原理 + 代码骨架 + 加分点 · 30 秒可口述"},{"a":"slide-27-1","h":"基础（送分，必须 3 秒答出 / 1 分钟写出）","x":"基础（送分，必须 3 秒答出 / 1 分钟写出） 点击每道题展开参考答案（先自测，再对照）。 基础 实现一个线程安全的 Singleton (4 种) // 1. Meyers Singleton — C++11 后首选 Singleton& get () { static Singleton inst; // 标准保证 thread-safe 且仅一次 return inst; } // 2. std::call_once static std::once_flag flag; static std::unique_ptr<Singleton> inst; Singleton& get () { std:: call_once (flag, []{ inst. reset ( new Singleton); }); return *inst; } // 3. DCLP (Double-Checked Locking) + atomic — C++11 起合法 static std::atomic<Singleton*> inst{ nullptr }; static std::mutex mu; Singleton* get () { Singleton* p = inst. load (std::memory_order_acquire); if (!p) { std::lock_guard lk(mu); p = inst. load (std::memory_order_relaxed); if (!p) { p = new Singleton; inst. store (p, std::memory_order_release); } } return p; } // 4. 老 DCLP（C++03 无 atomic，编译器/CPU 会重排）—— 仅作反例，不要写 加分： 几乎永远选 1 。call_once 跨 DLL 边界更稳；DCLP 仅在性能极敏感场合且需自己管析构时考虑。能讲出 SIOF (Static Init Order Fiasco) 与 Meyers Singleton 的关系再加分。 基础 什么是 RAII? 举 3 个例子 答 ： Resource Acquisition Is Initialization — 把资源生命周期绑定到栈对象生命周期：构造函数获取资源，析构函数释放资源。任何路径（含异常）下析构都会被调用，保证不泄漏。 std::unique_ptr / shared_ptr — 管理堆内存 std::lock_guard / scoped_lock — 管理 mutex std::fstream / jthread — 管理文件 / 线程 join 加分：能说出\" RAII 是 C++ 优于 GC 语言的根本之一 — 资源释放点确定 + 异常安全免费\"。Java try-with-resources / Python with、Rust Drop 都是模仿品。 基础 PIMPL 解决什么问题? 代价? 答 ：Pointer to IMPLementation — 把私有成员藏到 .cpp 里的 Impl 类，头文件只持有 std::unique_ptr<Impl> 。 解决 ：① 编译防火墙 （改私有成员不触发用户重编译）；② 二进制兼容 ABI （私有数据升级不破 ABI）；③ 隐藏依赖 （Impl 用到的 third-party 头不暴露）。 代价：① 每次访问 多一次堆间接 + cache miss；② 额外堆分配；③ 析构必须放 .cpp（unique_ptr 需 complete type），构造、拷贝、move 也常要外置 — 写起来啰嗦。 加分：Qt 全家桶用 PIMPL（ Q_D / Q_Q 宏自动生成）；Windows COM 接口本质也是 PIMPL 思想。 基础 CRTP 是什么? 用一个例子说明 答 ： Curiously Recurring Template Pattern — 派生类把自己作为模板参数传给基类： Derived : Base<Derived> 。基类编译期就知道派生类型 → 用 static_cast<Derived*>(this) 静态分发， 无 vptr / 无堆 / 零开销 。 template < class D> struct Shape { double area () const { return static_cast < const D*>( this )-> area_impl (); // 编译期分发 } }; struct Circle : Shape<Circle> { double r; double area_impl () const { return 3.14 * r * r; } }; 典型用途：① 静态多态（性能敏感场景）；② 给派生类注入功能 (mixin)；③ 标准库 std::enable_shared_from_this 。 基础 Strategy 模式在现代 C++ 怎么实现? 答 ：4 种方案，按现代化程度排序： 方案 开销 切换时机 适用 虚函数 + 抽象基类（GoF 原版） vptr + 可能堆 运行时 经典 OOP，类型多 std::function 成员 type erasure + 可能堆 运行时 单一回调，写得最简洁 模板参数 （policy-based design） 零开销 （编译期内联） 编译期 性能敏感（STL sort 的 cmp） std::variant<S1, S2, ...> + visit 内联 + 无堆 运行时 有限类型集合 加分：标准库 std::sort(it, it, cmp) 中的 cmp 就是 Strategy（模板参数版）；STL 容器的 Allocator 也是 policy-based Strategy。能讲出\"用 lambda + std::function 替代 90% 的 GoF Strategy 模板代码\"。"},{"a":"slide-27-2","h":"中级（拉开差距）","x":"中级（拉开差距） 中级 为什么 Meyers Singleton 在 C++11 后是首选? 答 ：C++11 标准 [stmt.dcl]/4 正式规定：函数内的 static 局部变量初始化是 线程安全 的（其他线程必须等待初始化完成）—— 编译器用类似 once_flag 的机制实现。 零代码 —— 一行 static Singleton inst; 完事 零开销 —— 初始化后访问就是普通函数调用（编译器一次 acquire load 即可） 延迟初始化 —— 第一次调用 get() 才构造 析构有序 —— 程序退出时按构造逆序析构 加分：知道 C++03 时代 Meyers Singleton 不是 thread-safe，需 DCLP 兜底；能讲 SIOF （跨翻译单元的全局 static 构造顺序不定）—— Meyers Singleton 用函数局部 static 自动规避。 中级 std::function 怎么实现的? (type erasure) 答 ：核心三件套： 抽象基类 ： struct Base { virtual R call(A...) = 0; virtual ~Base(); }; 模板派生包装 ： template<class F> struct Impl : Base { F f; R call(A... a) override { return f(a...); } }; 父指针擦除 ： std::unique_ptr<Base> p_; 通过 base 调 virtual 优化 ： SBO (Small Buffer Optimization) —— callable ≤ 16-24 字节直接存内部 buffer 不堆分配；高性能实现用 manual vtable（函数指针数组）代替 virtual 便于内联。 加分：指出 std::function 拷贝可能 抛 bad_alloc （大 lambda 时）；C++23 move_only_function / function_ref 是更轻量替代（不要求拷贝）。 中级 copy-and-swap 4 个好处? Widget& operator =(Widget o) { // 值传参 → 已拷贝/移动 swap (o); // noexcept return * this ; } 代码少 — 一份 copy ctor 自动派生 copy assign 强异常安全 — 拷贝在参数构造时若抛， *this 状态未变；swap 是 noexcept 不再抛 自赋值天然正确 — x = x 先拷贝再 swap，无需 if (&o == this) 统一处理 copy/move — 值传参，左值走 copy、右值走 move， 一份 operator= 同时支持两种 缺点：每次赋值强制构造临时对象 → move-only 类型或赋值极热路径应用经典 if-self-check 写法。 中级 variant + visit vs 虚函数 怎么选? virtual variant + visit 类型集合 开放 （外部可继承） 封闭 （编译期固定） 内存 vptr + 堆 栈上 / 嵌入对象 分发 vtable lookup（不易内联） switch/jump table（常可内联） 加类型 新子类，不动基类（OCP） 改 variant + 所有 visitor 加操作 改基类 + 所有子类 新增一个 visitor Expression Problem 法则 ：类型频繁扩、操作稳定 → virtual；操作频繁扩、类型稳定 → variant + visit。AST / 状态机 / 消息处理是 variant 天然场景。 中级 设计一个 LRU Cache (list + unordered_map) template < class K, class V> class LRU { size_t cap_; std::list<std::pair<K, V>> lst_; // 头=最新, 尾=最旧 std::unordered_map<K, typename decltype (lst_)::iterator> map_; public: explicit LRU ( size_t cap) : cap_(cap) {} std::optional<V> get ( const K& k) { auto it = map_. find (k); if (it == map_. end ()) return std::nullopt; lst_. splice (lst_. begin (), lst_, it->second); // 移到头 O(1) return it->second->second; } void put (K k, V v) { if ( auto it = map_. find (k); it != map_. end ()) { it->second->second = std:: move (v); lst_. splice (lst_. begin (), lst_, it->second); return ; } if (lst_. size () == cap_) { map_. erase (lst_. back ().first); // 淘汰尾 lst_. pop_back (); } lst_. emplace_front (k, std:: move (v)); map_[std:: move (k)] = lst_. begin (); } }; 关键点：① get/put 都 O(1) ；② splice 不让 map 里的 iterator 失效 （list 杀手锏，vector 做不到）；③ 不要用 erase+push_front — 那会导致 iterator 失效，必须重新插入到 map。"},{"a":"slide-27-3","h":"进阶（让面试官眼前一亮）","x":"进阶（让面试官眼前一亮） 高级 简化版 Object Pool (含自动归还) template < class T> class Pool { std::vector<std::unique_ptr<T>> idle_; std::mutex mu_; public: using Handle = std::unique_ptr<T, std::function< void (T*)>>; Handle acquire () { std::lock_guard lk(mu_); std::unique_ptr<T> p = idle_. empty () ? std:: make_unique <T>() : std:: move (idle_. back ()); if (!idle_. empty ()) idle_. pop_back (); return Handle(p. release (), [ this ](T* raw){ // 自定义 deleter = 归还 std::lock_guard lk(mu_); idle_. emplace_back (raw); }); } }; 加分：① deleter 闭包 让 unique_ptr 出作用域自动归还，使用者无感；② 生产环境用 shared_ptr<T>(raw, deleter) 避免 function 类型擦除开销；③ Pool 必须长于 Handle，工业级用 weak_ptr 检查 Pool 还在。 高级 BoundedQueue (Producer-Consumer) template < class T> class BoundedQueue { std::queue<T> q_; size_t cap_; std::mutex mu_; std::condition_variable not_full_, not_empty_; public: void push (T x) { std::unique_lock lk(mu_); not_full_. wait (lk, [&]{ return q_. size () < cap_; }); q_. push (std:: move (x)); lk. unlock (); not_empty_. notify_one (); } T pop () { std::unique_lock lk(mu_); not_empty_. wait (lk, [&]{ return !q_. empty (); }); T v = std:: move (q_. front ()); q_. pop (); lk. unlock (); not_full_. notify_one (); return v; } }; 要点：① 两个 condvar （分别管空/满），避免\"消费者唤醒消费者\"无效轮询；② 谓词版 wait 自动处理 spurious wake；③ notify 在 unlock 之后 ，避免被唤醒线程立刻又陷入锁等待；④ 进阶： std::stop_token + wait_until 支持优雅 shutdown。 高级 Active Object 异步消息驱动 答 ：把方法调用变成消息塞进队列，由对象自己的线程 串行 执行 → 调用者无锁、对象内部无锁。 class ActiveObject { BoundedQueue<std::function< void ()>> q_; std::jthread worker_; public: ActiveObject () : q_( 1024 ), worker_([ this ](std::stop_token st){ while (!st. stop_requested ()) q_. pop ()(); // 阻塞取 + 执行 }) {} template < class F> auto submit (F&& f) { auto t = std:: make_shared <std::packaged_task< decltype ( f ())()>>(std:: forward <F>(f)); auto fut = t-> get_future (); q_. push ([t]{ (*t)(); }); return fut; } }; 工程对应：Erlang Actor / Java AKKA / C# Dispatcher / ROS callback queue。优势： 对象状态天然线程安全 （单线程串行）；劣势： 单线程瓶颈 ，工业级需 sharding 多个 ActiveObject 分流。 高级 type erasure 简化版 std::function template < class Sig> class Func; template < class R, class ... A> class Func<R(A...)> { struct Base { virtual R call (A...) = 0 ; virtual ~ Base () = default ; }; template < class F> struct Impl : Base { F f; Impl (F x) : f(std:: move (x)) {} R call (A... a) override { return f (std:: forward <A>(a)...); } }; std::unique_ptr<Base> p_; public: template < class F> Func (F f) : p_(std:: make_unique <Impl<F>>(std:: move (f))) {} R operator ()(A... a) { return p_-> call (std:: forward <A>(a)...); } }; 加分：① 三要素 = 抽象基类 / 模板派生包装 / 父指针擦除 ；② 加 bool empty() 状态；③ SBO： union{ aligned_storage; Base*; } + flag，< 24 字节内联；④ 拷贝需 virtual unique_ptr<Base> clone() —— 这正是 std::function 比 move_only_function 慢的原因。 高级 CRTP vs 虚函数 — 什么时候用哪个? 维度 CRTP（静态多态） virtual（动态多态） 派生类型决定时机 编译期 运行期 调用开销 零（可完全内联） vtable lookup（间接调用） 异构容器存多种 ❌ 模板实例化是不同类型 ✅ 同一基类指针 ABI 稳定 / 库边界 ❌ 模板每次实例化 ✅ 二进制兼容好 编译时间 / 错误信息 慢 / 长 / 难懂 快 / 清晰 CRTP 优于虚函数 ：性能极敏感（STL 算法策略、Eigen 表达式模板）、静态多态足够、需要给派生类 注入功能 （mixin，如 enable_shared_from_this ）。 虚函数优于 CRTP ：需要异构容器（如 vector<Shape*> ）、类型由配置/运行时决定（plugin / 工厂）、库 ABI 稳定优先。 加分：\" Static Polymorphism vs Dynamic Polymorphism 是 C++ 独有的二元设计选择 ，其他大多数 OOP 语言只有后者\"。Eigen / Boost.Asio executor / Ranges adaptor 全部基于 CRTP 静态多态。"},{"a":"slide-27-4","h":"速答示范（第 4 题）","x":"速答示范（第 4 题） 问 ：CRTP 是什么? 例子? 答 ：CRTP = Curiously Recurring Template Pattern。派生类 把自己作为模板参数 传给基类： Derived : Base<Derived> 。基类编译期就知道派生类型 → 用 static_cast<Derived*>(this) 静态分发，无虚函数 / 无 vptr / 零开销 。 典型用途：① 静态多态；② 给派生类注入功能 (mixin)；③ 标准库 std::enable_shared_from_this 。 背完 15 题 + 能即兴写出 LRU / BoundedQueue / 简化版 std::function → C++ 设计模式章节面试 95% 安全。完整 15 题速答详解见下一张 slide。"},{"a":"end","h":"本专题结束 · 下一步","x":"29 本专题结束 · 下一步 设计模式是 语言能力 + 工程经验 的结晶。现代 C++ 让很多 GoF 模式失去存在感 — 但 Singleton / RAII / PIMPL / CRTP / Type Erasure / Visitor 仍是面试和工程的硬通货。 同期资料 cpp_value_categories_guide.html — 拷贝控制 (Rule of 5 配套) cpp_exceptions_guide.html — RAII / Scope Guard 来源 cpp_modern_features_ppt.html — variant / visit / lambda / CTAD 下一份: 多线程编程模式 长文 建议练习 实现 5 种 Singleton (Meyers / call_once / DCLP / atomic / function-static) 把项目中的 dynamic_cast 换成 variant + visit 写一个 PIMPL 类 + 量化编译时间收益 用 CRTP 实现一个 EventEmitter mixin 用 type erasure 实现一个简化版 std::any ← 上一篇 · 智能指针 相关 · 算法 相关 · 容器 相关 · 模板与 TMP 目录 主页 C++ 设计模式 — 用现代 C++ 重新表达的模式与惯用法 — 2026 ↑"}]},{"u":"stl/stl_algorithms_ppt.html","t":"C++ STL 算法深度 PPT","c":"现代 C++ / STL","s":[{"a":"","h":"","x":"C++ STL 算法深度 PPT — 排序 / 二分 / 堆 / 数值 / Ranges Kavin Tech Notes ← 主页 目录 30 min ◐ STL 算法 · Roadmap STL 算法全景与命名规则 排序家族 ① — sort 内部 introsort 排序家族 ② — stable_sort / partial_sort / nth_element 二分查找族 — lower/upper/equal_range/binary_search 堆算法 — make_heap / push_heap / pop_heap 修改型算法 + erase-remove 惯用法 划分算法 — partition / stable_partition / partition_point 集合操作 — set_union / intersection / difference / merge 数值算法 — accumulate / reduce / inner_product / iota 函数对象 + 谓词 + 内置 functors C++20 Ranges — 算法的现代写法 并行执行策略 — par / par_unseq / unseq 非修改型 — for_each / find / count / *_of 算法复杂度速查表 真实工程 · 算法组合实战 高频面试题（15 题 · 可折叠） 本专题结束 · 下一步 STL Algorithms · 2026 STL 算法深度剖析 「容器只是仓库， 算法 才是工具」 — 系统掌握 std 算法 + 函数对象 + C++20 ranges，覆盖 90% 工程场景与面试题。 核心难度 ★★★★☆ 面试出现率 高 阅读时长 ~30 min 前置 STL 容器 · 迭代器 17 大章节 排序 · 查找 · 堆 · 数值 C++20 Ranges · 并行策略 单文件离线可读"},{"a":"slide-01","h":"STL 算法全景与命名规则","x":"01 STL 算法全景与命名规则 先建立分类心智模型，再深入每族细节。 ① 八大算法族 非修改型 ：find / count / for_each / all_of / any_of / mismatch 修改型 ：copy / move / transform / replace / fill / generate / reverse / rotate 划分型 ：partition / stable_partition / partition_point 排序 ：sort / stable_sort / partial_sort / nth_element / is_sorted 二分查找 ：binary_search / lower_bound / upper_bound / equal_range 合并/集合 ：merge / inplace_merge / set_union / set_intersection / set_difference 堆 ：make_heap / push_heap / pop_heap / sort_heap 数值 ：accumulate / reduce(C++17) / inner_product / partial_sum / iota ② 命名后缀规则（重要） 后缀 含义 例子 无 原地 sort , reverse _if 谓词版本 find_if , count_if , copy_if _n 给个数 fill_n , copy_n , for_each_n _copy 不改原序列，写到新位置 remove_copy , replace_copy stable_ 保持等价元素相对顺序 stable_sort , stable_partition partial_ 只处理部分 partial_sort , partial_sum r 反向 copy_backward 记忆这张表 = 70% 算法你看到名字就能猜功能 std:: find (b, e, v); // 找等于 v 的 std:: find_if (b, e, pred); // 找满足谓词的 std:: copy (b, e, dst); // 拷贝到 dst std:: copy_if (b, e, dst, pred); // 满足谓词才拷贝 std:: copy_n (b, n, dst); // 拷贝前 n 个 std:: remove_copy (b, e, dst, v); // 拷贝过程中跳过 == v 的 面试可问\"如果想统计 v 中大于 0 的元素个数有几种写法?\" — 答 count_if(v.begin(), v.end(), [](int x){return x>0;}) 或 std::ranges::count_if(v, [](int x){return x>0;}) 或 std::accumulate(v.begin(), v.end(), 0, [](int s, int x){return x>0?s+1:s;}) 。能讲出哪种「最清晰、最快、最通用」即加分。"},{"a":"slide-02","h":"排序家族 ① — sort 内部 introsort","x":"02 排序家族 ① — sort 内部 introsort 用法 · 严格弱序的陷阱（高频面试题） std::sort 用什么算法? 不是单一算法 — 是 introsort（内省排序） ：3 个算法的混合体。 开始： quicksort （分治 + pivot） 递归深度 > 2 * log2(N) ：切到 heapsort （防最坏 O(n²)） 分段 < 16 元素：用 insertion sort （小数组缓存友好） 结果：最坏 O(n log n) ，常数因子小，几乎是「最优通用排序」。 关键约束 需要 随机访问迭代器 （vector/array/deque/string） 不稳定 （等价元素的相对顺序可能改变） 比较器必须满足 严格弱序 （strict weak ordering）—— 违反就是 UB 用法 std::vector< int > v{3,1,4,1,5,9,2,6}; std:: sort (v. begin (), v. end ()); // 默认升序 std:: sort (v. begin (), v. end (), std::greater<>{}); // 降序 std::vector< User > users; std:: sort (users. begin (), users. end (), []( const User & a, const User & b){ return a.score > b.score; }); // C++20 ranges 风格 —— 推荐 std::ranges:: sort (v); std::ranges:: sort (users, std::greater<>{}, & User ::score); // 投影! 严格弱序的陷阱（高频面试题） // ❌ 错误：浮点 NaN 破坏严格弱序，UB std::vector< double > v{1.0, 2.0, std::nan( \"\" ), 3.0}; std:: sort (v. begin (), v. end ()); // UB! NaN 与任何数比较都返回 false // ❌ 错误：<= 不是严格弱序 std:: sort (v. begin (), v. end (), []( int a, int b){ return a <= b; }); // UB // ✅ 正确：必须是 < std:: sort (v. begin (), v. end (), []( int a, int b){ return a < b; }); 严格弱序的 4 条规则 （comp(a,b) 表示比较结果）：① 反自反 ：comp(a,a) 必为 false；② 非对称 ：comp(a,b) 与 comp(b,a) 不能同时 true；③ 传递 ：comp(a,b) ∧ comp(b,c) ⇒ comp(a,c)；④ 等价传递 ：!comp(a,b) ∧ !comp(b,a) 视作等价，等价关系传递。违反任一条都是 UB（部分 libstdc++ 版本会真的崩溃）。 面试题\"为什么 sort 是 introsort 而不是纯 quicksort?\" — 答：纯 quicksort 最坏 O(n²)，恶意输入或已排序数据触发；introsort 用递归深度阈值检测后切换 heapsort 防御， 合并三种算法的优点 。"},{"a":"slide-03","h":"排序家族 ② — stable_sort / partial_sort / nth_element","x":"03 排序家族 ② — stable_sort / partial_sort / nth_element 三个高频场景 · REAL实际工程：日志的 P99 延迟统计 函数 复杂度 稳定? 典型场景 sort O(n log n) 否 通用排序 stable_sort O(n log n) [若内存够] / O(n log² n) 是 多关键字排序 partial_sort O(n log k) 否 Top-K 元素 nth_element O(n) 平均 否 找第 k 大 / 中位数 partial_sort_copy O(n log k) 否 不修改原数组的 Top-K is_sorted O(n) — 校验 三个高频场景 // ① stable_sort - 多关键字排序的关键 // 先按 score 降序，相同分数按 name 升序 std:: stable_sort (users. begin (), users. end (), []( auto & a, auto & b){ return a.name < b.name; }); std:: stable_sort (users. begin (), users. end (), []( auto & a, auto & b){ return a.score > b.score; }); // 主键，因稳定，name 顺序保留 // ② partial_sort - Top-K 元素到前面 std:: partial_sort (v. begin (), v. begin () + 10, v. end ()); // 前 10 个就是最小的 10 个（已排序）；后面顺序未定 // ③ nth_element - 找第 k 小（O(n) 平均，比 sort 快） auto mid = v. begin () + v. size ()/2; std:: nth_element (v. begin (), mid, v. end ()); // 现在 *mid 是中位数；mid 左边都 ≤ *mid，右边都 ≥ *mid（左右内部不排序） // 找第 k 小元素 std:: nth_element (v. begin (), v. begin () + k, v. end ()); int kth = v[k]; REAL 实际工程：日志的 P99 延迟统计 // 错误做法：sort 全部 + 取 0.99*N 索引 → O(n log n) // ✅ 正确：nth_element O(n) double percentile (std::vector< double >& samples, double p) { auto idx = size_t (p * samples. size ()); std:: nth_element (samples. begin (), samples. begin () + idx, samples. end ()); return samples[idx]; } double p99 = percentile (latency_us, 0.99); // 100M 样本: ~80ms vs sort ~1500ms \"如果只想要 Top-100 元素，怎么最快?\" — 答：① partial_sort （O(n log 100)）；② 维护大小为 100 的 最小堆 遍历输入（O(n log 100) 但常数更小）；③ nth_element 找出第 100 大再 sort 前 100（O(n + 100 log 100)，最快）。三种各有适用。"},{"a":"slide-04","h":"二分查找族 — lower/upper/equal_range/binary_search","x":"04 二分查找族 — lower/upper/equal_range/binary_search 前提：序列已排序（按比较器排）。否则结果未定义。 函数 含义 返回 复杂度 binary_search 是否存在 bool O(log n) lower_bound(b,e,v) 第一个 ≥ v 的位置 iterator O(log n) upper_bound(b,e,v) 第一个 > v 的位置 iterator O(log n) equal_range(b,e,v) {lower, upper} 一对 pair<it,it> O(log n) 图解 1 3 3 3 5 7 9 lower_bound(3) 第一个 ≥ 3 upper_bound(3) 第一个 > 3 equal_range(3) = [lower, upper) — 三个 3 典型用法 std::vector< int > v{1, 3, 3, 3, 5, 7, 9}; // 1. 是否存在 bool has = std:: binary_search (v. begin (), v. end (), 3); // true // 2. 在已排序 vector 中插入并保持有序 auto pos = std:: lower_bound (v. begin (), v. end (), 4); v. insert (pos, 4); // 3. 计算等于 v 的元素个数 auto [lo, hi] = std:: equal_range (v. begin (), v. end (), 3); auto count = hi - lo; // O(log n)，比 std::count O(n) 快 陷阱 ：① lower_bound 返回的是 插入位置 ，不一定指向 v；要先判断 it != end && *it == v 才算找到；② 在 std::list 上调用是 O(n)（迭代器只能 ++/--，二分跳转退化成线性扫描）；③ std::set::find() 自动 O(log n)， 不要 对 set 用 std::find （O(n)）。 面试问\"map 和 lower_bound 哪个查找快?\" — 答：都是 O(log n)；实际差异是 vector + lower_bound 缓存友好得多 ，N 不太大（< 10^5）时常优于 std::map。"},{"a":"slide-05","h":"堆算法 — make_heap / push_heap / pop_heap","x":"05 堆算法 — make_heap / push_heap / pop_heap 典型流程 · REAL实战：固定大小 Top-K（流式） API make_heap(b, e) — 把 [b,e) 整体调整为堆 (O(n)) push_heap(b, e) — 调用前已 push_back 新元素到末尾，调整上浮 (O(log n)) pop_heap(b, e) — 把堆顶 swap 到末尾，调整下沉 (O(log n)) sort_heap(b, e) — 把堆变成升序数组 (O(n log n)) is_heap(b, e) — 校验 默认 大顶堆 ，传 std::greater<>{} 变小顶堆。 为什么不直接用 priority_queue? priority_queue 没有迭代器 ，无法遍历或随机访问 priority_queue 只能 push 单元素， 不能批量 build 用 vector + std::*_heap 你能： ① 一次性 make_heap O(n)（vs N 次 push 是 O(n log n)） ② 仍能遍历底层 vector ③ 切换比较器更灵活 典型流程 std::vector< int > v{3, 1, 4, 1, 5, 9, 2, 6}; std:: make_heap (v. begin (), v. end ()); // O(n) — 整体建堆 v. push_back (8); // 先放末尾 std:: push_heap (v. begin (), v. end ()); // 再调整上浮 std:: pop_heap (v. begin (), v. end ()); // 堆顶移到末尾 int top = v. back (); v. pop_back (); // 真正移除 REAL 实战：固定大小 Top-K（流式） // 维护 K 个最大元素：只需大小为 K 的 小顶堆 class TopK { std::vector< int64_t > h_; size_t k_; public : explicit TopK ( size_t k) : k_(k) { h_. reserve (k); } void push ( int64_t x) { if (h_. size () < k_) { h_. push_back (x); std:: push_heap (h_. begin (), h_. end (), std::greater<>{}); } else if (x > h_. front ()) { // 比当前最小大才进入 std:: pop_heap (h_. begin (), h_. end (), std::greater<>{}); h_. back () = x; std:: push_heap (h_. begin (), h_. end (), std::greater<>{}); } } std::vector< int64_t > snapshot () const { return h_; } // 顺序未定但都是 top-k }; // 100M 流入，K=100：复杂度 O(n log k)，内存 O(k) \"为什么 make_heap 是 O(n) 而 N 次 push_heap 是 O(n log n)?\" — 答：从底向上 sift-down 时，大多数节点接近叶子（高度小），按 Σ h*N(h) 求和 = O(n)；而 push_heap 是从叶子向上 sift-up，每次都到根 O(log n)。这是经典分析题。"},{"a":"slide-06","h":"修改型算法 + erase-remove 惯用法","x":"06 修改型算法 + erase-remove 惯用法 erase-remove 惯用法 · unique 的正确用法 · transform — 一对一映射 常用修改型算法 copy / copy_if / copy_n / copy_backward move / move_backward — 用移动语义版本 transform — 一对一/二对一映射 fill / fill_n / generate / generate_n replace / replace_if remove / remove_if — 不真正删除 ，要配 erase unique — 去除 相邻 重复（先 sort 才能全去重） reverse / rotate / shuffle 关键陷阱：remove 不删除！ std::remove 把\"要删的元素\"的位置覆盖为后续的非删元素， 返回新逻辑末尾 。容器实际 size 不变。 必须配合 container.erase(new_end, container.end()) 才真正缩小。 erase-remove 惯用法 std::vector< int > v{1, 2, 3, 2, 4, 2, 5}; // ❌ 错误：v 大小没变，末尾留有未定义残留 std:: remove (v. begin (), v. end (), 2); // v = {1, 3, 4, 5, ?, ?, ?} size 仍是 7 // ✅ 正确：erase-remove 惯用法 v. erase (std:: remove (v. begin (), v. end (), 2), v. end ()); // v = {1, 3, 4, 5} // ✅ C++20 一行 std:: erase (v, 2); std:: erase_if (v, []( int x){ return x % 2 == 0; }); unique 的正确用法 // unique 只移除\"相邻\"重复 — 不先排序则无效 std::vector< int > v{1, 2, 2, 3, 2, 1, 1}; v. erase (std:: unique (v. begin (), v. end ()), v. end ()); // v = {1, 2, 3, 2, 1} 仅移除连续重复 // 全去重：先 sort + unique std:: sort (v. begin (), v. end ()); v. erase (std:: unique (v. begin (), v. end ()), v. end ()); // v = {1, 2, 3} transform — 一对一映射 std::vector< int > src{1, 2, 3, 4}; std::vector< int > dst(src. size ()); std:: transform (src. begin (), src. end (), dst. begin (), []( int x){ return x * x; }); // dst = {1, 4, 9, 16} // 二元 transform: 两个序列对应位置运算 std::vector< int > a{1,2,3}, b{10,20,30}, c(3); std:: transform (a. begin (), a. end (), b. begin (), c. begin (), std::plus<>{}); // c = {11, 22, 33} erase-remove 是 C++03 时代的\"老惯用法\"，C++20 起优先用 std::erase / std::erase_if ，更短更易读。"},{"a":"slide-07","h":"划分算法 — partition / stable_partition / partition_point","x":"07 划分算法 — partition / stable_partition / partition_point 典型用法 · REAL实战：连接表中分离活跃/空闲连接 API partition(b, e, pred) — 把满足谓词的放前面， 不稳定 O(n) stable_partition — 同上但保持相对顺序，O(n) 若额外内存够 partition_point(b, e, pred) — 在已划分序列中找分界点（O(log n)） is_partitioned — 检查是否已划分 为什么有用 quicksort 的 pivot 步骤就是 partition nth_element 内部反复 partition 直到 pivot 落在第 k 位 过滤 + 保留两种元素到不同区域 典型用法 std::vector< int > v{1, 5, 2, 8, 3, 9, 4}; // 把奇数放前，偶数放后 auto pivot = std:: partition (v. begin (), v. end (), []( int x){ return x % 2 == 1; }); // v 可能 = {1, 5, 9, 3, 8, 2, 4} 奇数 [begin, pivot)，偶数 [pivot, end) // stable_partition 保持原顺序 std::vector< int > v2{1, 5, 2, 8, 3, 9, 4}; std:: stable_partition (v2. begin (), v2. end (), []( int x){ return x % 2 == 1; }); // v2 = {1, 5, 3, 9, 2, 8, 4} 奇数和偶数内部相对顺序保留 REAL 实战：连接表中分离活跃/空闲连接 std::vector< Connection *> conns; // 把活跃连接放前面，空闲放后面（稳定划分保持优先级顺序） auto idle_begin = std:: stable_partition (conns. begin (), conns. end (), []( const Connection * c){ return c-> is_active (); }); // 现在： // [conns.begin(), idle_begin) = 活跃 // [idle_begin, conns.end()) = 空闲（可批量回收） std:: for_each (idle_begin, conns. end (), []( Connection * c){ c-> close (); }); conns. erase (idle_begin, conns. end ()); partition_point 之于 partitioned 序列 = lower_bound 之于 sorted 序列：都是 O(log n) 找分界点。"},{"a":"slide-08","h":"集合操作 — set_union / intersection / difference / merge","x":"08 集合操作 — set_union / intersection / difference / merge 用法 · REAL实战：增量配置 diff API merge(a1,a2,b1,b2,dst) — 合并两个已排序序列 inplace_merge(b, mid, e) — 把 [b,mid) 与 [mid,e) 合并为有序（原地） set_union — 并集 set_intersection — 交集 set_difference — 差集 (A \\ B) set_symmetric_difference — 对称差 (A △ B) includes — A ⊇ B 子集判定 共同前提 所有输入都 必须按同一比较器排序 结果也按该比较器有序 都是 O(n + m) 线性 不要求输入是 set 容器， 排序好的 vector 也行 用法 std::vector< int > a{1, 3, 5, 7}; std::vector< int > b{2, 3, 4, 7}; std::vector< int > out; std:: set_union (a. begin (), a. end (), b. begin (), b. end (), std::back_inserter(out)); // out = {1, 2, 3, 4, 5, 7} out. clear (); std:: set_intersection (a. begin (), a. end (), b. begin (), b. end (), std::back_inserter(out)); // out = {3, 7} out. clear (); std:: set_difference (a. begin (), a. end (), b. begin (), b. end (), std::back_inserter(out)); // out = {1, 5} (a 中有但 b 中没有) REAL 实战：增量配置 diff // 旧/新配置 - 已排序的 yang xpath 列表 std::vector<std::string> old_paths, new_paths; std::vector<std::string> added, removed; std:: set_difference (new_paths. begin (), new_paths. end (), old_paths. begin (), old_paths. end (), std::back_inserter(added)); // 新增的 std:: set_difference (old_paths. begin (), old_paths. end (), new_paths. begin (), new_paths. end (), std::back_inserter(removed)); // 删除的 // O(n+m) 替代两层循环 O(n*m) 的 diff 能讲出\"set_* 算法不要求容器是 std::set，已排序 vector 也能用 — 而且更快（缓存友好）\" 即加分。"},{"a":"slide-09","h":"数值算法 — accumulate / reduce / inner_product / iota","x":"09 数值算法 — accumulate / reduce / inner_product / iota accumulate vs reduce（高频面试题） · 实用模式 · REAL实战：监控指标聚合 函数 语义 头文件 accumulate(b,e,init,op) 左折叠 fold-left（顺序求值） <numeric> reduce(b,e,init,op) (C++17) 无序折叠（可并行 + 向量化） <numeric> transform_reduce (C++17) map + reduce 组合 <numeric> inner_product(b1,e1,b2,init) 点积 / 一一配对操作 <numeric> partial_sum / inclusive_scan / exclusive_scan 前缀和 <numeric> adjacent_difference 相邻差分 <numeric> iota(b,e,start) 填充递增整数 <numeric> accumulate vs reduce（高频面试题） std::vector< double > v(1'000'000); // 顺序求和 - 必须按从左到右顺序（浮点不满足结合律） double s1 = std:: accumulate (v. begin (), v. end (), 0.0); // 无序求和 - 允许并行，但需要 op 满足结合律 double s2 = std:: reduce (std::execution::par_unseq, v. begin (), v. end (), 0.0); // 浮点 reduce 结果可能与 accumulate 略有不同（求和顺序不同） 陷阱 ： accumulate 默认 init 类型决定结果类型！ std::accumulate(v.begin(), v.end(), 0 ) 结果是 int ，浮点数会被截断！ 必须 std::accumulate(v.begin(), v.end(), 0.0 ) 或 0LL 。 实用模式 // 平均值 double avg = std:: reduce (v. begin (), v. end (), 0.0) / v. size (); // 平方和（map+reduce） double ss = std:: transform_reduce (v. begin (), v. end (), 0.0, std::plus<>{}, []( double x){ return x * x; }); // 字符串拼接 std::vector<std::string> ws{ \"hello\" , \"world\" }; std::string joined = std:: accumulate (ws. begin (), ws. end (), std::string{}, [](std::string acc, const std::string& w){ return acc + (acc. empty ()? \"\" : \",\" ) + w; }); // 前缀和 std::vector< int > sums(v. size ()); std:: partial_sum (v. begin (), v. end (), sums. begin ()); // iota - 测试数据快速生成 / 索引序列 std::vector< int > idx(100); std:: iota (idx. begin (), idx. end (), 0); // 0..99 REAL 实战：监控指标聚合 // QPS 窗口统计 struct Sample { int64_t ts_us; double latency_us; uint32_t bytes; }; auto total_bytes = std:: transform_reduce ( std::execution::par_unseq, win. begin (), win. end (), uint64_t {0}, std::plus<>{}, []( const Sample & s){ return s.bytes; }); auto avg_lat = std:: transform_reduce ( win. begin (), win. end (), 0.0, std::plus<>{}, []( const Sample & s){ return s.latency_us; }) / win. size ();"},{"a":"slide-10","h":"函数对象 + 谓词 + 内置 functors","x":"10 函数对象 + 谓词 + 内置 functors 常见用法 · 自定义 functor 何时优于 lambda 什么是函数对象 定义了 operator() 的类型。可 带状态 ，可被模板内联，性能往往优于 std::function 包装。 STL 算法接受任意可调用对象：函数指针、lambda、bind 结果、自定义 functor。 STL 内置 functors（<functional>） 类型 类 算术 plus / minus / multiplies / divides / modulus / negate 比较 equal_to / not_equal_to / less / greater / less_equal / greater_equal 逻辑 logical_and / logical_or / logical_not 位运算 bit_and / bit_or / bit_xor / bit_not 常见用法 // 默认升序：等价于 std::sort(b, e, std::less<>{}) std:: sort (v. begin (), v. end (), std::greater<>{}); // C++14 透明 functor (无类型参数) // transform 求两序列差 std:: transform (a. begin (), a. end (), b. begin (), out. begin (), std::minus<>{}); // 组合：std::not_fn 否定一个谓词 auto is_neg = []( int x){ return x < 0; }; std:: count_if (v. begin (), v. end (), std:: not_fn (is_neg)); // 非负个数 // std::ref / std::cref - 把引用传给值语义参数 int counter = 0; std:: for_each (v. begin (), v. end (), [ c =std::ref(counter)]( int ){ ++c. get (); }); 自定义 functor 何时优于 lambda // 1. 需要在多个地方复用 struct DivisibleBy { int n; bool operator ()( int x) const { return x % n == 0; } }; std:: count_if (v. begin (), v. end (), DivisibleBy {3}); // 2. 需要类型可见 (用于模板特化、is_*_v traits) std::set< User , UserCompareByScore > rank; // lambda 类型每次都不同，不便于此 // 3. 需要继承/接口实现 struct EvenFilter : public Filter { bool operator ()( int x) const override { return x % 2 == 0; } }; \"lambda 与 functor 谁性能好?\" — 答：通常 等价 （lambda 实质是匿名 functor）。但 lambda 类型不可写出，无法用于需要类型名的 API（map 比较器、模板特化）。"},{"a":"slide-11","h":"C++20 Ranges — 算法的现代写法","x":"11 C++20 Ranges — 算法的现代写法 新写法 vs 旧写法 · 常用 views 三大改进 不必传 begin/end — 直接传容器（ std::ranges::sort(v) ） 投影 (projection) — 排序/查找时指定对象的某个字段 视图组合（view） — 用 | 管道惰性变换 错误信息变好 每个算法附带 concepts 约束 → 类型不匹配立刻一行报错（vs 旧 STL 几屏 SFINAE 噩梦）。 新写法 vs 旧写法 // 旧 std:: sort (v. begin (), v. end ()); std:: sort (users. begin (), users. end (), []( const User & a, const User & b){ return a.score > b.score; }); // 新 - 简洁 + 投影 std::ranges:: sort (v); std::ranges:: sort (users, std::greater<>{}, & User ::score); // 第3参=投影 // 找特定 user auto it = std::ranges:: find (users, 1001, & User ::id); if (it != users. end ()) {} // 配合视图：filter + transform + take 惰性求值 namespace rv = std::views; auto top3 = users | rv:: filter ([]( const User & u){ return u.active; }) | rv:: transform ([]( const User & u){ return u.score; }) | rv:: take (3); for ( auto s : top3) /* ... */ ; 常用 views view 作用 views::filter(pred) 过滤 views::transform(fn) 映射 views::take(n) / drop(n) 取前/丢前 n 个 views::reverse 反向 views::keys / values map 取 key/value views::split(d) 按分隔符切分 views::join 把多个序列拼成一个 views::iota(0) 无穷整数序列 views::zip(a, b) (C++23) 两序列配对 所有 views 惰性 ：不消耗 CPU/内存直到真正遍历；filter+transform+take 编译器可优化为单次循环（zero-overhead）。"},{"a":"slide-12","h":"并行执行策略 — par / par_unseq / unseq","x":"12 并行执行策略 — par / par_unseq / unseq 用法 · REAL实战：日志统计加速 策略 语义 典型用 execution::seq 顺序（与无策略一致） 调试时 execution::par 并行（多线程） 耗时计算 execution::par_unseq 并行 + 向量化（SIMD） 纯算术大数组 execution::unseq (C++20) 仅向量化（单线程） 避免线程开销 用法 #include <execution> std:: sort (std::execution::par, v. begin (), v. end ()); std:: transform (std::execution::par_unseq, src. begin (), src. end (), dst. begin (), []( double x){ return x * x; }); double sum = std:: reduce (std::execution::par_unseq, v. begin (), v. end (), 0.0); 陷阱 ：① par_unseq 中 不能 有锁、I/O、std::cout（不可重入）；② libstdc++ 需链接 -ltbb ；③ 数据量小时并行反而更慢（线程调度开销）；④ 浮点 reduce 顺序不确定 → 结果可能与 accumulate 略不同。 REAL 实战：日志统计加速 // 1000 万条日志的错误数 size_t errs = std:: count_if (std::execution::par, logs. begin (), logs. end (), []( const LogEntry & e){ return e.level >= LogLevel::Error; }); // CRC 校验整块缓冲 uint32_t crc = std:: transform_reduce (std::execution::par_unseq, buf. begin (), buf. end (), uint32_t {0xFFFFFFFF}, []( uint32_t a, uint32_t b){ return a ^ b; }, []( uint8_t b){ return kCrcTable[b]; }); 真实工程更多用 TBB / OpenMP / 业务自有线程池（更多控制）。STL 并行算法适合「快速给一段循环加并行」的轻量场景。"},{"a":"slide-13","h":"非修改型 — for_each / find / count / *_of","x":"13 非修改型 — for_each / find / count / *_of 常用模式 函数 含义 示例 for_each 对每个元素应用 fn 打印、累加副作用 find / find_if / find_if_not 找第一个匹配 查找 count / count_if 统计 计数 all_of / any_of / none_of 谓词的全/任/无 校验 mismatch 找两个序列首个不相等位置 diff equal 序列相等 比较 search / find_end 子序列查找（KMP-like） 字符串/字节流 adjacent_find 找首个相邻相等元素 去重前置 min_element / max_element / minmax_element 最小/大/同时 极值 clamp (C++17) 限制到 [lo, hi] 边界保护 常用模式 // all_of - 校验所有连接已断开 if (std:: all_of (conns. begin (), conns. end (), []( const Connection * c){ return c-> is_closed (); })) { stop_server (); } // any_of - 任意一个失败 if (std:: any_of (workers. begin (), workers. end (), []( const Worker & w){ return w.faulted; })) { trigger_alert (); } // minmax_element - 一次扫描得 min/max（O(n)） auto [lo, hi] = std:: minmax_element (v. begin (), v. end ()); // search - 字节流找 pattern (用于协议帧定位) auto it = std:: search (buf. begin (), buf. end (), pattern. begin (), pattern. end ()); // clamp - 安全截取 int port = std:: clamp ( user_input (), 1, 65535); // equal - 比较两段（C++14 起 4 参数版本更安全） if (std:: equal (a. begin (), a. end (), b. begin (), b. end ())) {} // ✅ 检查长度 3 参数 equal(a_begin, a_end, b_begin) 假设 b 至少有同样长度， 不检查越界 。永远用 4 参数版本。"},{"a":"slide-14","h":"算法复杂度速查表","x":"14 算法复杂度速查表 算法 时间 空间 稳定? 就地? sort O(n log n) O(log n) 栈 否 是 stable_sort O(n log n) 若内存够 / O(n log² n) O(n) 期望 是 是 partial_sort O(n log k) O(1) 否 是 nth_element O(n) 平均，O(n²) 最坏 O(1) 否 是 binary_search / lower/upper_bound O(log n) 比较，O(n) 跳转 (非随机访问) O(1) — — make_heap O(n) O(1) — 是 push_heap / pop_heap O(log n) O(1) — 是 partition O(n) O(1) 否 是 stable_partition O(n) 若内存够 / O(n log n) O(n) 期望 是 是 unique O(n) O(1) — 是 rotate O(n) O(1) — 是 reverse O(n) O(1) — 是 merge / set_* O(n+m) O(n+m) 输出 是 否（输出到 dst） inplace_merge O(n) 若内存够 / O(n log n) O(n) 期望 是 是 accumulate / reduce O(n) O(1) — — partial_sum / scan O(n) O(1) — 否（输出） 面试可问\"sort 和 stable_sort 有什么区别? 哪个更慢?\" — 答：① 稳定性不同；② stable_sort 通常使用归并，需要 O(n) 临时内存；③ 内存不足时退化为 O(n log² n)；④ 单测速度通常 sort 略快（常数因子小）。"},{"a":"slide-15","h":"真实工程 · 算法组合实战","x":"15 真实工程 · 算法组合实战 ① P99 延迟监控（每秒计算） · ② 配置 diff（增量发布） · ③ 多关键字排序 · ④ 已排序 vector 替代 set（小数据量更快） · … ① P99 延迟监控（每秒计算） double p99 (std::vector< double >& samples) { auto idx = size_t (0.99 * samples. size ()); std:: nth_element (samples. begin (), samples. begin () + idx, samples. end ()); return samples[idx]; // O(n) — 比 sort + 取索引快几个量级 } ② 配置 diff（增量发布） std::vector<std::string> added, removed; std:: set_difference (new_paths. begin (), new_paths. end (), old_paths. begin (), old_paths. end (), std::back_inserter(added)); std:: set_difference (old_paths. begin (), old_paths. end (), new_paths. begin (), new_paths. end (), std::back_inserter(removed)); ③ 多关键字排序 // 先按到达时间，再按优先级（priority 主键，时间次键） std:: stable_sort (tasks. begin (), tasks. end (), []( auto & a, auto & b){ return a.arrival < b.arrival; }); std:: stable_sort (tasks. begin (), tasks. end (), []( auto & a, auto & b){ return a.priority > b.priority; }); ④ 已排序 vector 替代 set（小数据量更快） class SmallSet { std::vector< int > v; public : void insert ( int x) { auto it = std:: lower_bound (v. begin (), v. end (), x); if (it == v. end () || *it != x) v. insert (it, x); } bool contains ( int x) const { return std:: binary_search (v. begin (), v. end (), x); } }; ⑤ 协议解析中 search 找帧边界 constexpr std::array< uint8_t , 4> kFrameStart{0xDE, 0xAD, 0xBE, 0xEF}; auto it = std:: search (buf. begin (), buf. end (), kFrameStart. begin (), kFrameStart. end ()); if (it != buf. end ()) { size_t offset = it - buf. begin (); // ... 从 offset 开始解析帧 ... }"},{"a":"slide-16","h":"高频面试题（15 题 · 可折叠）","x":"16 高频面试题（15 题 · 可折叠） 每题：关键词 + 原理 + 复杂度 + 加分点 · 30 秒可口述"},{"a":"slide-16-1","h":"基础（送分，必须 3 秒答出）","x":"基础（送分，必须 3 秒答出） 点击每道题展开参考答案（先自测，再对照）。 基础 std::sort 用什么算法? 答 ： introsort（内省排序） = quicksort + heapsort + insertion sort 混合：开始用 quicksort （常数小、缓存友好），递归深度超过 2·log₂(n) 时切换 heapsort 防最坏 O(n²) ，分段规模 < 16 用 insertion sort （小规模常数最小）。最终保证最坏 O(n log n) 。 加分：三种算法分别防御的问题 — quicksort 缓存最好、heapsort 最坏可控、insertion 在 nearly-sorted 上接近 O(n) 。 std::stable_sort 则是 mergesort（保稳定）。 基础 sort 与 stable_sort 区别? 答 ：① 稳定性 ：sort 不保证相等元素相对顺序；stable_sort 保证。② 复杂度 ：sort 最坏 O(n log n) ；stable_sort 内存足时 O(n log n) （mergesort），内存不足退化为 O(n log² n) 。③ 内存 ：stable_sort 需要 O(n) 辅助内存。④ 常数 ：sort 常数更小。 工程：多关键字排序时优先 stable_sort 先排次要字段再排主要字段；或一次性 sort + lambda 同时比较多字段。 基础 lower_bound 与 upper_bound 区别? 答 ：都做二分，要求有序区间。 lower_bound(v, x) ：第一个 ≥ x 的位置（找\"插入点\"或\"是否存在 x\"） upper_bound(v, x) ：第一个 > x 的位置 [lower, upper) = x 的全部等值区间 — 等价于 equal_range 判断 x 是否存在： it != end && *it == x ， 不要 用 binary_search （它只返 bool，不给位置）。 基础 remove 真的会删除元素吗? 答 ： 不会 。 remove / remove_if 是 erase-remove 惯用法 的前半段 — 它把不删的元素前移、返回新逻辑尾迭代器， 容器 size 不变 。必须配合 erase 才真删： v. erase (std:: remove (v. begin (), v. end (), 42 ), v. end ()); // C++20: std:: erase (v, 42 ); // 一句话完成 为什么这么设计 ：算法不持有容器、不能调整 size — 这是 STL\"算法/容器分离\"原则的体现。 基础 partial_sort 与 nth_element 各适合什么场景? partial_sort(b, mid, e) — 把前 k 个最小元素 排好序 放在 [b, mid) ，复杂度 O((n-k)·log k + k·log k) ，内部基于堆。适合\" Top K 还要排序 \"。 nth_element(b, nth, e) — 把第 k 小的元素放到 nth 位置，左边 ≤ nth、右边 ≥ nth， 内部无序 ，平均 O(n) （Hoare 快速选择 + introselect 防最坏）。适合\" 只要找第 k 个 / 分位数 \"。 组合拳：要 Top K 排序 → nth_element + 局部 sort([b, b+k]) 比 partial_sort 还快（ O(n + k·log k) ），因为 nth_element 是线性的、partial_sort 每次还要维护堆。"},{"a":"slide-16-2","h":"中级（拉开差距）","x":"中级（拉开差距） 中级 accumulate 和 reduce 区别? accumulate （C++98）： 严格顺序 从左到右 (((a+b)+c)+d) 。可指定 binary op，结果与顺序相关。 reduce （C++17）：允许 乱序 + 并行 ，op 必须满足 结合律 + 交换律 ，否则 UB（如浮点加法不严格结合 → 多次运行结果可能不同）。 transform_reduce ：先 map 再 reduce，并行经典 pattern（如 dot product）。 关键： std::reduce(std::execution::par_unseq, v.begin(), v.end(), 0.0) 可线程并行 + SIMD 向量化，大数据求和比 accumulate 快 4-8×。 中级 什么是严格弱序? 违反会怎样? 答 ：比较器必须满足 strict weak ordering ：① 非自反 !(a<a) ；② 反对称 a<b ⇒ !(b<a) ；③ 传递 a<b ∧ b<c ⇒ a<c ；④ 等价的传递性： !(a<b) ∧ !(b<a) 定义的\"等价\"也必须传递。 违反后果 ： std::sort 行为 未定义 — 实测会死循环、段错误、越界写。常见错误：用 <= 替代 < （破坏 ①）；浮点比较带 NaN（NaN 与任何值都 !(a<b) && !(b<a) 但又不等价）。 排查：构造小数据 + assert(is_sorted(v.begin(), v.end(), cmp)) 。 中级 find Top-K 元素有几种方法? 哪种最快? 答 ：4 种方案： 方案 复杂度 适用场景 sort 全部 O(n log n) k 接近 n、一次性 partial_sort O(n log k) 要 Top K 排序、k 中等 nth_element + 局部 sort O(n + k log k) Top K 排序， 最快 大小 k 的小顶堆遍历 O(n log k) 流式 数据，唯一选择 非流式选 nth_element + sort；流式（数据无法全部装入内存）必须用堆 — 这是唯一能 O(1) 空间在线维护 Top K 的方案。 中级 stable_sort 为什么需要额外内存? 答 ：通常实现为 归并排序 (mergesort) ，merge 步骤需要 O(n) 临时 buffer 存合并结果。libstdc++ 实现先 get_temporary_buffer 拿尽量大的辅助内存： 拿到足够内存 → 标准 mergesort， O(n log n) 。 内存不足 → 退化为 in-place merge， O(n log² n) 。 也是 STL 唯一会\"内存换性能\"的算法。常数比 std::sort 大 1.5-2 倍，不需要稳定性时优先 sort 。 中级 set_* 算法的输入需要什么前提? 答 ： set_union / set_intersection / set_difference / set_symmetric_difference / includes / merge 这一族都要求 两个输入区间已按相同比较器有序 。 输出区间不能与输入重叠（除非 in-place 版本）； 复杂度都是 O(n + m) 线性 — 比哈希集合的\"先建集合再求交\"快得多； 可用于任何有序序列 ，不限于 std::set —— 这是 STL 算法的威力：函数操作的是迭代器范围，不是容器。 工程实例：两个 vector<int> （已排序）求交集，比塞进 std::set 再求交集快 5-10×（无堆分配、缓存友好）。日志去重、ID 集合运算、倒排索引归并都是这一族的天然场景。"},{"a":"slide-16-3","h":"进阶 / 体现深度（让面试官眼前一亮）","x":"进阶 / 体现深度（让面试官眼前一亮） 高级 make_heap 为什么是 O(n) 而不是 O(n log n)? 答 ：从最后一个非叶节点开始 自底向上 sift_down （不是自顶向下 push_heap）。第 i 层（从底数起）节点数约 n/2ⁱ ，单次 sift_down 代价 O(i) ，总和： Σ (n / 2ⁱ) · i ≈ n · Σ (i / 2ⁱ) ≤ 2n = O(n) 直觉：底层节点数多但下沉路径短、顶层路径长但节点少 — 加权和收敛到常数。 对比： 逐个 push_heap 建堆是 O(n log n) — 每个新元素都可能上浮整棵树高，没有\"短路径多、长路径少\"的红利。 高级 STL 算法对 lambda / 函数指针 / std::function 性能差? 答 ：差距巨大（实测 std::sort 1M ints）： lambda / functor — 每个 lambda 是 唯一类型 ，编译器能 完全内联 ，零开销抽象。 最快 （基准 1x）。 函数指针 — 类型擦除少一层，但仍是间接调用（无 LTO 时无法内联）。 慢 2-5× 。 std::function — 类型擦除 + 可能小对象优化 + 虚表分发， 慢 5-10× ，还可能堆分配。 结论：STL 算法的 callable 参数永远首选 lambda / 仿函数；只在跨边界（plugin 接口）才用 std::function 。 std::sort + lambda 编出的汇编几乎和手写循环一样。 高级 par_unseq 不能有什么操作? 答 ： std::execution::par_unseq = 并行 + 向量化交错执行 。lambda body 内禁止： 任何同步原语 ：mutex、lock_guard、condition_variable — 同一线程内可能交错执行不同迭代，会 自死锁 （同一线程递归锁不可重入 mutex）。 抛异常 — 异常会直接调 std::terminate 。 不可重入的全局状态 ：随机数生成器、 std::cout 、IO。 依赖 执行顺序 的副作用 — 反正乱序，别假设顺序。 par （仅并行不向量化）则允许 mutex。 seq 是顺序、纯单线程。简单经验：lambda body 纯函数最安全 。 高级 怎么用 STL 算法实现 LRU 中\"移到头部\"? 答 ：经典 LRU = std::list<pair<K, V>> + std::unordered_map<K, list::iterator> 。\"访问后移到头部\"用 list::splice （节点指针操作， O(1) 且 迭代器不失效 ）： void touch (K key) { auto it = map_[key]; list_. splice (list_. begin (), list_, it); // 节点搬到 begin，O(1) } 不要用 erase + push_front — 那会让 map 里存的 iterator 失效 ，必须重新插入。 splice 是 list 的杀手锏 ，也是 list 唯一在 vector 上无法 O(1) 模拟的操作之一 — 这就是 LRU 必须用 list 的根本原因。 高级 透明 functor (less<>) 与 less<int> 区别? std::less<int> ：固定比较两个 int ，传入其他类型要先 构造 int。 std::less<> （C++14， 透明仿函数 ）：模板化的 operator()(auto&&, auto&&) ，不强制两侧同型，能直接比较 string vs string_view 、 int vs long 。 关键应用 — 异构查找 (heterogeneous lookup, C++14) ： std::map<std::string, V, std:: less <>> m; m. find (std::string_view{ \"abc\" }); // ✅ 不必构造 std::string m. find ( \"abc\" ); // ✅ 不必构造 std::string 每次 lookup 省一次堆分配 — 热点路径上能减少几十 % 的开销。 unordered_xxx 版本要求自定义 hash + equal 都标 using is_transparent = void; 才生效。STL find / lower_bound / sort 默认就是透明 lambda → 永远首选 std::less<> 、 std::greater<> ，几乎没有理由用类型固化版本。"},{"a":"slide-16-4","h":"速答示范（第 8 题）","x":"速答示范（第 8 题） 找 N 中最大 K 个： partial_sort — O(n log k)，前 k 个有序 nth_element + 局部 sort — O(n + k log k)，最快 大小为 k 的小顶堆遍历 — O(n log k)， 流式 场景唯一选 sort 全部 — O(n log n)，无脑做法 能在 4 种 Top-K 方案中根据场景给出选择 + 解释为什么 → C++ 算法面试 90% 安全。完整 15 题速答详解见下一张 slide。"},{"a":"end","h":"本专题结束 · 下一步","x":"17 本专题结束 · 下一步 STL 算法 + 函数对象 + Ranges 是 替代 80% 自己写循环 的工具箱。能熟练用 std 算法 = 资深 C++ 工程师标志之一。 同期资料 cpp_stl_containers_ppt.html （已有）— 容器深度 下一份： 值类别 + 拷贝控制长文 （Rule of 5 / RVO / xvalue） cpp_modern_features_ppt.html — 现代 C++ 特性 cpp_object_model.html — 对象模型 建议练习 用 nth_element + partial_sort 实现 K 个最大 + 排序 用 STL 算法实现 LRU \"move-to-front\"（list::splice） 对比 accumulate / reduce / par_unseq 的性能差 用 ranges 重写一个真实业务的 filter+map+reduce 链 godbolt 看看 std::sort + lambda 编译出的汇编 ← 上一篇 · 容器 下一篇 · 智能指针 → 相关 · 模板与 TMP 相关 · 设计模式 目录 主页 STL 算法深度剖析 — 排序 · 查找 · 数值 · Ranges — 2026 ↑"}]},{"u":"stl/stl_containers_ppt.html","t":"C++ STL 容器深度剖析 PPT","c":"现代 C++ / STL","s":[{"a":"","h":"","x":"C++ STL 容器深度剖析 PPT — 面试硬通货 Kavin Tech Notes ← 主页 目录 30 min ◐ STL 容器 · Roadmap STL 容器全景与分类 vector — 三指针模型与扩容 vector — 操作复杂度 + emplace vs push_back explicit 与 initializer_list 两坑 vector — 迭代器失效完全规则 ★ string — SSO 小字符串优化 array / span — 固定大小与视图 deque — 分段连续的双端队列 list / forward_list — 链表家族 map / set — 红黑树原理 unordered_map — 哈希表实现 unordered_map — 自定义 key 和 hasher multimap / multiset — 允许重复 key 容器适配器 — stack / queue / priority_queue 迭代器五大类与 iterator_traits 迭代器失效完全规则总表 ★★★ 容器选择决策树 C++17/20 容器新接口集锦 容器与异常安全 真实工程 · 容器选择实战 高频面试题集（15 题 · 可折叠） 本专题结束 · 下一步 STL Containers · 面试硬通货 · 2026 STL 容器深度剖析 面试硬通货 · 每张 slide 一个考点 · 涵盖 内部数据结构 / 复杂度 / 迭代器失效 / 选型决策 / 真实工程示例 核心难度 ★★★★☆ 面试出现率 极高 阅读时长 ~30 min 前置 指针 · 复杂度分析 21 大章节 序列 · 关联 · 无序 · 适配器 迭代器失效 · 选型决策 单文件离线可读"},{"a":"slide-01","h":"STL 容器全景与分类","x":"01 STL 容器全景与分类 先建立分类心智模型，后续每张 slide 才有锚点。 ① 序列容器 (Sequence) vector — 动态数组 (push_back O(1) 摊销) array (C++11) — 编译期固定大小 deque — 双端队列 (分段连续) list — 双向链表 forward_list (C++11) — 单向链表 string — 实质上是 vector<char> 的特化（含 SSO） ② 关联容器 (Associative · 红黑树) set / multiset — 元素本身有序 map / multimap — key→value 有序 底层：红黑树（自平衡 BST） 所有操作 O(log n) ，迭代有序 ③ 无序关联容器 (Unordered · 哈希表) C++11 unordered_set / unordered_multiset unordered_map / unordered_multimap 底层：拉链法哈希表（vector<list<...>>） 平均 O(1) ，最坏 O(n)；不保序 ④ 容器适配器 (Adapter) stack — 默认底层 deque（LIFO） queue — 默认底层 deque（FIFO） priority_queue — 底层 vector + heap 算法 不提供 begin/end 迭代器！ 面试常问 \"vector 和 list 怎么选?\" 这是最基础的考察点 — 不要只答「随机访问 vs 插入快」，要说 缓存友好性 + 链表节点开销 + 现代 CPU 上 vector 几乎全胜 。"},{"a":"slide-02","h":"vector — 三指针模型与扩容","x":"02 vector — 三指针模型与扩容 内存布局示意 内部结构（libstdc++ / libc++ 一致） template < class T> class vector { T* _begin; // 数据起始 T* _end; // 已用元素之后 (size 终点) T* _end_cap; // 已分配容量之后 }; // size() = _end - _begin // capacity() = _end_cap - _begin // sizeof(vector<int>) = 24 bytes (64-bit, 三指针) 扩容（关键面试点） 容量不够 → 分配新一段 → 把旧元素 移动/拷贝 过去 → 释放旧的 增长因子： libstdc++ = 2x ， libc++ / MSVC = 1.5x 2x 简单但内存利用率差；1.5x 让 新空间可能复用旧释放的内存 （黄金分割理论） 复杂度：单次最坏 O(n)， 摊销 O(1) （几何级数） 内存布局示意 10 20 30 40 _begin _end (size=4) _end_cap (capacity=8) 蓝色=已用元素 · 灰色虚线=已分配但未构造 关键 ： resize(n) 改变 size（构造或析构元素）； reserve(n) 只改变 capacity（仅分配，不构造）。 capacity 永远只增不减，除非调用 shrink_to_fit() （非强制）。 面试问\"为什么 1.5 优于 2\"： 2x 时新分配 = 之前所有累计容量之和 + 1， 无法复用 之前释放的内存（连续分配的内存地址递增）；1.5x 在 N 次扩容后 新需要的容量 ≤ 之前累计释放的容量 ，allocator 可能把它们合并复用 → 内存利用率更好。"},{"a":"slide-03","h":"vector — 操作复杂度 + emplace vs push_back","x":"03 vector — 操作复杂度 + emplace vs push_back 操作复杂度 · emplace_back vs push_back（高频面试题） 操作复杂度 操作 复杂度 会引起扩容/失效? 注意 operator[] / at O(1) 否 at 越界抛 out_of_range push_back / emplace_back O(1) 摊销 可能 cap 满才扩容 pop_back O(1) 仅尾后迭代器失效 不释放容量 insert / emplace (中部) O(n) 是 移动后续元素 erase O(n) 当前及之后 使用 erase-remove 惯用法 clear O(n) 所有迭代器失效 不释放容量 resize(n) O(|n - size|) 可能 (n > cap) 构造/析构元素 reserve(n) O(n) 若扩容 是 (n > cap) 不构造元素 shrink_to_fit O(n) 是 非强制；通常会重新分配 emplace_back vs push_back（高频面试题） 区别本质 push_back(x) ：要求 x 已经是 T；可能拷贝或移动。 emplace_back(args...) ：在原地 转发 构造 — 直接在 vector 内存上 placement new T(args...)， 避免临时对象 。 什么时候真的有差别 构造一个新对象时 → emplace 省一次 临时对象 + 移动 已经有现成对象时 → 几乎无差别 注意：emplace_back 会绕过 explicit 构造保护，可能被滥用 struct Conn { Conn ( int fd, std::string ip); }; std::vector< Conn > v; v. push_back ( Conn {fd, ip}); // ① 临时 Conn ② 移动到 vector ③ 析构临时 v. emplace_back (fd, ip); // 直接在 vector 内构造，省 ①②③ std::vector< Conn > v2; Conn c{fd, ip}; v2. push_back ( std::move (c)); // 移动构造 1 次 v2. emplace_back ( std::move (c)); // 也是移动构造 1 次（无差别）"},{"a":"slide-03-traps","h":"两坑： explicit 与 initializer_list","x":"两坑： explicit 与 initializer_list 已有对象用 push_back(std::move(x)) 即可，不必为了「显得现代」改成 emplace。下面两个坑才是 emplace 真正要记住的语义差。 场景 push_back emplace_back 取舍 explicit 单参构造 v.push_back(1) 常 编不过 （拒绝隐式转换） v.emplace_back(1) 直接调 explicit 构造 emplace 绕过「必须写成 T{1}」的保护；接口若靠 explicit 防误用，这里会被静默绕开 花括号列表 vv.push_back({1,2,3}) 常可以（推成 initializer_list → 临时 vector） vv.emplace_back({1,2,3}) 常 编不过 （变参转发推不出 initializer_list ） 列表初始化走 push；要 emplace 就写清类型： emplace_back(std::vector<int>{1,2,3}) struct Port { explicit Port ( int ); }; std::vector< Port > ports; ports. push_back ( 80 ); // ❌ explicit，拒绝 int→Port ports. emplace_back ( 80 ); // ✅ 原地调 explicit Port(int) — 保护被绕过 ports. push_back ( Port { 80 }); // ✅ 意图写在脸上 std::vector<std::vector< int >> vv; vv. push_back ({ 1 , 2 , 3 }); // ✅ 推成 initializer_list vv. emplace_back ({ 1 , 2 , 3 }); // ❌ 变参模板推不出 initializer_list vv. emplace_back (std::vector< int >{ 1 , 2 , 3 }); // ✅ 但此时并不比 push 省 权衡：构造新对象用 emplace 省临时；已有对象 push_back(std::move(x)) 就够。explicit 是接口契约，被 emplace 绕过时优先把意图写成 T{args} 再 push。 面试加分：能说出\"reserve 后 emplace_back 才是性能巅峰\" — 因为 reserve 消除扩容拷贝，emplace 消除临时对象，两者结合 = 理论最优。"},{"a":"slide-04","h":"vector — 迭代器失效完全规则 ★","x":"04 vector — 迭代器失效完全规则 ★ 典型 BUG 与正确写法 操作 失效范围 原因 push_back 未触发扩容 仅 end() 新元素加在末尾后 push_back 触发扩容 全部 迭代器、指针、引用 底层重新分配，旧地址失效 insert(pos, x) 未扩容 pos 及之后所有迭代器 后续元素被向后挪 insert(pos, x) 扩容 全部 同上 + 重新分配 erase(pos) pos 及之后所有迭代器 后续元素向前挪 clear() 全部（end 仍可用） 所有元素析构，size=0 reserve(n) n > cap 全部 重新分配 resize(n) n > cap 全部 同上 shrink_to_fit() 全部 （可能） 重新分配到刚好 size swap(other) 不失效（指向被交换前的元素） 仅交换三个指针 典型 BUG 与正确写法 // ❌ BUG: erase 之后 it 已失效 for ( auto it = v. begin (); it != v. end (); ++it) { if (*it < 0) v. erase (it); // UB: 之后 ++it 越界 } // ✅ 正确：用 erase 返回的下一个迭代器 for ( auto it = v. begin (); it != v. end (); ) { if (*it < 0) it = v. erase (it); else ++it; } // ✅ 更现代：erase-remove (C++20 起 std::erase_if 一行) v. erase (std:: remove_if (v. begin (), v. end (), []( int x){ return x < 0; }), v. end ()); std:: erase_if (v, []( int x){ return x < 0; }); // C++20 记忆口诀 ：vector 一旦 容量变化 → 全部失效； 位置变化 （insert/erase）→ 改动点之后失效。"},{"a":"slide-05","h":"string — SSO 小字符串优化","x":"05 string — SSO 小字符串优化 实现示意 (libc++ 风格) · 对工程的实际影响 核心概念 SSO (Small String Optimization) ：短字符串直接存在 string 对象内部 buffer， 不分配堆内存 。 避免大量短字符串场景的频繁 malloc/free，性能巨大提升。 历史上：g++ 5 之前 string 是 COW（写时复用） ，但与 C++11 多线程冲突 → 改为 SSO。 各实现的阈值 libstdc++ : SSO 阈值 ≤ 15 字节，sizeof(string) = 32 libc++ : ≤ 22 字节，sizeof(string) = 24（更激进） MSVC : ≤ 15 字节，sizeof(string) = 32 实现示意 (libc++ 风格) class string { union { struct __long { size_t cap; size_t size; char * data; }; struct __short { unsigned char size : 7; // 1-bit 标志位 + 7-bit size unsigned char is_long : 1; char data[23]; // 直接内嵌 }; }; }; 对工程的实际影响 SSO 受益的场景 HTTP header name (大多 < 22 字节) map 的 key 是短 string 日志 tag、metric 名字 配置项 key 注意事项 SSO 字符串 移动 = 拷贝 （数据在对象内部，没办法只 swap 指针） 故 std::move 短 string 不省事，长 string 才有效 跨 SSO 阈值的扩展会触发一次 malloc + memcpy // 验证你的实现的 SSO 阈值 std::string s = \"hello\" ; std::cout << ( void *)s. data () << \" \" << ( void *)&s << '\\n' ; // 如果 data() 地址在 &s 附近 → 走 SSO 面试可问\"为什么 g++ 放弃了 COW string?\" — 答：① C++11 要求 operator[] 不抛异常 + 多线程下 const 操作必须线程安全，COW 的 ref-count 增加复杂度；② 现代多核场景，原子计数开销甚至超过节省的内存。"},{"a":"slide-06","h":"array / span — 固定大小与视图","x":"06 array / span — 固定大小与视图 REAL实战示例 std::array<T, N> 编译期固定大小， 栈上 分配 替代 C 数组：有 size()/at()/begin()/end()，可拷贝、可与 std 算法配合 零开销： sizeof(array<int,4>) == sizeof(int[4]) == 16 支持结构化绑定 + CTAD (C++17) std::span<T> {T*, size_t} 的非拥有视图（\"轻引用\"） 统一接受：C 数组、std::array、std::vector、子段 替代 C 风格 (T* p, size_t n) 双参 不延长生命周期， 调用方负责 REAL 实战示例 // 协议字段固定大小 → array struct EthHeader { std::array< uint8_t , 6> dst_mac; std::array< uint8_t , 6> src_mac; uint16_t ether_type; }; static_assert ( sizeof ( EthHeader ) == 14); // 通用解析函数 → span（接收 vector / array / 裸指针） bool parse (std::span< const uint8_t > data) { if (data. size () < 14) return false ; auto hdr = data. first (14); // O(1) 子段 auto body = data. subspan (14); // ... return true ; } // 调用 char buf[1500]; std::vector< uint8_t > v = recv_packet (); std::array< uint8_t , 64> small; parse ({( const uint8_t *)buf, 1500}); parse (v); parse (small); // ✅ 全部兼容"},{"a":"slide-07","h":"deque — 分段连续的双端队列","x":"07 deque — 分段连续的双端队列 内部布局 · vector vs deque 选型 核心结构 不是单一连续内存，而是 「中央 map（指针数组）+ 多段固定大小 chunk」 。 每个 chunk 通常 4KB（一页），里面元素连续；chunk 之间不连续。 支持 O(1) 双端 push/pop； O(1) 随机访问（要算两次跳转）。 但 中部 insert/erase 是 O(n) （与 vector 类似）。 什么时候用 需要前后都能 push（vector 不能 push_front） 不需要长时间稳定的指针（push_front/back 不会让 已存在元素的指针 失效，但迭代器会失效） STL stack / queue 默认底层用 deque 内部布局 中央 map (指针数组) chunk 0 e0 e1 e2 ... eN chunk 1 chunk 2 chunk 3 每个 chunk 内连续，chunk 间通过中央 map 跳转 vector vs deque 选型 需求 选型 理由 只在尾部 push/pop vector 缓存友好，单一内存块 双端 push/pop deque vector 不支持 O(1) 头部 追求最高扫描速度 vector deque 跨 chunk 有跳转 大量元素，内存碎片敏感 deque 不需要单块巨大连续内存 需要稳定指针 (push 不让旧元素地址变) deque (头尾 push) vector 扩容会让所有指针失效 实际工程中 vector 用得远多于 deque 。deque 出现的最大场景是「stack/queue 的默认底层」和「需要频繁 push_front 的日志/任务队列」。"},{"a":"slide-08","h":"list / forward_list — 链表家族","x":"08 list / forward_list — 链表家族 仍然合适的场景（少而具体） 结构 list ：双向链表，每节点有 prev/next/T 三部分 forward_list (C++11)：单向链表，每节点 next/T 每节点单独 malloc → 内存不连续，缓存极不友好 为什么\"几乎不该用\" 每个 int 节点开销 = 24 字节（vs vector 的 4 字节） 遍历比 vector 慢 10~50 倍 （cache miss） insert/erase O(1) 的优势在 已知位置 下才生效；查找仍是 O(n) Bjarne Stroustrup 著名实验：1 万元素插入排序，vector 全胜 list 仍然合适的场景（少而具体） 稳定的迭代器/指针 ：节点不会因为其他元素 insert/erase 而失效（除了被删除的节点本身） splice O(1) ：把一个 list 的子段直接接到另一个， 不拷贝节点内容 大型对象（移动昂贵）的链表式调度队列（极少） 侵入式链表（intrusive list）— 但那一般是手写 // list 唯一显著优势：splice — O(1) 不拷贝 std::list< Task > ready, runnable; runnable. splice (runnable. end (), ready, ready. begin ()); // 把 ready 第一个节点接到 runnable // 迭代器稳定性：以下场景 it1 始终有效 std::list< int > l{1,2,3}; auto it1 = std:: next (l. begin ()); // 指向 2 l. push_front (0); l. push_back (99); // it1 仍指向 2 面试问\"vector 和 list 怎么选?\"答： 默认 vector ，仅当需要稳定迭代器/splice/超大元素的链表式调度时考虑 list。\"链表查找 O(1)\" 是错觉 — 必须先 O(n) 找到位置才能 O(1) 插。"},{"a":"slide-09","h":"map / set — 红黑树原理","x":"09 map / set — 红黑树原理 常用操作 · 迭代器失效（很温和） 核心概念 底层都是 红黑树（Red-Black Tree） ，一种自平衡 BST。 所有操作 严格 O(log n) ，元素按 key 自然有序。 map: key → value 映射；set: 仅 key（去重）。 multimap/multiset：允许相同 key。 红黑树性质（5 条，面试可能让背） 每个节点是红或黑 根节点是黑 叶子（NIL）是黑 红节点的子节点必须是黑（不能两红相邻） 从任一节点到其后代叶子的路径上黑节点数相同 → 树高最大 2 * log2(n+1) 常用操作 std::map<std::string, int > m; m[ \"a\" ] = 1; // O(log n) 插入或访问，注意： 会默认构造 不存在的 key // 安全访问： if ( auto it = m. find ( \"b\" ); it != m. end ()) use (it->second); // 不存在则插入，存在则跳过 — 用 try_emplace（C++17） m. try_emplace ( \"a\" , 999); // 不会更新已有的 1 // 不存在则插入，存在则更新 — insert_or_assign（C++17） m. insert_or_assign ( \"a\" , 999); // 范围查找（红黑树特有的有序优势） auto it_low = m. lower_bound ( \"a\" ); // 第一个 >= \"a\" auto it_high = m. upper_bound ( \"c\" ); // 第一个 > \"c\" auto [b, e] = m. equal_range ( \"a\" ); // [lower, upper) 迭代器失效（很温和） 操作 失效 insert/emplace 不失效任何已存在的 迭代器/引用 erase 仅被删除元素的迭代器；返回下一个 clear 全部 陷阱 ： m[\"key\"] 在 key 不存在时 会插入默认构造的 value ，统计场景极易踩坑。只读检查请用 find 或 contains (C++20)。"},{"a":"slide-10","h":"unordered_map — 哈希表实现","x":"10 unordered_map — 哈希表实现 结构示意 · 性能与陷阱 底层结构 STL 标准要求 拉链法（separate chaining） ： vector<forward_list> 风格。 每个 bucket 是一个链表，hash 冲突的元素挂在同一个 bucket 链表上。 这意味着： 元素地址稳定 （rehash 时迭代器失效，但指针/引用 仍有效 ）— 这是与多数语言（如 Java HashMap）不同的关键点。 关键参数 load_factor() = size / bucket_count max_load_factor() 默认 1.0；超过即 rehash rehash(n) ：手动指定至少 n 个 bucket reserve(n) ：保证容纳 n 个元素不 rehash 结构示意 bucket 数组（vector） bk0 bk1 bk2 bk3 bk4 bk5 k=\"cat\" v=1 k=\"dog\" v=2 ↑ 同一 bucket 的链表 (hash 冲突) k=\"bird\" 性能与陷阱 场景 复杂度 find/insert/erase 平均 O(1) 极端 hash 冲突 (恶意输入) 退化为 O(n) rehash (load_factor 超阈值) O(n) — 重新分配 bucket，重新 hash 所有元素 面试陷阱 ：①「unordered_map 比 map 一定快」是错的 — 元素少（< 100）时 map 因为缓存友好可能更快；② string key 的 hash 可能很慢（要遍历全字符串）；③ STL 的 unordered_map 因为 稳定地址保证 不能用 open addressing，性能不如 absl/flat_hash_map。 高级答法：\"如果性能敏感，用 absl::flat_hash_map 或 robin-hood-hashing；STL unordered_map 的实现因 ABI 历史包袱（指针稳定性 + bucket 接口）注定无法极致快。\""},{"a":"slide-11","h":"unordered_map — 自定义 key 和 hasher","x":"11 unordered_map — 自定义 key 和 hasher 正确写法（生产级） · 替代方案：模板参数传 Hasher 必须满足两点 operator== 或独立 KeyEqual：判等 std::hash<T> 特化 或 自定义 Hasher：算 hash 值 关键约束： a == b ⇒ hash(a) == hash(b) 常见 hash 组合错误 简单 XOR 多个字段： hash(a) ^ hash(b) — 字段相等时退化为 0 正确做法：boost::hash_combine 风格，混入位移 正确写法（生产级） struct Endpoint { std::string ip; uint16_t port; bool operator ==( const Endpoint &) const = default ; // C++20 }; namespace std { template <> struct hash< Endpoint > { size_t operator ()( const Endpoint & e) const noexcept { size_t h = std:: hash <std::string>{}(e.ip); // boost::hash_combine 公式（黄金分割数） h ^= std:: hash < uint16_t >{}(e.port) + 0x9e3779b9 + (h << 6) + (h >> 2); return h; } }; } std::unordered_map< Endpoint , Connection *> conn_table; 替代方案：模板参数传 Hasher struct EndpointHash { size_t operator ()( const Endpoint & e) const noexcept { /* ... */ } }; std::unordered_map< Endpoint , Connection *, EndpointHash > t; C++14 异构查找 ：透明 hasher ( std::hash<void> ) + 透明比较器允许用 string_view 查找 string key 的 unordered_map（C++20 才完整支持，需要 hasher 提供 is_transparent ）。"},{"a":"slide-12","h":"multimap / multiset — 允许重复 key","x":"12 multimap / multiset — 允许重复 key REAL定时器轮（简化版）— multimap 的经典用法 与 map/set 的差别 允许多个相同 key 无 operator[] （因为不知该返哪个） 查找用 equal_range 拿到所有匹配 count(k) 返回 k 的实际个数 插入按红黑树规则维持有序 典型用途 定时任务表：key=触发时间，value=任务 多对一 mapping：一个 key 多条记录 排行榜：按分数排，相同分数允许多人 REAL 定时器轮（简化版）— multimap 的经典用法 class TimerWheel { using TimePoint = std::chrono::steady_clock::time_point; std::multimap< TimePoint , Task > queue_; std::mutex mu_; public : void schedule ( TimePoint when, Task t) { std::lock_guard lk{mu_}; queue_. emplace (when, std::move(t)); } void tick () { std::lock_guard lk{mu_}; auto now = std::chrono:: steady_clock::now (); auto end = queue_. upper_bound (now); // 全部到期任务的边界 for ( auto it = queue_. begin (); it != end; ) it = queue_. erase (it).first; // run + erase } }; 高性能版本会用 四级时间轮 (hierarchical timing wheel) ，把 multimap 替换为环形 bucket 数组。但对中低 QPS 场景，multimap 简单可靠。"},{"a":"slide-13","h":"容器适配器 — stack / queue / priority_queue","x":"13 容器适配器 — stack / queue / priority_queue priority_queue 的内部实现 (面试高频) · 常见用法 共同点 不是新数据结构 — 是 对底层容器的限制接口包装 没有 begin()/end() 迭代器（不能遍历） 底层容器可通过模板参数指定 默认底层 适配器 默认底层 stack deque queue deque priority_queue vector + heap 算法 priority_queue 的内部实现 (面试高频) template < class T, class Container = std::vector<T>, class Compare = std::less<T>> class priority_queue { Container c; // 默认 vector Compare comp; public : void push ( const T& v) { c. push_back (v); std:: push_heap (c. begin (), c. end (), comp); // O(log n) } void pop () { std:: pop_heap (c. begin (), c. end (), comp); // O(log n) — 把堆顶移到末尾 c. pop_back (); // 移除末尾 } const T& top () const { return c. front (); } }; 常见用法 // 默认大顶堆 std::priority_queue< int > pq; pq. push (5); pq. push (2); pq. push (8); pq. top (); // 8 // 小顶堆 — 用 greater std::priority_queue< int , std::vector< int >, std::greater< int >> min_pq; // 自定义优先级 (任务调度) struct Task { int priority; int id; }; auto cmp = []( const Task & a, const Task & b){ return a.priority < b.priority; }; // 大顶 std::priority_queue< Task , std::vector< Task >, decltype (cmp)> tq(cmp); 能讲出\"priority_queue 不是新数据结构，是 vector + std::push_heap/pop_heap 包装\"= 加分；能写出 用 vector 自己跑 std::heap 算法（更灵活，能批量 build_heap） = 高分。"},{"a":"slide-14","h":"迭代器五大类与 iterator_traits","x":"14 迭代器五大类与 iterator_traits iterator_traits — 模板访问迭代器属性 分类 能力 典型容器 例子 Input 单向只读，单遍 istream_iterator 从流读取一次 Output 单向只写，单遍 ostream_iterator, back_inserter 写入流 Forward 单向读写，可多遍 forward_list, unordered_* ++ only Bidirectional 双向读写 list, set, map ++ / -- Random Access 随机访问 O(1) vector, deque, array, string += n / [n] Contiguous (C++17) 元素物理连续 vector, array, string, span data() 可线性访问 iterator_traits — 模板访问迭代器属性 template < class Iter> void algo (Iter b, Iter e) { using Cat = typename std::iterator_traits<Iter>::iterator_category; if constexpr (std::is_same_v<Cat, std::random_access_iterator_tag>) { // 可用 b + n、e - b（O(1)） } else { // 只能 ++/-- 慢慢移动 } } // 经典分发：std::distance / std::advance 内部用 traits auto n = std:: distance (v. begin (), v. end ()); // vector: O(1); list: O(n) 能解释「为什么 std::sort 不能作用于 std::list?」= 加分。 答 ：std::sort 要求随机访问迭代器（O(1) 跳转），list 是双向迭代器；list 自己提供 list::sort （O(n log n) 归并排序）。"},{"a":"slide-15","h":"迭代器失效完全规则总表 ★★★","x":"15 迭代器失效完全规则总表 ★★★ 口诀 容器 插入 删除 resize/rehash vector 未扩容: pos 之后失效 扩容: 全部失效 pos 及之后失效 resize>cap: 全部 deque 头/尾 push: 迭代器全部失效 但 指针/引用仍有效 。中部: 全部 头/尾 erase: 仅被删的失效。中部: 全部 — list / forward_list 从不失效 已存在的 仅被删的失效 — map / set / multi* 从不失效 已存在的 仅被删的失效 — unordered_* 不 rehash: 不失效 rehash: 迭代器全部失效 ， 指针/引用仍有效 仅被删的失效 rehash: 同插入 口诀 记 3 句话 vector / deque（中部）→ 看是否扩容/重新分配 node-based (list/set/map/unordered)：插入 不失效 已有迭代器 rehash / resize-with-realloc → 全部迭代器失效 「指针 vs 迭代器」失效不同 deque 头尾 push → 迭代器失效，指针/引用不失效 unordered_* rehash → 迭代器失效，指针/引用不失效 （节点内存没动，bucket 索引变了） 典型问 ：\"std::vector::push_back 会让所有迭代器失效吗?\" — 不一定 ，取决于是否触发扩容。这种\"看似简单实则有陷阱\"的问题最容易区分新手和资深。 资深加分： vector::end() 任何修改大小的操作都会失效 （因为 end 永远指向 size 的下一个位置）。这是为什么 for (auto it=v.begin(); it!=v.end(); ) 中循环体若改 size 必须重新取 end。"},{"a":"slide-16","h":"容器选择决策树","x":"16 容器选择决策树 场景 → 推荐容器 · 反模式 场景 → 推荐容器 场景 首选 备选 原因 默认线性容器 vector — 缓存友好，随机访问 O(1) 编译期固定大小 array C 数组 类型安全 + 零开销 函数参数（线性数据） span const T*+size 统一接受 vector/array/裸数组 双端 push/pop deque list O(1) 双端 + 缓存还行 需要稳定指针 + 频繁 splice list — 少有的合理 list 场景 键值映射，需要按键有序遍历 map — 红黑树天然有序 键值映射，只需查找快 unordered_map absl::flat_hash_map O(1) 平均 需要按 key 范围查找 (lower/upper_bound) map / set 有序 vector 红黑树原生支持 FIFO / LIFO 队列 queue / stack — 语义清晰 优先级队列 priority_queue multiset O(log n) push/pop 固定字符串 (常量比较) string_view const string& 零拷贝 反模式 常见误用 「element 频繁插入中部」→ 别用 vector？ 错 — 即便如此 vector 通常仍胜出（前提：n 不太大），缓存友好性碾压 「需要快速查找」→ 别用 vector？ 错 — 已排序 vector + binary_search 在 n < 1000 时常优于 set 「字符串场景必用 string」→ 函数参数应该用 string_view 性能数据感觉 vector 遍历 100 万 int：~1 ms list 遍历 100 万 int：~50 ms (50× 慢) unordered_map 比 map 快 ~3-5×（但 string key 时差距缩小） absl::flat_hash_map 比 std::unordered_map 快 ~2-3×"},{"a":"slide-17","h":"C++17/20 容器新接口集锦","x":"17 C++17/20 容器新接口集锦 C++17 · C++20 C++17 // try_emplace - 不存在才插入，避免无谓构造 m. try_emplace ( \"key\" , big_value); // 已存在不动；不会先构造 big_value // insert_or_assign - 不存在则插入，存在则更新（区别于 operator[] 不要求默认构造） m. insert_or_assign ( \"key\" , value); // extract / merge - 节点级别移动 (零拷贝转移) auto node = m1. extract ( \"key\" ); // 拿出节点 (节点不属于容器了) node.key() = \"new_key\" ; // 甚至可以改 key ! m2. insert (std::move(node)); // 转移到 m2 m2. merge (m1); // 把 m1 里 m2 没有的 key 全转过来 // std::sample (C++17) - 随机采样 std::vector< int > sampled; std:: sample (v. begin (), v. end (), std::back_inserter(sampled), 10, gen); // std::pmr 多态 allocator - 自定义内存池 std::pmr::vector< int > v(&my_pool); C++20 // contains() - 不需要 != end() 判定 if (m. contains ( \"key\" )) {} // 替代 m.find(\"key\") != m.end() // std::erase / erase_if - 统一删除接口 std:: erase (v, 42); // 删所有 42 std:: erase_if (v, []( int x){ return x < 0; }); std:: erase_if (m, []( const auto & kv){ return kv.second.expired(); }); // starts_with / ends_with for string if (s. starts_with ( \"http://\" )) {} if (s. ends_with ( \".json\" )) {} // 透明比较器/hash - 异构查找 std::map<std::string, int , std::less<>> m; m. find (std::string_view{ \"abc\" }); // ✅ 不必构造 std::string"},{"a":"slide-18","h":"容器与异常安全","x":"18 容器与异常安全 实测对比 STL 的「强异常安全保证」 vector 扩容时承诺：操作要么成功，要么 vector 状态完全不变（强保证）。 问题：扩容要把元素从旧 buffer 转到新 buffer。如果 移动构造可能抛异常 ，转移过程中失败 → 旧 buffer 被破坏，无法回滚。 解决方案 STL 在扩容时检查： noexcept(T(std::move(t))) 是否为 true？ true ：用 移动 构造（快） false ：用 拷贝 构造（慢但安全；旧 buffer 可保留作回滚） 实测对比 struct A { std::string s; A ( A &&) /* 没标 noexcept */ = default ; }; struct B { std::string s; B ( B &&) noexcept = default ; // ✅ }; std::vector< A > va; va. reserve (1); for ( int i=0; i< 1000000 ; ++i) va. emplace_back ( \"hello world\" ); // ↑ 每次扩容都走拷贝路径 → 慢 std::vector< B > vb; vb. reserve (1); for ( int i=0; i< 1000000 ; ++i) vb. emplace_back ( \"hello world\" ); // ↑ 走移动路径 → 快几个数量级 面试经典问：\"为什么自定义类的移动构造一定要标 noexcept ?\" — 答：① STL 容器扩容性能 退化成拷贝；② 多线程并发原语（如 std::lock_guard）期待 noexcept；③ 析构函数也应 noexcept（C++11 起默认就是）。"},{"a":"slide-19","h":"真实工程 · 容器选择实战","x":"19 真实工程 · 容器选择实战 platform agent / 网络框架中的容器实战 platform agent / 网络框架中的容器实战 // ① Session 表 - 高频查找，不要求有序 std::unordered_map< SessionId , std::shared_ptr< Session >> sessions_; // 改进：absl::flat_hash_map 提速 2~3 倍 // ② 订阅事件订阅者列表 - 顺序遍历回调 std::vector< EventHandler *> subscribers_; // vector 缓存友好 // ③ schema节点的子节点 - 数量小且有序遍历 struct YangNode { std::string name; std::map<std::string, YangNode > children; // 有序 + 节点稳定 }; // ④ 待处理 RPC 队列 - 双端 deque std::deque< RpcRequest > pending_; // ⑤ 配置项 - 编译期常量集合 static constexpr std::array<std::string_view, 5> kSupportedCapabilities = { \"urn:ietf:params:RPC:base:1.0\" , \"urn:ietf:params:RPC:base:1.1\" , // ... }; // ⑥ 待发的待写出 buffer - 一次性追加许多 byte std::vector< uint8_t > out_buf_; out_buf_. reserve (64 * 1024); // 预留 // ⑦ 定时器队列 - 按到期时间 std::multimap< TimePoint , TimerCallback > timers_; // ⑧ 路由表（method+path）→ handler — 静态注册后稳定 std::unordered_map<std::string, Handler > routes_; // ⑨ 短期热点缓存 - 容量小、频繁淘汰 → LRU = list + unordered_map class LruCache { std::list<std::pair< Key , Value >> lru_; std::unordered_map< Key , decltype (lru_)::iterator> idx_; // list 提供 O(1) 移到头部，unordered_map 提供 O(1) 查找 }; 这张 slide 是 面试万能模板 ：\"给我讲一个你做过的项目，告诉我你怎么选择容器\" — 任何回答能引用以上 9 个具体场景就比\"嗯…就是 vector 和 map\" 强 10 倍。"},{"a":"slide-20","h":"高频面试题集（15 题 · 可折叠）","x":"20 高频面试题集（15 题 · 可折叠） 每题：关键词 + 原理 + 复杂度 + 加分点 · 30 秒可口述"},{"a":"slide-20-1","h":"基础题（送分，必须 3 秒答出）","x":"基础题（送分，必须 3 秒答出） 点击每道题展开参考答案（先自测，再对照）。 基础 vector 扩容为什么是 1.5x / 2x? 答 ：摊还分析下都是 O(1) 。 1.5x（GCC libstdc++） ：N 次扩容后新需空间 ≤ 之前累计释放空间，allocator 可复用旧地址 → 内存利用率高。 2x（MSVC、libc++） ：实现简单、扩容次数少（ log₂N 次），但新分配 = 之前累计 + 1， 无法复用 之前释放的连续段。 加分：能说出\"1.5x 是为了让累计释放的内存可以被 allocator 合并复用\"，并提到黄金比例 φ ≈ 1.618 是理论最优值。 基础 vector 的 size 和 capacity 区别? 答 ： size() = 已构造的元素个数； capacity() = 已分配但未必构造的容量上限，恒有 capacity ≥ size 。 push_back 只在 size == capacity 时触发扩容（重分配 + 移动/拷贝旧元素 + 析构旧元素 + 迭代器全失效）。 reserve(n) 预分配 capacity 不动 size； resize(n) 改 size（少了析构、多了默认构造）； shrink_to_fit() 请求把 capacity 降到 size（非强制）。 基础 map 和 unordered_map 怎么选? 答 ：需要 有序遍历 / 范围查找 (lower_bound) / 稳定迭代器 → map （红黑树 O(log n) ）。 只关心查找快、不需要顺序 → unordered_map （哈希，平均 O(1) 、最坏 O(n) ）。 三个反直觉点：① 元素少（< 100）时 map 因缓存友好可能更快；② string key hash 要遍历全字符串，并不\"白嫖\"；③ 标准 unordered_map 因稳定地址保证不能用 open addressing，性能不如 absl::flat_hash_map （快 2-3×）。 基础 vector 和 list 怎么选? 答 ： 默认 vector 。哪怕\"中间频繁插入\"，n 不太大时 vector 也胜出 —— 缓存友好性碾压链表跳指针（vector 遍历 100 万 int ~1ms，list ~50ms，50× 慢）。 仅在 ① 需稳定迭代器/指针 ② 频繁 splice （链表 O(1) 拼接）③ 元素超大且按引用持有 时考虑 list。\"链表查找 O(1)\" 是错觉 —— 必须先 O(n) 找到位置才能 O(1) 插。 基础 iterator 失效有哪些规则? 答 （按容器分类记，5 条）： vector / string ：插入若触发扩容 → 全部失效；不扩容时 ≥ 插入点失效。erase ≥ 删除点失效。 deque ：插入/删除中部 → 全部迭代器失效；首尾 push/pop 时迭代器失效， 但引用还有效 。 list / forward_list ：仅被删除节点的迭代器失效；插入完全不影响其他迭代器。 map / set / multimap / multiset ：插入不失效；erase 仅失效被删除的那一个。 unordered_xxx ：rehash → 迭代器全失效， 但指针/引用仍有效 （因为节点没移动，只重连了 bucket 数组）。"},{"a":"slide-20-2","h":"中高级（拉开差距）","x":"中高级（拉开差距） 中级 SSO 是什么? 为什么 string 移动不一定快? 答 ： SSO (Small String Optimization) ：短字符串（libstdc++ ≤ 15 字节、libc++ ≤ 22 字节）直接存在 string 对象内的 buffer 里， 不堆分配 。 移动短字符串时只能 memcpy 整个 string 对象 —— 因为指针指向自己内部 buffer，不能转让所有权 —— 等价于拷贝。只有大字符串（堆上）才能 O(1) 偷指针。所以 vector<string> 装大量短串时， emplace_back 不会比 push_back 明显快。 中级 unordered_map rehash 是不是导致所有指针失效? 答 ： 迭代器失效，但指针/引用仍有效 。原因：std::unordered_map 必须用 链式哈希 (chaining) —— 每个 key-value 是堆上独立节点，bucket 数组只存指针。rehash 重建 bucket 数组并重连节点的 next 指针， 节点本身位置不变 。 所以 auto* p = &m[\"k\"]; 这种裸指针 rehash 后还能用；遍历顺序则不再保证。这也是 std::unordered_map 性能差（cache miss 严重）的根因 —— 标准要求节点稳定地址 → 不能用 open addressing。 中级 emplace 和 push 区别? 什么场景真有性能差别? 答 ： push_back(T) 接收 已构造 的对象 → 拷贝/移动 1 次进容器； emplace_back(args...) 接收 构造参数 → 在容器内存上 原地构造 ，省掉一次 move/copy。 真有性能差别的场景：① 元素无 noexcept move 构造 → 退化为拷贝；② move 成本高（如长字符串）；③ 临时对象本身构造代价大。 性能巅峰组合 = reserve() + emplace_back() （消除扩容拷贝 + 消除临时对象）。 两坑： explicit 构造上 push_back(1) 可能失败， emplace_back(1) 会直接调 explicit（绕过保护）； vv.push_back({1,2,3}) 常可以， emplace_back({1,2,3}) 常编不过（变参推不出 initializer_list ）。已有对象用 push_back(std::move(x)) 即可。 中级 移动构造为什么要 noexcept? 答 ：vector 扩容承诺 强异常安全 （要么成功，要么状态完全不变）。STL 在扩容时检查 std::is_nothrow_move_constructible<T> ： true → 用 move （快）； false → 退化为 copy （慢但安全，旧 buffer 保留作回滚）。 没标 noexcept 的 move = 偷偷退化成 copy → 性能巨坑。 结论：自定义类型的 move、swap、析构都默认 noexcept 。 中级 红黑树 5 条性质? 为什么 STL 不用 AVL? 5 条性质 ：① 节点非红即黑；② 根黑；③ 叶（NIL）黑；④ 红节点的子必为黑（无连续红）；⑤ 任一节点到所有 NIL 经过的黑节点数相同（黑高相等）。由 ④ + ⑤ 推出最长路径 ≤ 2× 最短路径，高度 ≤ 2·log(n+1) 。 为什么不用 AVL ：AVL 严格平衡（任意节点左右子高差 ≤ 1）→ 查找略快，但 插入/删除旋转更频繁 （删除最坏 O(log n) 次旋转 vs RB 最多 3 次）。STL 关联容器读写都频繁， RB 总成本更低 。Linux 内核 CFS 调度器、epoll 也用红黑树，同理。"},{"a":"slide-20-3","h":"进阶 / 体现深度（让面试官眼前一亮）","x":"进阶 / 体现深度（让面试官眼前一亮） 高级 std::sort 内部用什么算法? 答 ： introsort（内省排序） = quicksort + heapsort + insertion sort 的混合： 开始用 quicksort （常数小、缓存友好）； 递归深度超过 2·log₂(n) 时切换到 heapsort ，防止最坏 O(n²) ； 分段规模 < 16 时用 insertion sort （小规模常数最小）。 最终保证最坏 O(n log n) 且常数因子小。 std::stable_sort 则是 mergesort（保稳定）。 高级 priority_queue 为什么不能 erase 任意元素? 答 ：底层是 vector + heap 算法。 heap 只保证根是最值 ，其余位置无序，定位任意 key 要 O(n) 扫描，做不到 O(log n)。语义上它就是个\"取最值\"适配器，不暴露内部 vector。 三种替代方案：① 改用 std::set / multiset （红黑树，任意删除 O(log n)）；② 懒删除 ：维护一个 deleted set， top() 时 while 命中就 pop；③ Indexed PQ（自己写 / boost d_ary_heap 提供）。 高级 STL 容器为什么基本异常安全? 答 ： 基本保证 = 不泄漏资源 + 容器状态合法 （哪怕状态变了）。STL 实现技巧： 资源 RAII（节点用 unique_ptr 风格管理）； 操作分阶段： 先做可能抛的 （构造新元素、分配内存），最后做 nothrow 的（指针交换、size++）。典型如 vector 扩容：先在新 buffer 全部构造完，成功才析构旧 buffer。 强保证 （操作要么完全成功要么状态不变）只在元素 noexcept move 时给 —— 如 vector::push_back / emplace_back 。 insert / erase 中部仅基本保证。 高级 hash_combine 为什么要混入 0x9e3779b9 ? 答 ：组合多字段 hash 的标准 boost 公式： template < class T> void hash_combine ( size_t & s, const T& v) { s ^= std::hash<T>{}(v) + 0x9e3779b9 + (s<< 6 ) + (s>> 2 ); } 0x9e3779b9 = 2³² / φ （黄金比例倒数）， bit pattern 高度均匀 ，混入它能让相邻输入散布到 hash 空间各处，避免 hash(1,2) 与 hash(2,1) 撞 0； <<6 >>2 进一步打乱位，破除简单 XOR 的对称性 h(a,a)=0 。Knuth 黄金比例 hashing 同源思想。 高级 异构查找（transparent comparator）的意义? 答 ：C++14 (map/set) + C++20 (unordered_map/set) 引入。声明 std::map<std::string, V, std::less<>> （注意是 less<> 不是 less<string> ），就能直接： m. find (std::string_view{ \"abc\" }); // ✅ 不必构造 std::string m. find ( \"abc\" ); // ✅ 不必构造 std::string 对 string key 的容器尤其关键 —— 每次 lookup 省一次堆分配 ，热点路径上能减少几十 % 的开销。unordered 版本要求自定义 hash + equal 都标 using is_transparent = void; 才生效。同源还有 std::equal_to<> 、自定义 std::less<> 仿函数。"},{"a":"slide-20-4","h":"速答示范（第 11 题）","x":"速答示范（第 11 题） 问 ：std::sort 内部用什么算法？ 答 ： introsort （内省排序）— 开始用 quicksort，递归深度超过 2*log2(n) 时切换到 heapsort 防止最坏 O(n²)；分段规模小（<16）用 insertion sort 。这样保证最坏 O(n log n) 且常数因子小。 背完 15 题 + 能即兴讲 1 个真实工程例子 → C++ 容器章节面试 95% 安全。完整 15 题速答详解见下一张 slide。"},{"a":"end","h":"本专题结束 · 下一步","x":"21 本专题结束 · 下一步 STL 容器是 C++ 面试最基础也最难答全的话题之一。这份 PPT 已涵盖 28 个核心点 — 反复看 3 遍，每遍能讲出多 1 个细节。 同期资料 「 STL 算法 PPT 」(下一份) — 排序 / 查找 / heap / partition 「 值类别 + 拷贝控制 」(下下份) — Rule of 5 / RVO / xvalue 「 cpp_modern_features_ppt.html 」 — 现代特性 (已有) 「 cpp_object_model.html 」 — 对象模型 (已有) 建议练习 实现一个简化版 vector（含扩容 + 异常安全） 实现一个 LRU Cache（list + unordered_map） 用 godbolt.org 看 vector::push_back 的汇编 对比测试 std::unordered_map vs absl::flat_hash_map 为自定义 struct 写 std::hash 特化 + benchmarks ← 上一篇 · 模板与 TMP 下一篇 · 算法 → 相关 · 智能指针 相关 · 设计模式 目录 主页 STL 容器深度剖析 — 内部结构 · 复杂度 · 迭代器失效 · 选型 — 2026 ↑"}]}];
