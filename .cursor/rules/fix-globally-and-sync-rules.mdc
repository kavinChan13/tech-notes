---
description: 修复格式/共性问题时全仓库排查并修复，再回写规则防复发
alwaysApply: true
---

# 共性问题：全量修复 + 回写规则

修好一个格式类/结构类/主题类问题后，不要只改当前文件，按以下闭环处理：

1. **判断是否为共性问题**：这个问题是不是模板、共享样式、迁移遗留或习惯写法导致的，
   其他文件很可能同样存在？（如硬编码深色、缺失/错位的 `<head>` 引入、旧的 `data-tn-theme`、
   坏锚点、缺 `<summary>`、导航块不一致等。）
2. **全仓库排查**：用 Grep/Glob 搜出所有可能同样出问题的文件，列出清单，别凭印象漏掉。
3. **批量修复**：对确认有问题的文件统一修复，保持改法一致；改完运行
   `python scripts/audit_guides.py` 复检，并在浅/深两种模式下抽查。
4. **回写规则**：把「正确做法 / 禁止写法」补进 `.cursor/rules/` 里对应的规则文件
   （或新建一条），让以后新增/修改的内容不再犯同样的错。

范围大或有风险时，先用 ask_question 与我确认修复清单和方案，再动手。
