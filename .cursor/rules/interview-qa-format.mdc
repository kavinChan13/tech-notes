---
description: 面试题统一用可折叠 details.qa + 等级徽章 + ol.qa-list 分层格式
globs: **/*.html
alwaysApply: true
---

# 面试题格式统一（可折叠 + 分层）

全站面试题只有**一种**合法写法：可折叠的 `details.qa`，题号交给 `ol.qa-list`，
难度由 `<summary>` 里的等级徽章标出。样式**只**在 `assets/article.css` 第 4 节维护，
页面不要再写一份。参考实现：`cpp/value_categories_guide.html` §13。

## 唯一合法结构

```html
<h3 id="s13-1">13.1 基础（必答）</h3>
<p class="qa-hint">点击每道题展开参考答案（先自测，再对照）。</p>
<ol class="qa-list">
  <li><details class="qa">
    <summary><span class="lvl basic">基础</span>什么是 lvalue / rvalue / xvalue / prvalue?</summary>
    <div class="ans">
      <p>用「有无<strong>身份</strong> × 能否<strong>移动</strong>」两维度划分……</p>
      <ul><li>……</li></ul>
    </div>
  </details></li>
</ol>
```

硬性约定：

- 折叠容器必须是 `<details class="qa">`，直接子元素**只有** `<summary>` 和 `<div class="ans">`（各一个）。
- 每个 `details.qa` 必须包在 `<ol class="qa-list"> > <li>` 里；题号由 `ol` 自动生成，
  **不要**在 `<summary>` 里手写 `Q1:` / `1.` / `第 3 题`。
- `<summary>` 的**第一个**子元素必须是等级徽章 `<span class="lvl …">`。
- 跨 `h3` 分组续号用 `<ol class="qa-list" start="7">`；每个 `section.chapter` 内从 1 重新计数。
- 答案容器用 `.ans`。`.a` 只是**尚未迁移页面**的兼容别名，新增/改动一律用 `.ans`。
- 答案里可放 `<p>` `<ul>` `<ol>` `<pre>` `<table>`，以及「标签 + 说明」的 `.point` 行：

```html
<div class="point deep"><div class="label">dynamic_cast</div><div>唯一有运行时检查，依赖 RTTI……</div></div>
```

`.point` 变体：默认（accent）/ `.warn` / `.deep` / `.bad` / `.good`。

## 等级徽章

| 标签 | class | 语义 |
| --- | --- | --- |
| 基础 | `lvl basic` | 必答，答不出直接挂 |
| 原理 | `lvl principle` | 机制 / 底层原理 |
| 中级 | `lvl mid` | 区分熟练度 |
| 进阶 | `lvl pro` | 更深的用法与陷阱 |
| 高级 | `lvl adv` | 体现深度、加分项 |
| 工程 | `lvl eng` | 生产实战 / 排查 |
| 专家 | `lvl expert` | Staff / Principal 上限题 |
| 安全 | `lvl risk` | UB / 安全红线 |

- 主线三档是 **基础 / 中级 / 高级**，新页面优先只用这三档。
- 调试、汇编、ABI 这类系列已沿用 **基础 / 原理 / 进阶 / 工程**（个别有 `安全`），
  迁移这些页面时**保留原标签**，只换 DOM 与折叠交互，不要硬改成三档。
- 按职级分层（L4/L5/L6/L7、P5–P8）的页面用 `basic` / `mid` / `adv` / `expert` 四档，
  徽章文案保留原来的层级信息，如 `L4 初级`、`L7+ 专家`。
- 每个徽章 `class` 里**只能有一个**等级 class；不要再叠 `pill` / `cyan` 等旧类，
  也不要写 `style="background:…"` 硬编码颜色（颜色由 `--lvl-c` 决定）。

## 正文里的单独折叠块：`qa standalone`

不属于任何题集、只是正文里一个可展开的问答或案例，加 `standalone`：

```html
<details class="qa standalone">
  <summary><strong>案例 1 · IM 网关 RSS 每天涨 1G</strong> <span class="pill amber">假泄漏</span></summary>
  <div class="ans">…</div>
</details>
```

- 不用包 `ol.qa-list`，等级徽章可选，但答案容器仍必须是 `.ans`。
- ❌ 不能拿它绕过迁移：`<summary>` 里出现 `Q1:` / `1.` 这类**题号**一律报错——
  那说明它是题集的一员，应该收进 `ol.qa-list`。

## 什么时候用 h3 分组

- 题目数 **≥ 12** 且已按难度排序 → 用 `h3` 分三段：`13.1 基础（必答）` /
  `13.2 中级（区分熟练度）` / `13.3 高级（体现深度）`，后两段的 `ol` 加 `start=`。
  这些 `h3` 的 `id` 要挂进 TOC（见 anchors-and-audit 规则）。
- 题目数少（约 6–10 道）→ 一个扁平 `ol.qa-list` 就够，难度靠徽章区分，不必强行切成三段。

## 禁止写法

- ❌ 不可折叠的面试题块：
  `<div class="qa"><div class="q"><span class="lvl">基础</span>…</div><p>答案</p></div>`、
  `<div class="card"><h4>Q1: …</h4><p>答案</p></div>`、
  `<div class="phase-card"><h4>Q1: …</h4>…</div>`、
  一连串 `<div class="box q">问题</div><p>答案</p>`。
- ❌ 裸 `<details><summary>Q1: …</summary><div>…</div></details>`（缺 `.qa` / `.ans` / 徽章）。
- ❌ 在页面内联 `<style>` 里定义 `.qa` / `.qa-list` / `.lvl` / `.ans` / `.qa .q`——
  一律删掉，靠 `assets/article.css`（按 file-format-consistency 规则正常引入即可）。
- ❌ 徽章或折叠箭头写死颜色。颜色走 `--lvl-c` 与 `--info/--yellow/--purple/...` 等 token
  （见 light-dark-theming 规则）。

单独一个 `<div class="box q">` 作为正文里的「❓ 面试问」提示框**仍然合法**，
不算面试题集；只是不能拿它当 Q&A 列表用。

## 只有题目、没有答案的清单

有些页面的「面试题（15 道）」只是一串 `<ol><li>问题</li></ol>` 自测清单，**没有答案**
（`perf-debug/` 的 `abi_compatibility_guide` / `cmake_ci_guide` / `low_latency_guide` /
`modern_libs_guide` / `observability_guide` / `performance_guide` / `sanitizers_guide` /
`security_hardening_guide`，以及 `stl/concurrency_patterns_guide`，合计约 150 题）。

- 这类清单**不算**本规范约束的题集，保留 `<ol><li>` 形态即可，不要为了"统一格式"
  套上 `details.qa` 空壳。
- 要把它升级成折叠题集，必须**先补出答案**（那是内容工作，不是格式工作），
  补完再按本规范转换。

## 迁移状态与自检

- ✅ 已迁移：`cpp/`（237 道）、`perf-debug/`（71 道 + 8 个 `standalone`）、`stl/`（45 道）。
- ⏳ 待迁移（按批次推进）：`architect/` `communication/`（`details.interview-card`）、
  `system/`（裸 `<details>` / `div.qa-item` / `div.box.q`）、`ai-infra/` `bigdata/`
  `ai-native/` `neural-networks/` `reinforcement/` `interview/`（`details.qa` + `.a`，
  其中约 199 篇**完全没有等级徽章**，等级需按题目内容逐题判定）、
  `management/` `pm/`（`div.qa > div.q`）。
  **新增面试题一律用本规范，不要跟着旧格式写。**

### PPT 型页面：问题 slide + 答案 slide 要合并

`stl/` 三篇 PPT 原本是「一张 slide 列 15 道题，下一张 slide 给 15 段答案」。
折叠格式本身就是「先自测再对照」，两张 slide 并存等于把题干写两遍——
迁移时**合并成一张**，并同步四处：TOC 去掉答案 slide 那条、保留 slide 的标题、
`id="end"` 的 `.num` 减一、hero 里 `<span>N 大章节</span>` 减一。
「速答示范」面板讲的是**表达方式**而不是答案，合并后作为末尾一个 `h3` 保留。

改完后必须两条都跑干净：

```bash
python scripts/audit_interview_qa.py   # Total problems: 0（默认只查已迁移目录）
python scripts/audit_guides.py         # Total problems: 0
```

`audit_interview_qa.py` 会校验上面全部约定，并对已迁移目录（`MIGRATED_DIRS`）额外报旧格式残留。
迁移新目录的流程：先 `--migrated <dir>` 打开严格模式改到 0，再把该目录加进脚本的
`MIGRATED_DIRS`、更新本节迁移状态。`--all` 可以查看全站还剩多少积压。
新增/大改笔记页后同样别忘了 `python scripts/build_search_index.py`。
