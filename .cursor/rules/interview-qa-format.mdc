---
description: 面试题统一用可折叠 details.qa + 等级徽章 + ol.qa-list 分层格式
globs: **/*.html
alwaysApply: true
---

# 面试题格式统一（可折叠 + 分层）

全站面试题只有**一种**合法写法：可折叠的 `details.qa`，题号交给 `ol.qa-list`，
难度由 `<summary>` 里的等级徽章标出。样式**只**在 `assets/article.css` 第 4 节维护，
页面不要再写一份。参考实现：`cpp/value_categories_guide.html` §13。

## 唯一合法结构

```html
<h3 id="s13-1">13.1 基础（必答）</h3>
<p class="qa-hint">点击每道题展开参考答案（先自测，再对照）。</p>
<ol class="qa-list">
  <li><details class="qa">
    <summary><span class="lvl basic">基础</span>什么是 lvalue / rvalue / xvalue / prvalue?</summary>
    <div class="ans">
      <p>用「有无<strong>身份</strong> × 能否<strong>移动</strong>」两维度划分……</p>
      <ul><li>……</li></ul>
    </div>
  </details></li>
</ol>
```

硬性约定：

- 折叠容器必须是 `<details class="qa">`，直接子元素**只有** `<summary>` 和 `<div class="ans">`（各一个）。
- 每个 `details.qa` 必须包在 `<ol class="qa-list"> > <li>` 里；题号由 `ol` 自动生成，
  **不要**在 `<summary>` 里手写 `Q1:` / `1.` / `第 3 题`。
- `<summary>` 的**第一个**子元素必须是等级徽章 `<span class="lvl …">`。
- 跨 `h3` 分组续号用 `<ol class="qa-list" start="7">`；每个 `section.chapter` 内从 1 重新计数。
- 答案容器用 `.ans`。`.a` 只是**尚未迁移页面**的兼容别名，新增/改动一律用 `.ans`。
- 答案里可放 `<p>` `<ul>` `<ol>` `<pre>` `<table>`，以及「标签 + 说明」的 `.point` 行：

```html
<div class="point deep"><div class="label">dynamic_cast</div><div>唯一有运行时检查，依赖 RTTI……</div></div>
```

`.point` 变体：默认（accent）/ `.warn` / `.deep` / `.bad` / `.good`。

## 等级徽章

| 标签 | class | 语义 |
| --- | --- | --- |
| 基础 | `lvl basic` | 必答，答不出直接挂 |
| 原理 | `lvl principle` | 机制 / 底层原理 |
| 中级 | `lvl mid` | 区分熟练度 |
| 进阶 | `lvl pro` | 更深的用法与陷阱 |
| 高级 | `lvl adv` | 体现深度、加分项 |
| 工程 | `lvl eng` | 生产实战 / 排查 |
| 专家 | `lvl expert` | Staff / Principal 上限题 |
| 安全 | `lvl risk` | UB / 安全红线 |

- 主线三档是 **基础 / 中级 / 高级**，新页面优先只用这三档。
- 调试、汇编、ABI 这类系列已沿用 **基础 / 原理 / 进阶 / 工程**（个别有 `安全`），
  迁移这些页面时**保留原标签**，只换 DOM 与折叠交互，不要硬改成三档。
- 按职级分层（L4/L5/L6/L7、P5–P8）的页面用 `basic` / `mid` / `adv` / `expert` 四档，
  徽章文案保留原来的层级信息，如 `L4 初级`、`L7+ 专家`。
- 每个徽章 `class` 里**只能有一个**等级 class；不要再叠 `pill` / `cyan` 等旧类，
  也不要写 `style="background:…"` 硬编码颜色（颜色由 `--lvl-c` 决定）。

## 正文里的单独折叠块：`qa standalone`

不属于任何题集、只是正文里一个可展开的问答或案例，加 `standalone`：

```html
<details class="qa standalone">
  <summary><strong>案例 1 · IM 网关 RSS 每天涨 1G</strong> <span class="pill amber">假泄漏</span></summary>
  <div class="ans">…</div>
</details>
```

- 不用包 `ol.qa-list`，等级徽章可选，但答案容器仍必须是 `.ans`。
- ❌ 不能拿它绕过迁移：`<summary>` 里出现 `Q1:` / `1.` 这类**题号**一律报错——
  那说明它是题集的一员，应该收进 `ol.qa-list`。

## 什么时候用 h3 分组

- 题目数 **≥ 12** 且已按难度排序 → 用 `h3` 分三段：`13.1 基础（必答）` /
  `13.2 中级（区分熟练度）` / `13.3 高级（体现深度）`，后两段的 `ol` 加 `start=`。
  这些 `h3` 的 `id` 要挂进 TOC（见 anchors-and-audit 规则）。
- 题目数少（约 6–10 道）→ 一个扁平 `ol.qa-list` 就够，难度靠徽章区分，不必强行切成三段。

## 禁止写法

- ❌ 不可折叠的面试题块：
  `<div class="qa"><div class="q"><span class="lvl">基础</span>…</div><p>答案</p></div>`、
  `<div class="card"><h4>Q1: …</h4><p>答案</p></div>`、
  `<div class="phase-card"><h4>Q1: …</h4>…</div>`、
  一连串 `<div class="box q">问题</div><p>答案</p>`。
- ❌ 裸 `<details><summary>Q1: …</summary><div>…</div></details>`（缺 `.qa` / `.ans` / 徽章）。
- ❌ 在页面内联 `<style>` 里定义 `.qa` / `.qa-list` / `.lvl` / `.ans` / `.qa .q`——
  一律删掉，靠 `assets/article.css`（按 file-format-consistency 规则正常引入即可）。
- ❌ 徽章或折叠箭头写死颜色。颜色走 `--lvl-c` 与 `--info/--yellow/--purple/...` 等 token
  （见 light-dark-theming 规则）。

单独一个 `<div class="box q">` 作为正文里的「❓ 面试问」提示框**仍然合法**，
不算面试题集；只是不能拿它当 Q&A 列表用。

## 只有题目、没有答案的清单

有些页面的「面试题（15 道）」只是一串 `<ol><li>问题</li></ol>` 自测清单，**没有答案**。
现存 5 页、合计 75 题，全在 `perf-debug/`：`abi_compatibility_guide` / `cmake_ci_guide` /
`modern_libs_guide` / `observability_guide` / `security_hardening_guide`。
（原先记的 9 页里，`low_latency_guide` / `performance_guide` / `sanitizers_guide` 与
`stl/concurrency_patterns_guide` 已经补过答案并迁走了。）

- 这类清单**不算**本规范约束的题集，保留 `<ol><li>` 形态即可，不要为了"统一格式"
  套上 `details.qa` 空壳。
- 要把它升级成折叠题集，必须**先补出答案**（那是内容工作，不是格式工作），
  补完再按本规范转换。

## 迁移状态与自检

- ✅ 已迁移：`architect/`（**102 道**，见下）、`cpp/`（249 道）、`perf-debug/`（71 道 + 8 个 `standalone`）、`stl/`（45 道）、
 `desktop-gui/`（48 道，四篇 Qt 各 12 道，基础/中级/高级三档）、
 `algorithms/`（180 道，十五篇各 12 道，基础/中级/高级三档，建目录时即按本规范书写）、
 `robotics-comm/`（96 道，八篇各 12 道）、
 `system/`（**222 道 + 4 个 `standalone`**，18 篇全覆盖，见下）、
 `embedded-realtime/`（**60 道**，五篇各 12 道，随「骨架页补厚」一并新写）。
- ⏳ 待迁移（按批次推进）：`communication/`（`details.interview-card`）、
 `ai-infra/` `bigdata/` `ai-native/` `neural-networks/` `reinforcement/` `interview/`
 （`details.qa` + `.a`，其中约 199 篇**完全没有等级徽章**，等级需按题目内容逐题判定）、
 `management/` `pm/`（`div.qa > div.q`）。
 **新增面试题一律用本规范，不要跟着旧格式写。**

### `architect/` 的迁移记录

这个目录长期卡在一个页面上：`cloud_native_kubernetes.html` 的 30 道题用的是
`details.qa` + `div.a` + 缩写徽章（`lvl b` / `lvl i` / `lvl a`），共 91 处不合规，
它一个人挡着整个目录进不了严格模式。迁移很直接：

- 徽章映射 `b → basic`、`i → mid`、`a → adv`，文案统一成「基础 / 中级 / 高级」
 （原来写的是「基础 / 进阶 / 深入」）。
- 30 个 `div.a` → `div.ans`；每个 `details.qa` 包进 `<ol class="qa-list"><li>`。
 这 30 道题分布在 12 个 `section.chapter` 里，各章题目连续、中间没有 `h3` 分组，
 所以都用扁平 `ol`，没用到 `start=`。
- 删掉内联 `<style>` 里 4 条 `.qa .lvl*` 规则——该页 30 个 `<details>` 全是 `.qa`，
 整段删掉是安全的，徽章配色交给 `article.css` 的 `--lvl-c`。

⚠️ 迁移过程中 `audit_guides.py architect` 一度报 38 个失效锚点，全部来自**另一个
正在被写入的新文件**。这正是「别在文件还没写完时判读结果」那条：定稿后重跑即为 0。
并行改同一个目录时，判读自检结果前先确认没有别的任务在写。

### `system/` 的迁移记录（三种旧格式一次清完）

这个目录同时存在三种旧壳，是目前为止最杂的一个，迁移时按来源分别处理：

| 旧格式 | 出现在 | 处理 |
| --- | --- | --- |
| 裸 `<details>`（无 `.qa` / `.ans` / 徽章） | `os_fundamentals` `networking_guide` | 是题集的收进 `ol.qa-list`；只是正文可展开块的改成 `details.qa standalone` |
| `div.qa-item` + `div.a` | `io_linux_kernel_guide` `filedescriptor_deepdive` | 问题标题进 `<summary>`（去掉 `Q1:` 题号），答案进 `.ans` |
| `div.box.q` + 紧跟一段答案 | `linux_device_driver` | 同上；单独作为「❓面试问」提示框的 `box.q` 保留不动 |
| 只有题目、没有答案的 `<ol><li>` | `async_networking_guide` §17.1（15 道） | **先补出答案**再转换——那是内容工作 |

两个经验：

- **`standalone` 不是垃圾桶。** 判据是「`<summary>` 里是不是一个真正的面试问题」。
 `os_fundamentals` 里 Page Fault 三种类型、mmap 代码示例这类正文展开块才该用它。
- **分组编号会和既有小节撞车。** `async_networking_guide` 的 `s17-2` / `s17-3` 已被
 「Asio 速查」「跨库协程互通速查」占用，新分组只能用 `s17-1` / `s17-1b` / `s17-1c`，
 并把那几张速查表的**标题文字**顺延成 17.4/17.5/17.6（`id` 不动，否则断链）。
 动手前先 `Grep 'id="sN'` 把已占用的 id 列出来。

### 迁移时顺手删掉内联的 `details` / `summary` 样式

页面内联 `<style>` 里常有一整套 `details{…}` / `summary::before{…}` 的自绘折叠样式。
迁移到 `.qa` 之后，`article.css` §4 的 `details.qa > summary` 特异性更高、全都会赢，
这些内联规则**大部分变成死代码**——唯一还生效的是 `article.css` 没设的属性，
比如 `details{padding:.8rem 1.2rem}` 会在 summary 自己的 padding 之外再加一层，
让这一页的题卡比全站其它页胖一圈。

- 页面里**所有** `<details>` 都是 `.qa` 时 → 内联的 `details` / `summary` 规则整段删掉。
- 还有正文用的裸 `<details>` 时 → 保留（它们确实在服务那些元素），不要顺手删。
- 全站扫过一遍：18 个页面有这类内联规则，其中 17 个在**尚未迁移**的目录里
 （裸 `<details>` 仍是那里的合法格式），只有 `perf-debug/deadlock_debugging_guide.html`
 是「12 个 details 全是 `.qa`」的纯死代码，已清理。
 **所以这不是一个需要现在全量修的共性问题**，而是各目录迁移时的一个检查项。

### PPT 型页面：问题 slide + 答案 slide 要合并

`stl/` 三篇 PPT 原本是「一张 slide 列 15 道题，下一张 slide 给 15 段答案」。
折叠格式本身就是「先自测再对照」，两张 slide 并存等于把题干写两遍——
迁移时**合并成一张**，并同步四处：TOC 去掉答案 slide 那条、保留 slide 的标题、
`id="end"` 的 `.num` 减一、hero 里 `<span>N 大章节</span>` 减一。
「速答示范」面板讲的是**表达方式**而不是答案，合并后作为末尾一个 `h3` 保留。

改完后必须两条都跑干净：

```bash
python scripts/audit_interview_qa.py   # Total problems: 0（默认只查已迁移目录）
python scripts/audit_guides.py         # Total problems: 0
```

`audit_interview_qa.py` 会校验上面全部约定，并对已迁移目录（`MIGRATED_DIRS`）额外报旧格式残留。
迁移新目录的流程：先 `--migrated <dir>` 打开严格模式改到 0，再把该目录加进脚本的
`MIGRATED_DIRS`、更新本节迁移状态。**目录本身不用登记**——脚本从 `scripts/_site.py`
自动发现，`MIGRATED_DIRS` 只表示"这个目录已经迁移完、可以开严格模式"。

`--all` 现在覆盖全部 19 个内容目录（此前 `interview/` `management/` `pm/` `ai-templates/`
根本不在扫描范围内，积压被低估）。当前实况：全站 **3436 道题、4448 处旧格式**，
其中已迁移目录 **1611 道 + 13 个 `standalone`** 全部合规。

新增/大改笔记页后同样别忘了 `python scripts/build_search_index.py`。
