---
description: 明暗主题下的背景/前景配色必须随主题正确翻转
globs: **/*.html
alwaysApply: true
---

# 明暗切换配色

全站统一用 `data-theme` 切换：**默认无属性 = 浅色**，`<html data-theme="dark">` = 深色。
持久化用 `localStorage['theme']`，开关按钮 `#tn-theme-toggle`（文章）/ `#themeBtn`（主页/目录），
逻辑统一在 `assets/site-theme-toggle.js`。核心要求：
**浅色模式下所有内容背景必须是浅色，深色模式下所有内容背景必须是深色**，前景文字同理，保证对比度。

- 颜色一律走 CSS 变量（定义在 `assets/site-tokens.css`，浅色为 `:root`、深色为 `html[data-theme="dark"]`）：
  背景用 `--bg` `--bg-2` `--bg-3` `--bg-card` `--bg-card-2` `--code-bg`，
  文字用 `--text` `--text-dim` `--text-mute`，边框用 `--border` `--border-2`，强调色用 `--accent*`。
- ❌ 禁止在内联 `<style>` 里硬编码深色背景（如 `background:#10172b` / `#0c1120`），
  这类硬编码在浅色模式下不会翻转，会露出深色块。
- ✅ 用变量：`background:var(--bg-card)` / `background:var(--code-bg)`；
  半透明语义色用 `color-mix(in srgb, var(--green) 12%, var(--bg-card))` 之类，底色必须是变量。
- 内联 SVG 图：`fill`/`stroke` 优先用 `currentColor` 或主题变量。若沿用了写死的深色面（历史图表），
 不要在页面里硬编码浅色覆盖——运行 `python scripts/gen_svg_map.py`，它会扫描全仓库 SVG 填充色并在
 `assets/article.css` 的 `AUTO-SVG` 标记之间生成"浅色模式深色面→浅色、亮字→深字"的重映射
 （`html:not([data-theme="dark"]) svg [fill="#..."]`），深色模式保留原样。新增带图的页面后重跑该脚本。

### ⚠️ `gen_svg_map.py` 只认 `fill="…"` 属性，够不着 CSS 里的颜色

它生成的是**属性选择器**。所以这三处写死的深色它一个都扫不到，跑再多遍都没用：

- `@keyframes` 里的 `fill`（`memory_optimization_guide.html` 的 `lineBounce` 就是这样，
 动画高亮态写死 `fill:#3a1414`，浅色模式下闪出一块深褐）；
- 页面 `<style>` 里按 class 给的 `svg .foo{fill:#1e293b}`；
- 元素上的 `style="fill:#0f172a"`。

这几种一律**直接写主题变量**：`fill:var(--bg-card-2)`、
`fill:color-mix(in srgb, var(--red) 14%, var(--bg-card))`。

还有一个相邻的坑：**`<path>` 不写 `fill` 时 SVG 默认填黑**。用 `<path>` 画线（只给
`stroke`）必须补 `fill="none"`。两种形态，第二种最阴险：

- **单段曲线**：`networking_guide.html` 拥塞控制图里漏了一条，直接渲染成一整块黑色楔形。
- **多段直线**：`<path d="M120 98 v16 M290 98 v16 …">` 这种用一个 `<path>` 画五条刻度线的，
 每条单独看都是零面积、填了也看不见，**但多段子路径会被当成一个整体区域填充**，
 于是横跨全部子路径的包围盒变成一条黑带。`observability_ebpf.html` 的 Verifier 图就是这样。

全站扫过一遍，「有 `stroke`、没 `fill`、且多段子路径」的一共 7 处，已全部补上
`fill="none"`。给只描边的 path 补 `fill="none"` **永远是安全的**——作者本意就是画线。

这一类只能靠真渲染发现：`node scripts/check_diagram_contrast.mjs <page.html>`。
- 内联 `<style>` 里的深色背景/亮色文字：用变量或 `python scripts/fix_inline_darks.py` 批量转 token
  （深色底→`var(--bg-card)`/`color-mix(... var(--accent*) ...)`，亮字→`var(--text)`），使其随主题翻转。
- 内联 `<style>` 里**深色主题专用的浅色字/边框**（如 `color:#e4e9f5`、`#7dd3fc`、`border-*:#2a3556`）
  在浅色模式下会「浅字浅底」或边框过深：运行 `python scripts/fix_light_mode_inline_colors.py` 批量替换为
  `var(--text)`/`var(--accent*)`/`var(--border*)`；新增 guide 页大改内联样式后建议一并重跑。
- 共享样式（`assets/*.css`，如 `page-shell.css`、`guide-theme.css`）里除了 `:root`/`.ps` 的 `--*` 变量
  **定义**（深色默认值，light 由 `site-tokens.css`/`article.css` 覆盖），**组件的 `background` 一律不要写死深色**
  （如 `.ps{background:radial(#14213d…)}`、`.ps .box.tip{background:#1a1a2e}`）——要用 `var(--ps-*)`/`var(--bg-card)`
  或 `color-mix(... var(--ps-COLOR) …, var(--ps-card))`，否则整类 `.ps` 页在浅色下都会露深块。
## 主题初始化必须在 `<head>` 里、渲染之前

**每一个** `.html`（包括主页、`*_directory.html`、模板索引这些"外壳页"）的 `<head>` 开头都要有：

```html
<!-- tn-theme-init --><script>try{var d=document.documentElement,t=localStorage.getItem('theme')||localStorage.getItem('tn-theme');if(t==='dark')d.setAttribute('data-theme','dark');}catch(e){}</script>
```

- 两个 key 都要读：`theme` 是当前的，`tn-theme` 是旧文章主题系统留下的，只读一个会让
  用户在文章页设过的偏好在首页失效。
- ❌ 别把这段逻辑放到 `</body>` 前的脚本里。主页和 16 个目录页曾经就是这样
  （`index.html` 在第 1454 行才设属性），深色用户每次打开都先闪一屏白。
- 按钮与持久化交给共享脚本，页面里**不要**再写一份 toggle：
  - 文章 / 学习路径页 → `assets/site-theme-toggle.js`
  - 外壳页（主页 / 目录页 / 模板索引）→ `assets/directory-theme.js`
    （外壳页不用前者，因为它会额外注入 `.back-top` 按钮，而外壳页不加载 `article.css`，
    那颗按钮会以无样式状态露出来）
  - 两者写入的都是 `localStorage['theme']`，深色设 `data-theme="dark"`、浅色**移除属性**。

## 共享 CSS 里不要留"深色兜底值"

`assets/*.css` 里写 `var(--card, #111c33)` 这种 fallback：`site-tokens.css` 永远会定义
`--card`，所以兜底值**永远不会生效**，它唯一的作用是把一个深色硬编码摆在那儿等人复制到
真的会生效的地方去。`mock-interview.css` 曾有 171 处这样的写法，外加 9 处真的会生效的
`#0a1020` / `rgba(11,18,32,…)`，让 4 个 `*_cards.html` 在浅色模式下整块保持深色。

- ✅ `site-tokens.css` 已定义的 token 直接写 `var(--card)`，不带 fallback。
- ✅ 半透明面用 `color-mix(in srgb, var(--bg) 92%, transparent)`，不要 `rgba(11,18,32,.92)`。
- ❌ `rgba(255,255,255,.06)` 这种"白色微提亮"在浅色下等于隐形，
  改用 `color-mix(in srgb, var(--text) 7%, transparent)`。

完成后务必在浅色与深色两种模式下各自检查一遍：不应出现浅底深块或深底浅块。
`python scripts/audit_page_shell.py` 会自动校验 tn-theme-init、toggle 单一化、
样式表顺序与内联深色硬编码。
