---
description: 笔记内容通俗易懂且有深度，善用图/表/动画增强可视化与理解
globs: **/*.html
alwaysApply: true
---

# 内容通俗易懂 + 有深度 + 可视化

新增/更新内容时，先讲清「是什么、为什么、怎么用」，再深入细节。**入口通俗易懂、内核有深度**：
起点让不熟悉该主题的读者也能看懂，但整体深度要对齐**高级开发工程师 / 架构师**的水准，
经得起追问，而不是停留在科普。

- 抽象概念配**生活类比**（用 `.analogy` 卡片），关键名词给**名词解释**（`.def` 卡片）。
- 优先用**可视化**代替大段文字：
  - 结构/流程/关系 → 内联 SVG 图或 HTML/CSS 图（配色走主题变量）。
  - 对比/参数/步骤 → 表格。
  - 机制演示 → 轻量交互/动画（`.demo` 卡片、CSS transition、可选少量原生 JS），帮助理解运行过程。
- 代码示例短小可运行，配「坏例 ❌ / 好例 ✅」对照更佳。
- 循序渐进：由浅入深、由整体到细节，避免一上来堆术语。

## 🚨 图优先 · 代码 3–6 块 · 不要堆字数

**这一节覆盖此前「一页要 ≥16000 中文字 / ≥12 个 `<pre>` / ≥10 张表」那套扩张规格。**
那套指标把「深度」等同于「体量」，结果是一批页面变成**代码墙 + 文字墙**：
`system/filedescriptor_deepdive.html` 一页 53 个 `<pre>`、`system/ipc_guide.html` 49 个、
`system/numa_memory_advanced.html` 49 个，很多只是把上一段话用代码再写一遍。
全站现状可以自己看：`python scripts/report_page_density.py --all --over`。

### 三条硬指标

| 维度 | 目标 | 判据 |
| --- | --- | --- |
| `<pre>` | **一页 3–6 块** | 长内核 / 协议篇可放宽到 10，再多就是代码墙 |
| 信息性 SVG | **多于 `<pre>`** | 讲流程/结构/状态/时序的地方都该先问「能不能画」 |
| 可编辑正文 | **砍 20–30%** | 见下面「看哪一列」；深度靠洞见，不靠字数 |

表格**不是问题**，对比表是骨架，不要为了压缩去删。

### ⚠️ 看「可编辑」那一列，不要看「正文」

`report_page_density.py` 的「正文」列有两处会让压缩幅度**看起来比实际小**：

- 它**不数 `<pre>` 里的字，却数表格单元格**。把命令输出改写成
 「字段 → 含义 → 判据」的表正是我们鼓励的做法，可那批文字会从「不计数」
 搬进「计数」——`network_mechanisms` 整页只显示 −22%，按同口径折算实际是 −30%。
- **面试题答案不该压**，但它也在「正文」里。`socket_programming` 的面试题占了
 全页三分之一，整页 −20.5%，可编辑部分其实砍了 −27.7%。

所以脚本另列了「可编辑」= 正文 − 面试题。**判断压得够不够看这一列。**

### 代码留什么、删什么

留：**API 本身就是课**（`cmsghdr` 的 `CMSG_SPACE` vs `CMSG_LEN`、长度前缀的三道校验），
或者**必须能跑才说得清**（first-touch 落点验证、`ls | grep` 的 fork+dup2 编排）。

删：
- 把上一段话用代码再写一遍的——文字已经说清了，代码只是噪音。
- 整段贴内核头文件 / 结构体定义——改成一张标注了关键字段的图。
- 每个 API 各配一小块的碎片代码——合并成一张「API → 行为 → 坑」的表。
- 一串 `sysctl` / `cat /proc/...` / `docker run` 命令堆——改成「命令 → 目的 → 判据」的表。
- 长输出（`ss -tin`、`lsof`、`dmesg`）——只留能回答一个具体问题的那几行，其余进表。

### 什么该画成图

一段文字如果在描述**流程、结构、状态迁移、时序、层级、数据布局**，它就该是图。
判断方法：读完这段，脑子里是不是在画一张图？是就直接画出来，别让读者自己画。

图必须能替代文字，而不是配插图：**图画清楚了，就把那段文字删掉**，不要图文各讲一遍。

### 图的技术要求

- 信息性图：`role="img"` + 中文 `aria-label`；装饰性图才用 `aria-hidden="true"`。
- 包在 `overflow-x:auto` 的容器里；宽图用 `viewBox` + `style="min-width:…px;height:auto"`，
  不要写死一个 390px 下会撑破又没有滚动条的绝对宽。
- 颜色**只用主题变量**：`var(--accent)` `var(--green)` `var(--info)` `var(--text)`
  `var(--text-dim)` `var(--bg-card)` `var(--border)` `var(--red)` `var(--yellow)` `var(--purple)`；
  `.ps` 页用 `var(--ps-*)`。
  ❌ **禁止** `fill="#1a2234"` 这类硬编码深色——浅色模式下就是一块黑。
- 用 mermaid 的话必须走 `assets/mermaid-theme.js`，见 self-contained-assets 规则。

### 自检

```bash
python scripts/report_page_density.py <目录名>        # 按 pre 数排序
python scripts/report_page_density.py --all --over    # 只列代码墙
node scripts/check_diagram_contrast.mjs <page.html>   # 图在浅/深两种模式下都不能反色
```

`report_page_density.py` **只报数不判失败**——密度是编辑判断，不是能自动化的对错。
它的作用是让「这一页是不是代码墙」变成一个能看见的数字，而不是各人凭印象。

## 深度要求（高级工程师 / 架构师视角）

在通俗的基础上，内容要有以下至少几项深度，而不只是罗列 API 用法：

- **底层原理与机制**：讲清「为什么这样设计」「背后如何运作」，必要时下探到内存模型、
  执行/调度、协议、数据结构、编译器/内核等原理层。
- **权衡与取舍**：给出方案对比、适用与不适用场景、复杂度/性能/成本的 trade-off，而非只给单一「正确答案」。
- **工程与规模化视角**：结合真实生产场景——性能、可扩展性、可靠性、可观测性、失败模式、边界与陷阱。
- **深入而不啰嗦**：深度体现在洞见与准确性，不是堆字数；必要处点到关键细节即可。

图/表/动画是为**加深理解**服务，不要为炫技而加；能一句话说清就不必画图。

## 写完之后回头砍一遍

初稿几乎总是偏长，而且长出来的部分有固定形状，照着找就行：

- **同一事实讲了三遍**：正文一遍、`.callout` 里一遍、面试答案里再一遍。
  正文留最清楚的那次，其余改成「见 §X」或直接删。
- **「历史背景 + 推理 + 结论」三段式**：多数时候读者只需要结论加判据。
  背景确实有解释力时才留（例如「`execve` 默认不关 fd 是 1970 年代的历史包袱，
  而且永远改不了」——这一句能解释为什么后来所有接口都提供 CLOEXEC 变体）。
- **图已经画清楚的段落**：删掉，不要图文各讲一遍。
- **编年史式罗列**：「2.6.23 加了 X，2.6.27 加了 Y，2.6.28 加了 Z」→
  「2.6.23–2.6.28 一批补齐」。

砍完对着 `report_page_density.py` 看一眼数字，别只凭手感。
