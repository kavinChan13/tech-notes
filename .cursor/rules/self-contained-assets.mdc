---
description: 页面自包含、复用共享资源、路径与语言约定
globs: **/*.html
alwaysApply: true
---

# 自包含与资源复用

- 页面应能直接双击打开即用，**不引入需要联网的第三方脚本/字体/CDN**；页面特有样式写进内联 `<style>`，
  通用样式复用 `assets/` 下现有 CSS，不要重复定义或另建平行样式文件。
- 资源用**相对路径**（按目录层级用 `../`），不要写绝对路径或本机路径。
- 面向读者的文案统一用**简体中文**；代码、标识符、术语原文按需保留英文。
- 交互脚本用原生 JS、保持轻量；避免为小功能引入框架或构建步骤。

## 第三方库一律落地到 `assets/vendor/`

曾有 37 个页面写 `<script src="https://cdn.jsdelivr.net/npm/...">` 引 mermaid / chart.js。
这类写法**离线时不报错、只是图表整块空白**，所以能一直躺着没人发现。现在两个库都已落地：

| 库 | 本地路径 | 版本 |
| --- | --- | --- |
| mermaid | `assets/vendor/mermaid.min.js` | 10.x（UMD，挂 `window.mermaid`，支持 `mermaid.run()`） |
| Chart.js | `assets/vendor/chart.umd.min.js` | 4.4.0（UMD，挂 `window.Chart`） |

- ❌ 禁止任何 `cdn.jsdelivr.net` / `unpkg.com` / `cdnjs` / `fonts.googleapis.com` 的
  `<script src>` 或 `<link href>`。`audit_page_shell.py` 会直接判失败。
- ✅ 需要 mermaid / chart.js 时按层级引本地副本：同目录下一层写 `../assets/vendor/…`，
  两层写 `../../assets/vendor/…`。**不要再引第二个版本**（历史上 `@10` 和 `@11` 混用过）。

### 大库不能阻塞渲染，也不能"引了不用"

mermaid 有 3.3 MB。曾经 28 个页面把它当普通 `<script src>` 放在 `<head>` 里，
浏览器解析 HTML 到那一行就停下来，先下载并执行完 3.3 MB 才继续——用户在这期间看到白屏。
另有 5 个页面根本没有图表，却照样加载了整个库。两条硬规则：

- **位置**：库脚本**不放 `<head>`**。两种合法写法，取决于初始化方式：
  - 初始化是**页面底部的内联脚本**（直接调 `mermaid.initialize()` / `new Chart()`）→
    把库标签移到**紧挨着那段内联脚本之前**。这里**不能加 `defer`**：内联脚本不会被延迟，
    会先执行并撞上 `mermaid is not defined`。
  - 初始化**等 `DOMContentLoaded`**（如 `assets/mermaid-theme.js`）→
    放 body 末尾并加 **`defer`**，下载与解析并行，最快。

## 🚨 mermaid 的配色必须走 `assets/mermaid-theme.js`

mermaid 的颜色是 **`initialize()` 时烘进 SVG 属性**的，之后再改 CSS 变量完全没用。
所以页面里写死一套配色，就等于把图钉死在一种主题上：`io_linux_kernel_guide.html`
写了 `theme:'dark'`，`software_management_guide.html` 写了一整套深色 `themeVariables`，
两者在**浅色模式下都是深底浅字**；反过来 `em-templates/shared.js` 写死 paper 浅色，
让 21 个模板页在**深色模式下露出一块白纸**。

统一做法（有图的页面，`mermaid.min.js` 之后立刻引入，两个都带 `defer`）：

```html
<script src="../assets/vendor/mermaid.min.js" defer></script>
<script src="../assets/mermaid-theme.js" defer></script>
```

页面要调 `flowchart`/`sequence`/`gantt` 参数，就在引入**之前**设
`window.TN_MERMAID_CONFIG`，会被合并进去。

- ❌ 页面里**不要**再调 `mermaid.initialize()`——两处初始化互相覆盖，谁最后跑谁生效。
- ❌ `startOnLoad: true` 也不要写，共享脚本用 `mermaid.run()` 自己控制时机。

### 🚨 切主题不能重新渲染

第一版是「监听 `data-theme` 变化 → 全部重画」。功能对，但代价大到不能用：
`io_linux_kernel_guide.html` 有 23 张图，实测**首屏 13.6 秒、每次切主题 4.4 秒**，
比别的页面慢一个数量级，用户一眼就能看出来。

现在的做法是**只渲染一次**，然后把 SVG 里烘死的颜色替换成 CSS 变量
（`fill:#f1f5f9` → `fill:var(--mmc-3)`），两套值由脚本注入的 `<style>` 按
`data-theme` 给。此后切主题是纯 CSS 的事：**4417 ms → 68 ms**。

配套的是**滚到视口附近才渲染**（`IntersectionObserver`，`rootMargin: 800px`），
首屏只画看得见的那张：**13.6 s → 3.9 s**。

两条推论：

- 颜色映射表 = `LIGHT` 与 `DARK` 两个 `themeVariables` 按键配对 + `SRC`（图元级配色）。
  **两个字典的键必须一一对应**，漏一个那个颜色就永远停在浅色值上。
- 任何测量 mermaid 图的脚本都要**先滚一遍页面**再采样，否则只看得到首屏那一张。
  `check_diagram_contrast.mjs` 已经这么做了。

### 图元级配色：源码一律写浅色

`style X fill:#…` / `classDef … fill:#…` 是 `themeVariables` 管不到的，而 mermaid 的
**解析器不接受 `fill:var(--x)`**（直接 Parse error），CSS 变量这条路走不通。
唯一可行的是渲染前改写源码文本，这件事由 `mermaid-theme.js` 的 `LIGHT_TO_DARK` 做。

- ✅ **mermaid 源码里一律写浅色值**（`#dbeafe` `#d1fae5` `#fef3c7` `#fee2e2` …，
  文字 `#111827`），深色由脚本映射回去。
- ❌ 不要在源码里写 `fill:#1a2234` 这类深色，也不要写 `color:#fff`——
  浅色模式下就是白字白底。
- 新增配色**成对**加进 `LIGHT_TO_DARK`，不要在页面里另发明一组。映射是**单向**的
  （浅→深），所以同一个 hex 不能同时出现在两侧。

### 自检必须真渲染

静态 grep 只能抓到 `theme:'dark'` 这一种写法，看不见 `themeVariables` 里逐项写死的
深色，更看不见最终 SVG 上到底是什么颜色。用：

```bash
node scripts/check_diagram_contrast.mjs <page.html> ...   # 或 --all
```

它在浅/深两种模式下真渲染，取每张图面积最大的填充色算相对亮度：
浅色模式下不该出现 < 0.25 的大块，深色模式下不该出现 > 0.75 的。
和 `check_mobile_layout.mjs` 一样需要 Playwright，**不在 CI 里**，属于提交前手动跑的那一档。
⚠️ `--all` 要跑 494 个页面 × 2 种主题，很慢；平时只传**含 `class="mermaid"` 的那些页面**
（当前 26 个）就够了。
- **按需**：页面里没有 `<pre class="mermaid">` 就不要引 mermaid，没有 `<canvas>` 就不要引 chart.js。

`audit_page_shell.py` 会同时校验这两条：`<head>` 里的非 defer 库标签、以及"引了但没用"。
- 要引入一个新的第三方库前先问一句：能不能用内联 SVG / 原生 JS 做掉？
  确实需要就下载到 `assets/vendor/`，并把库名、版本、用途补进上面这张表。
- 例外只有 `index.html` 里指向 GitHub 的 `<link rel="alternate" ... .atom>`：
  它是元数据、不影响渲染。
