---
description: 全文搜索索引的生成与使用约定（主页 Cmd+K 与目录页筛选）
globs: **/*.html
alwaysApply: true
---
# 全文搜索索引

全站搜索走**预生成的全文索引**，不是浏览器实时扫 HTML。索引由脚本生成，主页 Cmd+K
与各目录页页内筛选都复用它。

- **生成**：`python scripts/build_search_index.py`，输出 `assets/search-index.js`
  （定义 `window.TN_SEARCH_INDEX`）。每条记录含 `u`（根相对路径）、`t`（标题）、
  `c`（领域）、`s`（按带 `id` 的 `section`/`h1~h4` 切分的正文段落，每段有 `a` 锚点、
  `h` 小标题、`x` 纯文本），支持章节级跳转与摘要高亮。
- ⚠️ **必须用 `.js`，不要用 `.json`**：页面要能双击（`file://`）直接打开，
  而 `file://` 下 `fetch('*.json')` 会被 CORS 拦截；`<script src>` 赋值全局变量才可离线用。
- **索引范围**：排除 `*_directory.html`、`*_cards.html`（面试/卡片页）、以及任意 `index.html`
  （主页/模板脚手架）。新增/大改笔记页后**必须重跑**该脚本刷新 `search-index.js`，
  和 `audit_guides.py` 一样当作发布前自检。
- **主页** `index.html`：Cmd+K 逻辑读 `window.TN_SEARCH_INDEX`，匹配标题+正文，
  结果显示「标题 › 命中小节 + 摘要（`<mark>` 高亮）」，回车/点击跳到 `u#锚点`。
  高亮/摘要样式走主题变量（`--accent`/`--accent-soft`/`--ink-*`），不要硬编码颜色。
- **目录页** `*_directory.html`：不要再写内联过滤脚本。统一在 `</body>` 前按顺序引入
  `<script src="../assets/search-index.js">` + `<script src="../assets/directory-search.js">`。
  `directory-search.js` 会用「本目录名 + 卡片 href 文件名」定位索引，把该文件正文并入匹配，
  因此筛选**只覆盖当前目录的文件**；链到未索引页（如 `*_cards.html`）时自动退化为标题/`data-search` 匹配。
  DOM 约定保持：`#q` 输入框、`.item`（内含 `<a href>`）、可选 `.sec`、可选 `#empty`。
