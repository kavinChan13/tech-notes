---
description: 全文搜索索引的生成与使用约定（主页 Cmd+K 与目录页筛选）
globs: **/*.html
alwaysApply: true
---
# 全文搜索索引

全站搜索走**预生成的全文索引**，不是浏览器实时扫 HTML。索引由脚本生成，主页 Cmd+K
与各目录页页内筛选都复用它。

- **生成**：`python scripts/build_search_index.py`，输出**两层**：
  - `assets/search-index-lite.js` —— 定义 `window.TN_SEARCH_LITE`（全站标题 + 小节标题 + 锚点）
    和 `window.TN_SEARCH_TOPICS`（主题列表）。约 483 KB / gzip 149 KB。
  - `assets/search-index/<topic>.js` —— 每个内容目录一个全文分片，
    写进 `window.TN_SEARCH_SHARD['<topic>']`。19 个分片合计约 15 MB，
    最大的 `ai-native.js` 4.1 MB，最小的 `ai-templates.js` 22 KB。
- 每条记录含 `u`（根相对路径）、`t`（标题）、`c`（领域）、`s`（按带 `id` 的
  `section`/`h1~h4` 切分的段落，每段有 `a` 锚点、`h` 小标题、`x` 纯文本）。
  **两层的记录形状一致，只有轻索引的段落没有 `x`**，所以同一套渲染代码能吃两层；
  消费方读正文一律写 `sec.x || ''`，不要假设它存在。
- ⚠️ **必须用 `.js`，不要用 `.json`**：页面要能双击（`file://`）直接打开，
  而 `file://` 下 `fetch('*.json')` 会被 CORS 拦截；`<script src>` 赋值全局变量才可离线用。

## 🚨 为什么要分两层：正文占 94%，而没人需要全部正文

索引原本是**一个 14.7 MB（gzip 5.9 MB）的单体文件**，所有消费方都拉全量。
量一下就知道问题在哪：正文 `x` 占 payload 的 **94.1%**，而

- **16 个目录页的筛选只覆盖当前目录的文件**（6～186 篇），却要为此下载另外 18 个主题；
- **主页 Cmd+K 在拿到全部 5.9 MB 之前一个结果都不显示**，只能干等。

现在：目录页只拉自己那一片；主页先拉轻索引出结果，再在后台补全文。

- 🚨 **任何页面都不要直接 `<script src=".../search-index-lite.js">` 或
  `.../search-index/*.js`**。统一引入 **`assets/search-index-loader.js`**（约 4 KB），
  它在用户表现出搜索意图时才动态插入 `<script>`——用注入 `<script>` 而不是 `fetch()`，
  正是为了 `file://` 下也能用。`audit_page_shell.py` 会把直接引用判为失败。
- 加载器 API（都幂等，同一个文件只拉一次）：

| 调用 | 拉取 | 用在 |
| --- | --- | --- |
| `TNSearchIndex.loadLite(cb)` | 轻索引 | 首页首屏结果 |
| `TNSearchIndex.loadTopic(t, cb)` | 单个主题分片 | 目录页筛选 |
| `TNSearchIndex.load(cb)` | 轻索引 + 全部分片 | 首页全文升级 |
| `TNSearchIndex.lite()` / `.get()` / `.ready` | 已加载的数据 | 状态查询 |

  索引未就绪时消费方要能降级：主页显示「索引加载中…」，目录页退回标题/`data-search` 匹配，
  加载完成后再重跑一次筛选。

### ⚠️ 分层的两个反直觉点

**1. 不能用「记录数」判断手上是哪一层。** 两层都是**一页一条记录**（各 476 条），
所以 `if (idx.length >= INDEX.length)` 这种写法会让**晚到的轻索引把全文索引覆盖掉**，
搜索能力静默退化。首页用显式的 `INDEX_TIER`（1 = 轻，2 = 全文）比较。

**2. 「表现出搜索意图」要分两档。** 悬停搜索框、按 Cmd+K 打开面板只换来轻索引；
**全文分片要等真正的第一次按键**。否则按 Cmd+K 又按 Esc 的人照样白付 5.9 MB——
那正是分片要消掉的浪费。

- **索引范围**：排除 `*_directory.html`、`*_cards.html`（面试/卡片页）、任意 `index.html`，
  以及**仓库根目录下的页面**（`index.html` / `404.html` 都是外壳而非笔记；它们没有主题目录，
  索引进去会多出一个按兜底分类命名的 `其他.js` 分片，URL 还是非 ASCII 的）。
  新增/大改笔记页后**必须重跑**该脚本，和 `audit_guides.py` 一样当作发布前自检。
- 生成脚本每次会**重建整个分片目录**：某个主题没有可索引页面了，它的分片文件也要一起删掉，
  否则加载器会一直去拉一个已经对不上任何内容的旧文件。CI 除了比对内容，
  还会检查分片目录有没有未提交的增删。
- **主页** `index.html`：Cmd+K 匹配标题+小节标题+正文，
  结果显示「标题 › 命中小节 + 摘要（`<mark>` 高亮）」，回车/点击跳到 `u#锚点`。
  高亮/摘要样式走主题变量（`--accent`/`--accent-soft`/`--ink-*`），不要硬编码颜色。
- **目录页** `*_directory.html`：不要再写内联过滤脚本。统一在 `</body>` 前按顺序引入
  `<script src="../assets/search-index-loader.js">` + `<script src="../assets/directory-search.js">`。
  `directory-search.js` 从 URL 推断本目录名、只 `loadTopic()` 这一片，再用卡片 href
  定位记录，因此筛选**只覆盖当前目录的文件**；链到未索引页（如 `*_cards.html`）时
  自动退化为标题/`data-search` 匹配。
  DOM 约定保持：`#q` 输入框、`.item`（内含 `<a href>`）、可选 `.sec`、可选 `#empty`。
- 改动加载器后跑 `node scripts/test_search_index_loader.mjs`（在 CI 里）。
  它用 DOM shim 验证 URL 解析、只加载一次、回调必然触发、
  以及**目录页确实只请求自己那一个分片**——后者正是分片的全部意义，退化了要能立刻发现。
