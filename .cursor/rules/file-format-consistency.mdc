---
description: 新增/修改笔记页时保持严格一致的页面结构（导航栏、标题、主题、TOC 等）
globs: **/*.html
alwaysApply: true
---

# 页面格式一致性

新增或修改笔记页时，结构必须与同目录下的现有页面**严格保持一致**，不要自创版式。

- `<html lang="zh-CN">`；`<head>` 先放 `tn-theme-init` 内联脚本
  （`localStorage.getItem('theme')||localStorage.getItem('tn-theme')`，为 `dark` 时设 `data-theme="dark"`），
  按需引入 `assets/site-theme.css`、`assets/guide-theme.css` 等，页面自身内联 `<style>` 放在其后。
  **关键顺序**：`assets/site-tokens.css` 与 `assets/article.css` 必须是**最后加载的两个样式表**
  （放在页面内联 `<style>` 之后、`</head>` 之前，相对路径按层级用 `../`），这样浅色 token 的 `:root`
  和 `article.css` 的组件规则才能覆盖内联 `:root` 与内联硬编码，保证浅色为默认、组件随主题翻转。
- 页面自身的组件样式写进内联 `<style>`，且**只写页面特有的**；外壳（hero / layout / toc / chapter / 标题 / 代码 / 表格 / callout）
  由 `guide-theme.css` + `article.css` 提供。内联里颜色一律用变量（`--bg` `--bg-card` `--text` `--accent*` 等），
  不要硬编码深色；如遗留硬编码，运行 `python scripts/fix_inline_darks.py` 批量转成 token。
- `<title>` 用「主标题 · 关键点1 / 关键点2 / …」的格式，简明且能被搜索命中。
- 顶部导航用统一的**顶栏** `<nav class="tn-topnav">`（并在 `<body>` 加 `has-topnav` 类），包在
  `<!-- tn-nav:v2 -->` / `<!-- /tn-nav:v2 -->` 注释之间。结构：左侧品牌 `Kavin <b>Tech Notes</b>`，
  右侧依次 `← 主页`（`tn-home`，指根 `index.html`）/ `目录`（指本主题 `<topic>_directory.html`）/
  GitHub 图标 / 阅读时长 `tn-readmeta` / 明暗开关 `<button class="theme-btn" id="themeBtn">◐</button>`。
  从同目录现有页整块复制，仅改「目录」链接、阅读时长与相对层级；样式来自 `assets/article.css` 的 `.tn-topnav`。
- 明暗切换统一由 `assets/site-theme-toggle.js`（`</body>` 前引入）处理；**不要**在页内写切换脚本，
  也**不要**再引入 `assets/site-theme.js`（它会额外注入悬浮"主页"按钮，与顶栏重复）。
- 正文用 `.layout` + `aside.toc` + `main` 结构；TOC 锚点与章节 `id` 必须一一对应（见 anchors-and-audit 规则）。
- 页尾的相关导航统一用 `<div class="footer-nav">` + 若干 `<a>`（上一篇 / 下一篇 / 相关篇 / 目录 / 主页），
  放在最后一个 `</section>` 之后、`</main>` 之前。样式**已在 `assets/article.css` 第 3 节提供**：
  ❌ 不要在页面内联 `<style>` 里再定义 `.footer-nav`，也不要写死颜色；`.ps` 页由 `page-shell.css` 的
  `.ps .footer-nav` 接管（选择器更具体，会自动胜出），两边都不需要页面级改动。
- 章节滚动高亮（scroll-spy）的选择器必须**正向限定**为 `aside.toc nav a`。
  不要用裸 `nav a`，也不要用 `nav:not(.tn-topnav) a` 这种**反向排除**写法——
  它今天恰好能跑，但会顺带命中页面后来新增的任何 `<nav>`，
  而且会抓到导航里的外链，`document.querySelector(href)` 遇到非 `#` 开头的 href 直接抛异常。
- 复用现成的语义化类（`.hero` `.badge` `.pill` `.ch-head` `.analogy` `.def` `.demo`
  `.fig` `.vs` `.box` `.panel` `.callout` 等），不要重复造类名，**也不要在内联
  `<style>` 里重新定义它们**——这些类现在真的都在共享 CSS 里有实现了（见下）。
- `<head>` 里要有 `<meta name="description">`。写不出来就照 `<title>` 的
 「主标题 · 关键点」补一句；缺失时搜索引擎会自己从正文里胡乱截一段当摘要。
 这一条现在还有第二个用途：分享元数据是从它派生的（见下）。
- 只有图标没有文字的按钮（`↑`、`◐`、`[]` 这类）必须加 `aria-label`；
  有中文/英文字样的按钮（`重置`、`清空`）本身就是标签，不用加。
- 纯装饰性的内联 `<svg>` 加 `aria-hidden="true"`，否则屏幕阅读器会把图里
  所有零散文字念一遍。要表达信息的图请改用 `role="img"` + `aria-label`。

新建一页最稳妥的做法：复制同目录一篇结构最接近的页面作为骨架，再替换内容。

## 分享与规范地址：`tn-social:v1` 区块，不要手写

`<head>` 里 `<!-- tn-social:v1 -->` / `<!-- /tn-social:v1 -->` 之间那一段
（`canonical` / `favicon` / `theme-color` / `og:*` / `twitter:*`）**全部由脚本生成**：

```bash
python scripts/gen_social_meta.py           # 写入
python scripts/gen_social_meta.py --check   # CI 用，检查是否过期
```

以前只有根 `index.html` 有这些，**500 个笔记页一个都没有**。后果很具体：
往微信 / Slack / 飞书里贴任何一篇文章链接，都只出一条裸 URL、不出预览卡片；
搜索引擎也拿不到规范地址。

- 值全部从**已有的** `<title>` 和 `<meta name="description">` 派生，没有需要手维护的字段。
 所以这两个标签写好了，分享卡片就是对的。
- `og:title` 用的是**去掉 `— Tech Notes` 后缀的完整标题**。
 ⚠️ 不要改成「按第一个 `·` 截断」：站内 `·` 既作主副标题分隔符也作并列分隔符，
 `限流 · 熔断 · 降级 · 隔离 — 弹性与过载保护` 会被截成「限流」。
- 区块位置在 `<meta name="description">` 之后、第一个 `<link rel="stylesheet">` 之前，
 所以不影响 `site-tokens.css` + `article.css` 必须最后加载那条。
- `og:image` 用绝对 URL（爬虫不解析相对路径），favicon 用相对路径按层级 `../`。
 前者不算违反自包含规则：它是 `<meta content>`，不会被页面请求。
- 新增页面后跑一次生成脚本即可；**不要手写或手改这个区块**，下次生成会覆盖。

## 二级目录（TOC）是脚本生成的，不用手写

`aside.toc` 里只列章节（`h2`）时，`site-theme-toggle.js` 的 `buildTocSubnav()`
会从正文的 `main h3[id]` 自动补出二级项，并**只展开当前章节那一组**。
`management/management_knowledge_system.html` 用 18 个 TOC 链接覆盖 112 个小节，
读者想跳到 11.3 只能滚正文去找——这就是它要解决的问题。

- 页面侧**不需要任何改动**，但 `h3` 必须有 `id`。没有 `id` 就没有可链的锚点，
 脚本会跳过该章节。全站还有几页（`ai-native/trae_agent_architecture` 68 个 `h3`、
 `prompt_complete_guide` 46 个、`reinforcement/policy_gradient_ppo` 40 个）
 的 `h3` 完全没有 `id`，要先按 `s<N>-<M>` 约定补锚点才能生效。
- **本来就是两级 TOC 的页面（159 篇）脚本会自动跳过**，不会叠加。
- 折叠样式在 `article.css` §4b；外观沿用 `guide-theme.css` 已有的
 `aside.toc nav ol ol`，不要另写一套。

## 教学组件：`.analogy` / `.def` / `.demo` / `.fig` / `.vs`

这几个类现在**在 `assets/article.css` 第 3 节里有共享实现**，用 `:where()` 包着。
新页面直接写 `<div class="analogy">…</div>` 就有统一外观，**不要再抄一份内联样式**。

为什么用 `:where()`：`article.css` 是最后加载的，如果写成普通的 `.analogy{}`，
它会盖掉每个页面自己的内联版本，把几十个刻意做了差异化的页面一起改掉。
`:where()` 特异性为 0，页面级的 `.analogy` 照样能赢——所以它是「默认值」而不是「强制值」。
**给共享 CSS 加这类"补充默认样式"时都应该用 `:where()`。**

（背景：规则里一直写着「复用 `.analogy` / `.def`」，但共享 CSS 根本没定义过它们，
于是 99 个页面各写各的 `.analogy`，同一种卡片有 15 种长相。）

反过来，`.back-top` 在 `article.css` 里是带 `!important` 的完整实现——
页面里再写 `.back-top{…}` **完全不会生效**，纯属死代码，不要写。

## 收尾自检

`python scripts/audit_page_shell.py` 会机检本节里可机检的部分：
CDN 外链、`tn-theme-init`、toggle 是否单一、`site-tokens.css` + `article.css` 的加载顺序、
`tn-nav:v2` 标记与 `has-topnav` 是否配对、scroll-spy 选择器、内联深色硬编码、
以及是否直接引了 12 MB 的 `search-index.js`。全站已清零，出现任何一条都是回归。

⚠️ `<!-- tn-nav:v2 -->` / `<!-- /tn-nav:v2 -->` 这对注释**不是装饰**：
`theme_rollout.py`、`add_dir_button.py` 这类批量脚本靠它定位并重写顶栏。
14 个 `*_study_path.html` 曾经只有 `<nav class="tn-topnav">` 而没有标记，
结果每次批量改导航都被静默跳过。加顶栏就一定要连标记一起加。
