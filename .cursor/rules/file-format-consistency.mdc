---
description: 新增/修改笔记页时保持严格一致的页面结构（导航栏、标题、主题、TOC 等）
globs: **/*.html
alwaysApply: true
---

# 页面格式一致性

新增或修改笔记页时，结构必须与同目录下的现有页面**严格保持一致**，不要自创版式。

- `<html lang="zh-CN">`；`<head>` 先放 `tn-theme-init` 内联脚本
  （`localStorage.getItem('theme')||localStorage.getItem('tn-theme')`，为 `dark` 时设 `data-theme="dark"`），
  按需引入 `assets/site-theme.css`、`assets/guide-theme.css` 等，页面自身内联 `<style>` 放在其后。
  **关键顺序**：`assets/site-tokens.css` 与 `assets/article.css` 必须是**最后加载的两个样式表**
  （放在页面内联 `<style>` 之后、`</head>` 之前，相对路径按层级用 `../`），这样浅色 token 的 `:root`
  和 `article.css` 的组件规则才能覆盖内联 `:root` 与内联硬编码，保证浅色为默认、组件随主题翻转。
- 页面自身的组件样式写进内联 `<style>`，且**只写页面特有的**；外壳（hero / layout / toc / chapter / 标题 / 代码 / 表格 / callout）
  由 `guide-theme.css` + `article.css` 提供。内联里颜色一律用变量（`--bg` `--bg-card` `--text` `--accent*` 等），
  不要硬编码深色；如遗留硬编码，运行 `python scripts/fix_inline_darks.py` 批量转成 token。
- `<title>` 用「主标题 · 关键点1 / 关键点2 / …」的格式，简明且能被搜索命中。
- 顶部导航用统一的**顶栏** `<nav class="tn-topnav">`（并在 `<body>` 加 `has-topnav` 类），包在
  `<!-- tn-nav:v2 -->` / `<!-- /tn-nav:v2 -->` 注释之间。结构：左侧品牌 `Kavin <b>Tech Notes</b>`，
  右侧依次 `← 主页`（`tn-home`，指根 `index.html`）/ `目录`（指本主题 `<topic>_directory.html`）/
  GitHub 图标 / 阅读时长 `tn-readmeta` / 明暗开关 `<button class="theme-btn" id="themeBtn">◐</button>`。
  从同目录现有页整块复制，仅改「目录」链接、阅读时长与相对层级；样式来自 `assets/article.css` 的 `.tn-topnav`。
- 明暗切换统一由 `assets/site-theme-toggle.js`（`</body>` 前引入）处理；**不要**在页内写切换脚本，
  也**不要**再引入 `assets/site-theme.js`（它会额外注入悬浮"主页"按钮，与顶栏重复）。
- 正文用 `.layout` + `aside.toc` + `main` 结构；TOC 锚点与章节 `id` 必须一一对应（见 anchors-and-audit 规则）。
- 章节滚动高亮（scroll-spy）的选择器必须**限定在** `aside.toc nav a`，不要用裸 `nav a`
  （否则会误命中顶部导航栏，并在点击外链时因非法选择器报错）。
- 复用现成的语义化类（`.hero` `.badge` `.pill` `.ch-head` `.analogy` `.def` `.demo` `.box` `.panel` `.callout` 等），不要重复造类名。

新建一页最稳妥的做法：复制同目录一篇结构最接近的页面作为骨架，再替换内容。
