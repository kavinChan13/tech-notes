---
description: TOC 锚点完整性与 details 结构，改完跑 audit 脚本自检
globs: **/*.html
alwaysApply: true
---

# 锚点完整性与自检

笔记页靠 TOC + 章节 `id` + scroll-spy 联动，锚点错位会破坏导航体验。

- TOC 里每个 `href="#xxx"` 都要有对应的元素 `id="xxx"`；改动章节 id 时同步改 TOC。
- `<details>` / `<summary>` 必须成对闭合；`.interview-card` 的 `<details>` 内必须含 `<summary>`。
- scroll-spy 的选择器要与实际 DOM 一致（TOC 用 `.toc` 就别去查 `.sidebar a`）。

改完内容页后，运行审计脚本自检，确保 `Total problems: 0`：

```bash
python scripts/audit_guides.py
```

有问题先修复再交付。
