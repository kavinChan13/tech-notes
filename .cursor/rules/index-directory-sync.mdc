---
description: 增删改笔记文件时，同步更新主页与主题目录页
alwaysApply: true
---

# 主页 / 目录页同步

新增、重命名或删除笔记页后，检查并同步相关入口，别让链接漏挂或失效：

- 根目录 `index.html`：最新更新列表、精选卡片、统计数字等是否需要新增/调整条目。
- 对应主题的目录页 `<topic>_directory.html`（如 `bigdata/bigdata_directory.html`、
  `cpp/modern_cpp_stl_directory.html`）：是否需要新增/更新该页的入口链接与描述。
- 若涉及模板类页面，检查对应的 `*/templates/index.html`。

## 首页统计数字与计数（必须保持一致）

增删文章后，务必同步以下几处数字，别让它们僵在旧值：

- **分类卡片计数** `index.html` 里每张 `.domain-head` 的 `<span class="cnt">N 篇</span>`，
  以及对应目录页 `<topic>_directory.html` 头部 `.meta` 的「N 篇 / M 个子分区」——
  两处要与该主题实际文章数一致。
- **首页顶部统计块** `<dl class="stats">`（`站点统计`）四个数字：
  - `文章总数`：等于**所有分类卡片 `cnt` 徽章之和**（当前 13 个分类求和≈382，取整写 `380+`），
    用 `NNN+` 形式，别写死到个位。
  - `分类`：等于 `index.html` 里 `.domain-head h3` 的分类卡片数量（当前 **13**）。
  - `学习路径`：等于「学习路径」区 `#paths` 中 `.path` 卡片数量（当前 **11**，
    每张卡首项是指向 `*_study_path.html` 的 `.sp-link`）。
  - `最后更新`：改为当月 `YYYY-MM`。
- 校验口诀：改完后让「顶部 `文章总数` = 各分类 `cnt` 之和」「`分类` = 分类卡片数」
  「`学习路径` = `#paths` 卡片数」三者自洽，不确定时数一遍再写。

判断标准：只要新页面/改动会影响读者的浏览入口，就一并更新对应的主页或目录页；
不确定是否需要更新时，用 ask_question 与我确认，而不是默默跳过。
