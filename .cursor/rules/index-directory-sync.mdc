---
description: 增删改笔记文件时，同步更新主页与主题目录页
alwaysApply: true
---

# 主页 / 目录页同步

新增、重命名或删除笔记页后，检查并同步相关入口，别让链接漏挂或失效：

- 根目录 `index.html`：最新更新列表、精选卡片、统计数字等是否需要新增/调整条目。
- 对应主题的目录页 `<topic>_directory.html`（如 `bigdata/bigdata_directory.html`、
  `cpp/modern_cpp_stl_directory.html`）：是否需要新增/更新该页的入口链接与描述。
- 若涉及模板类页面，检查对应的 `*/templates/index.html`。

## 篇数计数：目录页的 `.item` 数是唯一真值

三处数字描述同一件事，以前各说各话（`ai-native` 首页写 186、目录页 meta 写 42、
实际 185 张卡）。现在统一口径，并由 `python scripts/audit_counts.py` 强制校验：

```
index.html 的 .domain-head > span.cnt  ==  <topic>_directory.html 的 .meta「N 篇」
                                       ==  该目录页实际的 .item 卡片数   ← 真值
```

用目录页的 `.item` 数作真值，是因为它就是读者真正能点进去的那份清单，
不会像磁盘文件数那样把 `_study_path.html`、模板页算进来，也骗不了人。

首页顶部 `<dl class="stats">` 三个数字由上面派生：

| 字段 | 定义 |
| --- | --- |
| `文章总数` | 所有 `.cnt` 徽章之和，向下取整到十位再加 `+`（如 426 → `420+`） |
| `分类` | `index.html` 里 `.domain-head h3` 的数量 |
| `学习路径` | `#paths` 区里 `.sp-link` 的数量 |
| `最后更新` | 当月 `YYYY-MM`（这一项脚本不管，手动改） |

**不要手算这些数字。** 增删文章后跑 `python scripts/audit_counts.py --fix`
自动改写首页 `cnt`、目录页 meta 与统计块，再不带参数跑一遍确认为 0。

`audit_counts.py` 同时会报：
- 有目录页却没有首页 `.domain` 卡的主题；
- 全站没有任何页面链接的**孤儿页**（`cpp/modern_cpp_stl_hub.html` 就是这么被发现并删除的）；
- 既不在 `#paths`、也不在自己目录页里的 `*_study_path.html`。

## 阅读时长：三处必须一致，而且要算出来不要拍脑袋

和篇数一样，同一个数字写在三个地方，很容易各说各话：

```
文章页顶栏 .tn-readmeta「N min」
 == 文章页 hero 的阅读时长（四种写法，见下）
 == <topic>_directory.html 该卡片的 <span>阅读：N 分钟</span>
 == index.html「最新更新」里指向该页那一行的 <span class="l-time">
```

⚠️ **hero 的写法有四种**，`audit_read_time.py` 里必须四种都在：

| 写法 | 页数量级 | 脚本里的常量 |
| --- | --- | --- |
| `阅读时长 ~N min` | ~165 | `PILL` |
| `阅读 ~N min` | ~255 | `PILL2` |
| `~N min 阅读` | ~21 | `META` |
| `<div class="meta">…<span>~N min</span>` | ~11 | `META_BLOCK` + `META_MIN` |

**新增页面时不要再发明第五种写法**；真要加就同步补进脚本，否则它会被静默跳过。

**不要手算，也不要沿用模板里抄来的值**，跑脚本：

```bash
python scripts/audit_read_time.py         # 报告不一致
python scripts/audit_read_time.py --fix   # 三处一并改写
```

估算口径（写在脚本里，改口径只改那两个常量）：

```
分钟 = 正文中文字符数 / 300 + <pre> 内字符数 / 600，四舍五入到 5 的倍数
```

正文中文字符 = 去掉 `<script>` `<style>` `<svg>` `<pre>` 之后剩下文本里的
`[\u4e00-\u9fff]` 个数；代码按「扫读」算，速率是正文的两倍。这两个常量是拿
**本来就设得合理的那几页**（`system/ipc_guide` 32、`process_scheduling` 40、
`cgroup_namespace_containers` 38）反推校准的，公式对它们的预测都在一个取整步之内。

⚠️ **这个数字曾经两个方向都错得离谱。** 全站 457 个带标注的页面，反推出来的
「等效字符/分钟」从 30 到 867 都有，跨度 29 倍：

- `process_scheduling` 标 40 分钟，实际只有 10 分钟的量（骨架页，章节全但没内容）；
- `interview/ai_system_design` 标 90 分钟，正文只有 3185 个中文字；
- 反过来，新写的几篇大页（内核网络栈、内核崩溃分析等）标 38–42 分钟，
 实际是 95–110 分钟的量——**它们一度是全站低估最严重的页面**。

全站已按上述口径统一过一遍（443 个文件，总阅读时长从虚高的 235 h 回到 156 h），
`audit_read_time.py` 已进 CI，再漂就会判失败。

### 🚨 只认识一半写法的修复脚本，比没有脚本更危险

第一版 `audit_read_time.py` 只写了 `PILL`（`阅读时长 ~N min`）和 `META`，漏了
占大头的 `PILL2`（`阅读 ~N min`，255 页）和 hero meta 行（11 页）。于是那次全站
`--fix` **把顶栏改对、把 hero 留在旧值**——它亲手制造了 266 页的不一致，
然后一本正经地打印 `Total problems: 0`。因为报告和改写用的是同一组正则，
**看不见的地方既不会被改，也不会被报**，这种缺陷完全无法靠「跑一遍自检」发现。

给所有 `--fix` 类脚本的通用要求：

- **先穷举真实写法再动手**。用宽松正则扫一遍全站、按形态分组计数，
 确认覆盖率之后再写替换逻辑；不要照着手头这一个页面的样子写死。
- **改写集必须等于检查集**。凡是脚本会改的地方都要能报，反之亦然；
 两者用同一组常量，不要一边硬编码一边走变量。
- **验证要看 diff，不要只看 `Total problems: 0`**。零可能意味着「全对」，
 也可能意味着「压根没看」。

学习路径页 `<topic>_study_path.html` 里的小时数是**另一回事**（阅读 + 动手练习），
脚本不管它；不必等于阅读时长之和，但量级上应当能对得上（练习一般是阅读的 2–4 倍）。

## 新增一个主题目录时

脚本侧**不需要注册**：`audit_*.py` 都从 `scripts/_site.py` 自动发现内容目录
（见 anchors-and-audit 规则）。唯一还需要手动加的一处是

- `scripts/build_search_index.py` 的 `CATEGORY`：加 `"<topic>": "中文分类名"`。
  漏掉不会报错，只会让 Cmd+K 结果里的分类标签退化成裸文件夹名。

内容侧要配齐 `<topic>_directory.html` 与 `<topic>_study_path.html` 两个页面，
并在首页新增一张 `.domain` 卡 + 一张 `#paths` 里的 `.path` 卡；
漏了哪一样 `audit_counts.py` 都会报出来。

## 站点地图

新增/删除页面后跑 `python scripts/build_sitemap.py`，它重新生成根目录的
`sitemap.xml`（485 条 URL，`<lastmod>` 取自该文件最后一次提交的日期，
而不是文件系统 mtime——否则一次 clone 就会把全站标成「今天全改过」）
和 `robots.txt`。CI 会校验它是否过期。

站点 URL 写死在脚本的 `BASE`（`https://kavinchan13.github.io/tech-notes/`）；
换域名时改这一处，并同步 `index.html` 里的 `canonical` 与 `og:*` 绝对地址。

## 新页不能只挂在目录页上

目录页和首页是**索引**，不是**上下文**。一个读者正在读 `networking_guide.html`，
不会想到「也许还有一篇讲内核收发包路径的」——他得先退回目录才可能发现。
新增页面后除了挂目录，还要从**内容上真正相关的既有页面**链过去：

- 在最容易产生疑问的那一节插一个 `.note` / `.box.tip`，说清**两篇的分工与判据**
 （例：「进程没了但机器还在 → 看本文；机器没了 → 看内核崩溃分析」），
 比在页脚塞一个链接有用得多；
- 同时在页尾 `.footer-nav`（或该页既有的链接式 `<footer>`）里补上。

自查：`audit_counts.py` 的孤儿检测只看「有没有任何页面链过来」，目录页链了就算过——
**它发现不了「只有目录页链了」这种半孤儿**。新增一批页面后手工数一遍来自
正文页的入链，每篇至少 2 个。`system/` 那 6 篇新页刚建好时来自正文的入链是 0，
就是这么漏过去的。

判断标准：只要新页面/改动会影响读者的浏览入口，就一并更新对应的主页或目录页；
不确定是否需要更新时，用 ask_question 与我确认，而不是默默跳过。
